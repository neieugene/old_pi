/*!
 * jQuery JavaScript Library v2.2.3
 * http://jquery.com/
 *
 * Includes Sizzle.js
 * http://sizzlejs.com/
 *
 * Copyright jQuery Foundation and other contributors
 * Released under the MIT license
 * http://jquery.org/license
 *
 * Date: 2016-04-05T19:26Z
 */


(function( global, factory ) {

	if ( typeof module === "object" && typeof module.exports === "object" ) {
		// For CommonJS and CommonJS-like environments where a proper `window`
		// is present, execute the factory and get jQuery.
		// For environments that do not have a `window` with a `document`
		// (such as Node.js), expose a factory as module.exports.
		// This accentuates the need for the creation of a real `window`.
		// e.g. var jQuery = require("jquery")(window);
		// See ticket #14549 for more info.
		module.exports = global.document ?
			factory( global, true ) :
			function( w ) {
				if ( !w.document ) {
					throw new Error( "jQuery requires a window with a document" );
				}
				return factory( w );
			};
	} else {
		factory( global );
	}

// Pass this if window is not defined yet
}(typeof window !== "undefined" ? window : this, function( window, noGlobal ) {

// Support: Firefox 18+
// Can't be in strict mode, several libs including ASP.NET trace
// the stack via arguments.caller.callee and Firefox dies if
// you try to trace through "use strict" call chains. (#13335)
//"use strict";
var arr = [];

var document = window.document;

var slice = arr.slice;

var concat = arr.concat;

var push = arr.push;

var indexOf = arr.indexOf;

var class2type = {};

var toString = class2type.toString;

var hasOwn = class2type.hasOwnProperty;

var support = {};



var
	version = "2.2.3",

	// Define a local copy of jQuery
	jQuery = function( selector, context ) {

		// The jQuery object is actually just the init constructor 'enhanced'
		// Need init if jQuery is called (just allow error to be thrown if not included)
		return new jQuery.fn.init( selector, context );
	},

	// Support: Android<4.1
	// Make sure we trim BOM and NBSP
	rtrim = /^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g,

	// Matches dashed string for camelizing
	rmsPrefix = /^-ms-/,
	rdashAlpha = /-([\da-z])/gi,

	// Used by jQuery.camelCase as callback to replace()
	fcamelCase = function( all, letter ) {
		return letter.toUpperCase();
	};

jQuery.fn = jQuery.prototype = {

	// The current version of jQuery being used
	jquery: version,

	constructor: jQuery,

	// Start with an empty selector
	selector: "",

	// The default length of a jQuery object is 0
	length: 0,

	toArray: function() {
		return slice.call( this );
	},

	// Get the Nth element in the matched element set OR
	// Get the whole matched element set as a clean array
	get: function( num ) {
		return num != null ?

			// Return just the one element from the set
			( num < 0 ? this[ num + this.length ] : this[ num ] ) :

			// Return all the elements in a clean array
			slice.call( this );
	},

	// Take an array of elements and push it onto the stack
	// (returning the new matched element set)
	pushStack: function( elems ) {

		// Build a new jQuery matched element set
		var ret = jQuery.merge( this.constructor(), elems );

		// Add the old object onto the stack (as a reference)
		ret.prevObject = this;
		ret.context = this.context;

		// Return the newly-formed element set
		return ret;
	},

	// Execute a callback for every element in the matched set.
	each: function( callback ) {
		return jQuery.each( this, callback );
	},

	map: function( callback ) {
		return this.pushStack( jQuery.map( this, function( elem, i ) {
			return callback.call( elem, i, elem );
		} ) );
	},

	slice: function() {
		return this.pushStack( slice.apply( this, arguments ) );
	},

	first: function() {
		return this.eq( 0 );
	},

	last: function() {
		return this.eq( -1 );
	},

	eq: function( i ) {
		var len = this.length,
			j = +i + ( i < 0 ? len : 0 );
		return this.pushStack( j >= 0 && j < len ? [ this[ j ] ] : [] );
	},

	end: function() {
		return this.prevObject || this.constructor();
	},

	// For internal use only.
	// Behaves like an Array's method, not like a jQuery method.
	push: push,
	sort: arr.sort,
	splice: arr.splice
};

jQuery.extend = jQuery.fn.extend = function() {
	var options, name, src, copy, copyIsArray, clone,
		target = arguments[ 0 ] || {},
		i = 1,
		length = arguments.length,
		deep = false;

	// Handle a deep copy situation
	if ( typeof target === "boolean" ) {
		deep = target;

		// Skip the boolean and the target
		target = arguments[ i ] || {};
		i++;
	}

	// Handle case when target is a string or something (possible in deep copy)
	if ( typeof target !== "object" && !jQuery.isFunction( target ) ) {
		target = {};
	}

	// Extend jQuery itself if only one argument is passed
	if ( i === length ) {
		target = this;
		i--;
	}

	for ( ; i < length; i++ ) {

		// Only deal with non-null/undefined values
		if ( ( options = arguments[ i ] ) != null ) {

			// Extend the base object
			for ( name in options ) {
				src = target[ name ];
				copy = options[ name ];

				// Prevent never-ending loop
				if ( target === copy ) {
					continue;
				}

				// Recurse if we're merging plain objects or arrays
				if ( deep && copy && ( jQuery.isPlainObject( copy ) ||
					( copyIsArray = jQuery.isArray( copy ) ) ) ) {

					if ( copyIsArray ) {
						copyIsArray = false;
						clone = src && jQuery.isArray( src ) ? src : [];

					} else {
						clone = src && jQuery.isPlainObject( src ) ? src : {};
					}

					// Never move original objects, clone them
					target[ name ] = jQuery.extend( deep, clone, copy );

				// Don't bring in undefined values
				} else if ( copy !== undefined ) {
					target[ name ] = copy;
				}
			}
		}
	}

	// Return the modified object
	return target;
};

jQuery.extend( {

	// Unique for each copy of jQuery on the page
	expando: "jQuery" + ( version + Math.random() ).replace( /\D/g, "" ),

	// Assume jQuery is ready without the ready module
	isReady: true,

	error: function( msg ) {
		throw new Error( msg );
	},

	noop: function() {},

	isFunction: function( obj ) {
		return jQuery.type( obj ) === "function";
	},

	isArray: Array.isArray,

	isWindow: function( obj ) {
		return obj != null && obj === obj.window;
	},

	isNumeric: function( obj ) {

		// parseFloat NaNs numeric-cast false positives (null|true|false|"")
		// ...but misinterprets leading-number strings, particularly hex literals ("0x...")
		// subtraction forces infinities to NaN
		// adding 1 corrects loss of precision from parseFloat (#15100)
		var realStringObj = obj && obj.toString();
		return !jQuery.isArray( obj ) && ( realStringObj - parseFloat( realStringObj ) + 1 ) >= 0;
	},

	isPlainObject: function( obj ) {
		var key;

		// Not plain objects:
		// - Any object or value whose internal [[Class]] property is not "[object Object]"
		// - DOM nodes
		// - window
		if ( jQuery.type( obj ) !== "object" || obj.nodeType || jQuery.isWindow( obj ) ) {
			return false;
		}

		// Not own constructor property must be Object
		if ( obj.constructor &&
				!hasOwn.call( obj, "constructor" ) &&
				!hasOwn.call( obj.constructor.prototype || {}, "isPrototypeOf" ) ) {
			return false;
		}

		// Own properties are enumerated firstly, so to speed up,
		// if last one is own, then all properties are own
		for ( key in obj ) {}

		return key === undefined || hasOwn.call( obj, key );
	},

	isEmptyObject: function( obj ) {
		var name;
		for ( name in obj ) {
			return false;
		}
		return true;
	},

	type: function( obj ) {
		if ( obj == null ) {
			return obj + "";
		}

		// Support: Android<4.0, iOS<6 (functionish RegExp)
		return typeof obj === "object" || typeof obj === "function" ?
			class2type[ toString.call( obj ) ] || "object" :
			typeof obj;
	},

	// Evaluates a script in a global context
	globalEval: function( code ) {
		var script,
			indirect = eval;

		code = jQuery.trim( code );

		if ( code ) {

			// If the code includes a valid, prologue position
			// strict mode pragma, execute code by injecting a
			// script tag into the document.
			if ( code.indexOf( "use strict" ) === 1 ) {
				script = document.createElement( "script" );
				script.text = code;
				document.head.appendChild( script ).parentNode.removeChild( script );
			} else {

				// Otherwise, avoid the DOM node creation, insertion
				// and removal by using an indirect global eval

				indirect( code );
			}
		}
	},

	// Convert dashed to camelCase; used by the css and data modules
	// Support: IE9-11+
	// Microsoft forgot to hump their vendor prefix (#9572)
	camelCase: function( string ) {
		return string.replace( rmsPrefix, "ms-" ).replace( rdashAlpha, fcamelCase );
	},

	nodeName: function( elem, name ) {
		return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();
	},

	each: function( obj, callback ) {
		var length, i = 0;

		if ( isArrayLike( obj ) ) {
			length = obj.length;
			for ( ; i < length; i++ ) {
				if ( callback.call( obj[ i ], i, obj[ i ] ) === false ) {
					break;
				}
			}
		} else {
			for ( i in obj ) {
				if ( callback.call( obj[ i ], i, obj[ i ] ) === false ) {
					break;
				}
			}
		}

		return obj;
	},

	// Support: Android<4.1
	trim: function( text ) {
		return text == null ?
			"" :
			( text + "" ).replace( rtrim, "" );
	},

	// results is for internal usage only
	makeArray: function( arr, results ) {
		var ret = results || [];

		if ( arr != null ) {
			if ( isArrayLike( Object( arr ) ) ) {
				jQuery.merge( ret,
					typeof arr === "string" ?
					[ arr ] : arr
				);
			} else {
				push.call( ret, arr );
			}
		}

		return ret;
	},

	inArray: function( elem, arr, i ) {
		return arr == null ? -1 : indexOf.call( arr, elem, i );
	},

	merge: function( first, second ) {
		var len = +second.length,
			j = 0,
			i = first.length;

		for ( ; j < len; j++ ) {
			first[ i++ ] = second[ j ];
		}

		first.length = i;

		return first;
	},

	grep: function( elems, callback, invert ) {
		var callbackInverse,
			matches = [],
			i = 0,
			length = elems.length,
			callbackExpect = !invert;

		// Go through the array, only saving the items
		// that pass the validator function
		for ( ; i < length; i++ ) {
			callbackInverse = !callback( elems[ i ], i );
			if ( callbackInverse !== callbackExpect ) {
				matches.push( elems[ i ] );
			}
		}

		return matches;
	},

	// arg is for internal usage only
	map: function( elems, callback, arg ) {
		var length, value,
			i = 0,
			ret = [];

		// Go through the array, translating each of the items to their new values
		if ( isArrayLike( elems ) ) {
			length = elems.length;
			for ( ; i < length; i++ ) {
				value = callback( elems[ i ], i, arg );

				if ( value != null ) {
					ret.push( value );
				}
			}

		// Go through every key on the object,
		} else {
			for ( i in elems ) {
				value = callback( elems[ i ], i, arg );

				if ( value != null ) {
					ret.push( value );
				}
			}
		}

		// Flatten any nested arrays
		return concat.apply( [], ret );
	},

	// A global GUID counter for objects
	guid: 1,

	// Bind a function to a context, optionally partially applying any
	// arguments.
	proxy: function( fn, context ) {
		var tmp, args, proxy;

		if ( typeof context === "string" ) {
			tmp = fn[ context ];
			context = fn;
			fn = tmp;
		}

		// Quick check to determine if target is callable, in the spec
		// this throws a TypeError, but we will just return undefined.
		if ( !jQuery.isFunction( fn ) ) {
			return undefined;
		}

		// Simulated bind
		args = slice.call( arguments, 2 );
		proxy = function() {
			return fn.apply( context || this, args.concat( slice.call( arguments ) ) );
		};

		// Set the guid of unique handler to the same of original handler, so it can be removed
		proxy.guid = fn.guid = fn.guid || jQuery.guid++;

		return proxy;
	},

	now: Date.now,

	// jQuery.support is not used in Core but other projects attach their
	// properties to it so it needs to exist.
	support: support
} );

// JSHint would error on this code due to the Symbol not being defined in ES5.
// Defining this global in .jshintrc would create a danger of using the global
// unguarded in another place, it seems safer to just disable JSHint for these
// three lines.
/* jshint ignore: start */
if ( typeof Symbol === "function" ) {
	jQuery.fn[ Symbol.iterator ] = arr[ Symbol.iterator ];
}
/* jshint ignore: end */

// Populate the class2type map
jQuery.each( "Boolean Number String Function Array Date RegExp Object Error Symbol".split( " " ),
function( i, name ) {
	class2type[ "[object " + name + "]" ] = name.toLowerCase();
} );

function isArrayLike( obj ) {

	// Support: iOS 8.2 (not reproducible in simulator)
	// `in` check used to prevent JIT error (gh-2145)
	// hasOwn isn't used here due to false negatives
	// regarding Nodelist length in IE
	var length = !!obj && "length" in obj && obj.length,
		type = jQuery.type( obj );

	if ( type === "function" || jQuery.isWindow( obj ) ) {
		return false;
	}

	return type === "array" || length === 0 ||
		typeof length === "number" && length > 0 && ( length - 1 ) in obj;
}
var Sizzle =
/*!
 * Sizzle CSS Selector Engine v2.2.1
 * http://sizzlejs.com/
 *
 * Copyright jQuery Foundation and other contributors
 * Released under the MIT license
 * http://jquery.org/license
 *
 * Date: 2015-10-17
 */
(function( window ) {

var i,
	support,
	Expr,
	getText,
	isXML,
	tokenize,
	compile,
	select,
	outermostContext,
	sortInput,
	hasDuplicate,

	// Local document vars
	setDocument,
	document,
	docElem,
	documentIsHTML,
	rbuggyQSA,
	rbuggyMatches,
	matches,
	contains,

	// Instance-specific data
	expando = "sizzle" + 1 * new Date(),
	preferredDoc = window.document,
	dirruns = 0,
	done = 0,
	classCache = createCache(),
	tokenCache = createCache(),
	compilerCache = createCache(),
	sortOrder = function( a, b ) {
		if ( a === b ) {
			hasDuplicate = true;
		}
		return 0;
	},

	// General-purpose constants
	MAX_NEGATIVE = 1 << 31,

	// Instance methods
	hasOwn = ({}).hasOwnProperty,
	arr = [],
	pop = arr.pop,
	push_native = arr.push,
	push = arr.push,
	slice = arr.slice,
	// Use a stripped-down indexOf as it's faster than native
	// http://jsperf.com/thor-indexof-vs-for/5
	indexOf = function( list, elem ) {
		var i = 0,
			len = list.length;
		for ( ; i < len; i++ ) {
			if ( list[i] === elem ) {
				return i;
			}
		}
		return -1;
	},

	booleans = "checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|loop|multiple|open|readonly|required|scoped",

	// Regular expressions

	// http://www.w3.org/TR/css3-selectors/#whitespace
	whitespace = "[\\x20\\t\\r\\n\\f]",

	// http://www.w3.org/TR/CSS21/syndata.html#value-def-identifier
	identifier = "(?:\\\\.|[\\w-]|[^\\x00-\\xa0])+",

	// Attribute selectors: http://www.w3.org/TR/selectors/#attribute-selectors
	attributes = "\\[" + whitespace + "*(" + identifier + ")(?:" + whitespace +
		// Operator (capture 2)
		"*([*^$|!~]?=)" + whitespace +
		// "Attribute values must be CSS identifiers [capture 5] or strings [capture 3 or capture 4]"
		"*(?:'((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\"|(" + identifier + "))|)" + whitespace +
		"*\\]",

	pseudos = ":(" + identifier + ")(?:\\((" +
		// To reduce the number of selectors needing tokenize in the preFilter, prefer arguments:
		// 1. quoted (capture 3; capture 4 or capture 5)
		"('((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\")|" +
		// 2. simple (capture 6)
		"((?:\\\\.|[^\\\\()[\\]]|" + attributes + ")*)|" +
		// 3. anything else (capture 2)
		".*" +
		")\\)|)",

	// Leading and non-escaped trailing whitespace, capturing some non-whitespace characters preceding the latter
	rwhitespace = new RegExp( whitespace + "+", "g" ),
	rtrim = new RegExp( "^" + whitespace + "+|((?:^|[^\\\\])(?:\\\\.)*)" + whitespace + "+$", "g" ),

	rcomma = new RegExp( "^" + whitespace + "*," + whitespace + "*" ),
	rcombinators = new RegExp( "^" + whitespace + "*([>+~]|" + whitespace + ")" + whitespace + "*" ),

	rattributeQuotes = new RegExp( "=" + whitespace + "*([^\\]'\"]*?)" + whitespace + "*\\]", "g" ),

	rpseudo = new RegExp( pseudos ),
	ridentifier = new RegExp( "^" + identifier + "$" ),

	matchExpr = {
		"ID": new RegExp( "^#(" + identifier + ")" ),
		"CLASS": new RegExp( "^\\.(" + identifier + ")" ),
		"TAG": new RegExp( "^(" + identifier + "|[*])" ),
		"ATTR": new RegExp( "^" + attributes ),
		"PSEUDO": new RegExp( "^" + pseudos ),
		"CHILD": new RegExp( "^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\(" + whitespace +
			"*(even|odd|(([+-]|)(\\d*)n|)" + whitespace + "*(?:([+-]|)" + whitespace +
			"*(\\d+)|))" + whitespace + "*\\)|)", "i" ),
		"bool": new RegExp( "^(?:" + booleans + ")$", "i" ),
		// For use in libraries implementing .is()
		// We use this for POS matching in `select`
		"needsContext": new RegExp( "^" + whitespace + "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\(" +
			whitespace + "*((?:-\\d)?\\d*)" + whitespace + "*\\)|)(?=[^-]|$)", "i" )
	},

	rinputs = /^(?:input|select|textarea|button)$/i,
	rheader = /^h\d$/i,

	rnative = /^[^{]+\{\s*\[native \w/,

	// Easily-parseable/retrievable ID or TAG or CLASS selectors
	rquickExpr = /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/,

	rsibling = /[+~]/,
	rescape = /'|\\/g,

	// CSS escapes http://www.w3.org/TR/CSS21/syndata.html#escaped-characters
	runescape = new RegExp( "\\\\([\\da-f]{1,6}" + whitespace + "?|(" + whitespace + ")|.)", "ig" ),
	funescape = function( _, escaped, escapedWhitespace ) {
		var high = "0x" + escaped - 0x10000;
		// NaN means non-codepoint
		// Support: Firefox<24
		// Workaround erroneous numeric interpretation of +"0x"
		return high !== high || escapedWhitespace ?
			escaped :
			high < 0 ?
				// BMP codepoint
				String.fromCharCode( high + 0x10000 ) :
				// Supplemental Plane codepoint (surrogate pair)
				String.fromCharCode( high >> 10 | 0xD800, high & 0x3FF | 0xDC00 );
	},

	// Used for iframes
	// See setDocument()
	// Removing the function wrapper causes a "Permission Denied"
	// error in IE
	unloadHandler = function() {
		setDocument();
	};

// Optimize for push.apply( _, NodeList )
try {
	push.apply(
		(arr = slice.call( preferredDoc.childNodes )),
		preferredDoc.childNodes
	);
	// Support: Android<4.0
	// Detect silently failing push.apply
	arr[ preferredDoc.childNodes.length ].nodeType;
} catch ( e ) {
	push = { apply: arr.length ?

		// Leverage slice if possible
		function( target, els ) {
			push_native.apply( target, slice.call(els) );
		} :

		// Support: IE<9
		// Otherwise append directly
		function( target, els ) {
			var j = target.length,
				i = 0;
			// Can't trust NodeList.length
			while ( (target[j++] = els[i++]) ) {}
			target.length = j - 1;
		}
	};
}

function Sizzle( selector, context, results, seed ) {
	var m, i, elem, nid, nidselect, match, groups, newSelector,
		newContext = context && context.ownerDocument,

		// nodeType defaults to 9, since context defaults to document
		nodeType = context ? context.nodeType : 9;

	results = results || [];

	// Return early from calls with invalid selector or context
	if ( typeof selector !== "string" || !selector ||
		nodeType !== 1 && nodeType !== 9 && nodeType !== 11 ) {

		return results;
	}

	// Try to shortcut find operations (as opposed to filters) in HTML documents
	if ( !seed ) {

		if ( ( context ? context.ownerDocument || context : preferredDoc ) !== document ) {
			setDocument( context );
		}
		context = context || document;

		if ( documentIsHTML ) {

			// If the selector is sufficiently simple, try using a "get*By*" DOM method
			// (excepting DocumentFragment context, where the methods don't exist)
			if ( nodeType !== 11 && (match = rquickExpr.exec( selector )) ) {

				// ID selector
				if ( (m = match[1]) ) {

					// Document context
					if ( nodeType === 9 ) {
						if ( (elem = context.getElementById( m )) ) {

							// Support: IE, Opera, Webkit
							// TODO: identify versions
							// getElementById can match elements by name instead of ID
							if ( elem.id === m ) {
								results.push( elem );
								return results;
							}
						} else {
							return results;
						}

					// Element context
					} else {

						// Support: IE, Opera, Webkit
						// TODO: identify versions
						// getElementById can match elements by name instead of ID
						if ( newContext && (elem = newContext.getElementById( m )) &&
							contains( context, elem ) &&
							elem.id === m ) {

							results.push( elem );
							return results;
						}
					}

				// Type selector
				} else if ( match[2] ) {
					push.apply( results, context.getElementsByTagName( selector ) );
					return results;

				// Class selector
				} else if ( (m = match[3]) && support.getElementsByClassName &&
					context.getElementsByClassName ) {

					push.apply( results, context.getElementsByClassName( m ) );
					return results;
				}
			}

			// Take advantage of querySelectorAll
			if ( support.qsa &&
				!compilerCache[ selector + " " ] &&
				(!rbuggyQSA || !rbuggyQSA.test( selector )) ) {

				if ( nodeType !== 1 ) {
					newContext = context;
					newSelector = selector;

				// qSA looks outside Element context, which is not what we want
				// Thanks to Andrew Dupont for this workaround technique
				// Support: IE <=8
				// Exclude object elements
				} else if ( context.nodeName.toLowerCase() !== "object" ) {

					// Capture the context ID, setting it first if necessary
					if ( (nid = context.getAttribute( "id" )) ) {
						nid = nid.replace( rescape, "\\$&" );
					} else {
						context.setAttribute( "id", (nid = expando) );
					}

					// Prefix every selector in the list
					groups = tokenize( selector );
					i = groups.length;
					nidselect = ridentifier.test( nid ) ? "#" + nid : "[id='" + nid + "']";
					while ( i-- ) {
						groups[i] = nidselect + " " + toSelector( groups[i] );
					}
					newSelector = groups.join( "," );

					// Expand context for sibling selectors
					newContext = rsibling.test( selector ) && testContext( context.parentNode ) ||
						context;
				}

				if ( newSelector ) {
					try {
						push.apply( results,
							newContext.querySelectorAll( newSelector )
						);
						return results;
					} catch ( qsaError ) {
					} finally {
						if ( nid === expando ) {
							context.removeAttribute( "id" );
						}
					}
				}
			}
		}
	}

	// All others
	return select( selector.replace( rtrim, "$1" ), context, results, seed );
}

/**
 * Create key-value caches of limited size
 * @returns {function(string, object)} Returns the Object data after storing it on itself with
 *	property name the (space-suffixed) string and (if the cache is larger than Expr.cacheLength)
 *	deleting the oldest entry
 */
function createCache() {
	var keys = [];

	function cache( key, value ) {
		// Use (key + " ") to avoid collision with native prototype properties (see Issue #157)
		if ( keys.push( key + " " ) > Expr.cacheLength ) {
			// Only keep the most recent entries
			delete cache[ keys.shift() ];
		}
		return (cache[ key + " " ] = value);
	}
	return cache;
}

/**
 * Mark a function for special use by Sizzle
 * @param {Function} fn The function to mark
 */
function markFunction( fn ) {
	fn[ expando ] = true;
	return fn;
}

/**
 * Support testing using an element
 * @param {Function} fn Passed the created div and expects a boolean result
 */
function assert( fn ) {
	var div = document.createElement("div");

	try {
		return !!fn( div );
	} catch (e) {
		return false;
	} finally {
		// Remove from its parent by default
		if ( div.parentNode ) {
			div.parentNode.removeChild( div );
		}
		// release memory in IE
		div = null;
	}
}

/**
 * Adds the same handler for all of the specified attrs
 * @param {String} attrs Pipe-separated list of attributes
 * @param {Function} handler The method that will be applied
 */
function addHandle( attrs, handler ) {
	var arr = attrs.split("|"),
		i = arr.length;

	while ( i-- ) {
		Expr.attrHandle[ arr[i] ] = handler;
	}
}

/**
 * Checks document order of two siblings
 * @param {Element} a
 * @param {Element} b
 * @returns {Number} Returns less than 0 if a precedes b, greater than 0 if a follows b
 */
function siblingCheck( a, b ) {
	var cur = b && a,
		diff = cur && a.nodeType === 1 && b.nodeType === 1 &&
			( ~b.sourceIndex || MAX_NEGATIVE ) -
			( ~a.sourceIndex || MAX_NEGATIVE );

	// Use IE sourceIndex if available on both nodes
	if ( diff ) {
		return diff;
	}

	// Check if b follows a
	if ( cur ) {
		while ( (cur = cur.nextSibling) ) {
			if ( cur === b ) {
				return -1;
			}
		}
	}

	return a ? 1 : -1;
}

/**
 * Returns a function to use in pseudos for input types
 * @param {String} type
 */
function createInputPseudo( type ) {
	return function( elem ) {
		var name = elem.nodeName.toLowerCase();
		return name === "input" && elem.type === type;
	};
}

/**
 * Returns a function to use in pseudos for buttons
 * @param {String} type
 */
function createButtonPseudo( type ) {
	return function( elem ) {
		var name = elem.nodeName.toLowerCase();
		return (name === "input" || name === "button") && elem.type === type;
	};
}

/**
 * Returns a function to use in pseudos for positionals
 * @param {Function} fn
 */
function createPositionalPseudo( fn ) {
	return markFunction(function( argument ) {
		argument = +argument;
		return markFunction(function( seed, matches ) {
			var j,
				matchIndexes = fn( [], seed.length, argument ),
				i = matchIndexes.length;

			// Match elements found at the specified indexes
			while ( i-- ) {
				if ( seed[ (j = matchIndexes[i]) ] ) {
					seed[j] = !(matches[j] = seed[j]);
				}
			}
		});
	});
}

/**
 * Checks a node for validity as a Sizzle context
 * @param {Element|Object=} context
 * @returns {Element|Object|Boolean} The input node if acceptable, otherwise a falsy value
 */
function testContext( context ) {
	return context && typeof context.getElementsByTagName !== "undefined" && context;
}

// Expose support vars for convenience
support = Sizzle.support = {};

/**
 * Detects XML nodes
 * @param {Element|Object} elem An element or a document
 * @returns {Boolean} True iff elem is a non-HTML XML node
 */
isXML = Sizzle.isXML = function( elem ) {
	// documentElement is verified for cases where it doesn't yet exist
	// (such as loading iframes in IE - #4833)
	var documentElement = elem && (elem.ownerDocument || elem).documentElement;
	return documentElement ? documentElement.nodeName !== "HTML" : false;
};

/**
 * Sets document-related variables once based on the current document
 * @param {Element|Object} [doc] An element or document object to use to set the document
 * @returns {Object} Returns the current document
 */
setDocument = Sizzle.setDocument = function( node ) {
	var hasCompare, parent,
		doc = node ? node.ownerDocument || node : preferredDoc;

	// Return early if doc is invalid or already selected
	if ( doc === document || doc.nodeType !== 9 || !doc.documentElement ) {
		return document;
	}

	// Update global variables
	document = doc;
	docElem = document.documentElement;
	documentIsHTML = !isXML( document );

	// Support: IE 9-11, Edge
	// Accessing iframe documents after unload throws "permission denied" errors (jQuery #13936)
	if ( (parent = document.defaultView) && parent.top !== parent ) {
		// Support: IE 11
		if ( parent.addEventListener ) {
			parent.addEventListener( "unload", unloadHandler, false );

		// Support: IE 9 - 10 only
		} else if ( parent.attachEvent ) {
			parent.attachEvent( "onunload", unloadHandler );
		}
	}

	/* Attributes
	---------------------------------------------------------------------- */

	// Support: IE<8
	// Verify that getAttribute really returns attributes and not properties
	// (excepting IE8 booleans)
	support.attributes = assert(function( div ) {
		div.className = "i";
		return !div.getAttribute("className");
	});

	/* getElement(s)By*
	---------------------------------------------------------------------- */

	// Check if getElementsByTagName("*") returns only elements
	support.getElementsByTagName = assert(function( div ) {
		div.appendChild( document.createComment("") );
		return !div.getElementsByTagName("*").length;
	});

	// Support: IE<9
	support.getElementsByClassName = rnative.test( document.getElementsByClassName );

	// Support: IE<10
	// Check if getElementById returns elements by name
	// The broken getElementById methods don't pick up programatically-set names,
	// so use a roundabout getElementsByName test
	support.getById = assert(function( div ) {
		docElem.appendChild( div ).id = expando;
		return !document.getElementsByName || !document.getElementsByName( expando ).length;
	});

	// ID find and filter
	if ( support.getById ) {
		Expr.find["ID"] = function( id, context ) {
			if ( typeof context.getElementById !== "undefined" && documentIsHTML ) {
				var m = context.getElementById( id );
				return m ? [ m ] : [];
			}
		};
		Expr.filter["ID"] = function( id ) {
			var attrId = id.replace( runescape, funescape );
			return function( elem ) {
				return elem.getAttribute("id") === attrId;
			};
		};
	} else {
		// Support: IE6/7
		// getElementById is not reliable as a find shortcut
		delete Expr.find["ID"];

		Expr.filter["ID"] =  function( id ) {
			var attrId = id.replace( runescape, funescape );
			return function( elem ) {
				var node = typeof elem.getAttributeNode !== "undefined" &&
					elem.getAttributeNode("id");
				return node && node.value === attrId;
			};
		};
	}

	// Tag
	Expr.find["TAG"] = support.getElementsByTagName ?
		function( tag, context ) {
			if ( typeof context.getElementsByTagName !== "undefined" ) {
				return context.getElementsByTagName( tag );

			// DocumentFragment nodes don't have gEBTN
			} else if ( support.qsa ) {
				return context.querySelectorAll( tag );
			}
		} :

		function( tag, context ) {
			var elem,
				tmp = [],
				i = 0,
				// By happy coincidence, a (broken) gEBTN appears on DocumentFragment nodes too
				results = context.getElementsByTagName( tag );

			// Filter out possible comments
			if ( tag === "*" ) {
				while ( (elem = results[i++]) ) {
					if ( elem.nodeType === 1 ) {
						tmp.push( elem );
					}
				}

				return tmp;
			}
			return results;
		};

	// Class
	Expr.find["CLASS"] = support.getElementsByClassName && function( className, context ) {
		if ( typeof context.getElementsByClassName !== "undefined" && documentIsHTML ) {
			return context.getElementsByClassName( className );
		}
	};

	/* QSA/matchesSelector
	---------------------------------------------------------------------- */

	// QSA and matchesSelector support

	// matchesSelector(:active) reports false when true (IE9/Opera 11.5)
	rbuggyMatches = [];

	// qSa(:focus) reports false when true (Chrome 21)
	// We allow this because of a bug in IE8/9 that throws an error
	// whenever `document.activeElement` is accessed on an iframe
	// So, we allow :focus to pass through QSA all the time to avoid the IE error
	// See http://bugs.jquery.com/ticket/13378
	rbuggyQSA = [];

	if ( (support.qsa = rnative.test( document.querySelectorAll )) ) {
		// Build QSA regex
		// Regex strategy adopted from Diego Perini
		assert(function( div ) {
			// Select is set to empty string on purpose
			// This is to test IE's treatment of not explicitly
			// setting a boolean content attribute,
			// since its presence should be enough
			// http://bugs.jquery.com/ticket/12359
			docElem.appendChild( div ).innerHTML = "<a id='" + expando + "'></a>" +
				"<select id='" + expando + "-\r\\' msallowcapture=''>" +
				"<option selected=''></option></select>";

			// Support: IE8, Opera 11-12.16
			// Nothing should be selected when empty strings follow ^= or $= or *=
			// The test attribute must be unknown in Opera but "safe" for WinRT
			// http://msdn.microsoft.com/en-us/library/ie/hh465388.aspx#attribute_section
			if ( div.querySelectorAll("[msallowcapture^='']").length ) {
				rbuggyQSA.push( "[*^$]=" + whitespace + "*(?:''|\"\")" );
			}

			// Support: IE8
			// Boolean attributes and "value" are not treated correctly
			if ( !div.querySelectorAll("[selected]").length ) {
				rbuggyQSA.push( "\\[" + whitespace + "*(?:value|" + booleans + ")" );
			}

			// Support: Chrome<29, Android<4.4, Safari<7.0+, iOS<7.0+, PhantomJS<1.9.8+
			if ( !div.querySelectorAll( "[id~=" + expando + "-]" ).length ) {
				rbuggyQSA.push("~=");
			}

			// Webkit/Opera - :checked should return selected option elements
			// http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked
			// IE8 throws error here and will not see later tests
			if ( !div.querySelectorAll(":checked").length ) {
				rbuggyQSA.push(":checked");
			}

			// Support: Safari 8+, iOS 8+
			// https://bugs.webkit.org/show_bug.cgi?id=136851
			// In-page `selector#id sibing-combinator selector` fails
			if ( !div.querySelectorAll( "a#" + expando + "+*" ).length ) {
				rbuggyQSA.push(".#.+[+~]");
			}
		});

		assert(function( div ) {
			// Support: Windows 8 Native Apps
			// The type and name attributes are restricted during .innerHTML assignment
			var input = document.createElement("input");
			input.setAttribute( "type", "hidden" );
			div.appendChild( input ).setAttribute( "name", "D" );

			// Support: IE8
			// Enforce case-sensitivity of name attribute
			if ( div.querySelectorAll("[name=d]").length ) {
				rbuggyQSA.push( "name" + whitespace + "*[*^$|!~]?=" );
			}

			// FF 3.5 - :enabled/:disabled and hidden elements (hidden elements are still enabled)
			// IE8 throws error here and will not see later tests
			if ( !div.querySelectorAll(":enabled").length ) {
				rbuggyQSA.push( ":enabled", ":disabled" );
			}

			// Opera 10-11 does not throw on post-comma invalid pseudos
			div.querySelectorAll("*,:x");
			rbuggyQSA.push(",.*:");
		});
	}

	if ( (support.matchesSelector = rnative.test( (matches = docElem.matches ||
		docElem.webkitMatchesSelector ||
		docElem.mozMatchesSelector ||
		docElem.oMatchesSelector ||
		docElem.msMatchesSelector) )) ) {

		assert(function( div ) {
			// Check to see if it's possible to do matchesSelector
			// on a disconnected node (IE 9)
			support.disconnectedMatch = matches.call( div, "div" );

			// This should fail with an exception
			// Gecko does not error, returns false instead
			matches.call( div, "[s!='']:x" );
			rbuggyMatches.push( "!=", pseudos );
		});
	}

	rbuggyQSA = rbuggyQSA.length && new RegExp( rbuggyQSA.join("|") );
	rbuggyMatches = rbuggyMatches.length && new RegExp( rbuggyMatches.join("|") );

	/* Contains
	---------------------------------------------------------------------- */
	hasCompare = rnative.test( docElem.compareDocumentPosition );

	// Element contains another
	// Purposefully self-exclusive
	// As in, an element does not contain itself
	contains = hasCompare || rnative.test( docElem.contains ) ?
		function( a, b ) {
			var adown = a.nodeType === 9 ? a.documentElement : a,
				bup = b && b.parentNode;
			return a === bup || !!( bup && bup.nodeType === 1 && (
				adown.contains ?
					adown.contains( bup ) :
					a.compareDocumentPosition && a.compareDocumentPosition( bup ) & 16
			));
		} :
		function( a, b ) {
			if ( b ) {
				while ( (b = b.parentNode) ) {
					if ( b === a ) {
						return true;
					}
				}
			}
			return false;
		};

	/* Sorting
	---------------------------------------------------------------------- */

	// Document order sorting
	sortOrder = hasCompare ?
	function( a, b ) {

		// Flag for duplicate removal
		if ( a === b ) {
			hasDuplicate = true;
			return 0;
		}

		// Sort on method existence if only one input has compareDocumentPosition
		var compare = !a.compareDocumentPosition - !b.compareDocumentPosition;
		if ( compare ) {
			return compare;
		}

		// Calculate position if both inputs belong to the same document
		compare = ( a.ownerDocument || a ) === ( b.ownerDocument || b ) ?
			a.compareDocumentPosition( b ) :

			// Otherwise we know they are disconnected
			1;

		// Disconnected nodes
		if ( compare & 1 ||
			(!support.sortDetached && b.compareDocumentPosition( a ) === compare) ) {

			// Choose the first element that is related to our preferred document
			if ( a === document || a.ownerDocument === preferredDoc && contains(preferredDoc, a) ) {
				return -1;
			}
			if ( b === document || b.ownerDocument === preferredDoc && contains(preferredDoc, b) ) {
				return 1;
			}

			// Maintain original order
			return sortInput ?
				( indexOf( sortInput, a ) - indexOf( sortInput, b ) ) :
				0;
		}

		return compare & 4 ? -1 : 1;
	} :
	function( a, b ) {
		// Exit early if the nodes are identical
		if ( a === b ) {
			hasDuplicate = true;
			return 0;
		}

		var cur,
			i = 0,
			aup = a.parentNode,
			bup = b.parentNode,
			ap = [ a ],
			bp = [ b ];

		// Parentless nodes are either documents or disconnected
		if ( !aup || !bup ) {
			return a === document ? -1 :
				b === document ? 1 :
				aup ? -1 :
				bup ? 1 :
				sortInput ?
				( indexOf( sortInput, a ) - indexOf( sortInput, b ) ) :
				0;

		// If the nodes are siblings, we can do a quick check
		} else if ( aup === bup ) {
			return siblingCheck( a, b );
		}

		// Otherwise we need full lists of their ancestors for comparison
		cur = a;
		while ( (cur = cur.parentNode) ) {
			ap.unshift( cur );
		}
		cur = b;
		while ( (cur = cur.parentNode) ) {
			bp.unshift( cur );
		}

		// Walk down the tree looking for a discrepancy
		while ( ap[i] === bp[i] ) {
			i++;
		}

		return i ?
			// Do a sibling check if the nodes have a common ancestor
			siblingCheck( ap[i], bp[i] ) :

			// Otherwise nodes in our document sort first
			ap[i] === preferredDoc ? -1 :
			bp[i] === preferredDoc ? 1 :
			0;
	};

	return document;
};

Sizzle.matches = function( expr, elements ) {
	return Sizzle( expr, null, null, elements );
};

Sizzle.matchesSelector = function( elem, expr ) {
	// Set document vars if needed
	if ( ( elem.ownerDocument || elem ) !== document ) {
		setDocument( elem );
	}

	// Make sure that attribute selectors are quoted
	expr = expr.replace( rattributeQuotes, "='$1']" );

	if ( support.matchesSelector && documentIsHTML &&
		!compilerCache[ expr + " " ] &&
		( !rbuggyMatches || !rbuggyMatches.test( expr ) ) &&
		( !rbuggyQSA     || !rbuggyQSA.test( expr ) ) ) {

		try {
			var ret = matches.call( elem, expr );

			// IE 9's matchesSelector returns false on disconnected nodes
			if ( ret || support.disconnectedMatch ||
					// As well, disconnected nodes are said to be in a document
					// fragment in IE 9
					elem.document && elem.document.nodeType !== 11 ) {
				return ret;
			}
		} catch (e) {}
	}

	return Sizzle( expr, document, null, [ elem ] ).length > 0;
};

Sizzle.contains = function( context, elem ) {
	// Set document vars if needed
	if ( ( context.ownerDocument || context ) !== document ) {
		setDocument( context );
	}
	return contains( context, elem );
};

Sizzle.attr = function( elem, name ) {
	// Set document vars if needed
	if ( ( elem.ownerDocument || elem ) !== document ) {
		setDocument( elem );
	}

	var fn = Expr.attrHandle[ name.toLowerCase() ],
		// Don't get fooled by Object.prototype properties (jQuery #13807)
		val = fn && hasOwn.call( Expr.attrHandle, name.toLowerCase() ) ?
			fn( elem, name, !documentIsHTML ) :
			undefined;

	return val !== undefined ?
		val :
		support.attributes || !documentIsHTML ?
			elem.getAttribute( name ) :
			(val = elem.getAttributeNode(name)) && val.specified ?
				val.value :
				null;
};

Sizzle.error = function( msg ) {
	throw new Error( "Syntax error, unrecognized expression: " + msg );
};

/**
 * Document sorting and removing duplicates
 * @param {ArrayLike} results
 */
Sizzle.uniqueSort = function( results ) {
	var elem,
		duplicates = [],
		j = 0,
		i = 0;

	// Unless we *know* we can detect duplicates, assume their presence
	hasDuplicate = !support.detectDuplicates;
	sortInput = !support.sortStable && results.slice( 0 );
	results.sort( sortOrder );

	if ( hasDuplicate ) {
		while ( (elem = results[i++]) ) {
			if ( elem === results[ i ] ) {
				j = duplicates.push( i );
			}
		}
		while ( j-- ) {
			results.splice( duplicates[ j ], 1 );
		}
	}

	// Clear input after sorting to release objects
	// See https://github.com/jquery/sizzle/pull/225
	sortInput = null;

	return results;
};

/**
 * Utility function for retrieving the text value of an array of DOM nodes
 * @param {Array|Element} elem
 */
getText = Sizzle.getText = function( elem ) {
	var node,
		ret = "",
		i = 0,
		nodeType = elem.nodeType;

	if ( !nodeType ) {
		// If no nodeType, this is expected to be an array
		while ( (node = elem[i++]) ) {
			// Do not traverse comment nodes
			ret += getText( node );
		}
	} else if ( nodeType === 1 || nodeType === 9 || nodeType === 11 ) {
		// Use textContent for elements
		// innerText usage removed for consistency of new lines (jQuery #11153)
		if ( typeof elem.textContent === "string" ) {
			return elem.textContent;
		} else {
			// Traverse its children
			for ( elem = elem.firstChild; elem; elem = elem.nextSibling ) {
				ret += getText( elem );
			}
		}
	} else if ( nodeType === 3 || nodeType === 4 ) {
		return elem.nodeValue;
	}
	// Do not include comment or processing instruction nodes

	return ret;
};

Expr = Sizzle.selectors = {

	// Can be adjusted by the user
	cacheLength: 50,

	createPseudo: markFunction,

	match: matchExpr,

	attrHandle: {},

	find: {},

	relative: {
		">": { dir: "parentNode", first: true },
		" ": { dir: "parentNode" },
		"+": { dir: "previousSibling", first: true },
		"~": { dir: "previousSibling" }
	},

	preFilter: {
		"ATTR": function( match ) {
			match[1] = match[1].replace( runescape, funescape );

			// Move the given value to match[3] whether quoted or unquoted
			match[3] = ( match[3] || match[4] || match[5] || "" ).replace( runescape, funescape );

			if ( match[2] === "~=" ) {
				match[3] = " " + match[3] + " ";
			}

			return match.slice( 0, 4 );
		},

		"CHILD": function( match ) {
			/* matches from matchExpr["CHILD"]
				1 type (only|nth|...)
				2 what (child|of-type)
				3 argument (even|odd|\d*|\d*n([+-]\d+)?|...)
				4 xn-component of xn+y argument ([+-]?\d*n|)
				5 sign of xn-component
				6 x of xn-component
				7 sign of y-component
				8 y of y-component
			*/
			match[1] = match[1].toLowerCase();

			if ( match[1].slice( 0, 3 ) === "nth" ) {
				// nth-* requires argument
				if ( !match[3] ) {
					Sizzle.error( match[0] );
				}

				// numeric x and y parameters for Expr.filter.CHILD
				// remember that false/true cast respectively to 0/1
				match[4] = +( match[4] ? match[5] + (match[6] || 1) : 2 * ( match[3] === "even" || match[3] === "odd" ) );
				match[5] = +( ( match[7] + match[8] ) || match[3] === "odd" );

			// other types prohibit arguments
			} else if ( match[3] ) {
				Sizzle.error( match[0] );
			}

			return match;
		},

		"PSEUDO": function( match ) {
			var excess,
				unquoted = !match[6] && match[2];

			if ( matchExpr["CHILD"].test( match[0] ) ) {
				return null;
			}

			// Accept quoted arguments as-is
			if ( match[3] ) {
				match[2] = match[4] || match[5] || "";

			// Strip excess characters from unquoted arguments
			} else if ( unquoted && rpseudo.test( unquoted ) &&
				// Get excess from tokenize (recursively)
				(excess = tokenize( unquoted, true )) &&
				// advance to the next closing parenthesis
				(excess = unquoted.indexOf( ")", unquoted.length - excess ) - unquoted.length) ) {

				// excess is a negative index
				match[0] = match[0].slice( 0, excess );
				match[2] = unquoted.slice( 0, excess );
			}

			// Return only captures needed by the pseudo filter method (type and argument)
			return match.slice( 0, 3 );
		}
	},

	filter: {

		"TAG": function( nodeNameSelector ) {
			var nodeName = nodeNameSelector.replace( runescape, funescape ).toLowerCase();
			return nodeNameSelector === "*" ?
				function() { return true; } :
				function( elem ) {
					return elem.nodeName && elem.nodeName.toLowerCase() === nodeName;
				};
		},

		"CLASS": function( className ) {
			var pattern = classCache[ className + " " ];

			return pattern ||
				(pattern = new RegExp( "(^|" + whitespace + ")" + className + "(" + whitespace + "|$)" )) &&
				classCache( className, function( elem ) {
					return pattern.test( typeof elem.className === "string" && elem.className || typeof elem.getAttribute !== "undefined" && elem.getAttribute("class") || "" );
				});
		},

		"ATTR": function( name, operator, check ) {
			return function( elem ) {
				var result = Sizzle.attr( elem, name );

				if ( result == null ) {
					return operator === "!=";
				}
				if ( !operator ) {
					return true;
				}

				result += "";

				return operator === "=" ? result === check :
					operator === "!=" ? result !== check :
					operator === "^=" ? check && result.indexOf( check ) === 0 :
					operator === "*=" ? check && result.indexOf( check ) > -1 :
					operator === "$=" ? check && result.slice( -check.length ) === check :
					operator === "~=" ? ( " " + result.replace( rwhitespace, " " ) + " " ).indexOf( check ) > -1 :
					operator === "|=" ? result === check || result.slice( 0, check.length + 1 ) === check + "-" :
					false;
			};
		},

		"CHILD": function( type, what, argument, first, last ) {
			var simple = type.slice( 0, 3 ) !== "nth",
				forward = type.slice( -4 ) !== "last",
				ofType = what === "of-type";

			return first === 1 && last === 0 ?

				// Shortcut for :nth-*(n)
				function( elem ) {
					return !!elem.parentNode;
				} :

				function( elem, context, xml ) {
					var cache, uniqueCache, outerCache, node, nodeIndex, start,
						dir = simple !== forward ? "nextSibling" : "previousSibling",
						parent = elem.parentNode,
						name = ofType && elem.nodeName.toLowerCase(),
						useCache = !xml && !ofType,
						diff = false;

					if ( parent ) {

						// :(first|last|only)-(child|of-type)
						if ( simple ) {
							while ( dir ) {
								node = elem;
								while ( (node = node[ dir ]) ) {
									if ( ofType ?
										node.nodeName.toLowerCase() === name :
										node.nodeType === 1 ) {

										return false;
									}
								}
								// Reverse direction for :only-* (if we haven't yet done so)
								start = dir = type === "only" && !start && "nextSibling";
							}
							return true;
						}

						start = [ forward ? parent.firstChild : parent.lastChild ];

						// non-xml :nth-child(...) stores cache data on `parent`
						if ( forward && useCache ) {

							// Seek `elem` from a previously-cached index

							// ...in a gzip-friendly way
							node = parent;
							outerCache = node[ expando ] || (node[ expando ] = {});

							// Support: IE <9 only
							// Defend against cloned attroperties (jQuery gh-1709)
							uniqueCache = outerCache[ node.uniqueID ] ||
								(outerCache[ node.uniqueID ] = {});

							cache = uniqueCache[ type ] || [];
							nodeIndex = cache[ 0 ] === dirruns && cache[ 1 ];
							diff = nodeIndex && cache[ 2 ];
							node = nodeIndex && parent.childNodes[ nodeIndex ];

							while ( (node = ++nodeIndex && node && node[ dir ] ||

								// Fallback to seeking `elem` from the start
								(diff = nodeIndex = 0) || start.pop()) ) {

								// When found, cache indexes on `parent` and break
								if ( node.nodeType === 1 && ++diff && node === elem ) {
									uniqueCache[ type ] = [ dirruns, nodeIndex, diff ];
									break;
								}
							}

						} else {
							// Use previously-cached element index if available
							if ( useCache ) {
								// ...in a gzip-friendly way
								node = elem;
								outerCache = node[ expando ] || (node[ expando ] = {});

								// Support: IE <9 only
								// Defend against cloned attroperties (jQuery gh-1709)
								uniqueCache = outerCache[ node.uniqueID ] ||
									(outerCache[ node.uniqueID ] = {});

								cache = uniqueCache[ type ] || [];
								nodeIndex = cache[ 0 ] === dirruns && cache[ 1 ];
								diff = nodeIndex;
							}

							// xml :nth-child(...)
							// or :nth-last-child(...) or :nth(-last)?-of-type(...)
							if ( diff === false ) {
								// Use the same loop as above to seek `elem` from the start
								while ( (node = ++nodeIndex && node && node[ dir ] ||
									(diff = nodeIndex = 0) || start.pop()) ) {

									if ( ( ofType ?
										node.nodeName.toLowerCase() === name :
										node.nodeType === 1 ) &&
										++diff ) {

										// Cache the index of each encountered element
										if ( useCache ) {
											outerCache = node[ expando ] || (node[ expando ] = {});

											// Support: IE <9 only
											// Defend against cloned attroperties (jQuery gh-1709)
											uniqueCache = outerCache[ node.uniqueID ] ||
												(outerCache[ node.uniqueID ] = {});

											uniqueCache[ type ] = [ dirruns, diff ];
										}

										if ( node === elem ) {
											break;
										}
									}
								}
							}
						}

						// Incorporate the offset, then check against cycle size
						diff -= last;
						return diff === first || ( diff % first === 0 && diff / first >= 0 );
					}
				};
		},

		"PSEUDO": function( pseudo, argument ) {
			// pseudo-class names are case-insensitive
			// http://www.w3.org/TR/selectors/#pseudo-classes
			// Prioritize by case sensitivity in case custom pseudos are added with uppercase letters
			// Remember that setFilters inherits from pseudos
			var args,
				fn = Expr.pseudos[ pseudo ] || Expr.setFilters[ pseudo.toLowerCase() ] ||
					Sizzle.error( "unsupported pseudo: " + pseudo );

			// The user may use createPseudo to indicate that
			// arguments are needed to create the filter function
			// just as Sizzle does
			if ( fn[ expando ] ) {
				return fn( argument );
			}

			// But maintain support for old signatures
			if ( fn.length > 1 ) {
				args = [ pseudo, pseudo, "", argument ];
				return Expr.setFilters.hasOwnProperty( pseudo.toLowerCase() ) ?
					markFunction(function( seed, matches ) {
						var idx,
							matched = fn( seed, argument ),
							i = matched.length;
						while ( i-- ) {
							idx = indexOf( seed, matched[i] );
							seed[ idx ] = !( matches[ idx ] = matched[i] );
						}
					}) :
					function( elem ) {
						return fn( elem, 0, args );
					};
			}

			return fn;
		}
	},

	pseudos: {
		// Potentially complex pseudos
		"not": markFunction(function( selector ) {
			// Trim the selector passed to compile
			// to avoid treating leading and trailing
			// spaces as combinators
			var input = [],
				results = [],
				matcher = compile( selector.replace( rtrim, "$1" ) );

			return matcher[ expando ] ?
				markFunction(function( seed, matches, context, xml ) {
					var elem,
						unmatched = matcher( seed, null, xml, [] ),
						i = seed.length;

					// Match elements unmatched by `matcher`
					while ( i-- ) {
						if ( (elem = unmatched[i]) ) {
							seed[i] = !(matches[i] = elem);
						}
					}
				}) :
				function( elem, context, xml ) {
					input[0] = elem;
					matcher( input, null, xml, results );
					// Don't keep the element (issue #299)
					input[0] = null;
					return !results.pop();
				};
		}),

		"has": markFunction(function( selector ) {
			return function( elem ) {
				return Sizzle( selector, elem ).length > 0;
			};
		}),

		"contains": markFunction(function( text ) {
			text = text.replace( runescape, funescape );
			return function( elem ) {
				return ( elem.textContent || elem.innerText || getText( elem ) ).indexOf( text ) > -1;
			};
		}),

		// "Whether an element is represented by a :lang() selector
		// is based solely on the element's language value
		// being equal to the identifier C,
		// or beginning with the identifier C immediately followed by "-".
		// The matching of C against the element's language value is performed case-insensitively.
		// The identifier C does not have to be a valid language name."
		// http://www.w3.org/TR/selectors/#lang-pseudo
		"lang": markFunction( function( lang ) {
			// lang value must be a valid identifier
			if ( !ridentifier.test(lang || "") ) {
				Sizzle.error( "unsupported lang: " + lang );
			}
			lang = lang.replace( runescape, funescape ).toLowerCase();
			return function( elem ) {
				var elemLang;
				do {
					if ( (elemLang = documentIsHTML ?
						elem.lang :
						elem.getAttribute("xml:lang") || elem.getAttribute("lang")) ) {

						elemLang = elemLang.toLowerCase();
						return elemLang === lang || elemLang.indexOf( lang + "-" ) === 0;
					}
				} while ( (elem = elem.parentNode) && elem.nodeType === 1 );
				return false;
			};
		}),

		// Miscellaneous
		"target": function( elem ) {
			var hash = window.location && window.location.hash;
			return hash && hash.slice( 1 ) === elem.id;
		},

		"root": function( elem ) {
			return elem === docElem;
		},

		"focus": function( elem ) {
			return elem === document.activeElement && (!document.hasFocus || document.hasFocus()) && !!(elem.type || elem.href || ~elem.tabIndex);
		},

		// Boolean properties
		"enabled": function( elem ) {
			return elem.disabled === false;
		},

		"disabled": function( elem ) {
			return elem.disabled === true;
		},

		"checked": function( elem ) {
			// In CSS3, :checked should return both checked and selected elements
			// http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked
			var nodeName = elem.nodeName.toLowerCase();
			return (nodeName === "input" && !!elem.checked) || (nodeName === "option" && !!elem.selected);
		},

		"selected": function( elem ) {
			// Accessing this property makes selected-by-default
			// options in Safari work properly
			if ( elem.parentNode ) {
				elem.parentNode.selectedIndex;
			}

			return elem.selected === true;
		},

		// Contents
		"empty": function( elem ) {
			// http://www.w3.org/TR/selectors/#empty-pseudo
			// :empty is negated by element (1) or content nodes (text: 3; cdata: 4; entity ref: 5),
			//   but not by others (comment: 8; processing instruction: 7; etc.)
			// nodeType < 6 works because attributes (2) do not appear as children
			for ( elem = elem.firstChild; elem; elem = elem.nextSibling ) {
				if ( elem.nodeType < 6 ) {
					return false;
				}
			}
			return true;
		},

		"parent": function( elem ) {
			return !Expr.pseudos["empty"]( elem );
		},

		// Element/input types
		"header": function( elem ) {
			return rheader.test( elem.nodeName );
		},

		"input": function( elem ) {
			return rinputs.test( elem.nodeName );
		},

		"button": function( elem ) {
			var name = elem.nodeName.toLowerCase();
			return name === "input" && elem.type === "button" || name === "button";
		},

		"text": function( elem ) {
			var attr;
			return elem.nodeName.toLowerCase() === "input" &&
				elem.type === "text" &&

				// Support: IE<8
				// New HTML5 attribute values (e.g., "search") appear with elem.type === "text"
				( (attr = elem.getAttribute("type")) == null || attr.toLowerCase() === "text" );
		},

		// Position-in-collection
		"first": createPositionalPseudo(function() {
			return [ 0 ];
		}),

		"last": createPositionalPseudo(function( matchIndexes, length ) {
			return [ length - 1 ];
		}),

		"eq": createPositionalPseudo(function( matchIndexes, length, argument ) {
			return [ argument < 0 ? argument + length : argument ];
		}),

		"even": createPositionalPseudo(function( matchIndexes, length ) {
			var i = 0;
			for ( ; i < length; i += 2 ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		}),

		"odd": createPositionalPseudo(function( matchIndexes, length ) {
			var i = 1;
			for ( ; i < length; i += 2 ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		}),

		"lt": createPositionalPseudo(function( matchIndexes, length, argument ) {
			var i = argument < 0 ? argument + length : argument;
			for ( ; --i >= 0; ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		}),

		"gt": createPositionalPseudo(function( matchIndexes, length, argument ) {
			var i = argument < 0 ? argument + length : argument;
			for ( ; ++i < length; ) {
				matchIndexes.push( i );
			}
			return matchIndexes;
		})
	}
};

Expr.pseudos["nth"] = Expr.pseudos["eq"];

// Add button/input type pseudos
for ( i in { radio: true, checkbox: true, file: true, password: true, image: true } ) {
	Expr.pseudos[ i ] = createInputPseudo( i );
}
for ( i in { submit: true, reset: true } ) {
	Expr.pseudos[ i ] = createButtonPseudo( i );
}

// Easy API for creating new setFilters
function setFilters() {}
setFilters.prototype = Expr.filters = Expr.pseudos;
Expr.setFilters = new setFilters();

tokenize = Sizzle.tokenize = function( selector, parseOnly ) {
	var matched, match, tokens, type,
		soFar, groups, preFilters,
		cached = tokenCache[ selector + " " ];

	if ( cached ) {
		return parseOnly ? 0 : cached.slice( 0 );
	}

	soFar = selector;
	groups = [];
	preFilters = Expr.preFilter;

	while ( soFar ) {

		// Comma and first run
		if ( !matched || (match = rcomma.exec( soFar )) ) {
			if ( match ) {
				// Don't consume trailing commas as valid
				soFar = soFar.slice( match[0].length ) || soFar;
			}
			groups.push( (tokens = []) );
		}

		matched = false;

		// Combinators
		if ( (match = rcombinators.exec( soFar )) ) {
			matched = match.shift();
			tokens.push({
				value: matched,
				// Cast descendant combinators to space
				type: match[0].replace( rtrim, " " )
			});
			soFar = soFar.slice( matched.length );
		}

		// Filters
		for ( type in Expr.filter ) {
			if ( (match = matchExpr[ type ].exec( soFar )) && (!preFilters[ type ] ||
				(match = preFilters[ type ]( match ))) ) {
				matched = match.shift();
				tokens.push({
					value: matched,
					type: type,
					matches: match
				});
				soFar = soFar.slice( matched.length );
			}
		}

		if ( !matched ) {
			break;
		}
	}

	// Return the length of the invalid excess
	// if we're just parsing
	// Otherwise, throw an error or return tokens
	return parseOnly ?
		soFar.length :
		soFar ?
			Sizzle.error( selector ) :
			// Cache the tokens
			tokenCache( selector, groups ).slice( 0 );
};

function toSelector( tokens ) {
	var i = 0,
		len = tokens.length,
		selector = "";
	for ( ; i < len; i++ ) {
		selector += tokens[i].value;
	}
	return selector;
}

function addCombinator( matcher, combinator, base ) {
	var dir = combinator.dir,
		checkNonElements = base && dir === "parentNode",
		doneName = done++;

	return combinator.first ?
		// Check against closest ancestor/preceding element
		function( elem, context, xml ) {
			while ( (elem = elem[ dir ]) ) {
				if ( elem.nodeType === 1 || checkNonElements ) {
					return matcher( elem, context, xml );
				}
			}
		} :

		// Check against all ancestor/preceding elements
		function( elem, context, xml ) {
			var oldCache, uniqueCache, outerCache,
				newCache = [ dirruns, doneName ];

			// We can't set arbitrary data on XML nodes, so they don't benefit from combinator caching
			if ( xml ) {
				while ( (elem = elem[ dir ]) ) {
					if ( elem.nodeType === 1 || checkNonElements ) {
						if ( matcher( elem, context, xml ) ) {
							return true;
						}
					}
				}
			} else {
				while ( (elem = elem[ dir ]) ) {
					if ( elem.nodeType === 1 || checkNonElements ) {
						outerCache = elem[ expando ] || (elem[ expando ] = {});

						// Support: IE <9 only
						// Defend against cloned attroperties (jQuery gh-1709)
						uniqueCache = outerCache[ elem.uniqueID ] || (outerCache[ elem.uniqueID ] = {});

						if ( (oldCache = uniqueCache[ dir ]) &&
							oldCache[ 0 ] === dirruns && oldCache[ 1 ] === doneName ) {

							// Assign to newCache so results back-propagate to previous elements
							return (newCache[ 2 ] = oldCache[ 2 ]);
						} else {
							// Reuse newcache so results back-propagate to previous elements
							uniqueCache[ dir ] = newCache;

							// A match means we're done; a fail means we have to keep checking
							if ( (newCache[ 2 ] = matcher( elem, context, xml )) ) {
								return true;
							}
						}
					}
				}
			}
		};
}

function elementMatcher( matchers ) {
	return matchers.length > 1 ?
		function( elem, context, xml ) {
			var i = matchers.length;
			while ( i-- ) {
				if ( !matchers[i]( elem, context, xml ) ) {
					return false;
				}
			}
			return true;
		} :
		matchers[0];
}

function multipleContexts( selector, contexts, results ) {
	var i = 0,
		len = contexts.length;
	for ( ; i < len; i++ ) {
		Sizzle( selector, contexts[i], results );
	}
	return results;
}

function condense( unmatched, map, filter, context, xml ) {
	var elem,
		newUnmatched = [],
		i = 0,
		len = unmatched.length,
		mapped = map != null;

	for ( ; i < len; i++ ) {
		if ( (elem = unmatched[i]) ) {
			if ( !filter || filter( elem, context, xml ) ) {
				newUnmatched.push( elem );
				if ( mapped ) {
					map.push( i );
				}
			}
		}
	}

	return newUnmatched;
}

function setMatcher( preFilter, selector, matcher, postFilter, postFinder, postSelector ) {
	if ( postFilter && !postFilter[ expando ] ) {
		postFilter = setMatcher( postFilter );
	}
	if ( postFinder && !postFinder[ expando ] ) {
		postFinder = setMatcher( postFinder, postSelector );
	}
	return markFunction(function( seed, results, context, xml ) {
		var temp, i, elem,
			preMap = [],
			postMap = [],
			preexisting = results.length,

			// Get initial elements from seed or context
			elems = seed || multipleContexts( selector || "*", context.nodeType ? [ context ] : context, [] ),

			// Prefilter to get matcher input, preserving a map for seed-results synchronization
			matcherIn = preFilter && ( seed || !selector ) ?
				condense( elems, preMap, preFilter, context, xml ) :
				elems,

			matcherOut = matcher ?
				// If we have a postFinder, or filtered seed, or non-seed postFilter or preexisting results,
				postFinder || ( seed ? preFilter : preexisting || postFilter ) ?

					// ...intermediate processing is necessary
					[] :

					// ...otherwise use results directly
					results :
				matcherIn;

		// Find primary matches
		if ( matcher ) {
			matcher( matcherIn, matcherOut, context, xml );
		}

		// Apply postFilter
		if ( postFilter ) {
			temp = condense( matcherOut, postMap );
			postFilter( temp, [], context, xml );

			// Un-match failing elements by moving them back to matcherIn
			i = temp.length;
			while ( i-- ) {
				if ( (elem = temp[i]) ) {
					matcherOut[ postMap[i] ] = !(matcherIn[ postMap[i] ] = elem);
				}
			}
		}

		if ( seed ) {
			if ( postFinder || preFilter ) {
				if ( postFinder ) {
					// Get the final matcherOut by condensing this intermediate into postFinder contexts
					temp = [];
					i = matcherOut.length;
					while ( i-- ) {
						if ( (elem = matcherOut[i]) ) {
							// Restore matcherIn since elem is not yet a final match
							temp.push( (matcherIn[i] = elem) );
						}
					}
					postFinder( null, (matcherOut = []), temp, xml );
				}

				// Move matched elements from seed to results to keep them synchronized
				i = matcherOut.length;
				while ( i-- ) {
					if ( (elem = matcherOut[i]) &&
						(temp = postFinder ? indexOf( seed, elem ) : preMap[i]) > -1 ) {

						seed[temp] = !(results[temp] = elem);
					}
				}
			}

		// Add elements to results, through postFinder if defined
		} else {
			matcherOut = condense(
				matcherOut === results ?
					matcherOut.splice( preexisting, matcherOut.length ) :
					matcherOut
			);
			if ( postFinder ) {
				postFinder( null, results, matcherOut, xml );
			} else {
				push.apply( results, matcherOut );
			}
		}
	});
}

function matcherFromTokens( tokens ) {
	var checkContext, matcher, j,
		len = tokens.length,
		leadingRelative = Expr.relative[ tokens[0].type ],
		implicitRelative = leadingRelative || Expr.relative[" "],
		i = leadingRelative ? 1 : 0,

		// The foundational matcher ensures that elements are reachable from top-level context(s)
		matchContext = addCombinator( function( elem ) {
			return elem === checkContext;
		}, implicitRelative, true ),
		matchAnyContext = addCombinator( function( elem ) {
			return indexOf( checkContext, elem ) > -1;
		}, implicitRelative, true ),
		matchers = [ function( elem, context, xml ) {
			var ret = ( !leadingRelative && ( xml || context !== outermostContext ) ) || (
				(checkContext = context).nodeType ?
					matchContext( elem, context, xml ) :
					matchAnyContext( elem, context, xml ) );
			// Avoid hanging onto element (issue #299)
			checkContext = null;
			return ret;
		} ];

	for ( ; i < len; i++ ) {
		if ( (matcher = Expr.relative[ tokens[i].type ]) ) {
			matchers = [ addCombinator(elementMatcher( matchers ), matcher) ];
		} else {
			matcher = Expr.filter[ tokens[i].type ].apply( null, tokens[i].matches );

			// Return special upon seeing a positional matcher
			if ( matcher[ expando ] ) {
				// Find the next relative operator (if any) for proper handling
				j = ++i;
				for ( ; j < len; j++ ) {
					if ( Expr.relative[ tokens[j].type ] ) {
						break;
					}
				}
				return setMatcher(
					i > 1 && elementMatcher( matchers ),
					i > 1 && toSelector(
						// If the preceding token was a descendant combinator, insert an implicit any-element `*`
						tokens.slice( 0, i - 1 ).concat({ value: tokens[ i - 2 ].type === " " ? "*" : "" })
					).replace( rtrim, "$1" ),
					matcher,
					i < j && matcherFromTokens( tokens.slice( i, j ) ),
					j < len && matcherFromTokens( (tokens = tokens.slice( j )) ),
					j < len && toSelector( tokens )
				);
			}
			matchers.push( matcher );
		}
	}

	return elementMatcher( matchers );
}

function matcherFromGroupMatchers( elementMatchers, setMatchers ) {
	var bySet = setMatchers.length > 0,
		byElement = elementMatchers.length > 0,
		superMatcher = function( seed, context, xml, results, outermost ) {
			var elem, j, matcher,
				matchedCount = 0,
				i = "0",
				unmatched = seed && [],
				setMatched = [],
				contextBackup = outermostContext,
				// We must always have either seed elements or outermost context
				elems = seed || byElement && Expr.find["TAG"]( "*", outermost ),
				// Use integer dirruns iff this is the outermost matcher
				dirrunsUnique = (dirruns += contextBackup == null ? 1 : Math.random() || 0.1),
				len = elems.length;

			if ( outermost ) {
				outermostContext = context === document || context || outermost;
			}

			// Add elements passing elementMatchers directly to results
			// Support: IE<9, Safari
			// Tolerate NodeList properties (IE: "length"; Safari: <number>) matching elements by id
			for ( ; i !== len && (elem = elems[i]) != null; i++ ) {
				if ( byElement && elem ) {
					j = 0;
					if ( !context && elem.ownerDocument !== document ) {
						setDocument( elem );
						xml = !documentIsHTML;
					}
					while ( (matcher = elementMatchers[j++]) ) {
						if ( matcher( elem, context || document, xml) ) {
							results.push( elem );
							break;
						}
					}
					if ( outermost ) {
						dirruns = dirrunsUnique;
					}
				}

				// Track unmatched elements for set filters
				if ( bySet ) {
					// They will have gone through all possible matchers
					if ( (elem = !matcher && elem) ) {
						matchedCount--;
					}

					// Lengthen the array for every element, matched or not
					if ( seed ) {
						unmatched.push( elem );
					}
				}
			}

			// `i` is now the count of elements visited above, and adding it to `matchedCount`
			// makes the latter nonnegative.
			matchedCount += i;

			// Apply set filters to unmatched elements
			// NOTE: This can be skipped if there are no unmatched elements (i.e., `matchedCount`
			// equals `i`), unless we didn't visit _any_ elements in the above loop because we have
			// no element matchers and no seed.
			// Incrementing an initially-string "0" `i` allows `i` to remain a string only in that
			// case, which will result in a "00" `matchedCount` that differs from `i` but is also
			// numerically zero.
			if ( bySet && i !== matchedCount ) {
				j = 0;
				while ( (matcher = setMatchers[j++]) ) {
					matcher( unmatched, setMatched, context, xml );
				}

				if ( seed ) {
					// Reintegrate element matches to eliminate the need for sorting
					if ( matchedCount > 0 ) {
						while ( i-- ) {
							if ( !(unmatched[i] || setMatched[i]) ) {
								setMatched[i] = pop.call( results );
							}
						}
					}

					// Discard index placeholder values to get only actual matches
					setMatched = condense( setMatched );
				}

				// Add matches to results
				push.apply( results, setMatched );

				// Seedless set matches succeeding multiple successful matchers stipulate sorting
				if ( outermost && !seed && setMatched.length > 0 &&
					( matchedCount + setMatchers.length ) > 1 ) {

					Sizzle.uniqueSort( results );
				}
			}

			// Override manipulation of globals by nested matchers
			if ( outermost ) {
				dirruns = dirrunsUnique;
				outermostContext = contextBackup;
			}

			return unmatched;
		};

	return bySet ?
		markFunction( superMatcher ) :
		superMatcher;
}

compile = Sizzle.compile = function( selector, match /* Internal Use Only */ ) {
	var i,
		setMatchers = [],
		elementMatchers = [],
		cached = compilerCache[ selector + " " ];

	if ( !cached ) {
		// Generate a function of recursive functions that can be used to check each element
		if ( !match ) {
			match = tokenize( selector );
		}
		i = match.length;
		while ( i-- ) {
			cached = matcherFromTokens( match[i] );
			if ( cached[ expando ] ) {
				setMatchers.push( cached );
			} else {
				elementMatchers.push( cached );
			}
		}

		// Cache the compiled function
		cached = compilerCache( selector, matcherFromGroupMatchers( elementMatchers, setMatchers ) );

		// Save selector and tokenization
		cached.selector = selector;
	}
	return cached;
};

/**
 * A low-level selection function that works with Sizzle's compiled
 *  selector functions
 * @param {String|Function} selector A selector or a pre-compiled
 *  selector function built with Sizzle.compile
 * @param {Element} context
 * @param {Array} [results]
 * @param {Array} [seed] A set of elements to match against
 */
select = Sizzle.select = function( selector, context, results, seed ) {
	var i, tokens, token, type, find,
		compiled = typeof selector === "function" && selector,
		match = !seed && tokenize( (selector = compiled.selector || selector) );

	results = results || [];

	// Try to minimize operations if there is only one selector in the list and no seed
	// (the latter of which guarantees us context)
	if ( match.length === 1 ) {

		// Reduce context if the leading compound selector is an ID
		tokens = match[0] = match[0].slice( 0 );
		if ( tokens.length > 2 && (token = tokens[0]).type === "ID" &&
				support.getById && context.nodeType === 9 && documentIsHTML &&
				Expr.relative[ tokens[1].type ] ) {

			context = ( Expr.find["ID"]( token.matches[0].replace(runescape, funescape), context ) || [] )[0];
			if ( !context ) {
				return results;

			// Precompiled matchers will still verify ancestry, so step up a level
			} else if ( compiled ) {
				context = context.parentNode;
			}

			selector = selector.slice( tokens.shift().value.length );
		}

		// Fetch a seed set for right-to-left matching
		i = matchExpr["needsContext"].test( selector ) ? 0 : tokens.length;
		while ( i-- ) {
			token = tokens[i];

			// Abort if we hit a combinator
			if ( Expr.relative[ (type = token.type) ] ) {
				break;
			}
			if ( (find = Expr.find[ type ]) ) {
				// Search, expanding context for leading sibling combinators
				if ( (seed = find(
					token.matches[0].replace( runescape, funescape ),
					rsibling.test( tokens[0].type ) && testContext( context.parentNode ) || context
				)) ) {

					// If seed is empty or no tokens remain, we can return early
					tokens.splice( i, 1 );
					selector = seed.length && toSelector( tokens );
					if ( !selector ) {
						push.apply( results, seed );
						return results;
					}

					break;
				}
			}
		}
	}

	// Compile and execute a filtering function if one is not provided
	// Provide `match` to avoid retokenization if we modified the selector above
	( compiled || compile( selector, match ) )(
		seed,
		context,
		!documentIsHTML,
		results,
		!context || rsibling.test( selector ) && testContext( context.parentNode ) || context
	);
	return results;
};

// One-time assignments

// Sort stability
support.sortStable = expando.split("").sort( sortOrder ).join("") === expando;

// Support: Chrome 14-35+
// Always assume duplicates if they aren't passed to the comparison function
support.detectDuplicates = !!hasDuplicate;

// Initialize against the default document
setDocument();

// Support: Webkit<537.32 - Safari 6.0.3/Chrome 25 (fixed in Chrome 27)
// Detached nodes confoundingly follow *each other*
support.sortDetached = assert(function( div1 ) {
	// Should return 1, but returns 4 (following)
	return div1.compareDocumentPosition( document.createElement("div") ) & 1;
});

// Support: IE<8
// Prevent attribute/property "interpolation"
// http://msdn.microsoft.com/en-us/library/ms536429%28VS.85%29.aspx
if ( !assert(function( div ) {
	div.innerHTML = "<a href='#'></a>";
	return div.firstChild.getAttribute("href") === "#" ;
}) ) {
	addHandle( "type|href|height|width", function( elem, name, isXML ) {
		if ( !isXML ) {
			return elem.getAttribute( name, name.toLowerCase() === "type" ? 1 : 2 );
		}
	});
}

// Support: IE<9
// Use defaultValue in place of getAttribute("value")
if ( !support.attributes || !assert(function( div ) {
	div.innerHTML = "<input/>";
	div.firstChild.setAttribute( "value", "" );
	return div.firstChild.getAttribute( "value" ) === "";
}) ) {
	addHandle( "value", function( elem, name, isXML ) {
		if ( !isXML && elem.nodeName.toLowerCase() === "input" ) {
			return elem.defaultValue;
		}
	});
}

// Support: IE<9
// Use getAttributeNode to fetch booleans when getAttribute lies
if ( !assert(function( div ) {
	return div.getAttribute("disabled") == null;
}) ) {
	addHandle( booleans, function( elem, name, isXML ) {
		var val;
		if ( !isXML ) {
			return elem[ name ] === true ? name.toLowerCase() :
					(val = elem.getAttributeNode( name )) && val.specified ?
					val.value :
				null;
		}
	});
}

return Sizzle;

})( window );



jQuery.find = Sizzle;
jQuery.expr = Sizzle.selectors;
jQuery.expr[ ":" ] = jQuery.expr.pseudos;
jQuery.uniqueSort = jQuery.unique = Sizzle.uniqueSort;
jQuery.text = Sizzle.getText;
jQuery.isXMLDoc = Sizzle.isXML;
jQuery.contains = Sizzle.contains;



var dir = function( elem, dir, until ) {
	var matched = [],
		truncate = until !== undefined;

	while ( ( elem = elem[ dir ] ) && elem.nodeType !== 9 ) {
		if ( elem.nodeType === 1 ) {
			if ( truncate && jQuery( elem ).is( until ) ) {
				break;
			}
			matched.push( elem );
		}
	}
	return matched;
};


var siblings = function( n, elem ) {
	var matched = [];

	for ( ; n; n = n.nextSibling ) {
		if ( n.nodeType === 1 && n !== elem ) {
			matched.push( n );
		}
	}

	return matched;
};


var rneedsContext = jQuery.expr.match.needsContext;

var rsingleTag = ( /^<([\w-]+)\s*\/?>(?:<\/\1>|)$/ );



var risSimple = /^.[^:#\[\.,]*$/;

// Implement the identical functionality for filter and not
function winnow( elements, qualifier, not ) {
	if ( jQuery.isFunction( qualifier ) ) {
		return jQuery.grep( elements, function( elem, i ) {
			/* jshint -W018 */
			return !!qualifier.call( elem, i, elem ) !== not;
		} );

	}

	if ( qualifier.nodeType ) {
		return jQuery.grep( elements, function( elem ) {
			return ( elem === qualifier ) !== not;
		} );

	}

	if ( typeof qualifier === "string" ) {
		if ( risSimple.test( qualifier ) ) {
			return jQuery.filter( qualifier, elements, not );
		}

		qualifier = jQuery.filter( qualifier, elements );
	}

	return jQuery.grep( elements, function( elem ) {
		return ( indexOf.call( qualifier, elem ) > -1 ) !== not;
	} );
}

jQuery.filter = function( expr, elems, not ) {
	var elem = elems[ 0 ];

	if ( not ) {
		expr = ":not(" + expr + ")";
	}

	return elems.length === 1 && elem.nodeType === 1 ?
		jQuery.find.matchesSelector( elem, expr ) ? [ elem ] : [] :
		jQuery.find.matches( expr, jQuery.grep( elems, function( elem ) {
			return elem.nodeType === 1;
		} ) );
};

jQuery.fn.extend( {
	find: function( selector ) {
		var i,
			len = this.length,
			ret = [],
			self = this;

		if ( typeof selector !== "string" ) {
			return this.pushStack( jQuery( selector ).filter( function() {
				for ( i = 0; i < len; i++ ) {
					if ( jQuery.contains( self[ i ], this ) ) {
						return true;
					}
				}
			} ) );
		}

		for ( i = 0; i < len; i++ ) {
			jQuery.find( selector, self[ i ], ret );
		}

		// Needed because $( selector, context ) becomes $( context ).find( selector )
		ret = this.pushStack( len > 1 ? jQuery.unique( ret ) : ret );
		ret.selector = this.selector ? this.selector + " " + selector : selector;
		return ret;
	},
	filter: function( selector ) {
		return this.pushStack( winnow( this, selector || [], false ) );
	},
	not: function( selector ) {
		return this.pushStack( winnow( this, selector || [], true ) );
	},
	is: function( selector ) {
		return !!winnow(
			this,

			// If this is a positional/relative selector, check membership in the returned set
			// so $("p:first").is("p:last") won't return true for a doc with two "p".
			typeof selector === "string" && rneedsContext.test( selector ) ?
				jQuery( selector ) :
				selector || [],
			false
		).length;
	}
} );


// Initialize a jQuery object


// A central reference to the root jQuery(document)
var rootjQuery,

	// A simple way to check for HTML strings
	// Prioritize #id over <tag> to avoid XSS via location.hash (#9521)
	// Strict HTML recognition (#11290: must start with <)
	rquickExpr = /^(?:\s*(<[\w\W]+>)[^>]*|#([\w-]*))$/,

	init = jQuery.fn.init = function( selector, context, root ) {
		var match, elem;

		// HANDLE: $(""), $(null), $(undefined), $(false)
		if ( !selector ) {
			return this;
		}

		// Method init() accepts an alternate rootjQuery
		// so migrate can support jQuery.sub (gh-2101)
		root = root || rootjQuery;

		// Handle HTML strings
		if ( typeof selector === "string" ) {
			if ( selector[ 0 ] === "<" &&
				selector[ selector.length - 1 ] === ">" &&
				selector.length >= 3 ) {

				// Assume that strings that start and end with <> are HTML and skip the regex check
				match = [ null, selector, null ];

			} else {
				match = rquickExpr.exec( selector );
			}

			// Match html or make sure no context is specified for #id
			if ( match && ( match[ 1 ] || !context ) ) {

				// HANDLE: $(html) -> $(array)
				if ( match[ 1 ] ) {
					context = context instanceof jQuery ? context[ 0 ] : context;

					// Option to run scripts is true for back-compat
					// Intentionally let the error be thrown if parseHTML is not present
					jQuery.merge( this, jQuery.parseHTML(
						match[ 1 ],
						context && context.nodeType ? context.ownerDocument || context : document,
						true
					) );

					// HANDLE: $(html, props)
					if ( rsingleTag.test( match[ 1 ] ) && jQuery.isPlainObject( context ) ) {
						for ( match in context ) {

							// Properties of context are called as methods if possible
							if ( jQuery.isFunction( this[ match ] ) ) {
								this[ match ]( context[ match ] );

							// ...and otherwise set as attributes
							} else {
								this.attr( match, context[ match ] );
							}
						}
					}

					return this;

				// HANDLE: $(#id)
				} else {
					elem = document.getElementById( match[ 2 ] );

					// Support: Blackberry 4.6
					// gEBID returns nodes no longer in the document (#6963)
					if ( elem && elem.parentNode ) {

						// Inject the element directly into the jQuery object
						this.length = 1;
						this[ 0 ] = elem;
					}

					this.context = document;
					this.selector = selector;
					return this;
				}

			// HANDLE: $(expr, $(...))
			} else if ( !context || context.jquery ) {
				return ( context || root ).find( selector );

			// HANDLE: $(expr, context)
			// (which is just equivalent to: $(context).find(expr)
			} else {
				return this.constructor( context ).find( selector );
			}

		// HANDLE: $(DOMElement)
		} else if ( selector.nodeType ) {
			this.context = this[ 0 ] = selector;
			this.length = 1;
			return this;

		// HANDLE: $(function)
		// Shortcut for document ready
		} else if ( jQuery.isFunction( selector ) ) {
			return root.ready !== undefined ?
				root.ready( selector ) :

				// Execute immediately if ready is not present
				selector( jQuery );
		}

		if ( selector.selector !== undefined ) {
			this.selector = selector.selector;
			this.context = selector.context;
		}

		return jQuery.makeArray( selector, this );
	};

// Give the init function the jQuery prototype for later instantiation
init.prototype = jQuery.fn;

// Initialize central reference
rootjQuery = jQuery( document );


var rparentsprev = /^(?:parents|prev(?:Until|All))/,

	// Methods guaranteed to produce a unique set when starting from a unique set
	guaranteedUnique = {
		children: true,
		contents: true,
		next: true,
		prev: true
	};

jQuery.fn.extend( {
	has: function( target ) {
		var targets = jQuery( target, this ),
			l = targets.length;

		return this.filter( function() {
			var i = 0;
			for ( ; i < l; i++ ) {
				if ( jQuery.contains( this, targets[ i ] ) ) {
					return true;
				}
			}
		} );
	},

	closest: function( selectors, context ) {
		var cur,
			i = 0,
			l = this.length,
			matched = [],
			pos = rneedsContext.test( selectors ) || typeof selectors !== "string" ?
				jQuery( selectors, context || this.context ) :
				0;

		for ( ; i < l; i++ ) {
			for ( cur = this[ i ]; cur && cur !== context; cur = cur.parentNode ) {

				// Always skip document fragments
				if ( cur.nodeType < 11 && ( pos ?
					pos.index( cur ) > -1 :

					// Don't pass non-elements to Sizzle
					cur.nodeType === 1 &&
						jQuery.find.matchesSelector( cur, selectors ) ) ) {

					matched.push( cur );
					break;
				}
			}
		}

		return this.pushStack( matched.length > 1 ? jQuery.uniqueSort( matched ) : matched );
	},

	// Determine the position of an element within the set
	index: function( elem ) {

		// No argument, return index in parent
		if ( !elem ) {
			return ( this[ 0 ] && this[ 0 ].parentNode ) ? this.first().prevAll().length : -1;
		}

		// Index in selector
		if ( typeof elem === "string" ) {
			return indexOf.call( jQuery( elem ), this[ 0 ] );
		}

		// Locate the position of the desired element
		return indexOf.call( this,

			// If it receives a jQuery object, the first element is used
			elem.jquery ? elem[ 0 ] : elem
		);
	},

	add: function( selector, context ) {
		return this.pushStack(
			jQuery.uniqueSort(
				jQuery.merge( this.get(), jQuery( selector, context ) )
			)
		);
	},

	addBack: function( selector ) {
		return this.add( selector == null ?
			this.prevObject : this.prevObject.filter( selector )
		);
	}
} );

function sibling( cur, dir ) {
	while ( ( cur = cur[ dir ] ) && cur.nodeType !== 1 ) {}
	return cur;
}

jQuery.each( {
	parent: function( elem ) {
		var parent = elem.parentNode;
		return parent && parent.nodeType !== 11 ? parent : null;
	},
	parents: function( elem ) {
		return dir( elem, "parentNode" );
	},
	parentsUntil: function( elem, i, until ) {
		return dir( elem, "parentNode", until );
	},
	next: function( elem ) {
		return sibling( elem, "nextSibling" );
	},
	prev: function( elem ) {
		return sibling( elem, "previousSibling" );
	},
	nextAll: function( elem ) {
		return dir( elem, "nextSibling" );
	},
	prevAll: function( elem ) {
		return dir( elem, "previousSibling" );
	},
	nextUntil: function( elem, i, until ) {
		return dir( elem, "nextSibling", until );
	},
	prevUntil: function( elem, i, until ) {
		return dir( elem, "previousSibling", until );
	},
	siblings: function( elem ) {
		return siblings( ( elem.parentNode || {} ).firstChild, elem );
	},
	children: function( elem ) {
		return siblings( elem.firstChild );
	},
	contents: function( elem ) {
		return elem.contentDocument || jQuery.merge( [], elem.childNodes );
	}
}, function( name, fn ) {
	jQuery.fn[ name ] = function( until, selector ) {
		var matched = jQuery.map( this, fn, until );

		if ( name.slice( -5 ) !== "Until" ) {
			selector = until;
		}

		if ( selector && typeof selector === "string" ) {
			matched = jQuery.filter( selector, matched );
		}

		if ( this.length > 1 ) {

			// Remove duplicates
			if ( !guaranteedUnique[ name ] ) {
				jQuery.uniqueSort( matched );
			}

			// Reverse order for parents* and prev-derivatives
			if ( rparentsprev.test( name ) ) {
				matched.reverse();
			}
		}

		return this.pushStack( matched );
	};
} );
var rnotwhite = ( /\S+/g );



// Convert String-formatted options into Object-formatted ones
function createOptions( options ) {
	var object = {};
	jQuery.each( options.match( rnotwhite ) || [], function( _, flag ) {
		object[ flag ] = true;
	} );
	return object;
}

/*
 * Create a callback list using the following parameters:
 *
 *	options: an optional list of space-separated options that will change how
 *			the callback list behaves or a more traditional option object
 *
 * By default a callback list will act like an event callback list and can be
 * "fired" multiple times.
 *
 * Possible options:
 *
 *	once:			will ensure the callback list can only be fired once (like a Deferred)
 *
 *	memory:			will keep track of previous values and will call any callback added
 *					after the list has been fired right away with the latest "memorized"
 *					values (like a Deferred)
 *
 *	unique:			will ensure a callback can only be added once (no duplicate in the list)
 *
 *	stopOnFalse:	interrupt callings when a callback returns false
 *
 */
jQuery.Callbacks = function( options ) {

	// Convert options from String-formatted to Object-formatted if needed
	// (we check in cache first)
	options = typeof options === "string" ?
		createOptions( options ) :
		jQuery.extend( {}, options );

	var // Flag to know if list is currently firing
		firing,

		// Last fire value for non-forgettable lists
		memory,

		// Flag to know if list was already fired
		fired,

		// Flag to prevent firing
		locked,

		// Actual callback list
		list = [],

		// Queue of execution data for repeatable lists
		queue = [],

		// Index of currently firing callback (modified by add/remove as needed)
		firingIndex = -1,

		// Fire callbacks
		fire = function() {

			// Enforce single-firing
			locked = options.once;

			// Execute callbacks for all pending executions,
			// respecting firingIndex overrides and runtime changes
			fired = firing = true;
			for ( ; queue.length; firingIndex = -1 ) {
				memory = queue.shift();
				while ( ++firingIndex < list.length ) {

					// Run callback and check for early termination
					if ( list[ firingIndex ].apply( memory[ 0 ], memory[ 1 ] ) === false &&
						options.stopOnFalse ) {

						// Jump to end and forget the data so .add doesn't re-fire
						firingIndex = list.length;
						memory = false;
					}
				}
			}

			// Forget the data if we're done with it
			if ( !options.memory ) {
				memory = false;
			}

			firing = false;

			// Clean up if we're done firing for good
			if ( locked ) {

				// Keep an empty list if we have data for future add calls
				if ( memory ) {
					list = [];

				// Otherwise, this object is spent
				} else {
					list = "";
				}
			}
		},

		// Actual Callbacks object
		self = {

			// Add a callback or a collection of callbacks to the list
			add: function() {
				if ( list ) {

					// If we have memory from a past run, we should fire after adding
					if ( memory && !firing ) {
						firingIndex = list.length - 1;
						queue.push( memory );
					}

					( function add( args ) {
						jQuery.each( args, function( _, arg ) {
							if ( jQuery.isFunction( arg ) ) {
								if ( !options.unique || !self.has( arg ) ) {
									list.push( arg );
								}
							} else if ( arg && arg.length && jQuery.type( arg ) !== "string" ) {

								// Inspect recursively
								add( arg );
							}
						} );
					} )( arguments );

					if ( memory && !firing ) {
						fire();
					}
				}
				return this;
			},

			// Remove a callback from the list
			remove: function() {
				jQuery.each( arguments, function( _, arg ) {
					var index;
					while ( ( index = jQuery.inArray( arg, list, index ) ) > -1 ) {
						list.splice( index, 1 );

						// Handle firing indexes
						if ( index <= firingIndex ) {
							firingIndex--;
						}
					}
				} );
				return this;
			},

			// Check if a given callback is in the list.
			// If no argument is given, return whether or not list has callbacks attached.
			has: function( fn ) {
				return fn ?
					jQuery.inArray( fn, list ) > -1 :
					list.length > 0;
			},

			// Remove all callbacks from the list
			empty: function() {
				if ( list ) {
					list = [];
				}
				return this;
			},

			// Disable .fire and .add
			// Abort any current/pending executions
			// Clear all callbacks and values
			disable: function() {
				locked = queue = [];
				list = memory = "";
				return this;
			},
			disabled: function() {
				return !list;
			},

			// Disable .fire
			// Also disable .add unless we have memory (since it would have no effect)
			// Abort any pending executions
			lock: function() {
				locked = queue = [];
				if ( !memory ) {
					list = memory = "";
				}
				return this;
			},
			locked: function() {
				return !!locked;
			},

			// Call all callbacks with the given context and arguments
			fireWith: function( context, args ) {
				if ( !locked ) {
					args = args || [];
					args = [ context, args.slice ? args.slice() : args ];
					queue.push( args );
					if ( !firing ) {
						fire();
					}
				}
				return this;
			},

			// Call all the callbacks with the given arguments
			fire: function() {
				self.fireWith( this, arguments );
				return this;
			},

			// To know if the callbacks have already been called at least once
			fired: function() {
				return !!fired;
			}
		};

	return self;
};


jQuery.extend( {

	Deferred: function( func ) {
		var tuples = [

				// action, add listener, listener list, final state
				[ "resolve", "done", jQuery.Callbacks( "once memory" ), "resolved" ],
				[ "reject", "fail", jQuery.Callbacks( "once memory" ), "rejected" ],
				[ "notify", "progress", jQuery.Callbacks( "memory" ) ]
			],
			state = "pending",
			promise = {
				state: function() {
					return state;
				},
				always: function() {
					deferred.done( arguments ).fail( arguments );
					return this;
				},
				then: function( /* fnDone, fnFail, fnProgress */ ) {
					var fns = arguments;
					return jQuery.Deferred( function( newDefer ) {
						jQuery.each( tuples, function( i, tuple ) {
							var fn = jQuery.isFunction( fns[ i ] ) && fns[ i ];

							// deferred[ done | fail | progress ] for forwarding actions to newDefer
							deferred[ tuple[ 1 ] ]( function() {
								var returned = fn && fn.apply( this, arguments );
								if ( returned && jQuery.isFunction( returned.promise ) ) {
									returned.promise()
										.progress( newDefer.notify )
										.done( newDefer.resolve )
										.fail( newDefer.reject );
								} else {
									newDefer[ tuple[ 0 ] + "With" ](
										this === promise ? newDefer.promise() : this,
										fn ? [ returned ] : arguments
									);
								}
							} );
						} );
						fns = null;
					} ).promise();
				},

				// Get a promise for this deferred
				// If obj is provided, the promise aspect is added to the object
				promise: function( obj ) {
					return obj != null ? jQuery.extend( obj, promise ) : promise;
				}
			},
			deferred = {};

		// Keep pipe for back-compat
		promise.pipe = promise.then;

		// Add list-specific methods
		jQuery.each( tuples, function( i, tuple ) {
			var list = tuple[ 2 ],
				stateString = tuple[ 3 ];

			// promise[ done | fail | progress ] = list.add
			promise[ tuple[ 1 ] ] = list.add;

			// Handle state
			if ( stateString ) {
				list.add( function() {

					// state = [ resolved | rejected ]
					state = stateString;

				// [ reject_list | resolve_list ].disable; progress_list.lock
				}, tuples[ i ^ 1 ][ 2 ].disable, tuples[ 2 ][ 2 ].lock );
			}

			// deferred[ resolve | reject | notify ]
			deferred[ tuple[ 0 ] ] = function() {
				deferred[ tuple[ 0 ] + "With" ]( this === deferred ? promise : this, arguments );
				return this;
			};
			deferred[ tuple[ 0 ] + "With" ] = list.fireWith;
		} );

		// Make the deferred a promise
		promise.promise( deferred );

		// Call given func if any
		if ( func ) {
			func.call( deferred, deferred );
		}

		// All done!
		return deferred;
	},

	// Deferred helper
	when: function( subordinate /* , ..., subordinateN */ ) {
		var i = 0,
			resolveValues = slice.call( arguments ),
			length = resolveValues.length,

			// the count of uncompleted subordinates
			remaining = length !== 1 ||
				( subordinate && jQuery.isFunction( subordinate.promise ) ) ? length : 0,

			// the master Deferred.
			// If resolveValues consist of only a single Deferred, just use that.
			deferred = remaining === 1 ? subordinate : jQuery.Deferred(),

			// Update function for both resolve and progress values
			updateFunc = function( i, contexts, values ) {
				return function( value ) {
					contexts[ i ] = this;
					values[ i ] = arguments.length > 1 ? slice.call( arguments ) : value;
					if ( values === progressValues ) {
						deferred.notifyWith( contexts, values );
					} else if ( !( --remaining ) ) {
						deferred.resolveWith( contexts, values );
					}
				};
			},

			progressValues, progressContexts, resolveContexts;

		// Add listeners to Deferred subordinates; treat others as resolved
		if ( length > 1 ) {
			progressValues = new Array( length );
			progressContexts = new Array( length );
			resolveContexts = new Array( length );
			for ( ; i < length; i++ ) {
				if ( resolveValues[ i ] && jQuery.isFunction( resolveValues[ i ].promise ) ) {
					resolveValues[ i ].promise()
						.progress( updateFunc( i, progressContexts, progressValues ) )
						.done( updateFunc( i, resolveContexts, resolveValues ) )
						.fail( deferred.reject );
				} else {
					--remaining;
				}
			}
		}

		// If we're not waiting on anything, resolve the master
		if ( !remaining ) {
			deferred.resolveWith( resolveContexts, resolveValues );
		}

		return deferred.promise();
	}
} );


// The deferred used on DOM ready
var readyList;

jQuery.fn.ready = function( fn ) {

	// Add the callback
	jQuery.ready.promise().done( fn );

	return this;
};

jQuery.extend( {

	// Is the DOM ready to be used? Set to true once it occurs.
	isReady: false,

	// A counter to track how many items to wait for before
	// the ready event fires. See #6781
	readyWait: 1,

	// Hold (or release) the ready event
	holdReady: function( hold ) {
		if ( hold ) {
			jQuery.readyWait++;
		} else {
			jQuery.ready( true );
		}
	},

	// Handle when the DOM is ready
	ready: function( wait ) {

		// Abort if there are pending holds or we're already ready
		if ( wait === true ? --jQuery.readyWait : jQuery.isReady ) {
			return;
		}

		// Remember that the DOM is ready
		jQuery.isReady = true;

		// If a normal DOM Ready event fired, decrement, and wait if need be
		if ( wait !== true && --jQuery.readyWait > 0 ) {
			return;
		}

		// If there are functions bound, to execute
		readyList.resolveWith( document, [ jQuery ] );

		// Trigger any bound ready events
		if ( jQuery.fn.triggerHandler ) {
			jQuery( document ).triggerHandler( "ready" );
			jQuery( document ).off( "ready" );
		}
	}
} );

/**
 * The ready event handler and self cleanup method
 */
function completed() {
	document.removeEventListener( "DOMContentLoaded", completed );
	window.removeEventListener( "load", completed );
	jQuery.ready();
}

jQuery.ready.promise = function( obj ) {
	if ( !readyList ) {

		readyList = jQuery.Deferred();

		// Catch cases where $(document).ready() is called
		// after the browser event has already occurred.
		// Support: IE9-10 only
		// Older IE sometimes signals "interactive" too soon
		if ( document.readyState === "complete" ||
			( document.readyState !== "loading" && !document.documentElement.doScroll ) ) {

			// Handle it asynchronously to allow scripts the opportunity to delay ready
			window.setTimeout( jQuery.ready );

		} else {

			// Use the handy event callback
			document.addEventListener( "DOMContentLoaded", completed );

			// A fallback to window.onload, that will always work
			window.addEventListener( "load", completed );
		}
	}
	return readyList.promise( obj );
};

// Kick off the DOM ready check even if the user does not
jQuery.ready.promise();




// Multifunctional method to get and set values of a collection
// The value/s can optionally be executed if it's a function
var access = function( elems, fn, key, value, chainable, emptyGet, raw ) {
	var i = 0,
		len = elems.length,
		bulk = key == null;

	// Sets many values
	if ( jQuery.type( key ) === "object" ) {
		chainable = true;
		for ( i in key ) {
			access( elems, fn, i, key[ i ], true, emptyGet, raw );
		}

	// Sets one value
	} else if ( value !== undefined ) {
		chainable = true;

		if ( !jQuery.isFunction( value ) ) {
			raw = true;
		}

		if ( bulk ) {

			// Bulk operations run against the entire set
			if ( raw ) {
				fn.call( elems, value );
				fn = null;

			// ...except when executing function values
			} else {
				bulk = fn;
				fn = function( elem, key, value ) {
					return bulk.call( jQuery( elem ), value );
				};
			}
		}

		if ( fn ) {
			for ( ; i < len; i++ ) {
				fn(
					elems[ i ], key, raw ?
					value :
					value.call( elems[ i ], i, fn( elems[ i ], key ) )
				);
			}
		}
	}

	return chainable ?
		elems :

		// Gets
		bulk ?
			fn.call( elems ) :
			len ? fn( elems[ 0 ], key ) : emptyGet;
};
var acceptData = function( owner ) {

	// Accepts only:
	//  - Node
	//    - Node.ELEMENT_NODE
	//    - Node.DOCUMENT_NODE
	//  - Object
	//    - Any
	/* jshint -W018 */
	return owner.nodeType === 1 || owner.nodeType === 9 || !( +owner.nodeType );
};




function Data() {
	this.expando = jQuery.expando + Data.uid++;
}

Data.uid = 1;

Data.prototype = {

	register: function( owner, initial ) {
		var value = initial || {};

		// If it is a node unlikely to be stringify-ed or looped over
		// use plain assignment
		if ( owner.nodeType ) {
			owner[ this.expando ] = value;

		// Otherwise secure it in a non-enumerable, non-writable property
		// configurability must be true to allow the property to be
		// deleted with the delete operator
		} else {
			Object.defineProperty( owner, this.expando, {
				value: value,
				writable: true,
				configurable: true
			} );
		}
		return owner[ this.expando ];
	},
	cache: function( owner ) {

		// We can accept data for non-element nodes in modern browsers,
		// but we should not, see #8335.
		// Always return an empty object.
		if ( !acceptData( owner ) ) {
			return {};
		}

		// Check if the owner object already has a cache
		var value = owner[ this.expando ];

		// If not, create one
		if ( !value ) {
			value = {};

			// We can accept data for non-element nodes in modern browsers,
			// but we should not, see #8335.
			// Always return an empty object.
			if ( acceptData( owner ) ) {

				// If it is a node unlikely to be stringify-ed or looped over
				// use plain assignment
				if ( owner.nodeType ) {
					owner[ this.expando ] = value;

				// Otherwise secure it in a non-enumerable property
				// configurable must be true to allow the property to be
				// deleted when data is removed
				} else {
					Object.defineProperty( owner, this.expando, {
						value: value,
						configurable: true
					} );
				}
			}
		}

		return value;
	},
	set: function( owner, data, value ) {
		var prop,
			cache = this.cache( owner );

		// Handle: [ owner, key, value ] args
		if ( typeof data === "string" ) {
			cache[ data ] = value;

		// Handle: [ owner, { properties } ] args
		} else {

			// Copy the properties one-by-one to the cache object
			for ( prop in data ) {
				cache[ prop ] = data[ prop ];
			}
		}
		return cache;
	},
	get: function( owner, key ) {
		return key === undefined ?
			this.cache( owner ) :
			owner[ this.expando ] && owner[ this.expando ][ key ];
	},
	access: function( owner, key, value ) {
		var stored;

		// In cases where either:
		//
		//   1. No key was specified
		//   2. A string key was specified, but no value provided
		//
		// Take the "read" path and allow the get method to determine
		// which value to return, respectively either:
		//
		//   1. The entire cache object
		//   2. The data stored at the key
		//
		if ( key === undefined ||
				( ( key && typeof key === "string" ) && value === undefined ) ) {

			stored = this.get( owner, key );

			return stored !== undefined ?
				stored : this.get( owner, jQuery.camelCase( key ) );
		}

		// When the key is not a string, or both a key and value
		// are specified, set or extend (existing objects) with either:
		//
		//   1. An object of properties
		//   2. A key and value
		//
		this.set( owner, key, value );

		// Since the "set" path can have two possible entry points
		// return the expected data based on which path was taken[*]
		return value !== undefined ? value : key;
	},
	remove: function( owner, key ) {
		var i, name, camel,
			cache = owner[ this.expando ];

		if ( cache === undefined ) {
			return;
		}

		if ( key === undefined ) {
			this.register( owner );

		} else {

			// Support array or space separated string of keys
			if ( jQuery.isArray( key ) ) {

				// If "name" is an array of keys...
				// When data is initially created, via ("key", "val") signature,
				// keys will be converted to camelCase.
				// Since there is no way to tell _how_ a key was added, remove
				// both plain key and camelCase key. #12786
				// This will only penalize the array argument path.
				name = key.concat( key.map( jQuery.camelCase ) );
			} else {
				camel = jQuery.camelCase( key );

				// Try the string as a key before any manipulation
				if ( key in cache ) {
					name = [ key, camel ];
				} else {

					// If a key with the spaces exists, use it.
					// Otherwise, create an array by matching non-whitespace
					name = camel;
					name = name in cache ?
						[ name ] : ( name.match( rnotwhite ) || [] );
				}
			}

			i = name.length;

			while ( i-- ) {
				delete cache[ name[ i ] ];
			}
		}

		// Remove the expando if there's no more data
		if ( key === undefined || jQuery.isEmptyObject( cache ) ) {

			// Support: Chrome <= 35-45+
			// Webkit & Blink performance suffers when deleting properties
			// from DOM nodes, so set to undefined instead
			// https://code.google.com/p/chromium/issues/detail?id=378607
			if ( owner.nodeType ) {
				owner[ this.expando ] = undefined;
			} else {
				delete owner[ this.expando ];
			}
		}
	},
	hasData: function( owner ) {
		var cache = owner[ this.expando ];
		return cache !== undefined && !jQuery.isEmptyObject( cache );
	}
};
var dataPriv = new Data();

var dataUser = new Data();



//	Implementation Summary
//
//	1. Enforce API surface and semantic compatibility with 1.9.x branch
//	2. Improve the module's maintainability by reducing the storage
//		paths to a single mechanism.
//	3. Use the same single mechanism to support "private" and "user" data.
//	4. _Never_ expose "private" data to user code (TODO: Drop _data, _removeData)
//	5. Avoid exposing implementation details on user objects (eg. expando properties)
//	6. Provide a clear path for implementation upgrade to WeakMap in 2014

var rbrace = /^(?:\{[\w\W]*\}|\[[\w\W]*\])$/,
	rmultiDash = /[A-Z]/g;

function dataAttr( elem, key, data ) {
	var name;

	// If nothing was found internally, try to fetch any
	// data from the HTML5 data-* attribute
	if ( data === undefined && elem.nodeType === 1 ) {
		name = "data-" + key.replace( rmultiDash, "-$&" ).toLowerCase();
		data = elem.getAttribute( name );

		if ( typeof data === "string" ) {
			try {
				data = data === "true" ? true :
					data === "false" ? false :
					data === "null" ? null :

					// Only convert to a number if it doesn't change the string
					+data + "" === data ? +data :
					rbrace.test( data ) ? jQuery.parseJSON( data ) :
					data;
			} catch ( e ) {}

			// Make sure we set the data so it isn't changed later
			dataUser.set( elem, key, data );
		} else {
			data = undefined;
		}
	}
	return data;
}

jQuery.extend( {
	hasData: function( elem ) {
		return dataUser.hasData( elem ) || dataPriv.hasData( elem );
	},

	data: function( elem, name, data ) {
		return dataUser.access( elem, name, data );
	},

	removeData: function( elem, name ) {
		dataUser.remove( elem, name );
	},

	// TODO: Now that all calls to _data and _removeData have been replaced
	// with direct calls to dataPriv methods, these can be deprecated.
	_data: function( elem, name, data ) {
		return dataPriv.access( elem, name, data );
	},

	_removeData: function( elem, name ) {
		dataPriv.remove( elem, name );
	}
} );

jQuery.fn.extend( {
	data: function( key, value ) {
		var i, name, data,
			elem = this[ 0 ],
			attrs = elem && elem.attributes;

		// Gets all values
		if ( key === undefined ) {
			if ( this.length ) {
				data = dataUser.get( elem );

				if ( elem.nodeType === 1 && !dataPriv.get( elem, "hasDataAttrs" ) ) {
					i = attrs.length;
					while ( i-- ) {

						// Support: IE11+
						// The attrs elements can be null (#14894)
						if ( attrs[ i ] ) {
							name = attrs[ i ].name;
							if ( name.indexOf( "data-" ) === 0 ) {
								name = jQuery.camelCase( name.slice( 5 ) );
								dataAttr( elem, name, data[ name ] );
							}
						}
					}
					dataPriv.set( elem, "hasDataAttrs", true );
				}
			}

			return data;
		}

		// Sets multiple values
		if ( typeof key === "object" ) {
			return this.each( function() {
				dataUser.set( this, key );
			} );
		}

		return access( this, function( value ) {
			var data, camelKey;

			// The calling jQuery object (element matches) is not empty
			// (and therefore has an element appears at this[ 0 ]) and the
			// `value` parameter was not undefined. An empty jQuery object
			// will result in `undefined` for elem = this[ 0 ] which will
			// throw an exception if an attempt to read a data cache is made.
			if ( elem && value === undefined ) {

				// Attempt to get data from the cache
				// with the key as-is
				data = dataUser.get( elem, key ) ||

					// Try to find dashed key if it exists (gh-2779)
					// This is for 2.2.x only
					dataUser.get( elem, key.replace( rmultiDash, "-$&" ).toLowerCase() );

				if ( data !== undefined ) {
					return data;
				}

				camelKey = jQuery.camelCase( key );

				// Attempt to get data from the cache
				// with the key camelized
				data = dataUser.get( elem, camelKey );
				if ( data !== undefined ) {
					return data;
				}

				// Attempt to "discover" the data in
				// HTML5 custom data-* attrs
				data = dataAttr( elem, camelKey, undefined );
				if ( data !== undefined ) {
					return data;
				}

				// We tried really hard, but the data doesn't exist.
				return;
			}

			// Set the data...
			camelKey = jQuery.camelCase( key );
			this.each( function() {

				// First, attempt to store a copy or reference of any
				// data that might've been store with a camelCased key.
				var data = dataUser.get( this, camelKey );

				// For HTML5 data-* attribute interop, we have to
				// store property names with dashes in a camelCase form.
				// This might not apply to all properties...*
				dataUser.set( this, camelKey, value );

				// *... In the case of properties that might _actually_
				// have dashes, we need to also store a copy of that
				// unchanged property.
				if ( key.indexOf( "-" ) > -1 && data !== undefined ) {
					dataUser.set( this, key, value );
				}
			} );
		}, null, value, arguments.length > 1, null, true );
	},

	removeData: function( key ) {
		return this.each( function() {
			dataUser.remove( this, key );
		} );
	}
} );


jQuery.extend( {
	queue: function( elem, type, data ) {
		var queue;

		if ( elem ) {
			type = ( type || "fx" ) + "queue";
			queue = dataPriv.get( elem, type );

			// Speed up dequeue by getting out quickly if this is just a lookup
			if ( data ) {
				if ( !queue || jQuery.isArray( data ) ) {
					queue = dataPriv.access( elem, type, jQuery.makeArray( data ) );
				} else {
					queue.push( data );
				}
			}
			return queue || [];
		}
	},

	dequeue: function( elem, type ) {
		type = type || "fx";

		var queue = jQuery.queue( elem, type ),
			startLength = queue.length,
			fn = queue.shift(),
			hooks = jQuery._queueHooks( elem, type ),
			next = function() {
				jQuery.dequeue( elem, type );
			};

		// If the fx queue is dequeued, always remove the progress sentinel
		if ( fn === "inprogress" ) {
			fn = queue.shift();
			startLength--;
		}

		if ( fn ) {

			// Add a progress sentinel to prevent the fx queue from being
			// automatically dequeued
			if ( type === "fx" ) {
				queue.unshift( "inprogress" );
			}

			// Clear up the last queue stop function
			delete hooks.stop;
			fn.call( elem, next, hooks );
		}

		if ( !startLength && hooks ) {
			hooks.empty.fire();
		}
	},

	// Not public - generate a queueHooks object, or return the current one
	_queueHooks: function( elem, type ) {
		var key = type + "queueHooks";
		return dataPriv.get( elem, key ) || dataPriv.access( elem, key, {
			empty: jQuery.Callbacks( "once memory" ).add( function() {
				dataPriv.remove( elem, [ type + "queue", key ] );
			} )
		} );
	}
} );

jQuery.fn.extend( {
	queue: function( type, data ) {
		var setter = 2;

		if ( typeof type !== "string" ) {
			data = type;
			type = "fx";
			setter--;
		}

		if ( arguments.length < setter ) {
			return jQuery.queue( this[ 0 ], type );
		}

		return data === undefined ?
			this :
			this.each( function() {
				var queue = jQuery.queue( this, type, data );

				// Ensure a hooks for this queue
				jQuery._queueHooks( this, type );

				if ( type === "fx" && queue[ 0 ] !== "inprogress" ) {
					jQuery.dequeue( this, type );
				}
			} );
	},
	dequeue: function( type ) {
		return this.each( function() {
			jQuery.dequeue( this, type );
		} );
	},
	clearQueue: function( type ) {
		return this.queue( type || "fx", [] );
	},

	// Get a promise resolved when queues of a certain type
	// are emptied (fx is the type by default)
	promise: function( type, obj ) {
		var tmp,
			count = 1,
			defer = jQuery.Deferred(),
			elements = this,
			i = this.length,
			resolve = function() {
				if ( !( --count ) ) {
					defer.resolveWith( elements, [ elements ] );
				}
			};

		if ( typeof type !== "string" ) {
			obj = type;
			type = undefined;
		}
		type = type || "fx";

		while ( i-- ) {
			tmp = dataPriv.get( elements[ i ], type + "queueHooks" );
			if ( tmp && tmp.empty ) {
				count++;
				tmp.empty.add( resolve );
			}
		}
		resolve();
		return defer.promise( obj );
	}
} );
var pnum = ( /[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/ ).source;

var rcssNum = new RegExp( "^(?:([+-])=|)(" + pnum + ")([a-z%]*)$", "i" );


var cssExpand = [ "Top", "Right", "Bottom", "Left" ];

var isHidden = function( elem, el ) {

		// isHidden might be called from jQuery#filter function;
		// in that case, element will be second argument
		elem = el || elem;
		return jQuery.css( elem, "display" ) === "none" ||
			!jQuery.contains( elem.ownerDocument, elem );
	};



function adjustCSS( elem, prop, valueParts, tween ) {
	var adjusted,
		scale = 1,
		maxIterations = 20,
		currentValue = tween ?
			function() { return tween.cur(); } :
			function() { return jQuery.css( elem, prop, "" ); },
		initial = currentValue(),
		unit = valueParts && valueParts[ 3 ] || ( jQuery.cssNumber[ prop ] ? "" : "px" ),

		// Starting value computation is required for potential unit mismatches
		initialInUnit = ( jQuery.cssNumber[ prop ] || unit !== "px" && +initial ) &&
			rcssNum.exec( jQuery.css( elem, prop ) );

	if ( initialInUnit && initialInUnit[ 3 ] !== unit ) {

		// Trust units reported by jQuery.css
		unit = unit || initialInUnit[ 3 ];

		// Make sure we update the tween properties later on
		valueParts = valueParts || [];

		// Iteratively approximate from a nonzero starting point
		initialInUnit = +initial || 1;

		do {

			// If previous iteration zeroed out, double until we get *something*.
			// Use string for doubling so we don't accidentally see scale as unchanged below
			scale = scale || ".5";

			// Adjust and apply
			initialInUnit = initialInUnit / scale;
			jQuery.style( elem, prop, initialInUnit + unit );

		// Update scale, tolerating zero or NaN from tween.cur()
		// Break the loop if scale is unchanged or perfect, or if we've just had enough.
		} while (
			scale !== ( scale = currentValue() / initial ) && scale !== 1 && --maxIterations
		);
	}

	if ( valueParts ) {
		initialInUnit = +initialInUnit || +initial || 0;

		// Apply relative offset (+=/-=) if specified
		adjusted = valueParts[ 1 ] ?
			initialInUnit + ( valueParts[ 1 ] + 1 ) * valueParts[ 2 ] :
			+valueParts[ 2 ];
		if ( tween ) {
			tween.unit = unit;
			tween.start = initialInUnit;
			tween.end = adjusted;
		}
	}
	return adjusted;
}
var rcheckableType = ( /^(?:checkbox|radio)$/i );

var rtagName = ( /<([\w:-]+)/ );

var rscriptType = ( /^$|\/(?:java|ecma)script/i );



// We have to close these tags to support XHTML (#13200)
var wrapMap = {

	// Support: IE9
	option: [ 1, "<select multiple='multiple'>", "</select>" ],

	// XHTML parsers do not magically insert elements in the
	// same way that tag soup parsers do. So we cannot shorten
	// this by omitting <tbody> or other required elements.
	thead: [ 1, "<table>", "</table>" ],
	col: [ 2, "<table><colgroup>", "</colgroup></table>" ],
	tr: [ 2, "<table><tbody>", "</tbody></table>" ],
	td: [ 3, "<table><tbody><tr>", "</tr></tbody></table>" ],

	_default: [ 0, "", "" ]
};

// Support: IE9
wrapMap.optgroup = wrapMap.option;

wrapMap.tbody = wrapMap.tfoot = wrapMap.colgroup = wrapMap.caption = wrapMap.thead;
wrapMap.th = wrapMap.td;


function getAll( context, tag ) {

	// Support: IE9-11+
	// Use typeof to avoid zero-argument method invocation on host objects (#15151)
	var ret = typeof context.getElementsByTagName !== "undefined" ?
			context.getElementsByTagName( tag || "*" ) :
			typeof context.querySelectorAll !== "undefined" ?
				context.querySelectorAll( tag || "*" ) :
			[];

	return tag === undefined || tag && jQuery.nodeName( context, tag ) ?
		jQuery.merge( [ context ], ret ) :
		ret;
}


// Mark scripts as having already been evaluated
function setGlobalEval( elems, refElements ) {
	var i = 0,
		l = elems.length;

	for ( ; i < l; i++ ) {
		dataPriv.set(
			elems[ i ],
			"globalEval",
			!refElements || dataPriv.get( refElements[ i ], "globalEval" )
		);
	}
}


var rhtml = /<|&#?\w+;/;

function buildFragment( elems, context, scripts, selection, ignored ) {
	var elem, tmp, tag, wrap, contains, j,
		fragment = context.createDocumentFragment(),
		nodes = [],
		i = 0,
		l = elems.length;

	for ( ; i < l; i++ ) {
		elem = elems[ i ];

		if ( elem || elem === 0 ) {

			// Add nodes directly
			if ( jQuery.type( elem ) === "object" ) {

				// Support: Android<4.1, PhantomJS<2
				// push.apply(_, arraylike) throws on ancient WebKit
				jQuery.merge( nodes, elem.nodeType ? [ elem ] : elem );

			// Convert non-html into a text node
			} else if ( !rhtml.test( elem ) ) {
				nodes.push( context.createTextNode( elem ) );

			// Convert html into DOM nodes
			} else {
				tmp = tmp || fragment.appendChild( context.createElement( "div" ) );

				// Deserialize a standard representation
				tag = ( rtagName.exec( elem ) || [ "", "" ] )[ 1 ].toLowerCase();
				wrap = wrapMap[ tag ] || wrapMap._default;
				tmp.innerHTML = wrap[ 1 ] + jQuery.htmlPrefilter( elem ) + wrap[ 2 ];

				// Descend through wrappers to the right content
				j = wrap[ 0 ];
				while ( j-- ) {
					tmp = tmp.lastChild;
				}

				// Support: Android<4.1, PhantomJS<2
				// push.apply(_, arraylike) throws on ancient WebKit
				jQuery.merge( nodes, tmp.childNodes );

				// Remember the top-level container
				tmp = fragment.firstChild;

				// Ensure the created nodes are orphaned (#12392)
				tmp.textContent = "";
			}
		}
	}

	// Remove wrapper from fragment
	fragment.textContent = "";

	i = 0;
	while ( ( elem = nodes[ i++ ] ) ) {

		// Skip elements already in the context collection (trac-4087)
		if ( selection && jQuery.inArray( elem, selection ) > -1 ) {
			if ( ignored ) {
				ignored.push( elem );
			}
			continue;
		}

		contains = jQuery.contains( elem.ownerDocument, elem );

		// Append to fragment
		tmp = getAll( fragment.appendChild( elem ), "script" );

		// Preserve script evaluation history
		if ( contains ) {
			setGlobalEval( tmp );
		}

		// Capture executables
		if ( scripts ) {
			j = 0;
			while ( ( elem = tmp[ j++ ] ) ) {
				if ( rscriptType.test( elem.type || "" ) ) {
					scripts.push( elem );
				}
			}
		}
	}

	return fragment;
}


( function() {
	var fragment = document.createDocumentFragment(),
		div = fragment.appendChild( document.createElement( "div" ) ),
		input = document.createElement( "input" );

	// Support: Android 4.0-4.3, Safari<=5.1
	// Check state lost if the name is set (#11217)
	// Support: Windows Web Apps (WWA)
	// `name` and `type` must use .setAttribute for WWA (#14901)
	input.setAttribute( "type", "radio" );
	input.setAttribute( "checked", "checked" );
	input.setAttribute( "name", "t" );

	div.appendChild( input );

	// Support: Safari<=5.1, Android<4.2
	// Older WebKit doesn't clone checked state correctly in fragments
	support.checkClone = div.cloneNode( true ).cloneNode( true ).lastChild.checked;

	// Support: IE<=11+
	// Make sure textarea (and checkbox) defaultValue is properly cloned
	div.innerHTML = "<textarea>x</textarea>";
	support.noCloneChecked = !!div.cloneNode( true ).lastChild.defaultValue;
} )();


var
	rkeyEvent = /^key/,
	rmouseEvent = /^(?:mouse|pointer|contextmenu|drag|drop)|click/,
	rtypenamespace = /^([^.]*)(?:\.(.+)|)/;

function returnTrue() {
	return true;
}

function returnFalse() {
	return false;
}

// Support: IE9
// See #13393 for more info
function safeActiveElement() {
	try {
		return document.activeElement;
	} catch ( err ) { }
}

function on( elem, types, selector, data, fn, one ) {
	var origFn, type;

	// Types can be a map of types/handlers
	if ( typeof types === "object" ) {

		// ( types-Object, selector, data )
		if ( typeof selector !== "string" ) {

			// ( types-Object, data )
			data = data || selector;
			selector = undefined;
		}
		for ( type in types ) {
			on( elem, type, selector, data, types[ type ], one );
		}
		return elem;
	}

	if ( data == null && fn == null ) {

		// ( types, fn )
		fn = selector;
		data = selector = undefined;
	} else if ( fn == null ) {
		if ( typeof selector === "string" ) {

			// ( types, selector, fn )
			fn = data;
			data = undefined;
		} else {

			// ( types, data, fn )
			fn = data;
			data = selector;
			selector = undefined;
		}
	}
	if ( fn === false ) {
		fn = returnFalse;
	} else if ( !fn ) {
		return elem;
	}

	if ( one === 1 ) {
		origFn = fn;
		fn = function( event ) {

			// Can use an empty set, since event contains the info
			jQuery().off( event );
			return origFn.apply( this, arguments );
		};

		// Use same guid so caller can remove using origFn
		fn.guid = origFn.guid || ( origFn.guid = jQuery.guid++ );
	}
	return elem.each( function() {
		jQuery.event.add( this, types, fn, data, selector );
	} );
}

/*
 * Helper functions for managing events -- not part of the public interface.
 * Props to Dean Edwards' addEvent library for many of the ideas.
 */
jQuery.event = {

	global: {},

	add: function( elem, types, handler, data, selector ) {

		var handleObjIn, eventHandle, tmp,
			events, t, handleObj,
			special, handlers, type, namespaces, origType,
			elemData = dataPriv.get( elem );

		// Don't attach events to noData or text/comment nodes (but allow plain objects)
		if ( !elemData ) {
			return;
		}

		// Caller can pass in an object of custom data in lieu of the handler
		if ( handler.handler ) {
			handleObjIn = handler;
			handler = handleObjIn.handler;
			selector = handleObjIn.selector;
		}

		// Make sure that the handler has a unique ID, used to find/remove it later
		if ( !handler.guid ) {
			handler.guid = jQuery.guid++;
		}

		// Init the element's event structure and main handler, if this is the first
		if ( !( events = elemData.events ) ) {
			events = elemData.events = {};
		}
		if ( !( eventHandle = elemData.handle ) ) {
			eventHandle = elemData.handle = function( e ) {

				// Discard the second event of a jQuery.event.trigger() and
				// when an event is called after a page has unloaded
				return typeof jQuery !== "undefined" && jQuery.event.triggered !== e.type ?
					jQuery.event.dispatch.apply( elem, arguments ) : undefined;
			};
		}

		// Handle multiple events separated by a space
		types = ( types || "" ).match( rnotwhite ) || [ "" ];
		t = types.length;
		while ( t-- ) {
			tmp = rtypenamespace.exec( types[ t ] ) || [];
			type = origType = tmp[ 1 ];
			namespaces = ( tmp[ 2 ] || "" ).split( "." ).sort();

			// There *must* be a type, no attaching namespace-only handlers
			if ( !type ) {
				continue;
			}

			// If event changes its type, use the special event handlers for the changed type
			special = jQuery.event.special[ type ] || {};

			// If selector defined, determine special event api type, otherwise given type
			type = ( selector ? special.delegateType : special.bindType ) || type;

			// Update special based on newly reset type
			special = jQuery.event.special[ type ] || {};

			// handleObj is passed to all event handlers
			handleObj = jQuery.extend( {
				type: type,
				origType: origType,
				data: data,
				handler: handler,
				guid: handler.guid,
				selector: selector,
				needsContext: selector && jQuery.expr.match.needsContext.test( selector ),
				namespace: namespaces.join( "." )
			}, handleObjIn );

			// Init the event handler queue if we're the first
			if ( !( handlers = events[ type ] ) ) {
				handlers = events[ type ] = [];
				handlers.delegateCount = 0;

				// Only use addEventListener if the special events handler returns false
				if ( !special.setup ||
					special.setup.call( elem, data, namespaces, eventHandle ) === false ) {

					if ( elem.addEventListener ) {
						elem.addEventListener( type, eventHandle );
					}
				}
			}

			if ( special.add ) {
				special.add.call( elem, handleObj );

				if ( !handleObj.handler.guid ) {
					handleObj.handler.guid = handler.guid;
				}
			}

			// Add to the element's handler list, delegates in front
			if ( selector ) {
				handlers.splice( handlers.delegateCount++, 0, handleObj );
			} else {
				handlers.push( handleObj );
			}

			// Keep track of which events have ever been used, for event optimization
			jQuery.event.global[ type ] = true;
		}

	},

	// Detach an event or set of events from an element
	remove: function( elem, types, handler, selector, mappedTypes ) {

		var j, origCount, tmp,
			events, t, handleObj,
			special, handlers, type, namespaces, origType,
			elemData = dataPriv.hasData( elem ) && dataPriv.get( elem );

		if ( !elemData || !( events = elemData.events ) ) {
			return;
		}

		// Once for each type.namespace in types; type may be omitted
		types = ( types || "" ).match( rnotwhite ) || [ "" ];
		t = types.length;
		while ( t-- ) {
			tmp = rtypenamespace.exec( types[ t ] ) || [];
			type = origType = tmp[ 1 ];
			namespaces = ( tmp[ 2 ] || "" ).split( "." ).sort();

			// Unbind all events (on this namespace, if provided) for the element
			if ( !type ) {
				for ( type in events ) {
					jQuery.event.remove( elem, type + types[ t ], handler, selector, true );
				}
				continue;
			}

			special = jQuery.event.special[ type ] || {};
			type = ( selector ? special.delegateType : special.bindType ) || type;
			handlers = events[ type ] || [];
			tmp = tmp[ 2 ] &&
				new RegExp( "(^|\\.)" + namespaces.join( "\\.(?:.*\\.|)" ) + "(\\.|$)" );

			// Remove matching events
			origCount = j = handlers.length;
			while ( j-- ) {
				handleObj = handlers[ j ];

				if ( ( mappedTypes || origType === handleObj.origType ) &&
					( !handler || handler.guid === handleObj.guid ) &&
					( !tmp || tmp.test( handleObj.namespace ) ) &&
					( !selector || selector === handleObj.selector ||
						selector === "**" && handleObj.selector ) ) {
					handlers.splice( j, 1 );

					if ( handleObj.selector ) {
						handlers.delegateCount--;
					}
					if ( special.remove ) {
						special.remove.call( elem, handleObj );
					}
				}
			}

			// Remove generic event handler if we removed something and no more handlers exist
			// (avoids potential for endless recursion during removal of special event handlers)
			if ( origCount && !handlers.length ) {
				if ( !special.teardown ||
					special.teardown.call( elem, namespaces, elemData.handle ) === false ) {

					jQuery.removeEvent( elem, type, elemData.handle );
				}

				delete events[ type ];
			}
		}

		// Remove data and the expando if it's no longer used
		if ( jQuery.isEmptyObject( events ) ) {
			dataPriv.remove( elem, "handle events" );
		}
	},

	dispatch: function( event ) {

		// Make a writable jQuery.Event from the native event object
		event = jQuery.event.fix( event );

		var i, j, ret, matched, handleObj,
			handlerQueue = [],
			args = slice.call( arguments ),
			handlers = ( dataPriv.get( this, "events" ) || {} )[ event.type ] || [],
			special = jQuery.event.special[ event.type ] || {};

		// Use the fix-ed jQuery.Event rather than the (read-only) native event
		args[ 0 ] = event;
		event.delegateTarget = this;

		// Call the preDispatch hook for the mapped type, and let it bail if desired
		if ( special.preDispatch && special.preDispatch.call( this, event ) === false ) {
			return;
		}

		// Determine handlers
		handlerQueue = jQuery.event.handlers.call( this, event, handlers );

		// Run delegates first; they may want to stop propagation beneath us
		i = 0;
		while ( ( matched = handlerQueue[ i++ ] ) && !event.isPropagationStopped() ) {
			event.currentTarget = matched.elem;

			j = 0;
			while ( ( handleObj = matched.handlers[ j++ ] ) &&
				!event.isImmediatePropagationStopped() ) {

				// Triggered event must either 1) have no namespace, or 2) have namespace(s)
				// a subset or equal to those in the bound event (both can have no namespace).
				if ( !event.rnamespace || event.rnamespace.test( handleObj.namespace ) ) {

					event.handleObj = handleObj;
					event.data = handleObj.data;

					ret = ( ( jQuery.event.special[ handleObj.origType ] || {} ).handle ||
						handleObj.handler ).apply( matched.elem, args );

					if ( ret !== undefined ) {
						if ( ( event.result = ret ) === false ) {
							event.preventDefault();
							event.stopPropagation();
						}
					}
				}
			}
		}

		// Call the postDispatch hook for the mapped type
		if ( special.postDispatch ) {
			special.postDispatch.call( this, event );
		}

		return event.result;
	},

	handlers: function( event, handlers ) {
		var i, matches, sel, handleObj,
			handlerQueue = [],
			delegateCount = handlers.delegateCount,
			cur = event.target;

		// Support (at least): Chrome, IE9
		// Find delegate handlers
		// Black-hole SVG <use> instance trees (#13180)
		//
		// Support: Firefox<=42+
		// Avoid non-left-click in FF but don't block IE radio events (#3861, gh-2343)
		if ( delegateCount && cur.nodeType &&
			( event.type !== "click" || isNaN( event.button ) || event.button < 1 ) ) {

			for ( ; cur !== this; cur = cur.parentNode || this ) {

				// Don't check non-elements (#13208)
				// Don't process clicks on disabled elements (#6911, #8165, #11382, #11764)
				if ( cur.nodeType === 1 && ( cur.disabled !== true || event.type !== "click" ) ) {
					matches = [];
					for ( i = 0; i < delegateCount; i++ ) {
						handleObj = handlers[ i ];

						// Don't conflict with Object.prototype properties (#13203)
						sel = handleObj.selector + " ";

						if ( matches[ sel ] === undefined ) {
							matches[ sel ] = handleObj.needsContext ?
								jQuery( sel, this ).index( cur ) > -1 :
								jQuery.find( sel, this, null, [ cur ] ).length;
						}
						if ( matches[ sel ] ) {
							matches.push( handleObj );
						}
					}
					if ( matches.length ) {
						handlerQueue.push( { elem: cur, handlers: matches } );
					}
				}
			}
		}

		// Add the remaining (directly-bound) handlers
		if ( delegateCount < handlers.length ) {
			handlerQueue.push( { elem: this, handlers: handlers.slice( delegateCount ) } );
		}

		return handlerQueue;
	},

	// Includes some event props shared by KeyEvent and MouseEvent
	props: ( "altKey bubbles cancelable ctrlKey currentTarget detail eventPhase " +
		"metaKey relatedTarget shiftKey target timeStamp view which" ).split( " " ),

	fixHooks: {},

	keyHooks: {
		props: "char charCode key keyCode".split( " " ),
		filter: function( event, original ) {

			// Add which for key events
			if ( event.which == null ) {
				event.which = original.charCode != null ? original.charCode : original.keyCode;
			}

			return event;
		}
	},

	mouseHooks: {
		props: ( "button buttons clientX clientY offsetX offsetY pageX pageY " +
			"screenX screenY toElement" ).split( " " ),
		filter: function( event, original ) {
			var eventDoc, doc, body,
				button = original.button;

			// Calculate pageX/Y if missing and clientX/Y available
			if ( event.pageX == null && original.clientX != null ) {
				eventDoc = event.target.ownerDocument || document;
				doc = eventDoc.documentElement;
				body = eventDoc.body;

				event.pageX = original.clientX +
					( doc && doc.scrollLeft || body && body.scrollLeft || 0 ) -
					( doc && doc.clientLeft || body && body.clientLeft || 0 );
				event.pageY = original.clientY +
					( doc && doc.scrollTop  || body && body.scrollTop  || 0 ) -
					( doc && doc.clientTop  || body && body.clientTop  || 0 );
			}

			// Add which for click: 1 === left; 2 === middle; 3 === right
			// Note: button is not normalized, so don't use it
			if ( !event.which && button !== undefined ) {
				event.which = ( button & 1 ? 1 : ( button & 2 ? 3 : ( button & 4 ? 2 : 0 ) ) );
			}

			return event;
		}
	},

	fix: function( event ) {
		if ( event[ jQuery.expando ] ) {
			return event;
		}

		// Create a writable copy of the event object and normalize some properties
		var i, prop, copy,
			type = event.type,
			originalEvent = event,
			fixHook = this.fixHooks[ type ];

		if ( !fixHook ) {
			this.fixHooks[ type ] = fixHook =
				rmouseEvent.test( type ) ? this.mouseHooks :
				rkeyEvent.test( type ) ? this.keyHooks :
				{};
		}
		copy = fixHook.props ? this.props.concat( fixHook.props ) : this.props;

		event = new jQuery.Event( originalEvent );

		i = copy.length;
		while ( i-- ) {
			prop = copy[ i ];
			event[ prop ] = originalEvent[ prop ];
		}

		// Support: Cordova 2.5 (WebKit) (#13255)
		// All events should have a target; Cordova deviceready doesn't
		if ( !event.target ) {
			event.target = document;
		}

		// Support: Safari 6.0+, Chrome<28
		// Target should not be a text node (#504, #13143)
		if ( event.target.nodeType === 3 ) {
			event.target = event.target.parentNode;
		}

		return fixHook.filter ? fixHook.filter( event, originalEvent ) : event;
	},

	special: {
		load: {

			// Prevent triggered image.load events from bubbling to window.load
			noBubble: true
		},
		focus: {

			// Fire native event if possible so blur/focus sequence is correct
			trigger: function() {
				if ( this !== safeActiveElement() && this.focus ) {
					this.focus();
					return false;
				}
			},
			delegateType: "focusin"
		},
		blur: {
			trigger: function() {
				if ( this === safeActiveElement() && this.blur ) {
					this.blur();
					return false;
				}
			},
			delegateType: "focusout"
		},
		click: {

			// For checkbox, fire native event so checked state will be right
			trigger: function() {
				if ( this.type === "checkbox" && this.click && jQuery.nodeName( this, "input" ) ) {
					this.click();
					return false;
				}
			},

			// For cross-browser consistency, don't fire native .click() on links
			_default: function( event ) {
				return jQuery.nodeName( event.target, "a" );
			}
		},

		beforeunload: {
			postDispatch: function( event ) {

				// Support: Firefox 20+
				// Firefox doesn't alert if the returnValue field is not set.
				if ( event.result !== undefined && event.originalEvent ) {
					event.originalEvent.returnValue = event.result;
				}
			}
		}
	}
};

jQuery.removeEvent = function( elem, type, handle ) {

	// This "if" is needed for plain objects
	if ( elem.removeEventListener ) {
		elem.removeEventListener( type, handle );
	}
};

jQuery.Event = function( src, props ) {

	// Allow instantiation without the 'new' keyword
	if ( !( this instanceof jQuery.Event ) ) {
		return new jQuery.Event( src, props );
	}

	// Event object
	if ( src && src.type ) {
		this.originalEvent = src;
		this.type = src.type;

		// Events bubbling up the document may have been marked as prevented
		// by a handler lower down the tree; reflect the correct value.
		this.isDefaultPrevented = src.defaultPrevented ||
				src.defaultPrevented === undefined &&

				// Support: Android<4.0
				src.returnValue === false ?
			returnTrue :
			returnFalse;

	// Event type
	} else {
		this.type = src;
	}

	// Put explicitly provided properties onto the event object
	if ( props ) {
		jQuery.extend( this, props );
	}

	// Create a timestamp if incoming event doesn't have one
	this.timeStamp = src && src.timeStamp || jQuery.now();

	// Mark it as fixed
	this[ jQuery.expando ] = true;
};

// jQuery.Event is based on DOM3 Events as specified by the ECMAScript Language Binding
// http://www.w3.org/TR/2003/WD-DOM-Level-3-Events-20030331/ecma-script-binding.html
jQuery.Event.prototype = {
	constructor: jQuery.Event,
	isDefaultPrevented: returnFalse,
	isPropagationStopped: returnFalse,
	isImmediatePropagationStopped: returnFalse,

	preventDefault: function() {
		var e = this.originalEvent;

		this.isDefaultPrevented = returnTrue;

		if ( e ) {
			e.preventDefault();
		}
	},
	stopPropagation: function() {
		var e = this.originalEvent;

		this.isPropagationStopped = returnTrue;

		if ( e ) {
			e.stopPropagation();
		}
	},
	stopImmediatePropagation: function() {
		var e = this.originalEvent;

		this.isImmediatePropagationStopped = returnTrue;

		if ( e ) {
			e.stopImmediatePropagation();
		}

		this.stopPropagation();
	}
};

// Create mouseenter/leave events using mouseover/out and event-time checks
// so that event delegation works in jQuery.
// Do the same for pointerenter/pointerleave and pointerover/pointerout
//
// Support: Safari 7 only
// Safari sends mouseenter too often; see:
// https://code.google.com/p/chromium/issues/detail?id=470258
// for the description of the bug (it existed in older Chrome versions as well).
jQuery.each( {
	mouseenter: "mouseover",
	mouseleave: "mouseout",
	pointerenter: "pointerover",
	pointerleave: "pointerout"
}, function( orig, fix ) {
	jQuery.event.special[ orig ] = {
		delegateType: fix,
		bindType: fix,

		handle: function( event ) {
			var ret,
				target = this,
				related = event.relatedTarget,
				handleObj = event.handleObj;

			// For mouseenter/leave call the handler if related is outside the target.
			// NB: No relatedTarget if the mouse left/entered the browser window
			if ( !related || ( related !== target && !jQuery.contains( target, related ) ) ) {
				event.type = handleObj.origType;
				ret = handleObj.handler.apply( this, arguments );
				event.type = fix;
			}
			return ret;
		}
	};
} );

jQuery.fn.extend( {
	on: function( types, selector, data, fn ) {
		return on( this, types, selector, data, fn );
	},
	one: function( types, selector, data, fn ) {
		return on( this, types, selector, data, fn, 1 );
	},
	off: function( types, selector, fn ) {
		var handleObj, type;
		if ( types && types.preventDefault && types.handleObj ) {

			// ( event )  dispatched jQuery.Event
			handleObj = types.handleObj;
			jQuery( types.delegateTarget ).off(
				handleObj.namespace ?
					handleObj.origType + "." + handleObj.namespace :
					handleObj.origType,
				handleObj.selector,
				handleObj.handler
			);
			return this;
		}
		if ( typeof types === "object" ) {

			// ( types-object [, selector] )
			for ( type in types ) {
				this.off( type, selector, types[ type ] );
			}
			return this;
		}
		if ( selector === false || typeof selector === "function" ) {

			// ( types [, fn] )
			fn = selector;
			selector = undefined;
		}
		if ( fn === false ) {
			fn = returnFalse;
		}
		return this.each( function() {
			jQuery.event.remove( this, types, fn, selector );
		} );
	}
} );


var
	rxhtmlTag = /<(?!area|br|col|embed|hr|img|input|link|meta|param)(([\w:-]+)[^>]*)\/>/gi,

	// Support: IE 10-11, Edge 10240+
	// In IE/Edge using regex groups here causes severe slowdowns.
	// See https://connect.microsoft.com/IE/feedback/details/1736512/
	rnoInnerhtml = /<script|<style|<link/i,

	// checked="checked" or checked
	rchecked = /checked\s*(?:[^=]|=\s*.checked.)/i,
	rscriptTypeMasked = /^true\/(.*)/,
	rcleanScript = /^\s*<!(?:\[CDATA\[|--)|(?:\]\]|--)>\s*$/g;

// Manipulating tables requires a tbody
function manipulationTarget( elem, content ) {
	return jQuery.nodeName( elem, "table" ) &&
		jQuery.nodeName( content.nodeType !== 11 ? content : content.firstChild, "tr" ) ?

		elem.getElementsByTagName( "tbody" )[ 0 ] ||
			elem.appendChild( elem.ownerDocument.createElement( "tbody" ) ) :
		elem;
}

// Replace/restore the type attribute of script elements for safe DOM manipulation
function disableScript( elem ) {
	elem.type = ( elem.getAttribute( "type" ) !== null ) + "/" + elem.type;
	return elem;
}
function restoreScript( elem ) {
	var match = rscriptTypeMasked.exec( elem.type );

	if ( match ) {
		elem.type = match[ 1 ];
	} else {
		elem.removeAttribute( "type" );
	}

	return elem;
}

function cloneCopyEvent( src, dest ) {
	var i, l, type, pdataOld, pdataCur, udataOld, udataCur, events;

	if ( dest.nodeType !== 1 ) {
		return;
	}

	// 1. Copy private data: events, handlers, etc.
	if ( dataPriv.hasData( src ) ) {
		pdataOld = dataPriv.access( src );
		pdataCur = dataPriv.set( dest, pdataOld );
		events = pdataOld.events;

		if ( events ) {
			delete pdataCur.handle;
			pdataCur.events = {};

			for ( type in events ) {
				for ( i = 0, l = events[ type ].length; i < l; i++ ) {
					jQuery.event.add( dest, type, events[ type ][ i ] );
				}
			}
		}
	}

	// 2. Copy user data
	if ( dataUser.hasData( src ) ) {
		udataOld = dataUser.access( src );
		udataCur = jQuery.extend( {}, udataOld );

		dataUser.set( dest, udataCur );
	}
}

// Fix IE bugs, see support tests
function fixInput( src, dest ) {
	var nodeName = dest.nodeName.toLowerCase();

	// Fails to persist the checked state of a cloned checkbox or radio button.
	if ( nodeName === "input" && rcheckableType.test( src.type ) ) {
		dest.checked = src.checked;

	// Fails to return the selected option to the default selected state when cloning options
	} else if ( nodeName === "input" || nodeName === "textarea" ) {
		dest.defaultValue = src.defaultValue;
	}
}

function domManip( collection, args, callback, ignored ) {

	// Flatten any nested arrays
	args = concat.apply( [], args );

	var fragment, first, scripts, hasScripts, node, doc,
		i = 0,
		l = collection.length,
		iNoClone = l - 1,
		value = args[ 0 ],
		isFunction = jQuery.isFunction( value );

	// We can't cloneNode fragments that contain checked, in WebKit
	if ( isFunction ||
			( l > 1 && typeof value === "string" &&
				!support.checkClone && rchecked.test( value ) ) ) {
		return collection.each( function( index ) {
			var self = collection.eq( index );
			if ( isFunction ) {
				args[ 0 ] = value.call( this, index, self.html() );
			}
			domManip( self, args, callback, ignored );
		} );
	}

	if ( l ) {
		fragment = buildFragment( args, collection[ 0 ].ownerDocument, false, collection, ignored );
		first = fragment.firstChild;

		if ( fragment.childNodes.length === 1 ) {
			fragment = first;
		}

		// Require either new content or an interest in ignored elements to invoke the callback
		if ( first || ignored ) {
			scripts = jQuery.map( getAll( fragment, "script" ), disableScript );
			hasScripts = scripts.length;

			// Use the original fragment for the last item
			// instead of the first because it can end up
			// being emptied incorrectly in certain situations (#8070).
			for ( ; i < l; i++ ) {
				node = fragment;

				if ( i !== iNoClone ) {
					node = jQuery.clone( node, true, true );

					// Keep references to cloned scripts for later restoration
					if ( hasScripts ) {

						// Support: Android<4.1, PhantomJS<2
						// push.apply(_, arraylike) throws on ancient WebKit
						jQuery.merge( scripts, getAll( node, "script" ) );
					}
				}

				callback.call( collection[ i ], node, i );
			}

			if ( hasScripts ) {
				doc = scripts[ scripts.length - 1 ].ownerDocument;

				// Reenable scripts
				jQuery.map( scripts, restoreScript );

				// Evaluate executable scripts on first document insertion
				for ( i = 0; i < hasScripts; i++ ) {
					node = scripts[ i ];
					if ( rscriptType.test( node.type || "" ) &&
						!dataPriv.access( node, "globalEval" ) &&
						jQuery.contains( doc, node ) ) {

						if ( node.src ) {

							// Optional AJAX dependency, but won't run scripts if not present
							if ( jQuery._evalUrl ) {
								jQuery._evalUrl( node.src );
							}
						} else {
							jQuery.globalEval( node.textContent.replace( rcleanScript, "" ) );
						}
					}
				}
			}
		}
	}

	return collection;
}

function remove( elem, selector, keepData ) {
	var node,
		nodes = selector ? jQuery.filter( selector, elem ) : elem,
		i = 0;

	for ( ; ( node = nodes[ i ] ) != null; i++ ) {
		if ( !keepData && node.nodeType === 1 ) {
			jQuery.cleanData( getAll( node ) );
		}

		if ( node.parentNode ) {
			if ( keepData && jQuery.contains( node.ownerDocument, node ) ) {
				setGlobalEval( getAll( node, "script" ) );
			}
			node.parentNode.removeChild( node );
		}
	}

	return elem;
}

jQuery.extend( {
	htmlPrefilter: function( html ) {
		return html.replace( rxhtmlTag, "<$1></$2>" );
	},

	clone: function( elem, dataAndEvents, deepDataAndEvents ) {
		var i, l, srcElements, destElements,
			clone = elem.cloneNode( true ),
			inPage = jQuery.contains( elem.ownerDocument, elem );

		// Fix IE cloning issues
		if ( !support.noCloneChecked && ( elem.nodeType === 1 || elem.nodeType === 11 ) &&
				!jQuery.isXMLDoc( elem ) ) {

			// We eschew Sizzle here for performance reasons: http://jsperf.com/getall-vs-sizzle/2
			destElements = getAll( clone );
			srcElements = getAll( elem );

			for ( i = 0, l = srcElements.length; i < l; i++ ) {
				fixInput( srcElements[ i ], destElements[ i ] );
			}
		}

		// Copy the events from the original to the clone
		if ( dataAndEvents ) {
			if ( deepDataAndEvents ) {
				srcElements = srcElements || getAll( elem );
				destElements = destElements || getAll( clone );

				for ( i = 0, l = srcElements.length; i < l; i++ ) {
					cloneCopyEvent( srcElements[ i ], destElements[ i ] );
				}
			} else {
				cloneCopyEvent( elem, clone );
			}
		}

		// Preserve script evaluation history
		destElements = getAll( clone, "script" );
		if ( destElements.length > 0 ) {
			setGlobalEval( destElements, !inPage && getAll( elem, "script" ) );
		}

		// Return the cloned set
		return clone;
	},

	cleanData: function( elems ) {
		var data, elem, type,
			special = jQuery.event.special,
			i = 0;

		for ( ; ( elem = elems[ i ] ) !== undefined; i++ ) {
			if ( acceptData( elem ) ) {
				if ( ( data = elem[ dataPriv.expando ] ) ) {
					if ( data.events ) {
						for ( type in data.events ) {
							if ( special[ type ] ) {
								jQuery.event.remove( elem, type );

							// This is a shortcut to avoid jQuery.event.remove's overhead
							} else {
								jQuery.removeEvent( elem, type, data.handle );
							}
						}
					}

					// Support: Chrome <= 35-45+
					// Assign undefined instead of using delete, see Data#remove
					elem[ dataPriv.expando ] = undefined;
				}
				if ( elem[ dataUser.expando ] ) {

					// Support: Chrome <= 35-45+
					// Assign undefined instead of using delete, see Data#remove
					elem[ dataUser.expando ] = undefined;
				}
			}
		}
	}
} );

jQuery.fn.extend( {

	// Keep domManip exposed until 3.0 (gh-2225)
	domManip: domManip,

	detach: function( selector ) {
		return remove( this, selector, true );
	},

	remove: function( selector ) {
		return remove( this, selector );
	},

	text: function( value ) {
		return access( this, function( value ) {
			return value === undefined ?
				jQuery.text( this ) :
				this.empty().each( function() {
					if ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {
						this.textContent = value;
					}
				} );
		}, null, value, arguments.length );
	},

	append: function() {
		return domManip( this, arguments, function( elem ) {
			if ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {
				var target = manipulationTarget( this, elem );
				target.appendChild( elem );
			}
		} );
	},

	prepend: function() {
		return domManip( this, arguments, function( elem ) {
			if ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {
				var target = manipulationTarget( this, elem );
				target.insertBefore( elem, target.firstChild );
			}
		} );
	},

	before: function() {
		return domManip( this, arguments, function( elem ) {
			if ( this.parentNode ) {
				this.parentNode.insertBefore( elem, this );
			}
		} );
	},

	after: function() {
		return domManip( this, arguments, function( elem ) {
			if ( this.parentNode ) {
				this.parentNode.insertBefore( elem, this.nextSibling );
			}
		} );
	},

	empty: function() {
		var elem,
			i = 0;

		for ( ; ( elem = this[ i ] ) != null; i++ ) {
			if ( elem.nodeType === 1 ) {

				// Prevent memory leaks
				jQuery.cleanData( getAll( elem, false ) );

				// Remove any remaining nodes
				elem.textContent = "";
			}
		}

		return this;
	},

	clone: function( dataAndEvents, deepDataAndEvents ) {
		dataAndEvents = dataAndEvents == null ? false : dataAndEvents;
		deepDataAndEvents = deepDataAndEvents == null ? dataAndEvents : deepDataAndEvents;

		return this.map( function() {
			return jQuery.clone( this, dataAndEvents, deepDataAndEvents );
		} );
	},

	html: function( value ) {
		return access( this, function( value ) {
			var elem = this[ 0 ] || {},
				i = 0,
				l = this.length;

			if ( value === undefined && elem.nodeType === 1 ) {
				return elem.innerHTML;
			}

			// See if we can take a shortcut and just use innerHTML
			if ( typeof value === "string" && !rnoInnerhtml.test( value ) &&
				!wrapMap[ ( rtagName.exec( value ) || [ "", "" ] )[ 1 ].toLowerCase() ] ) {

				value = jQuery.htmlPrefilter( value );

				try {
					for ( ; i < l; i++ ) {
						elem = this[ i ] || {};

						// Remove element nodes and prevent memory leaks
						if ( elem.nodeType === 1 ) {
							jQuery.cleanData( getAll( elem, false ) );
							elem.innerHTML = value;
						}
					}

					elem = 0;

				// If using innerHTML throws an exception, use the fallback method
				} catch ( e ) {}
			}

			if ( elem ) {
				this.empty().append( value );
			}
		}, null, value, arguments.length );
	},

	replaceWith: function() {
		var ignored = [];

		// Make the changes, replacing each non-ignored context element with the new content
		return domManip( this, arguments, function( elem ) {
			var parent = this.parentNode;

			if ( jQuery.inArray( this, ignored ) < 0 ) {
				jQuery.cleanData( getAll( this ) );
				if ( parent ) {
					parent.replaceChild( elem, this );
				}
			}

		// Force callback invocation
		}, ignored );
	}
} );

jQuery.each( {
	appendTo: "append",
	prependTo: "prepend",
	insertBefore: "before",
	insertAfter: "after",
	replaceAll: "replaceWith"
}, function( name, original ) {
	jQuery.fn[ name ] = function( selector ) {
		var elems,
			ret = [],
			insert = jQuery( selector ),
			last = insert.length - 1,
			i = 0;

		for ( ; i <= last; i++ ) {
			elems = i === last ? this : this.clone( true );
			jQuery( insert[ i ] )[ original ]( elems );

			// Support: QtWebKit
			// .get() because push.apply(_, arraylike) throws
			push.apply( ret, elems.get() );
		}

		return this.pushStack( ret );
	};
} );


var iframe,
	elemdisplay = {

		// Support: Firefox
		// We have to pre-define these values for FF (#10227)
		HTML: "block",
		BODY: "block"
	};

/**
 * Retrieve the actual display of a element
 * @param {String} name nodeName of the element
 * @param {Object} doc Document object
 */

// Called only from within defaultDisplay
function actualDisplay( name, doc ) {
	var elem = jQuery( doc.createElement( name ) ).appendTo( doc.body ),

		display = jQuery.css( elem[ 0 ], "display" );

	// We don't have any data stored on the element,
	// so use "detach" method as fast way to get rid of the element
	elem.detach();

	return display;
}

/**
 * Try to determine the default display value of an element
 * @param {String} nodeName
 */
function defaultDisplay( nodeName ) {
	var doc = document,
		display = elemdisplay[ nodeName ];

	if ( !display ) {
		display = actualDisplay( nodeName, doc );

		// If the simple way fails, read from inside an iframe
		if ( display === "none" || !display ) {

			// Use the already-created iframe if possible
			iframe = ( iframe || jQuery( "<iframe frameborder='0' width='0' height='0'/>" ) )
				.appendTo( doc.documentElement );

			// Always write a new HTML skeleton so Webkit and Firefox don't choke on reuse
			doc = iframe[ 0 ].contentDocument;

			// Support: IE
			doc.write();
			doc.close();

			display = actualDisplay( nodeName, doc );
			iframe.detach();
		}

		// Store the correct default display
		elemdisplay[ nodeName ] = display;
	}

	return display;
}
var rmargin = ( /^margin/ );

var rnumnonpx = new RegExp( "^(" + pnum + ")(?!px)[a-z%]+$", "i" );

var getStyles = function( elem ) {

		// Support: IE<=11+, Firefox<=30+ (#15098, #14150)
		// IE throws on elements created in popups
		// FF meanwhile throws on frame elements through "defaultView.getComputedStyle"
		var view = elem.ownerDocument.defaultView;

		if ( !view || !view.opener ) {
			view = window;
		}

		return view.getComputedStyle( elem );
	};

var swap = function( elem, options, callback, args ) {
	var ret, name,
		old = {};

	// Remember the old values, and insert the new ones
	for ( name in options ) {
		old[ name ] = elem.style[ name ];
		elem.style[ name ] = options[ name ];
	}

	ret = callback.apply( elem, args || [] );

	// Revert the old values
	for ( name in options ) {
		elem.style[ name ] = old[ name ];
	}

	return ret;
};


var documentElement = document.documentElement;



( function() {
	var pixelPositionVal, boxSizingReliableVal, pixelMarginRightVal, reliableMarginLeftVal,
		container = document.createElement( "div" ),
		div = document.createElement( "div" );

	// Finish early in limited (non-browser) environments
	if ( !div.style ) {
		return;
	}

	// Support: IE9-11+
	// Style of cloned element affects source element cloned (#8908)
	div.style.backgroundClip = "content-box";
	div.cloneNode( true ).style.backgroundClip = "";
	support.clearCloneStyle = div.style.backgroundClip === "content-box";

	container.style.cssText = "border:0;width:8px;height:0;top:0;left:-9999px;" +
		"padding:0;margin-top:1px;position:absolute";
	container.appendChild( div );

	// Executing both pixelPosition & boxSizingReliable tests require only one layout
	// so they're executed at the same time to save the second computation.
	function computeStyleTests() {
		div.style.cssText =

			// Support: Firefox<29, Android 2.3
			// Vendor-prefix box-sizing
			"-webkit-box-sizing:border-box;-moz-box-sizing:border-box;box-sizing:border-box;" +
			"position:relative;display:block;" +
			"margin:auto;border:1px;padding:1px;" +
			"top:1%;width:50%";
		div.innerHTML = "";
		documentElement.appendChild( container );

		var divStyle = window.getComputedStyle( div );
		pixelPositionVal = divStyle.top !== "1%";
		reliableMarginLeftVal = divStyle.marginLeft === "2px";
		boxSizingReliableVal = divStyle.width === "4px";

		// Support: Android 4.0 - 4.3 only
		// Some styles come back with percentage values, even though they shouldn't
		div.style.marginRight = "50%";
		pixelMarginRightVal = divStyle.marginRight === "4px";

		documentElement.removeChild( container );
	}

	jQuery.extend( support, {
		pixelPosition: function() {

			// This test is executed only once but we still do memoizing
			// since we can use the boxSizingReliable pre-computing.
			// No need to check if the test was already performed, though.
			computeStyleTests();
			return pixelPositionVal;
		},
		boxSizingReliable: function() {
			if ( boxSizingReliableVal == null ) {
				computeStyleTests();
			}
			return boxSizingReliableVal;
		},
		pixelMarginRight: function() {

			// Support: Android 4.0-4.3
			// We're checking for boxSizingReliableVal here instead of pixelMarginRightVal
			// since that compresses better and they're computed together anyway.
			if ( boxSizingReliableVal == null ) {
				computeStyleTests();
			}
			return pixelMarginRightVal;
		},
		reliableMarginLeft: function() {

			// Support: IE <=8 only, Android 4.0 - 4.3 only, Firefox <=3 - 37
			if ( boxSizingReliableVal == null ) {
				computeStyleTests();
			}
			return reliableMarginLeftVal;
		},
		reliableMarginRight: function() {

			// Support: Android 2.3
			// Check if div with explicit width and no margin-right incorrectly
			// gets computed margin-right based on width of container. (#3333)
			// WebKit Bug 13343 - getComputedStyle returns wrong value for margin-right
			// This support function is only executed once so no memoizing is needed.
			var ret,
				marginDiv = div.appendChild( document.createElement( "div" ) );

			// Reset CSS: box-sizing; display; margin; border; padding
			marginDiv.style.cssText = div.style.cssText =

				// Support: Android 2.3
				// Vendor-prefix box-sizing
				"-webkit-box-sizing:content-box;box-sizing:content-box;" +
				"display:block;margin:0;border:0;padding:0";
			marginDiv.style.marginRight = marginDiv.style.width = "0";
			div.style.width = "1px";
			documentElement.appendChild( container );

			ret = !parseFloat( window.getComputedStyle( marginDiv ).marginRight );

			documentElement.removeChild( container );
			div.removeChild( marginDiv );

			return ret;
		}
	} );
} )();


function curCSS( elem, name, computed ) {
	var width, minWidth, maxWidth, ret,
		style = elem.style;

	computed = computed || getStyles( elem );
	ret = computed ? computed.getPropertyValue( name ) || computed[ name ] : undefined;

	// Support: Opera 12.1x only
	// Fall back to style even without computed
	// computed is undefined for elems on document fragments
	if ( ( ret === "" || ret === undefined ) && !jQuery.contains( elem.ownerDocument, elem ) ) {
		ret = jQuery.style( elem, name );
	}

	// Support: IE9
	// getPropertyValue is only needed for .css('filter') (#12537)
	if ( computed ) {

		// A tribute to the "awesome hack by Dean Edwards"
		// Android Browser returns percentage for some values,
		// but width seems to be reliably pixels.
		// This is against the CSSOM draft spec:
		// http://dev.w3.org/csswg/cssom/#resolved-values
		if ( !support.pixelMarginRight() && rnumnonpx.test( ret ) && rmargin.test( name ) ) {

			// Remember the original values
			width = style.width;
			minWidth = style.minWidth;
			maxWidth = style.maxWidth;

			// Put in the new values to get a computed value out
			style.minWidth = style.maxWidth = style.width = ret;
			ret = computed.width;

			// Revert the changed values
			style.width = width;
			style.minWidth = minWidth;
			style.maxWidth = maxWidth;
		}
	}

	return ret !== undefined ?

		// Support: IE9-11+
		// IE returns zIndex value as an integer.
		ret + "" :
		ret;
}


function addGetHookIf( conditionFn, hookFn ) {

	// Define the hook, we'll check on the first run if it's really needed.
	return {
		get: function() {
			if ( conditionFn() ) {

				// Hook not needed (or it's not possible to use it due
				// to missing dependency), remove it.
				delete this.get;
				return;
			}

			// Hook needed; redefine it so that the support test is not executed again.
			return ( this.get = hookFn ).apply( this, arguments );
		}
	};
}


var

	// Swappable if display is none or starts with table
	// except "table", "table-cell", or "table-caption"
	// See here for display values: https://developer.mozilla.org/en-US/docs/CSS/display
	rdisplayswap = /^(none|table(?!-c[ea]).+)/,

	cssShow = { position: "absolute", visibility: "hidden", display: "block" },
	cssNormalTransform = {
		letterSpacing: "0",
		fontWeight: "400"
	},

	cssPrefixes = [ "Webkit", "O", "Moz", "ms" ],
	emptyStyle = document.createElement( "div" ).style;

// Return a css property mapped to a potentially vendor prefixed property
function vendorPropName( name ) {

	// Shortcut for names that are not vendor prefixed
	if ( name in emptyStyle ) {
		return name;
	}

	// Check for vendor prefixed names
	var capName = name[ 0 ].toUpperCase() + name.slice( 1 ),
		i = cssPrefixes.length;

	while ( i-- ) {
		name = cssPrefixes[ i ] + capName;
		if ( name in emptyStyle ) {
			return name;
		}
	}
}

function setPositiveNumber( elem, value, subtract ) {

	// Any relative (+/-) values have already been
	// normalized at this point
	var matches = rcssNum.exec( value );
	return matches ?

		// Guard against undefined "subtract", e.g., when used as in cssHooks
		Math.max( 0, matches[ 2 ] - ( subtract || 0 ) ) + ( matches[ 3 ] || "px" ) :
		value;
}

function augmentWidthOrHeight( elem, name, extra, isBorderBox, styles ) {
	var i = extra === ( isBorderBox ? "border" : "content" ) ?

		// If we already have the right measurement, avoid augmentation
		4 :

		// Otherwise initialize for horizontal or vertical properties
		name === "width" ? 1 : 0,

		val = 0;

	for ( ; i < 4; i += 2 ) {

		// Both box models exclude margin, so add it if we want it
		if ( extra === "margin" ) {
			val += jQuery.css( elem, extra + cssExpand[ i ], true, styles );
		}

		if ( isBorderBox ) {

			// border-box includes padding, so remove it if we want content
			if ( extra === "content" ) {
				val -= jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );
			}

			// At this point, extra isn't border nor margin, so remove border
			if ( extra !== "margin" ) {
				val -= jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );
			}
		} else {

			// At this point, extra isn't content, so add padding
			val += jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );

			// At this point, extra isn't content nor padding, so add border
			if ( extra !== "padding" ) {
				val += jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );
			}
		}
	}

	return val;
}

function getWidthOrHeight( elem, name, extra ) {

	// Start with offset property, which is equivalent to the border-box value
	var valueIsBorderBox = true,
		val = name === "width" ? elem.offsetWidth : elem.offsetHeight,
		styles = getStyles( elem ),
		isBorderBox = jQuery.css( elem, "boxSizing", false, styles ) === "border-box";

	// Support: IE11 only
	// In IE 11 fullscreen elements inside of an iframe have
	// 100x too small dimensions (gh-1764).
	if ( document.msFullscreenElement && window.top !== window ) {

		// Support: IE11 only
		// Running getBoundingClientRect on a disconnected node
		// in IE throws an error.
		if ( elem.getClientRects().length ) {
			val = Math.round( elem.getBoundingClientRect()[ name ] * 100 );
		}
	}

	// Some non-html elements return undefined for offsetWidth, so check for null/undefined
	// svg - https://bugzilla.mozilla.org/show_bug.cgi?id=649285
	// MathML - https://bugzilla.mozilla.org/show_bug.cgi?id=491668
	if ( val <= 0 || val == null ) {

		// Fall back to computed then uncomputed css if necessary
		val = curCSS( elem, name, styles );
		if ( val < 0 || val == null ) {
			val = elem.style[ name ];
		}

		// Computed unit is not pixels. Stop here and return.
		if ( rnumnonpx.test( val ) ) {
			return val;
		}

		// Check for style in case a browser which returns unreliable values
		// for getComputedStyle silently falls back to the reliable elem.style
		valueIsBorderBox = isBorderBox &&
			( support.boxSizingReliable() || val === elem.style[ name ] );

		// Normalize "", auto, and prepare for extra
		val = parseFloat( val ) || 0;
	}

	// Use the active box-sizing model to add/subtract irrelevant styles
	return ( val +
		augmentWidthOrHeight(
			elem,
			name,
			extra || ( isBorderBox ? "border" : "content" ),
			valueIsBorderBox,
			styles
		)
	) + "px";
}

function showHide( elements, show ) {
	var display, elem, hidden,
		values = [],
		index = 0,
		length = elements.length;

	for ( ; index < length; index++ ) {
		elem = elements[ index ];
		if ( !elem.style ) {
			continue;
		}

		values[ index ] = dataPriv.get( elem, "olddisplay" );
		display = elem.style.display;
		if ( show ) {

			// Reset the inline display of this element to learn if it is
			// being hidden by cascaded rules or not
			if ( !values[ index ] && display === "none" ) {
				elem.style.display = "";
			}

			// Set elements which have been overridden with display: none
			// in a stylesheet to whatever the default browser style is
			// for such an element
			if ( elem.style.display === "" && isHidden( elem ) ) {
				values[ index ] = dataPriv.access(
					elem,
					"olddisplay",
					defaultDisplay( elem.nodeName )
				);
			}
		} else {
			hidden = isHidden( elem );

			if ( display !== "none" || !hidden ) {
				dataPriv.set(
					elem,
					"olddisplay",
					hidden ? display : jQuery.css( elem, "display" )
				);
			}
		}
	}

	// Set the display of most of the elements in a second loop
	// to avoid the constant reflow
	for ( index = 0; index < length; index++ ) {
		elem = elements[ index ];
		if ( !elem.style ) {
			continue;
		}
		if ( !show || elem.style.display === "none" || elem.style.display === "" ) {
			elem.style.display = show ? values[ index ] || "" : "none";
		}
	}

	return elements;
}

jQuery.extend( {

	// Add in style property hooks for overriding the default
	// behavior of getting and setting a style property
	cssHooks: {
		opacity: {
			get: function( elem, computed ) {
				if ( computed ) {

					// We should always get a number back from opacity
					var ret = curCSS( elem, "opacity" );
					return ret === "" ? "1" : ret;
				}
			}
		}
	},

	// Don't automatically add "px" to these possibly-unitless properties
	cssNumber: {
		"animationIterationCount": true,
		"columnCount": true,
		"fillOpacity": true,
		"flexGrow": true,
		"flexShrink": true,
		"fontWeight": true,
		"lineHeight": true,
		"opacity": true,
		"order": true,
		"orphans": true,
		"widows": true,
		"zIndex": true,
		"zoom": true
	},

	// Add in properties whose names you wish to fix before
	// setting or getting the value
	cssProps: {
		"float": "cssFloat"
	},

	// Get and set the style property on a DOM Node
	style: function( elem, name, value, extra ) {

		// Don't set styles on text and comment nodes
		if ( !elem || elem.nodeType === 3 || elem.nodeType === 8 || !elem.style ) {
			return;
		}

		// Make sure that we're working with the right name
		var ret, type, hooks,
			origName = jQuery.camelCase( name ),
			style = elem.style;

		name = jQuery.cssProps[ origName ] ||
			( jQuery.cssProps[ origName ] = vendorPropName( origName ) || origName );

		// Gets hook for the prefixed version, then unprefixed version
		hooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];

		// Check if we're setting a value
		if ( value !== undefined ) {
			type = typeof value;

			// Convert "+=" or "-=" to relative numbers (#7345)
			if ( type === "string" && ( ret = rcssNum.exec( value ) ) && ret[ 1 ] ) {
				value = adjustCSS( elem, name, ret );

				// Fixes bug #9237
				type = "number";
			}

			// Make sure that null and NaN values aren't set (#7116)
			if ( value == null || value !== value ) {
				return;
			}

			// If a number was passed in, add the unit (except for certain CSS properties)
			if ( type === "number" ) {
				value += ret && ret[ 3 ] || ( jQuery.cssNumber[ origName ] ? "" : "px" );
			}

			// Support: IE9-11+
			// background-* props affect original clone's values
			if ( !support.clearCloneStyle && value === "" && name.indexOf( "background" ) === 0 ) {
				style[ name ] = "inherit";
			}

			// If a hook was provided, use that value, otherwise just set the specified value
			if ( !hooks || !( "set" in hooks ) ||
				( value = hooks.set( elem, value, extra ) ) !== undefined ) {

				style[ name ] = value;
			}

		} else {

			// If a hook was provided get the non-computed value from there
			if ( hooks && "get" in hooks &&
				( ret = hooks.get( elem, false, extra ) ) !== undefined ) {

				return ret;
			}

			// Otherwise just get the value from the style object
			return style[ name ];
		}
	},

	css: function( elem, name, extra, styles ) {
		var val, num, hooks,
			origName = jQuery.camelCase( name );

		// Make sure that we're working with the right name
		name = jQuery.cssProps[ origName ] ||
			( jQuery.cssProps[ origName ] = vendorPropName( origName ) || origName );

		// Try prefixed name followed by the unprefixed name
		hooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];

		// If a hook was provided get the computed value from there
		if ( hooks && "get" in hooks ) {
			val = hooks.get( elem, true, extra );
		}

		// Otherwise, if a way to get the computed value exists, use that
		if ( val === undefined ) {
			val = curCSS( elem, name, styles );
		}

		// Convert "normal" to computed value
		if ( val === "normal" && name in cssNormalTransform ) {
			val = cssNormalTransform[ name ];
		}

		// Make numeric if forced or a qualifier was provided and val looks numeric
		if ( extra === "" || extra ) {
			num = parseFloat( val );
			return extra === true || isFinite( num ) ? num || 0 : val;
		}
		return val;
	}
} );

jQuery.each( [ "height", "width" ], function( i, name ) {
	jQuery.cssHooks[ name ] = {
		get: function( elem, computed, extra ) {
			if ( computed ) {

				// Certain elements can have dimension info if we invisibly show them
				// but it must have a current display style that would benefit
				return rdisplayswap.test( jQuery.css( elem, "display" ) ) &&
					elem.offsetWidth === 0 ?
						swap( elem, cssShow, function() {
							return getWidthOrHeight( elem, name, extra );
						} ) :
						getWidthOrHeight( elem, name, extra );
			}
		},

		set: function( elem, value, extra ) {
			var matches,
				styles = extra && getStyles( elem ),
				subtract = extra && augmentWidthOrHeight(
					elem,
					name,
					extra,
					jQuery.css( elem, "boxSizing", false, styles ) === "border-box",
					styles
				);

			// Convert to pixels if value adjustment is needed
			if ( subtract && ( matches = rcssNum.exec( value ) ) &&
				( matches[ 3 ] || "px" ) !== "px" ) {

				elem.style[ name ] = value;
				value = jQuery.css( elem, name );
			}

			return setPositiveNumber( elem, value, subtract );
		}
	};
} );

jQuery.cssHooks.marginLeft = addGetHookIf( support.reliableMarginLeft,
	function( elem, computed ) {
		if ( computed ) {
			return ( parseFloat( curCSS( elem, "marginLeft" ) ) ||
				elem.getBoundingClientRect().left -
					swap( elem, { marginLeft: 0 }, function() {
						return elem.getBoundingClientRect().left;
					} )
				) + "px";
		}
	}
);

// Support: Android 2.3
jQuery.cssHooks.marginRight = addGetHookIf( support.reliableMarginRight,
	function( elem, computed ) {
		if ( computed ) {
			return swap( elem, { "display": "inline-block" },
				curCSS, [ elem, "marginRight" ] );
		}
	}
);

// These hooks are used by animate to expand properties
jQuery.each( {
	margin: "",
	padding: "",
	border: "Width"
}, function( prefix, suffix ) {
	jQuery.cssHooks[ prefix + suffix ] = {
		expand: function( value ) {
			var i = 0,
				expanded = {},

				// Assumes a single number if not a string
				parts = typeof value === "string" ? value.split( " " ) : [ value ];

			for ( ; i < 4; i++ ) {
				expanded[ prefix + cssExpand[ i ] + suffix ] =
					parts[ i ] || parts[ i - 2 ] || parts[ 0 ];
			}

			return expanded;
		}
	};

	if ( !rmargin.test( prefix ) ) {
		jQuery.cssHooks[ prefix + suffix ].set = setPositiveNumber;
	}
} );

jQuery.fn.extend( {
	css: function( name, value ) {
		return access( this, function( elem, name, value ) {
			var styles, len,
				map = {},
				i = 0;

			if ( jQuery.isArray( name ) ) {
				styles = getStyles( elem );
				len = name.length;

				for ( ; i < len; i++ ) {
					map[ name[ i ] ] = jQuery.css( elem, name[ i ], false, styles );
				}

				return map;
			}

			return value !== undefined ?
				jQuery.style( elem, name, value ) :
				jQuery.css( elem, name );
		}, name, value, arguments.length > 1 );
	},
	show: function() {
		return showHide( this, true );
	},
	hide: function() {
		return showHide( this );
	},
	toggle: function( state ) {
		if ( typeof state === "boolean" ) {
			return state ? this.show() : this.hide();
		}

		return this.each( function() {
			if ( isHidden( this ) ) {
				jQuery( this ).show();
			} else {
				jQuery( this ).hide();
			}
		} );
	}
} );


function Tween( elem, options, prop, end, easing ) {
	return new Tween.prototype.init( elem, options, prop, end, easing );
}
jQuery.Tween = Tween;

Tween.prototype = {
	constructor: Tween,
	init: function( elem, options, prop, end, easing, unit ) {
		this.elem = elem;
		this.prop = prop;
		this.easing = easing || jQuery.easing._default;
		this.options = options;
		this.start = this.now = this.cur();
		this.end = end;
		this.unit = unit || ( jQuery.cssNumber[ prop ] ? "" : "px" );
	},
	cur: function() {
		var hooks = Tween.propHooks[ this.prop ];

		return hooks && hooks.get ?
			hooks.get( this ) :
			Tween.propHooks._default.get( this );
	},
	run: function( percent ) {
		var eased,
			hooks = Tween.propHooks[ this.prop ];

		if ( this.options.duration ) {
			this.pos = eased = jQuery.easing[ this.easing ](
				percent, this.options.duration * percent, 0, 1, this.options.duration
			);
		} else {
			this.pos = eased = percent;
		}
		this.now = ( this.end - this.start ) * eased + this.start;

		if ( this.options.step ) {
			this.options.step.call( this.elem, this.now, this );
		}

		if ( hooks && hooks.set ) {
			hooks.set( this );
		} else {
			Tween.propHooks._default.set( this );
		}
		return this;
	}
};

Tween.prototype.init.prototype = Tween.prototype;

Tween.propHooks = {
	_default: {
		get: function( tween ) {
			var result;

			// Use a property on the element directly when it is not a DOM element,
			// or when there is no matching style property that exists.
			if ( tween.elem.nodeType !== 1 ||
				tween.elem[ tween.prop ] != null && tween.elem.style[ tween.prop ] == null ) {
				return tween.elem[ tween.prop ];
			}

			// Passing an empty string as a 3rd parameter to .css will automatically
			// attempt a parseFloat and fallback to a string if the parse fails.
			// Simple values such as "10px" are parsed to Float;
			// complex values such as "rotate(1rad)" are returned as-is.
			result = jQuery.css( tween.elem, tween.prop, "" );

			// Empty strings, null, undefined and "auto" are converted to 0.
			return !result || result === "auto" ? 0 : result;
		},
		set: function( tween ) {

			// Use step hook for back compat.
			// Use cssHook if its there.
			// Use .style if available and use plain properties where available.
			if ( jQuery.fx.step[ tween.prop ] ) {
				jQuery.fx.step[ tween.prop ]( tween );
			} else if ( tween.elem.nodeType === 1 &&
				( tween.elem.style[ jQuery.cssProps[ tween.prop ] ] != null ||
					jQuery.cssHooks[ tween.prop ] ) ) {
				jQuery.style( tween.elem, tween.prop, tween.now + tween.unit );
			} else {
				tween.elem[ tween.prop ] = tween.now;
			}
		}
	}
};

// Support: IE9
// Panic based approach to setting things on disconnected nodes
Tween.propHooks.scrollTop = Tween.propHooks.scrollLeft = {
	set: function( tween ) {
		if ( tween.elem.nodeType && tween.elem.parentNode ) {
			tween.elem[ tween.prop ] = tween.now;
		}
	}
};

jQuery.easing = {
	linear: function( p ) {
		return p;
	},
	swing: function( p ) {
		return 0.5 - Math.cos( p * Math.PI ) / 2;
	},
	_default: "swing"
};

jQuery.fx = Tween.prototype.init;

// Back Compat <1.8 extension point
jQuery.fx.step = {};




var
	fxNow, timerId,
	rfxtypes = /^(?:toggle|show|hide)$/,
	rrun = /queueHooks$/;

// Animations created synchronously will run synchronously
function createFxNow() {
	window.setTimeout( function() {
		fxNow = undefined;
	} );
	return ( fxNow = jQuery.now() );
}

// Generate parameters to create a standard animation
function genFx( type, includeWidth ) {
	var which,
		i = 0,
		attrs = { height: type };

	// If we include width, step value is 1 to do all cssExpand values,
	// otherwise step value is 2 to skip over Left and Right
	includeWidth = includeWidth ? 1 : 0;
	for ( ; i < 4 ; i += 2 - includeWidth ) {
		which = cssExpand[ i ];
		attrs[ "margin" + which ] = attrs[ "padding" + which ] = type;
	}

	if ( includeWidth ) {
		attrs.opacity = attrs.width = type;
	}

	return attrs;
}

function createTween( value, prop, animation ) {
	var tween,
		collection = ( Animation.tweeners[ prop ] || [] ).concat( Animation.tweeners[ "*" ] ),
		index = 0,
		length = collection.length;
	for ( ; index < length; index++ ) {
		if ( ( tween = collection[ index ].call( animation, prop, value ) ) ) {

			// We're done with this property
			return tween;
		}
	}
}

function defaultPrefilter( elem, props, opts ) {
	/* jshint validthis: true */
	var prop, value, toggle, tween, hooks, oldfire, display, checkDisplay,
		anim = this,
		orig = {},
		style = elem.style,
		hidden = elem.nodeType && isHidden( elem ),
		dataShow = dataPriv.get( elem, "fxshow" );

	// Handle queue: false promises
	if ( !opts.queue ) {
		hooks = jQuery._queueHooks( elem, "fx" );
		if ( hooks.unqueued == null ) {
			hooks.unqueued = 0;
			oldfire = hooks.empty.fire;
			hooks.empty.fire = function() {
				if ( !hooks.unqueued ) {
					oldfire();
				}
			};
		}
		hooks.unqueued++;

		anim.always( function() {

			// Ensure the complete handler is called before this completes
			anim.always( function() {
				hooks.unqueued--;
				if ( !jQuery.queue( elem, "fx" ).length ) {
					hooks.empty.fire();
				}
			} );
		} );
	}

	// Height/width overflow pass
	if ( elem.nodeType === 1 && ( "height" in props || "width" in props ) ) {

		// Make sure that nothing sneaks out
		// Record all 3 overflow attributes because IE9-10 do not
		// change the overflow attribute when overflowX and
		// overflowY are set to the same value
		opts.overflow = [ style.overflow, style.overflowX, style.overflowY ];

		// Set display property to inline-block for height/width
		// animations on inline elements that are having width/height animated
		display = jQuery.css( elem, "display" );

		// Test default display if display is currently "none"
		checkDisplay = display === "none" ?
			dataPriv.get( elem, "olddisplay" ) || defaultDisplay( elem.nodeName ) : display;

		if ( checkDisplay === "inline" && jQuery.css( elem, "float" ) === "none" ) {
			style.display = "inline-block";
		}
	}

	if ( opts.overflow ) {
		style.overflow = "hidden";
		anim.always( function() {
			style.overflow = opts.overflow[ 0 ];
			style.overflowX = opts.overflow[ 1 ];
			style.overflowY = opts.overflow[ 2 ];
		} );
	}

	// show/hide pass
	for ( prop in props ) {
		value = props[ prop ];
		if ( rfxtypes.exec( value ) ) {
			delete props[ prop ];
			toggle = toggle || value === "toggle";
			if ( value === ( hidden ? "hide" : "show" ) ) {

				// If there is dataShow left over from a stopped hide or show
				// and we are going to proceed with show, we should pretend to be hidden
				if ( value === "show" && dataShow && dataShow[ prop ] !== undefined ) {
					hidden = true;
				} else {
					continue;
				}
			}
			orig[ prop ] = dataShow && dataShow[ prop ] || jQuery.style( elem, prop );

		// Any non-fx value stops us from restoring the original display value
		} else {
			display = undefined;
		}
	}

	if ( !jQuery.isEmptyObject( orig ) ) {
		if ( dataShow ) {
			if ( "hidden" in dataShow ) {
				hidden = dataShow.hidden;
			}
		} else {
			dataShow = dataPriv.access( elem, "fxshow", {} );
		}

		// Store state if its toggle - enables .stop().toggle() to "reverse"
		if ( toggle ) {
			dataShow.hidden = !hidden;
		}
		if ( hidden ) {
			jQuery( elem ).show();
		} else {
			anim.done( function() {
				jQuery( elem ).hide();
			} );
		}
		anim.done( function() {
			var prop;

			dataPriv.remove( elem, "fxshow" );
			for ( prop in orig ) {
				jQuery.style( elem, prop, orig[ prop ] );
			}
		} );
		for ( prop in orig ) {
			tween = createTween( hidden ? dataShow[ prop ] : 0, prop, anim );

			if ( !( prop in dataShow ) ) {
				dataShow[ prop ] = tween.start;
				if ( hidden ) {
					tween.end = tween.start;
					tween.start = prop === "width" || prop === "height" ? 1 : 0;
				}
			}
		}

	// If this is a noop like .hide().hide(), restore an overwritten display value
	} else if ( ( display === "none" ? defaultDisplay( elem.nodeName ) : display ) === "inline" ) {
		style.display = display;
	}
}

function propFilter( props, specialEasing ) {
	var index, name, easing, value, hooks;

	// camelCase, specialEasing and expand cssHook pass
	for ( index in props ) {
		name = jQuery.camelCase( index );
		easing = specialEasing[ name ];
		value = props[ index ];
		if ( jQuery.isArray( value ) ) {
			easing = value[ 1 ];
			value = props[ index ] = value[ 0 ];
		}

		if ( index !== name ) {
			props[ name ] = value;
			delete props[ index ];
		}

		hooks = jQuery.cssHooks[ name ];
		if ( hooks && "expand" in hooks ) {
			value = hooks.expand( value );
			delete props[ name ];

			// Not quite $.extend, this won't overwrite existing keys.
			// Reusing 'index' because we have the correct "name"
			for ( index in value ) {
				if ( !( index in props ) ) {
					props[ index ] = value[ index ];
					specialEasing[ index ] = easing;
				}
			}
		} else {
			specialEasing[ name ] = easing;
		}
	}
}

function Animation( elem, properties, options ) {
	var result,
		stopped,
		index = 0,
		length = Animation.prefilters.length,
		deferred = jQuery.Deferred().always( function() {

			// Don't match elem in the :animated selector
			delete tick.elem;
		} ),
		tick = function() {
			if ( stopped ) {
				return false;
			}
			var currentTime = fxNow || createFxNow(),
				remaining = Math.max( 0, animation.startTime + animation.duration - currentTime ),

				// Support: Android 2.3
				// Archaic crash bug won't allow us to use `1 - ( 0.5 || 0 )` (#12497)
				temp = remaining / animation.duration || 0,
				percent = 1 - temp,
				index = 0,
				length = animation.tweens.length;

			for ( ; index < length ; index++ ) {
				animation.tweens[ index ].run( percent );
			}

			deferred.notifyWith( elem, [ animation, percent, remaining ] );

			if ( percent < 1 && length ) {
				return remaining;
			} else {
				deferred.resolveWith( elem, [ animation ] );
				return false;
			}
		},
		animation = deferred.promise( {
			elem: elem,
			props: jQuery.extend( {}, properties ),
			opts: jQuery.extend( true, {
				specialEasing: {},
				easing: jQuery.easing._default
			}, options ),
			originalProperties: properties,
			originalOptions: options,
			startTime: fxNow || createFxNow(),
			duration: options.duration,
			tweens: [],
			createTween: function( prop, end ) {
				var tween = jQuery.Tween( elem, animation.opts, prop, end,
						animation.opts.specialEasing[ prop ] || animation.opts.easing );
				animation.tweens.push( tween );
				return tween;
			},
			stop: function( gotoEnd ) {
				var index = 0,

					// If we are going to the end, we want to run all the tweens
					// otherwise we skip this part
					length = gotoEnd ? animation.tweens.length : 0;
				if ( stopped ) {
					return this;
				}
				stopped = true;
				for ( ; index < length ; index++ ) {
					animation.tweens[ index ].run( 1 );
				}

				// Resolve when we played the last frame; otherwise, reject
				if ( gotoEnd ) {
					deferred.notifyWith( elem, [ animation, 1, 0 ] );
					deferred.resolveWith( elem, [ animation, gotoEnd ] );
				} else {
					deferred.rejectWith( elem, [ animation, gotoEnd ] );
				}
				return this;
			}
		} ),
		props = animation.props;

	propFilter( props, animation.opts.specialEasing );

	for ( ; index < length ; index++ ) {
		result = Animation.prefilters[ index ].call( animation, elem, props, animation.opts );
		if ( result ) {
			if ( jQuery.isFunction( result.stop ) ) {
				jQuery._queueHooks( animation.elem, animation.opts.queue ).stop =
					jQuery.proxy( result.stop, result );
			}
			return result;
		}
	}

	jQuery.map( props, createTween, animation );

	if ( jQuery.isFunction( animation.opts.start ) ) {
		animation.opts.start.call( elem, animation );
	}

	jQuery.fx.timer(
		jQuery.extend( tick, {
			elem: elem,
			anim: animation,
			queue: animation.opts.queue
		} )
	);

	// attach callbacks from options
	return animation.progress( animation.opts.progress )
		.done( animation.opts.done, animation.opts.complete )
		.fail( animation.opts.fail )
		.always( animation.opts.always );
}

jQuery.Animation = jQuery.extend( Animation, {
	tweeners: {
		"*": [ function( prop, value ) {
			var tween = this.createTween( prop, value );
			adjustCSS( tween.elem, prop, rcssNum.exec( value ), tween );
			return tween;
		} ]
	},

	tweener: function( props, callback ) {
		if ( jQuery.isFunction( props ) ) {
			callback = props;
			props = [ "*" ];
		} else {
			props = props.match( rnotwhite );
		}

		var prop,
			index = 0,
			length = props.length;

		for ( ; index < length ; index++ ) {
			prop = props[ index ];
			Animation.tweeners[ prop ] = Animation.tweeners[ prop ] || [];
			Animation.tweeners[ prop ].unshift( callback );
		}
	},

	prefilters: [ defaultPrefilter ],

	prefilter: function( callback, prepend ) {
		if ( prepend ) {
			Animation.prefilters.unshift( callback );
		} else {
			Animation.prefilters.push( callback );
		}
	}
} );

jQuery.speed = function( speed, easing, fn ) {
	var opt = speed && typeof speed === "object" ? jQuery.extend( {}, speed ) : {
		complete: fn || !fn && easing ||
			jQuery.isFunction( speed ) && speed,
		duration: speed,
		easing: fn && easing || easing && !jQuery.isFunction( easing ) && easing
	};

	opt.duration = jQuery.fx.off ? 0 : typeof opt.duration === "number" ?
		opt.duration : opt.duration in jQuery.fx.speeds ?
			jQuery.fx.speeds[ opt.duration ] : jQuery.fx.speeds._default;

	// Normalize opt.queue - true/undefined/null -> "fx"
	if ( opt.queue == null || opt.queue === true ) {
		opt.queue = "fx";
	}

	// Queueing
	opt.old = opt.complete;

	opt.complete = function() {
		if ( jQuery.isFunction( opt.old ) ) {
			opt.old.call( this );
		}

		if ( opt.queue ) {
			jQuery.dequeue( this, opt.queue );
		}
	};

	return opt;
};

jQuery.fn.extend( {
	fadeTo: function( speed, to, easing, callback ) {

		// Show any hidden elements after setting opacity to 0
		return this.filter( isHidden ).css( "opacity", 0 ).show()

			// Animate to the value specified
			.end().animate( { opacity: to }, speed, easing, callback );
	},
	animate: function( prop, speed, easing, callback ) {
		var empty = jQuery.isEmptyObject( prop ),
			optall = jQuery.speed( speed, easing, callback ),
			doAnimation = function() {

				// Operate on a copy of prop so per-property easing won't be lost
				var anim = Animation( this, jQuery.extend( {}, prop ), optall );

				// Empty animations, or finishing resolves immediately
				if ( empty || dataPriv.get( this, "finish" ) ) {
					anim.stop( true );
				}
			};
			doAnimation.finish = doAnimation;

		return empty || optall.queue === false ?
			this.each( doAnimation ) :
			this.queue( optall.queue, doAnimation );
	},
	stop: function( type, clearQueue, gotoEnd ) {
		var stopQueue = function( hooks ) {
			var stop = hooks.stop;
			delete hooks.stop;
			stop( gotoEnd );
		};

		if ( typeof type !== "string" ) {
			gotoEnd = clearQueue;
			clearQueue = type;
			type = undefined;
		}
		if ( clearQueue && type !== false ) {
			this.queue( type || "fx", [] );
		}

		return this.each( function() {
			var dequeue = true,
				index = type != null && type + "queueHooks",
				timers = jQuery.timers,
				data = dataPriv.get( this );

			if ( index ) {
				if ( data[ index ] && data[ index ].stop ) {
					stopQueue( data[ index ] );
				}
			} else {
				for ( index in data ) {
					if ( data[ index ] && data[ index ].stop && rrun.test( index ) ) {
						stopQueue( data[ index ] );
					}
				}
			}

			for ( index = timers.length; index--; ) {
				if ( timers[ index ].elem === this &&
					( type == null || timers[ index ].queue === type ) ) {

					timers[ index ].anim.stop( gotoEnd );
					dequeue = false;
					timers.splice( index, 1 );
				}
			}

			// Start the next in the queue if the last step wasn't forced.
			// Timers currently will call their complete callbacks, which
			// will dequeue but only if they were gotoEnd.
			if ( dequeue || !gotoEnd ) {
				jQuery.dequeue( this, type );
			}
		} );
	},
	finish: function( type ) {
		if ( type !== false ) {
			type = type || "fx";
		}
		return this.each( function() {
			var index,
				data = dataPriv.get( this ),
				queue = data[ type + "queue" ],
				hooks = data[ type + "queueHooks" ],
				timers = jQuery.timers,
				length = queue ? queue.length : 0;

			// Enable finishing flag on private data
			data.finish = true;

			// Empty the queue first
			jQuery.queue( this, type, [] );

			if ( hooks && hooks.stop ) {
				hooks.stop.call( this, true );
			}

			// Look for any active animations, and finish them
			for ( index = timers.length; index--; ) {
				if ( timers[ index ].elem === this && timers[ index ].queue === type ) {
					timers[ index ].anim.stop( true );
					timers.splice( index, 1 );
				}
			}

			// Look for any animations in the old queue and finish them
			for ( index = 0; index < length; index++ ) {
				if ( queue[ index ] && queue[ index ].finish ) {
					queue[ index ].finish.call( this );
				}
			}

			// Turn off finishing flag
			delete data.finish;
		} );
	}
} );

jQuery.each( [ "toggle", "show", "hide" ], function( i, name ) {
	var cssFn = jQuery.fn[ name ];
	jQuery.fn[ name ] = function( speed, easing, callback ) {
		return speed == null || typeof speed === "boolean" ?
			cssFn.apply( this, arguments ) :
			this.animate( genFx( name, true ), speed, easing, callback );
	};
} );

// Generate shortcuts for custom animations
jQuery.each( {
	slideDown: genFx( "show" ),
	slideUp: genFx( "hide" ),
	slideToggle: genFx( "toggle" ),
	fadeIn: { opacity: "show" },
	fadeOut: { opacity: "hide" },
	fadeToggle: { opacity: "toggle" }
}, function( name, props ) {
	jQuery.fn[ name ] = function( speed, easing, callback ) {
		return this.animate( props, speed, easing, callback );
	};
} );

jQuery.timers = [];
jQuery.fx.tick = function() {
	var timer,
		i = 0,
		timers = jQuery.timers;

	fxNow = jQuery.now();

	for ( ; i < timers.length; i++ ) {
		timer = timers[ i ];

		// Checks the timer has not already been removed
		if ( !timer() && timers[ i ] === timer ) {
			timers.splice( i--, 1 );
		}
	}

	if ( !timers.length ) {
		jQuery.fx.stop();
	}
	fxNow = undefined;
};

jQuery.fx.timer = function( timer ) {
	jQuery.timers.push( timer );
	if ( timer() ) {
		jQuery.fx.start();
	} else {
		jQuery.timers.pop();
	}
};

jQuery.fx.interval = 13;
jQuery.fx.start = function() {
	if ( !timerId ) {
		timerId = window.setInterval( jQuery.fx.tick, jQuery.fx.interval );
	}
};

jQuery.fx.stop = function() {
	window.clearInterval( timerId );

	timerId = null;
};

jQuery.fx.speeds = {
	slow: 600,
	fast: 200,

	// Default speed
	_default: 400
};


// Based off of the plugin by Clint Helfers, with permission.
// http://web.archive.org/web/20100324014747/http://blindsignals.com/index.php/2009/07/jquery-delay/
jQuery.fn.delay = function( time, type ) {
	time = jQuery.fx ? jQuery.fx.speeds[ time ] || time : time;
	type = type || "fx";

	return this.queue( type, function( next, hooks ) {
		var timeout = window.setTimeout( next, time );
		hooks.stop = function() {
			window.clearTimeout( timeout );
		};
	} );
};


( function() {
	var input = document.createElement( "input" ),
		select = document.createElement( "select" ),
		opt = select.appendChild( document.createElement( "option" ) );

	input.type = "checkbox";

	// Support: iOS<=5.1, Android<=4.2+
	// Default value for a checkbox should be "on"
	support.checkOn = input.value !== "";

	// Support: IE<=11+
	// Must access selectedIndex to make default options select
	support.optSelected = opt.selected;

	// Support: Android<=2.3
	// Options inside disabled selects are incorrectly marked as disabled
	select.disabled = true;
	support.optDisabled = !opt.disabled;

	// Support: IE<=11+
	// An input loses its value after becoming a radio
	input = document.createElement( "input" );
	input.value = "t";
	input.type = "radio";
	support.radioValue = input.value === "t";
} )();


var boolHook,
	attrHandle = jQuery.expr.attrHandle;

jQuery.fn.extend( {
	attr: function( name, value ) {
		return access( this, jQuery.attr, name, value, arguments.length > 1 );
	},

	removeAttr: function( name ) {
		return this.each( function() {
			jQuery.removeAttr( this, name );
		} );
	}
} );

jQuery.extend( {
	attr: function( elem, name, value ) {
		var ret, hooks,
			nType = elem.nodeType;

		// Don't get/set attributes on text, comment and attribute nodes
		if ( nType === 3 || nType === 8 || nType === 2 ) {
			return;
		}

		// Fallback to prop when attributes are not supported
		if ( typeof elem.getAttribute === "undefined" ) {
			return jQuery.prop( elem, name, value );
		}

		// All attributes are lowercase
		// Grab necessary hook if one is defined
		if ( nType !== 1 || !jQuery.isXMLDoc( elem ) ) {
			name = name.toLowerCase();
			hooks = jQuery.attrHooks[ name ] ||
				( jQuery.expr.match.bool.test( name ) ? boolHook : undefined );
		}

		if ( value !== undefined ) {
			if ( value === null ) {
				jQuery.removeAttr( elem, name );
				return;
			}

			if ( hooks && "set" in hooks &&
				( ret = hooks.set( elem, value, name ) ) !== undefined ) {
				return ret;
			}

			elem.setAttribute( name, value + "" );
			return value;
		}

		if ( hooks && "get" in hooks && ( ret = hooks.get( elem, name ) ) !== null ) {
			return ret;
		}

		ret = jQuery.find.attr( elem, name );

		// Non-existent attributes return null, we normalize to undefined
		return ret == null ? undefined : ret;
	},

	attrHooks: {
		type: {
			set: function( elem, value ) {
				if ( !support.radioValue && value === "radio" &&
					jQuery.nodeName( elem, "input" ) ) {
					var val = elem.value;
					elem.setAttribute( "type", value );
					if ( val ) {
						elem.value = val;
					}
					return value;
				}
			}
		}
	},

	removeAttr: function( elem, value ) {
		var name, propName,
			i = 0,
			attrNames = value && value.match( rnotwhite );

		if ( attrNames && elem.nodeType === 1 ) {
			while ( ( name = attrNames[ i++ ] ) ) {
				propName = jQuery.propFix[ name ] || name;

				// Boolean attributes get special treatment (#10870)
				if ( jQuery.expr.match.bool.test( name ) ) {

					// Set corresponding property to false
					elem[ propName ] = false;
				}

				elem.removeAttribute( name );
			}
		}
	}
} );

// Hooks for boolean attributes
boolHook = {
	set: function( elem, value, name ) {
		if ( value === false ) {

			// Remove boolean attributes when set to false
			jQuery.removeAttr( elem, name );
		} else {
			elem.setAttribute( name, name );
		}
		return name;
	}
};
jQuery.each( jQuery.expr.match.bool.source.match( /\w+/g ), function( i, name ) {
	var getter = attrHandle[ name ] || jQuery.find.attr;

	attrHandle[ name ] = function( elem, name, isXML ) {
		var ret, handle;
		if ( !isXML ) {

			// Avoid an infinite loop by temporarily removing this function from the getter
			handle = attrHandle[ name ];
			attrHandle[ name ] = ret;
			ret = getter( elem, name, isXML ) != null ?
				name.toLowerCase() :
				null;
			attrHandle[ name ] = handle;
		}
		return ret;
	};
} );




var rfocusable = /^(?:input|select|textarea|button)$/i,
	rclickable = /^(?:a|area)$/i;

jQuery.fn.extend( {
	prop: function( name, value ) {
		return access( this, jQuery.prop, name, value, arguments.length > 1 );
	},

	removeProp: function( name ) {
		return this.each( function() {
			delete this[ jQuery.propFix[ name ] || name ];
		} );
	}
} );

jQuery.extend( {
	prop: function( elem, name, value ) {
		var ret, hooks,
			nType = elem.nodeType;

		// Don't get/set properties on text, comment and attribute nodes
		if ( nType === 3 || nType === 8 || nType === 2 ) {
			return;
		}

		if ( nType !== 1 || !jQuery.isXMLDoc( elem ) ) {

			// Fix name and attach hooks
			name = jQuery.propFix[ name ] || name;
			hooks = jQuery.propHooks[ name ];
		}

		if ( value !== undefined ) {
			if ( hooks && "set" in hooks &&
				( ret = hooks.set( elem, value, name ) ) !== undefined ) {
				return ret;
			}

			return ( elem[ name ] = value );
		}

		if ( hooks && "get" in hooks && ( ret = hooks.get( elem, name ) ) !== null ) {
			return ret;
		}

		return elem[ name ];
	},

	propHooks: {
		tabIndex: {
			get: function( elem ) {

				// elem.tabIndex doesn't always return the
				// correct value when it hasn't been explicitly set
				// http://fluidproject.org/blog/2008/01/09/getting-setting-and-removing-tabindex-values-with-javascript/
				// Use proper attribute retrieval(#12072)
				var tabindex = jQuery.find.attr( elem, "tabindex" );

				return tabindex ?
					parseInt( tabindex, 10 ) :
					rfocusable.test( elem.nodeName ) ||
						rclickable.test( elem.nodeName ) && elem.href ?
							0 :
							-1;
			}
		}
	},

	propFix: {
		"for": "htmlFor",
		"class": "className"
	}
} );

// Support: IE <=11 only
// Accessing the selectedIndex property
// forces the browser to respect setting selected
// on the option
// The getter ensures a default option is selected
// when in an optgroup
if ( !support.optSelected ) {
	jQuery.propHooks.selected = {
		get: function( elem ) {
			var parent = elem.parentNode;
			if ( parent && parent.parentNode ) {
				parent.parentNode.selectedIndex;
			}
			return null;
		},
		set: function( elem ) {
			var parent = elem.parentNode;
			if ( parent ) {
				parent.selectedIndex;

				if ( parent.parentNode ) {
					parent.parentNode.selectedIndex;
				}
			}
		}
	};
}

jQuery.each( [
	"tabIndex",
	"readOnly",
	"maxLength",
	"cellSpacing",
	"cellPadding",
	"rowSpan",
	"colSpan",
	"useMap",
	"frameBorder",
	"contentEditable"
], function() {
	jQuery.propFix[ this.toLowerCase() ] = this;
} );




var rclass = /[\t\r\n\f]/g;

function getClass( elem ) {
	return elem.getAttribute && elem.getAttribute( "class" ) || "";
}

jQuery.fn.extend( {
	addClass: function( value ) {
		var classes, elem, cur, curValue, clazz, j, finalValue,
			i = 0;

		if ( jQuery.isFunction( value ) ) {
			return this.each( function( j ) {
				jQuery( this ).addClass( value.call( this, j, getClass( this ) ) );
			} );
		}

		if ( typeof value === "string" && value ) {
			classes = value.match( rnotwhite ) || [];

			while ( ( elem = this[ i++ ] ) ) {
				curValue = getClass( elem );
				cur = elem.nodeType === 1 &&
					( " " + curValue + " " ).replace( rclass, " " );

				if ( cur ) {
					j = 0;
					while ( ( clazz = classes[ j++ ] ) ) {
						if ( cur.indexOf( " " + clazz + " " ) < 0 ) {
							cur += clazz + " ";
						}
					}

					// Only assign if different to avoid unneeded rendering.
					finalValue = jQuery.trim( cur );
					if ( curValue !== finalValue ) {
						elem.setAttribute( "class", finalValue );
					}
				}
			}
		}

		return this;
	},

	removeClass: function( value ) {
		var classes, elem, cur, curValue, clazz, j, finalValue,
			i = 0;

		if ( jQuery.isFunction( value ) ) {
			return this.each( function( j ) {
				jQuery( this ).removeClass( value.call( this, j, getClass( this ) ) );
			} );
		}

		if ( !arguments.length ) {
			return this.attr( "class", "" );
		}

		if ( typeof value === "string" && value ) {
			classes = value.match( rnotwhite ) || [];

			while ( ( elem = this[ i++ ] ) ) {
				curValue = getClass( elem );

				// This expression is here for better compressibility (see addClass)
				cur = elem.nodeType === 1 &&
					( " " + curValue + " " ).replace( rclass, " " );

				if ( cur ) {
					j = 0;
					while ( ( clazz = classes[ j++ ] ) ) {

						// Remove *all* instances
						while ( cur.indexOf( " " + clazz + " " ) > -1 ) {
							cur = cur.replace( " " + clazz + " ", " " );
						}
					}

					// Only assign if different to avoid unneeded rendering.
					finalValue = jQuery.trim( cur );
					if ( curValue !== finalValue ) {
						elem.setAttribute( "class", finalValue );
					}
				}
			}
		}

		return this;
	},

	toggleClass: function( value, stateVal ) {
		var type = typeof value;

		if ( typeof stateVal === "boolean" && type === "string" ) {
			return stateVal ? this.addClass( value ) : this.removeClass( value );
		}

		if ( jQuery.isFunction( value ) ) {
			return this.each( function( i ) {
				jQuery( this ).toggleClass(
					value.call( this, i, getClass( this ), stateVal ),
					stateVal
				);
			} );
		}

		return this.each( function() {
			var className, i, self, classNames;

			if ( type === "string" ) {

				// Toggle individual class names
				i = 0;
				self = jQuery( this );
				classNames = value.match( rnotwhite ) || [];

				while ( ( className = classNames[ i++ ] ) ) {

					// Check each className given, space separated list
					if ( self.hasClass( className ) ) {
						self.removeClass( className );
					} else {
						self.addClass( className );
					}
				}

			// Toggle whole class name
			} else if ( value === undefined || type === "boolean" ) {
				className = getClass( this );
				if ( className ) {

					// Store className if set
					dataPriv.set( this, "__className__", className );
				}

				// If the element has a class name or if we're passed `false`,
				// then remove the whole classname (if there was one, the above saved it).
				// Otherwise bring back whatever was previously saved (if anything),
				// falling back to the empty string if nothing was stored.
				if ( this.setAttribute ) {
					this.setAttribute( "class",
						className || value === false ?
						"" :
						dataPriv.get( this, "__className__" ) || ""
					);
				}
			}
		} );
	},

	hasClass: function( selector ) {
		var className, elem,
			i = 0;

		className = " " + selector + " ";
		while ( ( elem = this[ i++ ] ) ) {
			if ( elem.nodeType === 1 &&
				( " " + getClass( elem ) + " " ).replace( rclass, " " )
					.indexOf( className ) > -1
			) {
				return true;
			}
		}

		return false;
	}
} );




var rreturn = /\r/g,
	rspaces = /[\x20\t\r\n\f]+/g;

jQuery.fn.extend( {
	val: function( value ) {
		var hooks, ret, isFunction,
			elem = this[ 0 ];

		if ( !arguments.length ) {
			if ( elem ) {
				hooks = jQuery.valHooks[ elem.type ] ||
					jQuery.valHooks[ elem.nodeName.toLowerCase() ];

				if ( hooks &&
					"get" in hooks &&
					( ret = hooks.get( elem, "value" ) ) !== undefined
				) {
					return ret;
				}

				ret = elem.value;

				return typeof ret === "string" ?

					// Handle most common string cases
					ret.replace( rreturn, "" ) :

					// Handle cases where value is null/undef or number
					ret == null ? "" : ret;
			}

			return;
		}

		isFunction = jQuery.isFunction( value );

		return this.each( function( i ) {
			var val;

			if ( this.nodeType !== 1 ) {
				return;
			}

			if ( isFunction ) {
				val = value.call( this, i, jQuery( this ).val() );
			} else {
				val = value;
			}

			// Treat null/undefined as ""; convert numbers to string
			if ( val == null ) {
				val = "";

			} else if ( typeof val === "number" ) {
				val += "";

			} else if ( jQuery.isArray( val ) ) {
				val = jQuery.map( val, function( value ) {
					return value == null ? "" : value + "";
				} );
			}

			hooks = jQuery.valHooks[ this.type ] || jQuery.valHooks[ this.nodeName.toLowerCase() ];

			// If set returns undefined, fall back to normal setting
			if ( !hooks || !( "set" in hooks ) || hooks.set( this, val, "value" ) === undefined ) {
				this.value = val;
			}
		} );
	}
} );

jQuery.extend( {
	valHooks: {
		option: {
			get: function( elem ) {

				var val = jQuery.find.attr( elem, "value" );
				return val != null ?
					val :

					// Support: IE10-11+
					// option.text throws exceptions (#14686, #14858)
					// Strip and collapse whitespace
					// https://html.spec.whatwg.org/#strip-and-collapse-whitespace
					jQuery.trim( jQuery.text( elem ) ).replace( rspaces, " " );
			}
		},
		select: {
			get: function( elem ) {
				var value, option,
					options = elem.options,
					index = elem.selectedIndex,
					one = elem.type === "select-one" || index < 0,
					values = one ? null : [],
					max = one ? index + 1 : options.length,
					i = index < 0 ?
						max :
						one ? index : 0;

				// Loop through all the selected options
				for ( ; i < max; i++ ) {
					option = options[ i ];

					// IE8-9 doesn't update selected after form reset (#2551)
					if ( ( option.selected || i === index ) &&

							// Don't return options that are disabled or in a disabled optgroup
							( support.optDisabled ?
								!option.disabled : option.getAttribute( "disabled" ) === null ) &&
							( !option.parentNode.disabled ||
								!jQuery.nodeName( option.parentNode, "optgroup" ) ) ) {

						// Get the specific value for the option
						value = jQuery( option ).val();

						// We don't need an array for one selects
						if ( one ) {
							return value;
						}

						// Multi-Selects return an array
						values.push( value );
					}
				}

				return values;
			},

			set: function( elem, value ) {
				var optionSet, option,
					options = elem.options,
					values = jQuery.makeArray( value ),
					i = options.length;

				while ( i-- ) {
					option = options[ i ];
					if ( option.selected =
						jQuery.inArray( jQuery.valHooks.option.get( option ), values ) > -1
					) {
						optionSet = true;
					}
				}

				// Force browsers to behave consistently when non-matching value is set
				if ( !optionSet ) {
					elem.selectedIndex = -1;
				}
				return values;
			}
		}
	}
} );

// Radios and checkboxes getter/setter
jQuery.each( [ "radio", "checkbox" ], function() {
	jQuery.valHooks[ this ] = {
		set: function( elem, value ) {
			if ( jQuery.isArray( value ) ) {
				return ( elem.checked = jQuery.inArray( jQuery( elem ).val(), value ) > -1 );
			}
		}
	};
	if ( !support.checkOn ) {
		jQuery.valHooks[ this ].get = function( elem ) {
			return elem.getAttribute( "value" ) === null ? "on" : elem.value;
		};
	}
} );




// Return jQuery for attributes-only inclusion


var rfocusMorph = /^(?:focusinfocus|focusoutblur)$/;

jQuery.extend( jQuery.event, {

	trigger: function( event, data, elem, onlyHandlers ) {

		var i, cur, tmp, bubbleType, ontype, handle, special,
			eventPath = [ elem || document ],
			type = hasOwn.call( event, "type" ) ? event.type : event,
			namespaces = hasOwn.call( event, "namespace" ) ? event.namespace.split( "." ) : [];

		cur = tmp = elem = elem || document;

		// Don't do events on text and comment nodes
		if ( elem.nodeType === 3 || elem.nodeType === 8 ) {
			return;
		}

		// focus/blur morphs to focusin/out; ensure we're not firing them right now
		if ( rfocusMorph.test( type + jQuery.event.triggered ) ) {
			return;
		}

		if ( type.indexOf( "." ) > -1 ) {

			// Namespaced trigger; create a regexp to match event type in handle()
			namespaces = type.split( "." );
			type = namespaces.shift();
			namespaces.sort();
		}
		ontype = type.indexOf( ":" ) < 0 && "on" + type;

		// Caller can pass in a jQuery.Event object, Object, or just an event type string
		event = event[ jQuery.expando ] ?
			event :
			new jQuery.Event( type, typeof event === "object" && event );

		// Trigger bitmask: & 1 for native handlers; & 2 for jQuery (always true)
		event.isTrigger = onlyHandlers ? 2 : 3;
		event.namespace = namespaces.join( "." );
		event.rnamespace = event.namespace ?
			new RegExp( "(^|\\.)" + namespaces.join( "\\.(?:.*\\.|)" ) + "(\\.|$)" ) :
			null;

		// Clean up the event in case it is being reused
		event.result = undefined;
		if ( !event.target ) {
			event.target = elem;
		}

		// Clone any incoming data and prepend the event, creating the handler arg list
		data = data == null ?
			[ event ] :
			jQuery.makeArray( data, [ event ] );

		// Allow special events to draw outside the lines
		special = jQuery.event.special[ type ] || {};
		if ( !onlyHandlers && special.trigger && special.trigger.apply( elem, data ) === false ) {
			return;
		}

		// Determine event propagation path in advance, per W3C events spec (#9951)
		// Bubble up to document, then to window; watch for a global ownerDocument var (#9724)
		if ( !onlyHandlers && !special.noBubble && !jQuery.isWindow( elem ) ) {

			bubbleType = special.delegateType || type;
			if ( !rfocusMorph.test( bubbleType + type ) ) {
				cur = cur.parentNode;
			}
			for ( ; cur; cur = cur.parentNode ) {
				eventPath.push( cur );
				tmp = cur;
			}

			// Only add window if we got to document (e.g., not plain obj or detached DOM)
			if ( tmp === ( elem.ownerDocument || document ) ) {
				eventPath.push( tmp.defaultView || tmp.parentWindow || window );
			}
		}

		// Fire handlers on the event path
		i = 0;
		while ( ( cur = eventPath[ i++ ] ) && !event.isPropagationStopped() ) {

			event.type = i > 1 ?
				bubbleType :
				special.bindType || type;

			// jQuery handler
			handle = ( dataPriv.get( cur, "events" ) || {} )[ event.type ] &&
				dataPriv.get( cur, "handle" );
			if ( handle ) {
				handle.apply( cur, data );
			}

			// Native handler
			handle = ontype && cur[ ontype ];
			if ( handle && handle.apply && acceptData( cur ) ) {
				event.result = handle.apply( cur, data );
				if ( event.result === false ) {
					event.preventDefault();
				}
			}
		}
		event.type = type;

		// If nobody prevented the default action, do it now
		if ( !onlyHandlers && !event.isDefaultPrevented() ) {

			if ( ( !special._default ||
				special._default.apply( eventPath.pop(), data ) === false ) &&
				acceptData( elem ) ) {

				// Call a native DOM method on the target with the same name name as the event.
				// Don't do default actions on window, that's where global variables be (#6170)
				if ( ontype && jQuery.isFunction( elem[ type ] ) && !jQuery.isWindow( elem ) ) {

					// Don't re-trigger an onFOO event when we call its FOO() method
					tmp = elem[ ontype ];

					if ( tmp ) {
						elem[ ontype ] = null;
					}

					// Prevent re-triggering of the same event, since we already bubbled it above
					jQuery.event.triggered = type;
					elem[ type ]();
					jQuery.event.triggered = undefined;

					if ( tmp ) {
						elem[ ontype ] = tmp;
					}
				}
			}
		}

		return event.result;
	},

	// Piggyback on a donor event to simulate a different one
	simulate: function( type, elem, event ) {
		var e = jQuery.extend(
			new jQuery.Event(),
			event,
			{
				type: type,
				isSimulated: true

				// Previously, `originalEvent: {}` was set here, so stopPropagation call
				// would not be triggered on donor event, since in our own
				// jQuery.event.stopPropagation function we had a check for existence of
				// originalEvent.stopPropagation method, so, consequently it would be a noop.
				//
				// But now, this "simulate" function is used only for events
				// for which stopPropagation() is noop, so there is no need for that anymore.
				//
				// For the 1.x branch though, guard for "click" and "submit"
				// events is still used, but was moved to jQuery.event.stopPropagation function
				// because `originalEvent` should point to the original event for the constancy
				// with other events and for more focused logic
			}
		);

		jQuery.event.trigger( e, null, elem );

		if ( e.isDefaultPrevented() ) {
			event.preventDefault();
		}
	}

} );

jQuery.fn.extend( {

	trigger: function( type, data ) {
		return this.each( function() {
			jQuery.event.trigger( type, data, this );
		} );
	},
	triggerHandler: function( type, data ) {
		var elem = this[ 0 ];
		if ( elem ) {
			return jQuery.event.trigger( type, data, elem, true );
		}
	}
} );


jQuery.each( ( "blur focus focusin focusout load resize scroll unload click dblclick " +
	"mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave " +
	"change select submit keydown keypress keyup error contextmenu" ).split( " " ),
	function( i, name ) {

	// Handle event binding
	jQuery.fn[ name ] = function( data, fn ) {
		return arguments.length > 0 ?
			this.on( name, null, data, fn ) :
			this.trigger( name );
	};
} );

jQuery.fn.extend( {
	hover: function( fnOver, fnOut ) {
		return this.mouseenter( fnOver ).mouseleave( fnOut || fnOver );
	}
} );




support.focusin = "onfocusin" in window;


// Support: Firefox
// Firefox doesn't have focus(in | out) events
// Related ticket - https://bugzilla.mozilla.org/show_bug.cgi?id=687787
//
// Support: Chrome, Safari
// focus(in | out) events fire after focus & blur events,
// which is spec violation - http://www.w3.org/TR/DOM-Level-3-Events/#events-focusevent-event-order
// Related ticket - https://code.google.com/p/chromium/issues/detail?id=449857
if ( !support.focusin ) {
	jQuery.each( { focus: "focusin", blur: "focusout" }, function( orig, fix ) {

		// Attach a single capturing handler on the document while someone wants focusin/focusout
		var handler = function( event ) {
			jQuery.event.simulate( fix, event.target, jQuery.event.fix( event ) );
		};

		jQuery.event.special[ fix ] = {
			setup: function() {
				var doc = this.ownerDocument || this,
					attaches = dataPriv.access( doc, fix );

				if ( !attaches ) {
					doc.addEventListener( orig, handler, true );
				}
				dataPriv.access( doc, fix, ( attaches || 0 ) + 1 );
			},
			teardown: function() {
				var doc = this.ownerDocument || this,
					attaches = dataPriv.access( doc, fix ) - 1;

				if ( !attaches ) {
					doc.removeEventListener( orig, handler, true );
					dataPriv.remove( doc, fix );

				} else {
					dataPriv.access( doc, fix, attaches );
				}
			}
		};
	} );
}
var location = window.location;

var nonce = jQuery.now();

var rquery = ( /\?/ );



// Support: Android 2.3
// Workaround failure to string-cast null input
jQuery.parseJSON = function( data ) {
	return JSON.parse( data + "" );
};


// Cross-browser xml parsing
jQuery.parseXML = function( data ) {
	var xml;
	if ( !data || typeof data !== "string" ) {
		return null;
	}

	// Support: IE9
	try {
		xml = ( new window.DOMParser() ).parseFromString( data, "text/xml" );
	} catch ( e ) {
		xml = undefined;
	}

	if ( !xml || xml.getElementsByTagName( "parsererror" ).length ) {
		jQuery.error( "Invalid XML: " + data );
	}
	return xml;
};


var
	rhash = /#.*$/,
	rts = /([?&])_=[^&]*/,
	rheaders = /^(.*?):[ \t]*([^\r\n]*)$/mg,

	// #7653, #8125, #8152: local protocol detection
	rlocalProtocol = /^(?:about|app|app-storage|.+-extension|file|res|widget):$/,
	rnoContent = /^(?:GET|HEAD)$/,
	rprotocol = /^\/\//,

	/* Prefilters
	 * 1) They are useful to introduce custom dataTypes (see ajax/jsonp.js for an example)
	 * 2) These are called:
	 *    - BEFORE asking for a transport
	 *    - AFTER param serialization (s.data is a string if s.processData is true)
	 * 3) key is the dataType
	 * 4) the catchall symbol "*" can be used
	 * 5) execution will start with transport dataType and THEN continue down to "*" if needed
	 */
	prefilters = {},

	/* Transports bindings
	 * 1) key is the dataType
	 * 2) the catchall symbol "*" can be used
	 * 3) selection will start with transport dataType and THEN go to "*" if needed
	 */
	transports = {},

	// Avoid comment-prolog char sequence (#10098); must appease lint and evade compression
	allTypes = "*/".concat( "*" ),

	// Anchor tag for parsing the document origin
	originAnchor = document.createElement( "a" );
	originAnchor.href = location.href;

// Base "constructor" for jQuery.ajaxPrefilter and jQuery.ajaxTransport
function addToPrefiltersOrTransports( structure ) {

	// dataTypeExpression is optional and defaults to "*"
	return function( dataTypeExpression, func ) {

		if ( typeof dataTypeExpression !== "string" ) {
			func = dataTypeExpression;
			dataTypeExpression = "*";
		}

		var dataType,
			i = 0,
			dataTypes = dataTypeExpression.toLowerCase().match( rnotwhite ) || [];

		if ( jQuery.isFunction( func ) ) {

			// For each dataType in the dataTypeExpression
			while ( ( dataType = dataTypes[ i++ ] ) ) {

				// Prepend if requested
				if ( dataType[ 0 ] === "+" ) {
					dataType = dataType.slice( 1 ) || "*";
					( structure[ dataType ] = structure[ dataType ] || [] ).unshift( func );

				// Otherwise append
				} else {
					( structure[ dataType ] = structure[ dataType ] || [] ).push( func );
				}
			}
		}
	};
}

// Base inspection function for prefilters and transports
function inspectPrefiltersOrTransports( structure, options, originalOptions, jqXHR ) {

	var inspected = {},
		seekingTransport = ( structure === transports );

	function inspect( dataType ) {
		var selected;
		inspected[ dataType ] = true;
		jQuery.each( structure[ dataType ] || [], function( _, prefilterOrFactory ) {
			var dataTypeOrTransport = prefilterOrFactory( options, originalOptions, jqXHR );
			if ( typeof dataTypeOrTransport === "string" &&
				!seekingTransport && !inspected[ dataTypeOrTransport ] ) {

				options.dataTypes.unshift( dataTypeOrTransport );
				inspect( dataTypeOrTransport );
				return false;
			} else if ( seekingTransport ) {
				return !( selected = dataTypeOrTransport );
			}
		} );
		return selected;
	}

	return inspect( options.dataTypes[ 0 ] ) || !inspected[ "*" ] && inspect( "*" );
}

// A special extend for ajax options
// that takes "flat" options (not to be deep extended)
// Fixes #9887
function ajaxExtend( target, src ) {
	var key, deep,
		flatOptions = jQuery.ajaxSettings.flatOptions || {};

	for ( key in src ) {
		if ( src[ key ] !== undefined ) {
			( flatOptions[ key ] ? target : ( deep || ( deep = {} ) ) )[ key ] = src[ key ];
		}
	}
	if ( deep ) {
		jQuery.extend( true, target, deep );
	}

	return target;
}

/* Handles responses to an ajax request:
 * - finds the right dataType (mediates between content-type and expected dataType)
 * - returns the corresponding response
 */
function ajaxHandleResponses( s, jqXHR, responses ) {

	var ct, type, finalDataType, firstDataType,
		contents = s.contents,
		dataTypes = s.dataTypes;

	// Remove auto dataType and get content-type in the process
	while ( dataTypes[ 0 ] === "*" ) {
		dataTypes.shift();
		if ( ct === undefined ) {
			ct = s.mimeType || jqXHR.getResponseHeader( "Content-Type" );
		}
	}

	// Check if we're dealing with a known content-type
	if ( ct ) {
		for ( type in contents ) {
			if ( contents[ type ] && contents[ type ].test( ct ) ) {
				dataTypes.unshift( type );
				break;
			}
		}
	}

	// Check to see if we have a response for the expected dataType
	if ( dataTypes[ 0 ] in responses ) {
		finalDataType = dataTypes[ 0 ];
	} else {

		// Try convertible dataTypes
		for ( type in responses ) {
			if ( !dataTypes[ 0 ] || s.converters[ type + " " + dataTypes[ 0 ] ] ) {
				finalDataType = type;
				break;
			}
			if ( !firstDataType ) {
				firstDataType = type;
			}
		}

		// Or just use first one
		finalDataType = finalDataType || firstDataType;
	}

	// If we found a dataType
	// We add the dataType to the list if needed
	// and return the corresponding response
	if ( finalDataType ) {
		if ( finalDataType !== dataTypes[ 0 ] ) {
			dataTypes.unshift( finalDataType );
		}
		return responses[ finalDataType ];
	}
}

/* Chain conversions given the request and the original response
 * Also sets the responseXXX fields on the jqXHR instance
 */
function ajaxConvert( s, response, jqXHR, isSuccess ) {
	var conv2, current, conv, tmp, prev,
		converters = {},

		// Work with a copy of dataTypes in case we need to modify it for conversion
		dataTypes = s.dataTypes.slice();

	// Create converters map with lowercased keys
	if ( dataTypes[ 1 ] ) {
		for ( conv in s.converters ) {
			converters[ conv.toLowerCase() ] = s.converters[ conv ];
		}
	}

	current = dataTypes.shift();

	// Convert to each sequential dataType
	while ( current ) {

		if ( s.responseFields[ current ] ) {
			jqXHR[ s.responseFields[ current ] ] = response;
		}

		// Apply the dataFilter if provided
		if ( !prev && isSuccess && s.dataFilter ) {
			response = s.dataFilter( response, s.dataType );
		}

		prev = current;
		current = dataTypes.shift();

		if ( current ) {

		// There's only work to do if current dataType is non-auto
			if ( current === "*" ) {

				current = prev;

			// Convert response if prev dataType is non-auto and differs from current
			} else if ( prev !== "*" && prev !== current ) {

				// Seek a direct converter
				conv = converters[ prev + " " + current ] || converters[ "* " + current ];

				// If none found, seek a pair
				if ( !conv ) {
					for ( conv2 in converters ) {

						// If conv2 outputs current
						tmp = conv2.split( " " );
						if ( tmp[ 1 ] === current ) {

							// If prev can be converted to accepted input
							conv = converters[ prev + " " + tmp[ 0 ] ] ||
								converters[ "* " + tmp[ 0 ] ];
							if ( conv ) {

								// Condense equivalence converters
								if ( conv === true ) {
									conv = converters[ conv2 ];

								// Otherwise, insert the intermediate dataType
								} else if ( converters[ conv2 ] !== true ) {
									current = tmp[ 0 ];
									dataTypes.unshift( tmp[ 1 ] );
								}
								break;
							}
						}
					}
				}

				// Apply converter (if not an equivalence)
				if ( conv !== true ) {

					// Unless errors are allowed to bubble, catch and return them
					if ( conv && s.throws ) {
						response = conv( response );
					} else {
						try {
							response = conv( response );
						} catch ( e ) {
							return {
								state: "parsererror",
								error: conv ? e : "No conversion from " + prev + " to " + current
							};
						}
					}
				}
			}
		}
	}

	return { state: "success", data: response };
}

jQuery.extend( {

	// Counter for holding the number of active queries
	active: 0,

	// Last-Modified header cache for next request
	lastModified: {},
	etag: {},

	ajaxSettings: {
		url: location.href,
		type: "GET",
		isLocal: rlocalProtocol.test( location.protocol ),
		global: true,
		processData: true,
		async: true,
		contentType: "application/x-www-form-urlencoded; charset=UTF-8",
		/*
		timeout: 0,
		data: null,
		dataType: null,
		username: null,
		password: null,
		cache: null,
		throws: false,
		traditional: false,
		headers: {},
		*/

		accepts: {
			"*": allTypes,
			text: "text/plain",
			html: "text/html",
			xml: "application/xml, text/xml",
			json: "application/json, text/javascript"
		},

		contents: {
			xml: /\bxml\b/,
			html: /\bhtml/,
			json: /\bjson\b/
		},

		responseFields: {
			xml: "responseXML",
			text: "responseText",
			json: "responseJSON"
		},

		// Data converters
		// Keys separate source (or catchall "*") and destination types with a single space
		converters: {

			// Convert anything to text
			"* text": String,

			// Text to html (true = no transformation)
			"text html": true,

			// Evaluate text as a json expression
			"text json": jQuery.parseJSON,

			// Parse text as xml
			"text xml": jQuery.parseXML
		},

		// For options that shouldn't be deep extended:
		// you can add your own custom options here if
		// and when you create one that shouldn't be
		// deep extended (see ajaxExtend)
		flatOptions: {
			url: true,
			context: true
		}
	},

	// Creates a full fledged settings object into target
	// with both ajaxSettings and settings fields.
	// If target is omitted, writes into ajaxSettings.
	ajaxSetup: function( target, settings ) {
		return settings ?

			// Building a settings object
			ajaxExtend( ajaxExtend( target, jQuery.ajaxSettings ), settings ) :

			// Extending ajaxSettings
			ajaxExtend( jQuery.ajaxSettings, target );
	},

	ajaxPrefilter: addToPrefiltersOrTransports( prefilters ),
	ajaxTransport: addToPrefiltersOrTransports( transports ),

	// Main method
	ajax: function( url, options ) {

		// If url is an object, simulate pre-1.5 signature
		if ( typeof url === "object" ) {
			options = url;
			url = undefined;
		}

		// Force options to be an object
		options = options || {};

		var transport,

			// URL without anti-cache param
			cacheURL,

			// Response headers
			responseHeadersString,
			responseHeaders,

			// timeout handle
			timeoutTimer,

			// Url cleanup var
			urlAnchor,

			// To know if global events are to be dispatched
			fireGlobals,

			// Loop variable
			i,

			// Create the final options object
			s = jQuery.ajaxSetup( {}, options ),

			// Callbacks context
			callbackContext = s.context || s,

			// Context for global events is callbackContext if it is a DOM node or jQuery collection
			globalEventContext = s.context &&
				( callbackContext.nodeType || callbackContext.jquery ) ?
					jQuery( callbackContext ) :
					jQuery.event,

			// Deferreds
			deferred = jQuery.Deferred(),
			completeDeferred = jQuery.Callbacks( "once memory" ),

			// Status-dependent callbacks
			statusCode = s.statusCode || {},

			// Headers (they are sent all at once)
			requestHeaders = {},
			requestHeadersNames = {},

			// The jqXHR state
			state = 0,

			// Default abort message
			strAbort = "canceled",

			// Fake xhr
			jqXHR = {
				readyState: 0,

				// Builds headers hashtable if needed
				getResponseHeader: function( key ) {
					var match;
					if ( state === 2 ) {
						if ( !responseHeaders ) {
							responseHeaders = {};
							while ( ( match = rheaders.exec( responseHeadersString ) ) ) {
								responseHeaders[ match[ 1 ].toLowerCase() ] = match[ 2 ];
							}
						}
						match = responseHeaders[ key.toLowerCase() ];
					}
					return match == null ? null : match;
				},

				// Raw string
				getAllResponseHeaders: function() {
					return state === 2 ? responseHeadersString : null;
				},

				// Caches the header
				setRequestHeader: function( name, value ) {
					var lname = name.toLowerCase();
					if ( !state ) {
						name = requestHeadersNames[ lname ] = requestHeadersNames[ lname ] || name;
						requestHeaders[ name ] = value;
					}
					return this;
				},

				// Overrides response content-type header
				overrideMimeType: function( type ) {
					if ( !state ) {
						s.mimeType = type;
					}
					return this;
				},

				// Status-dependent callbacks
				statusCode: function( map ) {
					var code;
					if ( map ) {
						if ( state < 2 ) {
							for ( code in map ) {

								// Lazy-add the new callback in a way that preserves old ones
								statusCode[ code ] = [ statusCode[ code ], map[ code ] ];
							}
						} else {

							// Execute the appropriate callbacks
							jqXHR.always( map[ jqXHR.status ] );
						}
					}
					return this;
				},

				// Cancel the request
				abort: function( statusText ) {
					var finalText = statusText || strAbort;
					if ( transport ) {
						transport.abort( finalText );
					}
					done( 0, finalText );
					return this;
				}
			};

		// Attach deferreds
		deferred.promise( jqXHR ).complete = completeDeferred.add;
		jqXHR.success = jqXHR.done;
		jqXHR.error = jqXHR.fail;

		// Remove hash character (#7531: and string promotion)
		// Add protocol if not provided (prefilters might expect it)
		// Handle falsy url in the settings object (#10093: consistency with old signature)
		// We also use the url parameter if available
		s.url = ( ( url || s.url || location.href ) + "" ).replace( rhash, "" )
			.replace( rprotocol, location.protocol + "//" );

		// Alias method option to type as per ticket #12004
		s.type = options.method || options.type || s.method || s.type;

		// Extract dataTypes list
		s.dataTypes = jQuery.trim( s.dataType || "*" ).toLowerCase().match( rnotwhite ) || [ "" ];

		// A cross-domain request is in order when the origin doesn't match the current origin.
		if ( s.crossDomain == null ) {
			urlAnchor = document.createElement( "a" );

			// Support: IE8-11+
			// IE throws exception if url is malformed, e.g. http://example.com:80x/
			try {
				urlAnchor.href = s.url;

				// Support: IE8-11+
				// Anchor's host property isn't correctly set when s.url is relative
				urlAnchor.href = urlAnchor.href;
				s.crossDomain = originAnchor.protocol + "//" + originAnchor.host !==
					urlAnchor.protocol + "//" + urlAnchor.host;
			} catch ( e ) {

				// If there is an error parsing the URL, assume it is crossDomain,
				// it can be rejected by the transport if it is invalid
				s.crossDomain = true;
			}
		}

		// Convert data if not already a string
		if ( s.data && s.processData && typeof s.data !== "string" ) {
			s.data = jQuery.param( s.data, s.traditional );
		}

		// Apply prefilters
		inspectPrefiltersOrTransports( prefilters, s, options, jqXHR );

		// If request was aborted inside a prefilter, stop there
		if ( state === 2 ) {
			return jqXHR;
		}

		// We can fire global events as of now if asked to
		// Don't fire events if jQuery.event is undefined in an AMD-usage scenario (#15118)
		fireGlobals = jQuery.event && s.global;

		// Watch for a new set of requests
		if ( fireGlobals && jQuery.active++ === 0 ) {
			jQuery.event.trigger( "ajaxStart" );
		}

		// Uppercase the type
		s.type = s.type.toUpperCase();

		// Determine if request has content
		s.hasContent = !rnoContent.test( s.type );

		// Save the URL in case we're toying with the If-Modified-Since
		// and/or If-None-Match header later on
		cacheURL = s.url;

		// More options handling for requests with no content
		if ( !s.hasContent ) {

			// If data is available, append data to url
			if ( s.data ) {
				cacheURL = ( s.url += ( rquery.test( cacheURL ) ? "&" : "?" ) + s.data );

				// #9682: remove data so that it's not used in an eventual retry
				delete s.data;
			}

			// Add anti-cache in url if needed
			if ( s.cache === false ) {
				s.url = rts.test( cacheURL ) ?

					// If there is already a '_' parameter, set its value
					cacheURL.replace( rts, "$1_=" + nonce++ ) :

					// Otherwise add one to the end
					cacheURL + ( rquery.test( cacheURL ) ? "&" : "?" ) + "_=" + nonce++;
			}
		}

		// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
		if ( s.ifModified ) {
			if ( jQuery.lastModified[ cacheURL ] ) {
				jqXHR.setRequestHeader( "If-Modified-Since", jQuery.lastModified[ cacheURL ] );
			}
			if ( jQuery.etag[ cacheURL ] ) {
				jqXHR.setRequestHeader( "If-None-Match", jQuery.etag[ cacheURL ] );
			}
		}

		// Set the correct header, if data is being sent
		if ( s.data && s.hasContent && s.contentType !== false || options.contentType ) {
			jqXHR.setRequestHeader( "Content-Type", s.contentType );
		}

		// Set the Accepts header for the server, depending on the dataType
		jqXHR.setRequestHeader(
			"Accept",
			s.dataTypes[ 0 ] && s.accepts[ s.dataTypes[ 0 ] ] ?
				s.accepts[ s.dataTypes[ 0 ] ] +
					( s.dataTypes[ 0 ] !== "*" ? ", " + allTypes + "; q=0.01" : "" ) :
				s.accepts[ "*" ]
		);

		// Check for headers option
		for ( i in s.headers ) {
			jqXHR.setRequestHeader( i, s.headers[ i ] );
		}

		// Allow custom headers/mimetypes and early abort
		if ( s.beforeSend &&
			( s.beforeSend.call( callbackContext, jqXHR, s ) === false || state === 2 ) ) {

			// Abort if not done already and return
			return jqXHR.abort();
		}

		// Aborting is no longer a cancellation
		strAbort = "abort";

		// Install callbacks on deferreds
		for ( i in { success: 1, error: 1, complete: 1 } ) {
			jqXHR[ i ]( s[ i ] );
		}

		// Get transport
		transport = inspectPrefiltersOrTransports( transports, s, options, jqXHR );

		// If no transport, we auto-abort
		if ( !transport ) {
			done( -1, "No Transport" );
		} else {
			jqXHR.readyState = 1;

			// Send global event
			if ( fireGlobals ) {
				globalEventContext.trigger( "ajaxSend", [ jqXHR, s ] );
			}

			// If request was aborted inside ajaxSend, stop there
			if ( state === 2 ) {
				return jqXHR;
			}

			// Timeout
			if ( s.async && s.timeout > 0 ) {
				timeoutTimer = window.setTimeout( function() {
					jqXHR.abort( "timeout" );
				}, s.timeout );
			}

			try {
				state = 1;
				transport.send( requestHeaders, done );
			} catch ( e ) {

				// Propagate exception as error if not done
				if ( state < 2 ) {
					done( -1, e );

				// Simply rethrow otherwise
				} else {
					throw e;
				}
			}
		}

		// Callback for when everything is done
		function done( status, nativeStatusText, responses, headers ) {
			var isSuccess, success, error, response, modified,
				statusText = nativeStatusText;

			// Called once
			if ( state === 2 ) {
				return;
			}

			// State is "done" now
			state = 2;

			// Clear timeout if it exists
			if ( timeoutTimer ) {
				window.clearTimeout( timeoutTimer );
			}

			// Dereference transport for early garbage collection
			// (no matter how long the jqXHR object will be used)
			transport = undefined;

			// Cache response headers
			responseHeadersString = headers || "";

			// Set readyState
			jqXHR.readyState = status > 0 ? 4 : 0;

			// Determine if successful
			isSuccess = status >= 200 && status < 300 || status === 304;

			// Get response data
			if ( responses ) {
				response = ajaxHandleResponses( s, jqXHR, responses );
			}

			// Convert no matter what (that way responseXXX fields are always set)
			response = ajaxConvert( s, response, jqXHR, isSuccess );

			// If successful, handle type chaining
			if ( isSuccess ) {

				// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
				if ( s.ifModified ) {
					modified = jqXHR.getResponseHeader( "Last-Modified" );
					if ( modified ) {
						jQuery.lastModified[ cacheURL ] = modified;
					}
					modified = jqXHR.getResponseHeader( "etag" );
					if ( modified ) {
						jQuery.etag[ cacheURL ] = modified;
					}
				}

				// if no content
				if ( status === 204 || s.type === "HEAD" ) {
					statusText = "nocontent";

				// if not modified
				} else if ( status === 304 ) {
					statusText = "notmodified";

				// If we have data, let's convert it
				} else {
					statusText = response.state;
					success = response.data;
					error = response.error;
					isSuccess = !error;
				}
			} else {

				// Extract error from statusText and normalize for non-aborts
				error = statusText;
				if ( status || !statusText ) {
					statusText = "error";
					if ( status < 0 ) {
						status = 0;
					}
				}
			}

			// Set data for the fake xhr object
			jqXHR.status = status;
			jqXHR.statusText = ( nativeStatusText || statusText ) + "";

			// Success/Error
			if ( isSuccess ) {
				deferred.resolveWith( callbackContext, [ success, statusText, jqXHR ] );
			} else {
				deferred.rejectWith( callbackContext, [ jqXHR, statusText, error ] );
			}

			// Status-dependent callbacks
			jqXHR.statusCode( statusCode );
			statusCode = undefined;

			if ( fireGlobals ) {
				globalEventContext.trigger( isSuccess ? "ajaxSuccess" : "ajaxError",
					[ jqXHR, s, isSuccess ? success : error ] );
			}

			// Complete
			completeDeferred.fireWith( callbackContext, [ jqXHR, statusText ] );

			if ( fireGlobals ) {
				globalEventContext.trigger( "ajaxComplete", [ jqXHR, s ] );

				// Handle the global AJAX counter
				if ( !( --jQuery.active ) ) {
					jQuery.event.trigger( "ajaxStop" );
				}
			}
		}

		return jqXHR;
	},

	getJSON: function( url, data, callback ) {
		return jQuery.get( url, data, callback, "json" );
	},

	getScript: function( url, callback ) {
		return jQuery.get( url, undefined, callback, "script" );
	}
} );

jQuery.each( [ "get", "post" ], function( i, method ) {
	jQuery[ method ] = function( url, data, callback, type ) {

		// Shift arguments if data argument was omitted
		if ( jQuery.isFunction( data ) ) {
			type = type || callback;
			callback = data;
			data = undefined;
		}

		// The url can be an options object (which then must have .url)
		return jQuery.ajax( jQuery.extend( {
			url: url,
			type: method,
			dataType: type,
			data: data,
			success: callback
		}, jQuery.isPlainObject( url ) && url ) );
	};
} );


jQuery._evalUrl = function( url ) {
	return jQuery.ajax( {
		url: url,

		// Make this explicit, since user can override this through ajaxSetup (#11264)
		type: "GET",
		dataType: "script",
		async: false,
		global: false,
		"throws": true
	} );
};


jQuery.fn.extend( {
	wrapAll: function( html ) {
		var wrap;

		if ( jQuery.isFunction( html ) ) {
			return this.each( function( i ) {
				jQuery( this ).wrapAll( html.call( this, i ) );
			} );
		}

		if ( this[ 0 ] ) {

			// The elements to wrap the target around
			wrap = jQuery( html, this[ 0 ].ownerDocument ).eq( 0 ).clone( true );

			if ( this[ 0 ].parentNode ) {
				wrap.insertBefore( this[ 0 ] );
			}

			wrap.map( function() {
				var elem = this;

				while ( elem.firstElementChild ) {
					elem = elem.firstElementChild;
				}

				return elem;
			} ).append( this );
		}

		return this;
	},

	wrapInner: function( html ) {
		if ( jQuery.isFunction( html ) ) {
			return this.each( function( i ) {
				jQuery( this ).wrapInner( html.call( this, i ) );
			} );
		}

		return this.each( function() {
			var self = jQuery( this ),
				contents = self.contents();

			if ( contents.length ) {
				contents.wrapAll( html );

			} else {
				self.append( html );
			}
		} );
	},

	wrap: function( html ) {
		var isFunction = jQuery.isFunction( html );

		return this.each( function( i ) {
			jQuery( this ).wrapAll( isFunction ? html.call( this, i ) : html );
		} );
	},

	unwrap: function() {
		return this.parent().each( function() {
			if ( !jQuery.nodeName( this, "body" ) ) {
				jQuery( this ).replaceWith( this.childNodes );
			}
		} ).end();
	}
} );


jQuery.expr.filters.hidden = function( elem ) {
	return !jQuery.expr.filters.visible( elem );
};
jQuery.expr.filters.visible = function( elem ) {

	// Support: Opera <= 12.12
	// Opera reports offsetWidths and offsetHeights less than zero on some elements
	// Use OR instead of AND as the element is not visible if either is true
	// See tickets #10406 and #13132
	return elem.offsetWidth > 0 || elem.offsetHeight > 0 || elem.getClientRects().length > 0;
};




var r20 = /%20/g,
	rbracket = /\[\]$/,
	rCRLF = /\r?\n/g,
	rsubmitterTypes = /^(?:submit|button|image|reset|file)$/i,
	rsubmittable = /^(?:input|select|textarea|keygen)/i;

function buildParams( prefix, obj, traditional, add ) {
	var name;

	if ( jQuery.isArray( obj ) ) {

		// Serialize array item.
		jQuery.each( obj, function( i, v ) {
			if ( traditional || rbracket.test( prefix ) ) {

				// Treat each array item as a scalar.
				add( prefix, v );

			} else {

				// Item is non-scalar (array or object), encode its numeric index.
				buildParams(
					prefix + "[" + ( typeof v === "object" && v != null ? i : "" ) + "]",
					v,
					traditional,
					add
				);
			}
		} );

	} else if ( !traditional && jQuery.type( obj ) === "object" ) {

		// Serialize object item.
		for ( name in obj ) {
			buildParams( prefix + "[" + name + "]", obj[ name ], traditional, add );
		}

	} else {

		// Serialize scalar item.
		add( prefix, obj );
	}
}

// Serialize an array of form elements or a set of
// key/values into a query string
jQuery.param = function( a, traditional ) {
	var prefix,
		s = [],
		add = function( key, value ) {

			// If value is a function, invoke it and return its value
			value = jQuery.isFunction( value ) ? value() : ( value == null ? "" : value );
			s[ s.length ] = encodeURIComponent( key ) + "=" + encodeURIComponent( value );
		};

	// Set traditional to true for jQuery <= 1.3.2 behavior.
	if ( traditional === undefined ) {
		traditional = jQuery.ajaxSettings && jQuery.ajaxSettings.traditional;
	}

	// If an array was passed in, assume that it is an array of form elements.
	if ( jQuery.isArray( a ) || ( a.jquery && !jQuery.isPlainObject( a ) ) ) {

		// Serialize the form elements
		jQuery.each( a, function() {
			add( this.name, this.value );
		} );

	} else {

		// If traditional, encode the "old" way (the way 1.3.2 or older
		// did it), otherwise encode params recursively.
		for ( prefix in a ) {
			buildParams( prefix, a[ prefix ], traditional, add );
		}
	}

	// Return the resulting serialization
	return s.join( "&" ).replace( r20, "+" );
};

jQuery.fn.extend( {
	serialize: function() {
		return jQuery.param( this.serializeArray() );
	},
	serializeArray: function() {
		return this.map( function() {

			// Can add propHook for "elements" to filter or add form elements
			var elements = jQuery.prop( this, "elements" );
			return elements ? jQuery.makeArray( elements ) : this;
		} )
		.filter( function() {
			var type = this.type;

			// Use .is( ":disabled" ) so that fieldset[disabled] works
			return this.name && !jQuery( this ).is( ":disabled" ) &&
				rsubmittable.test( this.nodeName ) && !rsubmitterTypes.test( type ) &&
				( this.checked || !rcheckableType.test( type ) );
		} )
		.map( function( i, elem ) {
			var val = jQuery( this ).val();

			return val == null ?
				null :
				jQuery.isArray( val ) ?
					jQuery.map( val, function( val ) {
						return { name: elem.name, value: val.replace( rCRLF, "\r\n" ) };
					} ) :
					{ name: elem.name, value: val.replace( rCRLF, "\r\n" ) };
		} ).get();
	}
} );


jQuery.ajaxSettings.xhr = function() {
	try {
		return new window.XMLHttpRequest();
	} catch ( e ) {}
};

var xhrSuccessStatus = {

		// File protocol always yields status code 0, assume 200
		0: 200,

		// Support: IE9
		// #1450: sometimes IE returns 1223 when it should be 204
		1223: 204
	},
	xhrSupported = jQuery.ajaxSettings.xhr();

support.cors = !!xhrSupported && ( "withCredentials" in xhrSupported );
support.ajax = xhrSupported = !!xhrSupported;

jQuery.ajaxTransport( function( options ) {
	var callback, errorCallback;

	// Cross domain only allowed if supported through XMLHttpRequest
	if ( support.cors || xhrSupported && !options.crossDomain ) {
		return {
			send: function( headers, complete ) {
				var i,
					xhr = options.xhr();

				xhr.open(
					options.type,
					options.url,
					options.async,
					options.username,
					options.password
				);

				// Apply custom fields if provided
				if ( options.xhrFields ) {
					for ( i in options.xhrFields ) {
						xhr[ i ] = options.xhrFields[ i ];
					}
				}

				// Override mime type if needed
				if ( options.mimeType && xhr.overrideMimeType ) {
					xhr.overrideMimeType( options.mimeType );
				}

				// X-Requested-With header
				// For cross-domain requests, seeing as conditions for a preflight are
				// akin to a jigsaw puzzle, we simply never set it to be sure.
				// (it can always be set on a per-request basis or even using ajaxSetup)
				// For same-domain requests, won't change header if already provided.
				if ( !options.crossDomain && !headers[ "X-Requested-With" ] ) {
					headers[ "X-Requested-With" ] = "XMLHttpRequest";
				}

				// Set headers
				for ( i in headers ) {
					xhr.setRequestHeader( i, headers[ i ] );
				}

				// Callback
				callback = function( type ) {
					return function() {
						if ( callback ) {
							callback = errorCallback = xhr.onload =
								xhr.onerror = xhr.onabort = xhr.onreadystatechange = null;

							if ( type === "abort" ) {
								xhr.abort();
							} else if ( type === "error" ) {

								// Support: IE9
								// On a manual native abort, IE9 throws
								// errors on any property access that is not readyState
								if ( typeof xhr.status !== "number" ) {
									complete( 0, "error" );
								} else {
									complete(

										// File: protocol always yields status 0; see #8605, #14207
										xhr.status,
										xhr.statusText
									);
								}
							} else {
								complete(
									xhrSuccessStatus[ xhr.status ] || xhr.status,
									xhr.statusText,

									// Support: IE9 only
									// IE9 has no XHR2 but throws on binary (trac-11426)
									// For XHR2 non-text, let the caller handle it (gh-2498)
									( xhr.responseType || "text" ) !== "text"  ||
									typeof xhr.responseText !== "string" ?
										{ binary: xhr.response } :
										{ text: xhr.responseText },
									xhr.getAllResponseHeaders()
								);
							}
						}
					};
				};

				// Listen to events
				xhr.onload = callback();
				errorCallback = xhr.onerror = callback( "error" );

				// Support: IE9
				// Use onreadystatechange to replace onabort
				// to handle uncaught aborts
				if ( xhr.onabort !== undefined ) {
					xhr.onabort = errorCallback;
				} else {
					xhr.onreadystatechange = function() {

						// Check readyState before timeout as it changes
						if ( xhr.readyState === 4 ) {

							// Allow onerror to be called first,
							// but that will not handle a native abort
							// Also, save errorCallback to a variable
							// as xhr.onerror cannot be accessed
							window.setTimeout( function() {
								if ( callback ) {
									errorCallback();
								}
							} );
						}
					};
				}

				// Create the abort callback
				callback = callback( "abort" );

				try {

					// Do send the request (this may raise an exception)
					xhr.send( options.hasContent && options.data || null );
				} catch ( e ) {

					// #14683: Only rethrow if this hasn't been notified as an error yet
					if ( callback ) {
						throw e;
					}
				}
			},

			abort: function() {
				if ( callback ) {
					callback();
				}
			}
		};
	}
} );




// Install script dataType
jQuery.ajaxSetup( {
	accepts: {
		script: "text/javascript, application/javascript, " +
			"application/ecmascript, application/x-ecmascript"
	},
	contents: {
		script: /\b(?:java|ecma)script\b/
	},
	converters: {
		"text script": function( text ) {
			jQuery.globalEval( text );
			return text;
		}
	}
} );

// Handle cache's special case and crossDomain
jQuery.ajaxPrefilter( "script", function( s ) {
	if ( s.cache === undefined ) {
		s.cache = false;
	}
	if ( s.crossDomain ) {
		s.type = "GET";
	}
} );

// Bind script tag hack transport
jQuery.ajaxTransport( "script", function( s ) {

	// This transport only deals with cross domain requests
	if ( s.crossDomain ) {
		var script, callback;
		return {
			send: function( _, complete ) {
				script = jQuery( "<script>" ).prop( {
					charset: s.scriptCharset,
					src: s.url
				} ).on(
					"load error",
					callback = function( evt ) {
						script.remove();
						callback = null;
						if ( evt ) {
							complete( evt.type === "error" ? 404 : 200, evt.type );
						}
					}
				);

				// Use native DOM manipulation to avoid our domManip AJAX trickery
				document.head.appendChild( script[ 0 ] );
			},
			abort: function() {
				if ( callback ) {
					callback();
				}
			}
		};
	}
} );




var oldCallbacks = [],
	rjsonp = /(=)\?(?=&|$)|\?\?/;

// Default jsonp settings
jQuery.ajaxSetup( {
	jsonp: "callback",
	jsonpCallback: function() {
		var callback = oldCallbacks.pop() || ( jQuery.expando + "_" + ( nonce++ ) );
		this[ callback ] = true;
		return callback;
	}
} );

// Detect, normalize options and install callbacks for jsonp requests
jQuery.ajaxPrefilter( "json jsonp", function( s, originalSettings, jqXHR ) {

	var callbackName, overwritten, responseContainer,
		jsonProp = s.jsonp !== false && ( rjsonp.test( s.url ) ?
			"url" :
			typeof s.data === "string" &&
				( s.contentType || "" )
					.indexOf( "application/x-www-form-urlencoded" ) === 0 &&
				rjsonp.test( s.data ) && "data"
		);

	// Handle iff the expected data type is "jsonp" or we have a parameter to set
	if ( jsonProp || s.dataTypes[ 0 ] === "jsonp" ) {

		// Get callback name, remembering preexisting value associated with it
		callbackName = s.jsonpCallback = jQuery.isFunction( s.jsonpCallback ) ?
			s.jsonpCallback() :
			s.jsonpCallback;

		// Insert callback into url or form data
		if ( jsonProp ) {
			s[ jsonProp ] = s[ jsonProp ].replace( rjsonp, "$1" + callbackName );
		} else if ( s.jsonp !== false ) {
			s.url += ( rquery.test( s.url ) ? "&" : "?" ) + s.jsonp + "=" + callbackName;
		}

		// Use data converter to retrieve json after script execution
		s.converters[ "script json" ] = function() {
			if ( !responseContainer ) {
				jQuery.error( callbackName + " was not called" );
			}
			return responseContainer[ 0 ];
		};

		// Force json dataType
		s.dataTypes[ 0 ] = "json";

		// Install callback
		overwritten = window[ callbackName ];
		window[ callbackName ] = function() {
			responseContainer = arguments;
		};

		// Clean-up function (fires after converters)
		jqXHR.always( function() {

			// If previous value didn't exist - remove it
			if ( overwritten === undefined ) {
				jQuery( window ).removeProp( callbackName );

			// Otherwise restore preexisting value
			} else {
				window[ callbackName ] = overwritten;
			}

			// Save back as free
			if ( s[ callbackName ] ) {

				// Make sure that re-using the options doesn't screw things around
				s.jsonpCallback = originalSettings.jsonpCallback;

				// Save the callback name for future use
				oldCallbacks.push( callbackName );
			}

			// Call if it was a function and we have a response
			if ( responseContainer && jQuery.isFunction( overwritten ) ) {
				overwritten( responseContainer[ 0 ] );
			}

			responseContainer = overwritten = undefined;
		} );

		// Delegate to script
		return "script";
	}
} );




// Argument "data" should be string of html
// context (optional): If specified, the fragment will be created in this context,
// defaults to document
// keepScripts (optional): If true, will include scripts passed in the html string
jQuery.parseHTML = function( data, context, keepScripts ) {
	if ( !data || typeof data !== "string" ) {
		return null;
	}
	if ( typeof context === "boolean" ) {
		keepScripts = context;
		context = false;
	}
	context = context || document;

	var parsed = rsingleTag.exec( data ),
		scripts = !keepScripts && [];

	// Single tag
	if ( parsed ) {
		return [ context.createElement( parsed[ 1 ] ) ];
	}

	parsed = buildFragment( [ data ], context, scripts );

	if ( scripts && scripts.length ) {
		jQuery( scripts ).remove();
	}

	return jQuery.merge( [], parsed.childNodes );
};


// Keep a copy of the old load method
var _load = jQuery.fn.load;

/**
 * Load a url into a page
 */
jQuery.fn.load = function( url, params, callback ) {
	if ( typeof url !== "string" && _load ) {
		return _load.apply( this, arguments );
	}

	var selector, type, response,
		self = this,
		off = url.indexOf( " " );

	if ( off > -1 ) {
		selector = jQuery.trim( url.slice( off ) );
		url = url.slice( 0, off );
	}

	// If it's a function
	if ( jQuery.isFunction( params ) ) {

		// We assume that it's the callback
		callback = params;
		params = undefined;

	// Otherwise, build a param string
	} else if ( params && typeof params === "object" ) {
		type = "POST";
	}

	// If we have elements to modify, make the request
	if ( self.length > 0 ) {
		jQuery.ajax( {
			url: url,

			// If "type" variable is undefined, then "GET" method will be used.
			// Make value of this field explicit since
			// user can override it through ajaxSetup method
			type: type || "GET",
			dataType: "html",
			data: params
		} ).done( function( responseText ) {

			// Save response for use in complete callback
			response = arguments;

			self.html( selector ?

				// If a selector was specified, locate the right elements in a dummy div
				// Exclude scripts to avoid IE 'Permission Denied' errors
				jQuery( "<div>" ).append( jQuery.parseHTML( responseText ) ).find( selector ) :

				// Otherwise use the full result
				responseText );

		// If the request succeeds, this function gets "data", "status", "jqXHR"
		// but they are ignored because response was set above.
		// If it fails, this function gets "jqXHR", "status", "error"
		} ).always( callback && function( jqXHR, status ) {
			self.each( function() {
				callback.apply( this, response || [ jqXHR.responseText, status, jqXHR ] );
			} );
		} );
	}

	return this;
};




// Attach a bunch of functions for handling common AJAX events
jQuery.each( [
	"ajaxStart",
	"ajaxStop",
	"ajaxComplete",
	"ajaxError",
	"ajaxSuccess",
	"ajaxSend"
], function( i, type ) {
	jQuery.fn[ type ] = function( fn ) {
		return this.on( type, fn );
	};
} );




jQuery.expr.filters.animated = function( elem ) {
	return jQuery.grep( jQuery.timers, function( fn ) {
		return elem === fn.elem;
	} ).length;
};




/**
 * Gets a window from an element
 */
function getWindow( elem ) {
	return jQuery.isWindow( elem ) ? elem : elem.nodeType === 9 && elem.defaultView;
}

jQuery.offset = {
	setOffset: function( elem, options, i ) {
		var curPosition, curLeft, curCSSTop, curTop, curOffset, curCSSLeft, calculatePosition,
			position = jQuery.css( elem, "position" ),
			curElem = jQuery( elem ),
			props = {};

		// Set position first, in-case top/left are set even on static elem
		if ( position === "static" ) {
			elem.style.position = "relative";
		}

		curOffset = curElem.offset();
		curCSSTop = jQuery.css( elem, "top" );
		curCSSLeft = jQuery.css( elem, "left" );
		calculatePosition = ( position === "absolute" || position === "fixed" ) &&
			( curCSSTop + curCSSLeft ).indexOf( "auto" ) > -1;

		// Need to be able to calculate position if either
		// top or left is auto and position is either absolute or fixed
		if ( calculatePosition ) {
			curPosition = curElem.position();
			curTop = curPosition.top;
			curLeft = curPosition.left;

		} else {
			curTop = parseFloat( curCSSTop ) || 0;
			curLeft = parseFloat( curCSSLeft ) || 0;
		}

		if ( jQuery.isFunction( options ) ) {

			// Use jQuery.extend here to allow modification of coordinates argument (gh-1848)
			options = options.call( elem, i, jQuery.extend( {}, curOffset ) );
		}

		if ( options.top != null ) {
			props.top = ( options.top - curOffset.top ) + curTop;
		}
		if ( options.left != null ) {
			props.left = ( options.left - curOffset.left ) + curLeft;
		}

		if ( "using" in options ) {
			options.using.call( elem, props );

		} else {
			curElem.css( props );
		}
	}
};

jQuery.fn.extend( {
	offset: function( options ) {
		if ( arguments.length ) {
			return options === undefined ?
				this :
				this.each( function( i ) {
					jQuery.offset.setOffset( this, options, i );
				} );
		}

		var docElem, win,
			elem = this[ 0 ],
			box = { top: 0, left: 0 },
			doc = elem && elem.ownerDocument;

		if ( !doc ) {
			return;
		}

		docElem = doc.documentElement;

		// Make sure it's not a disconnected DOM node
		if ( !jQuery.contains( docElem, elem ) ) {
			return box;
		}

		box = elem.getBoundingClientRect();
		win = getWindow( doc );
		return {
			top: box.top + win.pageYOffset - docElem.clientTop,
			left: box.left + win.pageXOffset - docElem.clientLeft
		};
	},

	position: function() {
		if ( !this[ 0 ] ) {
			return;
		}

		var offsetParent, offset,
			elem = this[ 0 ],
			parentOffset = { top: 0, left: 0 };

		// Fixed elements are offset from window (parentOffset = {top:0, left: 0},
		// because it is its only offset parent
		if ( jQuery.css( elem, "position" ) === "fixed" ) {

			// Assume getBoundingClientRect is there when computed position is fixed
			offset = elem.getBoundingClientRect();

		} else {

			// Get *real* offsetParent
			offsetParent = this.offsetParent();

			// Get correct offsets
			offset = this.offset();
			if ( !jQuery.nodeName( offsetParent[ 0 ], "html" ) ) {
				parentOffset = offsetParent.offset();
			}

			// Add offsetParent borders
			parentOffset.top += jQuery.css( offsetParent[ 0 ], "borderTopWidth", true );
			parentOffset.left += jQuery.css( offsetParent[ 0 ], "borderLeftWidth", true );
		}

		// Subtract parent offsets and element margins
		return {
			top: offset.top - parentOffset.top - jQuery.css( elem, "marginTop", true ),
			left: offset.left - parentOffset.left - jQuery.css( elem, "marginLeft", true )
		};
	},

	// This method will return documentElement in the following cases:
	// 1) For the element inside the iframe without offsetParent, this method will return
	//    documentElement of the parent window
	// 2) For the hidden or detached element
	// 3) For body or html element, i.e. in case of the html node - it will return itself
	//
	// but those exceptions were never presented as a real life use-cases
	// and might be considered as more preferable results.
	//
	// This logic, however, is not guaranteed and can change at any point in the future
	offsetParent: function() {
		return this.map( function() {
			var offsetParent = this.offsetParent;

			while ( offsetParent && jQuery.css( offsetParent, "position" ) === "static" ) {
				offsetParent = offsetParent.offsetParent;
			}

			return offsetParent || documentElement;
		} );
	}
} );

// Create scrollLeft and scrollTop methods
jQuery.each( { scrollLeft: "pageXOffset", scrollTop: "pageYOffset" }, function( method, prop ) {
	var top = "pageYOffset" === prop;

	jQuery.fn[ method ] = function( val ) {
		return access( this, function( elem, method, val ) {
			var win = getWindow( elem );

			if ( val === undefined ) {
				return win ? win[ prop ] : elem[ method ];
			}

			if ( win ) {
				win.scrollTo(
					!top ? val : win.pageXOffset,
					top ? val : win.pageYOffset
				);

			} else {
				elem[ method ] = val;
			}
		}, method, val, arguments.length );
	};
} );

// Support: Safari<7-8+, Chrome<37-44+
// Add the top/left cssHooks using jQuery.fn.position
// Webkit bug: https://bugs.webkit.org/show_bug.cgi?id=29084
// Blink bug: https://code.google.com/p/chromium/issues/detail?id=229280
// getComputedStyle returns percent when specified for top/left/bottom/right;
// rather than make the css module depend on the offset module, just check for it here
jQuery.each( [ "top", "left" ], function( i, prop ) {
	jQuery.cssHooks[ prop ] = addGetHookIf( support.pixelPosition,
		function( elem, computed ) {
			if ( computed ) {
				computed = curCSS( elem, prop );

				// If curCSS returns percentage, fallback to offset
				return rnumnonpx.test( computed ) ?
					jQuery( elem ).position()[ prop ] + "px" :
					computed;
			}
		}
	);
} );


// Create innerHeight, innerWidth, height, width, outerHeight and outerWidth methods
jQuery.each( { Height: "height", Width: "width" }, function( name, type ) {
	jQuery.each( { padding: "inner" + name, content: type, "": "outer" + name },
		function( defaultExtra, funcName ) {

		// Margin is only for outerHeight, outerWidth
		jQuery.fn[ funcName ] = function( margin, value ) {
			var chainable = arguments.length && ( defaultExtra || typeof margin !== "boolean" ),
				extra = defaultExtra || ( margin === true || value === true ? "margin" : "border" );

			return access( this, function( elem, type, value ) {
				var doc;

				if ( jQuery.isWindow( elem ) ) {

					// As of 5/8/2012 this will yield incorrect results for Mobile Safari, but there
					// isn't a whole lot we can do. See pull request at this URL for discussion:
					// https://github.com/jquery/jquery/pull/764
					return elem.document.documentElement[ "client" + name ];
				}

				// Get document width or height
				if ( elem.nodeType === 9 ) {
					doc = elem.documentElement;

					// Either scroll[Width/Height] or offset[Width/Height] or client[Width/Height],
					// whichever is greatest
					return Math.max(
						elem.body[ "scroll" + name ], doc[ "scroll" + name ],
						elem.body[ "offset" + name ], doc[ "offset" + name ],
						doc[ "client" + name ]
					);
				}

				return value === undefined ?

					// Get width or height on the element, requesting but not forcing parseFloat
					jQuery.css( elem, type, extra ) :

					// Set width or height on the element
					jQuery.style( elem, type, value, extra );
			}, type, chainable ? margin : undefined, chainable, null );
		};
	} );
} );


jQuery.fn.extend( {

	bind: function( types, data, fn ) {
		return this.on( types, null, data, fn );
	},
	unbind: function( types, fn ) {
		return this.off( types, null, fn );
	},

	delegate: function( selector, types, data, fn ) {
		return this.on( types, selector, data, fn );
	},
	undelegate: function( selector, types, fn ) {

		// ( namespace ) or ( selector, types [, fn] )
		return arguments.length === 1 ?
			this.off( selector, "**" ) :
			this.off( types, selector || "**", fn );
	},
	size: function() {
		return this.length;
	}
} );

jQuery.fn.andSelf = jQuery.fn.addBack;




// Register as a named AMD module, since jQuery can be concatenated with other
// files that may use define, but not via a proper concatenation script that
// understands anonymous AMD modules. A named AMD is safest and most robust
// way to register. Lowercase jquery is used because AMD module names are
// derived from file names, and jQuery is normally delivered in a lowercase
// file name. Do this after creating the global so that if an AMD module wants
// to call noConflict to hide this version of jQuery, it will work.

// Note that for maximum portability, libraries that are not jQuery should
// declare themselves as anonymous modules, and avoid setting a global if an
// AMD loader is present. jQuery is a special case. For more information, see
// https://github.com/jrburke/requirejs/wiki/Updating-existing-libraries#wiki-anon

if ( typeof define === "function" && define.amd ) {
	define( "jquery", [], function() {
		return jQuery;
	} );
}



var

	// Map over jQuery in case of overwrite
	_jQuery = window.jQuery,

	// Map over the $ in case of overwrite
	_$ = window.$;

jQuery.noConflict = function( deep ) {
	if ( window.$ === jQuery ) {
		window.$ = _$;
	}

	if ( deep && window.jQuery === jQuery ) {
		window.jQuery = _jQuery;
	}

	return jQuery;
};

// Expose jQuery and $ identifiers, even in AMD
// (#7102#comment:10, https://github.com/jquery/jquery/pull/557)
// and CommonJS for browser emulators (#13566)
if ( !noGlobal ) {
	window.jQuery = window.$ = jQuery;
}

return jQuery;
}));
(function($, undefined) {

/**
 * Unobtrusive scripting adapter for jQuery
 * https://github.com/rails/jquery-ujs
 *
 * Requires jQuery 1.8.0 or later.
 *
 * Released under the MIT license
 *
 */

  // Cut down on the number of issues from people inadvertently including jquery_ujs twice
  // by detecting and raising an error when it happens.
  if ( $.rails !== undefined ) {
    $.error('jquery-ujs has already been loaded!');
  }

  // Shorthand to make it a little easier to call public rails functions from within rails.js
  var rails;
  var $document = $(document);

  $.rails = rails = {
    // Link elements bound by jquery-ujs
    linkClickSelector: 'a[data-confirm], a[data-method], a[data-remote], a[data-disable-with], a[data-disable]',

    // Button elements bound by jquery-ujs
    buttonClickSelector: 'button[data-remote]:not(form button), button[data-confirm]:not(form button)',

    // Select elements bound by jquery-ujs
    inputChangeSelector: 'select[data-remote], input[data-remote], textarea[data-remote]',

    // Form elements bound by jquery-ujs
    formSubmitSelector: 'form',

    // Form input elements bound by jquery-ujs
    formInputClickSelector: 'form input[type=submit], form input[type=image], form button[type=submit], form button:not([type]), input[type=submit][form], input[type=image][form], button[type=submit][form], button[form]:not([type])',

    // Form input elements disabled during form submission
    disableSelector: 'input[data-disable-with]:enabled, button[data-disable-with]:enabled, textarea[data-disable-with]:enabled, input[data-disable]:enabled, button[data-disable]:enabled, textarea[data-disable]:enabled',

    // Form input elements re-enabled after form submission
    enableSelector: 'input[data-disable-with]:disabled, button[data-disable-with]:disabled, textarea[data-disable-with]:disabled, input[data-disable]:disabled, button[data-disable]:disabled, textarea[data-disable]:disabled',

    // Form required input elements
    requiredInputSelector: 'input[name][required]:not([disabled]),textarea[name][required]:not([disabled])',

    // Form file input elements
    fileInputSelector: 'input[type=file]',

    // Link onClick disable selector with possible reenable after remote submission
    linkDisableSelector: 'a[data-disable-with], a[data-disable]',

    // Button onClick disable selector with possible reenable after remote submission
    buttonDisableSelector: 'button[data-remote][data-disable-with], button[data-remote][data-disable]',

    // Make sure that every Ajax request sends the CSRF token
    CSRFProtection: function(xhr) {
      var token = $('meta[name="csrf-token"]').attr('content');
      if (token) xhr.setRequestHeader('X-CSRF-Token', token);
    },

    // making sure that all forms have actual up-to-date token(cached forms contain old one)
    refreshCSRFTokens: function(){
      var csrfToken = $('meta[name=csrf-token]').attr('content');
      var csrfParam = $('meta[name=csrf-param]').attr('content');
      $('form input[name="' + csrfParam + '"]').val(csrfToken);
    },

    // Triggers an event on an element and returns false if the event result is false
    fire: function(obj, name, data) {
      var event = $.Event(name);
      obj.trigger(event, data);
      return event.result !== false;
    },

    // Default confirm dialog, may be overridden with custom confirm dialog in $.rails.confirm
    confirm: function(message) {
      return confirm(message);
    },

    // Default ajax function, may be overridden with custom function in $.rails.ajax
    ajax: function(options) {
      return $.ajax(options);
    },

    // Default way to get an element's href. May be overridden at $.rails.href.
    href: function(element) {
      return element[0].href;
    },

    // Submits "remote" forms and links with ajax
    handleRemote: function(element) {
      var method, url, data, withCredentials, dataType, options;

      if (rails.fire(element, 'ajax:before')) {
        withCredentials = element.data('with-credentials') || null;
        dataType = element.data('type') || ($.ajaxSettings && $.ajaxSettings.dataType);

        if (element.is('form')) {
          method = element.attr('method');
          url = element.attr('action');
          data = element.serializeArray();
          // memoized value from clicked submit button
          var button = element.data('ujs:submit-button');
          if (button) {
            data.push(button);
            element.data('ujs:submit-button', null);
          }
        } else if (element.is(rails.inputChangeSelector)) {
          method = element.data('method');
          url = element.data('url');
          data = element.serialize();
          if (element.data('params')) data = data + "&" + element.data('params');
        } else if (element.is(rails.buttonClickSelector)) {
          method = element.data('method') || 'get';
          url = element.data('url');
          data = element.serialize();
          if (element.data('params')) data = data + "&" + element.data('params');
        } else {
          method = element.data('method');
          url = rails.href(element);
          data = element.data('params') || null;
        }

        options = {
          type: method || 'GET', data: data, dataType: dataType,
          // stopping the "ajax:beforeSend" event will cancel the ajax request
          beforeSend: function(xhr, settings) {
            if (settings.dataType === undefined) {
              xhr.setRequestHeader('accept', '*/*;q=0.5, ' + settings.accepts.script);
            }
            if (rails.fire(element, 'ajax:beforeSend', [xhr, settings])) {
              element.trigger('ajax:send', xhr);
            } else {
              return false;
            }
          },
          success: function(data, status, xhr) {
            element.trigger('ajax:success', [data, status, xhr]);
          },
          complete: function(xhr, status) {
            element.trigger('ajax:complete', [xhr, status]);
          },
          error: function(xhr, status, error) {
            element.trigger('ajax:error', [xhr, status, error]);
          },
          crossDomain: rails.isCrossDomain(url)
        };

        // There is no withCredentials for IE6-8 when
        // "Enable native XMLHTTP support" is disabled
        if (withCredentials) {
          options.xhrFields = {
            withCredentials: withCredentials
          };
        }

        // Only pass url to `ajax` options if not blank
        if (url) { options.url = url; }

        return rails.ajax(options);
      } else {
        return false;
      }
    },

    // Determines if the request is a cross domain request.
    isCrossDomain: function(url) {
      var originAnchor = document.createElement("a");
      originAnchor.href = location.href;
      var urlAnchor = document.createElement("a");

      try {
        urlAnchor.href = url;
        // This is a workaround to a IE bug.
        urlAnchor.href = urlAnchor.href;

        // Make sure that the browser parses the URL and that the protocols and hosts match.
        return !urlAnchor.protocol || !urlAnchor.host ||
          (originAnchor.protocol + "//" + originAnchor.host !==
            urlAnchor.protocol + "//" + urlAnchor.host);
      } catch (e) {
        // If there is an error parsing the URL, assume it is crossDomain.
        return true;
      }
    },

    // Handles "data-method" on links such as:
    // <a href="/users/5" data-method="delete" rel="nofollow" data-confirm="Are you sure?">Delete</a>
    handleMethod: function(link) {
      var href = rails.href(link),
        method = link.data('method'),
        target = link.attr('target'),
        csrfToken = $('meta[name=csrf-token]').attr('content'),
        csrfParam = $('meta[name=csrf-param]').attr('content'),
        form = $('<form method="post" action="' + href + '"></form>'),
        metadataInput = '<input name="_method" value="' + method + '" type="hidden" />';

      if (csrfParam !== undefined && csrfToken !== undefined && !rails.isCrossDomain(href)) {
        metadataInput += '<input name="' + csrfParam + '" value="' + csrfToken + '" type="hidden" />';
      }

      if (target) { form.attr('target', target); }

      form.hide().append(metadataInput).appendTo('body');
      form.submit();
    },

    // Helper function that returns form elements that match the specified CSS selector
    // If form is actually a "form" element this will return associated elements outside the from that have
    // the html form attribute set
    formElements: function(form, selector) {
      return form.is('form') ? $(form[0].elements).filter(selector) : form.find(selector);
    },

    /* Disables form elements:
      - Caches element value in 'ujs:enable-with' data store
      - Replaces element text with value of 'data-disable-with' attribute
      - Sets disabled property to true
    */
    disableFormElements: function(form) {
      rails.formElements(form, rails.disableSelector).each(function() {
        rails.disableFormElement($(this));
      });
    },

    disableFormElement: function(element) {
      var method, replacement;

      method = element.is('button') ? 'html' : 'val';
      replacement = element.data('disable-with');

      element.data('ujs:enable-with', element[method]());
      if (replacement !== undefined) {
        element[method](replacement);
      }

      element.prop('disabled', true);
    },

    /* Re-enables disabled form elements:
      - Replaces element text with cached value from 'ujs:enable-with' data store (created in `disableFormElements`)
      - Sets disabled property to false
    */
    enableFormElements: function(form) {
      rails.formElements(form, rails.enableSelector).each(function() {
        rails.enableFormElement($(this));
      });
    },

    enableFormElement: function(element) {
      var method = element.is('button') ? 'html' : 'val';
      if (element.data('ujs:enable-with')) element[method](element.data('ujs:enable-with'));
      element.prop('disabled', false);
    },

   /* For 'data-confirm' attribute:
      - Fires `confirm` event
      - Shows the confirmation dialog
      - Fires the `confirm:complete` event

      Returns `true` if no function stops the chain and user chose yes; `false` otherwise.
      Attaching a handler to the element's `confirm` event that returns a `falsy` value cancels the confirmation dialog.
      Attaching a handler to the element's `confirm:complete` event that returns a `falsy` value makes this function
      return false. The `confirm:complete` event is fired whether or not the user answered true or false to the dialog.
   */
    allowAction: function(element) {
      var message = element.data('confirm'),
          answer = false, callback;
      if (!message) { return true; }

      if (rails.fire(element, 'confirm')) {
        answer = rails.confirm(message);
        callback = rails.fire(element, 'confirm:complete', [answer]);
      }
      return answer && callback;
    },

    // Helper function which checks for blank inputs in a form that match the specified CSS selector
    blankInputs: function(form, specifiedSelector, nonBlank) {
      var inputs = $(), input, valueToCheck,
          selector = specifiedSelector || 'input,textarea',
          allInputs = form.find(selector);

      allInputs.each(function() {
        input = $(this);
        valueToCheck = input.is('input[type=checkbox],input[type=radio]') ? input.is(':checked') : input.val();
        // If nonBlank and valueToCheck are both truthy, or nonBlank and valueToCheck are both falsey
        if (!valueToCheck === !nonBlank) {

          // Don't count unchecked required radio if other radio with same name is checked
          if (input.is('input[type=radio]') && allInputs.filter('input[type=radio]:checked[name="' + input.attr('name') + '"]').length) {
            return true; // Skip to next input
          }

          inputs = inputs.add(input);
        }
      });
      return inputs.length ? inputs : false;
    },

    // Helper function which checks for non-blank inputs in a form that match the specified CSS selector
    nonBlankInputs: function(form, specifiedSelector) {
      return rails.blankInputs(form, specifiedSelector, true); // true specifies nonBlank
    },

    // Helper function, needed to provide consistent behavior in IE
    stopEverything: function(e) {
      $(e.target).trigger('ujs:everythingStopped');
      e.stopImmediatePropagation();
      return false;
    },

    //  replace element's html with the 'data-disable-with' after storing original html
    //  and prevent clicking on it
    disableElement: function(element) {
      var replacement = element.data('disable-with');

      element.data('ujs:enable-with', element.html()); // store enabled state
      if (replacement !== undefined) {
        element.html(replacement);
      }

      element.bind('click.railsDisable', function(e) { // prevent further clicking
        return rails.stopEverything(e);
      });
    },

    // restore element to its original state which was disabled by 'disableElement' above
    enableElement: function(element) {
      if (element.data('ujs:enable-with') !== undefined) {
        element.html(element.data('ujs:enable-with')); // set to old enabled state
        element.removeData('ujs:enable-with'); // clean up cache
      }
      element.unbind('click.railsDisable'); // enable element
    }
  };

  if (rails.fire($document, 'rails:attachBindings')) {

    $.ajaxPrefilter(function(options, originalOptions, xhr){ if ( !options.crossDomain ) { rails.CSRFProtection(xhr); }});

    // This event works the same as the load event, except that it fires every
    // time the page is loaded.
    //
    // See https://github.com/rails/jquery-ujs/issues/357
    // See https://developer.mozilla.org/en-US/docs/Using_Firefox_1.5_caching
    $(window).on("pageshow.rails", function () {
      $($.rails.enableSelector).each(function () {
        var element = $(this);

        if (element.data("ujs:enable-with")) {
          $.rails.enableFormElement(element);
        }
      });

      $($.rails.linkDisableSelector).each(function () {
        var element = $(this);

        if (element.data("ujs:enable-with")) {
          $.rails.enableElement(element);
        }
      });
    });

    $document.delegate(rails.linkDisableSelector, 'ajax:complete', function() {
        rails.enableElement($(this));
    });

    $document.delegate(rails.buttonDisableSelector, 'ajax:complete', function() {
        rails.enableFormElement($(this));
    });

    $document.delegate(rails.linkClickSelector, 'click.rails', function(e) {
      var link = $(this), method = link.data('method'), data = link.data('params'), metaClick = e.metaKey || e.ctrlKey;
      if (!rails.allowAction(link)) return rails.stopEverything(e);

      if (!metaClick && link.is(rails.linkDisableSelector)) rails.disableElement(link);

      if (link.data('remote') !== undefined) {
        if (metaClick && (!method || method === 'GET') && !data) { return true; }

        var handleRemote = rails.handleRemote(link);
        // response from rails.handleRemote() will either be false or a deferred object promise.
        if (handleRemote === false) {
          rails.enableElement(link);
        } else {
          handleRemote.fail( function() { rails.enableElement(link); } );
        }
        return false;

      } else if (method) {
        rails.handleMethod(link);
        return false;
      }
    });

    $document.delegate(rails.buttonClickSelector, 'click.rails', function(e) {
      var button = $(this);

      if (!rails.allowAction(button)) return rails.stopEverything(e);

      if (button.is(rails.buttonDisableSelector)) rails.disableFormElement(button);

      var handleRemote = rails.handleRemote(button);
      // response from rails.handleRemote() will either be false or a deferred object promise.
      if (handleRemote === false) {
        rails.enableFormElement(button);
      } else {
        handleRemote.fail( function() { rails.enableFormElement(button); } );
      }
      return false;
    });

    $document.delegate(rails.inputChangeSelector, 'change.rails', function(e) {
      var link = $(this);
      if (!rails.allowAction(link)) return rails.stopEverything(e);

      rails.handleRemote(link);
      return false;
    });

    $document.delegate(rails.formSubmitSelector, 'submit.rails', function(e) {
      var form = $(this),
        remote = form.data('remote') !== undefined,
        blankRequiredInputs,
        nonBlankFileInputs;

      if (!rails.allowAction(form)) return rails.stopEverything(e);

      // skip other logic when required values are missing or file upload is present
      if (form.attr('novalidate') == undefined) {
        blankRequiredInputs = rails.blankInputs(form, rails.requiredInputSelector);
        if (blankRequiredInputs && rails.fire(form, 'ajax:aborted:required', [blankRequiredInputs])) {
          return rails.stopEverything(e);
        }
      }

      if (remote) {
        nonBlankFileInputs = rails.nonBlankInputs(form, rails.fileInputSelector);
        if (nonBlankFileInputs) {
          // slight timeout so that the submit button gets properly serialized
          // (make it easy for event handler to serialize form without disabled values)
          setTimeout(function(){ rails.disableFormElements(form); }, 13);
          var aborted = rails.fire(form, 'ajax:aborted:file', [nonBlankFileInputs]);

          // re-enable form elements if event bindings return false (canceling normal form submission)
          if (!aborted) { setTimeout(function(){ rails.enableFormElements(form); }, 13); }

          return aborted;
        }

        rails.handleRemote(form);
        return false;

      } else {
        // slight timeout so that the submit button gets properly serialized
        setTimeout(function(){ rails.disableFormElements(form); }, 13);
      }
    });

    $document.delegate(rails.formInputClickSelector, 'click.rails', function(event) {
      var button = $(this);

      if (!rails.allowAction(button)) return rails.stopEverything(event);

      // register the pressed submit button
      var name = button.attr('name'),
        data = name ? {name:name, value:button.val()} : null;

      button.closest('form').data('ujs:submit-button', data);
    });

    $document.delegate(rails.formSubmitSelector, 'ajax:send.rails', function(event) {
      if (this == event.target) rails.disableFormElements($(this));
    });

    $document.delegate(rails.formSubmitSelector, 'ajax:complete.rails', function(event) {
      if (this == event.target) rails.enableFormElements($(this));
    });

    $(function(){
      rails.refreshCSRFTokens();
    });
  }

})( jQuery );
/*!
 * @preserve
 * jquery.scrolldepth.js | v0.8
 * Copyright (c) 2015 Rob Flaherty (@robflaherty)
 * Licensed under the MIT and GPL licenses.
 */

!function(e,n,t){"use strict";var r,o,a,l,i={minHeight:0,elements:[],percentage:!0,userTiming:!0,pixelDepth:!0,nonInteraction:!0,gaGlobal:!1,gtmOverride:!1},c=e(n),u=[],g=0;e.scrollDepth=function(h){function p(e){l?l({event:"ScrollDistance",eventCategory:"Scroll Depth",eventAction:e,eventLabel:"Baseline",eventValue:1,eventNonInteraction:!0}):(r&&n[a]("send","event","Scroll Depth",e,"Baseline",1,{nonInteraction:!0}),o&&_gaq.push(["_trackEvent","Scroll Depth",e,"Baseline",1,!0]))}function s(e,t,i,c){l?(l({event:"ScrollDistance",eventCategory:"Scroll Depth",eventAction:e,eventLabel:t,eventValue:1,eventNonInteraction:h.nonInteraction}),h.pixelDepth&&arguments.length>2&&i>g&&(g=i,l({event:"ScrollDistance",eventCategory:"Scroll Depth",eventAction:"Pixel Depth",eventLabel:D(i),eventValue:1,eventNonInteraction:h.nonInteraction})),h.userTiming&&arguments.length>3&&l({event:"ScrollTiming",eventCategory:"Scroll Depth",eventAction:e,eventLabel:t,eventTiming:c})):(r&&(n[a]("send","event","Scroll Depth",e,t,1,{nonInteraction:h.nonInteraction}),h.pixelDepth&&arguments.length>2&&i>g&&(g=i,n[a]("send","event","Scroll Depth","Pixel Depth",D(i),1,{nonInteraction:h.nonInteraction})),h.userTiming&&arguments.length>3&&n[a]("send","timing","Scroll Depth",e,c,t)),o&&(_gaq.push(["_trackEvent","Scroll Depth",e,t,1,h.nonInteraction]),h.pixelDepth&&arguments.length>2&&i>g&&(g=i,_gaq.push(["_trackEvent","Scroll Depth","Pixel Depth",D(i),1,h.nonInteraction])),h.userTiming&&arguments.length>3&&_gaq.push(["_trackTiming","Scroll Depth",e,c,t,100])))}function v(e){return{"25%":parseInt(.25*e,10),"50%":parseInt(.5*e,10),"75%":parseInt(.75*e,10),"100%":e-5}}function f(n,t,r){e.each(n,function(n,o){-1===e.inArray(n,u)&&t>=o&&(s("Percentage",n,t,r),u.push(n))})}function m(n,t,r){e.each(n,function(n,o){-1===e.inArray(o,u)&&e(o).length&&t>=e(o).offset().top&&(s("Elements",o,t,r),u.push(o))})}function D(e){return(250*Math.floor(e/250)).toString()}function d(e,n){var t,r,o,a=null,l=0,i=function(){l=new Date,a=null,o=e.apply(t,r)};return function(){var c=new Date;l||(l=c);var u=n-(c-l);return t=this,r=arguments,0>=u?(clearTimeout(a),a=null,l=c,o=e.apply(t,r)):a||(a=setTimeout(i,u)),o}}var y=+new Date;h=e.extend({},i,h),e(t).height()<h.minHeight||(h.gaGlobal?(r=!0,a=h.gaGlobal):"function"==typeof ga?(r=!0,a="ga"):"function"==typeof __gaTracker&&(r=!0,a="__gaTracker"),"undefined"!=typeof _gaq&&"function"==typeof _gaq.push&&(o=!0),"function"==typeof h.eventHandler?l=h.eventHandler:"undefined"==typeof dataLayer||"function"!=typeof dataLayer.push||h.gtmOverride||(l=function(e){dataLayer.push(e)}),h.percentage?p("Percentage"):h.elements&&p("Elements"),c.on("scroll.scrollDepth",d(function(){var r=e(t).height(),o=n.innerHeight?n.innerHeight:c.height(),a=c.scrollTop()+o,l=v(r),i=+new Date-y;return u.length>=4+h.elements.length?void c.off("scroll.scrollDepth"):(h.elements&&m(h.elements,a,i),void(h.percentage&&f(l,a,i)))},500)))}}(jQuery,window,document);
/*
     _ _      _       _
 ___| (_) ___| | __  (_)___
/ __| | |/ __| |/ /  | / __|
\__ \ | | (__|   < _ | \__ \
|___/_|_|\___|_|\_(_)/ |___/
                   |__/

 Version: 1.5.1
  Author: Ken Wheeler
 Website: http://kenwheeler.github.io
    Docs: http://kenwheeler.github.io/slick
    Repo: http://github.com/kenwheeler/slick
  Issues: http://github.com/kenwheeler/slick/issues

 */

!function(a){"use strict";"function"==typeof define&&define.amd?define(["jquery"],a):"undefined"!=typeof exports?module.exports=a(require("jquery")):a(jQuery)}(function(a){"use strict";var b=window.Slick||{};b=function(){function c(c,d){var f,g,h,e=this;if(e.defaults={accessibility:!0,adaptiveHeight:!1,appendArrows:a(c),appendDots:a(c),arrows:!0,asNavFor:null,prevArrow:'<button type="button" data-role="none" class="slick-prev" aria-label="previous">Previous</button>',nextArrow:'<button type="button" data-role="none" class="slick-next" aria-label="next">Next</button>',autoplay:!1,autoplaySpeed:3e3,centerMode:!1,centerPadding:"50px",cssEase:"ease",customPaging:function(a,b){return'<button type="button" data-role="none">'+(b+1)+"</button>"},dots:!1,dotsClass:"slick-dots",draggable:!0,easing:"linear",edgeFriction:.35,fade:!1,focusOnSelect:!1,infinite:!0,initialSlide:0,lazyLoad:"ondemand",mobileFirst:!1,pauseOnHover:!0,pauseOnDotsHover:!1,respondTo:"window",responsive:null,rows:1,rtl:!1,slide:"",slidesPerRow:1,slidesToShow:1,slidesToScroll:1,speed:500,swipe:!0,swipeToSlide:!1,touchMove:!0,touchThreshold:5,useCSS:!0,variableWidth:!1,vertical:!1,verticalSwiping:!1,waitForAnimate:!0},e.initials={animating:!1,dragging:!1,autoPlayTimer:null,currentDirection:0,currentLeft:null,currentSlide:0,direction:1,$dots:null,listWidth:null,listHeight:null,loadIndex:0,$nextArrow:null,$prevArrow:null,slideCount:null,slideWidth:null,$slideTrack:null,$slides:null,sliding:!1,slideOffset:0,swipeLeft:null,$list:null,touchObject:{},transformsEnabled:!1},a.extend(e,e.initials),e.activeBreakpoint=null,e.animType=null,e.animProp=null,e.breakpoints=[],e.breakpointSettings=[],e.cssTransitions=!1,e.hidden="hidden",e.paused=!1,e.positionProp=null,e.respondTo=null,e.rowCount=1,e.shouldClick=!0,e.$slider=a(c),e.$slidesCache=null,e.transformType=null,e.transitionType=null,e.visibilityChange="visibilitychange",e.windowWidth=0,e.windowTimer=null,f=a(c).data("slick")||{},e.options=a.extend({},e.defaults,f,d),e.currentSlide=e.options.initialSlide,e.originalSettings=e.options,g=e.options.responsive||null,g&&g.length>-1){e.respondTo=e.options.respondTo||"window";for(h in g)g.hasOwnProperty(h)&&(e.breakpoints.push(g[h].breakpoint),e.breakpointSettings[g[h].breakpoint]=g[h].settings);e.breakpoints.sort(function(a,b){return e.options.mobileFirst===!0?a-b:b-a})}"undefined"!=typeof document.mozHidden?(e.hidden="mozHidden",e.visibilityChange="mozvisibilitychange"):"undefined"!=typeof document.msHidden?(e.hidden="msHidden",e.visibilityChange="msvisibilitychange"):"undefined"!=typeof document.webkitHidden&&(e.hidden="webkitHidden",e.visibilityChange="webkitvisibilitychange"),e.autoPlay=a.proxy(e.autoPlay,e),e.autoPlayClear=a.proxy(e.autoPlayClear,e),e.changeSlide=a.proxy(e.changeSlide,e),e.clickHandler=a.proxy(e.clickHandler,e),e.selectHandler=a.proxy(e.selectHandler,e),e.setPosition=a.proxy(e.setPosition,e),e.swipeHandler=a.proxy(e.swipeHandler,e),e.dragHandler=a.proxy(e.dragHandler,e),e.keyHandler=a.proxy(e.keyHandler,e),e.autoPlayIterator=a.proxy(e.autoPlayIterator,e),e.instanceUid=b++,e.htmlExpr=/^(?:\s*(<[\w\W]+>)[^>]*)$/,e.init(),e.checkResponsive(!0)}var b=0;return c}(),b.prototype.addSlide=b.prototype.slickAdd=function(b,c,d){var e=this;if("boolean"==typeof c)d=c,c=null;else if(0>c||c>=e.slideCount)return!1;e.unload(),"number"==typeof c?0===c&&0===e.$slides.length?a(b).appendTo(e.$slideTrack):d?a(b).insertBefore(e.$slides.eq(c)):a(b).insertAfter(e.$slides.eq(c)):d===!0?a(b).prependTo(e.$slideTrack):a(b).appendTo(e.$slideTrack),e.$slides=e.$slideTrack.children(this.options.slide),e.$slideTrack.children(this.options.slide).detach(),e.$slideTrack.append(e.$slides),e.$slides.each(function(b,c){a(c).attr("data-slick-index",b)}),e.$slidesCache=e.$slides,e.reinit()},b.prototype.animateHeight=function(){var a=this;if(1===a.options.slidesToShow&&a.options.adaptiveHeight===!0&&a.options.vertical===!1){var b=a.$slides.eq(a.currentSlide).outerHeight(!0);a.$list.animate({height:b},a.options.speed)}},b.prototype.animateSlide=function(b,c){var d={},e=this;e.animateHeight(),e.options.rtl===!0&&e.options.vertical===!1&&(b=-b),e.transformsEnabled===!1?e.options.vertical===!1?e.$slideTrack.animate({left:b},e.options.speed,e.options.easing,c):e.$slideTrack.animate({top:b},e.options.speed,e.options.easing,c):e.cssTransitions===!1?(e.options.rtl===!0&&(e.currentLeft=-e.currentLeft),a({animStart:e.currentLeft}).animate({animStart:b},{duration:e.options.speed,easing:e.options.easing,step:function(a){a=Math.ceil(a),e.options.vertical===!1?(d[e.animType]="translate("+a+"px, 0px)",e.$slideTrack.css(d)):(d[e.animType]="translate(0px,"+a+"px)",e.$slideTrack.css(d))},complete:function(){c&&c.call()}})):(e.applyTransition(),b=Math.ceil(b),d[e.animType]=e.options.vertical===!1?"translate3d("+b+"px, 0px, 0px)":"translate3d(0px,"+b+"px, 0px)",e.$slideTrack.css(d),c&&setTimeout(function(){e.disableTransition(),c.call()},e.options.speed))},b.prototype.asNavFor=function(b){var c=this,d=null!==c.options.asNavFor?a(c.options.asNavFor).slick("getSlick"):null;null!==d&&d.slideHandler(b,!0)},b.prototype.applyTransition=function(a){var b=this,c={};c[b.transitionType]=b.options.fade===!1?b.transformType+" "+b.options.speed+"ms "+b.options.cssEase:"opacity "+b.options.speed+"ms "+b.options.cssEase,b.options.fade===!1?b.$slideTrack.css(c):b.$slides.eq(a).css(c)},b.prototype.autoPlay=function(){var a=this;a.autoPlayTimer&&clearInterval(a.autoPlayTimer),a.slideCount>a.options.slidesToShow&&a.paused!==!0&&(a.autoPlayTimer=setInterval(a.autoPlayIterator,a.options.autoplaySpeed))},b.prototype.autoPlayClear=function(){var a=this;a.autoPlayTimer&&clearInterval(a.autoPlayTimer)},b.prototype.autoPlayIterator=function(){var a=this;a.options.infinite===!1?1===a.direction?(a.currentSlide+1===a.slideCount-1&&(a.direction=0),a.slideHandler(a.currentSlide+a.options.slidesToScroll)):(0===a.currentSlide-1&&(a.direction=1),a.slideHandler(a.currentSlide-a.options.slidesToScroll)):a.slideHandler(a.currentSlide+a.options.slidesToScroll)},b.prototype.buildArrows=function(){var b=this;b.options.arrows===!0&&b.slideCount>b.options.slidesToShow&&(b.$prevArrow=a(b.options.prevArrow),b.$nextArrow=a(b.options.nextArrow),b.htmlExpr.test(b.options.prevArrow)&&b.$prevArrow.appendTo(b.options.appendArrows),b.htmlExpr.test(b.options.nextArrow)&&b.$nextArrow.appendTo(b.options.appendArrows),b.options.infinite!==!0&&b.$prevArrow.addClass("slick-disabled"))},b.prototype.buildDots=function(){var c,d,b=this;if(b.options.dots===!0&&b.slideCount>b.options.slidesToShow){for(d='<ul class="'+b.options.dotsClass+'">',c=0;c<=b.getDotCount();c+=1)d+="<li>"+b.options.customPaging.call(this,b,c)+"</li>";d+="</ul>",b.$dots=a(d).appendTo(b.options.appendDots),b.$dots.find("li").first().addClass("slick-active").attr("aria-hidden","false")}},b.prototype.buildOut=function(){var b=this;b.$slides=b.$slider.children(":not(.slick-cloned)").addClass("slick-slide"),b.slideCount=b.$slides.length,b.$slides.each(function(b,c){a(c).attr("data-slick-index",b)}),b.$slidesCache=b.$slides,b.$slider.addClass("slick-slider"),b.$slideTrack=0===b.slideCount?a('<div class="slick-track"/>').appendTo(b.$slider):b.$slides.wrapAll('<div class="slick-track"/>').parent(),b.$list=b.$slideTrack.wrap('<div aria-live="polite" class="slick-list"/>').parent(),b.$slideTrack.css("opacity",0),(b.options.centerMode===!0||b.options.swipeToSlide===!0)&&(b.options.slidesToScroll=1),a("img[data-lazy]",b.$slider).not("[src]").addClass("slick-loading"),b.setupInfinite(),b.buildArrows(),b.buildDots(),b.updateDots(),b.options.accessibility===!0&&b.$list.prop("tabIndex",0),b.setSlideClasses("number"==typeof this.currentSlide?this.currentSlide:0),b.options.draggable===!0&&b.$list.addClass("draggable")},b.prototype.buildRows=function(){var b,c,d,e,f,g,h,a=this;if(e=document.createDocumentFragment(),g=a.$slider.children(),a.options.rows>1){for(h=a.options.slidesPerRow*a.options.rows,f=Math.ceil(g.length/h),b=0;f>b;b++){var i=document.createElement("div");for(c=0;c<a.options.rows;c++){var j=document.createElement("div");for(d=0;d<a.options.slidesPerRow;d++){var k=b*h+(c*a.options.slidesPerRow+d);g.get(k)&&j.appendChild(g.get(k))}i.appendChild(j)}e.appendChild(i)}a.$slider.html(e),a.$slider.children().children().children().width(100/a.options.slidesPerRow+"%").css({display:"inline-block"})}},b.prototype.checkResponsive=function(b){var d,e,f,c=this,g=c.$slider.width(),h=window.innerWidth||a(window).width();if("window"===c.respondTo?f=h:"slider"===c.respondTo?f=g:"min"===c.respondTo&&(f=Math.min(h,g)),c.originalSettings.responsive&&c.originalSettings.responsive.length>-1&&null!==c.originalSettings.responsive){e=null;for(d in c.breakpoints)c.breakpoints.hasOwnProperty(d)&&(c.originalSettings.mobileFirst===!1?f<c.breakpoints[d]&&(e=c.breakpoints[d]):f>c.breakpoints[d]&&(e=c.breakpoints[d]));null!==e?null!==c.activeBreakpoint?e!==c.activeBreakpoint&&(c.activeBreakpoint=e,"unslick"===c.breakpointSettings[e]?c.unslick():(c.options=a.extend({},c.originalSettings,c.breakpointSettings[e]),b===!0&&(c.currentSlide=c.options.initialSlide),c.refresh())):(c.activeBreakpoint=e,"unslick"===c.breakpointSettings[e]?c.unslick():(c.options=a.extend({},c.originalSettings,c.breakpointSettings[e]),b===!0&&(c.currentSlide=c.options.initialSlide),c.refresh())):null!==c.activeBreakpoint&&(c.activeBreakpoint=null,c.options=c.originalSettings,b===!0&&(c.currentSlide=c.options.initialSlide),c.refresh())}},b.prototype.changeSlide=function(b,c){var f,g,h,d=this,e=a(b.target);switch(e.is("a")&&b.preventDefault(),h=0!==d.slideCount%d.options.slidesToScroll,f=h?0:(d.slideCount-d.currentSlide)%d.options.slidesToScroll,b.data.message){case"previous":g=0===f?d.options.slidesToScroll:d.options.slidesToShow-f,d.slideCount>d.options.slidesToShow&&d.slideHandler(d.currentSlide-g,!1,c);break;case"next":g=0===f?d.options.slidesToScroll:f,d.slideCount>d.options.slidesToShow&&d.slideHandler(d.currentSlide+g,!1,c);break;case"index":var i=0===b.data.index?0:b.data.index||a(b.target).parent().index()*d.options.slidesToScroll;d.slideHandler(d.checkNavigable(i),!1,c);break;default:return}},b.prototype.checkNavigable=function(a){var c,d,b=this;if(c=b.getNavigableIndexes(),d=0,a>c[c.length-1])a=c[c.length-1];else for(var e in c){if(a<c[e]){a=d;break}d=c[e]}return a},b.prototype.cleanUpEvents=function(){var b=this;b.options.dots===!0&&b.slideCount>b.options.slidesToShow&&a("li",b.$dots).off("click.slick",b.changeSlide),b.options.dots===!0&&b.options.pauseOnDotsHover===!0&&b.options.autoplay===!0&&a("li",b.$dots).off("mouseenter.slick",b.setPaused.bind(b,!0)).off("mouseleave.slick",b.setPaused.bind(b,!1)),b.options.arrows===!0&&b.slideCount>b.options.slidesToShow&&(b.$prevArrow&&b.$prevArrow.off("click.slick",b.changeSlide),b.$nextArrow&&b.$nextArrow.off("click.slick",b.changeSlide)),b.$list.off("touchstart.slick mousedown.slick",b.swipeHandler),b.$list.off("touchmove.slick mousemove.slick",b.swipeHandler),b.$list.off("touchend.slick mouseup.slick",b.swipeHandler),b.$list.off("touchcancel.slick mouseleave.slick",b.swipeHandler),b.$list.off("click.slick",b.clickHandler),b.options.autoplay===!0&&a(document).off(b.visibilityChange,b.visibility),b.$list.off("mouseenter.slick",b.setPaused.bind(b,!0)),b.$list.off("mouseleave.slick",b.setPaused.bind(b,!1)),b.options.accessibility===!0&&b.$list.off("keydown.slick",b.keyHandler),b.options.focusOnSelect===!0&&a(b.$slideTrack).children().off("click.slick",b.selectHandler),a(window).off("orientationchange.slick.slick-"+b.instanceUid,b.orientationChange),a(window).off("resize.slick.slick-"+b.instanceUid,b.resize),a("[draggable!=true]",b.$slideTrack).off("dragstart",b.preventDefault),a(window).off("load.slick.slick-"+b.instanceUid,b.setPosition),a(document).off("ready.slick.slick-"+b.instanceUid,b.setPosition)},b.prototype.cleanUpRows=function(){var b,a=this;a.options.rows>1&&(b=a.$slides.children().children(),b.removeAttr("style"),a.$slider.html(b))},b.prototype.clickHandler=function(a){var b=this;b.shouldClick===!1&&(a.stopImmediatePropagation(),a.stopPropagation(),a.preventDefault())},b.prototype.destroy=function(){var b=this;b.autoPlayClear(),b.touchObject={},b.cleanUpEvents(),a(".slick-cloned",b.$slider).detach(),b.$dots&&b.$dots.remove(),b.$prevArrow&&"object"!=typeof b.options.prevArrow&&b.$prevArrow.remove(),b.$nextArrow&&"object"!=typeof b.options.nextArrow&&b.$nextArrow.remove(),b.$slides&&(b.$slides.removeClass("slick-slide slick-active slick-center slick-visible").removeAttr("aria-hidden").removeAttr("data-slick-index").css({position:"",left:"",top:"",zIndex:"",opacity:"",width:""}),b.$slideTrack.children(this.options.slide).detach(),b.$slideTrack.detach(),b.$list.detach(),b.$slider.append(b.$slides)),b.cleanUpRows(),b.$slider.removeClass("slick-slider"),b.$slider.removeClass("slick-initialized")},b.prototype.disableTransition=function(a){var b=this,c={};c[b.transitionType]="",b.options.fade===!1?b.$slideTrack.css(c):b.$slides.eq(a).css(c)},b.prototype.fadeSlide=function(a,b){var c=this;c.cssTransitions===!1?(c.$slides.eq(a).css({zIndex:1e3}),c.$slides.eq(a).animate({opacity:1},c.options.speed,c.options.easing,b)):(c.applyTransition(a),c.$slides.eq(a).css({opacity:1,zIndex:1e3}),b&&setTimeout(function(){c.disableTransition(a),b.call()},c.options.speed))},b.prototype.filterSlides=b.prototype.slickFilter=function(a){var b=this;null!==a&&(b.unload(),b.$slideTrack.children(this.options.slide).detach(),b.$slidesCache.filter(a).appendTo(b.$slideTrack),b.reinit())},b.prototype.getCurrent=b.prototype.slickCurrentSlide=function(){var a=this;return a.currentSlide},b.prototype.getDotCount=function(){var a=this,b=0,c=0,d=0;if(a.options.infinite===!0)for(;b<a.slideCount;)++d,b=c+a.options.slidesToShow,c+=a.options.slidesToScroll<=a.options.slidesToShow?a.options.slidesToScroll:a.options.slidesToShow;else if(a.options.centerMode===!0)d=a.slideCount;else for(;b<a.slideCount;)++d,b=c+a.options.slidesToShow,c+=a.options.slidesToScroll<=a.options.slidesToShow?a.options.slidesToScroll:a.options.slidesToShow;return d-1},b.prototype.getLeft=function(a){var c,d,f,b=this,e=0;return b.slideOffset=0,d=b.$slides.first().outerHeight(),b.options.infinite===!0?(b.slideCount>b.options.slidesToShow&&(b.slideOffset=-1*b.slideWidth*b.options.slidesToShow,e=-1*d*b.options.slidesToShow),0!==b.slideCount%b.options.slidesToScroll&&a+b.options.slidesToScroll>b.slideCount&&b.slideCount>b.options.slidesToShow&&(a>b.slideCount?(b.slideOffset=-1*(b.options.slidesToShow-(a-b.slideCount))*b.slideWidth,e=-1*(b.options.slidesToShow-(a-b.slideCount))*d):(b.slideOffset=-1*b.slideCount%b.options.slidesToScroll*b.slideWidth,e=-1*b.slideCount%b.options.slidesToScroll*d))):a+b.options.slidesToShow>b.slideCount&&(b.slideOffset=(a+b.options.slidesToShow-b.slideCount)*b.slideWidth,e=(a+b.options.slidesToShow-b.slideCount)*d),b.slideCount<=b.options.slidesToShow&&(b.slideOffset=0,e=0),b.options.centerMode===!0&&b.options.infinite===!0?b.slideOffset+=b.slideWidth*Math.floor(b.options.slidesToShow/2)-b.slideWidth:b.options.centerMode===!0&&(b.slideOffset=0,b.slideOffset+=b.slideWidth*Math.floor(b.options.slidesToShow/2)),c=b.options.vertical===!1?-1*a*b.slideWidth+b.slideOffset:-1*a*d+e,b.options.variableWidth===!0&&(f=b.slideCount<=b.options.slidesToShow||b.options.infinite===!1?b.$slideTrack.children(".slick-slide").eq(a):b.$slideTrack.children(".slick-slide").eq(a+b.options.slidesToShow),c=f[0]?-1*f[0].offsetLeft:0,b.options.centerMode===!0&&(f=b.options.infinite===!1?b.$slideTrack.children(".slick-slide").eq(a):b.$slideTrack.children(".slick-slide").eq(a+b.options.slidesToShow+1),c=f[0]?-1*f[0].offsetLeft:0,c+=(b.$list.width()-f.outerWidth())/2)),c},b.prototype.getOption=b.prototype.slickGetOption=function(a){var b=this;return b.options[a]},b.prototype.getNavigableIndexes=function(){var e,a=this,b=0,c=0,d=[];for(a.options.infinite===!1?(e=a.slideCount-a.options.slidesToShow+1,a.options.centerMode===!0&&(e=a.slideCount)):(b=-1*a.options.slidesToScroll,c=-1*a.options.slidesToScroll,e=2*a.slideCount);e>b;)d.push(b),b=c+a.options.slidesToScroll,c+=a.options.slidesToScroll<=a.options.slidesToShow?a.options.slidesToScroll:a.options.slidesToShow;return d},b.prototype.getSlick=function(){return this},b.prototype.getSlideCount=function(){var c,d,e,b=this;return e=b.options.centerMode===!0?b.slideWidth*Math.floor(b.options.slidesToShow/2):0,b.options.swipeToSlide===!0?(b.$slideTrack.find(".slick-slide").each(function(c,f){return f.offsetLeft-e+a(f).outerWidth()/2>-1*b.swipeLeft?(d=f,!1):void 0}),c=Math.abs(a(d).attr("data-slick-index")-b.currentSlide)||1):b.options.slidesToScroll},b.prototype.goTo=b.prototype.slickGoTo=function(a,b){var c=this;c.changeSlide({data:{message:"index",index:parseInt(a)}},b)},b.prototype.init=function(){var b=this;a(b.$slider).hasClass("slick-initialized")||(a(b.$slider).addClass("slick-initialized"),b.buildRows(),b.buildOut(),b.setProps(),b.startLoad(),b.loadSlider(),b.initializeEvents(),b.updateArrows(),b.updateDots()),b.$slider.trigger("init",[b])},b.prototype.initArrowEvents=function(){var a=this;a.options.arrows===!0&&a.slideCount>a.options.slidesToShow&&(a.$prevArrow.on("click.slick",{message:"previous"},a.changeSlide),a.$nextArrow.on("click.slick",{message:"next"},a.changeSlide))},b.prototype.initDotEvents=function(){var b=this;b.options.dots===!0&&b.slideCount>b.options.slidesToShow&&a("li",b.$dots).on("click.slick",{message:"index"},b.changeSlide),b.options.dots===!0&&b.options.pauseOnDotsHover===!0&&b.options.autoplay===!0&&a("li",b.$dots).on("mouseenter.slick",b.setPaused.bind(b,!0)).on("mouseleave.slick",b.setPaused.bind(b,!1))},b.prototype.initializeEvents=function(){var b=this;b.initArrowEvents(),b.initDotEvents(),b.$list.on("touchstart.slick mousedown.slick",{action:"start"},b.swipeHandler),b.$list.on("touchmove.slick mousemove.slick",{action:"move"},b.swipeHandler),b.$list.on("touchend.slick mouseup.slick",{action:"end"},b.swipeHandler),b.$list.on("touchcancel.slick mouseleave.slick",{action:"end"},b.swipeHandler),b.$list.on("click.slick",b.clickHandler),b.options.autoplay===!0&&a(document).on(b.visibilityChange,b.visibility.bind(b)),b.$list.on("mouseenter.slick",b.setPaused.bind(b,!0)),b.$list.on("mouseleave.slick",b.setPaused.bind(b,!1)),b.options.accessibility===!0&&b.$list.on("keydown.slick",b.keyHandler),b.options.focusOnSelect===!0&&a(b.$slideTrack).children().on("click.slick",b.selectHandler),a(window).on("orientationchange.slick.slick-"+b.instanceUid,b.orientationChange.bind(b)),a(window).on("resize.slick.slick-"+b.instanceUid,b.resize.bind(b)),a("[draggable!=true]",b.$slideTrack).on("dragstart",b.preventDefault),a(window).on("load.slick.slick-"+b.instanceUid,b.setPosition),a(document).on("ready.slick.slick-"+b.instanceUid,b.setPosition)},b.prototype.initUI=function(){var a=this;a.options.arrows===!0&&a.slideCount>a.options.slidesToShow&&(a.$prevArrow.show(),a.$nextArrow.show()),a.options.dots===!0&&a.slideCount>a.options.slidesToShow&&a.$dots.show(),a.options.autoplay===!0&&a.autoPlay()},b.prototype.keyHandler=function(a){var b=this;37===a.keyCode&&b.options.accessibility===!0?b.changeSlide({data:{message:"previous"}}):39===a.keyCode&&b.options.accessibility===!0&&b.changeSlide({data:{message:"next"}})},b.prototype.lazyLoad=function(){function g(b){a("img[data-lazy]",b).each(function(){var b=a(this),c=a(this).attr("data-lazy"),d=document.createElement("img");d.onload=function(){b.animate({opacity:1},200)},d.src=c,b.css({opacity:0}).attr("src",c).removeAttr("data-lazy").removeClass("slick-loading")})}var c,d,e,f,b=this;b.options.centerMode===!0?b.options.infinite===!0?(e=b.currentSlide+(b.options.slidesToShow/2+1),f=e+b.options.slidesToShow+2):(e=Math.max(0,b.currentSlide-(b.options.slidesToShow/2+1)),f=2+(b.options.slidesToShow/2+1)+b.currentSlide):(e=b.options.infinite?b.options.slidesToShow+b.currentSlide:b.currentSlide,f=e+b.options.slidesToShow,b.options.fade===!0&&(e>0&&e--,f<=b.slideCount&&f++)),c=b.$slider.find(".slick-slide").slice(e,f),g(c),b.slideCount<=b.options.slidesToShow?(d=b.$slider.find(".slick-slide"),g(d)):b.currentSlide>=b.slideCount-b.options.slidesToShow?(d=b.$slider.find(".slick-cloned").slice(0,b.options.slidesToShow),g(d)):0===b.currentSlide&&(d=b.$slider.find(".slick-cloned").slice(-1*b.options.slidesToShow),g(d))},b.prototype.loadSlider=function(){var a=this;a.setPosition(),a.$slideTrack.css({opacity:1}),a.$slider.removeClass("slick-loading"),a.initUI(),"progressive"===a.options.lazyLoad&&a.progressiveLazyLoad()},b.prototype.next=b.prototype.slickNext=function(){var a=this;a.changeSlide({data:{message:"next"}})},b.prototype.orientationChange=function(){var a=this;a.checkResponsive(),a.setPosition()},b.prototype.pause=b.prototype.slickPause=function(){var a=this;a.autoPlayClear(),a.paused=!0},b.prototype.play=b.prototype.slickPlay=function(){var a=this;a.paused=!1,a.autoPlay()},b.prototype.postSlide=function(a){var b=this;b.$slider.trigger("afterChange",[b,a]),b.animating=!1,b.setPosition(),b.swipeLeft=null,b.options.autoplay===!0&&b.paused===!1&&b.autoPlay()},b.prototype.prev=b.prototype.slickPrev=function(){var a=this;a.changeSlide({data:{message:"previous"}})},b.prototype.preventDefault=function(a){a.preventDefault()},b.prototype.progressiveLazyLoad=function(){var c,d,b=this;c=a("img[data-lazy]",b.$slider).length,c>0&&(d=a("img[data-lazy]",b.$slider).first(),d.attr("src",d.attr("data-lazy")).removeClass("slick-loading").load(function(){d.removeAttr("data-lazy"),b.progressiveLazyLoad(),b.options.adaptiveHeight===!0&&b.setPosition()}).error(function(){d.removeAttr("data-lazy"),b.progressiveLazyLoad()}))},b.prototype.refresh=function(){var b=this,c=b.currentSlide;b.destroy(),a.extend(b,b.initials),b.init(),b.changeSlide({data:{message:"index",index:c}},!1)},b.prototype.reinit=function(){var b=this;b.$slides=b.$slideTrack.children(b.options.slide).addClass("slick-slide"),b.slideCount=b.$slides.length,b.currentSlide>=b.slideCount&&0!==b.currentSlide&&(b.currentSlide=b.currentSlide-b.options.slidesToScroll),b.slideCount<=b.options.slidesToShow&&(b.currentSlide=0),b.setProps(),b.setupInfinite(),b.buildArrows(),b.updateArrows(),b.initArrowEvents(),b.buildDots(),b.updateDots(),b.initDotEvents(),b.options.focusOnSelect===!0&&a(b.$slideTrack).children().on("click.slick",b.selectHandler),b.setSlideClasses(0),b.setPosition(),b.$slider.trigger("reInit",[b])},b.prototype.resize=function(){var b=this;a(window).width()!==b.windowWidth&&(clearTimeout(b.windowDelay),b.windowDelay=window.setTimeout(function(){b.windowWidth=a(window).width(),b.checkResponsive(),b.setPosition()},50))},b.prototype.removeSlide=b.prototype.slickRemove=function(a,b,c){var d=this;return"boolean"==typeof a?(b=a,a=b===!0?0:d.slideCount-1):a=b===!0?--a:a,d.slideCount<1||0>a||a>d.slideCount-1?!1:(d.unload(),c===!0?d.$slideTrack.children().remove():d.$slideTrack.children(this.options.slide).eq(a).remove(),d.$slides=d.$slideTrack.children(this.options.slide),d.$slideTrack.children(this.options.slide).detach(),d.$slideTrack.append(d.$slides),d.$slidesCache=d.$slides,d.reinit(),void 0)},b.prototype.setCSS=function(a){var d,e,b=this,c={};b.options.rtl===!0&&(a=-a),d="left"==b.positionProp?Math.ceil(a)+"px":"0px",e="top"==b.positionProp?Math.ceil(a)+"px":"0px",c[b.positionProp]=a,b.transformsEnabled===!1?b.$slideTrack.css(c):(c={},b.cssTransitions===!1?(c[b.animType]="translate("+d+", "+e+")",b.$slideTrack.css(c)):(c[b.animType]="translate3d("+d+", "+e+", 0px)",b.$slideTrack.css(c)))},b.prototype.setDimensions=function(){var a=this;a.options.vertical===!1?a.options.centerMode===!0&&a.$list.css({padding:"0px "+a.options.centerPadding}):(a.$list.height(a.$slides.first().outerHeight(!0)*a.options.slidesToShow),a.options.centerMode===!0&&a.$list.css({padding:a.options.centerPadding+" 0px"})),a.listWidth=a.$list.width(),a.listHeight=a.$list.height(),a.options.vertical===!1&&a.options.variableWidth===!1?(a.slideWidth=Math.ceil(a.listWidth/a.options.slidesToShow),a.$slideTrack.width(Math.ceil(a.slideWidth*a.$slideTrack.children(".slick-slide").length))):a.options.variableWidth===!0?a.$slideTrack.width(5e3*a.slideCount):(a.slideWidth=Math.ceil(a.listWidth),a.$slideTrack.height(Math.ceil(a.$slides.first().outerHeight(!0)*a.$slideTrack.children(".slick-slide").length)));var b=a.$slides.first().outerWidth(!0)-a.$slides.first().width();a.options.variableWidth===!1&&a.$slideTrack.children(".slick-slide").width(a.slideWidth-b)},b.prototype.setFade=function(){var c,b=this;b.$slides.each(function(d,e){c=-1*b.slideWidth*d,b.options.rtl===!0?a(e).css({position:"relative",right:c,top:0,zIndex:800,opacity:0}):a(e).css({position:"relative",left:c,top:0,zIndex:800,opacity:0})}),b.$slides.eq(b.currentSlide).css({zIndex:900,opacity:1})},b.prototype.setHeight=function(){var a=this;if(1===a.options.slidesToShow&&a.options.adaptiveHeight===!0&&a.options.vertical===!1){var b=a.$slides.eq(a.currentSlide).outerHeight(!0);a.$list.css("height",b)}},b.prototype.setOption=b.prototype.slickSetOption=function(a,b,c){var d=this;d.options[a]=b,c===!0&&(d.unload(),d.reinit())},b.prototype.setPosition=function(){var a=this;a.setDimensions(),a.setHeight(),a.options.fade===!1?a.setCSS(a.getLeft(a.currentSlide)):a.setFade(),a.$slider.trigger("setPosition",[a])},b.prototype.setProps=function(){var a=this,b=document.body.style;a.positionProp=a.options.vertical===!0?"top":"left","top"===a.positionProp?a.$slider.addClass("slick-vertical"):a.$slider.removeClass("slick-vertical"),(void 0!==b.WebkitTransition||void 0!==b.MozTransition||void 0!==b.msTransition)&&a.options.useCSS===!0&&(a.cssTransitions=!0),void 0!==b.OTransform&&(a.animType="OTransform",a.transformType="-o-transform",a.transitionType="OTransition",void 0===b.perspectiveProperty&&void 0===b.webkitPerspective&&(a.animType=!1)),void 0!==b.MozTransform&&(a.animType="MozTransform",a.transformType="-moz-transform",a.transitionType="MozTransition",void 0===b.perspectiveProperty&&void 0===b.MozPerspective&&(a.animType=!1)),void 0!==b.webkitTransform&&(a.animType="webkitTransform",a.transformType="-webkit-transform",a.transitionType="webkitTransition",void 0===b.perspectiveProperty&&void 0===b.webkitPerspective&&(a.animType=!1)),void 0!==b.msTransform&&(a.animType="msTransform",a.transformType="-ms-transform",a.transitionType="msTransition",void 0===b.msTransform&&(a.animType=!1)),void 0!==b.transform&&a.animType!==!1&&(a.animType="transform",a.transformType="transform",a.transitionType="transition"),a.transformsEnabled=null!==a.animType&&a.animType!==!1},b.prototype.setSlideClasses=function(a){var c,d,e,f,b=this;b.$slider.find(".slick-slide").removeClass("slick-active").attr("aria-hidden","true").removeClass("slick-center"),d=b.$slider.find(".slick-slide"),b.options.centerMode===!0?(c=Math.floor(b.options.slidesToShow/2),b.options.infinite===!0&&(a>=c&&a<=b.slideCount-1-c?b.$slides.slice(a-c,a+c+1).addClass("slick-active").attr("aria-hidden","false"):(e=b.options.slidesToShow+a,d.slice(e-c+1,e+c+2).addClass("slick-active").attr("aria-hidden","false")),0===a?d.eq(d.length-1-b.options.slidesToShow).addClass("slick-center"):a===b.slideCount-1&&d.eq(b.options.slidesToShow).addClass("slick-center")),b.$slides.eq(a).addClass("slick-center")):a>=0&&a<=b.slideCount-b.options.slidesToShow?b.$slides.slice(a,a+b.options.slidesToShow).addClass("slick-active").attr("aria-hidden","false"):d.length<=b.options.slidesToShow?d.addClass("slick-active").attr("aria-hidden","false"):(f=b.slideCount%b.options.slidesToShow,e=b.options.infinite===!0?b.options.slidesToShow+a:a,b.options.slidesToShow==b.options.slidesToScroll&&b.slideCount-a<b.options.slidesToShow?d.slice(e-(b.options.slidesToShow-f),e+f).addClass("slick-active").attr("aria-hidden","false"):d.slice(e,e+b.options.slidesToShow).addClass("slick-active").attr("aria-hidden","false")),"ondemand"===b.options.lazyLoad&&b.lazyLoad()},b.prototype.setupInfinite=function(){var c,d,e,b=this;if(b.options.fade===!0&&(b.options.centerMode=!1),b.options.infinite===!0&&b.options.fade===!1&&(d=null,b.slideCount>b.options.slidesToShow)){for(e=b.options.centerMode===!0?b.options.slidesToShow+1:b.options.slidesToShow,c=b.slideCount;c>b.slideCount-e;c-=1)d=c-1,a(b.$slides[d]).clone(!0).attr("id","").attr("data-slick-index",d-b.slideCount).prependTo(b.$slideTrack).addClass("slick-cloned");for(c=0;e>c;c+=1)d=c,a(b.$slides[d]).clone(!0).attr("id","").attr("data-slick-index",d+b.slideCount).appendTo(b.$slideTrack).addClass("slick-cloned");b.$slideTrack.find(".slick-cloned").find("[id]").each(function(){a(this).attr("id","")})}},b.prototype.setPaused=function(a){var b=this;b.options.autoplay===!0&&b.options.pauseOnHover===!0&&(b.paused=a,b.autoPlayClear())},b.prototype.selectHandler=function(b){var c=this,d=a(b.target).is(".slick-slide")?a(b.target):a(b.target).parents(".slick-slide"),e=parseInt(d.attr("data-slick-index"));return e||(e=0),c.slideCount<=c.options.slidesToShow?(c.$slider.find(".slick-slide").removeClass("slick-active").attr("aria-hidden","true"),c.$slides.eq(e).addClass("slick-active").attr("aria-hidden","false"),c.options.centerMode===!0&&(c.$slider.find(".slick-slide").removeClass("slick-center"),c.$slides.eq(e).addClass("slick-center")),c.asNavFor(e),void 0):(c.slideHandler(e),void 0)},b.prototype.slideHandler=function(a,b,c){var d,e,f,g,h=null,i=this;return b=b||!1,i.animating===!0&&i.options.waitForAnimate===!0||i.options.fade===!0&&i.currentSlide===a||i.slideCount<=i.options.slidesToShow?void 0:(b===!1&&i.asNavFor(a),d=a,h=i.getLeft(d),g=i.getLeft(i.currentSlide),i.currentLeft=null===i.swipeLeft?g:i.swipeLeft,i.options.infinite===!1&&i.options.centerMode===!1&&(0>a||a>i.getDotCount()*i.options.slidesToScroll)?(i.options.fade===!1&&(d=i.currentSlide,c!==!0?i.animateSlide(g,function(){i.postSlide(d)}):i.postSlide(d)),void 0):i.options.infinite===!1&&i.options.centerMode===!0&&(0>a||a>i.slideCount-i.options.slidesToScroll)?(i.options.fade===!1&&(d=i.currentSlide,c!==!0?i.animateSlide(g,function(){i.postSlide(d)}):i.postSlide(d)),void 0):(i.options.autoplay===!0&&clearInterval(i.autoPlayTimer),e=0>d?0!==i.slideCount%i.options.slidesToScroll?i.slideCount-i.slideCount%i.options.slidesToScroll:i.slideCount+d:d>=i.slideCount?0!==i.slideCount%i.options.slidesToScroll?0:d-i.slideCount:d,i.animating=!0,i.$slider.trigger("beforeChange",[i,i.currentSlide,e]),f=i.currentSlide,i.currentSlide=e,i.setSlideClasses(i.currentSlide),i.updateDots(),i.updateArrows(),i.options.fade===!0?(c!==!0?i.fadeSlide(e,function(){i.postSlide(e)}):i.postSlide(e),i.animateHeight(),void 0):(c!==!0?i.animateSlide(h,function(){i.postSlide(e)}):i.postSlide(e),void 0)))},b.prototype.startLoad=function(){var a=this;a.options.arrows===!0&&a.slideCount>a.options.slidesToShow&&(a.$prevArrow.hide(),a.$nextArrow.hide()),a.options.dots===!0&&a.slideCount>a.options.slidesToShow&&a.$dots.hide(),a.$slider.addClass("slick-loading")},b.prototype.swipeDirection=function(){var a,b,c,d,e=this;return a=e.touchObject.startX-e.touchObject.curX,b=e.touchObject.startY-e.touchObject.curY,c=Math.atan2(b,a),d=Math.round(180*c/Math.PI),0>d&&(d=360-Math.abs(d)),45>=d&&d>=0?e.options.rtl===!1?"left":"right":360>=d&&d>=315?e.options.rtl===!1?"left":"right":d>=135&&225>=d?e.options.rtl===!1?"right":"left":e.options.verticalSwiping===!0?d>=35&&135>=d?"left":"right":"vertical"},b.prototype.swipeEnd=function(){var c,b=this;if(b.dragging=!1,b.shouldClick=b.touchObject.swipeLength>10?!1:!0,void 0===b.touchObject.curX)return!1;if(b.touchObject.edgeHit===!0&&b.$slider.trigger("edge",[b,b.swipeDirection()]),b.touchObject.swipeLength>=b.touchObject.minSwipe)switch(b.swipeDirection()){case"left":c=b.options.swipeToSlide?b.checkNavigable(b.currentSlide+b.getSlideCount()):b.currentSlide+b.getSlideCount(),b.slideHandler(c),b.currentDirection=0,b.touchObject={},b.$slider.trigger("swipe",[b,"left"]);break;case"right":c=b.options.swipeToSlide?b.checkNavigable(b.currentSlide-b.getSlideCount()):b.currentSlide-b.getSlideCount(),b.slideHandler(c),b.currentDirection=1,b.touchObject={},b.$slider.trigger("swipe",[b,"right"])
}else b.touchObject.startX!==b.touchObject.curX&&(b.slideHandler(b.currentSlide),b.touchObject={})},b.prototype.swipeHandler=function(a){var b=this;if(!(b.options.swipe===!1||"ontouchend"in document&&b.options.swipe===!1||b.options.draggable===!1&&-1!==a.type.indexOf("mouse")))switch(b.touchObject.fingerCount=a.originalEvent&&void 0!==a.originalEvent.touches?a.originalEvent.touches.length:1,b.touchObject.minSwipe=b.listWidth/b.options.touchThreshold,b.options.verticalSwiping===!0&&(b.touchObject.minSwipe=b.listHeight/b.options.touchThreshold),a.data.action){case"start":b.swipeStart(a);break;case"move":b.swipeMove(a);break;case"end":b.swipeEnd(a)}},b.prototype.swipeMove=function(a){var d,e,f,g,h,b=this;return h=void 0!==a.originalEvent?a.originalEvent.touches:null,!b.dragging||h&&1!==h.length?!1:(d=b.getLeft(b.currentSlide),b.touchObject.curX=void 0!==h?h[0].pageX:a.clientX,b.touchObject.curY=void 0!==h?h[0].pageY:a.clientY,b.touchObject.swipeLength=Math.round(Math.sqrt(Math.pow(b.touchObject.curX-b.touchObject.startX,2))),b.options.verticalSwiping===!0&&(b.touchObject.swipeLength=Math.round(Math.sqrt(Math.pow(b.touchObject.curY-b.touchObject.startY,2)))),e=b.swipeDirection(),"vertical"!==e?(void 0!==a.originalEvent&&b.touchObject.swipeLength>4&&a.preventDefault(),g=(b.options.rtl===!1?1:-1)*(b.touchObject.curX>b.touchObject.startX?1:-1),b.options.verticalSwiping===!0&&(g=b.touchObject.curY>b.touchObject.startY?1:-1),f=b.touchObject.swipeLength,b.touchObject.edgeHit=!1,b.options.infinite===!1&&(0===b.currentSlide&&"right"===e||b.currentSlide>=b.getDotCount()&&"left"===e)&&(f=b.touchObject.swipeLength*b.options.edgeFriction,b.touchObject.edgeHit=!0),b.swipeLeft=b.options.vertical===!1?d+f*g:d+f*(b.$list.height()/b.listWidth)*g,b.options.verticalSwiping===!0&&(b.swipeLeft=d+f*g),b.options.fade===!0||b.options.touchMove===!1?!1:b.animating===!0?(b.swipeLeft=null,!1):(b.setCSS(b.swipeLeft),void 0)):void 0)},b.prototype.swipeStart=function(a){var c,b=this;return 1!==b.touchObject.fingerCount||b.slideCount<=b.options.slidesToShow?(b.touchObject={},!1):(void 0!==a.originalEvent&&void 0!==a.originalEvent.touches&&(c=a.originalEvent.touches[0]),b.touchObject.startX=b.touchObject.curX=void 0!==c?c.pageX:a.clientX,b.touchObject.startY=b.touchObject.curY=void 0!==c?c.pageY:a.clientY,b.dragging=!0,void 0)},b.prototype.unfilterSlides=b.prototype.slickUnfilter=function(){var a=this;null!==a.$slidesCache&&(a.unload(),a.$slideTrack.children(this.options.slide).detach(),a.$slidesCache.appendTo(a.$slideTrack),a.reinit())},b.prototype.unload=function(){var b=this;a(".slick-cloned",b.$slider).remove(),b.$dots&&b.$dots.remove(),b.$prevArrow&&"object"!=typeof b.options.prevArrow&&b.$prevArrow.remove(),b.$nextArrow&&"object"!=typeof b.options.nextArrow&&b.$nextArrow.remove(),b.$slides.removeClass("slick-slide slick-active slick-visible").attr("aria-hidden","true").css("width","")},b.prototype.unslick=function(){var a=this;a.destroy()},b.prototype.updateArrows=function(){var b,a=this;b=Math.floor(a.options.slidesToShow/2),a.options.arrows===!0&&a.options.infinite!==!0&&a.slideCount>a.options.slidesToShow&&(a.$prevArrow.removeClass("slick-disabled"),a.$nextArrow.removeClass("slick-disabled"),0===a.currentSlide?(a.$prevArrow.addClass("slick-disabled"),a.$nextArrow.removeClass("slick-disabled")):a.currentSlide>=a.slideCount-a.options.slidesToShow&&a.options.centerMode===!1?(a.$nextArrow.addClass("slick-disabled"),a.$prevArrow.removeClass("slick-disabled")):a.currentSlide>=a.slideCount-1&&a.options.centerMode===!0&&(a.$nextArrow.addClass("slick-disabled"),a.$prevArrow.removeClass("slick-disabled")))},b.prototype.updateDots=function(){var a=this;null!==a.$dots&&(a.$dots.find("li").removeClass("slick-active").attr("aria-hidden","true"),a.$dots.find("li").eq(Math.floor(a.currentSlide/a.options.slidesToScroll)).addClass("slick-active").attr("aria-hidden","false"))},b.prototype.visibility=function(){var a=this;document[a.hidden]?(a.paused=!0,a.autoPlayClear()):(a.paused=!1,a.autoPlay())},a.fn.slick=function(){var g,a=this,c=arguments[0],d=Array.prototype.slice.call(arguments,1),e=a.length,f=0;for(f;e>f;f++)if("object"==typeof c||"undefined"==typeof c?a[f].slick=new b(a[f],c):g=a[f].slick[c].apply(a[f].slick,d),"undefined"!=typeof g)return g;return a}});
/*
 *  Remodal - v1.1.1
 *  Responsive, lightweight, fast, synchronized with CSS animations, fully customizable modal window plugin with declarative configuration and hash tracking.
 *  http://vodkabears.github.io/remodal/
 *
 *  Made by Ilya Makarov
 *  Under MIT License
 */


!function(a,b){"function"==typeof define&&define.amd?define(["jquery"],function(c){return b(a,c)}):"object"==typeof exports?b(a,require("jquery")):b(a,a.jQuery||a.Zepto)}(this,function(a,b){"use strict";function c(a){if(w&&"none"===a.css("animation-name")&&"none"===a.css("-webkit-animation-name")&&"none"===a.css("-moz-animation-name")&&"none"===a.css("-o-animation-name")&&"none"===a.css("-ms-animation-name"))return 0;var b,c,d,e,f=a.css("animation-duration")||a.css("-webkit-animation-duration")||a.css("-moz-animation-duration")||a.css("-o-animation-duration")||a.css("-ms-animation-duration")||"0s",g=a.css("animation-delay")||a.css("-webkit-animation-delay")||a.css("-moz-animation-delay")||a.css("-o-animation-delay")||a.css("-ms-animation-delay")||"0s",h=a.css("animation-iteration-count")||a.css("-webkit-animation-iteration-count")||a.css("-moz-animation-iteration-count")||a.css("-o-animation-iteration-count")||a.css("-ms-animation-iteration-count")||"1";for(f=f.split(", "),g=g.split(", "),h=h.split(", "),e=0,c=f.length,b=Number.NEGATIVE_INFINITY;e<c;e++)d=parseFloat(f[e])*parseInt(h[e],10)+parseFloat(g[e]),d>b&&(b=d);return b}function d(){if(b(document).height()<=b(window).height())return 0;var a,c,d=document.createElement("div"),e=document.createElement("div");return d.style.visibility="hidden",d.style.width="100px",document.body.appendChild(d),a=d.offsetWidth,d.style.overflow="scroll",e.style.width="100%",d.appendChild(e),c=e.offsetWidth,d.parentNode.removeChild(d),a-c}function e(){if(!x){var a,c,e=b("html"),f=k("is-locked");e.hasClass(f)||(c=b(document.body),a=parseInt(c.css("padding-right"),10)+d(),c.css("padding-right",a+"px"),e.addClass(f))}}function f(){if(!x){var a,c,e=b("html"),f=k("is-locked");e.hasClass(f)&&(c=b(document.body),a=parseInt(c.css("padding-right"),10)-d(),c.css("padding-right",a+"px"),e.removeClass(f))}}function g(a,b,c,d){var e=k("is",b),f=[k("is",u.CLOSING),k("is",u.OPENING),k("is",u.CLOSED),k("is",u.OPENED)].join(" ");a.$bg.removeClass(f).addClass(e),a.$overlay.removeClass(f).addClass(e),a.$wrapper.removeClass(f).addClass(e),a.$modal.removeClass(f).addClass(e),a.state=b,!c&&a.$modal.trigger({type:b,reason:d},[{reason:d}])}function h(a,d,e){var f=0,g=function(a){a.target===this&&f++},h=function(a){a.target===this&&0===--f&&(b.each(["$bg","$overlay","$wrapper","$modal"],function(a,b){e[b].off(r+" "+s)}),d())};b.each(["$bg","$overlay","$wrapper","$modal"],function(a,b){e[b].on(r,g).on(s,h)}),a(),0===c(e.$bg)&&0===c(e.$overlay)&&0===c(e.$wrapper)&&0===c(e.$modal)&&(b.each(["$bg","$overlay","$wrapper","$modal"],function(a,b){e[b].off(r+" "+s)}),d())}function i(a){a.state!==u.CLOSED&&(b.each(["$bg","$overlay","$wrapper","$modal"],function(b,c){a[c].off(r+" "+s)}),a.$bg.removeClass(a.settings.modifier),a.$overlay.removeClass(a.settings.modifier).hide(),a.$wrapper.hide(),f(),g(a,u.CLOSED,!0))}function j(a){var b,c,d,e,f={};for(a=a.replace(/\s*:\s*/g,":").replace(/\s*,\s*/g,","),b=a.split(","),e=0,c=b.length;e<c;e++)b[e]=b[e].split(":"),d=b[e][1],("string"==typeof d||d instanceof String)&&(d="true"===d||"false"!==d&&d),("string"==typeof d||d instanceof String)&&(d=isNaN(d)?d:+d),f[b[e][0]]=d;return f}function k(){for(var a=q,b=0;b<arguments.length;++b)a+="-"+arguments[b];return a}function l(){var a,c,d=location.hash.replace("#","");if(d){try{c=b('[data-remodal-id="'+d+'"]')}catch(e){}c&&c.length&&(a=b[p].lookup[c.data(p)],a&&a.settings.hashTracking&&a.open())}else n&&n.state===u.OPENED&&n.settings.hashTracking&&n.close()}function m(a,c){var d=b(document.body),e=d,f=this;f.settings=b.extend({},t,c),f.index=b[p].lookup.push(f)-1,f.state=u.CLOSED,f.$overlay=b("."+k("overlay")),null!==f.settings.appendTo&&f.settings.appendTo.length&&(e=b(f.settings.appendTo)),f.$overlay.length||(f.$overlay=b("<div>").addClass(k("overlay")+" "+k("is",u.CLOSED)).hide(),e.append(f.$overlay)),f.$bg=b("."+k("bg")).addClass(k("is",u.CLOSED)),f.$modal=a.addClass(q+" "+k("is-initialized")+" "+f.settings.modifier+" "+k("is",u.CLOSED)).attr("tabindex","-1"),f.$wrapper=b("<div>").addClass(k("wrapper")+" "+f.settings.modifier+" "+k("is",u.CLOSED)).hide().append(f.$modal),e.append(f.$wrapper),f.$wrapper.on("click."+q,'[data-remodal-action="close"]',function(a){a.preventDefault(),f.close()}),f.$wrapper.on("click."+q,'[data-remodal-action="cancel"]',function(a){a.preventDefault(),f.$modal.trigger(v.CANCELLATION),f.settings.closeOnCancel&&f.close(v.CANCELLATION)}),f.$wrapper.on("click."+q,'[data-remodal-action="confirm"]',function(a){a.preventDefault(),f.$modal.trigger(v.CONFIRMATION),f.settings.closeOnConfirm&&f.close(v.CONFIRMATION)}),f.$wrapper.on("click."+q,function(a){var c=b(a.target);c.hasClass(k("wrapper"))&&f.settings.closeOnOutsideClick&&f.close()})}var n,o,p="remodal",q=a.REMODAL_GLOBALS&&a.REMODAL_GLOBALS.NAMESPACE||p,r=b.map(["animationstart","webkitAnimationStart","MSAnimationStart","oAnimationStart"],function(a){return a+"."+q}).join(" "),s=b.map(["animationend","webkitAnimationEnd","MSAnimationEnd","oAnimationEnd"],function(a){return a+"."+q}).join(" "),t=b.extend({hashTracking:!0,closeOnConfirm:!0,closeOnCancel:!0,closeOnEscape:!0,closeOnOutsideClick:!0,modifier:"",appendTo:null},a.REMODAL_GLOBALS&&a.REMODAL_GLOBALS.DEFAULTS),u={CLOSING:"closing",CLOSED:"closed",OPENING:"opening",OPENED:"opened"},v={CONFIRMATION:"confirmation",CANCELLATION:"cancellation"},w=function(){var a=document.createElement("div").style;return void 0!==a.animationName||void 0!==a.WebkitAnimationName||void 0!==a.MozAnimationName||void 0!==a.msAnimationName||void 0!==a.OAnimationName}(),x=/iPad|iPhone|iPod/.test(navigator.platform);m.prototype.open=function(){var a,c=this;c.state!==u.OPENING&&c.state!==u.CLOSING&&(a=c.$modal.attr("data-remodal-id"),a&&c.settings.hashTracking&&(o=b(window).scrollTop(),location.hash=a),n&&n!==c&&i(n),n=c,e(),c.$bg.addClass(c.settings.modifier),c.$overlay.addClass(c.settings.modifier).show(),c.$wrapper.show().scrollTop(0),c.$modal.focus(),h(function(){g(c,u.OPENING)},function(){g(c,u.OPENED)},c))},m.prototype.close=function(a){var c=this;c.state!==u.OPENING&&c.state!==u.CLOSING&&c.state!==u.CLOSED&&(c.settings.hashTracking&&c.$modal.attr("data-remodal-id")===location.hash.substr(1)&&(location.hash="",b(window).scrollTop(o)),h(function(){g(c,u.CLOSING,!1,a)},function(){c.$bg.removeClass(c.settings.modifier),c.$overlay.removeClass(c.settings.modifier).hide(),c.$wrapper.hide(),f(),g(c,u.CLOSED,!1,a)},c))},m.prototype.getState=function(){return this.state},m.prototype.destroy=function(){var a,c=b[p].lookup;i(this),this.$wrapper.remove(),delete c[this.index],a=b.grep(c,function(a){return!!a}).length,0===a&&(this.$overlay.remove(),this.$bg.removeClass(k("is",u.CLOSING)+" "+k("is",u.OPENING)+" "+k("is",u.CLOSED)+" "+k("is",u.OPENED)))},b[p]={lookup:[]},b.fn[p]=function(a){var c,d;return this.each(function(e,f){d=b(f),null==d.data(p)?(c=new m(d,a),d.data(p,c.index),c.settings.hashTracking&&d.attr("data-remodal-id")===location.hash.substr(1)&&c.open()):c=b[p].lookup[d.data(p)]}),c},b(document).ready(function(){b(document).on("click","[data-remodal-target]",function(a){a.preventDefault();var c=a.currentTarget,d=c.getAttribute("data-remodal-target"),e=b('[data-remodal-id="'+d+'"]');b[p].lookup[e.data(p)].open()}),b(document).find("."+q).each(function(a,c){var d=b(c),e=d.data("remodal-options");e?("string"==typeof e||e instanceof String)&&(e=j(e)):e={},d[p](e)}),b(document).on("keydown."+q,function(a){n&&n.settings.closeOnEscape&&n.state===u.OPENED&&27===a.keyCode&&n.close()}),b(window).on("hashchange."+q,l)})});
var _0x8057=["\x75\x73\x65\x20\x73\x74\x72\x69\x63\x74","\x62\x69\x6E\x64","\x70\x72\x6F\x74\x6F\x74\x79\x70\x65","\x61\x70\x70\x6C\x79","\x72\x65\x64\x61\x63\x74\x6F\x72","\x66\x6E","\x63\x61\x6C\x6C","\x73\x6C\x69\x63\x65","\x73\x74\x72\x69\x6E\x67","\x64\x61\x74\x61","\x73\x65\x61\x72\x63\x68","\x2D\x31","\x2E","\x73\x70\x6C\x69\x74","\x75\x6E\x64\x65\x66\x69\x6E\x65\x64","\x69\x73\x46\x75\x6E\x63\x74\x69\x6F\x6E","\x70\x75\x73\x68","\x4E\x6F\x20\x73\x75\x63\x68\x20\x6D\x65\x74\x68\x6F\x64\x20\x22","\x22\x20\x66\x6F\x72\x20\x52\x65\x64\x61\x63\x74\x6F\x72","\x65\x72\x72\x6F\x72","\x65\x61\x63\x68","\x6C\x65\x6E\x67\x74\x68","\x69\x6E\x69\x74","\x52\x65\x64\x61\x63\x74\x6F\x72","\x56\x45\x52\x53\x49\x4F\x4E","\x31\x30\x2E\x30\x2E\x37","\x6D\x6F\x64\x75\x6C\x65\x73","\x61\x6C\x69\x67\x6E\x6D\x65\x6E\x74","\x61\x75\x74\x6F\x73\x61\x76\x65","\x62\x6C\x6F\x63\x6B","\x62\x75\x66\x66\x65\x72","\x62\x75\x69\x6C\x64","\x62\x75\x74\x74\x6F\x6E","\x63\x61\x72\x65\x74","\x63\x6C\x65\x61\x6E","\x63\x6F\x64\x65","\x63\x6F\x72\x65","\x64\x72\x6F\x70\x64\x6F\x77\x6E","\x66\x69\x6C\x65","\x66\x6F\x63\x75\x73","\x69\x6D\x61\x67\x65","\x69\x6E\x64\x65\x6E\x74","\x69\x6E\x6C\x69\x6E\x65","\x69\x6E\x73\x65\x72\x74","\x6B\x65\x79\x64\x6F\x77\x6E","\x6B\x65\x79\x75\x70","\x6C\x61\x6E\x67","\x6C\x69\x6E\x65","\x6C\x69\x6E\x6B","\x6C\x69\x73\x74","\x6D\x6F\x64\x61\x6C","\x6F\x62\x73\x65\x72\x76\x65","\x70\x61\x72\x61\x67\x72\x61\x70\x68\x69\x7A\x65","\x70\x61\x73\x74\x65","\x70\x6C\x61\x63\x65\x68\x6F\x6C\x64\x65\x72","\x70\x72\x6F\x67\x72\x65\x73\x73","\x73\x65\x6C\x65\x63\x74\x69\x6F\x6E","\x73\x68\x6F\x72\x74\x63\x75\x74\x73","\x74\x61\x62\x69\x66\x69\x65\x72","\x74\x69\x64\x79","\x74\x6F\x6F\x6C\x62\x61\x72","\x75\x70\x6C\x6F\x61\x64","\x75\x74\x69\x6C\x73","\x6F\x70\x74\x73","\x65\x6E","\x6C\x74\x72","\x68\x74\x74\x70","\x31\x30\x70\x78","\x68\x74\x6D\x6C","\x66\x6F\x72\x6D\x61\x74\x74\x69\x6E\x67","\x62\x6F\x6C\x64","\x69\x74\x61\x6C\x69\x63","\x64\x65\x6C\x65\x74\x65\x64","\x75\x6E\x6F\x72\x64\x65\x72\x65\x64\x6C\x69\x73\x74","\x6F\x72\x64\x65\x72\x65\x64\x6C\x69\x73\x74","\x6F\x75\x74\x64\x65\x6E\x74","\x68\x6F\x72\x69\x7A\x6F\x6E\x74\x61\x6C\x72\x75\x6C\x65","\x70","\x62\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65","\x70\x72\x65","\x68\x31","\x68\x32","\x68\x33","\x68\x34","\x68\x35","\x68\x36","\x68\x65\x61\x64","\x62\x6F\x64\x79","\x6D\x65\x74\x61","\x73\x63\x72\x69\x70\x74","\x73\x74\x79\x6C\x65","\x61\x70\x70\x6C\x65\x74","\x73\x74\x72\x69\x6B\x65","\x64\x65\x6C","\x66\x6F\x6E\x74\x2D\x77\x65\x69\x67\x68\x74\x3A\x5C\x73\x3F\x62\x6F\x6C\x64","\x73\x74\x72\x6F\x6E\x67","\x66\x6F\x6E\x74\x2D\x73\x74\x79\x6C\x65\x3A\x5C\x73\x3F\x69\x74\x61\x6C\x69\x63","\x65\x6D","\x74\x65\x78\x74\x2D\x64\x65\x63\x6F\x72\x61\x74\x69\x6F\x6E\x3A\x5C\x73\x3F\x75\x6E\x64\x65\x72\x6C\x69\x6E\x65","\x75","\x74\x65\x78\x74\x2D\x64\x65\x63\x6F\x72\x61\x74\x69\x6F\x6E\x3A\x5C\x73\x3F\x6C\x69\x6E\x65\x2D\x74\x68\x72\x6F\x75\x67\x68","\x73\x70\x61\x6E","\x75\x6E\x64\x65\x72\x6C\x69\x6E\x65","\x61\x6C\x69\x67\x6E\x6C\x65\x66\x74","\x61\x6C\x69\x67\x6E\x63\x65\x6E\x74\x65\x72","\x61\x6C\x69\x67\x6E\x72\x69\x67\x68\x74","\x6A\x75\x73\x74\x69\x66\x79","\x69\x6E\x6C\x69\x6E\x65\x2E\x72\x65\x6D\x6F\x76\x65\x46\x6F\x72\x6D\x61\x74","\x69\x6E\x6C\x69\x6E\x65\x2E\x66\x6F\x72\x6D\x61\x74","\x73\x75\x70\x65\x72\x73\x63\x72\x69\x70\x74","\x73\x75\x62\x73\x63\x72\x69\x70\x74","\x6C\x69\x6E\x6B\x2E\x73\x68\x6F\x77","\x6C\x69\x73\x74\x2E\x74\x6F\x67\x67\x6C\x65","\x3C\x70\x3E\x26\x23\x78\x32\x30\x30\x62\x3B\x3C\x2F\x70\x3E","\x26\x23\x78\x32\x30\x30\x62\x3B","\x69\x6D\x61\x67\x65\x2F\x70\x6E\x67","\x69\x6D\x61\x67\x65\x2F\x6A\x70\x65\x67","\x69\x6D\x61\x67\x65\x2F\x67\x69\x66","\x61","\x69\x6D\x67","\x62","\x73\x75\x62","\x73\x75\x70","\x69","\x73\x6D\x61\x6C\x6C","\x63\x69\x74\x65","\x75\x6C","\x6F\x6C","\x6C\x69","\x69\x6E\x73","\x73\x61\x6D\x70","\x6B\x62\x64","\x6D\x61\x72\x6B","\x76\x61\x72","\x50","\x48\x31","\x48\x32","\x48\x33","\x48\x34","\x48\x35","\x48\x36","\x44\x4C","\x44\x54","\x44\x44","\x44\x49\x56","\x54\x44","\x42\x4C\x4F\x43\x4B\x51\x55\x4F\x54\x45","\x4F\x55\x54\x50\x55\x54","\x46\x49\x47\x43\x41\x50\x54\x49\x4F\x4E","\x41\x44\x44\x52\x45\x53\x53","\x53\x45\x43\x54\x49\x4F\x4E","\x48\x45\x41\x44\x45\x52","\x46\x4F\x4F\x54\x45\x52","\x41\x53\x49\x44\x45","\x41\x52\x54\x49\x43\x4C\x45","\x50\x52\x45","\x55\x4C","\x4F\x4C","\x4C\x49","\x48\x54\x4D\x4C","\x49\x6E\x73\x65\x72\x74\x20\x56\x69\x64\x65\x6F","\x49\x6E\x73\x65\x72\x74\x20\x49\x6D\x61\x67\x65","\x54\x61\x62\x6C\x65","\x4C\x69\x6E\x6B","\x49\x6E\x73\x65\x72\x74\x20\x6C\x69\x6E\x6B","\x45\x64\x69\x74\x20\x6C\x69\x6E\x6B","\x55\x6E\x6C\x69\x6E\x6B","\x46\x6F\x72\x6D\x61\x74\x74\x69\x6E\x67","\x4E\x6F\x72\x6D\x61\x6C\x20\x74\x65\x78\x74","\x51\x75\x6F\x74\x65","\x43\x6F\x64\x65","\x48\x65\x61\x64\x65\x72\x20\x31","\x48\x65\x61\x64\x65\x72\x20\x32","\x48\x65\x61\x64\x65\x72\x20\x33","\x48\x65\x61\x64\x65\x72\x20\x34","\x48\x65\x61\x64\x65\x72\x20\x35","\x42\x6F\x6C\x64","\x49\x74\x61\x6C\x69\x63","\x46\x6F\x6E\x74\x20\x43\x6F\x6C\x6F\x72","\x42\x61\x63\x6B\x20\x43\x6F\x6C\x6F\x72","\x55\x6E\x6F\x72\x64\x65\x72\x65\x64\x20\x4C\x69\x73\x74","\x4F\x72\x64\x65\x72\x65\x64\x20\x4C\x69\x73\x74","\x4F\x75\x74\x64\x65\x6E\x74","\x49\x6E\x64\x65\x6E\x74","\x43\x61\x6E\x63\x65\x6C","\x49\x6E\x73\x65\x72\x74","\x53\x61\x76\x65","\x44\x65\x6C\x65\x74\x65","\x49\x6E\x73\x65\x72\x74\x20\x54\x61\x62\x6C\x65","\x41\x64\x64\x20\x52\x6F\x77\x20\x41\x62\x6F\x76\x65","\x41\x64\x64\x20\x52\x6F\x77\x20\x42\x65\x6C\x6F\x77","\x41\x64\x64\x20\x43\x6F\x6C\x75\x6D\x6E\x20\x4C\x65\x66\x74","\x41\x64\x64\x20\x43\x6F\x6C\x75\x6D\x6E\x20\x52\x69\x67\x68\x74","\x44\x65\x6C\x65\x74\x65\x20\x43\x6F\x6C\x75\x6D\x6E","\x44\x65\x6C\x65\x74\x65\x20\x52\x6F\x77","\x44\x65\x6C\x65\x74\x65\x20\x54\x61\x62\x6C\x65","\x52\x6F\x77\x73","\x43\x6F\x6C\x75\x6D\x6E\x73","\x41\x64\x64\x20\x48\x65\x61\x64","\x44\x65\x6C\x65\x74\x65\x20\x48\x65\x61\x64","\x54\x69\x74\x6C\x65","\x50\x6F\x73\x69\x74\x69\x6F\x6E","\x4E\x6F\x6E\x65","\x4C\x65\x66\x74","\x52\x69\x67\x68\x74","\x43\x65\x6E\x74\x65\x72","\x49\x6D\x61\x67\x65\x20\x57\x65\x62\x20\x4C\x69\x6E\x6B","\x54\x65\x78\x74","\x45\x6D\x61\x69\x6C","\x55\x52\x4C","\x56\x69\x64\x65\x6F\x20\x45\x6D\x62\x65\x64\x20\x43\x6F\x64\x65\x20\x6F\x72\x20\x59\x6F\x75\x74\x75\x62\x65\x2F\x56\x69\x6D\x65\x6F\x20\x4C\x69\x6E\x6B","\x49\x6E\x73\x65\x72\x74\x20\x46\x69\x6C\x65","\x55\x70\x6C\x6F\x61\x64","\x44\x6F\x77\x6E\x6C\x6F\x61\x64","\x43\x68\x6F\x6F\x73\x65","\x4F\x72\x20\x63\x68\x6F\x6F\x73\x65","\x44\x72\x6F\x70\x20\x66\x69\x6C\x65\x20\x68\x65\x72\x65","\x41\x6C\x69\x67\x6E\x20\x74\x65\x78\x74\x20\x74\x6F\x20\x74\x68\x65\x20\x6C\x65\x66\x74","\x43\x65\x6E\x74\x65\x72\x20\x74\x65\x78\x74","\x41\x6C\x69\x67\x6E\x20\x74\x65\x78\x74\x20\x74\x6F\x20\x74\x68\x65\x20\x72\x69\x67\x68\x74","\x4A\x75\x73\x74\x69\x66\x79\x20\x74\x65\x78\x74","\x49\x6E\x73\x65\x72\x74\x20\x48\x6F\x72\x69\x7A\x6F\x6E\x74\x61\x6C\x20\x52\x75\x6C\x65","\x44\x65\x6C\x65\x74\x65\x64","\x41\x6E\x63\x68\x6F\x72","\x4F\x70\x65\x6E\x20\x6C\x69\x6E\x6B\x20\x69\x6E\x20\x6E\x65\x77\x20\x74\x61\x62","\x55\x6E\x64\x65\x72\x6C\x69\x6E\x65","\x41\x6C\x69\x67\x6E\x6D\x65\x6E\x74","\x4E\x61\x6D\x65\x20\x28\x6F\x70\x74\x69\x6F\x6E\x61\x6C\x29","\x45\x64\x69\x74","\x24\x65\x6C\x65\x6D\x65\x6E\x74","\x75\x75\x69\x64","\x72\x74\x65\x50\x61\x73\x74\x65","\x24\x70\x61\x73\x74\x65\x42\x6F\x78","\x6C\x6F\x61\x64\x4F\x70\x74\x69\x6F\x6E\x73","\x6C\x6F\x61\x64\x4D\x6F\x64\x75\x6C\x65\x73","\x62\x6C\x6F\x63\x6B\x4C\x65\x76\x65\x6C\x45\x6C\x65\x6D\x65\x6E\x74\x73","\x61\x6C\x69\x67\x6E\x6D\x65\x6E\x74\x54\x61\x67\x73","\x6D\x65\x72\x67\x65","\x72\x65\x49\x73\x42\x6C\x6F\x63\x6B","\x5E\x28","\x7C","\x6A\x6F\x69\x6E","\x29\x24","\x73\x65\x74\x75\x70\x41\x6C\x6C\x6F\x77\x65\x64","\x6C\x6F\x61\x64","\x73\x68\x6F\x72\x74\x63\x75\x74\x73\x41\x64\x64","\x65\x78\x74\x65\x6E\x64","\x73\x74\x61\x72\x74","\x73\x65\x74\x43\x61\x6C\x6C\x62\x61\x63\x6B","\x72\x75\x6E","\x66\x75\x6E\x63\x74\x69\x6F\x6E","\x66\x69\x6C\x74\x65\x72","\x67\x65\x74\x4F\x77\x6E\x50\x72\x6F\x70\x65\x72\x74\x79\x4E\x61\x6D\x65\x73","\x62\x69\x6E\x64\x4D\x6F\x64\x75\x6C\x65\x4D\x65\x74\x68\x6F\x64\x73","\x67\x65\x74\x4D\x6F\x64\x75\x6C\x65\x4D\x65\x74\x68\x6F\x64\x73","","\x73\x65\x74","\x72\x69\x67\x68\x74","\x63\x65\x6E\x74\x65\x72","\x6D\x73\x69\x65","\x62\x72\x6F\x77\x73\x65\x72","\x24\x65\x64\x69\x74\x6F\x72","\x73\x61\x76\x65","\x62\x6C\x6F\x63\x6B\x73","\x67\x65\x74\x42\x6C\x6F\x63\x6B\x73","\x74\x79\x70\x65","\x69\x73\x4C\x69\x6E\x65\x62\x72\x65\x61\x6B\x73\x4F\x72\x4E\x6F\x42\x6C\x6F\x63\x6B\x73","\x73\x65\x74\x54\x65\x78\x74","\x73\x65\x74\x42\x6C\x6F\x63\x6B\x73","\x72\x65\x73\x74\x6F\x72\x65","\x73\x79\x6E\x63","\x64\x69\x76","\x77\x72\x61\x70","\x74\x65\x78\x74\x2D\x61\x6C\x69\x67\x6E","\x63\x73\x73","\x64\x61\x74\x61\x2D\x74\x61\x67\x62\x6C\x6F\x63\x6B","\x61\x74\x74\x72","\x67\x65\x74\x41\x6C\x69\x67\x6E\x6D\x65\x6E\x74\x45\x6C\x65\x6D\x65\x6E\x74","\x69\x73\x4E\x65\x65\x64\x52\x65\x70\x6C\x61\x63\x65\x45\x6C\x65\x6D\x65\x6E\x74","\x72\x65\x70\x6C\x61\x63\x65\x45\x6C\x65\x6D\x65\x6E\x74","\x61\x6C\x69\x67\x6E\x45\x6C\x65\x6D\x65\x6E\x74","\x70\x72\x6F\x78\x79","\x6C\x69\x6E\x65\x62\x72\x65\x61\x6B\x73","\x74\x61\x67\x62\x6C\x6F\x63\x6B","\x72\x65\x70\x6C\x61\x63\x65\x57\x69\x74\x68","\x72\x65\x6D\x6F\x76\x65\x45\x6D\x70\x74\x79\x41\x74\x74\x72","\x6E\x61\x6D\x65","\x61\x75\x74\x6F\x73\x61\x76\x65\x4E\x61\x6D\x65","\x24\x74\x65\x78\x74\x61\x72\x65\x61","\x61\x75\x74\x6F\x73\x61\x76\x65\x4F\x6E\x43\x68\x61\x6E\x67\x65","\x61\x75\x74\x6F\x73\x61\x76\x65\x49\x6E\x74\x65\x72\x76\x61\x6C","\x73\x6F\x75\x72\x63\x65","\x67\x65\x74","\x69\x73\x45\x6D\x70\x74\x79","\x70\x6F\x73\x74","\x61\x6A\x61\x78","\x73\x75\x63\x63\x65\x73\x73","\x64\x6F\x6E\x65","\x70\x61\x72\x73\x65\x4A\x53\x4F\x4E","\x61\x75\x74\x6F\x73\x61\x76\x65\x45\x72\x72\x6F\x72","\x63\x6C\x65\x61\x72\x53\x74\x79\x6C\x65","\x63\x6C\x61\x73\x73","\x63\x6C\x65\x61\x72","\x74\x61\x67","\x66\x6F\x72\x6D\x61\x74","\x71\x75\x6F\x74\x65","\x69\x6E\x41\x72\x72\x61\x79","\x69\x73\x52\x65\x6D\x6F\x76\x65\x49\x6E\x6C\x69\x6E\x65","\x62\x6C\x6F\x63\x6B\x73\x53\x69\x7A\x65","\x76\x61\x6C\x75\x65","\x63\x6F\x6E\x74\x61\x69\x6E\x65\x72\x54\x61\x67","\x74\x61\x67\x4E\x61\x6D\x65","\x63\x6F\x6D\x6D\x6F\x6E\x41\x6E\x63\x65\x73\x74\x6F\x72\x43\x6F\x6E\x74\x61\x69\x6E\x65\x72","\x72\x61\x6E\x67\x65","\x63\x6F\x6C\x6C\x61\x70\x73\x65\x64","\x73\x65\x74\x43\x6F\x6C\x6C\x61\x70\x73\x65\x64","\x73\x65\x74\x4D\x75\x6C\x74\x69\x70\x6C\x65","\x66\x6F\x72\x6D\x61\x74\x4C\x69\x73\x74\x54\x6F\x42\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65","\x54\x48","\x66\x6F\x72\x6D\x61\x74\x62\x6C\x6F\x63\x6B","\x3C","\x3E","\x65\x78\x65\x63\x43\x6F\x6D\x6D\x61\x6E\x64","\x67\x65\x74\x42\x6C\x6F\x63\x6B","\x74\x6F\x67\x67\x6C\x65","\x74\x6F\x4C\x6F\x77\x65\x72\x43\x61\x73\x65","\x3C\x62\x72\x3E","\x61\x70\x70\x65\x6E\x64","\x70\x72\x65\x70\x65\x6E\x64","\x72\x65\x70\x6C\x61\x63\x65\x57\x69\x74\x68\x43\x6F\x6E\x74\x65\x6E\x74\x73","\x72\x65\x70\x6C\x61\x63\x65\x54\x6F\x54\x61\x67","\x72\x65\x6D\x6F\x76\x65","\x66\x69\x6E\x64","\x72\x65\x6D\x6F\x76\x65\x49\x6E\x6C\x69\x6E\x65\x54\x61\x67\x73","\x68\x65\x61\x64\x54\x61\x67","\x75\x6E\x77\x72\x61\x70","\x63\x6F\x6E\x74\x65\x6E\x74\x73","\x66\x6F\x72\x6D\x61\x74\x54\x61\x62\x6C\x65\x57\x72\x61\x70\x70\x69\x6E\x67","\x66\x6F\x72\x6D\x61\x74\x42\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65","\x66\x6F\x72\x6D\x61\x74\x57\x72\x61\x70","\x74\x64","\x74\x68","\x64\x6C","\x64\x74","\x64\x64","\x73\x65\x74\x46\x6F\x72\x63\x65","\x72\x65\x6D\x6F\x76\x65\x41\x74\x74\x72","\x61\x64\x64\x43\x6C\x61\x73\x73","\x74\x6F\x67\x67\x6C\x65\x43\x6C\x61\x73\x73","\x73\x74\x79\x6C\x65\x20\x63\x6C\x61\x73\x73","\x72\x65\x6D\x6F\x76\x65\x43\x6C\x61\x73\x73","\x75\x6C\x2C\x20\x6F\x6C","\x63\x6C\x6F\x73\x65\x73\x74","\x63\x6C\x65\x61\x72\x55\x6E\x76\x65\x72\x69\x66\x69\x65\x64","\x70\x3A\x65\x6D\x70\x74\x79","\x72\x65\x6D\x6F\x76\x65\x45\x6D\x70\x74\x79","\x75\x6C\x2C\x20\x6F\x6C\x2C\x20\x74\x72\x2C\x20\x62\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65\x2C\x20\x70","\x2C","\x2C\x20\x74\x64\x2C\x20\x74\x61\x62\x6C\x65\x2C\x20\x74\x68\x65\x61\x64\x2C\x20\x74\x62\x6F\x64\x79\x2C\x20\x74\x66\x6F\x6F\x74\x2C\x20\x74\x68\x2C\x20\x74\x72","\x3C\x62\x72\x20\x2F\x3E","\x67\x65\x74\x4D\x61\x72\x6B\x65\x72","\x62\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65\x3A\x65\x6D\x70\x74\x79","\x74\x61\x62\x6C\x65","\x74\x72","\x3C\x74\x72\x3E","\x3C\x74\x64\x3E","\x64\x61\x74\x61\x2D","\x75\x6E\x64\x6F","\x73\x65\x74\x55\x6E\x64\x6F","\x73\x65\x74\x52\x65\x64\x6F","\x72\x65\x62\x75\x66\x66\x65\x72","\x70\x6F\x70","\x72\x65\x64\x6F","\x67\x65\x74\x55\x6E\x64\x6F","\x67\x65\x74\x52\x65\x64\x6F","\x63\x72\x65\x61\x74\x65\x43\x6F\x6E\x74\x61\x69\x6E\x65\x72\x42\x6F\x78","\x6C\x6F\x61\x64\x43\x6F\x6E\x74\x65\x6E\x74","\x6C\x6F\x61\x64\x45\x64\x69\x74\x6F\x72","\x65\x6E\x61\x62\x6C\x65\x45\x64\x69\x74\x6F\x72","\x73\x65\x74\x43\x6F\x64\x65\x41\x6E\x64\x43\x61\x6C\x6C","\x54\x45\x58\x54\x41\x52\x45\x41","\x24\x62\x6F\x78","\x3C\x64\x69\x76\x20\x63\x6C\x61\x73\x73\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x62\x6F\x78\x22\x20\x2F\x3E","\x67\x65\x74\x54\x65\x78\x74\x61\x72\x65\x61\x4E\x61\x6D\x65","\x3C\x74\x65\x78\x74\x61\x72\x65\x61\x20\x2F\x3E","\x63\x6F\x6E\x74\x65\x6E\x74\x2D","\x69\x64","\x69\x73\x54\x65\x78\x74\x61\x72\x65\x61","\x76\x61\x6C","\x63\x6F\x6E\x74\x65\x6E\x74","\x74\x72\x69\x6D","\x64\x69\x72\x65\x63\x74\x69\x6F\x6E","\x66\x72\x6F\x6D\x54\x65\x78\x74\x61\x72\x65\x61","\x66\x72\x6F\x6D\x45\x6C\x65\x6D\x65\x6E\x74","\x3C\x64\x69\x76\x20\x2F\x3E","\x69\x6E\x73\x65\x72\x74\x41\x66\x74\x65\x72","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x65\x64\x69\x74\x6F\x72","\x68\x69\x64\x65","\x63\x72\x65\x61\x74\x65\x54\x65\x78\x74\x61\x72\x65\x61","\x73\x65\x74\x4F\x70\x74\x69\x6F\x6E\x73","\x63\x61\x6C\x6C\x45\x64\x69\x74\x6F\x72","\x76\x69\x73\x75\x61\x6C","\x73\x68\x6F\x77\x43\x6F\x64\x65","\x64\x69\x73\x61\x62\x6C\x65\x4D\x6F\x7A\x69\x6C\x6C\x61\x45\x64\x69\x74\x69\x6E\x67","\x73\x65\x74\x45\x76\x65\x6E\x74\x73","\x73\x65\x74\x48\x65\x6C\x70\x65\x72\x73","\x6C\x6F\x61\x64\x54\x65\x6D\x70\x6C\x61\x74\x65\x73","\x70\x6C\x75\x67\x69\x6E\x73","\x64\x69\x72","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6C\x69\x6E\x65\x62\x72\x65\x61\x6B\x73","\x74\x61\x62\x69\x6E\x64\x65\x78","\x6D\x69\x6E\x48\x65\x69\x67\x68\x74","\x6D\x61\x78\x48\x65\x69\x67\x68\x74","\x70\x72\x65\x76\x65\x6E\x74\x44\x65\x66\x61\x75\x6C\x74","\x64\x72\x61\x67\x49\x6D\x61\x67\x65\x55\x70\x6C\x6F\x61\x64","\x64\x72\x61\x67\x46\x69\x6C\x65\x55\x70\x6C\x6F\x61\x64","\x66\x69\x6C\x65\x73","\x64\x61\x74\x61\x54\x72\x61\x6E\x73\x66\x65\x72","\x64\x69\x72\x65\x63\x74\x55\x70\x6C\x6F\x61\x64","\x64\x72\x6F\x70","\x64\x72\x6F\x70\x2E\x72\x65\x64\x61\x63\x74\x6F\x72","\x6F\x72\x69\x67\x69\x6E\x61\x6C\x45\x76\x65\x6E\x74","\x46\x6F\x72\x6D\x44\x61\x74\x61","\x73\x65\x74\x45\x76\x65\x6E\x74\x44\x72\x6F\x70","\x73\x65\x74\x45\x76\x65\x6E\x74\x44\x72\x6F\x70\x55\x70\x6C\x6F\x61\x64","\x6F\x6E","\x63\x6C\x69\x63\x6B\x2E\x72\x65\x64\x61\x63\x74\x6F\x72","\x67\x65\x74\x45\x76\x65\x6E\x74","\x63\x6C\x69\x63\x6B","\x61\x72\x72\x6F\x77","\x61\x64\x64\x45\x76\x65\x6E\x74","\x64\x69\x73\x61\x62\x6C\x65\x53\x65\x6C\x65\x63\x74\x41\x6C\x6C","\x70\x61\x73\x74\x65\x2E\x72\x65\x64\x61\x63\x74\x6F\x72","\x6B\x65\x79\x64\x6F\x77\x6E\x2E\x72\x65\x64\x61\x63\x74\x6F\x72","\x6B\x65\x79\x75\x70\x2E\x72\x65\x64\x61\x63\x74\x6F\x72","\x63\x6F\x64\x65\x4B\x65\x79\x64\x6F\x77\x6E\x43\x61\x6C\x6C\x62\x61\x63\x6B","\x6B\x65\x79\x64\x6F\x77\x6E\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x65\x78\x74\x61\x72\x65\x61","\x63\x6F\x64\x65\x4B\x65\x79\x75\x70\x43\x61\x6C\x6C\x62\x61\x63\x6B","\x6B\x65\x79\x75\x70\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x65\x78\x74\x61\x72\x65\x61","\x66\x6F\x63\x75\x73\x43\x61\x6C\x6C\x62\x61\x63\x6B","\x66\x6F\x63\x75\x73\x2E\x72\x65\x64\x61\x63\x74\x6F\x72","\x6D\x6F\x75\x73\x65\x64\x6F\x77\x6E","\x74\x61\x72\x67\x65\x74","\x62\x6C\x75\x72\x2E\x72\x65\x64\x61\x63\x74\x6F\x72","\x69\x73\x42\x6C\x75\x72\x65\x64","\x62\x6C\x75\x72\x43\x61\x6C\x6C\x62\x61\x63\x6B","\x62\x6C\x75\x72","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x6F\x6F\x6C\x62\x61\x72\x2C\x20\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x64\x72\x6F\x70\x64\x6F\x77\x6E","\x68\x61\x73\x43\x6C\x61\x73\x73","\x23\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C","\x69\x73","\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x6F\x6F\x6C\x62\x61\x72\x2C\x20\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x64\x72\x6F\x70\x64\x6F\x77\x6E\x2C\x20\x23\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C","\x70\x61\x72\x65\x6E\x74\x73","\x65\x6E\x61\x62\x6C\x65","\x73\x65\x74\x53\x74\x61\x72\x74","\x66\x6F\x63\x75\x73\x45\x6E\x64","\x73\x65\x74\x45\x6E\x64","\x50\x6C\x75\x67\x69\x6E\x20\x6E\x61\x6D\x65\x20\x22","\x22\x20\x6D\x61\x74\x63\x68\x65\x73\x20\x74\x68\x65\x20\x6E\x61\x6D\x65\x20\x6F\x66\x20\x74\x68\x65\x20\x52\x65\x64\x61\x63\x74\x6F\x72\x27\x73\x20\x6D\x6F\x64\x75\x6C\x65\x2E","\x6D\x6F\x7A\x69\x6C\x6C\x61","\x65\x6E\x61\x62\x6C\x65\x4F\x62\x6A\x65\x63\x74\x52\x65\x73\x69\x7A\x69\x6E\x67","\x65\x6E\x61\x62\x6C\x65\x49\x6E\x6C\x69\x6E\x65\x54\x61\x62\x6C\x65\x45\x64\x69\x74\x69\x6E\x67","\x3C\x61\x20\x68\x72\x65\x66\x3D\x22\x23\x22\x20\x63\x6C\x61\x73\x73\x3D\x22\x72\x65\x2D\x69\x63\x6F\x6E\x20\x72\x65\x2D","\x22\x20\x72\x65\x6C\x3D\x22","\x22\x20\x2F\x3E","\x66\x75\x6E\x63","\x63\x6F\x6D\x6D\x61\x6E\x64","\x73\x65\x74\x45\x76\x65\x6E\x74","\x3C\x64\x69\x76\x20\x63\x6C\x61\x73\x73\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x64\x72\x6F\x70\x64\x6F\x77\x6E\x20\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x64\x72\x6F\x70\x64\x6F\x77\x6E\x2D\x62\x6F\x78\x2D","\x22\x20\x73\x74\x79\x6C\x65\x3D\x22\x64\x69\x73\x70\x6C\x61\x79\x3A\x20\x6E\x6F\x6E\x65\x3B\x22\x3E","\x69\x73\x44\x65\x73\x6B\x74\x6F\x70","\x74\x69\x74\x6C\x65","\x63\x72\x65\x61\x74\x65\x54\x6F\x6F\x6C\x74\x69\x70","\x74\x6F\x75\x63\x68\x73\x74\x61\x72\x74\x20\x63\x6C\x69\x63\x6B","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x62\x75\x74\x74\x6F\x6E\x2D\x64\x69\x73\x61\x62\x6C\x65\x64","\x6F\x6E\x43\x6C\x69\x63\x6B","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x6F\x6F\x6C\x62\x61\x72\x2D\x74\x6F\x6F\x6C\x74\x69\x70\x20\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x6F\x6F\x6C\x62\x61\x72\x2D\x74\x6F\x6F\x6C\x74\x69\x70\x2D","\x3C\x73\x70\x61\x6E\x3E","\x61\x70\x70\x65\x6E\x64\x54\x6F","\x6D\x6F\x75\x73\x65\x6F\x76\x65\x72","\x6F\x66\x66\x73\x65\x74","\x73\x68\x6F\x77","\x74\x6F\x70","\x69\x6E\x6E\x65\x72\x48\x65\x69\x67\x68\x74","\x70\x78","\x6C\x65\x66\x74","\x69\x6E\x6E\x65\x72\x57\x69\x64\x74\x68","\x6D\x6F\x75\x73\x65\x6F\x75\x74","\x63\x61\x72\x65\x74\x4F\x66\x66\x73\x65\x74","\x67\x65\x74\x4F\x66\x66\x73\x65\x74","\x72\x65\x74\x75\x72\x6E\x56\x61\x6C\x75\x65","\x6F\x6E\x43\x6C\x69\x63\x6B\x43\x61\x6C\x6C\x62\x61\x63\x6B","\x62\x75\x74\x74\x6F\x6E\x73","\x61\x2E\x72\x65\x2D","\x24\x74\x6F\x6F\x6C\x62\x61\x72","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x61\x63\x74","\x61\x2E\x72\x65\x2D\x69\x63\x6F\x6E","\x2E\x72\x65\x2D","\x6E\x6F\x74","\x61\x2E\x72\x65\x2D\x68\x74\x6D\x6C","\x72\x65\x2D","\x66\x61\x2D\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x62\x74\x6E","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x62\x74\x6E\x2D\x69\x6D\x61\x67\x65","\x3C\x69\x20\x63\x6C\x61\x73\x73\x3D\x22\x66\x61\x20","\x22\x3E\x3C\x2F\x69\x3E","\x72\x65\x6C","\x61\x64\x64\x43\x61\x6C\x6C\x62\x61\x63\x6B","\x3C\x6C\x69\x3E","\x61\x66\x74\x65\x72","\x70\x61\x72\x65\x6E\x74","\x62\x65\x66\x6F\x72\x65","\x69\x73\x42\x6C\x6F\x63\x6B","\x63\x72\x65\x61\x74\x65\x53\x70\x61\x63\x65\x45\x6C\x65\x6D\x65\x6E\x74","\x69\x73\x42\x6C\x6F\x63\x6B\x54\x61\x67","\x69\x6E\x6E\x65\x72\x48\x54\x4D\x4C","\x69\x6E\x76\x69\x73\x69\x62\x6C\x65\x53\x70\x61\x63\x65","\x42\x52","\x65\x6D\x70\x74\x79\x48\x74\x6D\x6C","\x61\x64\x64\x52\x61\x6E\x67\x65","\x6E\x65\x78\x74","\x73\x65\x74\x41\x66\x74\x65\x72\x4F\x72\x42\x65\x66\x6F\x72\x65","\x70\x72\x65\x76","\x73\x65\x74\x53\x74\x61\x72\x74\x41\x66\x74\x65\x72","\x73\x65\x74\x45\x6E\x64\x41\x66\x74\x65\x72","\x73\x65\x74\x53\x74\x61\x72\x74\x42\x65\x66\x6F\x72\x65","\x73\x65\x74\x45\x6E\x64\x42\x65\x66\x6F\x72\x65","\x63\x6F\x6C\x6C\x61\x70\x73\x65","\x63\x6C\x6F\x6E\x65\x52\x61\x6E\x67\x65","\x73\x65\x6C\x65\x63\x74\x4E\x6F\x64\x65\x43\x6F\x6E\x74\x65\x6E\x74\x73","\x65\x6E\x64\x43\x6F\x6E\x74\x61\x69\x6E\x65\x72","\x65\x6E\x64\x4F\x66\x66\x73\x65\x74","\x67\x65\x74\x53\x65\x6C\x65\x63\x74\x69\x6F\x6E","\x72\x61\x6E\x67\x65\x43\x6F\x75\x6E\x74","\x67\x65\x74\x52\x61\x6E\x67\x65\x41\x74","\x69\x73\x46\x6F\x63\x75\x73\x65\x64","\x63\x72\x65\x61\x74\x65\x54\x72\x65\x65\x57\x61\x6C\x6B\x65\x72","\x6E\x6F\x64\x65\x56\x61\x6C\x75\x65","\x6E\x65\x78\x74\x4E\x6F\x64\x65","\x73\x65\x74\x4F\x66\x66\x73\x65\x74","\x73\x61\x76\x65\x50\x72\x65\x43\x6F\x64\x65","","\x72\x65\x70\x6C\x61\x63\x65","\x26\x23\x33\x36\x3B","\x22","\x27","\x72\x65\x70\x6C\x61\x63\x65\x44\x69\x76\x73","\x72\x65\x70\x6C\x61\x63\x65\x50\x61\x72\x61\x67\x72\x61\x70\x68\x73\x54\x6F\x42\x72","\x73\x61\x76\x65\x46\x6F\x72\x6D\x54\x61\x67\x73","\x3C\x64\x69\x76\x3E","\x66\x6F\x6E\x74\x5B\x73\x74\x79\x6C\x65\x5D","\x73\x65\x74\x56\x65\x72\x69\x66\x69\x65\x64","\x63\x6F\x6E\x76\x65\x72\x74\x49\x6E\x6C\x69\x6E\x65","\x63\x6C\x65\x61\x6E\x53\x70\x61\x63\x65\x73","\x20","\x3C\x73\x63\x72\x69\x70\x74\x24\x31\x3E\x24\x32\x3C\x2F\x73\x63\x72\x69\x70\x74\x3E","\x72\x65\x73\x74\x6F\x72\x65\x46\x6F\x72\x6D\x54\x61\x67\x73","\x26\x74\x72\x61\x64\x65\x3B","\x26\x63\x6F\x70\x79\x3B","\x26\x68\x65\x6C\x6C\x69\x70\x3B","\x26\x6D\x64\x61\x73\x68\x3B","\x26\x64\x61\x73\x68\x3B","\x67","\x3C\x62\x72\x5C\x73\x3F\x2F\x3F\x3E\x3C\x2F\x6C\x69\x3E","\x67\x69","\x3C\x2F\x6C\x69\x3E","\x3C\x2F\x6C\x69\x3E\x3C\x62\x72\x5C\x73\x3F\x2F\x3F\x3E","\x3C\x64\x69\x76\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x20\x64\x61\x74\x61\x2D\x74\x61\x67\x62\x6C\x6F\x63\x6B\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x22\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x3E","\x3C\x64\x69\x76\x24\x31\x24\x32\x3E","\x3C\x28\x2E\x2A\x3F\x29\x20\x64\x61\x74\x61\x2D\x76\x65\x72\x69\x66\x69\x65\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x22\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x3E","\x3C\x24\x31\x24\x32\x3E","\x3C\x73\x70\x61\x6E\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x73\x72\x65\x6C\x3D\x22\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x22\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x3E","\x3C\x73\x70\x61\x6E\x24\x31\x24\x33\x3E","\x3C\x69\x6D\x67\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x73\x72\x65\x6C\x3D\x22\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x22\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x3E","\x3C\x69\x6D\x67\x24\x31\x24\x33\x3E","\x3C\x69\x6D\x67\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x73\x73\x74\x79\x6C\x65\x3D\x22\x22\x20\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x3E","\x3C\x69\x6D\x67\x24\x31\x20\x24\x32\x3E","\x3C\x69\x6D\x67\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x73\x73\x74\x79\x6C\x65\x20\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x3E","\x3C\x73\x70\x61\x6E\x20\x63\x6C\x61\x73\x73\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6E\x76\x69\x73\x69\x62\x6C\x65\x2D\x73\x70\x61\x63\x65\x22\x3E\x28\x2E\x2A\x3F\x29\x3C\x2F\x73\x70\x61\x6E\x3E","\x24\x31","\x24\x33\x3C\x69\x6D\x67\x24\x34\x3E","\x6C\x69\x6E\x6B\x4E\x6F\x66\x6F\x6C\x6C\x6F\x77","\x3C\x61\x24\x31\x24\x32\x3E","\x3C\x61\x24\x31\x20\x72\x65\x6C\x3D\x22\x6E\x6F\x66\x6F\x6C\x6C\x6F\x77\x22\x3E","\x3C\x28\x2E\x2A\x3F\x29\x20\x64\x61\x74\x61\x2D\x76\x65\x72\x69\x66\x69\x65\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x22\x3E","\x3C\x24\x31\x3E","\x09","\x70\x61\x73\x74\x65\x50\x6C\x61\x69\x6E\x54\x65\x78\x74","\x67\x65\x74\x50\x6C\x61\x69\x6E\x54\x65\x78\x74","\x69\x73\x53\x65\x6C\x65\x63\x74\x41\x6C\x6C","\x41","\x69\x73\x43\x75\x72\x72\x65\x6E\x74\x4F\x72\x50\x61\x72\x65\x6E\x74","\x67\x65\x74\x50\x72\x65\x43\x6F\x64\x65","\x67\x65\x74\x4F\x6E\x6C\x79\x49\x6D\x61\x67\x65\x73","\x3C\x70\x3E\x3C\x69\x6D\x67\x24\x31\x3E\x3C\x2F\x70\x3E","\x6F\x6E\x50\x61\x73\x74\x65\x54\x69\x64\x79","\x72\x65\x70\x6C\x61\x63\x65\x44\x69\x76\x73\x54\x6F\x42\x72","\x69\x73\x53\x69\x6E\x67\x6C\x65\x4C\x69\x6E\x65","\x73\x69\x6E\x67\x6C\x65\x4C\x69\x6E\x65","\x6F\x6E\x50\x61\x73\x74\x65\x57\x6F\x72\x64","\x6F\x6E\x50\x61\x73\x74\x65\x45\x78\x74\x72\x61","\x61\x6C\x6C","\x72\x65\x6D\x6F\x76\x65\x44\x69\x72\x74\x79\x53\x74\x79\x6C\x65\x73","\x6F\x6E\x50\x61\x73\x74\x65\x52\x65\x6D\x6F\x76\x65\x53\x70\x61\x6E\x73","\x6F\x6E\x50\x61\x73\x74\x65\x52\x65\x6D\x6F\x76\x65\x45\x6D\x70\x74\x79","\x74\x65\x73\x74","\x6F\x6E\x50\x61\x73\x74\x65\x49\x65\x46\x69\x78\x4C\x69\x6E\x6B\x73","\x73\x72\x63\x3D\x22\x22","\x3C\x75\x6C\x3E\x3C\x6C\x69\x24\x32\x3C\x2F\x6C\x69\x3E","\x3C\x6C\x69\x24\x32\x3C\x2F\x6C\x69\x3E","\x3C\x6C\x69\x24\x32\x3C\x2F\x6C\x69\x3E\x3C\x2F\x75\x6C\x3E","\x3C\x75\x6C\x3E\x3C\x6C\x69\x24\x32\x3C\x2F\x6C\x69\x3E\x3C\x2F\x75\x6C\x3E","\x3C\x70","\x24\x32","\x24\x33","","\x3C\x73\x70\x61\x6E\x20\x73\x74\x79\x6C\x65\x3D\x22\x66\x6F\x6E\x74\x2D\x73\x74\x79\x6C\x65\x3A\x20\x69\x74\x61\x6C\x69\x63\x3B\x22\x3E","\x3C\x73\x70\x61\x6E\x20\x73\x74\x79\x6C\x65\x3D\x22\x66\x6F\x6E\x74\x2D\x77\x65\x69\x67\x68\x74\x3A\x20\x62\x6F\x6C\x64\x3B\x22\x3E","\x3C\x73\x70\x61\x6E\x20\x73\x74\x79\x6C\x65\x3D\x22\x74\x65\x78\x74\x2D\x64\x65\x63\x6F\x72\x61\x74\x69\x6F\x6E\x3A\x20\x75\x6E\x64\x65\x72\x6C\x69\x6E\x65\x3B\x22\x3E","\x0A","\x3C\x70\x3E","\x3C\x2F\x70\x3E","\x3C\x2F\x70\x3E\x3C\x70","\x3C\x6C\x69\x3E\x24\x31\x3C\x2F\x6C\x69\x3E","\x61\x64\x64\x72\x65\x73\x73","\x64\x66\x6E","\x62\x72","\x76\x69\x64\x65\x6F","\x61\x75\x64\x69\x6F","\x69\x66\x72\x61\x6D\x65","\x65\x6D\x62\x65\x64","\x70\x61\x72\x61\x6D","\x6F\x62\x6A\x65\x63\x74","\x74\x62\x6F\x64\x79","\x74\x66\x6F\x6F\x74","\x74\x68\x65\x61\x64","\x2A","\x73\x72\x63","\x61\x6C\x74","\x64\x61\x74\x61\x2D\x76\x65\x72\x69\x66\x69\x65\x64","\x63\x6F\x6C\x73\x70\x61\x6E","\x72\x6F\x77\x73\x70\x61\x6E","\x64\x61\x74\x61\x2D\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6E\x73\x65\x72\x74\x65\x64\x2D\x69\x6D\x61\x67\x65","\x64\x65\x6E\x69\x65\x64\x54\x61\x67\x73","\x44\x49\x56\x7C","\x50\x7C","\x3C\x2F\x28","\x29\x3E","\x6D\x61\x74\x63\x68","\x69\x6E\x64\x65\x78\x4F\x66","\x70\x72\x65\x53\x70\x61\x63\x65\x73","\x65\x6E\x63\x6F\x64\x65\x45\x6E\x74\x69\x74\x69\x65\x73","\x3C\x2F","\x63\x72\x65\x61\x74\x65\x45\x6C\x65\x6D\x65\x6E\x74","\x74\x65\x78\x74\x43\x6F\x6E\x74\x65\x6E\x74","\x69\x6E\x6E\x65\x72\x54\x65\x78\x74","\x67\x65\x74\x54\x65\x78\x74\x46\x72\x6F\x6D\x48\x74\x6D\x6C","\x26\x6C\x74\x3B\x69\x6D\x67\x24\x31\x26\x67\x74\x3B","\x5B\x69\x6D\x67\x24\x31\x5D","\x3C\x69\x6D\x67\x24\x31\x3E","\x5B\x61\x20\x68\x72\x65\x66\x3D\x22\x24\x32\x22\x5D\x24\x34\x5B\x2F\x61\x5D","\x3C\x61\x20\x68\x72\x65\x66\x3D\x22\x24\x31\x22\x3E\x24\x32\x3C\x2F\x61\x3E","\x26","\x26\x71\x75\x6F\x74\x3B","\x26\x67\x74\x3B","\x26\x6C\x74\x3B","\x26\x61\x6D\x70\x3B","\x63\x6C\x65\x61\x72\x55\x6E\x76\x65\x72\x69\x66\x69\x65\x64\x52\x65\x6D\x6F\x76\x65","\x68\x31\x2C\x20\x68\x32\x2C\x20\x68\x33\x2C\x20\x68\x34\x2C\x20\x68\x35\x2C\x20\x68\x36","\x2C\x20","\x76\x65\x72\x69\x66\x69\x65\x64\x54\x61\x67\x73","\x5B\x64\x61\x74\x61\x2D\x76\x65\x72\x69\x66\x69\x65\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x22\x5D","\x73\x70\x61\x6E\x5B\x64\x61\x74\x61\x2D\x76\x65\x72\x69\x66\x69\x65\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x22\x5D\x2C\x20\x69\x6D\x67\x5B\x64\x61\x74\x61\x2D\x76\x65\x72\x69\x66\x69\x65\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x22\x5D","\x3C\x69\x6D\x67\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x3E","\x3C\x69\x6D\x67\x24\x31\x20\x64\x61\x74\x61\x2D\x76\x65\x72\x69\x66\x69\x65\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x22\x3E","\x3C\x73\x70\x61\x6E\x28\x2E\x2A\x3F\x29\x3E","\x3C\x73\x70\x61\x6E\x24\x31\x20\x64\x61\x74\x61\x2D\x76\x65\x72\x69\x66\x69\x65\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x22\x3E","\x3C\x28\x73\x70\x61\x6E\x7C\x69\x6D\x67\x29\x28\x2E\x2A\x3F\x29\x73\x74\x79\x6C\x65\x3D\x22\x28\x2E\x2A\x3F\x29\x22\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x3E","\x73\x74\x79\x6C\x65\x3D\x22\x24\x31\x22\x20\x72\x65\x6C\x3D\x22\x24\x31\x22","\x69\x6E\x6C\x69\x6E\x65\x54\x61\x67\x73","\x64\x61\x74\x61\x2D\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x61\x67","\x64\x61\x74\x61\x2D\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x73\x74\x79\x6C\x65","\x64\x61\x74\x61\x2D\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x63\x6C\x61\x73\x73","\x3E\x20\x3C","\x24\x32\x3C\x62\x72\x20\x2F\x3E","\x3C\x70\x24\x31\x3E\x24\x32\x3C\x2F\x70\x3E","\x3C\x62\x72\x20\x2F\x3E\x3C\x62\x72\x20\x2F\x3E","\x24\x31\x3C\x62\x72\x20\x2F\x3E\x3C\x62\x72\x20\x2F\x3E","\x3C\x2F\x62\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65\x3E","\x3C\x73\x65\x63\x74\x69\x6F\x6E\x24\x31\x20\x72\x65\x6C\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x66\x6F\x72\x6D\x2D\x74\x61\x67\x22\x3E\x24\x32\x3C\x2F\x73\x65\x63\x74\x69\x6F\x6E\x3E","\x3C\x66\x6F\x72\x6D\x24\x31\x24\x32\x3E\x24\x33\x3C\x2F\x66\x6F\x72\x6D\x3E","\x6F\x6E\x53\x65\x74","\x61\x64\x64","\x73\x74\x61\x72\x74\x53\x79\x6E\x63","\x73\x79\x6E\x63\x43\x6F\x64\x65","\x73\x79\x6E\x63\x42\x65\x66\x6F\x72\x65","\x6F\x6E\x53\x79\x6E\x63","\x63\x68\x61\x6E\x67\x65","\x6F\x6E\x43\x68\x61\x6E\x67\x65","\x73\x68\x6F\x77\x56\x69\x73\x75\x61\x6C","\x73\x63\x72\x6F\x6C\x6C\x54\x6F\x70","\x6D\x6F\x64\x69\x66\x69\x65\x64","\x72\x65\x6D\x6F\x76\x65\x53\x70\x61\x63\x65\x73","\x68\x65\x69\x67\x68\x74","\x6B\x65\x79\x64\x6F\x77\x6E\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x65\x78\x74\x61\x72\x65\x61\x2D\x69\x6E\x64\x65\x6E\x74\x69\x6E\x67","\x74\x65\x78\x74\x61\x72\x65\x61\x49\x6E\x64\x65\x6E\x74\x69\x6E\x67","\x73\x65\x74\x53\x65\x6C\x65\x63\x74\x69\x6F\x6E\x52\x61\x6E\x67\x65","\x73\x65\x74\x49\x6E\x61\x63\x74\x69\x76\x65\x49\x6E\x43\x6F\x64\x65","\x73\x65\x74\x41\x63\x74\x69\x76\x65","\x6F\x66\x66","\x73\x65\x74\x41\x63\x74\x69\x76\x65\x49\x6E\x56\x69\x73\x75\x61\x6C","\x73\x65\x74\x49\x6E\x61\x63\x74\x69\x76\x65","\x6B\x65\x79\x43\x6F\x64\x65","\x73\x65\x6C\x65\x63\x74\x69\x6F\x6E\x53\x74\x61\x72\x74","\x73\x75\x62\x73\x74\x72\x69\x6E\x67","\x73\x65\x6C\x65\x63\x74\x69\x6F\x6E\x45\x6E\x64","\x65\x76\x65\x6E\x74","\x43\x61\x6C\x6C\x62\x61\x63\x6B","\x64\x65\x73\x74\x72\x6F\x79","\x72\x65\x6D\x6F\x76\x65\x44\x61\x74\x61","\x2E\x72\x65\x64\x61\x63\x74\x6F\x72","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x65\x64\x69\x74\x6F\x72\x20\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6C\x69\x6E\x65\x62\x72\x65\x61\x6B\x73\x20\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x70\x6C\x61\x63\x65\x68\x6F\x6C\x64\x65\x72","\x63\x6F\x6E\x74\x65\x6E\x74\x65\x64\x69\x74\x61\x62\x6C\x65","\x24\x6D\x6F\x64\x61\x6C\x42\x6F\x78","\x24\x6D\x6F\x64\x61\x6C\x4F\x76\x65\x72\x6C\x61\x79","\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x6F\x6F\x6C\x62\x61\x72\x2D\x74\x6F\x6F\x6C\x74\x69\x70","\x66\x6F\x72\x6D\x61\x74\x74\x69\x6E\x67\x41\x64\x64","\x2D","\x69\x6E\x6C\x69\x6E\x65\x2E\x66\x6F\x72\x6D\x61\x74\x74\x69\x6E\x67","\x62\x6C\x6F\x63\x6B\x2E\x66\x6F\x72\x6D\x61\x74\x74\x69\x6E\x67","\x3C\x61\x20\x68\x72\x65\x66\x3D\x22\x23\x22\x20\x63\x6C\x61\x73\x73\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x64\x72\x6F\x70\x64\x6F\x77\x6E\x2D","\x22\x3E","\x3C\x2F\x61\x3E","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x66\x6F\x72\x6D\x61\x74\x74\x69\x6E\x67\x2D","\x68\x69\x64\x65\x41\x6C\x6C","\x69\x73\x4D\x6F\x62\x69\x6C\x65","\x61\x63\x74\x69\x76\x65\x45\x6C\x65\x6D\x65\x6E\x74","\x64\x72\x6F\x70\x61\x63\x74","\x64\x72\x6F\x70\x64\x6F\x77\x6E\x53\x68\x6F\x77","\x77\x69\x64\x74\x68","\x6D\x61\x78","\x74\x6F\x6F\x6C\x62\x61\x72\x2D\x66\x69\x78\x65\x64\x2D\x62\x6F\x78","\x74\x6F\x6F\x6C\x62\x61\x72\x46\x69\x78\x65\x64\x54\x6F\x70\x4F\x66\x66\x73\x65\x74","\x66\x69\x78\x65\x64","\x74\x6F\x6F\x6C\x62\x61\x72\x46\x69\x78\x65\x64\x54\x61\x72\x67\x65\x74","\x61\x62\x73\x6F\x6C\x75\x74\x65","\x64\x72\x6F\x70\x64\x6F\x77\x6E\x53\x68\x6F\x77\x6E","\x6F\x6E\x65","\x62\x6F\x64\x79\x2D\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x68\x69\x64\x64\x65\x6E","\x6D\x61\x72\x67\x69\x6E\x2D\x72\x69\x67\x68\x74","\x73\x74\x6F\x70\x50\x72\x6F\x70\x61\x67\x61\x74\x69\x6F\x6E","\x61\x2E\x64\x72\x6F\x70\x61\x63\x74","\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x64\x72\x6F\x70\x64\x6F\x77\x6E","\x64\x72\x6F\x70\x64\x6F\x77\x6E\x48\x69\x64\x65","\x23\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x66\x69\x6C\x65\x2D\x75\x70\x6C\x6F\x61\x64","\x66\x69\x6C\x65\x55\x70\x6C\x6F\x61\x64","\x73\x65\x6C","\x23\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x66\x69\x6C\x65\x6E\x61\x6D\x65","\x63\x6C\x6F\x73\x65","\x66\x69\x6C\x65\x55\x70\x6C\x6F\x61\x64\x45\x72\x72\x6F\x72","\x66\x69\x6C\x65\x6E\x61\x6D\x65","\x3C\x61\x20\x68\x72\x65\x66\x3D\x22","\x66\x69\x6C\x65\x6C\x69\x6E\x6B","\x22\x20\x69\x64\x3D\x22\x66\x69\x6C\x65\x6C\x69\x6E\x6B\x2D\x6D\x61\x72\x6B\x65\x72\x22\x3E","\x72\x65\x6D\x6F\x76\x65\x4D\x61\x72\x6B\x65\x72\x73","\x6E\x6F\x64\x65\x54\x6F\x43\x61\x72\x65\x74\x50\x6F\x73\x69\x74\x69\x6F\x6E\x46\x72\x6F\x6D\x50\x6F\x69\x6E\x74","\x68\x74\x6D\x6C\x57\x69\x74\x68\x6F\x75\x74\x43\x6C\x65\x61\x6E","\x61\x23\x66\x69\x6C\x65\x6C\x69\x6E\x6B\x2D\x6D\x61\x72\x6B\x65\x72","\x66\x69\x72\x73\x74","\x63\x68\x69\x6C\x64\x72\x65\x6E","\x6E\x6F\x64\x65\x54\x79\x70\x65","\x74\x65\x78\x74","\x6C\x61\x73\x74","\x66\x6F\x63\x75\x73\x4E\x6F\x64\x65","\x70\x61\x72\x65\x6E\x74\x4E\x6F\x64\x65","\x69\x73\x52\x65\x64\x61\x63\x74\x6F\x72\x50\x61\x72\x65\x6E\x74","\x3A\x66\x6F\x63\x75\x73","\x23\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x69\x6D\x61\x67\x65\x2D\x64\x72\x6F\x70\x61\x72\x65\x61","\x69\x6D\x61\x67\x65\x55\x70\x6C\x6F\x61\x64","\x69\x6D\x61\x67\x65\x45\x64\x69\x74","\x65\x64\x69\x74","\x63\x72\x65\x61\x74\x65\x43\x61\x6E\x63\x65\x6C\x42\x75\x74\x74\x6F\x6E","\x62\x75\x74\x74\x6F\x6E\x44\x65\x6C\x65\x74\x65","\x5F\x64\x65\x6C\x65\x74\x65","\x63\x72\x65\x61\x74\x65\x44\x65\x6C\x65\x74\x65\x42\x75\x74\x74\x6F\x6E","\x62\x75\x74\x74\x6F\x6E\x53\x61\x76\x65","\x63\x72\x65\x61\x74\x65\x41\x63\x74\x69\x6F\x6E\x42\x75\x74\x74\x6F\x6E","\x75\x70\x64\x61\x74\x65","\x23\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x74\x69\x74\x6C\x65","\x69\x6D\x61\x67\x65\x4C\x69\x6E\x6B","\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x6C\x69\x6E\x6B\x2D\x6F\x70\x74\x69\x6F\x6E","\x23\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x6C\x69\x6E\x6B","\x68\x72\x65\x66","\x5F\x62\x6C\x61\x6E\x6B","\x63\x68\x65\x63\x6B\x65\x64","\x70\x72\x6F\x70","\x23\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x6C\x69\x6E\x6B\x2D\x62\x6C\x61\x6E\x6B","\x69\x6D\x61\x67\x65\x50\x6F\x73\x69\x74\x69\x6F\x6E","\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x70\x6F\x73\x69\x74\x69\x6F\x6E\x2D\x6F\x70\x74\x69\x6F\x6E","\x64\x69\x73\x70\x6C\x61\x79","\x66\x6C\x6F\x61\x74","\x6E\x6F\x6E\x65","\x23\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x61\x6C\x69\x67\x6E","\x30\x20","\x69\x6D\x61\x67\x65\x46\x6C\x6F\x61\x74\x4D\x61\x72\x67\x69\x6E","\x20\x30","\x30\x20\x30\x20","\x61\x75\x74\x6F","\x68\x69\x64\x65\x52\x65\x73\x69\x7A\x65","\x73\x65\x74\x46\x6C\x6F\x61\x74\x69\x6E\x67","\x28\x28\x78\x6E\x2D\x2D\x29\x3F\x5B\x61\x2D\x7A\x30\x2D\x39\x5D\x2B\x28\x2D\x5B\x61\x2D\x7A\x30\x2D\x39\x5D\x2B\x29\x2A\x5C\x2E\x29\x2B\x5B\x61\x2D\x7A\x5D\x7B\x32\x2C\x7D","\x5E\x28\x68\x74\x74\x70\x7C\x66\x74\x70\x7C\x68\x74\x74\x70\x73\x29\x3A\x2F\x2F","\x5E","\x6C\x69\x6E\x6B\x50\x72\x6F\x74\x6F\x63\x6F\x6C","\x3A\x2F\x2F","\x67\x65\x74\x4F\x75\x74\x65\x72\x48\x74\x6D\x6C","\x69\x6D\x61\x67\x65\x73","\x69\x6D\x61\x67\x65\x45\x64\x69\x74\x61\x62\x6C\x65","\x64\x72\x61\x67\x73\x74\x61\x72\x74","\x6F\x6E\x44\x72\x61\x67","\x63\x6C\x69\x63\x6B\x20\x74\x6F\x75\x63\x68\x73\x74\x61\x72\x74","\x23\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x62\x6F\x78","\x72\x65\x73\x69\x7A\x65\x72","\x6C\x6F\x61\x64\x45\x64\x69\x74\x61\x62\x6C\x65\x43\x6F\x6E\x74\x72\x6F\x6C\x73","\x63\x6C\x69\x63\x6B\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x72\x65\x73\x69\x7A\x65\x2D\x68\x69\x64\x65","\x69\x6D\x61\x67\x65\x52\x65\x73\x69\x7A\x61\x62\x6C\x65","\x6D\x6F\x75\x73\x65\x64\x6F\x77\x6E\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x20\x74\x6F\x75\x63\x68\x73\x74\x61\x72\x74\x2E\x72\x65\x64\x61\x63\x74\x6F\x72","\x73\x65\x74\x52\x65\x73\x69\x7A\x61\x62\x6C\x65","\x72\x65\x73\x69\x7A\x65\x48\x61\x6E\x64\x6C\x65","\x70\x61\x67\x65\x58","\x70\x61\x67\x65\x59","\x74\x61\x72\x67\x65\x74\x54\x6F\x75\x63\x68\x65\x73","\x78","\x79","\x73\x74\x61\x72\x74\x52\x65\x73\x69\x7A\x65","\x6D\x6F\x75\x73\x65\x6D\x6F\x76\x65\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x72\x65\x73\x69\x7A\x65\x20\x74\x6F\x75\x63\x68\x6D\x6F\x76\x65\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x72\x65\x73\x69\x7A\x65","\x6D\x6F\x76\x65\x52\x65\x73\x69\x7A\x65","\x6D\x6F\x75\x73\x65\x75\x70\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x72\x65\x73\x69\x7A\x65\x20\x74\x6F\x75\x63\x68\x65\x6E\x64\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x72\x65\x73\x69\x7A\x65","\x73\x74\x6F\x70\x52\x65\x73\x69\x7A\x65","\x68","\x72\x61\x74\x69\x6F","\x72\x6F\x75\x6E\x64","\x65\x6C","\x68\x61\x6E\x64\x6C\x65","\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x72\x65\x73\x69\x7A\x65","\x64\x72\x6F\x70\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x69\x6E\x73\x69\x64\x65\x2D\x64\x72\x6F\x70","\x6F\x6E\x44\x72\x6F\x70","\x66\x69\x78\x49\x6D\x61\x67\x65\x53\x6F\x75\x72\x63\x65\x41\x66\x74\x65\x72\x44\x72\x6F\x70","\x64\x61\x74\x61\x2D\x73\x61\x76\x65\x2D\x75\x72\x6C","\x69\x6D\x67\x5B\x64\x61\x74\x61\x2D\x73\x61\x76\x65\x2D\x75\x72\x6C\x5D","\x49\x4D\x47","\x65\x64\x69\x74\x74\x65\x72","\x6D\x61\x72\x67\x69\x6E\x54\x6F\x70","\x6D\x61\x72\x67\x69\x6E\x42\x6F\x74\x74\x6F\x6D","\x6D\x61\x72\x67\x69\x6E\x4C\x65\x66\x74","\x6D\x61\x72\x67\x69\x6E\x52\x69\x67\x68\x74","\x6D\x61\x72\x67\x69\x6E","\x6F\x70\x61\x63\x69\x74\x79","","\x31\x35\x70\x78","\x3C\x73\x70\x61\x6E\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x62\x6F\x78\x22\x20\x64\x61\x74\x61\x2D\x72\x65\x64\x61\x63\x74\x6F\x72\x3D\x22\x76\x65\x72\x69\x66\x69\x65\x64\x22\x3E","\x2E\x35","\x3C\x73\x70\x61\x6E\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x65\x64\x69\x74\x74\x65\x72\x22\x20\x64\x61\x74\x61\x2D\x72\x65\x64\x61\x63\x74\x6F\x72\x3D\x22\x76\x65\x72\x69\x66\x69\x65\x64\x22\x3E","\x3C\x2F\x73\x70\x61\x6E\x3E","\x73\x68\x6F\x77\x45\x64\x69\x74","\x6D\x61\x72\x67\x69\x6E\x2D\x6C\x65\x66\x74","\x6C\x6F\x61\x64\x52\x65\x73\x69\x7A\x61\x62\x6C\x65\x43\x6F\x6E\x74\x72\x6F\x6C\x73","\x66\x69\x67\x75\x72\x65","\x69\x6D\x61\x67\x65\x44\x65\x6C\x65\x74\x65","\x69\x6D\x61\x67\x65\x55\x70\x6C\x6F\x61\x64\x45\x72\x72\x6F\x72","\x74\x72\x75\x65","\x3C\x69\x6D\x67\x3E","\x3C\x62\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65\x20\x2F\x3E","\x69\x6D\x67\x5B\x64\x61\x74\x61\x2D\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6E\x73\x65\x72\x74\x65\x64\x2D\x69\x6D\x61\x67\x65\x3D\x74\x72\x75\x65\x5D","\x3C\x70\x20\x2F\x3E","\x69\x6E\x63\x72\x65\x61\x73\x65\x4C\x69\x73\x74\x73","\x69\x6E\x63\x72\x65\x61\x73\x65\x54\x65\x78\x74","\x69\x6E\x63\x72\x65\x61\x73\x65\x42\x6C\x6F\x63\x6B\x73","\x66\x69\x78\x45\x6D\x70\x74\x79\x49\x6E\x64\x65\x6E\x74","\x6E\x6F\x72\x6D\x61\x6C\x69\x7A\x65\x4C\x69\x73\x74\x73","\x6E\x6F\x72\x6D\x61\x6C\x69\x7A\x65","\x69\x6E\x64\x65\x6E\x74\x56\x61\x6C\x75\x65","\x64\x65\x63\x72\x65\x61\x73\x65\x4C\x69\x73\x74\x73","\x64\x65\x63\x72\x65\x61\x73\x65\x42\x6C\x6F\x63\x6B\x73","\x67\x65\x74\x43\x75\x72\x72\x65\x6E\x74","\x75\x6C\x2C\x20\x6F\x6C\x2C\x20\x62\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65\x2C\x20\x70","\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x73\x65\x6C\x65\x63\x74\x69\x6F\x6E\x2D\x6D\x61\x72\x6B\x65\x72","\x69\x73\x43\x75\x72\x72\x65\x6E\x74\x4F\x72\x50\x61\x72\x65\x6E\x74\x48\x65\x61\x64\x65\x72","\x73\x74\x72\x69\x6B\x65\x74\x68\x72\x6F\x75\x67\x68","\x66\x6F\x72\x6D\x61\x74\x43\x6F\x6C\x6C\x61\x70\x73\x65\x64","\x66\x6F\x72\x6D\x61\x74\x4D\x75\x6C\x74\x69\x70\x6C\x65","\x5B\x64\x61\x74\x61\x2D\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x61\x67\x3D","\x5D","\x53\x50\x41\x4E","\x73\x65\x74\x41\x66\x74\x65\x72","\x73\x65\x74\x46\x6F\x72\x6D\x61\x74","\x6E\x6F\x64\x65","\x66\x6F\x72\x6D\x61\x74\x43\x6F\x6E\x76\x65\x72\x74","\x66\x6F\x72\x6D\x61\x74\x52\x65\x6D\x6F\x76\x65\x53\x61\x6D\x65\x43\x68\x69\x6C\x64\x72\x65\x6E","\x3B","\x3A","\x74\x65\x78\x74\x2D\x64\x65\x63\x6F\x72\x61\x74\x69\x6F\x6E","\x6C\x69\x6E\x65\x2D\x74\x68\x72\x6F\x75\x67\x68","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x73\x65\x6C\x65\x63\x74\x69\x6F\x6E\x2D\x6D\x61\x72\x6B\x65\x72","\x5B\x64\x61\x74\x61\x2D\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x63\x6C\x61\x73\x73\x3D","\x5B\x64\x61\x74\x61\x2D\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x73\x74\x79\x6C\x65\x3D\x22","\x22\x5D","\x3C\x73\x74\x72\x69\x6B\x65\x20\x2F\x3E","\x5B\x64\x61\x74\x61\x2D\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x61\x67\x3D\x22","\x63\x73\x73\x54\x65\x78\x74","\x67\x65\x74\x49\x6E\x6C\x69\x6E\x65\x73","\x61\x74\x74\x72\x69\x62\x75\x74\x65\x73","\x67\x65\x74\x50\x61\x72\x65\x6E\x74","\x72\x65\x6D\x6F\x76\x65\x46\x6F\x72\x6D\x61\x74","\x67\x65\x74\x4E\x6F\x64\x65\x73","\x6F\x6E\x50\x61\x73\x74\x65","\x68\x74\x6D\x6C\x49\x65","\x64\x65\x6C\x65\x74\x65\x43\x6F\x6E\x74\x65\x6E\x74\x73","\x63\x72\x65\x61\x74\x65\x44\x6F\x63\x75\x6D\x65\x6E\x74\x46\x72\x61\x67\x6D\x65\x6E\x74","\x61\x70\x70\x65\x6E\x64\x43\x68\x69\x6C\x64","\x66\x69\x72\x73\x74\x43\x68\x69\x6C\x64","\x69\x6E\x73\x65\x72\x74\x4E\x6F\x64\x65","\x72\x65\x6D\x6F\x76\x65\x41\x6C\x6C\x52\x61\x6E\x67\x65\x73","\x65\x78\x65\x63\x48\x74\x6D\x6C","\x69\x6E\x73\x65\x72\x74\x48\x54\x4D\x4C","\x68\x74\x6D\x6C\x46\x69\x78\x4D\x6F\x7A\x69\x6C\x6C\x61","\x69\x73\x49\x65\x31\x31","","\x69\x65\x31\x31\x46\x69\x78\x49\x6E\x73\x65\x72\x74\x69\x6E\x67","\x69\x65\x31\x31\x50\x61\x73\x74\x65\x46\x72\x61\x67","\x70\x61\x73\x74\x65\x48\x54\x4D\x4C","\x63\x72\x65\x61\x74\x65\x52\x61\x6E\x67\x65","\x63\x61\x72\x65\x74\x50\x6F\x73\x69\x74\x69\x6F\x6E\x46\x72\x6F\x6D\x50\x6F\x69\x6E\x74","\x6F\x66\x66\x73\x65\x74\x4E\x6F\x64\x65","\x63\x61\x72\x65\x74\x52\x61\x6E\x67\x65\x46\x72\x6F\x6D\x50\x6F\x69\x6E\x74","\x63\x72\x65\x61\x74\x65\x54\x65\x78\x74\x52\x61\x6E\x67\x65","\x6D\x6F\x76\x65\x54\x6F\x50\x6F\x69\x6E\x74","\x64\x75\x70\x6C\x69\x63\x61\x74\x65","\x45\x6E\x64\x54\x6F\x45\x6E\x64","\x73\x65\x74\x45\x6E\x64\x50\x6F\x69\x6E\x74","\x73\x65\x6C\x65\x63\x74","\x63\x6C\x69\x65\x6E\x74\x58","\x63\x6C\x69\x65\x6E\x74\x59","\x63\x6C\x61\x73\x73\x4E\x61\x6D\x65","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x65\x2D\x70\x61\x73\x74\x65","\x77\x68\x69\x63\x68","\x63\x74\x72\x6C","\x63\x74\x72\x6C\x4B\x65\x79","\x6D\x65\x74\x61\x4B\x65\x79","\x63\x75\x72\x72\x65\x6E\x74","\x69\x73\x54\x61\x67","\x66\x69\x67\x63\x61\x70\x74\x69\x6F\x6E","\x63\x68\x65\x63\x6B\x45\x76\x65\x6E\x74\x73","\x73\x65\x74\x75\x70\x42\x75\x66\x66\x65\x72","\x61\x64\x64\x41\x72\x72\x6F\x77\x73\x45\x76\x65\x6E\x74","\x73\x65\x74\x75\x70\x53\x65\x6C\x65\x63\x74\x41\x6C\x6C","\x65\x6E\x74\x65\x72\x4B\x65\x79","\x44\x4F\x57\x4E","\x52\x49\x47\x48\x54","\x69\x73\x45\x6E\x64\x4F\x66\x45\x6C\x65\x6D\x65\x6E\x74","\x6F\x6E\x41\x72\x72\x6F\x77\x44\x6F\x77\x6E","\x45\x4E\x54\x45\x52","\x73\x68\x69\x66\x74\x4B\x65\x79","\x65\x6E\x74\x65\x72","\x65\x78\x69\x74\x46\x72\x6F\x6D\x42\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65","\x69\x6E\x73\x65\x72\x74\x4E\x65\x77\x4C\x69\x6E\x65","\x69\x6E\x73\x65\x72\x74\x42\x72\x65\x61\x6B\x4C\x69\x6E\x65","\x69\x6E\x73\x65\x72\x74\x44\x62\x6C\x42\x72\x65\x61\x6B\x4C\x69\x6E\x65","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6E\x76\x69\x73\x69\x62\x6C\x65\x2D\x73\x70\x61\x63\x65","\x63\x6F\x6E\x74\x65\x78\x74","\x69\x73\x45\x6E\x64\x4F\x66\x45\x64\x69\x74\x6F\x72","\x72\x65\x70\x6C\x61\x63\x65\x44\x69\x76\x54\x6F\x42\x72\x65\x61\x6B\x4C\x69\x6E\x65","\x72\x65\x70\x6C\x61\x63\x65\x44\x69\x76\x54\x6F\x50\x61\x72\x61\x67\x72\x61\x70\x68","\x69\x6E\x73\x65\x72\x74\x50\x61\x72\x61\x67\x72\x61\x70\x68","\x6F\x6E\x53\x68\x69\x66\x74\x45\x6E\x74\x65\x72","\x54\x41\x42","\x6F\x6E\x54\x61\x62","\x42\x41\x43\x4B\x53\x50\x41\x43\x45","\x44\x45\x4C\x45\x54\x45","\x72\x65\x6D\x6F\x76\x65\x49\x6E\x76\x69\x73\x69\x62\x6C\x65\x53\x70\x61\x63\x65","\x72\x65\x6D\x6F\x76\x65\x45\x6D\x70\x74\x79\x4C\x69\x73\x74\x49\x6E\x54\x61\x62\x6C\x65","\x63\x68\x65\x63\x6B\x4B\x65\x79\x45\x76\x65\x6E\x74\x73","\x53\x50\x41\x43\x45","\x45\x53\x43","\x43\x54\x52\x4C","\x4D\x45\x54\x41","\x41\x4C\x54","\x53\x48\x49\x46\x54","\x61\x6C\x74\x4B\x65\x79","\x65\x6E\x61\x62\x6C\x65\x53\x65\x6C\x65\x63\x74\x41\x6C\x6C","\x4C\x45\x46\x54\x5F\x57\x49\x4E","\x69\x6E\x73\x65\x72\x74\x41\x66\x74\x65\x72\x4C\x61\x73\x74\x45\x6C\x65\x6D\x65\x6E\x74","\x74\x61\x62\x4B\x65\x79","\x74\x61\x62\x41\x73\x53\x70\x61\x63\x65\x73","\xA0","\x63\x72\x65\x61\x74\x65\x54\x65\x78\x74\x4E\x6F\x64\x65","\x64\x65\x63\x72\x65\x61\x73\x65","\x69\x6E\x63\x72\x65\x61\x73\x65","\x73\x65\x74\x42\x65\x66\x6F\x72\x65","\x63\x6C\x65\x61\x6E\x53\x74\x79\x6C\x65\x4F\x6E\x45\x6E\x74\x65\x72","\x69\x6E\x73\x65\x72\x74\x42\x72\x65\x61\x6B\x4C\x69\x6E\x65\x50\x72\x6F\x63\x65\x73\x73\x69\x6E\x67","\x42\x4F\x44\x59","\x72\x65\x70\x6C\x61\x63\x65\x54\x6F\x50\x61\x72\x61\x67\x72\x61\x70\x68","\x69\x73\x4C\x69\x6E\x6B\x69\x66\x79","\x63\x6F\x6E\x76\x65\x72\x74\x4C\x69\x6E\x6B\x73","\x63\x6F\x6E\x76\x65\x72\x74\x55\x72\x6C\x4C\x69\x6E\x6B\x73","\x63\x6F\x6E\x76\x65\x72\x74\x49\x6D\x61\x67\x65\x4C\x69\x6E\x6B\x73","\x63\x6F\x6E\x76\x65\x72\x74\x56\x69\x64\x65\x6F\x4C\x69\x6E\x6B\x73","\x6C\x69\x6E\x6B\x53\x69\x7A\x65","\x66\x6F\x72\x6D\x61\x74\x4C\x69\x6E\x6B\x69\x66\x79","\x67\x65\x74\x4D\x61\x72\x6B\x65\x72\x41\x73\x48\x74\x6D\x6C","\x66\x6F\x72\x6D\x61\x74\x45\x6D\x70\x74\x79","\x63\x6C\x6F\x6E\x65","\x3C\x70\x3E\x3C\x62\x72\x20\x2F\x3E\x3C\x2F\x70\x3E","\x63\x75\x72\x4C\x61\x6E\x67","\x6C\x61\x6E\x67\x73","\x69\x73\x45\x78\x63\x65\x70\x74\x4C\x61\x73\x74\x4F\x72\x46\x69\x72\x73\x74","\x69\x6E\x73\x65\x72\x74\x49\x6E\x49\x65","\x69\x6E\x73\x65\x72\x74\x49\x6E\x4F\x74\x68\x65\x72\x73\x42\x72\x6F\x77\x73\x65\x72\x73","\x67\x65\x74\x4C\x61\x73\x74\x42\x6C\x6F\x63\x6B","\x73\x61\x76\x65\x53\x63\x72\x6F\x6C\x6C","\x68\x72","\x72\x65\x73\x74\x6F\x72\x65\x53\x63\x72\x6F\x6C\x6C","\x3C\x70\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6E\x73\x65\x72\x74\x2D\x6C\x69\x6E\x65\x22\x3E\x3C\x62\x72\x20\x2F\x3E\x3C\x2F\x70\x3E","\x3C\x62\x72\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6E\x73\x65\x72\x74\x2D\x6C\x69\x6E\x65\x22\x3E","\x3C\x68\x72\x3E","\x73\x65\x74\x46\x6F\x63\x75\x73","\x23\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6E\x73\x65\x72\x74\x2D\x6C\x69\x6E\x65","\x6C\x69\x6E\x6B\x5F\x69\x6E\x73\x65\x72\x74","\x62\x75\x74\x74\x6F\x6E\x49\x6E\x73\x65\x72\x74","\x67\x65\x74\x44\x61\x74\x61","\x63\x6C\x65\x61\x6E\x55\x72\x6C","\x23\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6C\x69\x6E\x6B\x2D\x62\x6C\x61\x6E\x6B","\x24\x69\x6E\x70\x75\x74\x55\x72\x6C","\x23\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6C\x69\x6E\x6B\x2D\x75\x72\x6C","\x24\x69\x6E\x70\x75\x74\x54\x65\x78\x74","\x23\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6C\x69\x6E\x6B\x2D\x75\x72\x6C\x2D\x74\x65\x78\x74","\x75\x72\x6C","\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6C\x69\x6E\x6B\x2D\x74\x6F\x6F\x6C\x74\x69\x70","\x6C\x6F\x63\x61\x74\x69\x6F\x6E","\x23","\x6D\x61\x69\x6C\x74\x6F\x3A","\x68\x6F\x73\x74","\x24\x6E\x6F\x64\x65","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6E\x70\x75\x74\x2D\x65\x72\x72\x6F\x72","\x40","\x2E\x28\x68\x74\x6D\x6C\x7C\x70\x68\x70\x29\x24","\x3C\x61\x20\x2F\x3E","\x73\x65\x6C\x65\x63\x74\x45\x6C\x65\x6D\x65\x6E\x74","\x63\x72\x65\x61\x74\x65\x4C\x69\x6E\x6B","\x69\x6E\x73\x65\x72\x74\x65\x64\x4C\x69\x6E\x6B","\x6C\x69\x6E\x6B\x73","\x6F\x6C\x2C\x20\x75\x6C","\x74\x64\x2C\x20\x74\x68","\x3C\x6F\x6C\x3E","\x3C\x75\x6C\x3E","\x23\x73\x65\x6C\x65\x63\x74\x69\x6F\x6E\x2D\x6D\x61\x72\x6B\x65\x72\x2D\x31","\x6C\x69\x2C\x20\x74\x68\x2C\x20\x74\x64","\x75\x6C\x2C\x20\x6F\x6C\x2C\x20\x62\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65","\x3C\x73\x65\x63\x74\x69\x6F\x6E\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x69\x6D\x61\x67\x65\x2D\x65\x64\x69\x74\x22\x3E","\x3C\x6C\x61\x62\x65\x6C\x3E","\x3C\x2F\x6C\x61\x62\x65\x6C\x3E","\x3C\x69\x6E\x70\x75\x74\x20\x74\x79\x70\x65\x3D\x22\x74\x65\x78\x74\x22\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x74\x69\x74\x6C\x65\x22\x20\x2F\x3E","\x3C\x6C\x61\x62\x65\x6C\x20\x63\x6C\x61\x73\x73\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x6C\x69\x6E\x6B\x2D\x6F\x70\x74\x69\x6F\x6E\x22\x3E","","","\x6C\x69\x6E\x6B\x5F\x6E\x65\x77\x5F\x74\x61\x62","\x3C\x6C\x61\x62\x65\x6C\x20\x63\x6C\x61\x73\x73\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x70\x6F\x73\x69\x74\x69\x6F\x6E\x2D\x6F\x70\x74\x69\x6F\x6E\x22\x3E","\x69\x6D\x61\x67\x65\x5F\x70\x6F\x73\x69\x74\x69\x6F\x6E","","\x3C\x6F\x70\x74\x69\x6F\x6E\x20\x76\x61\x6C\x75\x65\x3D\x22\x6E\x6F\x6E\x65\x22\x3E","\x3C\x2F\x6F\x70\x74\x69\x6F\x6E\x3E","\x3C\x6F\x70\x74\x69\x6F\x6E\x20\x76\x61\x6C\x75\x65\x3D\x22\x6C\x65\x66\x74\x22\x3E","\x3C\x6F\x70\x74\x69\x6F\x6E\x20\x76\x61\x6C\x75\x65\x3D\x22\x63\x65\x6E\x74\x65\x72\x22\x3E","\x3C\x6F\x70\x74\x69\x6F\x6E\x20\x76\x61\x6C\x75\x65\x3D\x22\x72\x69\x67\x68\x74\x22\x3E","\x3C\x2F\x73\x65\x6C\x65\x63\x74\x3E","\x3C\x2F\x73\x65\x63\x74\x69\x6F\x6E\x3E","\x3C\x73\x65\x63\x74\x69\x6F\x6E\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x69\x6D\x61\x67\x65\x2D\x69\x6E\x73\x65\x72\x74\x22\x3E","\x3C\x64\x69\x76\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x69\x6D\x61\x67\x65\x2D\x64\x72\x6F\x70\x61\x72\x65\x61\x22\x3E\x3C\x2F\x64\x69\x76\x3E","\x3C\x73\x65\x63\x74\x69\x6F\x6E\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x66\x69\x6C\x65\x2D\x69\x6E\x73\x65\x72\x74\x22\x3E","\x3C\x64\x69\x76\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x66\x69\x6C\x65\x2D\x75\x70\x6C\x6F\x61\x64\x2D\x62\x6F\x78\x22\x3E","\x3C\x69\x6E\x70\x75\x74\x20\x74\x79\x70\x65\x3D\x22\x74\x65\x78\x74\x22\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x66\x69\x6C\x65\x6E\x61\x6D\x65\x22\x20\x2F\x3E\x3C\x62\x72\x3E\x3C\x62\x72\x3E","\x3C\x64\x69\x76\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x66\x69\x6C\x65\x2D\x75\x70\x6C\x6F\x61\x64\x22\x3E\x3C\x2F\x64\x69\x76\x3E","\x3C\x2F\x64\x69\x76\x3E","\x3C\x73\x65\x63\x74\x69\x6F\x6E\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x6C\x69\x6E\x6B\x2D\x69\x6E\x73\x65\x72\x74\x22\x3E","\x3C\x6C\x61\x62\x65\x6C\x3E\x55\x52\x4C\x3C\x2F\x6C\x61\x62\x65\x6C\x3E","\x3C\x69\x6E\x70\x75\x74\x20\x74\x79\x70\x65\x3D\x22\x75\x72\x6C\x22\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6C\x69\x6E\x6B\x2D\x75\x72\x6C\x22\x20\x2F\x3E","\x3C\x69\x6E\x70\x75\x74\x20\x74\x79\x70\x65\x3D\x22\x74\x65\x78\x74\x22\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6C\x69\x6E\x6B\x2D\x75\x72\x6C\x2D\x74\x65\x78\x74\x22\x20\x2F\x3E","\x3C\x6C\x61\x62\x65\x6C\x3E\x3C\x69\x6E\x70\x75\x74\x20\x74\x79\x70\x65\x3D\x22\x63\x68\x65\x63\x6B\x62\x6F\x78\x22\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6C\x69\x6E\x6B\x2D\x62\x6C\x61\x6E\x6B\x22\x3E\x20","\x63\x61\x6C\x6C\x62\x61\x63\x6B\x73","\x24\x74\x61\x62\x62\x65\x72","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x74\x61\x62\x62\x65\x72","\x3C\x61\x20\x68\x72\x65\x66\x3D\x22\x23\x22\x20\x72\x65\x6C\x3D\x22\x74\x61\x62","\x61\x63\x74\x69\x76\x65","\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x61\x62","\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D","\x73\x65\x63\x74\x69\x6F\x6E","\x24\x6D\x6F\x64\x61\x6C\x42\x6F\x64\x79","\x74\x65\x6D\x70\x6C\x61\x74\x65\x4E\x61\x6D\x65","\x65\x6E\x61\x62\x6C\x65\x45\x76\x65\x6E\x74\x73","\x73\x65\x74\x54\x69\x74\x6C\x65","\x73\x65\x74\x44\x72\x61\x67\x67\x61\x62\x6C\x65","\x73\x65\x74\x43\x6F\x6E\x74\x65\x6E\x74","\x62\x6F\x64\x79\x4F\x76\x65\x66\x6C\x6F\x77","\x6F\x76\x65\x72\x66\x6C\x6F\x77","\x68\x69\x64\x64\x65\x6E","\x73\x68\x6F\x77\x4F\x6E\x4D\x6F\x62\x69\x6C\x65","\x73\x68\x6F\x77\x4F\x6E\x44\x65\x73\x6B\x74\x6F\x70","\x73\x65\x74\x42\x75\x74\x74\x6F\x6E\x73\x57\x69\x64\x74\x68","\x72\x65\x73\x69\x7A\x65\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C","\x72\x65\x73\x69\x7A\x65","\x6D\x6F\x64\x61\x6C\x4F\x70\x65\x6E\x65\x64","\x66\x6F\x63\x75\x73\x69\x6E\x2E\x6D\x6F\x64\x61\x6C","\x6B\x65\x79\x64\x6F\x77\x6E\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C","\x73\x65\x74\x45\x6E\x74\x65\x72","\x69\x6E\x70\x75\x74\x5B\x74\x79\x70\x65\x3D\x74\x65\x78\x74\x5D\x2C\x69\x6E\x70\x75\x74\x5B\x74\x79\x70\x65\x3D\x75\x72\x6C\x5D\x2C\x69\x6E\x70\x75\x74\x5B\x74\x79\x70\x65\x3D\x65\x6D\x61\x69\x6C\x5D","\x24\x6D\x6F\x64\x61\x6C","\x6F\x75\x74\x65\x72\x48\x65\x69\x67\x68\x74","\x39\x36\x25","\x32\x30\x70\x78","\x32\x25","\x24\x6D\x6F\x64\x61\x6C\x48\x65\x61\x64\x65\x72","\x67\x65\x74\x54\x65\x6D\x70\x6C\x61\x74\x65","\x64\x72\x61\x67\x67\x61\x62\x6C\x65","\x63\x75\x72\x73\x6F\x72","\x6D\x6F\x76\x65","\x62\x75\x74\x74\x6F\x6E\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x61\x63\x74\x69\x6F\x6E\x2D\x62\x74\x6E","\x63\x61\x6E\x63\x65\x6C","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x62\x74\x6E\x20\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x63\x6C\x6F\x73\x65\x2D\x62\x74\x6E","\x3C\x62\x75\x74\x74\x6F\x6E\x3E","\x24\x6D\x6F\x64\x61\x6C\x46\x6F\x6F\x74\x65\x72","\x64\x65\x6C\x65\x74\x65","\x63\x72\x65\x61\x74\x65\x42\x75\x74\x74\x6F\x6E","\x61\x63\x74\x69\x6F\x6E","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D","\x2D\x62\x74\x6E","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x62\x74\x6E","\x25","\x62\x75\x69\x6C\x64\x4F\x76\x65\x72\x6C\x61\x79","\x3C\x64\x69\x76\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x62\x6F\x78\x22\x20\x2F\x3E","\x3C\x64\x69\x76\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x22\x20\x2F\x3E","\x3C\x68\x65\x61\x64\x65\x72\x20\x2F\x3E","\x24\x6D\x6F\x64\x61\x6C\x43\x6C\x6F\x73\x65","\x26\x74\x69\x6D\x65\x73\x3B","\x3C\x73\x70\x61\x6E\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x63\x6C\x6F\x73\x65\x22\x20\x2F\x3E","\x3C\x64\x69\x76\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x62\x6F\x64\x79\x22\x20\x2F\x3E","\x3C\x66\x6F\x6F\x74\x65\x72\x20\x2F\x3E","\x3C\x64\x69\x76\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x6F\x76\x65\x72\x6C\x61\x79\x22\x3E","\x63\x6C\x69\x63\x6B\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C","\x6B\x65\x79\x75\x70\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C","\x63\x6C\x6F\x73\x65\x48\x61\x6E\x64\x6C\x65\x72","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6D\x6F\x64\x61\x6C\x2D\x63\x6C\x6F\x73\x65\x2D\x62\x74\x6E","\x64\x69\x73\x61\x62\x6C\x65\x45\x76\x65\x6E\x74\x73","\x66\x61\x73\x74","\x6D\x6F\x64\x61\x6C\x43\x6C\x6F\x73\x65\x64","\x66\x61\x64\x65\x4F\x75\x74","\x73\x65\x74\x49\x6E\x61\x63\x74\x69\x76\x65\x41\x6C\x6C","\x61\x63\x74\x69\x76\x65\x42\x75\x74\x74\x6F\x6E\x73","\x74\x6F\x67\x67\x6C\x65\x41\x63\x74\x69\x76\x65","\x61\x63\x74\x69\x76\x65\x42\x75\x74\x74\x6F\x6E\x73\x53\x74\x61\x74\x65\x73","\x61\x6C\x69\x67\x6E","\x75\x6E\x73\x65\x6C\x65\x63\x74\x61\x62\x6C\x65","\x73\x65\x74\x45\x64\x69\x74\x61\x62\x6C\x65","\x63\x6C\x69\x63\x6B\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6D\x61\x67\x65\x2D\x64\x65\x6C\x65\x74\x65","\x6C\x69\x6E\x6B\x54\x6F\x6F\x6C\x74\x69\x70","\x73\x68\x6F\x77\x54\x6F\x6F\x6C\x74\x69\x70","\x74\x6F\x75\x63\x68\x73\x74\x61\x72\x74\x20\x63\x6C\x69\x63\x6B\x2E\x72\x65\x64\x61\x63\x74\x6F\x72","\x63\x6C\x6F\x73\x65\x54\x6F\x6F\x6C\x74\x69\x70","\x67\x65\x74\x54\x6F\x6F\x6C\x74\x69\x70\x50\x6F\x73\x69\x74\x69\x6F\x6E","\x3C\x73\x70\x61\x6E\x20\x63\x6C\x61\x73\x73\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6C\x69\x6E\x6B\x2D\x74\x6F\x6F\x6C\x74\x69\x70\x22\x3E\x3C\x2F\x73\x70\x61\x6E\x3E","\x2E\x2E\x2E","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6C\x69\x6E\x6B\x2D\x74\x6F\x6F\x6C\x74\x69\x70\x2D\x61\x63\x74\x69\x6F\x6E","\x22\x20\x74\x61\x72\x67\x65\x74\x3D\x22\x5F\x62\x6C\x61\x6E\x6B\x22\x20\x2F\x3E","\x3C\x61\x20\x68\x72\x65\x66\x3D\x22\x23\x22\x20\x2F\x3E","\x75\x6E\x6C\x69\x6E\x6B","\x20\x7C\x20","\x6C\x69\x6E\x65\x2D\x68\x65\x69\x67\x68\x74","\x3C\x70\x3E\x3C\x2F\x70\x3E","\x66\x6F\x72\x6D","\x68\x65\x61\x64\x65\x72","\x66\x6F\x6F\x74\x65\x72","\x61\x73\x69\x64\x65","\x61\x72\x74\x69\x63\x6C\x65","\x69\x6E\x70\x75\x74","\x74\x65\x78\x74\x61\x72\x65\x61","\x6F\x70\x74\x69\x6F\x6E","\x6D\x61\x70","\x61\x72\x65\x61","\x6D\x61\x74\x68","\x66\x69\x65\x6C\x64\x73\x65\x74","\x6C\x65\x67\x65\x6E\x64","\x68\x67\x72\x6F\x75\x70","\x6E\x61\x76","\x64\x65\x74\x61\x69\x6C\x73","\x6D\x65\x6E\x75","\x73\x75\x6D\x6D\x61\x72\x79","\x73\x61\x66\x65\x73","\x7A","\x67\x65\x74\x53\x61\x66\x65\x73","\x67\x65\x74\x53\x61\x66\x65\x73\x43\x6F\x6D\x6D\x65\x6E\x74\x73","\x72\x65\x70\x6C\x61\x63\x65\x42\x72\x65\x61\x6B\x73\x54\x6F\x4E\x65\x77\x4C\x69\x6E\x65\x73","\x72\x65\x70\x6C\x61\x63\x65\x42\x72\x65\x61\x6B\x73\x54\x6F\x50\x61\x72\x61\x67\x72\x61\x70\x68\x73","\x72\x65\x73\x74\x6F\x72\x65\x53\x61\x66\x65\x73","\x3C\x62\x72\x5C\x73\x3F\x2F\x3F\x3E\x0A\x3F\x3C\x28","\x29\x28\x2E\x2A\x3F\x5B\x5E\x3E\x5D\x29\x3E","\x3C\x70\x3E\x3C\x62\x72\x20\x2F\x3E\x3C\x2F\x70\x3E\x0A\x3C\x24\x31\x24\x32\x3E","\x62\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65\x20\x70","\x6F\x75\x74\x65\x72\x48\x54\x4D\x4C","\x0A\x7B\x72\x65\x70\x6C\x61\x63\x65","\x7D","\x7B\x72\x65\x70\x6C\x61\x63\x65","\x68\x61\x73\x4F\x77\x6E\x50\x72\x6F\x70\x65\x72\x74\x79","\x0A\x0A","\x0A\x3C\x62\x72\x20\x2F\x3E\x3C\x62\x72\x20\x2F\x3E","\x0D\x0A","\x0D","\x2F\x0A\x0A\x2B\x2F","\x3C\x2F\x62\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65\x3E\x3C\x2F\x70\x3E","\x3C\x70\x3E\x3C\x2F\x62\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65\x3E","\x3C\x70\x3E\x3C\x62\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65\x3E","\x3C\x62\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65\x3E","\x3C\x62\x6C\x6F\x63\x6B\x71\x75\x6F\x74\x65\x3E\x3C\x2F\x70\x3E","\x3C\x70\x3E\x3C\x70\x20","\x3C\x70\x20","\x3C\x70\x3E\x3C\x70\x3E","\x3C\x2F\x70\x3E\x3C\x2F\x70\x3E","\x3C\x70\x3E\x5C\x73\x3F\x3C\x2F\x70\x3E","\x0A\x3C\x2F\x70\x3E","\x3C\x70\x3E\x09\x3F\x09\x3F\x0A\x3F\x3C\x70\x3E","\x3C\x70\x3E\x09\x2A\x3C\x2F\x70\x3E","\x63\x6C\x65\x61\x6E\x4F\x6E\x50\x61\x73\x74\x65","\x63\x72\x65\x61\x74\x65\x50\x61\x73\x74\x65\x42\x6F\x78","\x73\x63\x72\x6F\x6C\x6C\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x66\x72\x65\x65\x7A\x65","\x73\x61\x76\x65\x42\x6F\x64\x79\x53\x63\x72\x6F\x6C\x6C","\x2D\x39\x39\x39\x39\x70\x78","\x70\x61\x73\x74\x65\x42\x65\x66\x6F\x72\x65","\x6B\x65\x79\x75\x70\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x70\x6C\x61\x63\x65\x68\x6F\x6C\x64\x65\x72","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x70\x6C\x61\x63\x65\x68\x6F\x6C\x64\x65\x72","\x3C\x64\x69\x76\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x70\x72\x6F\x67\x72\x65\x73\x73\x22\x3E\x3C\x73\x70\x61\x6E\x3E\x3C\x2F\x73\x70\x61\x6E\x3E\x3C\x2F\x64\x69\x76\x3E","\x66\x61\x64\x65\x49\x6E","\x23\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x70\x72\x6F\x67\x72\x65\x73\x73","\x73\x74\x61\x72\x74\x43\x6F\x6E\x74\x61\x69\x6E\x65\x72","\x6C\x61\x73\x74\x42\x6C\x6F\x63\x6B","\x67\x65\x74\x4E\x6F\x64\x65\x73\x4D\x61\x72\x6B\x65\x72","\x73\x74\x61\x72\x74\x4F\x66\x66\x73\x65\x74","\x73\x65\x74\x4E\x6F\x64\x65\x73\x4D\x61\x72\x6B\x65\x72","\x6E\x6F\x64\x65\x73\x2D\x6D\x61\x72\x6B\x65\x72\x2D\x31","\x6E\x6F\x64\x65\x73\x2D\x6D\x61\x72\x6B\x65\x72\x2D\x32","\x72\x65\x6D\x6F\x76\x65\x4E\x6F\x64\x65\x73\x4D\x61\x72\x6B\x65\x72\x73","\x3C\x73\x70\x61\x6E\x20\x69\x64\x3D\x22\x6E\x6F\x64\x65\x73\x2D\x6D\x61\x72\x6B\x65\x72\x2D","\x22\x20\x63\x6C\x61\x73\x73\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6E\x6F\x64\x65\x73\x2D\x6D\x61\x72\x6B\x65\x72\x22\x20\x64\x61\x74\x61\x2D\x76\x65\x72\x69\x66\x69\x65\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x22\x3E","\x73\x70\x61\x6E\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x6E\x6F\x64\x65\x73\x2D\x6D\x61\x72\x6B\x65\x72","\x65\x78\x74\x72\x61\x63\x74\x43\x6F\x6E\x74\x65\x6E\x74\x73","\x63\x72\x65\x61\x74\x65\x4D\x61\x72\x6B\x65\x72\x73","\x73\x65\x74\x4D\x61\x72\x6B\x65\x72","\x73\x61\x76\x65\x64\x53\x65\x6C","\x3C\x73\x70\x61\x6E\x20\x69\x64\x3D\x22\x73\x65\x6C\x65\x63\x74\x69\x6F\x6E\x2D\x6D\x61\x72\x6B\x65\x72\x2D","\x22\x20\x63\x6C\x61\x73\x73\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x73\x65\x6C\x65\x63\x74\x69\x6F\x6E\x2D\x6D\x61\x72\x6B\x65\x72\x22\x20\x20\x64\x61\x74\x61\x2D\x76\x65\x72\x69\x66\x69\x65\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x22\x3E","\x73\x70\x61\x6E\x23\x73\x65\x6C\x65\x63\x74\x69\x6F\x6E\x2D\x6D\x61\x72\x6B\x65\x72\x2D\x31","\x73\x70\x61\x6E\x23\x73\x65\x6C\x65\x63\x74\x69\x6F\x6E\x2D\x6D\x61\x72\x6B\x65\x72\x2D\x32","\x73\x70\x61\x6E\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x73\x65\x6C\x65\x63\x74\x69\x6F\x6E\x2D\x6D\x61\x72\x6B\x65\x72","\x63\x6C\x6F\x6E\x65\x43\x6F\x6E\x74\x65\x6E\x74\x73","\x70\x61\x72\x61\x6D\x73","\x68\x61\x6E\x64\x6C\x65\x72","\x62\x61\x63\x6B\x73\x70\x61\x63\x65","\x74\x61\x62","\x72\x65\x74\x75\x72\x6E","\x73\x68\x69\x66\x74","\x70\x61\x75\x73\x65","\x63\x61\x70\x73\x6C\x6F\x63\x6B","\x65\x73\x63","\x73\x70\x61\x63\x65","\x70\x61\x67\x65\x75\x70","\x70\x61\x67\x65\x64\x6F\x77\x6E","\x65\x6E\x64","\x68\x6F\x6D\x65","\x75\x70","\x64\x6F\x77\x6E","\x3D","\x30","\x31","\x32","\x33","\x34","\x35","\x36","\x37","\x38","\x39","\x2B","\x2F","\x66\x31","\x66\x32","\x66\x33","\x66\x34","\x66\x35","\x66\x36","\x66\x37","\x66\x38","\x66\x39","\x66\x31\x30","\x66\x31\x31","\x66\x31\x32","\x6E\x75\x6D\x6C\x6F\x63\x6B","\x73\x63\x72\x6F\x6C\x6C","\x60","\x5B","\x5C","\x7E","\x21","\x24","\x28","\x29","\x5F","\x3A\x20","\x3F","\x66\x72\x6F\x6D\x43\x68\x61\x72\x43\x6F\x64\x65","\x4B\x65\x79","\x73\x68\x69\x66\x74\x2B","\x69\x3F\x66\x72\x61\x6D\x65","\x6E\x6F\x73\x63\x72\x69\x70\x74","\x68\x5B\x31\x2D\x36\x5D","\x66\x72\x61\x6D\x65\x73\x65\x74","\x6C\x69\x6E\x65\x42\x65\x66\x6F\x72\x65","\x5E\x3C\x28\x2F\x3F","\x7C\x2F\x3F","\x29\x5B\x20\x3E\x5D","\x6C\x69\x6E\x65\x41\x66\x74\x65\x72","\x5E\x3C\x28\x62\x72\x7C\x2F\x3F","\x7C\x2F","\x6E\x65\x77\x4C\x65\x76\x65\x6C","\x5E\x3C\x2F\x3F\x28","\x63\x6C\x65\x61\x6E\x6C\x65\x76\x65\x6C","\x73\x75\x62\x73\x74\x72","\x66\x69\x6E\x69\x73\x68","\x63\x68\x61\x72\x41\x74","\x67\x65\x74\x54\x61\x62\x73","\x21\x2D\x2D","\x2D\x2D\x3E","\x3E\x0A","\x70\x6C\x61\x63\x65\x54\x61\x67","\x63\x6C\x65\x61\x6E\x54\x61\x67","\x3C\x73\x63\x72\x69\x70\x74\x24\x31\x3E\x3C\x2F\x73\x63\x72\x69\x70\x74\x3E","\x65\x78\x65\x63","\x61\x6C\x6C\x6F\x77\x65\x64\x54\x61\x67\x73","\x61\x6C\x6C\x6F\x77\x65\x64\x41\x74\x74\x72","\x61\x64\x64\x54\x6F\x41\x6C\x6C\x6F\x77\x65\x64","\x72\x65\x6D\x6F\x76\x65\x46\x72\x6F\x6D\x44\x65\x6E\x69\x65\x64","\x73\x70\x6C\x69\x63\x65","\x73\x65\x74\x74\x69\x6E\x67\x73","\x72\x65\x6D\x6F\x76\x65\x43\x6F\x6D\x6D\x65\x6E\x74\x73","\x72\x65\x70\x6C\x61\x63\x65\x54\x61\x67\x73","\x72\x65\x70\x6C\x61\x63\x65\x53\x74\x79\x6C\x65\x73","\x72\x65\x6D\x6F\x76\x65\x44\x61\x74\x61\x41\x74\x74\x72","\x72\x65\x6D\x6F\x76\x65\x57\x69\x74\x68\x6F\x75\x74\x41\x74\x74\x72","\x24\x64\x69\x76","\x72\x65\x6D\x6F\x76\x65\x54\x61\x67\x73","\x72\x65\x6D\x6F\x76\x65\x50\x61\x72\x61\x67\x72\x61\x70\x68\x73\x49\x6E\x4C\x69\x73\x74\x73","\x20\x2F\x3E","\x72\x65\x6D\x6F\x76\x65\x41\x74\x74\x72\x47\x65\x74\x52\x65\x6D\x6F\x76\x65\x73","\x69\x73\x41\x72\x72\x61\x79","\x73\x70\x65\x63\x69\x66\x69\x65\x64","\x6C\x69\x20\x70","\x5E\x28\x64\x61\x74\x61\x2D\x29","\x72\x65\x6D\x6F\x76\x65\x41\x74\x74\x72\x73","\x63\x6F\x64\x65\x2E\x74\x6F\x67\x67\x6C\x65","\x70\x61\x72\x61\x67\x72\x61\x70\x68","\x62\x6C\x6F\x63\x6B\x2E\x66\x6F\x72\x6D\x61\x74","\x68\x65\x61\x64\x65\x72\x31","\x68\x65\x61\x64\x65\x72\x32","\x68\x65\x61\x64\x65\x72\x33","\x68\x65\x61\x64\x65\x72\x34","\x68\x65\x61\x64\x65\x72\x35","\x26\x62\x75\x6C\x6C\x3B\x20","\x31\x2E\x20","\x3C\x20","\x69\x6E\x64\x65\x6E\x74\x2E\x64\x65\x63\x72\x65\x61\x73\x65","\x3E\x20","\x69\x6E\x64\x65\x6E\x74\x2E\x69\x6E\x63\x72\x65\x61\x73\x65","\x69\x6D\x61\x67\x65\x2E\x73\x68\x6F\x77","\x66\x69\x6C\x65\x2E\x73\x68\x6F\x77","\x6C\x69\x6E\x6B\x2E\x75\x6E\x6C\x69\x6E\x6B","\x61\x6C\x69\x67\x6E\x5F\x6C\x65\x66\x74","\x61\x6C\x69\x67\x6E\x6D\x65\x6E\x74\x2E\x6C\x65\x66\x74","\x61\x6C\x69\x67\x6E\x5F\x63\x65\x6E\x74\x65\x72","\x61\x6C\x69\x67\x6E\x6D\x65\x6E\x74\x2E\x63\x65\x6E\x74\x65\x72","\x61\x6C\x69\x67\x6E\x5F\x72\x69\x67\x68\x74","\x61\x6C\x69\x67\x6E\x6D\x65\x6E\x74\x2E\x72\x69\x67\x68\x74","\x61\x6C\x69\x67\x6E\x5F\x6A\x75\x73\x74\x69\x66\x79","\x61\x6C\x69\x67\x6E\x6D\x65\x6E\x74\x2E\x6A\x75\x73\x74\x69\x66\x79","\x6C\x69\x6E\x65\x2E\x69\x6E\x73\x65\x72\x74","\x68\x69\x64\x65\x42\x75\x74\x74\x6F\x6E\x73","\x68\x69\x64\x65\x42\x75\x74\x74\x6F\x6E\x73\x4F\x6E\x4D\x6F\x62\x69\x6C\x65","\x69\x73\x42\x75\x74\x74\x6F\x6E\x53\x6F\x75\x72\x63\x65\x4E\x65\x65\x64\x65\x64","\x63\x72\x65\x61\x74\x65\x43\x6F\x6E\x74\x61\x69\x6E\x65\x72","\x73\x65\x74\x4F\x76\x65\x72\x66\x6C\x6F\x77","\x73\x65\x74\x46\x6F\x72\x6D\x61\x74\x74\x69\x6E\x67\x54\x61\x67\x73","\x6C\x6F\x61\x64\x42\x75\x74\x74\x6F\x6E\x73","\x73\x65\x74\x46\x69\x78\x65\x64","\x6D\x6F\x75\x73\x65\x75\x70\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x20\x6B\x65\x79\x75\x70\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x20\x66\x6F\x63\x75\x73\x2E\x72\x65\x64\x61\x63\x74\x6F\x72","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x6F\x6F\x6C\x62\x61\x72\x2D","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x6F\x6F\x6C\x62\x61\x72","\x73\x33","\x74\x6F\x6F\x6C\x62\x61\x72\x45\x78\x74\x65\x72\x6E\x61\x6C","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x6F\x6F\x6C\x62\x61\x72\x2D\x65\x78\x74\x65\x72\x6E\x61\x6C","\x74\x6F\x6F\x6C\x62\x61\x72\x46\x69\x78\x65\x64","\x6F\x62\x73\x65\x72\x76\x65\x53\x63\x72\x6F\x6C\x6C","\x73\x63\x72\x6F\x6C\x6C\x2E\x72\x65\x64\x61\x63\x74\x6F\x72","\x74\x6F\x6F\x6C\x62\x61\x72\x4F\x76\x65\x72\x66\x6C\x6F\x77","\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x74\x6F\x6F\x6C\x62\x61\x72\x2D\x6F\x76\x65\x72\x66\x6C\x6F\x77","\x62\x75\x74\x74\x6F\x6E\x73\x48\x69\x64\x65","\x62\x75\x74\x74\x6F\x6E\x73\x48\x69\x64\x65\x4F\x6E\x4D\x6F\x62\x69\x6C\x65","\x6F\x62\x73\x65\x72\x76\x65\x53\x63\x72\x6F\x6C\x6C\x45\x6E\x61\x62\x6C\x65","\x6F\x62\x73\x65\x72\x76\x65\x53\x63\x72\x6F\x6C\x6C\x44\x69\x73\x61\x62\x6C\x65","\x73\x65\x74\x44\x72\x6F\x70\x64\x6F\x77\x6E\x73\x46\x69\x78\x65\x64","\x76\x69\x73\x69\x62\x69\x6C\x69\x74\x79","\x76\x69\x73\x69\x62\x6C\x65","\x72\x65\x6C\x61\x74\x69\x76\x65","\x75\x6E\x73\x65\x74\x44\x72\x6F\x70\x64\x6F\x77\x6E\x73\x46\x69\x78\x65\x64","\x64\x69\x72\x65\x63\x74","\x63\x61\x6C\x6C\x62\x61\x63\x6B","\x24\x65\x6C","\x24\x64\x72\x6F\x70\x61\x72\x65\x61","\x3C\x64\x69\x76\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x64\x72\x6F\x70\x61\x72\x65\x61\x22\x20\x2F\x3E","\x24\x70\x6C\x61\x63\x65\x68\x6F\x6C\x64\x6C\x65\x72","\x44\x72\x6F\x70\x20\x66\x69\x6C\x65\x20\x68\x65\x72\x65\x20\x6F\x72\x20","\x3C\x64\x69\x76\x20\x69\x64\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x64\x72\x6F\x70\x61\x72\x65\x61\x2D\x70\x6C\x61\x63\x65\x68\x6F\x6C\x64\x65\x72\x22\x20\x2F\x3E","\x24\x69\x6E\x70\x75\x74","\x3C\x69\x6E\x70\x75\x74\x20\x74\x79\x70\x65\x3D\x22\x66\x69\x6C\x65\x22\x20\x6E\x61\x6D\x65\x3D\x22\x66\x69\x6C\x65\x22\x20\x2F\x3E","\x72\x65\x64\x61\x63\x74\x6F\x72\x2E\x75\x70\x6C\x6F\x61\x64","\x64\x72\x61\x67\x6F\x76\x65\x72\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2E\x75\x70\x6C\x6F\x61\x64","\x64\x72\x61\x67\x6C\x65\x61\x76\x65\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2E\x75\x70\x6C\x6F\x61\x64","\x6F\x6E\x44\x72\x61\x67\x4C\x65\x61\x76\x65","\x63\x68\x61\x6E\x67\x65\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2E\x75\x70\x6C\x6F\x61\x64","\x74\x72\x61\x76\x65\x72\x73\x65\x46\x69\x6C\x65","\x64\x72\x6F\x70\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2E\x75\x70\x6C\x6F\x61\x64","\x64\x72\x61\x67\x2D\x64\x72\x6F\x70","\x64\x72\x61\x67\x2D\x68\x6F\x76\x65\x72","\x73\x65\x74\x43\x6F\x6E\x66\x69\x67","\x73\x33\x75\x70\x6C\x6F\x61\x64\x46\x69\x6C\x65","\x69\x6D\x61\x67\x65\x55\x70\x6C\x6F\x61\x64\x50\x61\x72\x61\x6D","\x66\x69\x6C\x65\x55\x70\x6C\x6F\x61\x64\x50\x61\x72\x61\x6D","\x73\x65\x6E\x64\x44\x61\x74\x61","\x67\x65\x74\x54\x79\x70\x65","\x69\x6D\x61\x67\x65\x54\x79\x70\x65\x73","\x75\x70\x6C\x6F\x61\x64\x49\x6D\x61\x67\x65\x46\x69\x65\x6C\x64\x73","\x67\x65\x74\x48\x69\x64\x64\x65\x6E\x46\x69\x65\x6C\x64\x73","\x69\x6D\x61\x67\x65\x46\x69\x65\x6C\x64\x73","\x75\x70\x6C\x6F\x61\x64\x46\x69\x6C\x65\x46\x69\x65\x6C\x64\x73","\x66\x69\x6C\x65\x46\x69\x65\x6C\x64\x73","\x50\x4F\x53\x54","\x6F\x70\x65\x6E","\x6F\x6E\x72\x65\x61\x64\x79\x73\x74\x61\x74\x65\x63\x68\x61\x6E\x67\x65","\x72\x65\x61\x64\x79\x53\x74\x61\x74\x65","\x72\x65\x73\x70\x6F\x6E\x73\x65\x54\x65\x78\x74","\x73\x65\x6E\x64","\x73\x33\x75\x70\x6C\x6F\x61\x64\x54\x6F\x53\x33","\x73\x33\x65\x78\x65\x63\x75\x74\x65\x4F\x6E\x53\x69\x67\x6E\x65\x64\x55\x72\x6C","\x47\x45\x54","\x6E\x61\x6D\x65\x3D","\x26\x74\x79\x70\x65\x3D","\x6F\x76\x65\x72\x72\x69\x64\x65\x4D\x69\x6D\x65\x54\x79\x70\x65","\x74\x65\x78\x74\x2F\x70\x6C\x61\x69\x6E\x3B\x20\x63\x68\x61\x72\x73\x65\x74\x3D\x78\x2D\x75\x73\x65\x72\x2D\x64\x65\x66\x69\x6E\x65\x64","\x73\x74\x61\x74\x75\x73","\x77\x69\x74\x68\x43\x72\x65\x64\x65\x6E\x74\x69\x61\x6C\x73","\x50\x55\x54","\x73\x33\x63\x72\x65\x61\x74\x65\x43\x4F\x52\x53\x52\x65\x71\x75\x65\x73\x74","\x6F\x6E\x6C\x6F\x61\x64","\x6F\x6E\x65\x72\x72\x6F\x72","\x6F\x6E\x70\x72\x6F\x67\x72\x65\x73\x73","\x43\x6F\x6E\x74\x65\x6E\x74\x2D\x54\x79\x70\x65","\x73\x65\x74\x52\x65\x71\x75\x65\x73\x74\x48\x65\x61\x64\x65\x72","\x78\x2D\x61\x6D\x7A\x2D\x61\x63\x6C","\x70\x75\x62\x6C\x69\x63\x2D\x72\x65\x61\x64","\x75\x73\x65\x72\x41\x67\x65\x6E\x74","\x74\x6F\x53\x74\x72\x69\x6E\x67","\x5B\x6F\x62\x6A\x65\x63\x74\x20\x53\x74\x72\x69\x6E\x67\x5D","\x72\x67\x62\x28","\x65\x71","\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x69\x6E\x76\x69\x73\x69\x62\x6C\x65\x2D\x73\x70\x61\x63\x65","\x68\x72\x2C\x20\x62\x72\x2C\x20\x69\x6D\x67\x2C\x20\x69\x66\x72\x61\x6D\x65","\x73\x61\x76\x65\x45\x64\x69\x74\x6F\x72\x53\x63\x72\x6F\x6C\x6C","\x73\x63\x72\x6F\x6C\x6C\x54\x61\x72\x67\x65\x74","\x73\x61\x76\x65\x54\x61\x72\x67\x65\x74\x53\x63\x72\x6F\x6C\x6C","\x67\x65\x74\x4F\x66\x66\x73\x65\x74\x4F\x66\x45\x6C\x65\x6D\x65\x6E\x74","\x73\x65\x6C\x65\x63\x74\x41\x6C\x6C","\x2E\x72\x65\x64\x61\x63\x74\x6F\x72\x2D\x65\x64\x69\x74\x6F\x72","\x69\x73\x43\x75\x72\x72\x65\x6E\x74\x4F\x72\x50\x61\x72\x65\x6E\x74\x4F\x6E\x65","\x74\x6F\x55\x70\x70\x65\x72\x43\x61\x73\x65","\x76\x65\x72\x73\x69\x6F\x6E","\x74\x72\x69\x64\x65\x6E\x74","\x63\x6F\x6D\x70\x61\x74\x69\x62\x6C\x65","\x73\x61\x66\x61\x72\x69","\x77\x65\x62\x6B\x69\x74","\x63\x68\x72\x6F\x6D\x65","\x72\x76","\x6F\x70\x72","\x6C\x6F\x61\x64\x2E\x74\x6F\x6F\x6C\x73\x2E\x72\x65\x64\x61\x63\x74\x6F\x72","\x5B\x64\x61\x74\x61\x2D\x74\x6F\x6F\x6C\x73\x3D\x22\x72\x65\x64\x61\x63\x74\x6F\x72\x22\x5D","","\x63\x68\x69\x6C\x64\x4E\x6F\x64\x65\x73","\x3C\x69\x66\x72\x61\x6D\x65\x20\x77\x69\x64\x74\x68\x3D\x22\x35\x30\x30\x22\x20\x68\x65\x69\x67\x68\x74\x3D\x22\x32\x38\x31\x22\x20\x73\x72\x63\x3D\x22","\x22\x20\x66\x72\x61\x6D\x65\x62\x6F\x72\x64\x65\x72\x3D\x22\x30\x22\x20\x61\x6C\x6C\x6F\x77\x66\x75\x6C\x6C\x73\x63\x72\x65\x65\x6E\x3E\x3C\x2F\x69\x66\x72\x61\x6D\x65\x3E","\x2F\x2F\x77\x77\x77\x2E\x79\x6F\x75\x74\x75\x62\x65\x2E\x63\x6F\x6D\x2F\x65\x6D\x62\x65\x64\x2F\x24\x31","\x2F\x2F\x70\x6C\x61\x79\x65\x72\x2E\x76\x69\x6D\x65\x6F\x2E\x63\x6F\x6D\x2F\x76\x69\x64\x65\x6F\x2F\x24\x32","\x3C\x69\x6D\x67\x20\x73\x72\x63\x3D\x22\x24\x31\x22\x20\x2F\x3E"];(function ($){_0x8057[0];if(!Function[_0x8057[2]][_0x8057[1]]){Function[_0x8057[2]][_0x8057[1]]=function (scope){var fn=this;return function (){return fn[_0x8057[3]](scope);} ;} ;} ;var uuid=0;var reUrlYoutube=/https?:\/\/(?:[0-9A-Z-]+\.)?(?:youtu\.be\/|youtube\.com\S*[^\w\-\s])([\w\-]{11})(?=[^\w\-]|$)(?![?=&+%\w.\-]*(?:['"][^<>]*>|<\/a>))[?=&+%\w.-]*/ig;var reUrlVimeo=/https?:\/\/(www\.)?vimeo.com\/(\d+)($|\/)/;$[_0x8057[5]][_0x8057[4]]=function (options){var val=[];var args=Array[_0x8057[2]][_0x8057[7]][_0x8057[6]](arguments,1);if( typeof options===_0x8057[8]){this[_0x8057[20]](function (){var instance=$[_0x8057[9]](this,_0x8057[4]);var func;if(options[_0x8057[10]](/\./)!=_0x8057[11]){func=options[_0x8057[13]](_0x8057[12]);if( typeof instance[func[0]]!=_0x8057[14]){func=instance[func[0]][func[1]];} ;} else {func=instance[options];} ;if( typeof instance!==_0x8057[14]&&$[_0x8057[15]](func)){var methodVal=func[_0x8057[3]](instance,args);if(methodVal!==undefined&&methodVal!==instance){val[_0x8057[16]](methodVal);} ;} else {$[_0x8057[19]](_0x8057[17]+options+_0x8057[18]);} ;} );} else {this[_0x8057[20]](function (){$[_0x8057[9]](this,_0x8057[4],{});$[_0x8057[9]](this,_0x8057[4],Redactor(this,options));} );} ;if(val[_0x8057[21]]===0){return this;} else {if(val[_0x8057[21]]===1){return val[0];} else {return val;} ;} ;} ;function Redactor(el,options){return  new Redactor[_0x8057[2]][_0x8057[22]](el,options);} ;$[_0x8057[23]]=Redactor;$[_0x8057[23]][_0x8057[24]]=_0x8057[25];$[_0x8057[23]][_0x8057[26]]=[_0x8057[27],_0x8057[28],_0x8057[29],_0x8057[30],_0x8057[31],_0x8057[32],_0x8057[33],_0x8057[34],_0x8057[35],_0x8057[36],_0x8057[37],_0x8057[38],_0x8057[39],_0x8057[40],_0x8057[41],_0x8057[42],_0x8057[43],_0x8057[44],_0x8057[45],_0x8057[46],_0x8057[47],_0x8057[48],_0x8057[49],_0x8057[50],_0x8057[51],_0x8057[52],_0x8057[53],_0x8057[54],_0x8057[55],_0x8057[56],_0x8057[57],_0x8057[58],_0x8057[59],_0x8057[60],_0x8057[61],_0x8057[62]];$[_0x8057[23]][_0x8057[63]]={lang:_0x8057[64],direction:_0x8057[65],plugins:false,focus:false,focusEnd:false,placeholder:false,visual:true,tabindex:false,minHeight:false,maxHeight:false,linebreaks:false,replaceDivs:true,paragraphize:true,cleanStyleOnEnter:false,enterKey:true,cleanOnPaste:true,cleanSpaces:true,pastePlainText:false,autosave:false,autosaveName:false,autosaveInterval:60,autosaveOnChange:false,linkTooltip:true,linkProtocol:_0x8057[66],linkNofollow:false,linkSize:50,imageEditable:true,imageLink:true,imagePosition:true,imageFloatMargin:_0x8057[67],imageResizable:true,imageUpload:null,imageUploadParam:_0x8057[38],uploadImageField:false,dragImageUpload:true,fileUpload:null,fileUploadParam:_0x8057[38],dragFileUpload:true,s3:false,convertLinks:true,convertUrlLinks:true,convertImageLinks:true,convertVideoLinks:true,preSpaces:4,tabAsSpaces:false,tabKey:true,scrollTarget:false,toolbar:true,toolbarFixed:true,toolbarFixedTarget:document,toolbarFixedTopOffset:0,toolbarExternal:false,toolbarOverflow:false,source:true,buttons:[_0x8057[68],_0x8057[69],_0x8057[70],_0x8057[71],_0x8057[72],_0x8057[73],_0x8057[74],_0x8057[75],_0x8057[41],_0x8057[40],_0x8057[38],_0x8057[48],_0x8057[27],_0x8057[76]],buttonsHide:[],buttonsHideOnMobile:[],formatting:[_0x8057[77],_0x8057[78],_0x8057[79],_0x8057[80],_0x8057[81],_0x8057[82],_0x8057[83],_0x8057[84],_0x8057[85]],formattingAdd:false,tabifier:true,deniedTags:[_0x8057[68],_0x8057[86],_0x8057[48],_0x8057[87],_0x8057[88],_0x8057[89],_0x8057[90],_0x8057[91]],allowedTags:false,removeComments:false,replaceTags:[[_0x8057[92],_0x8057[93]]],replaceStyles:[[_0x8057[94],_0x8057[95]],[_0x8057[96],_0x8057[97]],[_0x8057[98],_0x8057[99]],[_0x8057[100],_0x8057[93]]],removeDataAttr:false,removeAttr:false,allowedAttr:false,removeWithoutAttr:[_0x8057[101]],removeEmpty:[_0x8057[77]],activeButtons:[_0x8057[72],_0x8057[71],_0x8057[70],_0x8057[102],_0x8057[73],_0x8057[74],_0x8057[103],_0x8057[104],_0x8057[105],_0x8057[106]],activeButtonsStates:{b:_0x8057[70],strong:_0x8057[70],i:_0x8057[71],em:_0x8057[71],del:_0x8057[72],strike:_0x8057[72],ul:_0x8057[73],ol:_0x8057[74],u:_0x8057[102]},shortcuts:{"\x63\x74\x72\x6C\x2B\x73\x68\x69\x66\x74\x2B\x6D\x2C\x20\x6D\x65\x74\x61\x2B\x73\x68\x69\x66\x74\x2B\x6D":{func:_0x8057[107]},"\x63\x74\x72\x6C\x2B\x62\x2C\x20\x6D\x65\x74\x61\x2B\x62":{func:_0x8057[108],params:[_0x8057[70]]},"\x63\x74\x72\x6C\x2B\x69\x2C\x20\x6D\x65\x74\x61\x2B\x69":{func:_0x8057[108],params:[_0x8057[71]]},"\x63\x74\x72\x6C\x2B\x68\x2C\x20\x6D\x65\x74\x61\x2B\x68":{func:_0x8057[108],params:[_0x8057[109]]},"\x63\x74\x72\x6C\x2B\x6C\x2C\x20\x6D\x65\x74\x61\x2B\x6C":{func:_0x8057[108],params:[_0x8057[110]]},"\x63\x74\x72\x6C\x2B\x6B\x2C\x20\x6D\x65\x74\x61\x2B\x6B":{func:_0x8057[111]},"\x63\x74\x72\x6C\x2B\x73\x68\x69\x66\x74\x2B\x37":{func:_0x8057[112],params:[_0x8057[74]]},"\x63\x74\x72\x6C\x2B\x73\x68\x69\x66\x74\x2B\x38":{func:_0x8057[112],params:[_0x8057[73]]}},shortcutsAdd:false,buffer:[],rebuffer:[],emptyHtml:_0x8057[113],invisibleSpace:_0x8057[114],imageTypes:[_0x8057[115],_0x8057[116],_0x8057[117]],indentValue:20,verifiedTags:[_0x8057[118],_0x8057[119],_0x8057[120],_0x8057[95],_0x8057[121],_0x8057[122],_0x8057[123],_0x8057[97],_0x8057[99],_0x8057[124],_0x8057[92],_0x8057[93],_0x8057[125],_0x8057[126],_0x8057[127],_0x8057[128]],inlineTags:[_0x8057[95],_0x8057[120],_0x8057[99],_0x8057[97],_0x8057[123],_0x8057[35],_0x8057[93],_0x8057[129],_0x8057[130],_0x8057[131],_0x8057[122],_0x8057[121],_0x8057[132],_0x8057[133],_0x8057[125],_0x8057[124]],alignmentTags:[_0x8057[134],_0x8057[135],_0x8057[136],_0x8057[137],_0x8057[138],_0x8057[139],_0x8057[140],_0x8057[141],_0x8057[142],_0x8057[143],_0x8057[144],_0x8057[145],_0x8057[146],_0x8057[147],_0x8057[148],_0x8057[149],_0x8057[150],_0x8057[151],_0x8057[152],_0x8057[153],_0x8057[154]],blockLevelElements:[_0x8057[155],_0x8057[156],_0x8057[157],_0x8057[158]],langs:{en:{html:_0x8057[159],video:_0x8057[160],image:_0x8057[161],table:_0x8057[162],link:_0x8057[163],link_insert:_0x8057[164],link_edit:_0x8057[165],unlink:_0x8057[166],formatting:_0x8057[167],paragraph:_0x8057[168],quote:_0x8057[169],code:_0x8057[170],header1:_0x8057[171],header2:_0x8057[172],header3:_0x8057[173],header4:_0x8057[174],header5:_0x8057[175],bold:_0x8057[176],italic:_0x8057[177],fontcolor:_0x8057[178],backcolor:_0x8057[179],unorderedlist:_0x8057[180],orderedlist:_0x8057[181],outdent:_0x8057[182],indent:_0x8057[183],cancel:_0x8057[184],insert:_0x8057[185],save:_0x8057[186],_delete:_0x8057[187],insert_table:_0x8057[188],insert_row_above:_0x8057[189],insert_row_below:_0x8057[190],insert_column_left:_0x8057[191],insert_column_right:_0x8057[192],delete_column:_0x8057[193],delete_row:_0x8057[194],delete_table:_0x8057[195],rows:_0x8057[196],columns:_0x8057[197],add_head:_0x8057[198],delete_head:_0x8057[199],title:_0x8057[200],image_position:_0x8057[201],none:_0x8057[202],left:_0x8057[203],right:_0x8057[204],center:_0x8057[205],image_web_link:_0x8057[206],text:_0x8057[207],mailto:_0x8057[208],web:_0x8057[209],video_html_code:_0x8057[210],file:_0x8057[211],upload:_0x8057[212],download:_0x8057[213],choose:_0x8057[214],or_choose:_0x8057[215],drop_file_here:_0x8057[216],align_left:_0x8057[217],align_center:_0x8057[218],align_right:_0x8057[219],align_justify:_0x8057[220],horizontalrule:_0x8057[221],deleted:_0x8057[222],anchor:_0x8057[223],link_new_tab:_0x8057[224],underline:_0x8057[225],alignment:_0x8057[226],filename:_0x8057[227],edit:_0x8057[228]}}};Redactor[_0x8057[5]]=$[_0x8057[23]][_0x8057[2]]={keyCode:{BACKSPACE:8,DELETE:46,DOWN:40,ENTER:13,SPACE:32,ESC:27,TAB:9,CTRL:17,META:91,SHIFT:16,ALT:18,RIGHT:39,LEFT:37,LEFT_WIN:91},init:function (el,options){this[_0x8057[229]]=$(el);this[_0x8057[230]]=uuid++;this[_0x8057[231]]=false;this[_0x8057[232]]=false;this[_0x8057[233]](options);this[_0x8057[234]]();this[_0x8057[69]]={};$[_0x8057[237]](this[_0x8057[63]][_0x8057[235]],this[_0x8057[63]][_0x8057[236]]);this[_0x8057[238]]= new RegExp(_0x8057[239]+this[_0x8057[63]][_0x8057[235]][_0x8057[241]](_0x8057[240])+_0x8057[242],_0x8057[123]);this[_0x8057[59]][_0x8057[243]]();this[_0x8057[46]][_0x8057[244]]();$[_0x8057[246]](this[_0x8057[63]][_0x8057[57]],this[_0x8057[63]][_0x8057[245]]);this[_0x8057[36]][_0x8057[248]](_0x8057[247]);this[_0x8057[247]]=true;this[_0x8057[31]][_0x8057[249]]();} ,loadOptions:function (options){this[_0x8057[63]]=$[_0x8057[246]]({},$[_0x8057[246]](true,{},$[_0x8057[23]][_0x8057[63]]),this[_0x8057[229]][_0x8057[9]](),options);} ,getModuleMethods:function (object){return Object[_0x8057[252]](object)[_0x8057[251]](function (property){return  typeof object[property]==_0x8057[250];} );} ,loadModules:function (){var len=$[_0x8057[23]][_0x8057[26]][_0x8057[21]];for(var i=0;i<len;i++){this[_0x8057[253]]($[_0x8057[23]][_0x8057[26]][i]);} ;} ,bindModuleMethods:function (module){if( typeof this[module]==_0x8057[14]){return ;} ;this[module]=this[module]();var methods=this[_0x8057[254]](this[module]);var len=methods[_0x8057[21]];for(var z=0;z<len;z++){this[module][methods[z]]=this[module][methods[z]][_0x8057[1]](this);} ;} ,alignment:function (){return {left:function (){this[_0x8057[27]][_0x8057[256]](_0x8057[255]);} ,right:function (){this[_0x8057[27]][_0x8057[256]](_0x8057[257]);} ,center:function (){this[_0x8057[27]][_0x8057[256]](_0x8057[258]);} ,justify:function (){this[_0x8057[27]][_0x8057[256]](_0x8057[106]);} ,set:function (type){if(!this[_0x8057[62]][_0x8057[260]](_0x8057[259])){this[_0x8057[261]][_0x8057[39]]();} ;this[_0x8057[30]][_0x8057[256]]();this[_0x8057[56]][_0x8057[262]]();this[_0x8057[27]][_0x8057[263]]=this[_0x8057[56]][_0x8057[264]]();this[_0x8057[27]][_0x8057[265]]=type;if(this[_0x8057[27]][_0x8057[266]]()){this[_0x8057[27]][_0x8057[267]]();} else {this[_0x8057[27]][_0x8057[268]]();} ;this[_0x8057[56]][_0x8057[269]]();this[_0x8057[35]][_0x8057[270]]();} ,setText:function (){var wrapper=this[_0x8057[56]][_0x8057[272]](_0x8057[271]);$(wrapper)[_0x8057[276]](_0x8057[275],_0x8057[4])[_0x8057[274]](_0x8057[273],this[_0x8057[27]][_0x8057[265]]);} ,setBlocks:function (){$[_0x8057[20]](this[_0x8057[27]][_0x8057[263]],$[_0x8057[281]](function (i,el){var $el=this[_0x8057[62]][_0x8057[277]](el);if(!$el){return ;} ;if(this[_0x8057[27]][_0x8057[278]]($el)){this[_0x8057[27]][_0x8057[279]]($el);} else {this[_0x8057[27]][_0x8057[280]]($el);} ;} ,this));} ,isLinebreaksOrNoBlocks:function (){return (this[_0x8057[63]][_0x8057[282]]&&this[_0x8057[27]][_0x8057[263]][0]===false);} ,isNeedReplaceElement:function ($el){return (this[_0x8057[27]][_0x8057[265]]===_0x8057[255]&& typeof ($el[_0x8057[9]](_0x8057[283]))!==_0x8057[14]);} ,replaceElement:function ($el){$el[_0x8057[284]]($el[_0x8057[68]]());} ,alignElement:function ($el){$el[_0x8057[274]](_0x8057[273],this[_0x8057[27]][_0x8057[265]]);this[_0x8057[62]][_0x8057[285]]($el,_0x8057[90]);} };} ,autosave:function (){return {enable:function (){if(!this[_0x8057[63]][_0x8057[28]]){return ;} ;this[_0x8057[28]][_0x8057[68]]=false;this[_0x8057[28]][_0x8057[286]]=(this[_0x8057[63]][_0x8057[287]])?this[_0x8057[63]][_0x8057[287]]:this[_0x8057[288]][_0x8057[276]](_0x8057[286]);if(this[_0x8057[63]][_0x8057[289]]){return ;} ;this[_0x8057[290]]=setInterval(this[_0x8057[28]][_0x8057[244]],this[_0x8057[63]][_0x8057[290]]*1000);} ,onChange:function (){if(!this[_0x8057[63]][_0x8057[289]]){return ;} ;this[_0x8057[28]][_0x8057[244]]();} ,load:function (){this[_0x8057[28]][_0x8057[291]]=this[_0x8057[35]][_0x8057[292]]();if(this[_0x8057[28]][_0x8057[68]]===this[_0x8057[28]][_0x8057[291]]){return ;} ;if(this[_0x8057[62]][_0x8057[293]](this[_0x8057[28]][_0x8057[291]])){return ;} ;var data={};data[_0x8057[286]]=this[_0x8057[28]][_0x8057[286]];data[this[_0x8057[28]][_0x8057[286]]]=escape(encodeURIComponent(this[_0x8057[28]][_0x8057[291]]));var jsxhr=$[_0x8057[295]]({url:this[_0x8057[63]][_0x8057[28]],type:_0x8057[294],data:data});jsxhr[_0x8057[297]](this[_0x8057[28]][_0x8057[296]]);} ,success:function (data){var json;try{json=$[_0x8057[298]](data);} catch(e){json=data;} ;var callbackName=( typeof json[_0x8057[19]]==_0x8057[14])?_0x8057[28]:_0x8057[299];this[_0x8057[36]][_0x8057[248]](callbackName,this[_0x8057[28]][_0x8057[286]],json);this[_0x8057[28]][_0x8057[68]]=this[_0x8057[28]][_0x8057[291]];} ,disable:function (){clearInterval(this[_0x8057[290]]);} };} ,block:function (){return {formatting:function (name){this[_0x8057[29]][_0x8057[300]]=false;var type,value;if( typeof this[_0x8057[69]][name][_0x8057[9]]!=_0x8057[14]){type=_0x8057[9];} else {if( typeof this[_0x8057[69]][name][_0x8057[276]]!=_0x8057[14]){type=_0x8057[276];} else {if( typeof this[_0x8057[69]][name][_0x8057[301]]!=_0x8057[14]){type=_0x8057[301];} ;} ;} ;if( typeof this[_0x8057[69]][name][_0x8057[302]]!=_0x8057[14]){this[_0x8057[29]][_0x8057[300]]=true;} ;if(type){value=this[_0x8057[69]][name][type];} ;this[_0x8057[29]][_0x8057[304]](this[_0x8057[69]][name][_0x8057[303]],type,value);} ,format:function (tag,type,value){if(tag==_0x8057[305]){tag=_0x8057[78];} ;var formatTags=[_0x8057[77],_0x8057[79],_0x8057[78],_0x8057[80],_0x8057[81],_0x8057[82],_0x8057[83],_0x8057[84],_0x8057[85]];if($[_0x8057[306]](tag,formatTags)==-1){return ;} ;this[_0x8057[29]][_0x8057[307]]=(tag==_0x8057[79]||tag[_0x8057[10]](/h[1-6]/i)!=-1);if(!this[_0x8057[62]][_0x8057[260]](_0x8057[259])){this[_0x8057[261]][_0x8057[39]]();} ;this[_0x8057[29]][_0x8057[263]]=this[_0x8057[56]][_0x8057[264]]();this[_0x8057[29]][_0x8057[308]]=this[_0x8057[29]][_0x8057[263]][_0x8057[21]];this[_0x8057[29]][_0x8057[265]]=type;this[_0x8057[29]][_0x8057[309]]=value;this[_0x8057[30]][_0x8057[256]]();this[_0x8057[56]][_0x8057[262]]();this[_0x8057[29]][_0x8057[256]](tag);this[_0x8057[56]][_0x8057[269]]();this[_0x8057[35]][_0x8057[270]]();} ,set:function (tag){this[_0x8057[56]][_0x8057[292]]();this[_0x8057[29]][_0x8057[310]]=this[_0x8057[313]][_0x8057[312]][_0x8057[311]];if(this[_0x8057[313]][_0x8057[314]]){this[_0x8057[29]][_0x8057[315]](tag);} else {this[_0x8057[29]][_0x8057[316]](tag);} ;} ,setCollapsed:function (tag){var block=this[_0x8057[29]][_0x8057[263]][0];if(block===false){return ;} ;if(block[_0x8057[311]]==_0x8057[158]){if(tag!=_0x8057[78]){return ;} ;this[_0x8057[29]][_0x8057[317]]();return ;} ;var isContainerTable=(this[_0x8057[29]][_0x8057[310]]==_0x8057[145]||this[_0x8057[29]][_0x8057[310]]==_0x8057[318]);if(isContainerTable&&!this[_0x8057[63]][_0x8057[282]]){document[_0x8057[322]](_0x8057[319],false,_0x8057[320]+tag+_0x8057[321]);block=this[_0x8057[56]][_0x8057[323]]();this[_0x8057[29]][_0x8057[324]]($(block));} else {if(block[_0x8057[311]][_0x8057[325]]()!=tag){if(this[_0x8057[63]][_0x8057[282]]&&tag==_0x8057[77]){$(block)[_0x8057[328]](_0x8057[326])[_0x8057[327]](_0x8057[326]);this[_0x8057[62]][_0x8057[329]](block);} else {var $formatted=this[_0x8057[62]][_0x8057[330]](block,tag);this[_0x8057[29]][_0x8057[324]]($formatted);if(tag!=_0x8057[77]&&tag!=_0x8057[78]){$formatted[_0x8057[332]](_0x8057[119])[_0x8057[331]]();} ;if(this[_0x8057[29]][_0x8057[307]]){this[_0x8057[62]][_0x8057[333]]($formatted);} ;if(tag==_0x8057[77]||this[_0x8057[29]][_0x8057[334]]){$formatted[_0x8057[332]](_0x8057[77])[_0x8057[336]]()[_0x8057[335]]();} ;this[_0x8057[29]][_0x8057[337]]($formatted);} ;} else {if(tag==_0x8057[78]&&block[_0x8057[311]][_0x8057[325]]()==tag){if(this[_0x8057[63]][_0x8057[282]]){$(block)[_0x8057[328]](_0x8057[326])[_0x8057[327]](_0x8057[326]);this[_0x8057[62]][_0x8057[329]](block);} else {var $el=this[_0x8057[62]][_0x8057[330]](block,_0x8057[77]);this[_0x8057[29]][_0x8057[324]]($el);} ;} else {if(block[_0x8057[311]][_0x8057[325]]()==tag){this[_0x8057[29]][_0x8057[324]]($(block));} ;} ;} ;} ;} ,setMultiple:function (tag){var block=this[_0x8057[29]][_0x8057[263]][0];var isContainerTable=(this[_0x8057[29]][_0x8057[310]]==_0x8057[145]||this[_0x8057[29]][_0x8057[310]]==_0x8057[318]);if(block!==false&&this[_0x8057[29]][_0x8057[308]]===1){if(block[_0x8057[311]][_0x8057[325]]()==tag&&tag==_0x8057[78]){if(this[_0x8057[63]][_0x8057[282]]){$(block)[_0x8057[328]](_0x8057[326])[_0x8057[327]](_0x8057[326]);this[_0x8057[62]][_0x8057[329]](block);} else {var $el=this[_0x8057[62]][_0x8057[330]](block,_0x8057[77]);this[_0x8057[29]][_0x8057[324]]($el);} ;} else {if(block[_0x8057[311]]==_0x8057[158]){if(tag!=_0x8057[78]){return ;} ;this[_0x8057[29]][_0x8057[317]]();} else {if(this[_0x8057[29]][_0x8057[310]]==_0x8057[146]){this[_0x8057[29]][_0x8057[338]](tag);} else {if(this[_0x8057[63]][_0x8057[282]]&&((isContainerTable)||(this[_0x8057[313]][_0x8057[312]]!=block))){this[_0x8057[29]][_0x8057[339]](tag);} else {if(this[_0x8057[63]][_0x8057[282]]&&tag==_0x8057[77]){$(block)[_0x8057[328]](_0x8057[326])[_0x8057[327]](_0x8057[326]);this[_0x8057[62]][_0x8057[329]](block);} else {if(block[_0x8057[311]]===_0x8057[145]){this[_0x8057[29]][_0x8057[339]](tag);} else {var $formatted=this[_0x8057[62]][_0x8057[330]](block,tag);this[_0x8057[29]][_0x8057[324]]($formatted);if(this[_0x8057[29]][_0x8057[307]]){this[_0x8057[62]][_0x8057[333]]($formatted);} ;if(tag==_0x8057[77]||this[_0x8057[29]][_0x8057[334]]){$formatted[_0x8057[332]](_0x8057[77])[_0x8057[336]]()[_0x8057[335]]();} ;} ;} ;} ;} ;} ;} ;} else {if(this[_0x8057[63]][_0x8057[282]]||tag!=_0x8057[77]){if(tag==_0x8057[78]){var count=0;for(var i=0;i<this[_0x8057[29]][_0x8057[308]];i++){if(this[_0x8057[29]][_0x8057[263]][i][_0x8057[311]]==_0x8057[146]){count++;} ;} ;if(count==this[_0x8057[29]][_0x8057[308]]){$[_0x8057[20]](this[_0x8057[29]][_0x8057[263]],$[_0x8057[281]](function (i,s){if(this[_0x8057[63]][_0x8057[282]]){$(s)[_0x8057[328]](_0x8057[326])[_0x8057[327]](_0x8057[326]);this[_0x8057[62]][_0x8057[329]](s);} else {this[_0x8057[62]][_0x8057[330]](s,_0x8057[77]);} ;} ,this));return ;} ;} ;this[_0x8057[29]][_0x8057[339]](tag);} else {var classSize=0;var toggleType=false;if(this[_0x8057[29]][_0x8057[265]]==_0x8057[301]){toggleType=_0x8057[324];classSize=$(this[_0x8057[29]][_0x8057[263]])[_0x8057[251]](_0x8057[12]+this[_0x8057[29]][_0x8057[309]])[_0x8057[21]];if(this[_0x8057[29]][_0x8057[308]]==classSize){toggleType=_0x8057[324];} else {if(this[_0x8057[29]][_0x8057[308]]>classSize){toggleType=_0x8057[256];} else {if(classSize===0){toggleType=_0x8057[256];} ;} ;} ;} ;var exceptTags=[_0x8057[126],_0x8057[127],_0x8057[128],_0x8057[340],_0x8057[341],_0x8057[342],_0x8057[343],_0x8057[344]];$[_0x8057[20]](this[_0x8057[29]][_0x8057[263]],$[_0x8057[281]](function (i,s){if($[_0x8057[306]](s[_0x8057[311]][_0x8057[325]](),exceptTags)!=-1){return ;} ;var $formatted=this[_0x8057[62]][_0x8057[330]](s,tag);if(toggleType){if(toggleType==_0x8057[324]){this[_0x8057[29]][_0x8057[324]]($formatted);} else {if(toggleType==_0x8057[331]){this[_0x8057[29]][_0x8057[331]]($formatted);} else {if(toggleType==_0x8057[256]){this[_0x8057[29]][_0x8057[345]]($formatted);} ;} ;} ;} else {this[_0x8057[29]][_0x8057[324]]($formatted);} ;if(tag!=_0x8057[77]&&tag!=_0x8057[78]){$formatted[_0x8057[332]](_0x8057[119])[_0x8057[331]]();} ;if(this[_0x8057[29]][_0x8057[307]]){this[_0x8057[62]][_0x8057[333]]($formatted);} ;if(tag==_0x8057[77]||this[_0x8057[29]][_0x8057[334]]){$formatted[_0x8057[332]](_0x8057[77])[_0x8057[336]]()[_0x8057[335]]();} ;} ,this));} ;} ;} ,setForce:function ($el){if(this[_0x8057[29]][_0x8057[300]]){$el[_0x8057[346]](_0x8057[301])[_0x8057[346]](_0x8057[90]);} ;if(this[_0x8057[29]][_0x8057[265]]==_0x8057[301]){$el[_0x8057[347]](this[_0x8057[29]][_0x8057[309]]);return ;} else {if(this[_0x8057[29]][_0x8057[265]]==_0x8057[276]||this[_0x8057[29]][_0x8057[265]]==_0x8057[9]){$el[_0x8057[276]](this[_0x8057[29]][_0x8057[309]][_0x8057[286]],this[_0x8057[29]][_0x8057[309]][_0x8057[309]]);return ;} ;} ;} ,toggle:function ($el){if(this[_0x8057[29]][_0x8057[300]]){$el[_0x8057[346]](_0x8057[301])[_0x8057[346]](_0x8057[90]);} ;if(this[_0x8057[29]][_0x8057[265]]==_0x8057[301]){$el[_0x8057[348]](this[_0x8057[29]][_0x8057[309]]);return ;} else {if(this[_0x8057[29]][_0x8057[265]]==_0x8057[276]||this[_0x8057[29]][_0x8057[265]]==_0x8057[9]){if($el[_0x8057[276]](this[_0x8057[29]][_0x8057[309]][_0x8057[286]])==this[_0x8057[29]][_0x8057[309]][_0x8057[309]]){$el[_0x8057[346]](this[_0x8057[29]][_0x8057[309]][_0x8057[286]]);} else {$el[_0x8057[276]](this[_0x8057[29]][_0x8057[309]][_0x8057[286]],this[_0x8057[29]][_0x8057[309]][_0x8057[309]]);} ;return ;} else {$el[_0x8057[346]](_0x8057[349]);return ;} ;} ;} ,remove:function ($el){$el[_0x8057[350]](this[_0x8057[29]][_0x8057[309]]);} ,formatListToBlockquote:function (){var block=$(this[_0x8057[29]][_0x8057[263]][0])[_0x8057[352]](_0x8057[351]);$(block)[_0x8057[332]](_0x8057[351])[_0x8057[336]]()[_0x8057[335]]();$(block)[_0x8057[332]](_0x8057[128])[_0x8057[327]]($(_0x8057[326]))[_0x8057[336]]()[_0x8057[335]]();var $el=this[_0x8057[62]][_0x8057[330]](block,_0x8057[78]);this[_0x8057[29]][_0x8057[324]]($el);} ,formatBlockquote:function (tag){document[_0x8057[322]](_0x8057[75]);document[_0x8057[322]](_0x8057[319],false,tag);this[_0x8057[34]][_0x8057[353]]();this[_0x8057[261]][_0x8057[332]](_0x8057[354])[_0x8057[331]]();var formatted=this[_0x8057[56]][_0x8057[323]]();if(tag!=_0x8057[77]){$(formatted)[_0x8057[332]](_0x8057[119])[_0x8057[331]]();} ;if(!this[_0x8057[63]][_0x8057[282]]){this[_0x8057[29]][_0x8057[324]]($(formatted));} ;this[_0x8057[261]][_0x8057[332]](_0x8057[356])[_0x8057[20]]($[_0x8057[281]](this[_0x8057[62]][_0x8057[355]],this));if(this[_0x8057[63]][_0x8057[282]]&&tag==_0x8057[77]){this[_0x8057[62]][_0x8057[329]](formatted);} ;} ,formatWrap:function (tag){if(this[_0x8057[29]][_0x8057[310]]==_0x8057[156]||this[_0x8057[29]][_0x8057[310]]==_0x8057[157]){if(tag==_0x8057[78]){this[_0x8057[29]][_0x8057[317]]();} else {return ;} ;} ;var formatted=this[_0x8057[56]][_0x8057[272]](tag);if(formatted===false){return ;} ;var $formatted=$(formatted);this[_0x8057[29]][_0x8057[337]]($formatted);var $elements=$formatted[_0x8057[332]](this[_0x8057[63]][_0x8057[235]][_0x8057[241]](_0x8057[357])+_0x8057[358]);if((this[_0x8057[63]][_0x8057[282]]&&tag==_0x8057[77])||tag==_0x8057[79]||tag==_0x8057[78]){$elements[_0x8057[327]](_0x8057[359]);} ;$elements[_0x8057[336]]()[_0x8057[335]]();if(tag!=_0x8057[77]&&tag!=_0x8057[78]){$formatted[_0x8057[332]](_0x8057[119])[_0x8057[331]]();} ;$[_0x8057[20]](this[_0x8057[29]][_0x8057[263]],$[_0x8057[281]](this[_0x8057[62]][_0x8057[355]],this));$formatted[_0x8057[327]](this[_0x8057[56]][_0x8057[360]](2));if(!this[_0x8057[63]][_0x8057[282]]){this[_0x8057[29]][_0x8057[324]]($formatted);} ;this[_0x8057[261]][_0x8057[332]](_0x8057[356])[_0x8057[20]]($[_0x8057[281]](this[_0x8057[62]][_0x8057[355]],this));$formatted[_0x8057[332]](_0x8057[361])[_0x8057[331]]();if(this[_0x8057[29]][_0x8057[307]]){this[_0x8057[62]][_0x8057[333]]($formatted);} ;if(this[_0x8057[63]][_0x8057[282]]&&tag==_0x8057[77]){this[_0x8057[62]][_0x8057[329]]($formatted);} ;} ,formatTableWrapping:function ($formatted){if($formatted[_0x8057[352]](_0x8057[362])[_0x8057[21]]===0){return ;} ;if($formatted[_0x8057[352]](_0x8057[363])[_0x8057[21]]===0){$formatted[_0x8057[272]](_0x8057[364]);} ;if($formatted[_0x8057[352]](_0x8057[340])[_0x8057[21]]===0&&$formatted[_0x8057[352]](_0x8057[341])[_0x8057[21]]===0){$formatted[_0x8057[272]](_0x8057[365]);} ;} ,removeData:function (name,value){var blocks=this[_0x8057[56]][_0x8057[264]]();$(blocks)[_0x8057[346]](_0x8057[366]+name);this[_0x8057[35]][_0x8057[270]]();} ,setData:function (name,value){var blocks=this[_0x8057[56]][_0x8057[264]]();$(blocks)[_0x8057[276]](_0x8057[366]+name,value);this[_0x8057[35]][_0x8057[270]]();} ,toggleData:function (name,value){var blocks=this[_0x8057[56]][_0x8057[264]]();$[_0x8057[20]](blocks,function (){if($(this)[_0x8057[276]](_0x8057[366]+name)){$(this)[_0x8057[346]](_0x8057[366]+name);} else {$(this)[_0x8057[276]](_0x8057[366]+name,value);} ;} );} ,removeAttr:function (attr,value){var blocks=this[_0x8057[56]][_0x8057[264]]();$(blocks)[_0x8057[346]](attr);this[_0x8057[35]][_0x8057[270]]();} ,setAttr:function (attr,value){var blocks=this[_0x8057[56]][_0x8057[264]]();$(blocks)[_0x8057[276]](attr,value);this[_0x8057[35]][_0x8057[270]]();} ,toggleAttr:function (attr,value){var blocks=this[_0x8057[56]][_0x8057[264]]();$[_0x8057[20]](blocks,function (){if($(this)[_0x8057[276]](name)){$(this)[_0x8057[346]](name);} else {$(this)[_0x8057[276]](name,value);} ;} );} ,removeClass:function (className){var blocks=this[_0x8057[56]][_0x8057[264]]();$(blocks)[_0x8057[350]](className);this[_0x8057[62]][_0x8057[285]](blocks,_0x8057[301]);this[_0x8057[35]][_0x8057[270]]();} ,setClass:function (className){var blocks=this[_0x8057[56]][_0x8057[264]]();$(blocks)[_0x8057[347]](className);this[_0x8057[35]][_0x8057[270]]();} ,toggleClass:function (className){var blocks=this[_0x8057[56]][_0x8057[264]]();$(blocks)[_0x8057[348]](className);this[_0x8057[35]][_0x8057[270]]();} };} ,buffer:function (){return {set:function (type){if( typeof type==_0x8057[14]||type==_0x8057[367]){this[_0x8057[30]][_0x8057[368]]();} else {this[_0x8057[30]][_0x8057[369]]();} ;} ,setUndo:function (){this[_0x8057[56]][_0x8057[262]]();this[_0x8057[63]][_0x8057[30]][_0x8057[16]](this[_0x8057[261]][_0x8057[68]]());this[_0x8057[56]][_0x8057[269]]();} ,setRedo:function (){this[_0x8057[56]][_0x8057[262]]();this[_0x8057[63]][_0x8057[370]][_0x8057[16]](this[_0x8057[261]][_0x8057[68]]());this[_0x8057[56]][_0x8057[269]]();} ,getUndo:function (){this[_0x8057[261]][_0x8057[68]](this[_0x8057[63]][_0x8057[30]][_0x8057[371]]());} ,getRedo:function (){this[_0x8057[261]][_0x8057[68]](this[_0x8057[63]][_0x8057[370]][_0x8057[371]]());} ,add:function (){this[_0x8057[63]][_0x8057[30]][_0x8057[16]](this[_0x8057[261]][_0x8057[68]]());} ,undo:function (){if(this[_0x8057[63]][_0x8057[30]][_0x8057[21]]===0){return ;} ;this[_0x8057[30]][_0x8057[256]](_0x8057[372]);this[_0x8057[30]][_0x8057[373]]();this[_0x8057[56]][_0x8057[269]]();setTimeout($[_0x8057[281]](this[_0x8057[51]][_0x8057[244]],this),50);} ,redo:function (){if(this[_0x8057[63]][_0x8057[370]][_0x8057[21]]===0){return ;} ;this[_0x8057[30]][_0x8057[256]](_0x8057[367]);this[_0x8057[30]][_0x8057[374]]();this[_0x8057[56]][_0x8057[269]]();setTimeout($[_0x8057[281]](this[_0x8057[51]][_0x8057[244]],this),50);} };} ,build:function (){return {run:function (){this[_0x8057[31]][_0x8057[375]]();this[_0x8057[31]][_0x8057[376]]();this[_0x8057[31]][_0x8057[377]]();this[_0x8057[31]][_0x8057[378]]();this[_0x8057[31]][_0x8057[379]]();} ,isTextarea:function (){return (this[_0x8057[229]][0][_0x8057[311]]===_0x8057[380]);} ,createContainerBox:function (){this[_0x8057[381]]=$(_0x8057[382]);} ,createTextarea:function (){this[_0x8057[288]]=$(_0x8057[384])[_0x8057[276]](_0x8057[286],this[_0x8057[31]][_0x8057[383]]());} ,getTextareaName:function (){return (( typeof (name)==_0x8057[14]))?_0x8057[385]+this[_0x8057[230]]:this[_0x8057[229]][_0x8057[276]](_0x8057[386]);} ,loadContent:function (){var func=(this[_0x8057[31]][_0x8057[387]]())?_0x8057[388]:_0x8057[68];this[_0x8057[389]]=$[_0x8057[390]](this[_0x8057[229]][func]());} ,enableEditor:function (){this[_0x8057[261]][_0x8057[276]]({"\x63\x6F\x6E\x74\x65\x6E\x74\x65\x64\x69\x74\x61\x62\x6C\x65":true,"\x64\x69\x72":this[_0x8057[63]][_0x8057[391]]});} ,loadEditor:function (){var func=(this[_0x8057[31]][_0x8057[387]]())?_0x8057[392]:_0x8057[393];this[_0x8057[31]][func]();} ,fromTextarea:function (){this[_0x8057[261]]=$(_0x8057[394]);this[_0x8057[288]]=this[_0x8057[229]];this[_0x8057[381]][_0x8057[395]](this.$element)[_0x8057[327]](this.$editor)[_0x8057[327]](this.$element);this[_0x8057[261]][_0x8057[347]](_0x8057[396]);this[_0x8057[229]][_0x8057[397]]();} ,fromElement:function (){this[_0x8057[261]]=this[_0x8057[229]];this[_0x8057[31]][_0x8057[398]]();this[_0x8057[381]][_0x8057[395]](this.$editor)[_0x8057[327]](this.$editor)[_0x8057[327]](this.$textarea);this[_0x8057[261]][_0x8057[347]](_0x8057[396]);this[_0x8057[288]][_0x8057[397]]();} ,setCodeAndCall:function (){this[_0x8057[35]][_0x8057[256]](this[_0x8057[389]]);this[_0x8057[31]][_0x8057[399]]();this[_0x8057[31]][_0x8057[400]]();if(this[_0x8057[63]][_0x8057[401]]){return ;} ;setTimeout($[_0x8057[281]](this[_0x8057[35]][_0x8057[402]],this),200);} ,callEditor:function (){this[_0x8057[31]][_0x8057[403]]();this[_0x8057[31]][_0x8057[404]]();this[_0x8057[31]][_0x8057[405]]();if(this[_0x8057[63]][_0x8057[60]]){this[_0x8057[63]][_0x8057[60]]=this[_0x8057[60]][_0x8057[22]]();this[_0x8057[60]][_0x8057[31]]();} ;this[_0x8057[50]][_0x8057[406]]();this[_0x8057[31]][_0x8057[407]]();setTimeout($[_0x8057[281]](this[_0x8057[51]][_0x8057[244]],this),4);this[_0x8057[36]][_0x8057[248]](_0x8057[22]);} ,setOptions:function (){$(this.$textarea)[_0x8057[276]](_0x8057[408],this[_0x8057[63]][_0x8057[391]]);if(this[_0x8057[63]][_0x8057[282]]){this[_0x8057[261]][_0x8057[347]](_0x8057[409]);} ;if(this[_0x8057[63]][_0x8057[410]]){this[_0x8057[261]][_0x8057[276]](_0x8057[410],this[_0x8057[63]][_0x8057[410]]);} ;if(this[_0x8057[63]][_0x8057[411]]){this[_0x8057[261]][_0x8057[274]](_0x8057[411],this[_0x8057[63]][_0x8057[411]]);} ;if(this[_0x8057[63]][_0x8057[412]]){this[_0x8057[261]][_0x8057[274]](_0x8057[412],this[_0x8057[63]][_0x8057[412]]);} ;} ,setEventDropUpload:function (e){e[_0x8057[413]]();if(!this[_0x8057[63]][_0x8057[414]]||!this[_0x8057[63]][_0x8057[415]]){return ;} ;var files=e[_0x8057[417]][_0x8057[416]];this[_0x8057[61]][_0x8057[418]](files[0],e);} ,setEventDrop:function (e){this[_0x8057[35]][_0x8057[270]]();setTimeout(this[_0x8057[34]][_0x8057[353]],1);this[_0x8057[36]][_0x8057[248]](_0x8057[419],e);} ,setEvents:function (){this[_0x8057[261]][_0x8057[425]](_0x8057[420],$[_0x8057[281]](function (e){e=e[_0x8057[421]]||e;if(window[_0x8057[422]]===undefined||!e[_0x8057[417]]){return true;} ;if(e[_0x8057[417]][_0x8057[416]][_0x8057[21]]===0){return this[_0x8057[31]][_0x8057[423]](e);} else {this[_0x8057[31]][_0x8057[424]](e);} ;setTimeout(this[_0x8057[34]][_0x8057[353]],1);this[_0x8057[36]][_0x8057[248]](_0x8057[419],e);} ,this));this[_0x8057[261]][_0x8057[425]](_0x8057[426],$[_0x8057[281]](function (e){var event=this[_0x8057[36]][_0x8057[427]]();var type=(event==_0x8057[428]||event==_0x8057[429])?false:_0x8057[428];this[_0x8057[36]][_0x8057[430]](type);this[_0x8057[62]][_0x8057[431]]();this[_0x8057[36]][_0x8057[248]](_0x8057[428],e);} ,this));this[_0x8057[261]][_0x8057[425]](_0x8057[432],$[_0x8057[281]](this[_0x8057[53]][_0x8057[22]],this));this[_0x8057[261]][_0x8057[425]](_0x8057[433],$[_0x8057[281]](this[_0x8057[44]][_0x8057[22]],this));this[_0x8057[261]][_0x8057[425]](_0x8057[434],$[_0x8057[281]](this[_0x8057[45]][_0x8057[22]],this));if($[_0x8057[15]](this[_0x8057[63]][_0x8057[435]])){this[_0x8057[288]][_0x8057[425]](_0x8057[436],$[_0x8057[281]](this[_0x8057[63]][_0x8057[435]],this));} ;if($[_0x8057[15]](this[_0x8057[63]][_0x8057[437]])){this[_0x8057[288]][_0x8057[425]](_0x8057[438],$[_0x8057[281]](this[_0x8057[63]][_0x8057[437]],this));} ;if($[_0x8057[15]](this[_0x8057[63]][_0x8057[439]])){this[_0x8057[261]][_0x8057[425]](_0x8057[440],$[_0x8057[281]](this[_0x8057[63]][_0x8057[439]],this));} ;var clickedElement;$(document)[_0x8057[425]](_0x8057[441],function (e){clickedElement=e[_0x8057[442]];} );this[_0x8057[261]][_0x8057[425]](_0x8057[443],$[_0x8057[281]](function (e){if(this[_0x8057[231]]){return ;} ;if(!this[_0x8057[31]][_0x8057[444]](clickedElement)){return ;} ;this[_0x8057[62]][_0x8057[431]]();if($[_0x8057[15]](this[_0x8057[63]][_0x8057[445]])){this[_0x8057[36]][_0x8057[248]](_0x8057[446],e);} ;} ,this));} ,isBlured:function (clickedElement){var $el=$(clickedElement);return (!$el[_0x8057[448]](_0x8057[447])&&!$el[_0x8057[450]](_0x8057[449])&&$el[_0x8057[452]](_0x8057[451])[_0x8057[21]]===0);} ,setHelpers:function (){this[_0x8057[28]][_0x8057[453]]();this[_0x8057[54]][_0x8057[453]]();if(this[_0x8057[63]][_0x8057[39]]){setTimeout(this[_0x8057[39]][_0x8057[454]],100);} ;if(this[_0x8057[63]][_0x8057[455]]){setTimeout(this[_0x8057[39]][_0x8057[456]],100);} ;} ,plugins:function (){if(!this[_0x8057[63]][_0x8057[407]]){return ;} ;if(!RedactorPlugins){return ;} ;$[_0x8057[20]](this[_0x8057[63]][_0x8057[407]],$[_0x8057[281]](function (i,s){if( typeof RedactorPlugins[s]===_0x8057[14]){return ;} ;if($[_0x8057[306]](s,$[_0x8057[23]][_0x8057[26]])!==-1){$[_0x8057[19]](_0x8057[457]+s+_0x8057[458]);return ;} ;if(!$[_0x8057[15]](RedactorPlugins[s])){return ;} ;this[s]=RedactorPlugins[s]();var methods=this[_0x8057[254]](this[s]);var len=methods[_0x8057[21]];for(var z=0;z<len;z++){this[s][methods[z]]=this[s][methods[z]][_0x8057[1]](this);} ;if($[_0x8057[15]](this[s][_0x8057[22]])){this[s][_0x8057[22]]();} ;} ,this));} ,disableMozillaEditing:function (){if(!this[_0x8057[62]][_0x8057[260]](_0x8057[459])){return ;} ;try{document[_0x8057[322]](_0x8057[460],false,false);document[_0x8057[322]](_0x8057[461],false,false);} catch(e){} ;} };} ,button:function (){return {build:function (btnName,btnObject){var $button=$(_0x8057[462]+btnName+_0x8057[463]+btnName+_0x8057[464])[_0x8057[276]](_0x8057[410],_0x8057[11]);if(btnObject[_0x8057[465]]||btnObject[_0x8057[466]]||btnObject[_0x8057[37]]){this[_0x8057[32]][_0x8057[467]]($button,btnName,btnObject);} ;if(btnObject[_0x8057[37]]){var $dropdown=$(_0x8057[468]+btnName+_0x8057[469]);$button[_0x8057[9]](_0x8057[37],$dropdown);this[_0x8057[37]][_0x8057[31]](btnName,$dropdown,btnObject[_0x8057[37]]);} ;if(this[_0x8057[62]][_0x8057[470]]()){this[_0x8057[32]][_0x8057[472]]($button,btnName,btnObject[_0x8057[471]]);} ;return $button;} ,setEvent:function ($button,btnName,btnObject){$button[_0x8057[425]](_0x8057[473],$[_0x8057[281]](function (e){if($button[_0x8057[448]](_0x8057[474])){return false;} ;var type=_0x8057[465];var callback=btnObject[_0x8057[465]];if(btnObject[_0x8057[466]]){type=_0x8057[466];callback=btnObject[_0x8057[466]];} else {if(btnObject[_0x8057[37]]){type=_0x8057[37];callback=false;} ;} ;this[_0x8057[32]][_0x8057[475]](e,btnName,type,callback);} ,this));} ,createTooltip:function ($button,name,title){var $tooltip=$(_0x8057[477])[_0x8057[347]](_0x8057[476]+name)[_0x8057[397]]()[_0x8057[68]](title);$tooltip[_0x8057[478]](_0x8057[87]);$button[_0x8057[425]](_0x8057[479],function (){if($(this)[_0x8057[448]](_0x8057[474])){return ;} ;var pos=$button[_0x8057[480]]();$tooltip[_0x8057[481]]();$tooltip[_0x8057[274]]({top:(pos[_0x8057[482]]+$button[_0x8057[483]]())+_0x8057[484],left:(pos[_0x8057[485]]+$button[_0x8057[486]]()/2-$tooltip[_0x8057[486]]()/2)+_0x8057[484]});} );$button[_0x8057[425]](_0x8057[487],function (){$tooltip[_0x8057[397]]();} );} ,onClick:function (e,btnName,type,callback){this[_0x8057[32]][_0x8057[488]]=this[_0x8057[33]][_0x8057[489]]();e[_0x8057[413]]();if(this[_0x8057[62]][_0x8057[260]](_0x8057[259])){e[_0x8057[490]]=false;} ;if(type==_0x8057[466]){this[_0x8057[42]][_0x8057[304]](callback);} else {if(type==_0x8057[37]){this[_0x8057[37]][_0x8057[481]](e,btnName);} else {this[_0x8057[32]][_0x8057[491]](e,callback,btnName);} ;} ;} ,onClickCallback:function (e,callback,btnName){var func;if($[_0x8057[15]](callback)){callback[_0x8057[6]](this,btnName);} else {if(callback[_0x8057[10]](/\./)!=_0x8057[11]){func=callback[_0x8057[13]](_0x8057[12]);if( typeof this[func[0]]==_0x8057[14]){return ;} ;this[func[0]][func[1]](btnName);} else {this[callback](btnName);} ;} ;this[_0x8057[51]][_0x8057[492]](e,btnName);} ,get:function (key){return this[_0x8057[494]][_0x8057[332]](_0x8057[493]+key);} ,setActive:function (key){this[_0x8057[32]][_0x8057[292]](key)[_0x8057[347]](_0x8057[495]);} ,setInactive:function (key){this[_0x8057[32]][_0x8057[292]](key)[_0x8057[350]](_0x8057[495]);} ,setInactiveAll:function (key){if( typeof key==_0x8057[14]){this[_0x8057[494]][_0x8057[332]](_0x8057[496])[_0x8057[350]](_0x8057[495]);} else {this[_0x8057[494]][_0x8057[332]](_0x8057[496])[_0x8057[498]](_0x8057[497]+key)[_0x8057[350]](_0x8057[495]);} ;} ,setActiveInVisual:function (){this[_0x8057[494]][_0x8057[332]](_0x8057[496])[_0x8057[498]](_0x8057[499])[_0x8057[350]](_0x8057[474]);} ,setInactiveInCode:function (){this[_0x8057[494]][_0x8057[332]](_0x8057[496])[_0x8057[498]](_0x8057[499])[_0x8057[347]](_0x8057[474]);} ,changeIcon:function (key,classname){this[_0x8057[32]][_0x8057[292]](key)[_0x8057[347]](_0x8057[500]+classname);} ,removeIcon:function (key,classname){this[_0x8057[32]][_0x8057[292]](key)[_0x8057[350]](_0x8057[500]+classname);} ,setAwesome:function (key,name){var $button=this[_0x8057[32]][_0x8057[292]](key);$button[_0x8057[350]](_0x8057[502])[_0x8057[347]](_0x8057[501]);$button[_0x8057[68]](_0x8057[503]+name+_0x8057[504]);} ,addCallback:function ($btn,callback){var type=(callback==_0x8057[37])?_0x8057[37]:_0x8057[465];var key=$btn[_0x8057[276]](_0x8057[505]);$btn[_0x8057[425]](_0x8057[473],$[_0x8057[281]](function (e){if($btn[_0x8057[448]](_0x8057[474])){return false;} ;this[_0x8057[32]][_0x8057[475]](e,key,type,callback);} ,this));} ,addDropdown:function ($btn,dropdown){var key=$btn[_0x8057[276]](_0x8057[505]);this[_0x8057[32]][_0x8057[506]]($btn,_0x8057[37]);var $dropdown=$(_0x8057[468]+key+_0x8057[469]);$btn[_0x8057[9]](_0x8057[37],$dropdown);if(dropdown){this[_0x8057[37]][_0x8057[31]](key,$dropdown,dropdown);} ;return $dropdown;} ,add:function (key,title){if(!this[_0x8057[63]][_0x8057[60]]){return ;} ;var btn=this[_0x8057[32]][_0x8057[31]](key,{title:title});btn[_0x8057[347]](_0x8057[502]);this[_0x8057[494]][_0x8057[327]]($(_0x8057[507])[_0x8057[327]](btn));return btn;} ,addFirst:function (key,title){if(!this[_0x8057[63]][_0x8057[60]]){return ;} ;var btn=this[_0x8057[32]][_0x8057[31]](key,{title:title});this[_0x8057[494]][_0x8057[328]]($(_0x8057[507])[_0x8057[327]](btn));return btn;} ,addAfter:function (afterkey,key,title){if(!this[_0x8057[63]][_0x8057[60]]){return ;} ;var btn=this[_0x8057[32]][_0x8057[31]](key,{title:title});var $btn=this[_0x8057[32]][_0x8057[292]](afterkey);if($btn[_0x8057[21]]!==0){$btn[_0x8057[509]]()[_0x8057[508]]($(_0x8057[507])[_0x8057[327]](btn));} else {this[_0x8057[494]][_0x8057[327]]($(_0x8057[507])[_0x8057[327]](btn));} ;return btn;} ,addBefore:function (beforekey,key,title){if(!this[_0x8057[63]][_0x8057[60]]){return ;} ;var btn=this[_0x8057[32]][_0x8057[31]](key,{title:title});var $btn=this[_0x8057[32]][_0x8057[292]](beforekey);if($btn[_0x8057[21]]!==0){$btn[_0x8057[509]]()[_0x8057[510]]($(_0x8057[507])[_0x8057[327]](btn));} else {this[_0x8057[494]][_0x8057[327]]($(_0x8057[507])[_0x8057[327]](btn));} ;return btn;} ,remove:function (key){this[_0x8057[32]][_0x8057[292]](key)[_0x8057[331]]();} };} ,caret:function (){return {setStart:function (node){if(!this[_0x8057[62]][_0x8057[511]](node)){var space=this[_0x8057[62]][_0x8057[512]]();$(node)[_0x8057[328]](space);this[_0x8057[33]][_0x8057[456]](space);} else {this[_0x8057[33]][_0x8057[256]](node,0,node,0);} ;} ,setEnd:function (node){this[_0x8057[33]][_0x8057[256]](node,1,node,1);} ,set:function (orgn,orgo,focn,foco){orgn=orgn[0]||orgn;focn=focn[0]||focn;if(this[_0x8057[62]][_0x8057[513]](orgn[_0x8057[311]])&&orgn[_0x8057[514]]===_0x8057[255]){orgn[_0x8057[514]]=this[_0x8057[63]][_0x8057[515]];} ;if(orgn[_0x8057[311]]==_0x8057[516]&&this[_0x8057[63]][_0x8057[282]]===false){var parent=$(this[_0x8057[63]][_0x8057[517]])[0];$(orgn)[_0x8057[284]](parent);orgn=parent;focn=orgn;} ;this[_0x8057[56]][_0x8057[292]]();try{this[_0x8057[313]][_0x8057[454]](orgn,orgo);this[_0x8057[313]][_0x8057[456]](focn,foco);} catch(e){} ;this[_0x8057[56]][_0x8057[518]]();} ,setAfter:function (node){try{var tag=$(node)[0][_0x8057[311]];if(tag!=_0x8057[516]&&!this[_0x8057[62]][_0x8057[511]](node)){var space=this[_0x8057[62]][_0x8057[512]]();$(node)[_0x8057[508]](space);this[_0x8057[33]][_0x8057[456]](space);} else {if(tag!=_0x8057[516]&&this[_0x8057[62]][_0x8057[260]](_0x8057[259])){this[_0x8057[33]][_0x8057[454]]($(node)[_0x8057[519]]());} else {this[_0x8057[33]][_0x8057[520]](node,_0x8057[508]);} ;} ;} catch(e){var space=this[_0x8057[62]][_0x8057[512]]();$(node)[_0x8057[508]](space);this[_0x8057[33]][_0x8057[456]](space);} ;} ,setBefore:function (node){if(this[_0x8057[62]][_0x8057[511]](node)){this[_0x8057[33]][_0x8057[456]]($(node)[_0x8057[521]]());} else {this[_0x8057[33]][_0x8057[520]](node,_0x8057[510]);} ;} ,setAfterOrBefore:function (node,type){if(!this[_0x8057[62]][_0x8057[260]](_0x8057[259])){this[_0x8057[261]][_0x8057[39]]();} ;node=node[0]||node;this[_0x8057[56]][_0x8057[292]]();if(type==_0x8057[508]){try{this[_0x8057[313]][_0x8057[522]](node);this[_0x8057[313]][_0x8057[523]](node);} catch(e){} ;} else {try{this[_0x8057[313]][_0x8057[524]](node);this[_0x8057[313]][_0x8057[525]](node);} catch(e){} ;} ;this[_0x8057[313]][_0x8057[526]](false);this[_0x8057[56]][_0x8057[518]]();} ,getOffsetOfElement:function (node){node=node[0]||node;this[_0x8057[56]][_0x8057[292]]();var cloned=this[_0x8057[313]][_0x8057[527]]();cloned[_0x8057[528]](node);cloned[_0x8057[456]](this[_0x8057[313]][_0x8057[529]],this[_0x8057[313]][_0x8057[530]]);return $[_0x8057[390]](cloned.toString())[_0x8057[21]];} ,getOffset:function (){var offset=0;var sel=window[_0x8057[531]]();if(sel[_0x8057[532]]>0){var range=window[_0x8057[531]]()[_0x8057[533]](0);var caretRange=range[_0x8057[527]]();caretRange[_0x8057[528]](this[_0x8057[261]][0]);caretRange[_0x8057[456]](range[_0x8057[529]],range[_0x8057[530]]);offset=caretRange.toString()[_0x8057[21]];} ;return offset;} ,setOffset:function (start,end){if( typeof end==_0x8057[14]){end=start;} ;if(!this[_0x8057[39]][_0x8057[534]]()){this[_0x8057[39]][_0x8057[454]]();} ;var sel=this[_0x8057[56]][_0x8057[292]]();var node,offset=0;var walker=document[_0x8057[535]](this[_0x8057[261]][0],NodeFilter.SHOW_TEXT,null,null);while(node=walker[_0x8057[537]]()){offset+=node[_0x8057[536]][_0x8057[21]];if(offset>start){this[_0x8057[313]][_0x8057[454]](node,node[_0x8057[536]][_0x8057[21]]+start-offset);start=Infinity;} ;if(offset>=end){this[_0x8057[313]][_0x8057[456]](node,node[_0x8057[536]][_0x8057[21]]+end-offset);break ;} ;} ;this[_0x8057[313]][_0x8057[526]](false);this[_0x8057[56]][_0x8057[518]]();} ,setToPoint:function (start,end){this[_0x8057[33]][_0x8057[538]](start,end);} ,getCoords:function (){return this[_0x8057[33]][_0x8057[489]]();} };} ,clean:function (){return {onSet:function (html){html=this[_0x8057[34]][_0x8057[539]](html);html=html[_0x8057[541]](/<script(.*?[^>]?)>([\w\W]*?)<\/script>/gi,_0x8057[540]);html=html[_0x8057[541]](/\$/g,_0x8057[542]);html=html[_0x8057[541]](/”/g,_0x8057[543]);html=html[_0x8057[541]](/‘/g,_0x8057[544]);html=html[_0x8057[541]](/’/g,_0x8057[544]);if(this[_0x8057[63]][_0x8057[545]]){html=this[_0x8057[34]][_0x8057[545]](html);} ;if(this[_0x8057[63]][_0x8057[282]]){html=this[_0x8057[34]][_0x8057[546]](html);} ;html=this[_0x8057[34]][_0x8057[547]](html);var $div=$(_0x8057[548]);$div[_0x8057[68]](html);var fonts=$div[_0x8057[332]](_0x8057[549]);if(fonts[_0x8057[21]]!==0){fonts[_0x8057[284]](function (){var $el=$(this);var $span=$(_0x8057[477])[_0x8057[276]](_0x8057[90],$el[_0x8057[276]](_0x8057[90]));return $span[_0x8057[327]]($el[_0x8057[336]]());} );html=$div[_0x8057[68]]();} ;$div[_0x8057[331]]();html=html[_0x8057[541]](/<font(.*?[^<])>/gi,_0x8057[255]);html=html[_0x8057[541]](/<\/font>/gi,_0x8057[255]);html=this[_0x8057[59]][_0x8057[244]](html);if(this[_0x8057[63]][_0x8057[52]]){html=this[_0x8057[52]][_0x8057[244]](html);} ;html=this[_0x8057[34]][_0x8057[550]](html);html=this[_0x8057[34]][_0x8057[551]](html);return html;} ,onSync:function (html){html=html[_0x8057[541]](/[\u200B-\u200D\uFEFF]/g,_0x8057[255]);html=html[_0x8057[541]](/&#x200b;/gi,_0x8057[255]);if(this[_0x8057[63]][_0x8057[552]]){html=html[_0x8057[541]](/&nbsp;/gi,_0x8057[553]);} ;if(html[_0x8057[10]](/^<p>(||\s||<br\s?\/?>||&nbsp;)<\/p>$/i)!=-1){return _0x8057[255];} ;html=html[_0x8057[541]](/<pre class="redactor-script-tag" style="display: none;"(.*?[^>]?)>([\w\W]*?)<\/pre>/gi,_0x8057[554]);html=this[_0x8057[34]][_0x8057[555]](html);var chars={"\u2122":_0x8057[556],"\xA9":_0x8057[557],"\u2026":_0x8057[558],"\u2014":_0x8057[559],"\u2010":_0x8057[560]};$[_0x8057[20]](chars,function (i,s){html=html[_0x8057[541]]( new RegExp(i,_0x8057[561]),s);} );html=html[_0x8057[541]]( new RegExp(_0x8057[562],_0x8057[563]),_0x8057[564]);html=html[_0x8057[541]]( new RegExp(_0x8057[565],_0x8057[563]),_0x8057[564]);html=html[_0x8057[541]]( new RegExp(_0x8057[566],_0x8057[563]),_0x8057[567]);html=html[_0x8057[541]]( new RegExp(_0x8057[568],_0x8057[563]),_0x8057[569]);html=html[_0x8057[541]]( new RegExp(_0x8057[570],_0x8057[563]),_0x8057[571]);html=html[_0x8057[541]]( new RegExp(_0x8057[572],_0x8057[563]),_0x8057[573]);html=html[_0x8057[541]]( new RegExp(_0x8057[574],_0x8057[563]),_0x8057[575]);html=html[_0x8057[541]]( new RegExp(_0x8057[576],_0x8057[563]),_0x8057[575]);html=html[_0x8057[541]]( new RegExp(_0x8057[577],_0x8057[563]),_0x8057[578]);html=html[_0x8057[541]](/ data-save-url="(.*?[^>])"/gi,_0x8057[255]);html=html[_0x8057[541]](/<span(.*?)id="redactor-image-box"(.*?[^>])>([\w\W]*?)<img(.*?)><\/span>/gi,_0x8057[579]);html=html[_0x8057[541]](/<span(.*?)id="redactor-image-resizer"(.*?[^>])>(.*?)<\/span>/gi,_0x8057[255]);html=html[_0x8057[541]](/<span(.*?)id="redactor-image-editter"(.*?[^>])>(.*?)<\/span>/gi,_0x8057[255]);html=html[_0x8057[541]](/<font(.*?[^<])>/gi,_0x8057[255]);html=html[_0x8057[541]](/<\/font>/gi,_0x8057[255]);html=this[_0x8057[59]][_0x8057[244]](html);if(this[_0x8057[63]][_0x8057[580]]){html=html[_0x8057[541]](/<a(.*?)rel="nofollow"(.*?[^>])>/gi,_0x8057[581]);html=html[_0x8057[541]](/<a(.*?[^>])>/gi,_0x8057[582]);} ;html=html[_0x8057[541]](/\sdata-redactor-(tag|class|style)="(.*?[^>])"/gi,_0x8057[255]);html=html[_0x8057[541]]( new RegExp(_0x8057[568],_0x8057[563]),_0x8057[569]);html=html[_0x8057[541]]( new RegExp(_0x8057[583],_0x8057[563]),_0x8057[584]);return html;} ,onPaste:function (html,setMode){html=$[_0x8057[390]](html);html=html[_0x8057[541]](/\$/g,_0x8057[542]);html=html[_0x8057[541]](/‘/g,_0x8057[544]);html=html[_0x8057[541]](/’/g,_0x8057[544]);html=html[_0x8057[541]](/<span class="s1">/gi,_0x8057[477]);html=html[_0x8057[541]](/<span class="Apple-converted-space">&nbsp;<\/span>/gi,_0x8057[553]);html=html[_0x8057[541]](/<span class="Apple-tab-span"[^>]*>\t<\/span>/gi,_0x8057[585]);html=html[_0x8057[541]](/<span[^>]*>(\s|&nbsp;)<\/span>/gi,_0x8057[553]);if(this[_0x8057[63]][_0x8057[586]]){return this[_0x8057[34]][_0x8057[587]](html);} ;if(!this[_0x8057[62]][_0x8057[588]]()&& typeof setMode==_0x8057[14]){if(this[_0x8057[62]][_0x8057[590]]([_0x8057[148],_0x8057[589]])){return this[_0x8057[34]][_0x8057[587]](html,false);} ;if(this[_0x8057[62]][_0x8057[590]](_0x8057[155])){html=html[_0x8057[541]](/”/g,_0x8057[543]);html=html[_0x8057[541]](/“/g,_0x8057[543]);return this[_0x8057[34]][_0x8057[591]](html);} ;if(this[_0x8057[62]][_0x8057[590]]([_0x8057[146],_0x8057[135],_0x8057[136],_0x8057[137],_0x8057[138],_0x8057[139],_0x8057[140]])){html=this[_0x8057[34]][_0x8057[592]](html);if(!this[_0x8057[62]][_0x8057[260]](_0x8057[259])){var block=this[_0x8057[56]][_0x8057[323]]();if(block&&block[_0x8057[311]]==_0x8057[134]){html=html[_0x8057[541]](/<img(.*?)>/gi,_0x8057[593]);} ;} ;return html;} ;if(this[_0x8057[62]][_0x8057[590]]([_0x8057[145]])){html=this[_0x8057[34]][_0x8057[594]](html,_0x8057[340]);if(this[_0x8057[63]][_0x8057[282]]){html=this[_0x8057[34]][_0x8057[546]](html);} ;html=this[_0x8057[34]][_0x8057[595]](html);return html;} ;if(this[_0x8057[62]][_0x8057[590]]([_0x8057[158]])){return this[_0x8057[34]][_0x8057[594]](html,_0x8057[128]);} ;} ;html=this[_0x8057[34]][_0x8057[596]](html,setMode);if(!this[_0x8057[34]][_0x8057[597]]){if(this[_0x8057[63]][_0x8057[282]]){html=this[_0x8057[34]][_0x8057[546]](html);} ;if(this[_0x8057[63]][_0x8057[545]]){html=this[_0x8057[34]][_0x8057[545]](html);} ;html=this[_0x8057[34]][_0x8057[547]](html);} ;html=this[_0x8057[34]][_0x8057[598]](html);html=this[_0x8057[34]][_0x8057[599]](html);html=this[_0x8057[34]][_0x8057[594]](html,_0x8057[600]);if(!this[_0x8057[34]][_0x8057[597]]&&this[_0x8057[63]][_0x8057[52]]){html=this[_0x8057[52]][_0x8057[244]](html);} ;html=this[_0x8057[34]][_0x8057[601]](html);html=this[_0x8057[34]][_0x8057[602]](html);html=this[_0x8057[34]][_0x8057[603]](html);html=this[_0x8057[34]][_0x8057[551]](html);return html;} ,onPasteWord:function (html){html=html[_0x8057[541]](/<!--[\s\S]*?-->/gi,_0x8057[255]);html=html[_0x8057[541]](/<style[^>]*>[\s\S]*?<\/style>/gi,_0x8057[255]);if(/(class=\"?Mso|style=\"[^\"]*\bmso\-|w:WordDocument)/[_0x8057[604]](html)){html=this[_0x8057[34]][_0x8057[605]](html);html=html[_0x8057[541]](/<img(.*?)v:shapes=(.*?)>/gi,_0x8057[255]);html=html[_0x8057[541]](/src="file\:\/\/(.*?)"/,_0x8057[606]);html=html[_0x8057[541]](/<p(.*?)class="MsoListParagraphCxSpFirst"([\w\W]*?)<\/p>/gi,_0x8057[607]);html=html[_0x8057[541]](/<p(.*?)class="MsoListParagraphCxSpMiddle"([\w\W]*?)<\/p>/gi,_0x8057[608]);html=html[_0x8057[541]](/<p(.*?)class="MsoListParagraphCxSpLast"([\w\W]*?)<\/p>/gi,_0x8057[609]);html=html[_0x8057[541]](/<p(.*?)class="MsoListParagraph"([\w\W]*?)<\/p>/gi,_0x8057[610]);html=html[_0x8057[541]](/·/g,_0x8057[255]);html=html[_0x8057[541]](/<p class="Mso(.*?)"/gi,_0x8057[611]);html=html[_0x8057[541]](/ class=\"(mso[^\"]*)\"/gi,_0x8057[255]);html=html[_0x8057[541]](/ class=(mso\w+)/gi,_0x8057[255]);html=html[_0x8057[541]](/<o:p(.*?)>([\w\W]*?)<\/o:p>/gi,_0x8057[612]);html=html[_0x8057[541]](/\n/g,_0x8057[553]);html=html[_0x8057[541]](/<p>\n?<li>/gi,_0x8057[507]);} ;if(this[_0x8057[63]][_0x8057[552]]){html=html[_0x8057[541]](/(\s|&nbsp;)+/g,_0x8057[553]);} ;return html;} ,onPasteExtra:function (html){html=html[_0x8057[541]](/<b\sid="internal-source-marker(.*?)">([\w\W]*?)<\/b>/gi,_0x8057[612]);html=html[_0x8057[541]](/<b(.*?)id="docs-internal-guid(.*?)">([\w\W]*?)<\/b>/gi,_0x8057[613]);html=html[_0x8057[541]](/<span[^>]*(font-style: italic; font-weight: bold|font-weight: bold; font-style: italic)[^>]*>/gi,_0x8057[614]);html=html[_0x8057[541]](/<span[^>]*font-style: italic[^>]*>/gi,_0x8057[615]);html=html[_0x8057[541]](/<span[^>]*font-weight: bold[^>]*>/gi,_0x8057[616]);html=html[_0x8057[541]](/<span[^>]*text-decoration: underline[^>]*>/gi,_0x8057[617]);html=html[_0x8057[541]](/<img>/gi,_0x8057[255]);html=html[_0x8057[541]](/\n{3,}/gi,_0x8057[618]);html=html[_0x8057[541]](/<font(.*?)>([\w\W]*?)<\/font>/gi,_0x8057[612]);html=html[_0x8057[541]](/<p><p>/gi,_0x8057[619]);html=html[_0x8057[541]](/<\/p><\/p>/gi,_0x8057[620]);html=html[_0x8057[541]](/<li>(\s*|\t*|\n*)<p>/gi,_0x8057[507]);html=html[_0x8057[541]](/<\/p>(\s*|\t*|\n*)<\/li>/gi,_0x8057[564]);html=html[_0x8057[541]](/<\/p>\s<p/gi,_0x8057[621]);html=html[_0x8057[541]](/<img src="webkit-fake-url\:\/\/(.*?)"(.*?)>/gi,_0x8057[255]);html=html[_0x8057[541]](/<p>•([\w\W]*?)<\/p>/gi,_0x8057[622]);if(this[_0x8057[62]][_0x8057[260]](_0x8057[459])){html=html[_0x8057[541]](/<br\s?\/?>$/gi,_0x8057[255]);} ;return html;} ,onPasteTidy:function (html,type){var tags=[_0x8057[101],_0x8057[118],_0x8057[79],_0x8057[78],_0x8057[124],_0x8057[97],_0x8057[95],_0x8057[35],_0x8057[131],_0x8057[132],_0x8057[623],_0x8057[125],_0x8057[133],_0x8057[130],_0x8057[624],_0x8057[122],_0x8057[121],_0x8057[120],_0x8057[123],_0x8057[99],_0x8057[93],_0x8057[127],_0x8057[126],_0x8057[128],_0x8057[342],_0x8057[343],_0x8057[344],_0x8057[77],_0x8057[625],_0x8057[626],_0x8057[627],_0x8057[628],_0x8057[629],_0x8057[630],_0x8057[631],_0x8057[119],_0x8057[362],_0x8057[340],_0x8057[341],_0x8057[363],_0x8057[632],_0x8057[633],_0x8057[634],_0x8057[80],_0x8057[81],_0x8057[82],_0x8057[83],_0x8057[84],_0x8057[85]];var tagsEmpty=false;var attrAllowed=[[_0x8057[118],_0x8057[635]],[_0x8057[119],[_0x8057[636],_0x8057[637]]],[_0x8057[101],[_0x8057[301],_0x8057[505],_0x8057[638]]],[_0x8057[628],_0x8057[635]],[_0x8057[626],_0x8057[635]],[_0x8057[627],_0x8057[635]],[_0x8057[629],_0x8057[635]],[_0x8057[631],_0x8057[635]],[_0x8057[630],_0x8057[635]],[_0x8057[291],_0x8057[635]]];if(type==_0x8057[600]){tagsEmpty=[_0x8057[77],_0x8057[101],_0x8057[80],_0x8057[81],_0x8057[82],_0x8057[83],_0x8057[84],_0x8057[85]];attrAllowed=[[_0x8057[362],_0x8057[301]],[_0x8057[340],[_0x8057[639],_0x8057[640]]],[_0x8057[118],_0x8057[635]],[_0x8057[119],[_0x8057[636],_0x8057[637],_0x8057[641]]],[_0x8057[101],[_0x8057[301],_0x8057[505],_0x8057[638]]],[_0x8057[628],_0x8057[635]],[_0x8057[626],_0x8057[635]],[_0x8057[627],_0x8057[635]],[_0x8057[629],_0x8057[635]],[_0x8057[631],_0x8057[635]],[_0x8057[630],_0x8057[635]],[_0x8057[291],_0x8057[635]]];} else {if(type==_0x8057[340]){tags=[_0x8057[126],_0x8057[127],_0x8057[128],_0x8057[101],_0x8057[118],_0x8057[124],_0x8057[97],_0x8057[95],_0x8057[35],_0x8057[131],_0x8057[132],_0x8057[125],_0x8057[133],_0x8057[130],_0x8057[624],_0x8057[122],_0x8057[121],_0x8057[120],_0x8057[123],_0x8057[99],_0x8057[93],_0x8057[127],_0x8057[126],_0x8057[128],_0x8057[342],_0x8057[343],_0x8057[344],_0x8057[625],_0x8057[628],_0x8057[626],_0x8057[627],_0x8057[629],_0x8057[630],_0x8057[631],_0x8057[119],_0x8057[80],_0x8057[81],_0x8057[82],_0x8057[83],_0x8057[84],_0x8057[85]];} else {if(type==_0x8057[128]){tags=[_0x8057[126],_0x8057[127],_0x8057[128],_0x8057[101],_0x8057[118],_0x8057[124],_0x8057[97],_0x8057[95],_0x8057[35],_0x8057[131],_0x8057[132],_0x8057[125],_0x8057[133],_0x8057[130],_0x8057[624],_0x8057[122],_0x8057[121],_0x8057[120],_0x8057[123],_0x8057[99],_0x8057[93],_0x8057[625],_0x8057[628],_0x8057[626],_0x8057[627],_0x8057[629],_0x8057[630],_0x8057[631],_0x8057[119]];} ;} ;} ;var options={deniedTags:false,allowedTags:tags,removeComments:true,removePhp:true,removeAttr:false,allowedAttr:attrAllowed,removeEmpty:tagsEmpty};if(this[_0x8057[63]][_0x8057[642]]){options[_0x8057[642]]=this[_0x8057[63]][_0x8057[642]];} ;return this[_0x8057[59]][_0x8057[244]](html,options);} ,onPasteRemoveEmpty:function (html){html=html[_0x8057[541]](/<(p|h[1-6])>(|\s|\n|\t|<br\s?\/?>)<\/(p|h[1-6])>/gi,_0x8057[255]);if(!this[_0x8057[63]][_0x8057[282]]){html=html[_0x8057[541]](/<br>$/i,_0x8057[255]);} ;return html;} ,onPasteRemoveSpans:function (html){html=html[_0x8057[541]](/<span>(.*?)<\/span>/gi,_0x8057[578]);html=html[_0x8057[541]](/<span[^>]*>\s|&nbsp;<\/span>/gi,_0x8057[553]);return html;} ,onPasteIeFixLinks:function (html){if(!this[_0x8057[62]][_0x8057[260]](_0x8057[259])){return html;} ;var tmp=$[_0x8057[390]](html);if(tmp[_0x8057[10]](/^<a(.*?)>(.*?)<\/a>$/i)===0){html=html[_0x8057[541]](/^<a(.*?)>(.*?)<\/a>$/i,_0x8057[612]);} ;return html;} ,isSingleLine:function (html,setMode){this[_0x8057[34]][_0x8057[597]]=false;if(!this[_0x8057[62]][_0x8057[588]]()&& typeof setMode==_0x8057[14]){var blocks=this[_0x8057[63]][_0x8057[235]][_0x8057[241]](_0x8057[240])[_0x8057[541]](_0x8057[644],_0x8057[255])[_0x8057[541]](_0x8057[643],_0x8057[255]);var matchBlocks=html[_0x8057[647]]( new RegExp(_0x8057[645]+blocks+_0x8057[646],_0x8057[563]));var matchContainers=html[_0x8057[647]](/<\/(p|div)>/gi);if(!matchBlocks&&(matchContainers===null||(matchContainers&&matchContainers[_0x8057[21]]<=1))){var matchBR=html[_0x8057[647]](/<br\s?\/?>/gi);var matchIMG=html[_0x8057[647]](/<img(.*?[^>])>/gi);if(!matchBR&&!matchIMG){this[_0x8057[34]][_0x8057[597]]=true;html=html[_0x8057[541]](/<\/?(p|div)(.*?)>/gi,_0x8057[255]);} ;} ;} ;return html;} ,stripTags:function (input,allowed){allowed=(((allowed||_0x8057[255])+_0x8057[255])[_0x8057[325]]()[_0x8057[647]](/<[a-z][a-z0-9]*>/g)||[])[_0x8057[241]](_0x8057[255]);var tags=/<\/?([a-z][a-z0-9]*)\b[^>]*>/gi;return input[_0x8057[541]](tags,function ($0,$1){return allowed[_0x8057[648]](_0x8057[320]+$1[_0x8057[325]]()+_0x8057[321])>-1?$0:_0x8057[255];} );} ,savePreCode:function (html){var pre=html[_0x8057[647]](/<(pre|code)(.*?)>([\w\W]*?)<\/(pre|code)>/gi);if(pre!==null){$[_0x8057[20]](pre,$[_0x8057[281]](function (i,s){var arr=s[_0x8057[647]](/<(pre|code)(.*?)>([\w\W]*?)<\/(pre|code)>/i);arr[3]=arr[3][_0x8057[541]](/<br\s?\/?>/g,_0x8057[618]);arr[3]=arr[3][_0x8057[541]](/&nbsp;/g,_0x8057[553]);if(this[_0x8057[63]][_0x8057[649]]){arr[3]=arr[3][_0x8057[541]](/\t/g,Array(this[_0x8057[63]][_0x8057[649]]+1)[_0x8057[241]](_0x8057[553]));} ;arr[3]=this[_0x8057[34]][_0x8057[650]](arr[3]);arr[3]=arr[3][_0x8057[541]](/\$/g,_0x8057[542]);html=html[_0x8057[541]](s,_0x8057[320]+arr[1]+arr[2]+_0x8057[321]+arr[3]+_0x8057[651]+arr[1]+_0x8057[321]);} ,this));} ;return html;} ,getTextFromHtml:function (html){html=html[_0x8057[541]](/<br\s?\/?>|<\/H[1-6]>|<\/p>|<\/div>|<\/li>|<\/td>/gi,_0x8057[618]);var tmp=document[_0x8057[652]](_0x8057[271]);tmp[_0x8057[514]]=html;html=tmp[_0x8057[653]]||tmp[_0x8057[654]];return $[_0x8057[390]](html);} ,getPlainText:function (html,paragraphize){html=this[_0x8057[34]][_0x8057[655]](html);html=html[_0x8057[541]](/\n/g,_0x8057[359]);if(this[_0x8057[63]][_0x8057[52]]&& typeof paragraphize==_0x8057[14]){html=this[_0x8057[52]][_0x8057[244]](html);} ;return html;} ,getPreCode:function (html){html=html[_0x8057[541]](/<img(.*?) style="(.*?)"(.*?[^>])>/gi,_0x8057[573]);html=html[_0x8057[541]](/<img(.*?)>/gi,_0x8057[656]);html=this[_0x8057[34]][_0x8057[655]](html);if(this[_0x8057[63]][_0x8057[649]]){html=html[_0x8057[541]](/\t/g,Array(this[_0x8057[63]][_0x8057[649]]+1)[_0x8057[241]](_0x8057[553]));} ;html=this[_0x8057[34]][_0x8057[650]](html);return html;} ,getOnlyImages:function (html){html=html[_0x8057[541]](/<img(.*?)>/gi,_0x8057[657]);html=html[_0x8057[541]](/<([Ss]*?)>/gi,_0x8057[255]);html=html[_0x8057[541]](/\[img(.*?)\]/gi,_0x8057[658]);return html;} ,getOnlyLinksAndImages:function (html){html=html[_0x8057[541]](/<a(.*?)href="(.*?)"(.*?)>([\w\W]*?)<\/a>/gi,_0x8057[659]);html=html[_0x8057[541]](/<img(.*?)>/gi,_0x8057[657]);html=html[_0x8057[541]](/<(.*?)>/gi,_0x8057[255]);html=html[_0x8057[541]](/\[a href="(.*?)"\]([\w\W]*?)\[\/a\]/gi,_0x8057[660]);html=html[_0x8057[541]](/\[img(.*?)\]/gi,_0x8057[658]);return html;} ,encodeEntities:function (str){str=String(str)[_0x8057[541]](/&amp;/g,_0x8057[661])[_0x8057[541]](/&lt;/g,_0x8057[320])[_0x8057[541]](/&gt;/g,_0x8057[321])[_0x8057[541]](/&quot;/g,_0x8057[543]);return str[_0x8057[541]](/&/g,_0x8057[665])[_0x8057[541]](/</g,_0x8057[664])[_0x8057[541]](/>/g,_0x8057[663])[_0x8057[541]](/"/g,_0x8057[662]);} ,removeDirtyStyles:function (html){if(this[_0x8057[62]][_0x8057[260]](_0x8057[259])){return html;} ;var div=document[_0x8057[652]](_0x8057[271]);div[_0x8057[514]]=html;this[_0x8057[34]][_0x8057[666]]($(div));html=div[_0x8057[514]];$(div)[_0x8057[331]]();return html;} ,clearUnverified:function (){if(this[_0x8057[62]][_0x8057[260]](_0x8057[259])){return ;} ;this[_0x8057[34]][_0x8057[666]](this.$editor);var headers=this[_0x8057[261]][_0x8057[332]](_0x8057[667]);headers[_0x8057[332]](_0x8057[101])[_0x8057[346]](_0x8057[90]);headers[_0x8057[332]](this[_0x8057[63]][_0x8057[669]][_0x8057[241]](_0x8057[668]))[_0x8057[346]](_0x8057[90]);this[_0x8057[35]][_0x8057[270]]();} ,clearUnverifiedRemove:function ($editor){$editor[_0x8057[332]](this[_0x8057[63]][_0x8057[669]][_0x8057[241]](_0x8057[668]))[_0x8057[346]](_0x8057[90]);$editor[_0x8057[332]](_0x8057[101])[_0x8057[498]](_0x8057[670])[_0x8057[346]](_0x8057[90]);$editor[_0x8057[332]](_0x8057[671])[_0x8057[20]](function (i,s){var $s=$(s);$s[_0x8057[276]](_0x8057[90],$s[_0x8057[276]](_0x8057[505]));} );} ,setVerified:function (html){if(this[_0x8057[62]][_0x8057[260]](_0x8057[259])){return html;} ;html=html[_0x8057[541]]( new RegExp(_0x8057[672],_0x8057[563]),_0x8057[673]);html=html[_0x8057[541]]( new RegExp(_0x8057[674],_0x8057[563]),_0x8057[675]);var matches=html[_0x8057[647]]( new RegExp(_0x8057[676],_0x8057[563]));if(matches){var len=matches[_0x8057[21]];for(var i=0;i<len;i++){try{var newTag=matches[i][_0x8057[541]](/style="(.*?)"/i,_0x8057[677]);html=html[_0x8057[541]]( new RegExp(matches[i],_0x8057[563]),newTag);} catch(e){} ;} ;} ;return html;} ,convertInline:function (html){var $div=$(_0x8057[394])[_0x8057[68]](html);var tags=this[_0x8057[63]][_0x8057[678]];tags[_0x8057[16]](_0x8057[101]);$div[_0x8057[332]](tags[_0x8057[241]](_0x8057[357]))[_0x8057[20]](function (){var $el=$(this);var tag=this[_0x8057[311]][_0x8057[325]]();$el[_0x8057[276]](_0x8057[679],tag);if(tag==_0x8057[101]){if($el[_0x8057[276]](_0x8057[90])){$el[_0x8057[276]](_0x8057[680],$el[_0x8057[276]](_0x8057[90]));} else {if($el[_0x8057[276]](_0x8057[301])){$el[_0x8057[276]](_0x8057[681],$el[_0x8057[276]](_0x8057[301]));} ;} ;} ;} );html=$div[_0x8057[68]]();$div[_0x8057[331]]();return html;} ,normalizeLists:function (){this[_0x8057[261]][_0x8057[332]](_0x8057[128])[_0x8057[20]](function (i,s){var $next=$(s)[_0x8057[519]]();if($next[_0x8057[21]]!==0&&($next[0][_0x8057[311]]==_0x8057[156]||$next[0][_0x8057[311]]==_0x8057[157])){$(s)[_0x8057[327]]($next);} ;} );} ,removeSpaces:function (html){html=html[_0x8057[541]](/\n/g,_0x8057[255]);html=html[_0x8057[541]](/[\t]*/g,_0x8057[255]);html=html[_0x8057[541]](/\n\s*\n/g,_0x8057[618]);html=html[_0x8057[541]](/^[\s\n]*/g,_0x8057[553]);html=html[_0x8057[541]](/[\s\n]*$/g,_0x8057[553]);html=html[_0x8057[541]](/>\s{2,}</g,_0x8057[682]);html=html[_0x8057[541]](/\n\n/g,_0x8057[618]);html=html[_0x8057[541]](/[\u200B-\u200D\uFEFF]/g,_0x8057[255]);return html;} ,replaceDivs:function (html){if(this[_0x8057[63]][_0x8057[282]]){html=html[_0x8057[541]](/<div><br\s?\/?><\/div>/gi,_0x8057[359]);html=html[_0x8057[541]](/<div(.*?)>([\w\W]*?)<\/div>/gi,_0x8057[683]);} else {html=html[_0x8057[541]](/<div(.*?)>([\w\W]*?)<\/div>/gi,_0x8057[684]);} ;return html;} ,replaceDivsToBr:function (html){html=html[_0x8057[541]](/<div\s(.*?)>/gi,_0x8057[619]);html=html[_0x8057[541]](/<div><br\s?\/?><\/div>/gi,_0x8057[685]);html=html[_0x8057[541]](/<div>([\w\W]*?)<\/div>/gi,_0x8057[686]);return html;} ,replaceParagraphsToBr:function (html){html=html[_0x8057[541]](/<p\s(.*?)>/gi,_0x8057[619]);html=html[_0x8057[541]](/<p><br\s?\/?><\/p>/gi,_0x8057[359]);html=html[_0x8057[541]](/<p>([\w\W]*?)<\/p>/gi,_0x8057[686]);html=html[_0x8057[541]](/(<br\s?\/?>){1,}\n?<\/blockquote>/gi,_0x8057[687]);return html;} ,saveFormTags:function (html){return html[_0x8057[541]](/<form(.*?)>([\w\W]*?)<\/form>/gi,_0x8057[688]);} ,restoreFormTags:function (html){return html[_0x8057[541]](/<section(.*?) rel="redactor-form-tag"(.*?)>([\w\W]*?)<\/section>/gi,_0x8057[689]);} };} ,code:function (){return {set:function (html){html=$[_0x8057[390]](html.toString());html=this[_0x8057[34]][_0x8057[690]](html);this[_0x8057[261]][_0x8057[68]](html);this[_0x8057[35]][_0x8057[270]]();setTimeout($[_0x8057[281]](this[_0x8057[30]][_0x8057[691]],this),15);if(this[_0x8057[247]]===false){this[_0x8057[51]][_0x8057[244]]();} ;} ,get:function (){var code=this[_0x8057[288]][_0x8057[388]]();code=this[_0x8057[58]][_0x8057[292]](code);return code;} ,sync:function (){setTimeout($[_0x8057[281]](this[_0x8057[35]][_0x8057[692]],this),10);} ,startSync:function (){var html=this[_0x8057[261]][_0x8057[68]]();if(this[_0x8057[35]][_0x8057[693]]&&this[_0x8057[35]][_0x8057[693]]==html){return ;} ;this[_0x8057[35]][_0x8057[693]]=html;html=this[_0x8057[36]][_0x8057[248]](_0x8057[694],html);html=this[_0x8057[34]][_0x8057[695]](html);this[_0x8057[288]][_0x8057[388]](html);this[_0x8057[36]][_0x8057[248]](_0x8057[270],html);if(this[_0x8057[247]]===false){this[_0x8057[36]][_0x8057[248]](_0x8057[696],html);} ;this[_0x8057[247]]=false;this[_0x8057[28]][_0x8057[697]]();} ,toggle:function (){if(this[_0x8057[63]][_0x8057[401]]){this[_0x8057[35]][_0x8057[402]]();} else {this[_0x8057[35]][_0x8057[698]]();} ;} ,showCode:function (){this[_0x8057[35]][_0x8057[480]]=this[_0x8057[33]][_0x8057[489]]();var scroll=$(window)[_0x8057[699]]();var height=this[_0x8057[261]][_0x8057[483]]();this[_0x8057[261]][_0x8057[397]]();var html=this[_0x8057[288]][_0x8057[388]]();this[_0x8057[700]]=this[_0x8057[34]][_0x8057[701]](html);html=this[_0x8057[58]][_0x8057[292]](html);this[_0x8057[288]][_0x8057[388]](html)[_0x8057[702]](height)[_0x8057[481]]()[_0x8057[39]]();this[_0x8057[288]][_0x8057[425]](_0x8057[703],this[_0x8057[35]][_0x8057[704]]);$(window)[_0x8057[699]](scroll);if(this[_0x8057[288]][0][_0x8057[705]]){this[_0x8057[288]][0][_0x8057[705]](0,0);} ;this[_0x8057[288]][0][_0x8057[699]]=0;this[_0x8057[63]][_0x8057[401]]=false;this[_0x8057[32]][_0x8057[706]]();this[_0x8057[32]][_0x8057[707]](_0x8057[68]);this[_0x8057[36]][_0x8057[248]](_0x8057[291],html);} ,showVisual:function (){if(this[_0x8057[63]][_0x8057[401]]){return ;} ;var html=this[_0x8057[288]][_0x8057[397]]()[_0x8057[388]]();if(this[_0x8057[700]]!==this[_0x8057[34]][_0x8057[701]](html)){this[_0x8057[35]][_0x8057[256]](html);} ;this[_0x8057[261]][_0x8057[481]]();if(!this[_0x8057[62]][_0x8057[293]](html)){this[_0x8057[54]][_0x8057[331]]();} ;this[_0x8057[33]][_0x8057[538]](this[_0x8057[35]][_0x8057[480]]);this[_0x8057[288]][_0x8057[708]](_0x8057[703]);this[_0x8057[32]][_0x8057[709]]();this[_0x8057[32]][_0x8057[710]](_0x8057[68]);this[_0x8057[51]][_0x8057[244]]();this[_0x8057[63]][_0x8057[401]]=true;} ,textareaIndenting:function (e){if(e[_0x8057[711]]!==9){return true;} ;var $el=this[_0x8057[288]];var start=$el[_0x8057[292]](0)[_0x8057[712]];$el[_0x8057[388]]($el[_0x8057[388]]()[_0x8057[713]](0,start)+_0x8057[585]+$el[_0x8057[388]]()[_0x8057[713]]($el[_0x8057[292]](0)[_0x8057[714]]));$el[_0x8057[292]](0)[_0x8057[712]]=$el[_0x8057[292]](0)[_0x8057[714]]=start+1;return false;} };} ,core:function (){return {getObject:function (){return $[_0x8057[246]]({},this);} ,getEditor:function (){return this[_0x8057[261]];} ,getBox:function (){return this[_0x8057[381]];} ,getElement:function (){return this[_0x8057[229]];} ,getTextarea:function (){return this[_0x8057[288]];} ,getToolbar:function (){return (this[_0x8057[494]])?this[_0x8057[494]]:false;} ,addEvent:function (name){this[_0x8057[36]][_0x8057[715]]=name;} ,getEvent:function (){return this[_0x8057[36]][_0x8057[715]];} ,setCallback:function (type,e,data){var callback=this[_0x8057[63]][type+_0x8057[716]];if($[_0x8057[15]](callback)){return ( typeof data==_0x8057[14])?callback[_0x8057[6]](this,e):callback[_0x8057[6]](this,e,data);} else {return ( typeof data==_0x8057[14])?e:data;} ;} ,destroy:function (){this[_0x8057[36]][_0x8057[248]](_0x8057[717]);this[_0x8057[229]][_0x8057[708]](_0x8057[719])[_0x8057[718]](_0x8057[4]);this[_0x8057[261]][_0x8057[708]](_0x8057[719]);this[_0x8057[261]][_0x8057[350]](_0x8057[720]);this[_0x8057[261]][_0x8057[346]](_0x8057[721]);var html=this[_0x8057[35]][_0x8057[292]]();if(this[_0x8057[31]][_0x8057[387]]()){this[_0x8057[381]][_0x8057[508]](this.$element);this[_0x8057[381]][_0x8057[331]]();this[_0x8057[229]][_0x8057[388]](html)[_0x8057[481]]();} else {this[_0x8057[381]][_0x8057[508]](this.$editor);this[_0x8057[381]][_0x8057[331]]();this[_0x8057[229]][_0x8057[68]](html)[_0x8057[481]]();} ;if(this[_0x8057[232]]){this[_0x8057[232]][_0x8057[331]]();} ;if(this[_0x8057[722]]){this[_0x8057[722]][_0x8057[331]]();} ;if(this[_0x8057[723]]){this[_0x8057[723]][_0x8057[331]]();} ;$(_0x8057[724])[_0x8057[331]]();clearInterval(this[_0x8057[290]]);} };} ,dropdown:function (){return {build:function (name,$dropdown,dropdownObject){if(name==_0x8057[69]&&this[_0x8057[63]][_0x8057[725]]){$[_0x8057[20]](this[_0x8057[63]][_0x8057[725]],$[_0x8057[281]](function (i,s){var name=s[_0x8057[303]];if( typeof s[_0x8057[301]]!=_0x8057[14]){name=name+_0x8057[726]+s[_0x8057[301]];} ;s[_0x8057[265]]=(this[_0x8057[62]][_0x8057[513]](s[_0x8057[303]]))?_0x8057[29]:_0x8057[42];var func=(s[_0x8057[265]]==_0x8057[42])?_0x8057[727]:_0x8057[728];if(this[_0x8057[63]][_0x8057[282]]&&s[_0x8057[265]]==_0x8057[29]&&s[_0x8057[303]]==_0x8057[77]){return ;} ;this[_0x8057[69]][name]={tag:s[_0x8057[303]],style:s[_0x8057[90]],"\x63\x6C\x61\x73\x73":s[_0x8057[301]],attr:s[_0x8057[276]],data:s[_0x8057[9]],clear:s[_0x8057[302]]};dropdownObject[name]={func:func,title:s[_0x8057[471]]};} ,this));} ;$[_0x8057[20]](dropdownObject,$[_0x8057[281]](function (btnName,btnObject){var $item=$(_0x8057[729]+btnName+_0x8057[730]+btnObject[_0x8057[471]]+_0x8057[731]);if(name==_0x8057[69]){$item[_0x8057[347]](_0x8057[732]+btnName);} ;$item[_0x8057[425]](_0x8057[428],$[_0x8057[281]](function (e){e[_0x8057[413]]();var type=_0x8057[465];var callback=btnObject[_0x8057[465]];if(btnObject[_0x8057[466]]){type=_0x8057[466];callback=btnObject[_0x8057[466]];} else {if(btnObject[_0x8057[37]]){type=_0x8057[37];callback=btnObject[_0x8057[37]];} ;} ;this[_0x8057[32]][_0x8057[475]](e,btnName,type,callback);this[_0x8057[37]][_0x8057[733]]();} ,this));$dropdown[_0x8057[327]]($item);} ,this));} ,show:function (e,key){if(!this[_0x8057[63]][_0x8057[401]]){e[_0x8057[413]]();return false;} ;var $button=this[_0x8057[32]][_0x8057[292]](key);var $dropdown=$button[_0x8057[9]](_0x8057[37])[_0x8057[478]](document[_0x8057[87]]);if(this[_0x8057[62]][_0x8057[734]]()&&!this[_0x8057[62]][_0x8057[260]](_0x8057[259])){document[_0x8057[735]][_0x8057[446]]();} ;if($button[_0x8057[448]](_0x8057[736])){this[_0x8057[37]][_0x8057[733]]();} else {this[_0x8057[37]][_0x8057[733]]();this[_0x8057[36]][_0x8057[248]](_0x8057[737],{dropdown:$dropdown,key:key,button:$button});this[_0x8057[32]][_0x8057[707]](key);$button[_0x8057[347]](_0x8057[736]);var keyPosition=$button[_0x8057[480]]();var dropdownWidth=$dropdown[_0x8057[738]]();if((keyPosition[_0x8057[485]]+dropdownWidth)>$(document)[_0x8057[738]]()){keyPosition[_0x8057[485]]=Math[_0x8057[739]](0,keyPosition[_0x8057[485]]-dropdownWidth);} ;var left=keyPosition[_0x8057[485]]+_0x8057[484];if(this[_0x8057[494]][_0x8057[448]](_0x8057[740])){var top=this[_0x8057[494]][_0x8057[483]]()+this[_0x8057[63]][_0x8057[741]];var position=_0x8057[742];if(this[_0x8057[63]][_0x8057[743]]!==document){top=(this[_0x8057[494]][_0x8057[483]]()+this[_0x8057[494]][_0x8057[480]]()[_0x8057[482]])+this[_0x8057[63]][_0x8057[741]];position=_0x8057[744];} ;$dropdown[_0x8057[274]]({position:position,left:left,top:top+_0x8057[484]})[_0x8057[481]]();} else {var top=($button[_0x8057[483]]()+keyPosition[_0x8057[482]])+_0x8057[484];$dropdown[_0x8057[274]]({position:_0x8057[744],left:left,top:top})[_0x8057[481]]();} ;this[_0x8057[36]][_0x8057[248]](_0x8057[745],{dropdown:$dropdown,key:key,button:$button});} ;$(document)[_0x8057[746]](_0x8057[428],$[_0x8057[281]](this[_0x8057[37]][_0x8057[397]],this));this[_0x8057[261]][_0x8057[746]](_0x8057[428],$[_0x8057[281]](this[_0x8057[37]][_0x8057[397]],this));var $body=$(document[_0x8057[87]]);var width=$body[_0x8057[738]]();$dropdown[_0x8057[425]](_0x8057[479],function (){$body[_0x8057[347]](_0x8057[747]);$body[_0x8057[274]](_0x8057[748],($body[_0x8057[738]]()-width)+_0x8057[484]);} );$dropdown[_0x8057[425]](_0x8057[487],function (){$body[_0x8057[350]](_0x8057[747])[_0x8057[274]](_0x8057[748],0);} );e[_0x8057[749]]();} ,hideAll:function (){this[_0x8057[494]][_0x8057[332]](_0x8057[750])[_0x8057[350]](_0x8057[495])[_0x8057[350]](_0x8057[736]);$(document[_0x8057[87]])[_0x8057[350]](_0x8057[747])[_0x8057[274]](_0x8057[748],0);$(_0x8057[751])[_0x8057[397]]();this[_0x8057[36]][_0x8057[248]](_0x8057[752]);} ,hide:function (e){var $dropdown=$(e[_0x8057[442]]);if(!$dropdown[_0x8057[448]](_0x8057[736])){$dropdown[_0x8057[350]](_0x8057[736]);this[_0x8057[37]][_0x8057[733]]();} ;} };} ,file:function (){return {show:function (){this[_0x8057[50]][_0x8057[244]](_0x8057[38],this[_0x8057[46]][_0x8057[292]](_0x8057[38]),700);this[_0x8057[61]][_0x8057[22]](_0x8057[753],this[_0x8057[63]][_0x8057[754]],this[_0x8057[38]][_0x8057[43]]);this[_0x8057[56]][_0x8057[262]]();this[_0x8057[56]][_0x8057[292]]();var text=this[_0x8057[755]].toString();$(_0x8057[756])[_0x8057[388]](text);this[_0x8057[50]][_0x8057[481]]();} ,insert:function (json,direct,e){if( typeof json[_0x8057[19]]!=_0x8057[14]){this[_0x8057[50]][_0x8057[757]]();this[_0x8057[56]][_0x8057[269]]();this[_0x8057[36]][_0x8057[248]](_0x8057[758],json);return ;} ;var link;if( typeof json==_0x8057[8]){link=json;} else {var text=$(_0x8057[756])[_0x8057[388]]();if( typeof text==_0x8057[14]||text===_0x8057[255]){text=json[_0x8057[759]];} ;link=_0x8057[760]+json[_0x8057[761]]+_0x8057[762]+text+_0x8057[731];} ;if(direct){this[_0x8057[56]][_0x8057[763]]();var marker=this[_0x8057[56]][_0x8057[360]]();this[_0x8057[43]][_0x8057[764]](e,marker);} else {this[_0x8057[50]][_0x8057[757]]();} ;this[_0x8057[56]][_0x8057[269]]();this[_0x8057[30]][_0x8057[256]]();this[_0x8057[43]][_0x8057[765]](link);if( typeof json==_0x8057[8]){return ;} ;var linkmarker=$(this[_0x8057[261]][_0x8057[332]](_0x8057[766]));if(linkmarker[_0x8057[21]]!==0){linkmarker[_0x8057[346]](_0x8057[386])[_0x8057[346]](_0x8057[90]);} else {linkmarker=false;} ;this[_0x8057[36]][_0x8057[248]](_0x8057[754],linkmarker,json);} };} ,focus:function (){return {setStart:function (){this[_0x8057[261]][_0x8057[39]]();var first=this[_0x8057[261]][_0x8057[768]]()[_0x8057[767]]();if(first[_0x8057[21]]===0){return ;} ;if(first[0][_0x8057[21]]===0||first[0][_0x8057[311]]==_0x8057[516]||first[0][_0x8057[769]]==3){return ;} ;if(first[0][_0x8057[311]]==_0x8057[156]||first[0][_0x8057[311]]==_0x8057[157]){first=first[_0x8057[332]](_0x8057[128])[_0x8057[767]]();var child=first[_0x8057[768]]()[_0x8057[767]]();if(!this[_0x8057[62]][_0x8057[511]](child)&&child[_0x8057[770]]()===_0x8057[255]){this[_0x8057[33]][_0x8057[454]](child);return ;} ;} ;if(this[_0x8057[63]][_0x8057[282]]&&!this[_0x8057[62]][_0x8057[513]](first[0][_0x8057[311]])){this[_0x8057[56]][_0x8057[292]]();this[_0x8057[313]][_0x8057[454]](this[_0x8057[261]][0],0);this[_0x8057[313]][_0x8057[456]](this[_0x8057[261]][0],0);this[_0x8057[56]][_0x8057[518]]();return ;} ;this[_0x8057[33]][_0x8057[454]](first);} ,setEnd:function (){if(this[_0x8057[62]][_0x8057[260]](_0x8057[459])||this[_0x8057[62]][_0x8057[260]](_0x8057[259])){var last=this[_0x8057[261]][_0x8057[768]]()[_0x8057[771]]();this[_0x8057[33]][_0x8057[456]](last);} else {this[_0x8057[56]][_0x8057[292]]();try{this[_0x8057[313]][_0x8057[528]](this[_0x8057[261]][0]);this[_0x8057[313]][_0x8057[526]](false);this[_0x8057[56]][_0x8057[518]]();} catch(e){} ;} ;} ,isFocused:function (){var focusNode=document[_0x8057[531]]()[_0x8057[772]];if(focusNode===null){return false;} ;if(this[_0x8057[63]][_0x8057[282]]&&$(focusNode[_0x8057[773]])[_0x8057[448]](_0x8057[409])){return true;} else {if(!this[_0x8057[62]][_0x8057[774]](focusNode[_0x8057[773]])){return false;} ;} ;return this[_0x8057[261]][_0x8057[450]](_0x8057[775]);} };} ,image:function (){return {show:function (){this[_0x8057[50]][_0x8057[244]](_0x8057[40],this[_0x8057[46]][_0x8057[292]](_0x8057[40]),700);this[_0x8057[61]][_0x8057[22]](_0x8057[776],this[_0x8057[63]][_0x8057[777]],this[_0x8057[40]][_0x8057[43]]);this[_0x8057[56]][_0x8057[262]]();this[_0x8057[50]][_0x8057[481]]();} ,showEdit:function ($image){var $link=$image[_0x8057[352]](_0x8057[118]);this[_0x8057[50]][_0x8057[244]](_0x8057[778],this[_0x8057[46]][_0x8057[292]](_0x8057[779]),705);this[_0x8057[50]][_0x8057[780]]();this[_0x8057[40]][_0x8057[781]]=this[_0x8057[50]][_0x8057[783]](this[_0x8057[46]][_0x8057[292]](_0x8057[782]));this[_0x8057[40]][_0x8057[784]]=this[_0x8057[50]][_0x8057[785]](this[_0x8057[46]][_0x8057[292]](_0x8057[262]));this[_0x8057[40]][_0x8057[781]][_0x8057[425]](_0x8057[428],$[_0x8057[281]](function (){this[_0x8057[40]][_0x8057[331]]($image);} ,this));this[_0x8057[40]][_0x8057[784]][_0x8057[425]](_0x8057[428],$[_0x8057[281]](function (){this[_0x8057[40]][_0x8057[786]]($image);} ,this));$(_0x8057[787])[_0x8057[388]]($image[_0x8057[276]](_0x8057[637]));if(!this[_0x8057[63]][_0x8057[788]]){$(_0x8057[789])[_0x8057[397]]();} else {var $redactorImageLink=$(_0x8057[790]);$redactorImageLink[_0x8057[276]](_0x8057[791],$image[_0x8057[276]](_0x8057[636]));if($link[_0x8057[21]]!==0){$redactorImageLink[_0x8057[388]]($link[_0x8057[276]](_0x8057[791]));if($link[_0x8057[276]](_0x8057[442])==_0x8057[792]){$(_0x8057[795])[_0x8057[794]](_0x8057[793],true);} ;} ;} ;if(!this[_0x8057[63]][_0x8057[796]]){$(_0x8057[797])[_0x8057[397]]();} else {var floatValue=($image[_0x8057[274]](_0x8057[798])==_0x8057[29]&&$image[_0x8057[274]](_0x8057[799])==_0x8057[800])?_0x8057[258]:$image[_0x8057[274]](_0x8057[799]);$(_0x8057[801])[_0x8057[388]](floatValue);} ;this[_0x8057[50]][_0x8057[481]]();} ,setFloating:function ($image){var floating=$(_0x8057[801])[_0x8057[388]]();var imageFloat=_0x8057[255];var imageDisplay=_0x8057[255];var imageMargin=_0x8057[255];switch(floating){case _0x8057[485]:imageFloat=_0x8057[485];imageMargin=_0x8057[802]+this[_0x8057[63]][_0x8057[803]]+_0x8057[553]+this[_0x8057[63]][_0x8057[803]]+_0x8057[804];break ;;case _0x8057[257]:imageFloat=_0x8057[257];imageMargin=_0x8057[805]+this[_0x8057[63]][_0x8057[803]]+_0x8057[553]+this[_0x8057[63]][_0x8057[803]];break ;;case _0x8057[258]:imageDisplay=_0x8057[29];imageMargin=_0x8057[806];break ;;} ;$image[_0x8057[274]]({"\x66\x6C\x6F\x61\x74":imageFloat,display:imageDisplay,margin:imageMargin});$image[_0x8057[276]](_0x8057[505],$image[_0x8057[276]](_0x8057[90]));} ,update:function ($image){this[_0x8057[40]][_0x8057[807]]();this[_0x8057[30]][_0x8057[256]]();var $link=$image[_0x8057[352]](_0x8057[118]);$image[_0x8057[276]](_0x8057[637],$(_0x8057[787])[_0x8057[388]]());this[_0x8057[40]][_0x8057[808]]($image);var link=$[_0x8057[390]]($(_0x8057[790])[_0x8057[388]]());if(link!==_0x8057[255]){var pattern=_0x8057[809];var re= new RegExp(_0x8057[810]+pattern,_0x8057[123]);var re2= new RegExp(_0x8057[811]+pattern,_0x8057[123]);if(link[_0x8057[10]](re)==-1&&link[_0x8057[10]](re2)===0&&this[_0x8057[63]][_0x8057[812]]){link=this[_0x8057[63]][_0x8057[812]]+_0x8057[813]+link;} ;var target=($(_0x8057[795])[_0x8057[794]](_0x8057[793]))?true:false;if($link[_0x8057[21]]===0){var a=$(_0x8057[760]+link+_0x8057[730]+this[_0x8057[62]][_0x8057[814]]($image)+_0x8057[731]);if(target){a[_0x8057[276]](_0x8057[442],_0x8057[792]);} ;$image[_0x8057[284]](a);} else {$link[_0x8057[276]](_0x8057[791],link);if(target){$link[_0x8057[276]](_0x8057[442],_0x8057[792]);} else {$link[_0x8057[346]](_0x8057[442]);} ;} ;} else {if($link[_0x8057[21]]!==0){$link[_0x8057[284]](this[_0x8057[62]][_0x8057[814]]($image));} ;} ;this[_0x8057[50]][_0x8057[757]]();this[_0x8057[51]][_0x8057[815]]();this[_0x8057[35]][_0x8057[270]]();} ,setEditable:function ($image){if(this[_0x8057[63]][_0x8057[816]]){$image[_0x8057[425]](_0x8057[817],$[_0x8057[281]](this[_0x8057[40]][_0x8057[818]],this));} ;$image[_0x8057[425]](_0x8057[441],$[_0x8057[281]](this[_0x8057[40]][_0x8057[807]],this));$image[_0x8057[425]](_0x8057[819],$[_0x8057[281]](function (e){this[_0x8057[51]][_0x8057[40]]=$image;if(this[_0x8057[261]][_0x8057[332]](_0x8057[820])[_0x8057[21]]!==0){return false;} ;this[_0x8057[40]][_0x8057[821]]=this[_0x8057[40]][_0x8057[822]]($image);$(document)[_0x8057[425]](_0x8057[823],$[_0x8057[281]](this[_0x8057[40]][_0x8057[807]],this));this[_0x8057[261]][_0x8057[425]](_0x8057[823],$[_0x8057[281]](this[_0x8057[40]][_0x8057[807]],this));if(!this[_0x8057[63]][_0x8057[824]]){return ;} ;this[_0x8057[40]][_0x8057[821]][_0x8057[425]](_0x8057[825],$[_0x8057[281]](function (e){this[_0x8057[40]][_0x8057[826]](e,$image);} ,this));} ,this));} ,setResizable:function (e,$image){e[_0x8057[413]]();this[_0x8057[40]][_0x8057[827]]={x:e[_0x8057[828]],y:e[_0x8057[829]],el:$image,ratio:$image[_0x8057[738]]()/$image[_0x8057[702]](),h:$image[_0x8057[702]]()};e=e[_0x8057[421]]||e;if(e[_0x8057[830]]){this[_0x8057[40]][_0x8057[827]][_0x8057[831]]=e[_0x8057[830]][0][_0x8057[828]];this[_0x8057[40]][_0x8057[827]][_0x8057[832]]=e[_0x8057[830]][0][_0x8057[829]];} ;this[_0x8057[40]][_0x8057[833]]();} ,startResize:function (){$(document)[_0x8057[425]](_0x8057[834],$[_0x8057[281]](this[_0x8057[40]][_0x8057[835]],this));$(document)[_0x8057[425]](_0x8057[836],$[_0x8057[281]](this[_0x8057[40]][_0x8057[837]],this));} ,moveResize:function (e){e[_0x8057[413]]();e=e[_0x8057[421]]||e;var height=this[_0x8057[40]][_0x8057[827]][_0x8057[838]];if(e[_0x8057[830]]){height+=(e[_0x8057[830]][0][_0x8057[829]]-this[_0x8057[40]][_0x8057[827]][_0x8057[832]]);} else {height+=(e[_0x8057[829]]-this[_0x8057[40]][_0x8057[827]][_0x8057[832]]);} ;var width=Math[_0x8057[840]](height*this[_0x8057[40]][_0x8057[827]][_0x8057[839]]);if(height<50||width<100){return ;} ;this[_0x8057[40]][_0x8057[827]][_0x8057[841]][_0x8057[738]](width);this[_0x8057[40]][_0x8057[827]][_0x8057[841]][_0x8057[702]](this[_0x8057[40]][_0x8057[827]][_0x8057[841]][_0x8057[738]]()/this[_0x8057[40]][_0x8057[827]][_0x8057[839]]);this[_0x8057[35]][_0x8057[270]]();} ,stopResize:function (){this[_0x8057[842]]=false;$(document)[_0x8057[708]](_0x8057[843]);this[_0x8057[40]][_0x8057[807]]();} ,onDrag:function (e){if(this[_0x8057[261]][_0x8057[332]](_0x8057[820])[_0x8057[21]]!==0){e[_0x8057[413]]();return false;} ;this[_0x8057[261]][_0x8057[425]](_0x8057[844],$[_0x8057[281]](function (){setTimeout($[_0x8057[281]](this[_0x8057[40]][_0x8057[845]],this),1);} ,this));} ,onDrop:function (){this[_0x8057[40]][_0x8057[846]]();this[_0x8057[51]][_0x8057[815]]();this[_0x8057[261]][_0x8057[708]](_0x8057[844]);this[_0x8057[34]][_0x8057[353]]();this[_0x8057[35]][_0x8057[270]]();} ,fixImageSourceAfterDrop:function (){this[_0x8057[261]][_0x8057[332]](_0x8057[848])[_0x8057[20]](function (){var $el=$(this);$el[_0x8057[276]](_0x8057[636],$el[_0x8057[276]](_0x8057[847]));$el[_0x8057[346]](_0x8057[847]);} );} ,hideResize:function (e){if(e&&$(e[_0x8057[442]])[_0x8057[352]](_0x8057[820])[_0x8057[21]]!==0){return ;} ;if(e&&e[_0x8057[442]][_0x8057[311]]==_0x8057[849]){var $image=$(e[_0x8057[442]]);$image[_0x8057[276]](_0x8057[847],$image[_0x8057[276]](_0x8057[636]));} ;var imageBox=this[_0x8057[261]][_0x8057[332]](_0x8057[820]);if(imageBox[_0x8057[21]]===0){return ;} ;if(this[_0x8057[63]][_0x8057[816]]){this[_0x8057[40]][_0x8057[850]][_0x8057[331]]();} ;$(this[_0x8057[40]][_0x8057[821]])[_0x8057[331]]();imageBox[_0x8057[332]](_0x8057[119])[_0x8057[274]]({marginTop:imageBox[0][_0x8057[90]][_0x8057[851]],marginBottom:imageBox[0][_0x8057[90]][_0x8057[852]],marginLeft:imageBox[0][_0x8057[90]][_0x8057[853]],marginRight:imageBox[0][_0x8057[90]][_0x8057[854]]});imageBox[_0x8057[274]](_0x8057[855],_0x8057[255]);imageBox[_0x8057[332]](_0x8057[119])[_0x8057[274]](_0x8057[856],_0x8057[255]);imageBox[_0x8057[284]](function (){return $(this)[_0x8057[336]]();} );$(document)[_0x8057[708]](_0x8057[823]);this[_0x8057[261]][_0x8057[708]](_0x8057[823]);if( typeof this[_0x8057[40]][_0x8057[827]]!==_0x8057[14]){this[_0x8057[40]][_0x8057[827]][_0x8057[841]][_0x8057[276]](_0x8057[505],this[_0x8057[40]][_0x8057[827]][_0x8057[841]][_0x8057[276]](_0x8057[90]));} ;this[_0x8057[35]][_0x8057[270]]();} ,loadResizableControls:function ($image,imageBox){if(this[_0x8057[63]][_0x8057[824]]&&!this[_0x8057[62]][_0x8057[734]]()){var imageResizer=$(_0x8057[857]);if(!this[_0x8057[62]][_0x8057[470]]()){imageResizer[_0x8057[274]]({width:_0x8057[858],height:_0x8057[858]});} ;imageResizer[_0x8057[276]](_0x8057[721],false);imageBox[_0x8057[327]](imageResizer);imageBox[_0x8057[327]]($image);return imageResizer;} else {imageBox[_0x8057[327]]($image);return false;} ;} ,loadEditableControls:function ($image){var imageBox=$(_0x8057[859]);imageBox[_0x8057[274]](_0x8057[799],$image[_0x8057[274]](_0x8057[799]))[_0x8057[276]](_0x8057[721],false);if($image[0][_0x8057[90]][_0x8057[855]]!=_0x8057[806]){imageBox[_0x8057[274]]({marginTop:$image[0][_0x8057[90]][_0x8057[851]],marginBottom:$image[0][_0x8057[90]][_0x8057[852]],marginLeft:$image[0][_0x8057[90]][_0x8057[853]],marginRight:$image[0][_0x8057[90]][_0x8057[854]]});$image[_0x8057[274]](_0x8057[855],_0x8057[255]);} else {imageBox[_0x8057[274]]({"\x64\x69\x73\x70\x6C\x61\x79":_0x8057[29],"\x6D\x61\x72\x67\x69\x6E":_0x8057[806]});} ;$image[_0x8057[274]](_0x8057[856],_0x8057[860])[_0x8057[508]](imageBox);if(this[_0x8057[63]][_0x8057[816]]){this[_0x8057[40]][_0x8057[850]]=$(_0x8057[861]+this[_0x8057[46]][_0x8057[292]](_0x8057[779])+_0x8057[862]);this[_0x8057[40]][_0x8057[850]][_0x8057[276]](_0x8057[721],false);this[_0x8057[40]][_0x8057[850]][_0x8057[425]](_0x8057[428],$[_0x8057[281]](function (){this[_0x8057[40]][_0x8057[863]]($image);} ,this));imageBox[_0x8057[327]](this[_0x8057[40]][_0x8057[850]]);var editerWidth=this[_0x8057[40]][_0x8057[850]][_0x8057[486]]();this[_0x8057[40]][_0x8057[850]][_0x8057[274]](_0x8057[864],_0x8057[726]+editerWidth/2+_0x8057[484]);} ;return this[_0x8057[40]][_0x8057[865]]($image,imageBox);} ,remove:function (image){var $image=$(image);var $link=$image[_0x8057[352]](_0x8057[118]);var $figure=$image[_0x8057[352]](_0x8057[866]);var $parent=$image[_0x8057[509]]();if($(_0x8057[820])[_0x8057[21]]!==0){$parent=$(_0x8057[820])[_0x8057[509]]();} ;var $next;if($figure[_0x8057[21]]!==0){$next=$figure[_0x8057[519]]();$figure[_0x8057[331]]();} else {if($link[_0x8057[21]]!==0){$parent=$link[_0x8057[509]]();$link[_0x8057[331]]();} else {$image[_0x8057[331]]();} ;} ;$(_0x8057[820])[_0x8057[331]]();if($figure[_0x8057[21]]!==0){this[_0x8057[33]][_0x8057[454]]($next);} else {this[_0x8057[33]][_0x8057[454]]($parent);} ;this[_0x8057[36]][_0x8057[248]](_0x8057[867],$image[0][_0x8057[636]],$image);this[_0x8057[50]][_0x8057[757]]();this[_0x8057[35]][_0x8057[270]]();} ,insert:function (json,direct,e){if( typeof json[_0x8057[19]]!=_0x8057[14]){this[_0x8057[50]][_0x8057[757]]();this[_0x8057[56]][_0x8057[269]]();this[_0x8057[36]][_0x8057[248]](_0x8057[868],json);return ;} ;var $img;if( typeof json==_0x8057[8]){$img=$(json)[_0x8057[276]](_0x8057[641],_0x8057[869]);} else {$img=$(_0x8057[870]);$img[_0x8057[276]](_0x8057[636],json[_0x8057[761]])[_0x8057[276]](_0x8057[641],_0x8057[869]);} ;var node=$img;var isP=this[_0x8057[62]][_0x8057[590]](_0x8057[134]);if(isP){node=$(_0x8057[871])[_0x8057[327]]($img);} ;if(direct){this[_0x8057[56]][_0x8057[763]]();var marker=this[_0x8057[56]][_0x8057[360]]();this[_0x8057[43]][_0x8057[764]](e,marker);} else {this[_0x8057[50]][_0x8057[757]]();} ;this[_0x8057[56]][_0x8057[269]]();this[_0x8057[30]][_0x8057[256]]();this[_0x8057[43]][_0x8057[68]](this[_0x8057[62]][_0x8057[814]](node),false);var $image=this[_0x8057[261]][_0x8057[332]](_0x8057[872])[_0x8057[346]](_0x8057[641]);if(isP){$image[_0x8057[509]]()[_0x8057[336]]()[_0x8057[335]]()[_0x8057[272]](_0x8057[873]);} else {if(this[_0x8057[63]][_0x8057[282]]){$image[_0x8057[510]](_0x8057[326])[_0x8057[508]](_0x8057[326]);} ;} ;if( typeof json==_0x8057[8]){return ;} ;this[_0x8057[36]][_0x8057[248]](_0x8057[777],$image,json);} };} ,indent:function (){return {increase:function (){if(!this[_0x8057[62]][_0x8057[260]](_0x8057[259])){this[_0x8057[261]][_0x8057[39]]();} ;this[_0x8057[30]][_0x8057[256]]();this[_0x8057[56]][_0x8057[262]]();var block=this[_0x8057[56]][_0x8057[323]]();if(block&&block[_0x8057[311]]==_0x8057[158]){this[_0x8057[41]][_0x8057[874]]();} else {if(block===false&&this[_0x8057[63]][_0x8057[282]]){this[_0x8057[41]][_0x8057[875]]();} else {this[_0x8057[41]][_0x8057[876]]();} ;} ;this[_0x8057[56]][_0x8057[269]]();this[_0x8057[35]][_0x8057[270]]();} ,increaseLists:function (){document[_0x8057[322]](_0x8057[41]);this[_0x8057[41]][_0x8057[877]]();this[_0x8057[34]][_0x8057[878]]();this[_0x8057[34]][_0x8057[353]]();} ,increaseBlocks:function (){$[_0x8057[20]](this[_0x8057[56]][_0x8057[264]](),$[_0x8057[281]](function (i,elem){if(elem[_0x8057[311]]===_0x8057[145]||elem[_0x8057[311]]===_0x8057[318]){return ;} ;var $el=this[_0x8057[62]][_0x8057[277]](elem);var left=this[_0x8057[62]][_0x8057[879]]($el[_0x8057[274]](_0x8057[864]))+this[_0x8057[63]][_0x8057[880]];$el[_0x8057[274]](_0x8057[864],left+_0x8057[484]);} ,this));} ,increaseText:function (){var wrapper=this[_0x8057[56]][_0x8057[272]](_0x8057[271]);$(wrapper)[_0x8057[276]](_0x8057[275],_0x8057[4]);$(wrapper)[_0x8057[274]](_0x8057[864],this[_0x8057[63]][_0x8057[880]]+_0x8057[484]);} ,decrease:function (){this[_0x8057[30]][_0x8057[256]]();this[_0x8057[56]][_0x8057[262]]();var block=this[_0x8057[56]][_0x8057[323]]();if(block&&block[_0x8057[311]]==_0x8057[158]){this[_0x8057[41]][_0x8057[881]]();} else {this[_0x8057[41]][_0x8057[882]]();} ;this[_0x8057[56]][_0x8057[269]]();this[_0x8057[35]][_0x8057[270]]();} ,decreaseLists:function (){document[_0x8057[322]](_0x8057[75]);var current=this[_0x8057[56]][_0x8057[883]]();var $item=$(current)[_0x8057[352]](_0x8057[128]);var $parent=$item[_0x8057[509]]();if($item[_0x8057[21]]!==0&&$parent[_0x8057[21]]!==0&&$parent[0][_0x8057[311]]==_0x8057[158]){$parent[_0x8057[508]]($item);} ;this[_0x8057[41]][_0x8057[877]]();if(!this[_0x8057[63]][_0x8057[282]]&&$item[_0x8057[21]]===0){document[_0x8057[322]](_0x8057[319],false,_0x8057[77]);this[_0x8057[261]][_0x8057[332]](_0x8057[884])[_0x8057[20]]($[_0x8057[281]](this[_0x8057[62]][_0x8057[355]],this));} ;this[_0x8057[34]][_0x8057[353]]();} ,decreaseBlocks:function (){$[_0x8057[20]](this[_0x8057[56]][_0x8057[264]](),$[_0x8057[281]](function (i,elem){var $el=this[_0x8057[62]][_0x8057[277]](elem);var left=this[_0x8057[62]][_0x8057[879]]($el[_0x8057[274]](_0x8057[864]))-this[_0x8057[63]][_0x8057[880]];if(left<=0){if(this[_0x8057[63]][_0x8057[282]]&& typeof ($el[_0x8057[9]](_0x8057[283]))!==_0x8057[14]){$el[_0x8057[284]]($el[_0x8057[68]]()+_0x8057[359]);} else {$el[_0x8057[274]](_0x8057[864],_0x8057[255]);this[_0x8057[62]][_0x8057[285]]($el,_0x8057[90]);} ;} else {$el[_0x8057[274]](_0x8057[864],left+_0x8057[484]);} ;} ,this));} ,fixEmptyIndent:function (){var block=this[_0x8057[56]][_0x8057[323]]();if(this[_0x8057[313]][_0x8057[314]]&&block&&block[_0x8057[311]]==_0x8057[158]&&this[_0x8057[62]][_0x8057[293]]($(block)[_0x8057[770]]())){var $block=$(block);$block[_0x8057[332]](_0x8057[101])[_0x8057[498]](_0x8057[885])[_0x8057[336]]()[_0x8057[335]]();$block[_0x8057[327]](_0x8057[326]);} ;} };} ,inline:function (){return {formatting:function (name){var type,value;if( typeof this[_0x8057[69]][name][_0x8057[90]]!=_0x8057[14]){type=_0x8057[90];} else {if( typeof this[_0x8057[69]][name][_0x8057[301]]!=_0x8057[14]){type=_0x8057[301];} ;} ;if(type){value=this[_0x8057[69]][name][type];} ;this[_0x8057[42]][_0x8057[304]](this[_0x8057[69]][name][_0x8057[303]],type,value);} ,format:function (tag,type,value){if(this[_0x8057[62]][_0x8057[590]](_0x8057[155])||this[_0x8057[62]][_0x8057[886]]()){return ;} ;var tags=[_0x8057[120],_0x8057[70],_0x8057[123],_0x8057[71],_0x8057[102],_0x8057[887],_0x8057[72],_0x8057[109],_0x8057[110]];var replaced=[_0x8057[95],_0x8057[95],_0x8057[97],_0x8057[97],_0x8057[99],_0x8057[93],_0x8057[93],_0x8057[122],_0x8057[121]];for(var i=0;i<tags[_0x8057[21]];i++){if(tag==tags[i]){tag=replaced[i];} ;} ;this[_0x8057[42]][_0x8057[265]]=type||false;this[_0x8057[42]][_0x8057[309]]=value||false;this[_0x8057[30]][_0x8057[256]]();this[_0x8057[261]][_0x8057[39]]();this[_0x8057[56]][_0x8057[292]]();if(this[_0x8057[313]][_0x8057[314]]){this[_0x8057[42]][_0x8057[888]](tag);} else {this[_0x8057[42]][_0x8057[889]](tag);} ;} ,formatCollapsed:function (tag){var current=this[_0x8057[56]][_0x8057[883]]();var $parent=$(current)[_0x8057[352]](tag+_0x8057[890]+tag+_0x8057[891]);if($parent[_0x8057[21]]!==0&&(this[_0x8057[42]][_0x8057[265]]!=_0x8057[90]&&$parent[0][_0x8057[311]]!=_0x8057[892])){this[_0x8057[33]][_0x8057[893]]($parent[0]);if(this[_0x8057[62]][_0x8057[293]]($parent[_0x8057[770]]())){$parent[_0x8057[331]]();this[_0x8057[35]][_0x8057[270]]();} ;return ;} ;var node=$(_0x8057[320]+tag+_0x8057[321])[_0x8057[276]](_0x8057[638],_0x8057[4])[_0x8057[276]](_0x8057[679],tag);node[_0x8057[68]](this[_0x8057[63]][_0x8057[515]]);node=this[_0x8057[42]][_0x8057[894]](node);var node=this[_0x8057[43]][_0x8057[895]](node);this[_0x8057[33]][_0x8057[456]](node);this[_0x8057[35]][_0x8057[270]]();} ,formatMultiple:function (tag){this[_0x8057[42]][_0x8057[896]](tag);this[_0x8057[56]][_0x8057[262]]();document[_0x8057[322]](_0x8057[887]);this[_0x8057[261]][_0x8057[332]](_0x8057[92])[_0x8057[20]]($[_0x8057[281]](function (i,s){var $el=$(s);this[_0x8057[42]][_0x8057[897]]($el,tag);var $span;if(this[_0x8057[42]][_0x8057[265]]){$span=$(_0x8057[477])[_0x8057[276]](_0x8057[679],tag)[_0x8057[276]](_0x8057[638],_0x8057[4]);$span=this[_0x8057[42]][_0x8057[894]]($span);} else {$span=$(_0x8057[320]+tag+_0x8057[321])[_0x8057[276]](_0x8057[679],tag)[_0x8057[276]](_0x8057[638],_0x8057[4]);} ;$el[_0x8057[284]]($span[_0x8057[68]]($el[_0x8057[336]]()));if(tag==_0x8057[101]){var $parent=$span[_0x8057[509]]();if($parent&&$parent[0][_0x8057[311]]==_0x8057[892]&&this[_0x8057[42]][_0x8057[265]]==_0x8057[90]){var arr=this[_0x8057[42]][_0x8057[309]][_0x8057[13]](_0x8057[898]);for(var z=0;z<arr[_0x8057[21]];z++){if(arr[z]===_0x8057[255]){return ;} ;var style=arr[z][_0x8057[13]](_0x8057[899]);$parent[_0x8057[274]](style[0],_0x8057[255]);if(this[_0x8057[62]][_0x8057[285]]($parent,_0x8057[90])){$parent[_0x8057[284]]($parent[_0x8057[336]]());} ;} ;} ;} ;} ,this));if(tag!=_0x8057[101]){this[_0x8057[261]][_0x8057[332]](this[_0x8057[63]][_0x8057[678]][_0x8057[241]](_0x8057[668]))[_0x8057[20]]($[_0x8057[281]](function (i,s){var $el=$(s);var property=$el[_0x8057[274]](_0x8057[900]);if(property==_0x8057[901]){$el[_0x8057[274]](_0x8057[900],_0x8057[255]);this[_0x8057[62]][_0x8057[285]]($el,_0x8057[90]);} ;} ,this));} ;if(tag!=_0x8057[93]){var _this=this;this[_0x8057[261]][_0x8057[332]](_0x8057[42])[_0x8057[20]](function (i,s){_this[_0x8057[62]][_0x8057[330]](s,_0x8057[93]);} );} ;this[_0x8057[56]][_0x8057[269]]();this[_0x8057[35]][_0x8057[270]]();} ,formatRemoveSameChildren:function ($el,tag){var self=this;$el[_0x8057[768]](tag)[_0x8057[20]](function (){var $child=$(this);if(!$child[_0x8057[448]](_0x8057[902])){if(self[_0x8057[42]][_0x8057[265]]==_0x8057[90]){var arr=self[_0x8057[42]][_0x8057[309]][_0x8057[13]](_0x8057[898]);for(var z=0;z<arr[_0x8057[21]];z++){if(arr[z]===_0x8057[255]){return ;} ;var style=arr[z][_0x8057[13]](_0x8057[899]);$child[_0x8057[274]](style[0],_0x8057[255]);if(self[_0x8057[62]][_0x8057[285]]($child,_0x8057[90])){$child[_0x8057[284]]($child[_0x8057[336]]());} ;} ;} else {$child[_0x8057[336]]()[_0x8057[335]]();} ;} ;} );} ,formatConvert:function (tag){this[_0x8057[56]][_0x8057[262]]();var find=_0x8057[255];if(this[_0x8057[42]][_0x8057[265]]==_0x8057[301]){find=_0x8057[903]+this[_0x8057[42]][_0x8057[309]]+_0x8057[891];} else {if(this[_0x8057[42]][_0x8057[265]]==_0x8057[90]){find=_0x8057[904]+this[_0x8057[42]][_0x8057[309]]+_0x8057[905];} ;} ;var self=this;if(tag!=_0x8057[93]){this[_0x8057[261]][_0x8057[332]](_0x8057[93])[_0x8057[20]](function (i,s){self[_0x8057[62]][_0x8057[330]](s,_0x8057[42]);} );} ;if(tag!=_0x8057[101]){this[_0x8057[261]][_0x8057[332]](tag)[_0x8057[20]](function (){var $el=$(this);$el[_0x8057[284]]($(_0x8057[906])[_0x8057[68]]($el[_0x8057[336]]()));} );} ;this[_0x8057[261]][_0x8057[332]](_0x8057[907]+tag+_0x8057[905]+find)[_0x8057[20]](function (){if(find===_0x8057[255]&&tag==_0x8057[101]&&this[_0x8057[311]][_0x8057[325]]()==tag){return ;} ;var $el=$(this);$el[_0x8057[284]]($(_0x8057[906])[_0x8057[68]]($el[_0x8057[336]]()));} );this[_0x8057[56]][_0x8057[269]]();} ,setFormat:function (node){switch(this[_0x8057[42]][_0x8057[265]]){case _0x8057[301]:if(node[_0x8057[448]](this[_0x8057[42]][_0x8057[309]])){node[_0x8057[350]](this[_0x8057[42]][_0x8057[309]]);node[_0x8057[346]](_0x8057[681]);} else {node[_0x8057[347]](this[_0x8057[42]][_0x8057[309]]);node[_0x8057[276]](_0x8057[681],this[_0x8057[42]][_0x8057[309]]);} ;break ;;case _0x8057[90]:node[0][_0x8057[90]][_0x8057[908]]=this[_0x8057[42]][_0x8057[309]];node[_0x8057[276]](_0x8057[680],this[_0x8057[42]][_0x8057[309]]);break ;;} ;return node;} ,removeStyle:function (){this[_0x8057[30]][_0x8057[256]]();var current=this[_0x8057[56]][_0x8057[883]]();var nodes=this[_0x8057[56]][_0x8057[909]]();this[_0x8057[56]][_0x8057[262]]();if(current&&current[_0x8057[311]]===_0x8057[892]){var $s=$(current);$s[_0x8057[346]](_0x8057[90]);if($s[0][_0x8057[910]][_0x8057[21]]===0){$s[_0x8057[284]]($s[_0x8057[336]]());} ;} ;$[_0x8057[20]](nodes,$[_0x8057[281]](function (i,s){var $s=$(s);if($[_0x8057[306]](s[_0x8057[311]][_0x8057[325]](),this[_0x8057[63]][_0x8057[678]])!=-1&&!$s[_0x8057[448]](_0x8057[902])){$s[_0x8057[346]](_0x8057[90]);if($s[0][_0x8057[910]][_0x8057[21]]===0){$s[_0x8057[284]]($s[_0x8057[336]]());} ;} ;} ,this));this[_0x8057[56]][_0x8057[269]]();this[_0x8057[35]][_0x8057[270]]();} ,removeStyleRule:function (name){this[_0x8057[30]][_0x8057[256]]();var parent=this[_0x8057[56]][_0x8057[911]]();var nodes=this[_0x8057[56]][_0x8057[909]]();this[_0x8057[56]][_0x8057[262]]();if(parent&&parent[_0x8057[311]]===_0x8057[892]){var $s=$(parent);$s[_0x8057[274]](name,_0x8057[255]);this[_0x8057[62]][_0x8057[285]]($s,_0x8057[90]);if($s[0][_0x8057[910]][_0x8057[21]]===0){$s[_0x8057[284]]($s[_0x8057[336]]());} ;} ;$[_0x8057[20]](nodes,$[_0x8057[281]](function (i,s){var $s=$(s);if($[_0x8057[306]](s[_0x8057[311]][_0x8057[325]](),this[_0x8057[63]][_0x8057[678]])!=-1&&!$s[_0x8057[448]](_0x8057[902])){$s[_0x8057[274]](name,_0x8057[255]);this[_0x8057[62]][_0x8057[285]]($s,_0x8057[90]);if($s[0][_0x8057[910]][_0x8057[21]]===0){$s[_0x8057[284]]($s[_0x8057[336]]());} ;} ;} ,this));this[_0x8057[56]][_0x8057[269]]();this[_0x8057[35]][_0x8057[270]]();} ,removeFormat:function (){this[_0x8057[30]][_0x8057[256]]();var current=this[_0x8057[56]][_0x8057[883]]();this[_0x8057[56]][_0x8057[262]]();document[_0x8057[322]](_0x8057[912]);if(current&&current[_0x8057[311]]===_0x8057[892]){$(current)[_0x8057[284]]($(current)[_0x8057[336]]());} ;$[_0x8057[20]](this[_0x8057[56]][_0x8057[913]](),$[_0x8057[281]](function (i,s){var $s=$(s);if($[_0x8057[306]](s[_0x8057[311]][_0x8057[325]](),this[_0x8057[63]][_0x8057[678]])!=-1&&!$s[_0x8057[448]](_0x8057[902])){$s[_0x8057[284]]($s[_0x8057[336]]());} ;} ,this));this[_0x8057[56]][_0x8057[269]]();this[_0x8057[35]][_0x8057[270]]();} ,toggleClass:function (className){this[_0x8057[42]][_0x8057[304]](_0x8057[101],_0x8057[301],className);} ,toggleStyle:function (value){this[_0x8057[42]][_0x8057[304]](_0x8057[101],_0x8057[90],value);} };} ,insert:function (){return {set:function (html,clean){this[_0x8057[54]][_0x8057[331]]();html=this[_0x8057[34]][_0x8057[550]](html);if( typeof clean==_0x8057[14]){html=this[_0x8057[34]][_0x8057[914]](html,false);} ;this[_0x8057[261]][_0x8057[68]](html);this[_0x8057[56]][_0x8057[331]]();this[_0x8057[39]][_0x8057[456]]();this[_0x8057[34]][_0x8057[878]]();this[_0x8057[35]][_0x8057[270]]();this[_0x8057[51]][_0x8057[244]]();if( typeof clean==_0x8057[14]){setTimeout($[_0x8057[281]](this[_0x8057[34]][_0x8057[353]],this),10);} ;} ,text:function (text){this[_0x8057[54]][_0x8057[331]]();text=text.toString();text=$[_0x8057[390]](text);text=this[_0x8057[34]][_0x8057[587]](text,false);this[_0x8057[261]][_0x8057[39]]();if(this[_0x8057[62]][_0x8057[260]](_0x8057[259])){this[_0x8057[43]][_0x8057[915]](text);} else {this[_0x8057[56]][_0x8057[292]]();this[_0x8057[313]][_0x8057[916]]();var el=document[_0x8057[652]](_0x8057[271]);el[_0x8057[514]]=text;var frag=document[_0x8057[917]](),node,lastNode;while((node=el[_0x8057[919]])){lastNode=frag[_0x8057[918]](node);} ;this[_0x8057[313]][_0x8057[920]](frag);if(lastNode){var range=this[_0x8057[313]][_0x8057[527]]();range[_0x8057[522]](lastNode);range[_0x8057[526]](true);this[_0x8057[755]][_0x8057[921]]();this[_0x8057[755]][_0x8057[518]](range);} ;} ;this[_0x8057[35]][_0x8057[270]]();this[_0x8057[34]][_0x8057[353]]();} ,htmlWithoutClean:function (html){this[_0x8057[43]][_0x8057[68]](html,false);} ,html:function (html,clean){this[_0x8057[54]][_0x8057[331]]();if( typeof clean==_0x8057[14]){clean=true;} ;this[_0x8057[261]][_0x8057[39]]();html=this[_0x8057[34]][_0x8057[550]](html);if(clean){html=this[_0x8057[34]][_0x8057[914]](html);} ;if(this[_0x8057[62]][_0x8057[260]](_0x8057[259])){this[_0x8057[43]][_0x8057[915]](html);} else {if(this[_0x8057[34]][_0x8057[597]]){this[_0x8057[43]][_0x8057[922]](html);} else {document[_0x8057[322]](_0x8057[923],false,html);} ;this[_0x8057[43]][_0x8057[924]]();} ;this[_0x8057[34]][_0x8057[878]]();if(!this[_0x8057[63]][_0x8057[282]]){this[_0x8057[261]][_0x8057[332]](_0x8057[77])[_0x8057[20]]($[_0x8057[281]](this[_0x8057[62]][_0x8057[355]],this));} ;this[_0x8057[35]][_0x8057[270]]();this[_0x8057[51]][_0x8057[244]]();if(clean){this[_0x8057[34]][_0x8057[353]]();} ;} ,htmlFixMozilla:function (){if(!this[_0x8057[62]][_0x8057[260]](_0x8057[459])){return ;} ;var $next=$(this[_0x8057[56]][_0x8057[323]]())[_0x8057[519]]();if($next[_0x8057[21]]>0&&$next[0][_0x8057[311]]==_0x8057[134]&&$next[_0x8057[68]]()===_0x8057[255]){$next[_0x8057[331]]();} ;} ,htmlIe:function (html){if(this[_0x8057[62]][_0x8057[925]]()){var parent=this[_0x8057[62]][_0x8057[590]](_0x8057[134]);var $html=$(_0x8057[548])[_0x8057[327]](html);var blocksMatch=$html[_0x8057[336]]()[_0x8057[450]](_0x8057[926]);if(parent&&blocksMatch){this[_0x8057[43]][_0x8057[927]](parent,html);} else {this[_0x8057[43]][_0x8057[928]](html);} ;return ;} ;document[_0x8057[56]][_0x8057[930]]()[_0x8057[929]](html);} ,execHtml:function (html){html=this[_0x8057[34]][_0x8057[550]](html);this[_0x8057[56]][_0x8057[292]]();this[_0x8057[313]][_0x8057[916]]();var el=document[_0x8057[652]](_0x8057[271]);el[_0x8057[514]]=html;var frag=document[_0x8057[917]](),node,lastNode;while((node=el[_0x8057[919]])){lastNode=frag[_0x8057[918]](node);} ;this[_0x8057[313]][_0x8057[920]](frag);this[_0x8057[313]][_0x8057[526]](true);this[_0x8057[33]][_0x8057[893]](lastNode);} ,node:function (node,deleteContents){node=node[0]||node;var html=this[_0x8057[62]][_0x8057[814]](node);html=this[_0x8057[34]][_0x8057[550]](html);if(html[_0x8057[647]](/</g)!==null){node=$(html)[0];} ;this[_0x8057[56]][_0x8057[292]]();if(deleteContents!==false){this[_0x8057[313]][_0x8057[916]]();} ;this[_0x8057[313]][_0x8057[920]](node);this[_0x8057[313]][_0x8057[526]](false);this[_0x8057[56]][_0x8057[518]]();return node;} ,nodeToPoint:function (node,x,y){node=node[0]||node;this[_0x8057[56]][_0x8057[292]]();var range;if(document[_0x8057[931]]){var pos=document[_0x8057[931]](x,y);this[_0x8057[313]][_0x8057[454]](pos[_0x8057[932]],pos[_0x8057[480]]);this[_0x8057[313]][_0x8057[526]](true);this[_0x8057[313]][_0x8057[920]](node);} else {if(document[_0x8057[933]]){range=document[_0x8057[933]](x,y);range[_0x8057[920]](node);} else {if( typeof document[_0x8057[87]][_0x8057[934]]!=_0x8057[14]){range=document[_0x8057[87]][_0x8057[934]]();range[_0x8057[935]](x,y);var endRange=range[_0x8057[936]]();endRange[_0x8057[935]](x,y);range[_0x8057[938]](_0x8057[937],endRange);range[_0x8057[939]]();} ;} ;} ;} ,nodeToCaretPositionFromPoint:function (e,node){node=node[0]||node;var range;var x=e[_0x8057[940]],y=e[_0x8057[941]];if(document[_0x8057[931]]){var pos=document[_0x8057[931]](x,y);var sel=document[_0x8057[531]]();range=sel[_0x8057[533]](0);range[_0x8057[454]](pos[_0x8057[932]],pos[_0x8057[480]]);range[_0x8057[526]](true);range[_0x8057[920]](node);} else {if(document[_0x8057[933]]){range=document[_0x8057[933]](x,y);range[_0x8057[920]](node);} else {if( typeof document[_0x8057[87]][_0x8057[934]]!=_0x8057[14]){range=document[_0x8057[87]][_0x8057[934]]();range[_0x8057[935]](x,y);var endRange=range[_0x8057[936]]();endRange[_0x8057[935]](x,y);range[_0x8057[938]](_0x8057[937],endRange);range[_0x8057[939]]();} ;} ;} ;} ,ie11FixInserting:function (parent,html){var node=document[_0x8057[652]](_0x8057[101]);node[_0x8057[942]]=_0x8057[943];this[_0x8057[43]][_0x8057[895]](node);var parHtml=$(parent)[_0x8057[68]]();parHtml=_0x8057[619]+parHtml[_0x8057[541]](/<span class="redactor-ie-paste"><\/span>/gi,_0x8057[620]+html+_0x8057[619])+_0x8057[620];$(parent)[_0x8057[284]](parHtml);} ,ie11PasteFrag:function (html){this[_0x8057[56]][_0x8057[292]]();this[_0x8057[313]][_0x8057[916]]();var el=document[_0x8057[652]](_0x8057[271]);el[_0x8057[514]]=html;var frag=document[_0x8057[917]](),node,lastNode;while((node=el[_0x8057[919]])){lastNode=frag[_0x8057[918]](node);} ;this[_0x8057[313]][_0x8057[920]](frag);} };} ,keydown:function (){return {init:function (e){if(this[_0x8057[231]]){return ;} ;var key=e[_0x8057[944]];var arrow=(key>=37&&key<=40);this[_0x8057[44]][_0x8057[945]]=e[_0x8057[946]]||e[_0x8057[947]];this[_0x8057[44]][_0x8057[948]]=this[_0x8057[56]][_0x8057[883]]();this[_0x8057[44]][_0x8057[509]]=this[_0x8057[56]][_0x8057[911]]();this[_0x8057[44]][_0x8057[29]]=this[_0x8057[56]][_0x8057[323]]();this[_0x8057[44]][_0x8057[79]]=this[_0x8057[62]][_0x8057[949]](this[_0x8057[44]][_0x8057[948]],_0x8057[79]);this[_0x8057[44]][_0x8057[78]]=this[_0x8057[62]][_0x8057[949]](this[_0x8057[44]][_0x8057[948]],_0x8057[78]);this[_0x8057[44]][_0x8057[950]]=this[_0x8057[62]][_0x8057[949]](this[_0x8057[44]][_0x8057[948]],_0x8057[950]);this[_0x8057[57]][_0x8057[22]](e,key);this[_0x8057[44]][_0x8057[951]](arrow,key);this[_0x8057[44]][_0x8057[952]](e,key);this[_0x8057[44]][_0x8057[953]](arrow);this[_0x8057[44]][_0x8057[954]](e,key);var keydownStop=this[_0x8057[36]][_0x8057[248]](_0x8057[44],e);if(keydownStop===false){e[_0x8057[413]]();return false;} ;if(this[_0x8057[63]][_0x8057[955]]&&(this[_0x8057[62]][_0x8057[260]](_0x8057[259])||this[_0x8057[62]][_0x8057[260]](_0x8057[459]))&&(key===this[_0x8057[711]][_0x8057[956]]||key===this[_0x8057[711]][_0x8057[957]])){var isEndOfTable=false;var $table=false;if(this[_0x8057[44]][_0x8057[29]]&&this[_0x8057[44]][_0x8057[29]][_0x8057[311]]===_0x8057[145]){$table=$(this[_0x8057[44]][_0x8057[29]])[_0x8057[352]](_0x8057[362]);} ;if($table&&$table[_0x8057[332]](_0x8057[340])[_0x8057[771]]()[0]===this[_0x8057[44]][_0x8057[29]]){isEndOfTable=true;} ;if(this[_0x8057[62]][_0x8057[958]]()&&isEndOfTable){var node=$(this[_0x8057[63]][_0x8057[517]]);$table[_0x8057[508]](node);this[_0x8057[33]][_0x8057[454]](node);} ;} ;if(this[_0x8057[63]][_0x8057[955]]&&key===this[_0x8057[711]][_0x8057[956]]){this[_0x8057[44]][_0x8057[959]]();} ;if(!this[_0x8057[63]][_0x8057[955]]&&key===this[_0x8057[711]][_0x8057[960]]){e[_0x8057[413]]();if(!this[_0x8057[313]][_0x8057[314]]){this[_0x8057[313]][_0x8057[916]]();} ;return ;} ;if(key==this[_0x8057[711]][_0x8057[960]]&&!e[_0x8057[961]]&&!e[_0x8057[946]]&&!e[_0x8057[947]]){var stop=this[_0x8057[36]][_0x8057[248]](_0x8057[962],e);if(stop===false){e[_0x8057[413]]();return false;} ;if(this[_0x8057[44]][_0x8057[78]]&&this[_0x8057[44]][_0x8057[963]](e)===true){return false;} ;var current,$next;if(this[_0x8057[44]][_0x8057[79]]){return this[_0x8057[44]][_0x8057[964]](e);} else {if(this[_0x8057[44]][_0x8057[78]]||this[_0x8057[44]][_0x8057[950]]){current=this[_0x8057[56]][_0x8057[883]]();$next=$(current)[_0x8057[519]]();if($next[_0x8057[21]]!==0&&$next[0][_0x8057[311]]==_0x8057[516]){return this[_0x8057[44]][_0x8057[965]](e);} else {if(this[_0x8057[62]][_0x8057[958]]()&&(current&&current!=_0x8057[892])){return this[_0x8057[44]][_0x8057[966]](e);} else {return this[_0x8057[44]][_0x8057[965]](e);} ;} ;} else {if(this[_0x8057[63]][_0x8057[282]]&&!this[_0x8057[44]][_0x8057[29]]){current=this[_0x8057[56]][_0x8057[883]]();$next=$(this[_0x8057[44]][_0x8057[948]])[_0x8057[519]]();if(current!==false&&$(current)[_0x8057[448]](_0x8057[967])){this[_0x8057[33]][_0x8057[893]](current);$(current)[_0x8057[336]]()[_0x8057[335]]();return this[_0x8057[44]][_0x8057[966]](e);} else {if($next[_0x8057[21]]===0&&current===false&& typeof $next[_0x8057[968]]!=_0x8057[14]){return this[_0x8057[44]][_0x8057[966]](e);} else {if(this[_0x8057[62]][_0x8057[969]]()){return this[_0x8057[44]][_0x8057[966]](e);} ;} ;return this[_0x8057[44]][_0x8057[965]](e);} ;} else {if(this[_0x8057[63]][_0x8057[282]]&&this[_0x8057[44]][_0x8057[29]]){setTimeout($[_0x8057[281]](this[_0x8057[44]][_0x8057[970]],this),1);} else {if(!this[_0x8057[63]][_0x8057[282]]&&this[_0x8057[44]][_0x8057[29]]&&this[_0x8057[44]][_0x8057[29]][_0x8057[311]]!==_0x8057[158]){setTimeout($[_0x8057[281]](this[_0x8057[44]][_0x8057[971]],this),1);} else {if(!this[_0x8057[63]][_0x8057[282]]&&!this[_0x8057[44]][_0x8057[29]]){return this[_0x8057[44]][_0x8057[972]](e);} ;} ;} ;} ;} ;} ;} ;if(key===this[_0x8057[711]][_0x8057[960]]&&(e[_0x8057[946]]||e[_0x8057[961]])){return this[_0x8057[44]][_0x8057[973]](e);} ;if(key===this[_0x8057[711]][_0x8057[974]]||e[_0x8057[947]]&&key===221||e[_0x8057[947]]&&key===219){return this[_0x8057[44]][_0x8057[975]](e,key);} ;if(key===this[_0x8057[711]][_0x8057[976]]||key===this[_0x8057[711]][_0x8057[977]]){var nodes=this[_0x8057[56]][_0x8057[913]]();if(nodes){var len=nodes[_0x8057[21]];var last;for(var i=0;i<len;i++){var children=$(nodes[i])[_0x8057[768]](_0x8057[119]);if(children[_0x8057[21]]!==0){var self=this;$[_0x8057[20]](children,function (z,s){var $s=$(s);if($s[_0x8057[274]](_0x8057[799])!=_0x8057[800]){return ;} ;self[_0x8057[36]][_0x8057[248]](_0x8057[867],s[_0x8057[636]],$s);last=s;} );} else {if(nodes[i][_0x8057[311]]==_0x8057[849]){if(last!=nodes[i]){this[_0x8057[36]][_0x8057[248]](_0x8057[867],nodes[i][_0x8057[636]],$(nodes[i]));last=nodes[i];} ;} ;} ;} ;} ;} ;if(key===this[_0x8057[711]][_0x8057[976]]){this[_0x8057[44]][_0x8057[978]]();this[_0x8057[44]][_0x8057[979]](e);} ;this[_0x8057[35]][_0x8057[270]]();} ,checkEvents:function (arrow,key){if(!arrow&&(this[_0x8057[36]][_0x8057[427]]()==_0x8057[428]||this[_0x8057[36]][_0x8057[427]]()==_0x8057[429])){this[_0x8057[36]][_0x8057[430]](false);if(this[_0x8057[44]][_0x8057[980]](key)){this[_0x8057[30]][_0x8057[256]]();} ;} ;} ,checkKeyEvents:function (key){var k=this[_0x8057[711]];var keys=[k[_0x8057[976]],k[_0x8057[977]],k[_0x8057[960]],k[_0x8057[981]],k[_0x8057[982]],k[_0x8057[974]],k[_0x8057[983]],k[_0x8057[984]],k[_0x8057[985]],k[_0x8057[986]]];return ($[_0x8057[306]](key,keys)==-1)?true:false;} ,addArrowsEvent:function (arrow){if(!arrow){return ;} ;if((this[_0x8057[36]][_0x8057[427]]()==_0x8057[428]||this[_0x8057[36]][_0x8057[427]]()==_0x8057[429])){this[_0x8057[36]][_0x8057[430]](false);return ;} ;this[_0x8057[36]][_0x8057[430]](_0x8057[429]);} ,setupBuffer:function (e,key){if(this[_0x8057[44]][_0x8057[945]]&&key===90&&!e[_0x8057[961]]&&!e[_0x8057[987]]&&this[_0x8057[63]][_0x8057[30]][_0x8057[21]]){e[_0x8057[413]]();this[_0x8057[30]][_0x8057[367]]();return ;} else {if(this[_0x8057[44]][_0x8057[945]]&&key===90&&e[_0x8057[961]]&&!e[_0x8057[987]]&&this[_0x8057[63]][_0x8057[370]][_0x8057[21]]!==0){e[_0x8057[413]]();this[_0x8057[30]][_0x8057[372]]();return ;} else {if(!this[_0x8057[44]][_0x8057[945]]){if(key==this[_0x8057[711]][_0x8057[976]]||key==this[_0x8057[711]][_0x8057[977]]||(key==this[_0x8057[711]][_0x8057[960]]&&!e[_0x8057[946]]&&!e[_0x8057[961]])||key==this[_0x8057[711]][_0x8057[981]]){this[_0x8057[30]][_0x8057[256]]();} ;} ;} ;} ;} ,setupSelectAll:function (e,key){if(this[_0x8057[44]][_0x8057[945]]&&key===65){this[_0x8057[62]][_0x8057[988]]();} else {if(key!=this[_0x8057[711]][_0x8057[989]]&&!this[_0x8057[44]][_0x8057[945]]){this[_0x8057[62]][_0x8057[431]]();} ;} ;} ,onArrowDown:function (){var tags=[this[_0x8057[44]][_0x8057[78]],this[_0x8057[44]][_0x8057[79]],this[_0x8057[44]][_0x8057[950]]];for(var i=0;i<tags[_0x8057[21]];i++){if(tags[i]){this[_0x8057[44]][_0x8057[990]](tags[i]);return false;} ;} ;} ,onShiftEnter:function (e){this[_0x8057[30]][_0x8057[256]]();if(this[_0x8057[62]][_0x8057[958]]()){return this[_0x8057[44]][_0x8057[966]](e);} ;return this[_0x8057[44]][_0x8057[965]](e);} ,onTab:function (e,key){if(!this[_0x8057[63]][_0x8057[991]]){return true;} ;if(this[_0x8057[62]][_0x8057[293]](this[_0x8057[35]][_0x8057[292]]())&&this[_0x8057[63]][_0x8057[992]]===false){return true;} ;e[_0x8057[413]]();var node;if(this[_0x8057[44]][_0x8057[79]]&&!e[_0x8057[961]]){node=(this[_0x8057[63]][_0x8057[649]])?document[_0x8057[994]](Array(this[_0x8057[63]][_0x8057[649]]+1)[_0x8057[241]](_0x8057[993])):document[_0x8057[994]](_0x8057[585]);this[_0x8057[43]][_0x8057[895]](node);this[_0x8057[35]][_0x8057[270]]();} else {if(this[_0x8057[63]][_0x8057[992]]!==false){node=document[_0x8057[994]](Array(this[_0x8057[63]][_0x8057[992]]+1)[_0x8057[241]](_0x8057[993]));this[_0x8057[43]][_0x8057[895]](node);this[_0x8057[35]][_0x8057[270]]();} else {if(e[_0x8057[947]]&&key===219){this[_0x8057[41]][_0x8057[995]]();} else {if(e[_0x8057[947]]&&key===221){this[_0x8057[41]][_0x8057[996]]();} else {if(!e[_0x8057[961]]){this[_0x8057[41]][_0x8057[996]]();} else {this[_0x8057[41]][_0x8057[995]]();} ;} ;} ;} ;} ;return false;} ,replaceDivToBreakLine:function (){var blockElem=this[_0x8057[56]][_0x8057[323]]();var blockHtml=blockElem[_0x8057[514]][_0x8057[541]](/<br\s?\/?>/gi,_0x8057[255]);if((blockElem[_0x8057[311]]===_0x8057[144]||blockElem[_0x8057[311]]===_0x8057[134])&&blockHtml===_0x8057[255]&&!$(blockElem)[_0x8057[448]](_0x8057[396])){var br=document[_0x8057[652]](_0x8057[625]);$(blockElem)[_0x8057[284]](br);this[_0x8057[33]][_0x8057[997]](br);this[_0x8057[35]][_0x8057[270]]();return false;} ;} ,replaceDivToParagraph:function (){var blockElem=this[_0x8057[56]][_0x8057[323]]();var blockHtml=blockElem[_0x8057[514]][_0x8057[541]](/<br\s?\/?>/gi,_0x8057[255]);if(blockElem[_0x8057[311]]===_0x8057[144]&&blockHtml===_0x8057[255]&&!$(blockElem)[_0x8057[448]](_0x8057[396])){var p=document[_0x8057[652]](_0x8057[77]);p[_0x8057[514]]=this[_0x8057[63]][_0x8057[515]];$(blockElem)[_0x8057[284]](p);this[_0x8057[33]][_0x8057[454]](p);this[_0x8057[35]][_0x8057[270]]();return false;} else {if(this[_0x8057[63]][_0x8057[998]]&&blockElem[_0x8057[311]]==_0x8057[134]){$(blockElem)[_0x8057[346]](_0x8057[301])[_0x8057[346]](_0x8057[90]);} ;} ;} ,insertParagraph:function (e){e[_0x8057[413]]();this[_0x8057[56]][_0x8057[292]]();var p=document[_0x8057[652]](_0x8057[77]);p[_0x8057[514]]=this[_0x8057[63]][_0x8057[515]];this[_0x8057[313]][_0x8057[916]]();this[_0x8057[313]][_0x8057[920]](p);this[_0x8057[33]][_0x8057[454]](p);this[_0x8057[35]][_0x8057[270]]();return false;} ,exitFromBlockquote:function (e){if(!this[_0x8057[62]][_0x8057[958]]()){return ;} ;var tmp=$[_0x8057[390]]($(this[_0x8057[44]][_0x8057[29]])[_0x8057[68]]());if(tmp[_0x8057[10]](/(<br\s?\/?>){2}$/i)!=-1){e[_0x8057[413]]();if(this[_0x8057[63]][_0x8057[282]]){var br=document[_0x8057[652]](_0x8057[625]);$(this[_0x8057[44]][_0x8057[78]])[_0x8057[508]](br);this[_0x8057[33]][_0x8057[997]](br);$(this[_0x8057[44]][_0x8057[29]])[_0x8057[68]](tmp[_0x8057[541]](/<br\s?\/?>$/i,_0x8057[255]));} else {var node=$(this[_0x8057[63]][_0x8057[517]]);$(this[_0x8057[44]][_0x8057[78]])[_0x8057[508]](node);this[_0x8057[33]][_0x8057[454]](node);} ;return true;} ;return ;} ,insertAfterLastElement:function (element){if(!this[_0x8057[62]][_0x8057[958]]()){return ;} ;this[_0x8057[30]][_0x8057[256]]();if(this[_0x8057[63]][_0x8057[282]]){var contents=$(_0x8057[548])[_0x8057[327]]($[_0x8057[390]](this[_0x8057[261]][_0x8057[68]]()))[_0x8057[336]]();var last=contents[_0x8057[771]]()[0];if(last[_0x8057[311]]==_0x8057[892]&&last[_0x8057[514]]===_0x8057[255]){last=contents[_0x8057[521]]()[0];} ;if(this[_0x8057[62]][_0x8057[814]](last)!=this[_0x8057[62]][_0x8057[814]](element)){return ;} ;var br=document[_0x8057[652]](_0x8057[625]);$(element)[_0x8057[508]](br);this[_0x8057[33]][_0x8057[893]](br);} else {if(this[_0x8057[261]][_0x8057[336]]()[_0x8057[771]]()[0]!==element){return ;} ;var node=$(this[_0x8057[63]][_0x8057[517]]);$(element)[_0x8057[508]](node);this[_0x8057[33]][_0x8057[454]](node);} ;} ,insertNewLine:function (e){e[_0x8057[413]]();var node=document[_0x8057[994]](_0x8057[618]);this[_0x8057[56]][_0x8057[292]]();this[_0x8057[313]][_0x8057[916]]();this[_0x8057[313]][_0x8057[920]](node);this[_0x8057[33]][_0x8057[893]](node);this[_0x8057[35]][_0x8057[270]]();return false;} ,insertBreakLine:function (e){return this[_0x8057[44]][_0x8057[999]](e);} ,insertDblBreakLine:function (e){return this[_0x8057[44]][_0x8057[999]](e,true);} ,insertBreakLineProcessing:function (e,dbl){e[_0x8057[749]]();this[_0x8057[56]][_0x8057[292]]();var br1=document[_0x8057[652]](_0x8057[625]);this[_0x8057[313]][_0x8057[916]]();this[_0x8057[313]][_0x8057[920]](br1);if(dbl===true){var br2=document[_0x8057[652]](_0x8057[625]);this[_0x8057[313]][_0x8057[920]](br2);this[_0x8057[33]][_0x8057[893]](br2);} else {this[_0x8057[33]][_0x8057[893]](br1);} ;this[_0x8057[35]][_0x8057[270]]();return false;} ,removeInvisibleSpace:function (){var $current=$(this[_0x8057[44]][_0x8057[948]]);if($current[_0x8057[770]]()[_0x8057[10]](/^\u200B$/g)===0){$current[_0x8057[331]]();} ;} ,removeEmptyListInTable:function (e){var $current=$(this[_0x8057[44]][_0x8057[948]]);var $parent=$(this[_0x8057[44]][_0x8057[509]]);var td=$current[_0x8057[352]](_0x8057[340]);if(td[_0x8057[21]]!==0&&$current[_0x8057[352]](_0x8057[128])&&$parent[_0x8057[768]](_0x8057[128])[_0x8057[21]]===1){if(!this[_0x8057[62]][_0x8057[293]]($current[_0x8057[770]]())){return ;} ;e[_0x8057[413]]();$current[_0x8057[331]]();$parent[_0x8057[331]]();this[_0x8057[33]][_0x8057[454]](td);} ;} };} ,keyup:function (){return {init:function (e){if(this[_0x8057[231]]){return ;} ;var key=e[_0x8057[944]];this[_0x8057[45]][_0x8057[948]]=this[_0x8057[56]][_0x8057[883]]();this[_0x8057[45]][_0x8057[509]]=this[_0x8057[56]][_0x8057[911]]();var $parent=this[_0x8057[62]][_0x8057[774]]($(this[_0x8057[45]][_0x8057[509]])[_0x8057[509]]());var keyupStop=this[_0x8057[36]][_0x8057[248]](_0x8057[45],e);if(keyupStop===false){e[_0x8057[413]]();return false;} ;if(!this[_0x8057[63]][_0x8057[282]]&&this[_0x8057[45]][_0x8057[948]][_0x8057[769]]==3&&this[_0x8057[45]][_0x8057[948]][_0x8057[21]]<=1&&(this[_0x8057[45]][_0x8057[509]]===false||this[_0x8057[45]][_0x8057[509]][_0x8057[311]]==_0x8057[1000])){this[_0x8057[45]][_0x8057[1001]]();} ;if(!this[_0x8057[63]][_0x8057[282]]&&this[_0x8057[62]][_0x8057[774]](this[_0x8057[45]][_0x8057[948]])&&this[_0x8057[45]][_0x8057[948]][_0x8057[311]]===_0x8057[144]){this[_0x8057[45]][_0x8057[1001]](false);} ;if(!this[_0x8057[63]][_0x8057[282]]&&$(this[_0x8057[45]][_0x8057[509]])[_0x8057[448]](_0x8057[967])&&($parent===false||$parent[0][_0x8057[311]]==_0x8057[1000])){$(this[_0x8057[45]][_0x8057[509]])[_0x8057[336]]()[_0x8057[335]]();this[_0x8057[45]][_0x8057[1001]]();} ;if(this[_0x8057[45]][_0x8057[1002]](key)){this[_0x8057[1008]](this[_0x8057[63]][_0x8057[812]],this[_0x8057[63]][_0x8057[1003]],this[_0x8057[63]][_0x8057[1004]],this[_0x8057[63]][_0x8057[1005]],this[_0x8057[63]][_0x8057[1006]],this[_0x8057[63]][_0x8057[1007]]);this[_0x8057[51]][_0x8057[244]]();this[_0x8057[35]][_0x8057[270]]();} ;if(key===this[_0x8057[711]][_0x8057[977]]||key===this[_0x8057[711]][_0x8057[976]]){this[_0x8057[34]][_0x8057[353]]();if(this[_0x8057[51]][_0x8057[40]]){e[_0x8057[413]]();this[_0x8057[40]][_0x8057[807]]();this[_0x8057[30]][_0x8057[256]]();this[_0x8057[40]][_0x8057[331]](this[_0x8057[51]][_0x8057[40]]);this[_0x8057[51]][_0x8057[40]]=false;return false;} ;this[_0x8057[261]][_0x8057[332]](_0x8057[77])[_0x8057[20]]($[_0x8057[281]](this[_0x8057[62]][_0x8057[355]],this));if(this[_0x8057[63]][_0x8057[282]]&&this[_0x8057[45]][_0x8057[948]]&&this[_0x8057[45]][_0x8057[948]][_0x8057[311]]==_0x8057[144]&&this[_0x8057[62]][_0x8057[293]](this[_0x8057[45]][_0x8057[948]][_0x8057[514]])){$(this[_0x8057[45]][_0x8057[948]])[_0x8057[508]](this[_0x8057[56]][_0x8057[1009]]());this[_0x8057[56]][_0x8057[269]]();$(this[_0x8057[45]][_0x8057[948]])[_0x8057[331]]();} ;return this[_0x8057[45]][_0x8057[1010]](e);} ;} ,isLinkify:function (key){return this[_0x8057[63]][_0x8057[1003]]&&(this[_0x8057[63]][_0x8057[1004]]||this[_0x8057[63]][_0x8057[1005]]||this[_0x8057[63]][_0x8057[1006]])&&key===this[_0x8057[711]][_0x8057[960]]&&!this[_0x8057[62]][_0x8057[590]](_0x8057[155]);} ,replaceToParagraph:function (clone){var $current=$(this[_0x8057[45]][_0x8057[948]]);var node;if(clone===false){node=$(_0x8057[619])[_0x8057[327]]($current[_0x8057[68]]());} else {node=$(_0x8057[619])[_0x8057[327]]($current[_0x8057[1011]]());} ;$current[_0x8057[284]](node);var next=$(node)[_0x8057[519]]();if( typeof (next[0])!==_0x8057[14]&&next[0][_0x8057[311]]==_0x8057[516]){next[_0x8057[331]]();} ;this[_0x8057[33]][_0x8057[456]](node);} ,formatEmpty:function (e){var html=$[_0x8057[390]](this[_0x8057[261]][_0x8057[68]]());if(!this[_0x8057[62]][_0x8057[293]](html)){return ;} ;e[_0x8057[413]]();if(this[_0x8057[63]][_0x8057[282]]){this[_0x8057[261]][_0x8057[68]](this[_0x8057[56]][_0x8057[1009]]());this[_0x8057[56]][_0x8057[269]]();} else {html=_0x8057[1012];this[_0x8057[261]][_0x8057[68]](html);this[_0x8057[39]][_0x8057[454]]();} ;this[_0x8057[35]][_0x8057[270]]();return false;} };} ,lang:function (){return {load:function (){this[_0x8057[63]][_0x8057[1013]]=this[_0x8057[63]][_0x8057[1014]][this[_0x8057[63]][_0x8057[46]]];} ,get:function (name){return ( typeof this[_0x8057[63]][_0x8057[1013]][name]!=_0x8057[14])?this[_0x8057[63]][_0x8057[1013]][name]:_0x8057[255];} };} ,line:function (){return {insert:function (){this[_0x8057[30]][_0x8057[256]]();var blocks=this[_0x8057[56]][_0x8057[264]]();if(blocks[0]!==false&&this[_0x8057[47]][_0x8057[1015]](blocks)){if(!this[_0x8057[62]][_0x8057[260]](_0x8057[259])){this[_0x8057[261]][_0x8057[39]]();} ;return ;} ;if(this[_0x8057[62]][_0x8057[260]](_0x8057[259])){this[_0x8057[47]][_0x8057[1016]]();} else {this[_0x8057[47]][_0x8057[1017]]();} ;} ,isExceptLastOrFirst:function (blocks){var exceptTags=[_0x8057[128],_0x8057[340],_0x8057[341],_0x8057[78],_0x8057[950],_0x8057[79],_0x8057[342],_0x8057[343],_0x8057[344]];var first=blocks[0][_0x8057[311]][_0x8057[325]]();var last=this[_0x8057[56]][_0x8057[1018]]();last=( typeof last==_0x8057[14])?first:last[_0x8057[311]][_0x8057[325]]();var firstFound=$[_0x8057[306]](first,exceptTags)!=-1;var lastFound=$[_0x8057[306]](last,exceptTags)!=-1;if((firstFound&&lastFound)||firstFound){return true;} ;} ,insertInIe:function (){this[_0x8057[62]][_0x8057[1019]]();this[_0x8057[30]][_0x8057[256]]();this[_0x8057[43]][_0x8057[895]](document[_0x8057[652]](_0x8057[1020]));this[_0x8057[62]][_0x8057[1021]]();this[_0x8057[35]][_0x8057[270]]();} ,insertInOthersBrowsers:function (){this[_0x8057[30]][_0x8057[256]]();var extra=_0x8057[1022];if(this[_0x8057[63]][_0x8057[282]]){extra=_0x8057[1023];} ;document[_0x8057[322]](_0x8057[923],false,_0x8057[1024]+extra);this[_0x8057[47]][_0x8057[1025]]();this[_0x8057[35]][_0x8057[270]]();} ,setFocus:function (){var node=this[_0x8057[261]][_0x8057[332]](_0x8057[1026]);var next=$(node)[_0x8057[519]]()[0];if(next){this[_0x8057[33]][_0x8057[893]](node);node[_0x8057[331]]();} else {node[_0x8057[346]](_0x8057[386]);} ;} };} ,link:function (){return {show:function (e){if( typeof e!=_0x8057[14]&&e[_0x8057[413]]){e[_0x8057[413]]();} ;this[_0x8057[50]][_0x8057[244]](_0x8057[48],this[_0x8057[46]][_0x8057[292]](_0x8057[1027]),600);this[_0x8057[50]][_0x8057[780]]();this[_0x8057[48]][_0x8057[1028]]=this[_0x8057[50]][_0x8057[785]](this[_0x8057[46]][_0x8057[292]](_0x8057[43]));this[_0x8057[56]][_0x8057[292]]();this[_0x8057[48]][_0x8057[1029]]();this[_0x8057[48]][_0x8057[1030]]();if(this[_0x8057[48]][_0x8057[442]]==_0x8057[792]){$(_0x8057[1031])[_0x8057[794]](_0x8057[793],true);} ;this[_0x8057[48]][_0x8057[1032]]=$(_0x8057[1033]);this[_0x8057[48]][_0x8057[1034]]=$(_0x8057[1035]);this[_0x8057[48]][_0x8057[1034]][_0x8057[388]](this[_0x8057[48]][_0x8057[770]]);this[_0x8057[48]][_0x8057[1032]][_0x8057[388]](this[_0x8057[48]][_0x8057[1036]]);this[_0x8057[48]][_0x8057[1028]][_0x8057[425]](_0x8057[428],$[_0x8057[281]](this[_0x8057[48]][_0x8057[43]],this));$(_0x8057[1037])[_0x8057[331]]();this[_0x8057[56]][_0x8057[262]]();this[_0x8057[50]][_0x8057[481]]();this[_0x8057[48]][_0x8057[1032]][_0x8057[39]]();} ,cleanUrl:function (){var thref=self[_0x8057[1038]][_0x8057[791]][_0x8057[541]](/\/$/i,_0x8057[255]);this[_0x8057[48]][_0x8057[1036]]=this[_0x8057[48]][_0x8057[1036]][_0x8057[541]](thref,_0x8057[255]);this[_0x8057[48]][_0x8057[1036]]=this[_0x8057[48]][_0x8057[1036]][_0x8057[541]](/^\/#/,_0x8057[1039]);this[_0x8057[48]][_0x8057[1036]]=this[_0x8057[48]][_0x8057[1036]][_0x8057[541]](_0x8057[1040],_0x8057[255]);if(!this[_0x8057[63]][_0x8057[812]]){var re= new RegExp(_0x8057[810]+self[_0x8057[1038]][_0x8057[1041]],_0x8057[123]);this[_0x8057[48]][_0x8057[1036]]=this[_0x8057[48]][_0x8057[1036]][_0x8057[541]](re,_0x8057[255]);} ;} ,getData:function (){this[_0x8057[48]][_0x8057[1042]]=false;var $el=$(this[_0x8057[56]][_0x8057[883]]())[_0x8057[352]](_0x8057[118]);if($el[_0x8057[21]]!==0&&$el[0][_0x8057[311]]===_0x8057[589]){this[_0x8057[48]][_0x8057[1042]]=$el;this[_0x8057[48]][_0x8057[1036]]=$el[_0x8057[276]](_0x8057[791]);this[_0x8057[48]][_0x8057[770]]=$el[_0x8057[770]]();this[_0x8057[48]][_0x8057[442]]=$el[_0x8057[276]](_0x8057[442]);} else {this[_0x8057[48]][_0x8057[770]]=this[_0x8057[755]].toString();this[_0x8057[48]][_0x8057[1036]]=_0x8057[255];this[_0x8057[48]][_0x8057[442]]=_0x8057[255];} ;} ,insert:function (){var target=_0x8057[255];var link=this[_0x8057[48]][_0x8057[1032]][_0x8057[388]]();var text=this[_0x8057[48]][_0x8057[1034]][_0x8057[388]]();if($[_0x8057[390]](link)===_0x8057[255]){this[_0x8057[48]][_0x8057[1032]][_0x8057[347]](_0x8057[1043])[_0x8057[425]](_0x8057[45],function (){$(this)[_0x8057[350]](_0x8057[1043]);$(this)[_0x8057[708]](_0x8057[45]);} );return ;} ;if(link[_0x8057[10]](_0x8057[1044])!=-1&&/(http|ftp|https):\/\//i[_0x8057[604]](link)===false){link=_0x8057[1040]+link;} else {if(link[_0x8057[10]](_0x8057[1039])!==0){if($(_0x8057[1031])[_0x8057[794]](_0x8057[793])){target=_0x8057[792];} ;var pattern=_0x8057[809];var re= new RegExp(_0x8057[810]+pattern,_0x8057[123]);var re2= new RegExp(_0x8057[811]+pattern,_0x8057[123]);var re3= new RegExp(_0x8057[1045],_0x8057[123]);if(link[_0x8057[10]](re)==-1&&link[_0x8057[10]](re3)==-1&&link[_0x8057[10]](re2)===0&&this[_0x8057[63]][_0x8057[812]]){link=this[_0x8057[63]][_0x8057[812]]+_0x8057[813]+link;} ;} ;} ;this[_0x8057[48]][_0x8057[256]](text,link,target);this[_0x8057[50]][_0x8057[757]]();} ,set:function (text,link,target){text=$[_0x8057[390]](text[_0x8057[541]](/<|>/g,_0x8057[255]));this[_0x8057[56]][_0x8057[269]]();if(text===_0x8057[255]&&link===_0x8057[255]){return ;} ;if(text===_0x8057[255]&&link!==_0x8057[255]){text=link;} ;if(this[_0x8057[48]][_0x8057[1042]]){this[_0x8057[30]][_0x8057[256]]();this[_0x8057[48]][_0x8057[1042]][_0x8057[770]](text)[_0x8057[276]](_0x8057[791],link);if(target!==_0x8057[255]){this[_0x8057[48]][_0x8057[1042]][_0x8057[276]](_0x8057[442],target);} else {this[_0x8057[48]][_0x8057[1042]][_0x8057[346]](_0x8057[442]);} ;this[_0x8057[35]][_0x8057[270]]();} else {if(this[_0x8057[62]][_0x8057[260]](_0x8057[459])&&this[_0x8057[48]][_0x8057[770]]===_0x8057[255]){var $a=$(_0x8057[1046])[_0x8057[276]](_0x8057[791],link)[_0x8057[770]](text);if(target!==_0x8057[255]){$a[_0x8057[276]](_0x8057[442],target);} ;this[_0x8057[43]][_0x8057[895]]($a);this[_0x8057[56]][_0x8057[1047]]($a);} else {var $a;if(this[_0x8057[62]][_0x8057[260]](_0x8057[259])){$a=$(_0x8057[760]+link+_0x8057[730])[_0x8057[770]](text);if(target!==_0x8057[255]){$a[_0x8057[276]](_0x8057[442],target);} ;$a=$(this[_0x8057[43]][_0x8057[895]]($a));this[_0x8057[56]][_0x8057[1047]]($a);} else {document[_0x8057[322]](_0x8057[1048],false,link);$a=$(this[_0x8057[56]][_0x8057[883]]())[_0x8057[352]](_0x8057[118]);if(target!==_0x8057[255]){$a[_0x8057[276]](_0x8057[442],target);} ;$a[_0x8057[346]](_0x8057[90]);if(this[_0x8057[48]][_0x8057[770]]===_0x8057[255]||this[_0x8057[48]][_0x8057[770]]!=text){$a[_0x8057[770]](text);this[_0x8057[56]][_0x8057[1047]]($a);} ;} ;} ;this[_0x8057[35]][_0x8057[270]]();this[_0x8057[36]][_0x8057[248]](_0x8057[1049],$a);} ;setTimeout($[_0x8057[281]](function (){this[_0x8057[51]][_0x8057[1050]]();} ,this),5);} ,unlink:function (e){if( typeof e!=_0x8057[14]&&e[_0x8057[413]]){e[_0x8057[413]]();} ;var nodes=this[_0x8057[56]][_0x8057[913]]();if(!nodes){return ;} ;this[_0x8057[30]][_0x8057[256]]();var len=nodes[_0x8057[21]];for(var i=0;i<len;i++){if(nodes[i][_0x8057[311]]==_0x8057[589]){var $node=$(nodes[i]);$node[_0x8057[284]]($node[_0x8057[336]]());} ;} ;$(_0x8057[1037])[_0x8057[331]]();this[_0x8057[35]][_0x8057[270]]();} };} ,list:function (){return {toggle:function (cmd){this[_0x8057[54]][_0x8057[331]]();if(!this[_0x8057[62]][_0x8057[260]](_0x8057[259])){this[_0x8057[261]][_0x8057[39]]();} ;this[_0x8057[30]][_0x8057[256]]();this[_0x8057[56]][_0x8057[262]]();var parent=this[_0x8057[56]][_0x8057[911]]();var $list=$(parent)[_0x8057[352]](_0x8057[1051]);if(!this[_0x8057[62]][_0x8057[774]]($list)&&$list[_0x8057[21]]!==0){$list=false;} ;var isUnorderedCmdOrdered,isOrderedCmdUnordered;var remove=false;if($list&&$list[_0x8057[21]]){remove=true;var listTag=$list[0][_0x8057[311]];isUnorderedCmdOrdered=(cmd===_0x8057[74]&&listTag===_0x8057[156]);isOrderedCmdUnordered=(cmd===_0x8057[73]&&listTag===_0x8057[157]);} ;if(isUnorderedCmdOrdered){this[_0x8057[62]][_0x8057[330]]($list,_0x8057[127]);} else {if(isOrderedCmdUnordered){this[_0x8057[62]][_0x8057[330]]($list,_0x8057[126]);} else {if(remove){this[_0x8057[49]][_0x8057[331]](cmd);} else {this[_0x8057[49]][_0x8057[43]](cmd);} ;} ;} ;this[_0x8057[56]][_0x8057[269]]();this[_0x8057[35]][_0x8057[270]]();} ,insert:function (cmd){var parent=this[_0x8057[56]][_0x8057[911]]();var current=this[_0x8057[56]][_0x8057[883]]();var $td=$(current)[_0x8057[352]](_0x8057[1052]);if(this[_0x8057[62]][_0x8057[260]](_0x8057[259])&&this[_0x8057[63]][_0x8057[282]]){this[_0x8057[49]][_0x8057[1016]](cmd);} else {document[_0x8057[322]](_0x8057[43]+cmd);} ;var $list=$(this[_0x8057[56]][_0x8057[911]]())[_0x8057[352]](_0x8057[1051]);if($td[_0x8057[21]]!==0){var prev=$td[_0x8057[521]]();var html=$td[_0x8057[68]]();$td[_0x8057[68]](_0x8057[255]);if(prev&&prev[_0x8057[21]]===1&&(prev[0][_0x8057[311]]===_0x8057[145]||prev[0][_0x8057[311]]===_0x8057[318])){$(prev)[_0x8057[508]]($td);} else {$(parent)[_0x8057[328]]($td);} ;$td[_0x8057[68]](html);} ;if(this[_0x8057[62]][_0x8057[293]]($list[_0x8057[332]](_0x8057[128])[_0x8057[770]]())){var $children=$list[_0x8057[768]](_0x8057[128]);$children[_0x8057[332]](_0x8057[625])[_0x8057[331]]();$children[_0x8057[327]](this[_0x8057[56]][_0x8057[1009]]());} ;if($list[_0x8057[21]]){var $listParent=$list[_0x8057[509]]();if(this[_0x8057[62]][_0x8057[774]]($listParent)&&$listParent[0][_0x8057[311]]!=_0x8057[158]&&this[_0x8057[62]][_0x8057[511]]($listParent[0])){$listParent[_0x8057[284]]($listParent[_0x8057[336]]());} ;} ;if(!this[_0x8057[62]][_0x8057[260]](_0x8057[259])){this[_0x8057[261]][_0x8057[39]]();} ;this[_0x8057[34]][_0x8057[353]]();} ,insertInIe:function (cmd){var wrapper=this[_0x8057[56]][_0x8057[272]](_0x8057[271]);var wrapperHtml=$(wrapper)[_0x8057[68]]();var tmpList=(cmd==_0x8057[74])?$(_0x8057[1053]):$(_0x8057[1054]);var tmpLi=$(_0x8057[507]);if($[_0x8057[390]](wrapperHtml)===_0x8057[255]){tmpLi[_0x8057[327]](this[_0x8057[56]][_0x8057[1009]]());tmpList[_0x8057[327]](tmpLi);this[_0x8057[261]][_0x8057[332]](_0x8057[1055])[_0x8057[284]](tmpList);} else {var items=wrapperHtml[_0x8057[13]](/<br\s?\/?>/gi);if(items){for(var i=0;i<items[_0x8057[21]];i++){if($[_0x8057[390]](items[i])!==_0x8057[255]){tmpList[_0x8057[327]]($(_0x8057[507])[_0x8057[68]](items[i]));} ;} ;} else {tmpLi[_0x8057[327]](wrapperHtml);tmpList[_0x8057[327]](tmpLi);} ;$(wrapper)[_0x8057[284]](tmpList);} ;} ,remove:function (cmd){document[_0x8057[322]](_0x8057[43]+cmd);var $current=$(this[_0x8057[56]][_0x8057[883]]());this[_0x8057[41]][_0x8057[877]]();if(!this[_0x8057[63]][_0x8057[282]]&&$current[_0x8057[352]](_0x8057[1056])[_0x8057[21]]===0){document[_0x8057[322]](_0x8057[319],false,_0x8057[77]);this[_0x8057[261]][_0x8057[332]](_0x8057[1057])[_0x8057[20]]($[_0x8057[281]](this[_0x8057[62]][_0x8057[355]],this));} ;var $table=$(this[_0x8057[56]][_0x8057[883]]())[_0x8057[352]](_0x8057[362]);var $prev=$table[_0x8057[521]]();if(!this[_0x8057[63]][_0x8057[282]]&&$table[_0x8057[21]]!==0&&$prev[_0x8057[21]]!==0&&$prev[0][_0x8057[311]]==_0x8057[516]){$prev[_0x8057[331]]();} ;this[_0x8057[34]][_0x8057[353]]();} };} ,modal:function (){return {callbacks:{},loadTemplates:function (){this[_0x8057[63]][_0x8057[50]]={imageEdit:String()+_0x8057[1058]+_0x8057[1059]+this[_0x8057[46]][_0x8057[292]](_0x8057[471])+_0x8057[1060]+_0x8057[1061]+_0x8057[1062]+this[_0x8057[46]][_0x8057[292]](_0x8057[48])+_0x8057[1060]+_0x8057[1063]+_0x8057[1064]+this[_0x8057[46]][_0x8057[292]](_0x8057[1065])+_0x8057[1060]+_0x8057[1066]+this[_0x8057[46]][_0x8057[292]](_0x8057[1067])+_0x8057[1060]+_0x8057[1068]+_0x8057[1069]+this[_0x8057[46]][_0x8057[292]](_0x8057[800])+_0x8057[1070]+_0x8057[1071]+this[_0x8057[46]][_0x8057[292]](_0x8057[485])+_0x8057[1070]+_0x8057[1072]+this[_0x8057[46]][_0x8057[292]](_0x8057[258])+_0x8057[1070]+_0x8057[1073]+this[_0x8057[46]][_0x8057[292]](_0x8057[257])+_0x8057[1070]+_0x8057[1074]+_0x8057[1075],image:String()+_0x8057[1076]+_0x8057[1077]+_0x8057[1075],file:String()+_0x8057[1078]+_0x8057[1079]+_0x8057[1059]+this[_0x8057[46]][_0x8057[292]](_0x8057[759])+_0x8057[1060]+_0x8057[1080]+_0x8057[1081]+_0x8057[1082]+_0x8057[1075],link:String()+_0x8057[1083]+_0x8057[1084]+_0x8057[1085]+_0x8057[1059]+this[_0x8057[46]][_0x8057[292]](_0x8057[770])+_0x8057[1060]+_0x8057[1086]+_0x8057[1087]+this[_0x8057[46]][_0x8057[292]](_0x8057[1065])+_0x8057[1060]+_0x8057[1075]};$[_0x8057[246]](this[_0x8057[63]],this[_0x8057[63]][_0x8057[50]]);} ,addCallback:function (name,callback){this[_0x8057[50]][_0x8057[1088]][name]=callback;} ,createTabber:function ($modal){this[_0x8057[50]][_0x8057[1089]]=$(_0x8057[548])[_0x8057[276]](_0x8057[386],_0x8057[1090]);$modal[_0x8057[328]](this[_0x8057[50]].$tabber);} ,addTab:function (id,name,active){var $tab=$(_0x8057[1091]+id+_0x8057[730])[_0x8057[770]](name);if(active){$tab[_0x8057[347]](_0x8057[1092]);} ;var self=this;$tab[_0x8057[425]](_0x8057[428],function (e){e[_0x8057[413]]();$(_0x8057[1093])[_0x8057[397]]();$(_0x8057[1094]+$(this)[_0x8057[276]](_0x8057[505]))[_0x8057[481]]();self[_0x8057[50]][_0x8057[1089]][_0x8057[332]](_0x8057[118])[_0x8057[350]](_0x8057[1092]);$(this)[_0x8057[347]](_0x8057[1092]);} );this[_0x8057[50]][_0x8057[1089]][_0x8057[327]]($tab);} ,addTemplate:function (name,template){this[_0x8057[63]][_0x8057[50]][name]=template;} ,getTemplate:function (name){return this[_0x8057[63]][_0x8057[50]][name];} ,getModal:function (){return this[_0x8057[1096]][_0x8057[332]](_0x8057[1095]);} ,load:function (templateName,title,width){this[_0x8057[50]][_0x8057[1097]]=templateName;this[_0x8057[50]][_0x8057[738]]=width;this[_0x8057[50]][_0x8057[31]]();this[_0x8057[50]][_0x8057[1098]]();this[_0x8057[50]][_0x8057[1099]](title);this[_0x8057[50]][_0x8057[1100]]();this[_0x8057[50]][_0x8057[1101]]();if( typeof this[_0x8057[50]][_0x8057[1088]][templateName]!=_0x8057[14]){this[_0x8057[50]][_0x8057[1088]][templateName][_0x8057[6]](this);} ;} ,show:function (){if(this[_0x8057[62]][_0x8057[734]]()&&!this[_0x8057[62]][_0x8057[260]](_0x8057[259])){document[_0x8057[735]][_0x8057[446]]();} ;$(document[_0x8057[87]])[_0x8057[350]](_0x8057[747]);this[_0x8057[50]][_0x8057[1102]]=$(document[_0x8057[87]])[_0x8057[274]](_0x8057[1103]);$(document[_0x8057[87]])[_0x8057[274]](_0x8057[1103],_0x8057[1104]);if(this[_0x8057[62]][_0x8057[734]]()){this[_0x8057[50]][_0x8057[1105]]();} else {this[_0x8057[50]][_0x8057[1106]]();} ;this[_0x8057[723]][_0x8057[481]]();this[_0x8057[722]][_0x8057[481]]();this[_0x8057[50]][_0x8057[1107]]();this[_0x8057[62]][_0x8057[1019]]();if(!this[_0x8057[62]][_0x8057[734]]()){setTimeout($[_0x8057[281]](this[_0x8057[50]][_0x8057[1106]],this),0);$(window)[_0x8057[425]](_0x8057[1108],$[_0x8057[281]](this[_0x8057[50]][_0x8057[1109]],this));} ;this[_0x8057[36]][_0x8057[248]](_0x8057[1110],this[_0x8057[50]][_0x8057[1097]],this.$modal);$(document)[_0x8057[708]](_0x8057[1111]);this[_0x8057[1115]][_0x8057[332]](_0x8057[1114])[_0x8057[425]](_0x8057[1112],$[_0x8057[281]](this[_0x8057[50]][_0x8057[1113]],this));} ,showOnDesktop:function (){var height=this[_0x8057[1115]][_0x8057[1116]]();var windowHeight=$(window)[_0x8057[702]]();var windowWidth=$(window)[_0x8057[738]]();if(this[_0x8057[50]][_0x8057[738]]>windowWidth){this[_0x8057[1115]][_0x8057[274]]({width:_0x8057[1117],marginTop:(windowHeight/2-height/2)+_0x8057[484]});return ;} ;if(height>windowHeight){this[_0x8057[1115]][_0x8057[274]]({width:this[_0x8057[50]][_0x8057[738]]+_0x8057[484],marginTop:_0x8057[1118]});} else {this[_0x8057[1115]][_0x8057[274]]({width:this[_0x8057[50]][_0x8057[738]]+_0x8057[484],marginTop:(windowHeight/2-height/2)+_0x8057[484]});} ;} ,showOnMobile:function (){this[_0x8057[1115]][_0x8057[274]]({width:_0x8057[1117],marginTop:_0x8057[1119]});} ,resize:function (){if(this[_0x8057[62]][_0x8057[734]]()){this[_0x8057[50]][_0x8057[1105]]();} else {this[_0x8057[50]][_0x8057[1106]]();} ;} ,setTitle:function (title){this[_0x8057[1120]][_0x8057[68]](title);} ,setContent:function (){this[_0x8057[1096]][_0x8057[68]](this[_0x8057[50]][_0x8057[1121]](this[_0x8057[50]][_0x8057[1097]]));} ,setDraggable:function (){if( typeof $[_0x8057[5]][_0x8057[1122]]===_0x8057[14]){return ;} ;this[_0x8057[1115]][_0x8057[1122]]({handle:this[_0x8057[1120]]});this[_0x8057[1120]][_0x8057[274]](_0x8057[1123],_0x8057[1124]);} ,setEnter:function (e){if(e[_0x8057[944]]!=13){return ;} ;e[_0x8057[413]]();this[_0x8057[1115]][_0x8057[332]](_0x8057[1125])[_0x8057[428]]();} ,createCancelButton:function (){var button=$(_0x8057[1128])[_0x8057[347]](_0x8057[1127])[_0x8057[68]](this[_0x8057[46]][_0x8057[292]](_0x8057[1126]));button[_0x8057[425]](_0x8057[428],$[_0x8057[281]](this[_0x8057[50]][_0x8057[757]],this));this[_0x8057[1129]][_0x8057[327]](button);} ,createDeleteButton:function (label){return this[_0x8057[50]][_0x8057[1131]](label,_0x8057[1130]);} ,createActionButton:function (label){return this[_0x8057[50]][_0x8057[1131]](label,_0x8057[1132]);} ,createButton:function (label,className){var button=$(_0x8057[1128])[_0x8057[347]](_0x8057[1135])[_0x8057[347]](_0x8057[1133]+className+_0x8057[1134])[_0x8057[68]](label);this[_0x8057[1129]][_0x8057[327]](button);return button;} ,setButtonsWidth:function (){var buttons=this[_0x8057[1129]][_0x8057[332]](_0x8057[32]);var buttonsSize=buttons[_0x8057[21]];if(buttonsSize===0){return ;} ;buttons[_0x8057[274]](_0x8057[738],(100/buttonsSize)+_0x8057[1136]);} ,build:function (){this[_0x8057[50]][_0x8057[1137]]();this[_0x8057[722]]=$(_0x8057[1138])[_0x8057[397]]();this[_0x8057[1115]]=$(_0x8057[1139]);this[_0x8057[1120]]=$(_0x8057[1140]);this[_0x8057[1141]]=$(_0x8057[1143])[_0x8057[68]](_0x8057[1142]);this[_0x8057[1096]]=$(_0x8057[1144]);this[_0x8057[1129]]=$(_0x8057[1145]);this[_0x8057[1115]][_0x8057[327]](this.$modalHeader);this[_0x8057[1115]][_0x8057[327]](this.$modalClose);this[_0x8057[1115]][_0x8057[327]](this.$modalBody);this[_0x8057[1115]][_0x8057[327]](this.$modalFooter);this[_0x8057[722]][_0x8057[327]](this.$modal);this[_0x8057[722]][_0x8057[478]](document[_0x8057[87]]);} ,buildOverlay:function (){this[_0x8057[723]]=$(_0x8057[1146])[_0x8057[397]]();$(_0x8057[87])[_0x8057[328]](this.$modalOverlay);} ,enableEvents:function (){this[_0x8057[1141]][_0x8057[425]](_0x8057[1147],$[_0x8057[281]](this[_0x8057[50]][_0x8057[757]],this));$(document)[_0x8057[425]](_0x8057[1148],$[_0x8057[281]](this[_0x8057[50]][_0x8057[1149]],this));this[_0x8057[261]][_0x8057[425]](_0x8057[1148],$[_0x8057[281]](this[_0x8057[50]][_0x8057[1149]],this));this[_0x8057[722]][_0x8057[425]](_0x8057[1147],$[_0x8057[281]](this[_0x8057[50]][_0x8057[757]],this));} ,disableEvents:function (){this[_0x8057[1141]][_0x8057[708]](_0x8057[1147]);$(document)[_0x8057[708]](_0x8057[1148]);this[_0x8057[261]][_0x8057[708]](_0x8057[1148]);this[_0x8057[722]][_0x8057[708]](_0x8057[1147]);$(window)[_0x8057[708]](_0x8057[1108]);} ,closeHandler:function (e){if(e[_0x8057[944]]!=this[_0x8057[711]][_0x8057[982]]){return ;} ;this[_0x8057[50]][_0x8057[757]](false);} ,close:function (e){if(e){if(!$(e[_0x8057[442]])[_0x8057[448]](_0x8057[1150])&&e[_0x8057[442]]!=this[_0x8057[1141]][0]&&e[_0x8057[442]]!=this[_0x8057[722]][0]){return ;} ;e[_0x8057[413]]();} ;if(!this[_0x8057[722]]){return ;} ;this[_0x8057[50]][_0x8057[1151]]();this[_0x8057[723]][_0x8057[331]]();this[_0x8057[722]][_0x8057[1154]](_0x8057[1152],$[_0x8057[281]](function (){this[_0x8057[722]][_0x8057[331]]();setTimeout($[_0x8057[281]](this[_0x8057[62]][_0x8057[1021]],this),0);if(e!==undefined){this[_0x8057[56]][_0x8057[269]]();} ;$(document[_0x8057[87]])[_0x8057[274]](_0x8057[1103],this[_0x8057[50]][_0x8057[1102]]);this[_0x8057[36]][_0x8057[248]](_0x8057[1153],this[_0x8057[50]][_0x8057[1097]]);} ,this));} };} ,observe:function (){return {load:function (){this[_0x8057[51]][_0x8057[815]]();this[_0x8057[51]][_0x8057[1050]]();} ,buttons:function (e,btnName){var current=this[_0x8057[56]][_0x8057[883]]();var parent=this[_0x8057[56]][_0x8057[911]]();this[_0x8057[32]][_0x8057[1155]](btnName);if(e===false&&btnName!==_0x8057[68]){if($[_0x8057[306]](btnName,this[_0x8057[63]][_0x8057[1156]])!=-1){this[_0x8057[32]][_0x8057[1157]](btnName);} ;return ;} ;$[_0x8057[20]](this[_0x8057[63]][_0x8057[1158]],$[_0x8057[281]](function (key,value){var parentEl=$(parent)[_0x8057[352]](key);var currentEl=$(current)[_0x8057[352]](key);if(parentEl[_0x8057[21]]!==0&&!this[_0x8057[62]][_0x8057[774]](parentEl)){return ;} ;if(!this[_0x8057[62]][_0x8057[774]](currentEl)){return ;} ;if(parentEl[_0x8057[21]]!==0||currentEl[_0x8057[352]](key)[_0x8057[21]]!==0){this[_0x8057[32]][_0x8057[707]](value);} ;} ,this));var $parent=$(parent)[_0x8057[352]](this[_0x8057[63]][_0x8057[236]].toString()[_0x8057[325]]());if(this[_0x8057[62]][_0x8057[774]](parent)&&$parent[_0x8057[21]]){var align=($parent[_0x8057[274]](_0x8057[273])===_0x8057[255])?_0x8057[485]:$parent[_0x8057[274]](_0x8057[273]);this[_0x8057[32]][_0x8057[707]](_0x8057[1159]+align);} ;} ,addButton:function (tag,btnName){this[_0x8057[63]][_0x8057[1156]][_0x8057[16]](btnName);this[_0x8057[63]][_0x8057[1158]][tag]=btnName;} ,images:function (){this[_0x8057[261]][_0x8057[332]](_0x8057[119])[_0x8057[20]]($[_0x8057[281]](function (i,img){var $img=$(img);$img[_0x8057[352]](_0x8057[118])[_0x8057[425]](_0x8057[428],function (e){e[_0x8057[413]]();} );if(this[_0x8057[62]][_0x8057[260]](_0x8057[259])){$img[_0x8057[276]](_0x8057[1160],_0x8057[425]);} ;this[_0x8057[40]][_0x8057[1161]]($img);} ,this));$(document)[_0x8057[425]](_0x8057[1162],$[_0x8057[281]](function (e){this[_0x8057[51]][_0x8057[40]]=false;if(e[_0x8057[442]][_0x8057[311]]==_0x8057[849]&&this[_0x8057[62]][_0x8057[774]](e[_0x8057[442]])){this[_0x8057[51]][_0x8057[40]]=(this[_0x8057[51]][_0x8057[40]]&&this[_0x8057[51]][_0x8057[40]]==e[_0x8057[442]])?false:e[_0x8057[442]];} ;} ,this));} ,links:function (){if(!this[_0x8057[63]][_0x8057[1163]]){return ;} ;this[_0x8057[261]][_0x8057[332]](_0x8057[118])[_0x8057[425]](_0x8057[473],$[_0x8057[281]](this[_0x8057[51]][_0x8057[1164]],this));this[_0x8057[261]][_0x8057[425]](_0x8057[1165],$[_0x8057[281]](this[_0x8057[51]][_0x8057[1166]],this));$(document)[_0x8057[425]](_0x8057[1165],$[_0x8057[281]](this[_0x8057[51]][_0x8057[1166]],this));} ,getTooltipPosition:function ($link){return $link[_0x8057[480]]();} ,showTooltip:function (e){var $link=$(e[_0x8057[442]]);var $parent=$link[_0x8057[352]](_0x8057[118]);var tag=($link[_0x8057[21]]!==0)?$link[0][_0x8057[311]]:false;if($parent[0][_0x8057[311]]===_0x8057[589]){if(tag===_0x8057[849]){return ;} else {if(tag!==_0x8057[589]){$link=$parent;} ;} ;} ;if(tag!==_0x8057[589]){return ;} ;var pos=this[_0x8057[51]][_0x8057[1167]]($link);var tooltip=$(_0x8057[1168]);var href=$link[_0x8057[276]](_0x8057[791]);if(href===undefined){href=_0x8057[255];} ;if(href[_0x8057[21]]>24){href=href[_0x8057[713]](0,24)+_0x8057[1169];} ;var aLink=$(_0x8057[760]+$link[_0x8057[276]](_0x8057[791])+_0x8057[1171])[_0x8057[68]](href)[_0x8057[347]](_0x8057[1170]);var aEdit=$(_0x8057[1172])[_0x8057[68]](this[_0x8057[46]][_0x8057[292]](_0x8057[779]))[_0x8057[425]](_0x8057[428],$[_0x8057[281]](this[_0x8057[48]][_0x8057[481]],this))[_0x8057[347]](_0x8057[1170]);var aUnlink=$(_0x8057[1172])[_0x8057[68]](this[_0x8057[46]][_0x8057[292]](_0x8057[1173]))[_0x8057[425]](_0x8057[428],$[_0x8057[281]](this[_0x8057[48]][_0x8057[1173]],this))[_0x8057[347]](_0x8057[1170]);tooltip[_0x8057[327]](aLink)[_0x8057[327]](_0x8057[1174])[_0x8057[327]](aEdit)[_0x8057[327]](_0x8057[1174])[_0x8057[327]](aUnlink);tooltip[_0x8057[274]]({top:(pos[_0x8057[482]]+parseInt($link[_0x8057[274]](_0x8057[1175]),10))+_0x8057[484],left:pos[_0x8057[485]]+_0x8057[484]});$(_0x8057[1037])[_0x8057[331]]();$(_0x8057[87])[_0x8057[327]](tooltip);} ,closeTooltip:function (e){e=e[_0x8057[421]]||e;var target=e[_0x8057[442]];var $parent=$(target)[_0x8057[352]](_0x8057[118]);if($parent[_0x8057[21]]!==0&&$parent[0][_0x8057[311]]===_0x8057[589]&&target[_0x8057[311]]!==_0x8057[589]){return ;} else {if((target[_0x8057[311]]===_0x8057[589]&&this[_0x8057[62]][_0x8057[774]](target))||$(target)[_0x8057[448]](_0x8057[1170])){return ;} ;} ;$(_0x8057[1037])[_0x8057[331]]();} };} ,paragraphize:function (){return {load:function (html){if(this[_0x8057[63]][_0x8057[282]]){return html;} ;if(html===_0x8057[255]||html===_0x8057[1176]){return this[_0x8057[63]][_0x8057[517]];} ;this[_0x8057[52]][_0x8057[263]]=[_0x8057[362],_0x8057[271],_0x8057[79],_0x8057[1177],_0x8057[126],_0x8057[127],_0x8057[80],_0x8057[81],_0x8057[82],_0x8057[83],_0x8057[84],_0x8057[85],_0x8057[342],_0x8057[78],_0x8057[950],_0x8057[623],_0x8057[1095],_0x8057[1178],_0x8057[1179],_0x8057[1180],_0x8057[1181],_0x8057[631],_0x8057[90],_0x8057[89],_0x8057[628],_0x8057[939],_0x8057[1182],_0x8057[1183],_0x8057[32],_0x8057[1184],_0x8057[1185],_0x8057[1186],_0x8057[1187],_0x8057[1020],_0x8057[1188],_0x8057[1189],_0x8057[1190],_0x8057[1191],_0x8057[866],_0x8057[1192],_0x8057[1193],_0x8057[1194],_0x8057[77]];html=html+_0x8057[618];this[_0x8057[52]][_0x8057[1195]]=[];this[_0x8057[52]][_0x8057[1196]]=0;html=html[_0x8057[541]](/(<br\s?\/?>){1,}\n?<\/blockquote>/gi,_0x8057[687]);html=this[_0x8057[52]][_0x8057[1197]](html);html=this[_0x8057[52]][_0x8057[1198]](html);html=this[_0x8057[52]][_0x8057[1199]](html);html=this[_0x8057[52]][_0x8057[1200]](html);html=this[_0x8057[52]][_0x8057[302]](html);html=this[_0x8057[52]][_0x8057[1201]](html);html=html[_0x8057[541]]( new RegExp(_0x8057[1202]+this[_0x8057[52]][_0x8057[263]][_0x8057[241]](_0x8057[240])+_0x8057[1203],_0x8057[563]),_0x8057[1204]);return $[_0x8057[390]](html);} ,getSafes:function (html){var $div=$(_0x8057[394])[_0x8057[327]](html);$div[_0x8057[332]](_0x8057[1205])[_0x8057[284]](function (){return $(this)[_0x8057[327]](_0x8057[359])[_0x8057[336]]();} );html=$div[_0x8057[68]]();$div[_0x8057[332]](this[_0x8057[52]][_0x8057[263]][_0x8057[241]](_0x8057[668]))[_0x8057[20]]($[_0x8057[281]](function (i,s){this[_0x8057[52]][_0x8057[1196]]++;this[_0x8057[52]][_0x8057[1195]][this[_0x8057[52]][_0x8057[1196]]]=s[_0x8057[1206]];html=html[_0x8057[541]](s[_0x8057[1206]],_0x8057[1207]+this[_0x8057[52]][_0x8057[1196]]+_0x8057[1208]);} ,this));return html;} ,getSafesComments:function (html){var commentsMatches=html[_0x8057[647]](/<!--([\w\W]*?)-->/gi);if(!commentsMatches){return html;} ;$[_0x8057[20]](commentsMatches,$[_0x8057[281]](function (i,s){this[_0x8057[52]][_0x8057[1196]]++;this[_0x8057[52]][_0x8057[1195]][this[_0x8057[52]][_0x8057[1196]]]=s;html=html[_0x8057[541]](s,_0x8057[1207]+this[_0x8057[52]][_0x8057[1196]]+_0x8057[1208]);} ,this));return html;} ,restoreSafes:function (html){$[_0x8057[20]](this[_0x8057[52]][_0x8057[1195]],function (i,s){html=html[_0x8057[541]](_0x8057[1209]+i+_0x8057[1208],s);} );return html;} ,replaceBreaksToParagraphs:function (html){var htmls=html[_0x8057[13]]( new RegExp(_0x8057[618],_0x8057[561]),-1);html=_0x8057[255];if(htmls){var len=htmls[_0x8057[21]];for(var i=0;i<len;i++){if(!htmls[_0x8057[1210]](i)){return ;} ;if(htmls[i][_0x8057[10]](_0x8057[1209])==-1){htmls[i]=htmls[i][_0x8057[541]](/<p>\n\t?<\/p>/gi,_0x8057[255]);htmls[i]=htmls[i][_0x8057[541]](/<p><\/p>/gi,_0x8057[255]);if(htmls[i]!==_0x8057[255]){html+=_0x8057[619]+htmls[i][_0x8057[541]](/^\n+|\n+$/g,_0x8057[255])+_0x8057[620];} ;} else {html+=htmls[i];} ;} ;} ;return html;} ,replaceBreaksToNewLines:function (html){html=html[_0x8057[541]](/<br \/>\s*<br \/>/gi,_0x8057[1211]);html=html[_0x8057[541]](/<br\s?\/?>\n?<br\s?\/?>/gi,_0x8057[1212]);html=html[_0x8057[541]]( new RegExp(_0x8057[1213],_0x8057[561]),_0x8057[618]);html=html[_0x8057[541]]( new RegExp(_0x8057[1214],_0x8057[561]),_0x8057[618]);html=html[_0x8057[541]]( new RegExp(_0x8057[1215]),_0x8057[561],_0x8057[1211]);return html;} ,clear:function (html){html=html[_0x8057[541]]( new RegExp(_0x8057[1216],_0x8057[563]),_0x8057[687]);html=html[_0x8057[541]]( new RegExp(_0x8057[1217],_0x8057[563]),_0x8057[687]);html=html[_0x8057[541]]( new RegExp(_0x8057[1218],_0x8057[563]),_0x8057[1219]);html=html[_0x8057[541]]( new RegExp(_0x8057[1220],_0x8057[563]),_0x8057[1219]);html=html[_0x8057[541]]( new RegExp(_0x8057[1221],_0x8057[563]),_0x8057[1222]);html=html[_0x8057[541]]( new RegExp(_0x8057[1223],_0x8057[563]),_0x8057[619]);html=html[_0x8057[541]]( new RegExp(_0x8057[1224],_0x8057[563]),_0x8057[620]);html=html[_0x8057[541]]( new RegExp(_0x8057[1225],_0x8057[563]),_0x8057[255]);html=html[_0x8057[541]]( new RegExp(_0x8057[1226],_0x8057[563]),_0x8057[620]);html=html[_0x8057[541]]( new RegExp(_0x8057[1227],_0x8057[563]),_0x8057[619]);html=html[_0x8057[541]]( new RegExp(_0x8057[1228],_0x8057[563]),_0x8057[255]);return html;} };} ,paste:function (){return {init:function (e){if(!this[_0x8057[63]][_0x8057[1229]]){return ;} ;this[_0x8057[231]]=true;this[_0x8057[30]][_0x8057[256]]();this[_0x8057[56]][_0x8057[262]]();this[_0x8057[62]][_0x8057[1019]]();this[_0x8057[53]][_0x8057[1230]]();$(window)[_0x8057[425]](_0x8057[1231],$[_0x8057[281]](function (){$(window)[_0x8057[699]](this[_0x8057[1232]]);} ,this));setTimeout($[_0x8057[281]](function (){var html=this[_0x8057[232]][_0x8057[68]]();this[_0x8057[232]][_0x8057[331]]();this[_0x8057[56]][_0x8057[269]]();this[_0x8057[62]][_0x8057[1021]]();this[_0x8057[53]][_0x8057[43]](html);$(window)[_0x8057[708]](_0x8057[1231]);} ,this),1);} ,createPasteBox:function (){this[_0x8057[232]]=$(_0x8057[548])[_0x8057[68]](_0x8057[255])[_0x8057[276]](_0x8057[721],_0x8057[869])[_0x8057[274]]({position:_0x8057[742],width:0,top:0,left:_0x8057[1233]});this[_0x8057[381]][_0x8057[509]]()[_0x8057[327]](this.$pasteBox);this[_0x8057[232]][_0x8057[39]]();} ,insert:function (html){html=this[_0x8057[36]][_0x8057[248]](_0x8057[1234],html);html=(this[_0x8057[62]][_0x8057[588]]())?this[_0x8057[34]][_0x8057[914]](html,false):this[_0x8057[34]][_0x8057[914]](html);html=this[_0x8057[36]][_0x8057[248]](_0x8057[53],html);if(this[_0x8057[62]][_0x8057[588]]()){this[_0x8057[43]][_0x8057[256]](html,false);} else {this[_0x8057[43]][_0x8057[68]](html,false);} ;this[_0x8057[62]][_0x8057[431]]();this[_0x8057[231]]=false;setTimeout($[_0x8057[281]](this[_0x8057[34]][_0x8057[353]],this),10);setTimeout($[_0x8057[281]](function (){var spans=this[_0x8057[261]][_0x8057[332]](_0x8057[101]);$[_0x8057[20]](spans,function (i,s){var html=s[_0x8057[514]][_0x8057[541]](/[\u200B-\u200D\uFEFF]/,_0x8057[255]);if(html===_0x8057[255]&&s[_0x8057[910]][_0x8057[21]]===0){$(s)[_0x8057[331]]();} ;} );} ,this),10);} };} ,placeholder:function (){return {enable:function (){if(!this[_0x8057[54]][_0x8057[450]]()){return ;} ;this[_0x8057[261]][_0x8057[276]](_0x8057[54],this[_0x8057[229]][_0x8057[276]](_0x8057[54]));this[_0x8057[54]][_0x8057[324]]();this[_0x8057[261]][_0x8057[425]](_0x8057[1235],$[_0x8057[281]](this[_0x8057[54]][_0x8057[324]],this));} ,toggle:function (){var func=_0x8057[350];if(this[_0x8057[62]][_0x8057[293]](this[_0x8057[261]][_0x8057[68]](),false)){func=_0x8057[347];} ;this[_0x8057[261]][func](_0x8057[1236]);} ,remove:function (){this[_0x8057[261]][_0x8057[350]](_0x8057[1236]);} ,is:function (){if(this[_0x8057[63]][_0x8057[54]]){return this[_0x8057[229]][_0x8057[276]](_0x8057[54],this[_0x8057[63]][_0x8057[54]]);} else {return !( typeof this[_0x8057[229]][_0x8057[276]](_0x8057[54])==_0x8057[14]||this[_0x8057[229]][_0x8057[276]](_0x8057[54])===_0x8057[255]);} ;} };} ,progress:function (){return {show:function (){$(document[_0x8057[87]])[_0x8057[327]]($(_0x8057[1237]));$(_0x8057[1239])[_0x8057[1238]]();} ,hide:function (){$(_0x8057[1239])[_0x8057[1154]](1500,function (){$(this)[_0x8057[331]]();} );} };} ,selection:function (){return {get:function (){this[_0x8057[755]]=document[_0x8057[531]]();if(document[_0x8057[531]]&&this[_0x8057[755]][_0x8057[533]]&&this[_0x8057[755]][_0x8057[532]]){this[_0x8057[313]]=this[_0x8057[755]][_0x8057[533]](0);} else {this[_0x8057[313]]=document[_0x8057[930]]();} ;} ,addRange:function (){try{this[_0x8057[755]][_0x8057[921]]();} catch(e){} ;this[_0x8057[755]][_0x8057[518]](this[_0x8057[313]]);} ,getCurrent:function (){var el=false;this[_0x8057[56]][_0x8057[292]]();if(this[_0x8057[755]]&&this[_0x8057[755]][_0x8057[532]]>0){el=this[_0x8057[755]][_0x8057[533]](0)[_0x8057[1240]];} ;return this[_0x8057[62]][_0x8057[774]](el);} ,getParent:function (elem){elem=elem||this[_0x8057[56]][_0x8057[883]]();if(elem){return this[_0x8057[62]][_0x8057[774]]($(elem)[_0x8057[509]]()[0]);} ;return false;} ,getBlock:function (node){node=node||this[_0x8057[56]][_0x8057[883]]();while(node){if(this[_0x8057[62]][_0x8057[513]](node[_0x8057[311]])){return ($(node)[_0x8057[448]](_0x8057[396]))?false:node;} ;node=node[_0x8057[773]];} ;return false;} ,getInlines:function (nodes){this[_0x8057[56]][_0x8057[292]]();if(this[_0x8057[313]]&&this[_0x8057[313]][_0x8057[314]]){return false;} ;var inlines=[];nodes=( typeof nodes==_0x8057[14])?this[_0x8057[56]][_0x8057[913]]():nodes;var inlineTags=this[_0x8057[63]][_0x8057[678]];inlineTags[_0x8057[16]](_0x8057[101]);$[_0x8057[20]](nodes,$[_0x8057[281]](function (i,node){if($[_0x8057[306]](node[_0x8057[311]][_0x8057[325]](),inlineTags)!=-1){inlines[_0x8057[16]](node);} ;} ,this));return (inlines[_0x8057[21]]===0)?false:inlines;} ,getBlocks:function (nodes){this[_0x8057[56]][_0x8057[292]]();if(this[_0x8057[313]]&&this[_0x8057[313]][_0x8057[314]]){return [this[_0x8057[56]][_0x8057[323]]()];} ;var blocks=[];nodes=( typeof nodes==_0x8057[14])?this[_0x8057[56]][_0x8057[913]]():nodes;$[_0x8057[20]](nodes,$[_0x8057[281]](function (i,node){if(this[_0x8057[62]][_0x8057[511]](node)){this[_0x8057[56]][_0x8057[1241]]=node;blocks[_0x8057[16]](node);} ;} ,this));return (blocks[_0x8057[21]]===0)?[this[_0x8057[56]][_0x8057[323]]()]:blocks;} ,getLastBlock:function (){return this[_0x8057[56]][_0x8057[1241]];} ,getNodes:function (){this[_0x8057[56]][_0x8057[292]]();var startNode=this[_0x8057[56]][_0x8057[1242]](1);var endNode=this[_0x8057[56]][_0x8057[1242]](2);var range=this[_0x8057[313]][_0x8057[527]]();if(this[_0x8057[313]][_0x8057[314]]===false){var startContainer=range[_0x8057[1240]];var startOffset=range[_0x8057[1243]];this[_0x8057[56]][_0x8057[1244]](range,endNode,false);range[_0x8057[454]](startContainer,startOffset);this[_0x8057[56]][_0x8057[1244]](range,startNode,true);} else {this[_0x8057[56]][_0x8057[1244]](range,startNode,true);endNode=startNode;} ;var nodes=[];var counter=0;var self=this;this[_0x8057[261]][_0x8057[332]](_0x8057[635])[_0x8057[20]](function (){if(this==startNode){var parent=$(this)[_0x8057[509]]();if(parent[_0x8057[21]]!==0&&parent[0][_0x8057[311]]!=_0x8057[1000]&&self[_0x8057[62]][_0x8057[774]](parent[0])){nodes[_0x8057[16]](parent[0]);} ;nodes[_0x8057[16]](this);counter=1;} else {if(counter>0){nodes[_0x8057[16]](this);counter=counter+1;} ;} ;if(this==endNode){return false;} ;} );var finalNodes=[];var len=nodes[_0x8057[21]];for(var i=0;i<len;i++){if(nodes[i][_0x8057[386]]!=_0x8057[1245]&&nodes[i][_0x8057[386]]!=_0x8057[1246]){finalNodes[_0x8057[16]](nodes[i]);} ;} ;this[_0x8057[56]][_0x8057[1247]]();return finalNodes;} ,getNodesMarker:function (num){return $(_0x8057[1248]+num+_0x8057[1249]+this[_0x8057[63]][_0x8057[515]]+_0x8057[862])[0];} ,setNodesMarker:function (range,node,type){try{range[_0x8057[526]](type);range[_0x8057[920]](node);} catch(e){} ;} ,removeNodesMarkers:function (){$(document)[_0x8057[332]](_0x8057[1250])[_0x8057[331]]();this[_0x8057[261]][_0x8057[332]](_0x8057[1250])[_0x8057[331]]();} ,fromPoint:function (start,end){this[_0x8057[33]][_0x8057[538]](start,end);} ,wrap:function (tag){this[_0x8057[56]][_0x8057[292]]();if(this[_0x8057[313]][_0x8057[314]]){return false;} ;var wrapper=document[_0x8057[652]](tag);wrapper[_0x8057[918]](this[_0x8057[313]][_0x8057[1251]]());this[_0x8057[313]][_0x8057[920]](wrapper);return wrapper;} ,selectElement:function (node){this[_0x8057[33]][_0x8057[256]](node,0,node,1);} ,selectAll:function (){this[_0x8057[56]][_0x8057[292]]();this[_0x8057[313]][_0x8057[528]](this[_0x8057[261]][0]);this[_0x8057[56]][_0x8057[518]]();} ,remove:function (){this[_0x8057[56]][_0x8057[292]]();this[_0x8057[755]][_0x8057[921]]();} ,save:function (){this[_0x8057[56]][_0x8057[1252]]();} ,createMarkers:function (){this[_0x8057[56]][_0x8057[292]]();var node1=this[_0x8057[56]][_0x8057[360]](1);this[_0x8057[56]][_0x8057[1253]](this[_0x8057[313]],node1,true);if(this[_0x8057[313]][_0x8057[314]]===false){var node2=this[_0x8057[56]][_0x8057[360]](2);this[_0x8057[56]][_0x8057[1253]](this[_0x8057[313]],node2,false);} ;this[_0x8057[1254]]=this[_0x8057[261]][_0x8057[68]]();} ,getMarker:function (num){if( typeof num==_0x8057[14]){num=1;} ;return $(_0x8057[1255]+num+_0x8057[1256]+this[_0x8057[63]][_0x8057[515]]+_0x8057[862])[0];} ,getMarkerAsHtml:function (num){return this[_0x8057[62]][_0x8057[814]](this[_0x8057[56]][_0x8057[360]](num));} ,setMarker:function (range,node,type){range=range[_0x8057[527]]();try{range[_0x8057[526]](type);range[_0x8057[920]](node);} catch(e){this[_0x8057[39]][_0x8057[454]]();} ;} ,restore:function (){var node1=this[_0x8057[261]][_0x8057[332]](_0x8057[1257]);var node2=this[_0x8057[261]][_0x8057[332]](_0x8057[1258]);if(node1[_0x8057[21]]!==0&&node2[_0x8057[21]]!==0){this[_0x8057[33]][_0x8057[256]](node1,0,node2,0);} else {if(node1[_0x8057[21]]!==0){this[_0x8057[33]][_0x8057[256]](node1,0,node1,0);} else {this[_0x8057[261]][_0x8057[39]]();} ;} ;this[_0x8057[56]][_0x8057[763]]();this[_0x8057[1254]]=false;} ,removeMarkers:function (){this[_0x8057[261]][_0x8057[332]](_0x8057[1259])[_0x8057[331]]();} ,getText:function (){this[_0x8057[56]][_0x8057[292]]();return this[_0x8057[755]].toString();} ,getHtml:function (){var html=_0x8057[255];this[_0x8057[56]][_0x8057[292]]();if(this[_0x8057[755]][_0x8057[532]]){var container=document[_0x8057[652]](_0x8057[271]);var len=this[_0x8057[755]][_0x8057[532]];for(var i=0;i<len;++i){container[_0x8057[918]](this[_0x8057[755]][_0x8057[533]](i)[_0x8057[1260]]());} ;html=container[_0x8057[514]];} ;return this[_0x8057[34]][_0x8057[695]](html);} };} ,shortcuts:function (){return {init:function (e,key){if(!this[_0x8057[63]][_0x8057[57]]){if((e[_0x8057[946]]||e[_0x8057[947]])&&(key===66||key===73)){e[_0x8057[413]]();} ;return false;} ;$[_0x8057[20]](this[_0x8057[63]][_0x8057[57]],$[_0x8057[281]](function (str,command){var keys=str[_0x8057[13]](_0x8057[357]);var len=keys[_0x8057[21]];for(var i=0;i<len;i++){if( typeof keys[i]===_0x8057[8]){this[_0x8057[57]][_0x8057[1262]](e,$[_0x8057[390]](keys[i]),$[_0x8057[281]](function (){var func;if(command[_0x8057[465]][_0x8057[10]](/\./)!=_0x8057[11]){func=command[_0x8057[465]][_0x8057[13]](_0x8057[12]);if( typeof this[func[0]]!=_0x8057[14]){this[func[0]][func[1]][_0x8057[3]](this,command[_0x8057[1261]]);} ;} else {this[command[_0x8057[465]]][_0x8057[3]](this,command[_0x8057[1261]]);} ;} ,this));} ;} ;} ,this));} ,handler:function (e,keys,origHandler){var hotkeysSpecialKeys={8:_0x8057[1263],9:_0x8057[1264],10:_0x8057[1265],13:_0x8057[1265],16:_0x8057[1266],17:_0x8057[945],18:_0x8057[637],19:_0x8057[1267],20:_0x8057[1268],27:_0x8057[1269],32:_0x8057[1270],33:_0x8057[1271],34:_0x8057[1272],35:_0x8057[1273],36:_0x8057[1274],37:_0x8057[485],38:_0x8057[1275],39:_0x8057[257],40:_0x8057[1276],45:_0x8057[43],46:_0x8057[93],59:_0x8057[898],61:_0x8057[1277],96:_0x8057[1278],97:_0x8057[1279],98:_0x8057[1280],99:_0x8057[1281],100:_0x8057[1282],101:_0x8057[1283],102:_0x8057[1284],103:_0x8057[1285],104:_0x8057[1286],105:_0x8057[1287],106:_0x8057[635],107:_0x8057[1288],109:_0x8057[726],110:_0x8057[12],111:_0x8057[1289],112:_0x8057[1290],113:_0x8057[1291],114:_0x8057[1292],115:_0x8057[1293],116:_0x8057[1294],117:_0x8057[1295],118:_0x8057[1296],119:_0x8057[1297],120:_0x8057[1298],121:_0x8057[1299],122:_0x8057[1300],123:_0x8057[1301],144:_0x8057[1302],145:_0x8057[1303],173:_0x8057[726],186:_0x8057[898],187:_0x8057[1277],188:_0x8057[357],189:_0x8057[726],190:_0x8057[12],191:_0x8057[1289],192:_0x8057[1304],219:_0x8057[1305],220:_0x8057[1306],221:_0x8057[891],222:_0x8057[544]};var hotkeysShiftNums={"\x60":_0x8057[1307],"\x31":_0x8057[1308],"\x32":_0x8057[1044],"\x33":_0x8057[1039],"\x34":_0x8057[1309],"\x35":_0x8057[1136],"\x36":_0x8057[811],"\x37":_0x8057[661],"\x38":_0x8057[635],"\x39":_0x8057[1310],"\x30":_0x8057[1311],"\x2D":_0x8057[1312],"\x3D":_0x8057[1288],"\x3B":_0x8057[1313],"\x27":_0x8057[543],"\x2C":_0x8057[320],"\x2E":_0x8057[321],"\x2F":_0x8057[1314],"\x5C":_0x8057[240]};keys=keys[_0x8057[325]]()[_0x8057[13]](_0x8057[553]);var special=hotkeysSpecialKeys[e[_0x8057[711]]],character=String[_0x8057[1315]](e[_0x8057[944]])[_0x8057[325]](),modif=_0x8057[255],possible={};$[_0x8057[20]]([_0x8057[637],_0x8057[945],_0x8057[88],_0x8057[1266]],function (index,specialKey){if(e[specialKey+_0x8057[1316]]&&special!==specialKey){modif+=specialKey+_0x8057[1288];} ;} );if(special){possible[modif+special]=true;} ;if(character){possible[modif+character]=true;possible[modif+hotkeysShiftNums[character]]=true;if(modif===_0x8057[1317]){possible[hotkeysShiftNums[character]]=true;} ;} ;for(var i=0,len=keys[_0x8057[21]];i<len;i++){if(possible[keys[i]]){e[_0x8057[413]]();return origHandler[_0x8057[3]](this,arguments);} ;} ;} };} ,tabifier:function (){return {get:function (code){if(!this[_0x8057[63]][_0x8057[58]]){return code;} ;var ownLine=[_0x8057[1186],_0x8057[87],_0x8057[86],_0x8057[1020],_0x8057[1318],_0x8057[48],_0x8057[88],_0x8057[1319],_0x8057[90],_0x8057[89],_0x8057[362],_0x8057[632],_0x8057[634],_0x8057[633]];var contOwnLine=[_0x8057[128],_0x8057[343],_0x8057[343],_0x8057[1320],_0x8057[1184],_0x8057[89]];var newLevel=[_0x8057[78],_0x8057[271],_0x8057[342],_0x8057[1188],_0x8057[1177],_0x8057[1321],_0x8057[1185],_0x8057[127],_0x8057[77],_0x8057[79],_0x8057[939],_0x8057[340],_0x8057[341],_0x8057[363],_0x8057[126]];this[_0x8057[58]][_0x8057[1322]]= new RegExp(_0x8057[1323]+ownLine[_0x8057[241]](_0x8057[1324])+_0x8057[240]+contOwnLine[_0x8057[241]](_0x8057[240])+_0x8057[1325]);this[_0x8057[58]][_0x8057[1326]]= new RegExp(_0x8057[1327]+ownLine[_0x8057[241]](_0x8057[1324])+_0x8057[1328]+contOwnLine[_0x8057[241]](_0x8057[1328])+_0x8057[1325]);this[_0x8057[58]][_0x8057[1329]]= new RegExp(_0x8057[1330]+newLevel[_0x8057[241]](_0x8057[240])+_0x8057[1325]);var i=0,codeLength=code[_0x8057[21]],point=0,start=null,end=null,tag=_0x8057[255],out=_0x8057[255],cont=_0x8057[255];this[_0x8057[58]][_0x8057[1331]]=0;for(;i<codeLength;i++){point=i;if(-1==code[_0x8057[1332]](i)[_0x8057[648]](_0x8057[320])){out+=code[_0x8057[1332]](i);return this[_0x8057[58]][_0x8057[1333]](out);} ;while(point<codeLength&&code[_0x8057[1334]](point)!=_0x8057[320]){point++;} ;if(i!=point){cont=code[_0x8057[1332]](i,point-i);if(!cont[_0x8057[647]](/^\s{2,}$/g)){if(_0x8057[618]==out[_0x8057[1334]](out[_0x8057[21]]-1)){out+=this[_0x8057[58]][_0x8057[1335]]();} else {if(_0x8057[618]==cont[_0x8057[1334]](0)){out+=_0x8057[618]+this[_0x8057[58]][_0x8057[1335]]();cont=cont[_0x8057[541]](/^\s+/,_0x8057[255]);} ;} ;out+=cont;} ;if(cont[_0x8057[647]](/\n/)){out+=_0x8057[618]+this[_0x8057[58]][_0x8057[1335]]();} ;} ;start=point;while(point<codeLength&&_0x8057[321]!=code[_0x8057[1334]](point)){point++;} ;tag=code[_0x8057[1332]](start,point-start);i=point;var t;if(_0x8057[1336]==tag[_0x8057[1332]](1,3)){if(!tag[_0x8057[647]](/--$/)){while(_0x8057[1337]!=code[_0x8057[1332]](point,3)){point++;} ;point+=2;tag=code[_0x8057[1332]](start,point-start);i=point;} ;if(_0x8057[618]!=out[_0x8057[1334]](out[_0x8057[21]]-1)){out+=_0x8057[618];} ;out+=this[_0x8057[58]][_0x8057[1335]]();out+=tag+_0x8057[1338];} else {if(_0x8057[1308]==tag[1]){out=this[_0x8057[58]][_0x8057[1339]](tag+_0x8057[321],out);} else {if(_0x8057[1314]==tag[1]){out+=tag+_0x8057[1338];} else {if(t=tag[_0x8057[647]](/^<(script|style|pre)/i)){t[1]=t[1][_0x8057[325]]();tag=this[_0x8057[58]][_0x8057[1340]](tag);out=this[_0x8057[58]][_0x8057[1339]](tag,out);end=String(code[_0x8057[1332]](i+1))[_0x8057[325]]()[_0x8057[648]](_0x8057[651]+t[1]);if(end){cont=code[_0x8057[1332]](i+1,end);i+=end;out+=cont;} ;} else {tag=this[_0x8057[58]][_0x8057[1340]](tag);out=this[_0x8057[58]][_0x8057[1339]](tag,out);} ;} ;} ;} ;} ;return this[_0x8057[58]][_0x8057[1333]](out);} ,getTabs:function (){var s=_0x8057[255];for(var j=0;j<this[_0x8057[58]][_0x8057[1331]];j++){s+=_0x8057[585];} ;return s;} ,finish:function (code){code=code[_0x8057[541]](/\n\s*\n/g,_0x8057[618]);code=code[_0x8057[541]](/^[\s\n]*/,_0x8057[255]);code=code[_0x8057[541]](/[\s\n]*$/,_0x8057[255]);code=code[_0x8057[541]](/<script(.*?)>\n<\/script>/gi,_0x8057[1341]);this[_0x8057[58]][_0x8057[1331]]=0;return code;} ,cleanTag:function (tag){var tagout=_0x8057[255];tag=tag[_0x8057[541]](/\n/g,_0x8057[553]);tag=tag[_0x8057[541]](/\s{2,}/g,_0x8057[553]);tag=tag[_0x8057[541]](/^\s+|\s+$/g,_0x8057[553]);var suffix=_0x8057[255];if(tag[_0x8057[647]](/\/$/)){suffix=_0x8057[1289];tag=tag[_0x8057[541]](/\/+$/,_0x8057[255]);} ;var m;while(m=/\s*([^= ]+)(?:=((['"']).*?\3|[^ ]+))?/[_0x8057[1342]](tag)){if(m[2]){tagout+=m[1][_0x8057[325]]()+_0x8057[1277]+m[2];} else {if(m[1]){tagout+=m[1][_0x8057[325]]();} ;} ;tagout+=_0x8057[553];tag=tag[_0x8057[1332]](m[0][_0x8057[21]]);} ;return tagout[_0x8057[541]](/\s*$/,_0x8057[255])+suffix+_0x8057[321];} ,placeTag:function (tag,out){var nl=tag[_0x8057[647]](this[_0x8057[58]][_0x8057[1329]]);if(tag[_0x8057[647]](this[_0x8057[58]][_0x8057[1322]])||nl){out=out[_0x8057[541]](/\s*$/,_0x8057[255]);out+=_0x8057[618];} ;if(nl&&_0x8057[1289]==tag[_0x8057[1334]](1)){this[_0x8057[58]][_0x8057[1331]]--;} ;if(_0x8057[618]==out[_0x8057[1334]](out[_0x8057[21]]-1)){out+=this[_0x8057[58]][_0x8057[1335]]();} ;if(nl&&_0x8057[1289]!=tag[_0x8057[1334]](1)){this[_0x8057[58]][_0x8057[1331]]++;} ;out+=tag;if(tag[_0x8057[647]](this[_0x8057[58]][_0x8057[1326]])||tag[_0x8057[647]](this[_0x8057[58]][_0x8057[1329]])){out=out[_0x8057[541]](/ *$/,_0x8057[255]);out+=_0x8057[618];} ;return out;} };} ,tidy:function (){return {setupAllowed:function (){if(this[_0x8057[63]][_0x8057[1343]]){this[_0x8057[63]][_0x8057[642]]=false;} ;if(this[_0x8057[63]][_0x8057[1344]]){this[_0x8057[63]][_0x8057[346]]=false;} ;if(this[_0x8057[63]][_0x8057[282]]){return ;} ;var tags=[_0x8057[77],_0x8057[1095]];if(this[_0x8057[63]][_0x8057[1343]]){this[_0x8057[59]][_0x8057[1345]](tags);} ;if(this[_0x8057[63]][_0x8057[642]]){this[_0x8057[59]][_0x8057[1346]](tags);} ;} ,addToAllowed:function (tags){var len=tags[_0x8057[21]];for(var i=0;i<len;i++){if($[_0x8057[306]](tags[i],this[_0x8057[63]][_0x8057[1343]])==-1){this[_0x8057[63]][_0x8057[1343]][_0x8057[16]](tags[i]);} ;} ;} ,removeFromDenied:function (tags){var len=tags[_0x8057[21]];for(var i=0;i<len;i++){var pos=$[_0x8057[306]](tags[i],this[_0x8057[63]][_0x8057[642]]);if(pos!=-1){this[_0x8057[63]][_0x8057[642]][_0x8057[1347]](pos,1);} ;} ;} ,load:function (html,options){this[_0x8057[59]][_0x8057[1348]]={deniedTags:this[_0x8057[63]][_0x8057[642]],allowedTags:this[_0x8057[63]][_0x8057[1343]],removeComments:this[_0x8057[63]][_0x8057[1349]],replaceTags:this[_0x8057[63]][_0x8057[1350]],replaceStyles:this[_0x8057[63]][_0x8057[1351]],removeDataAttr:this[_0x8057[63]][_0x8057[1352]],removeAttr:this[_0x8057[63]][_0x8057[346]],allowedAttr:this[_0x8057[63]][_0x8057[1344]],removeWithoutAttr:this[_0x8057[63]][_0x8057[1353]],removeEmpty:this[_0x8057[63]][_0x8057[355]]};$[_0x8057[246]](this[_0x8057[59]][_0x8057[1348]],options);html=this[_0x8057[59]][_0x8057[1349]](html);this[_0x8057[59]][_0x8057[1354]]=$(_0x8057[394])[_0x8057[327]](html);this[_0x8057[59]][_0x8057[1350]]();this[_0x8057[59]][_0x8057[1351]]();this[_0x8057[59]][_0x8057[1355]]();this[_0x8057[59]][_0x8057[346]]();this[_0x8057[59]][_0x8057[355]]();this[_0x8057[59]][_0x8057[1356]]();this[_0x8057[59]][_0x8057[1352]]();this[_0x8057[59]][_0x8057[1353]]();html=this[_0x8057[59]][_0x8057[1354]][_0x8057[68]]();this[_0x8057[59]][_0x8057[1354]][_0x8057[331]]();return html;} ,removeComments:function (html){if(!this[_0x8057[59]][_0x8057[1348]][_0x8057[1349]]){return html;} ;return html[_0x8057[541]](/<!--[\s\S]*?-->/gi,_0x8057[255]);} ,replaceTags:function (html){if(!this[_0x8057[59]][_0x8057[1348]][_0x8057[1350]]){return html;} ;var len=this[_0x8057[59]][_0x8057[1348]][_0x8057[1350]][_0x8057[21]];var replacement=[],rTags=[];for(var i=0;i<len;i++){rTags[_0x8057[16]](this[_0x8057[59]][_0x8057[1348]][_0x8057[1350]][i][1]);replacement[_0x8057[16]](this[_0x8057[59]][_0x8057[1348]][_0x8057[1350]][i][0]);} ;this[_0x8057[59]][_0x8057[1354]][_0x8057[332]](replacement[_0x8057[241]](_0x8057[357]))[_0x8057[20]]($[_0x8057[281]](function (n,s){var tag=rTags[n];$(s)[_0x8057[284]](function (){var replaced=$(_0x8057[320]+tag+_0x8057[1357])[_0x8057[327]]($(this)[_0x8057[336]]());for(var i=0;i<this[_0x8057[910]][_0x8057[21]];i++){replaced[_0x8057[276]](this[_0x8057[910]][i][_0x8057[286]],this[_0x8057[910]][i][_0x8057[309]]);} ;return replaced;} );} ,this));return html;} ,replaceStyles:function (){if(!this[_0x8057[59]][_0x8057[1348]][_0x8057[1351]]){return ;} ;var len=this[_0x8057[59]][_0x8057[1348]][_0x8057[1351]][_0x8057[21]];this[_0x8057[59]][_0x8057[1354]][_0x8057[332]](_0x8057[101])[_0x8057[20]]($[_0x8057[281]](function (n,s){var $el=$(s);var style=$el[_0x8057[276]](_0x8057[90]);for(var i=0;i<len;i++){if(style&&style[_0x8057[647]]( new RegExp(_0x8057[811]+this[_0x8057[59]][_0x8057[1348]][_0x8057[1351]][i][0],_0x8057[123]))){var tagName=this[_0x8057[59]][_0x8057[1348]][_0x8057[1351]][i][1];$el[_0x8057[284]](function (){var tag=document[_0x8057[652]](tagName);return $(tag)[_0x8057[327]]($(this)[_0x8057[336]]());} );} ;} ;} ,this));} ,removeTags:function (){if(!this[_0x8057[59]][_0x8057[1348]][_0x8057[642]]&&this[_0x8057[59]][_0x8057[1348]][_0x8057[1343]]){this[_0x8057[59]][_0x8057[1354]][_0x8057[332]](_0x8057[635])[_0x8057[498]](this[_0x8057[59]][_0x8057[1348]][_0x8057[1343]][_0x8057[241]](_0x8057[357]))[_0x8057[20]](function (i,s){if(s[_0x8057[514]]===_0x8057[255]){$(s)[_0x8057[331]]();} else {$(s)[_0x8057[336]]()[_0x8057[335]]();} ;} );} ;if(this[_0x8057[59]][_0x8057[1348]][_0x8057[642]]){this[_0x8057[59]][_0x8057[1354]][_0x8057[332]](this[_0x8057[59]][_0x8057[1348]][_0x8057[642]][_0x8057[241]](_0x8057[357]))[_0x8057[20]](function (i,s){if(s[_0x8057[514]]===_0x8057[255]){$(s)[_0x8057[331]]();} else {$(s)[_0x8057[336]]()[_0x8057[335]]();} ;} );} ;} ,removeAttr:function (){var len;if(!this[_0x8057[59]][_0x8057[1348]][_0x8057[346]]&&this[_0x8057[59]][_0x8057[1348]][_0x8057[1344]]){var allowedAttrTags=[],allowedAttrData=[];len=this[_0x8057[59]][_0x8057[1348]][_0x8057[1344]][_0x8057[21]];for(var i=0;i<len;i++){allowedAttrTags[_0x8057[16]](this[_0x8057[59]][_0x8057[1348]][_0x8057[1344]][i][0]);allowedAttrData[_0x8057[16]](this[_0x8057[59]][_0x8057[1348]][_0x8057[1344]][i][1]);} ;this[_0x8057[59]][_0x8057[1354]][_0x8057[332]](_0x8057[635])[_0x8057[20]]($[_0x8057[281]](function (n,s){var $el=$(s);var pos=$[_0x8057[306]]($el[0][_0x8057[311]][_0x8057[325]](),allowedAttrTags);var attributesRemove=this[_0x8057[59]][_0x8057[1358]](pos,allowedAttrData,$el);if(attributesRemove){$[_0x8057[20]](attributesRemove,function (z,f){$el[_0x8057[346]](f);} );} ;} ,this));} ;if(this[_0x8057[59]][_0x8057[1348]][_0x8057[346]]){len=this[_0x8057[59]][_0x8057[1348]][_0x8057[346]][_0x8057[21]];for(var i=0;i<len;i++){var attrs=this[_0x8057[59]][_0x8057[1348]][_0x8057[346]][i][1];if($[_0x8057[1359]](attrs)){attrs=attrs[_0x8057[241]](_0x8057[553]);} ;this[_0x8057[59]][_0x8057[1354]][_0x8057[332]](this[_0x8057[59]][_0x8057[1348]][_0x8057[346]][i][0])[_0x8057[346]](attrs);} ;} ;} ,removeAttrGetRemoves:function (pos,allowed,$el){var attributesRemove=[];if(pos==-1){$[_0x8057[20]]($el[0][_0x8057[910]],function (i,item){attributesRemove[_0x8057[16]](item[_0x8057[286]]);} );} else {if(allowed[pos]==_0x8057[635]){attributesRemove=[];} else {$[_0x8057[20]]($el[0][_0x8057[910]],function (i,item){if($[_0x8057[1359]](allowed[pos])){if($[_0x8057[306]](item[_0x8057[286]],allowed[pos])==-1){attributesRemove[_0x8057[16]](item[_0x8057[286]]);} ;} else {if(allowed[pos]!=item[_0x8057[286]]){attributesRemove[_0x8057[16]](item[_0x8057[286]]);} ;} ;} );} ;} ;return attributesRemove;} ,removeAttrs:function (el,regex){regex= new RegExp(regex,_0x8057[561]);return el[_0x8057[20]](function (){var self=$(this);var len=this[_0x8057[910]][_0x8057[21]]-1;for(var i=len;i>=0;i--){var item=this[_0x8057[910]][i];if(item&&item[_0x8057[1360]]&&item[_0x8057[286]][_0x8057[10]](regex)>=0){self[_0x8057[346]](item[_0x8057[286]]);} ;} ;} );} ,removeEmpty:function (){if(!this[_0x8057[59]][_0x8057[1348]][_0x8057[355]]){return ;} ;this[_0x8057[59]][_0x8057[1354]][_0x8057[332]](this[_0x8057[59]][_0x8057[1348]][_0x8057[355]][_0x8057[241]](_0x8057[357]))[_0x8057[20]](function (){var $el=$(this);var text=$el[_0x8057[770]]();text=text[_0x8057[541]](/[\u200B-\u200D\uFEFF]/g,_0x8057[255]);text=text[_0x8057[541]](/&nbsp;/gi,_0x8057[255]);text=text[_0x8057[541]](/\s/g,_0x8057[255]);if(text===_0x8057[255]&&$el[_0x8057[768]]()[_0x8057[21]]===0){$el[_0x8057[331]]();} ;} );} ,removeParagraphsInLists:function (){this[_0x8057[59]][_0x8057[1354]][_0x8057[332]](_0x8057[1361])[_0x8057[336]]()[_0x8057[335]]();} ,removeDataAttr:function (){if(!this[_0x8057[59]][_0x8057[1348]][_0x8057[1352]]){return ;} ;var tags=this[_0x8057[59]][_0x8057[1348]][_0x8057[1352]];if($[_0x8057[1359]](this[_0x8057[59]][_0x8057[1348]][_0x8057[1352]])){tags=this[_0x8057[59]][_0x8057[1348]][_0x8057[1352]][_0x8057[241]](_0x8057[357]);} ;this[_0x8057[59]][_0x8057[1363]](this[_0x8057[59]][_0x8057[1354]][_0x8057[332]](tags),_0x8057[1362]);} ,removeWithoutAttr:function (){if(!this[_0x8057[59]][_0x8057[1348]][_0x8057[1353]]){return ;} ;this[_0x8057[59]][_0x8057[1354]][_0x8057[332]](this[_0x8057[59]][_0x8057[1348]][_0x8057[1353]][_0x8057[241]](_0x8057[357]))[_0x8057[20]](function (){if(this[_0x8057[910]][_0x8057[21]]===0){$(this)[_0x8057[336]]()[_0x8057[335]]();} ;} );} };} ,toolbar:function (){return {init:function (){return {html:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[68]),func:_0x8057[1364]},formatting:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[69]),dropdown:{p:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[1365]),func:_0x8057[1366]},blockquote:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[305]),func:_0x8057[1366]},pre:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[35]),func:_0x8057[1366]},h1:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[1367]),func:_0x8057[1366]},h2:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[1368]),func:_0x8057[1366]},h3:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[1369]),func:_0x8057[1366]},h4:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[1370]),func:_0x8057[1366]},h5:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[1371]),func:_0x8057[1366]}}},bold:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[70]),func:_0x8057[108]},italic:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[71]),func:_0x8057[108]},deleted:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[72]),func:_0x8057[108]},underline:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[102]),func:_0x8057[108]},unorderedlist:{title:_0x8057[1372]+this[_0x8057[46]][_0x8057[292]](_0x8057[73]),func:_0x8057[112]},orderedlist:{title:_0x8057[1373]+this[_0x8057[46]][_0x8057[292]](_0x8057[74]),func:_0x8057[112]},outdent:{title:_0x8057[1374]+this[_0x8057[46]][_0x8057[292]](_0x8057[75]),func:_0x8057[1375]},indent:{title:_0x8057[1376]+this[_0x8057[46]][_0x8057[292]](_0x8057[41]),func:_0x8057[1377]},image:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[40]),func:_0x8057[1378]},file:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[38]),func:_0x8057[1379]},link:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[48]),dropdown:{link:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[1027]),func:_0x8057[111]},unlink:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[1173]),func:_0x8057[1380]}}},alignment:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[27]),dropdown:{left:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[1381]),func:_0x8057[1382]},center:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[1383]),func:_0x8057[1384]},right:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[1385]),func:_0x8057[1386]},justify:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[1387]),func:_0x8057[1388]}}},horizontalrule:{title:this[_0x8057[46]][_0x8057[292]](_0x8057[76]),func:_0x8057[1389]}};} ,build:function (){this[_0x8057[60]][_0x8057[1390]]();this[_0x8057[60]][_0x8057[1391]]();this[_0x8057[60]][_0x8057[1392]]();if(this[_0x8057[63]][_0x8057[492]][_0x8057[21]]===0){return ;} ;this[_0x8057[494]]=this[_0x8057[60]][_0x8057[1393]]();this[_0x8057[60]][_0x8057[1394]]();this[_0x8057[60]][_0x8057[327]]();this[_0x8057[60]][_0x8057[1395]]();this[_0x8057[60]][_0x8057[1396]]();this[_0x8057[60]][_0x8057[1397]]();if(this[_0x8057[63]][_0x8057[1156]]){this[_0x8057[261]][_0x8057[425]](_0x8057[1398],$[_0x8057[281]](this[_0x8057[51]][_0x8057[492]],this));} ;} ,createContainer:function (){return $(_0x8057[1054])[_0x8057[347]](_0x8057[1400])[_0x8057[276]](_0x8057[386],_0x8057[1399]+this[_0x8057[230]]);} ,setFormattingTags:function (){$[_0x8057[20]](this[_0x8057[63]][_0x8057[60]][_0x8057[69]][_0x8057[37]],$[_0x8057[281]](function (i,s){if($[_0x8057[306]](i,this[_0x8057[63]][_0x8057[69]])==-1){delete this[_0x8057[63]][_0x8057[60]][_0x8057[69]][_0x8057[37]][i];} ;} ,this));} ,loadButtons:function (){$[_0x8057[20]](this[_0x8057[63]][_0x8057[492]],$[_0x8057[281]](function (i,btnName){if(!this[_0x8057[63]][_0x8057[60]][btnName]){return ;} ;if(btnName===_0x8057[38]){if(this[_0x8057[63]][_0x8057[754]]===false){return ;} else {if(!this[_0x8057[63]][_0x8057[754]]&&this[_0x8057[63]][_0x8057[1401]]===false){return ;} ;} ;} ;if(btnName===_0x8057[40]){if(this[_0x8057[63]][_0x8057[777]]===false){return ;} else {if(!this[_0x8057[63]][_0x8057[777]]&&this[_0x8057[63]][_0x8057[1401]]===false){return ;} ;} ;} ;var btnObject=this[_0x8057[63]][_0x8057[60]][btnName];this[_0x8057[494]][_0x8057[327]]($(_0x8057[507])[_0x8057[327]](this[_0x8057[32]][_0x8057[31]](btnName,btnObject)));} ,this));} ,append:function (){if(this[_0x8057[63]][_0x8057[1402]]){this[_0x8057[494]][_0x8057[347]](_0x8057[1403]);$(this[_0x8057[63]][_0x8057[1402]])[_0x8057[68]](this.$toolbar);} else {this[_0x8057[381]][_0x8057[328]](this.$toolbar);} ;} ,setFixed:function (){if(!this[_0x8057[62]][_0x8057[470]]()){return ;} ;if(this[_0x8057[63]][_0x8057[1402]]){return ;} ;if(!this[_0x8057[63]][_0x8057[1404]]){return ;} ;this[_0x8057[60]][_0x8057[1405]]();$(this[_0x8057[63]][_0x8057[743]])[_0x8057[425]](_0x8057[1406],$[_0x8057[281]](this[_0x8057[60]][_0x8057[1405]],this));} ,setOverflow:function (){if(this[_0x8057[62]][_0x8057[734]]()&&this[_0x8057[63]][_0x8057[1407]]){this[_0x8057[494]][_0x8057[347]](_0x8057[1408]);} ;} ,isButtonSourceNeeded:function (){if(this[_0x8057[63]][_0x8057[291]]){return ;} ;var index=this[_0x8057[63]][_0x8057[492]][_0x8057[648]](_0x8057[68]);if(index!==-1){this[_0x8057[63]][_0x8057[492]][_0x8057[1347]](index,1);} ;} ,hideButtons:function (){if(this[_0x8057[63]][_0x8057[1409]][_0x8057[21]]===0){return ;} ;$[_0x8057[20]](this[_0x8057[63]][_0x8057[1409]],$[_0x8057[281]](function (i,s){var index=this[_0x8057[63]][_0x8057[492]][_0x8057[648]](s);this[_0x8057[63]][_0x8057[492]][_0x8057[1347]](index,1);} ,this));} ,hideButtonsOnMobile:function (){if(!this[_0x8057[62]][_0x8057[734]]()||this[_0x8057[63]][_0x8057[1410]][_0x8057[21]]===0){return ;} ;$[_0x8057[20]](this[_0x8057[63]][_0x8057[1410]],$[_0x8057[281]](function (i,s){var index=this[_0x8057[63]][_0x8057[492]][_0x8057[648]](s);this[_0x8057[63]][_0x8057[492]][_0x8057[1347]](index,1);} ,this));} ,observeScroll:function (){var scrollTop=$(this[_0x8057[63]][_0x8057[743]])[_0x8057[699]]();var boxTop=1;if(this[_0x8057[63]][_0x8057[743]]===document){boxTop=this[_0x8057[381]][_0x8057[480]]()[_0x8057[482]];} ;if(scrollTop>boxTop){this[_0x8057[60]][_0x8057[1411]](scrollTop,boxTop);} else {this[_0x8057[60]][_0x8057[1412]]();} ;} ,observeScrollEnable:function (scrollTop,boxTop){var top=this[_0x8057[63]][_0x8057[741]]+scrollTop-boxTop;var left=0;var end=boxTop+this[_0x8057[381]][_0x8057[702]]()+30;var width=this[_0x8057[381]][_0x8057[486]]();this[_0x8057[494]][_0x8057[347]](_0x8057[740]);this[_0x8057[494]][_0x8057[274]]({position:_0x8057[744],width:width,top:top+_0x8057[484],left:left});this[_0x8057[60]][_0x8057[1413]]();this[_0x8057[494]][_0x8057[274]](_0x8057[1414],(scrollTop<end)?_0x8057[1415]:_0x8057[1104]);} ,observeScrollDisable:function (){this[_0x8057[494]][_0x8057[274]]({position:_0x8057[1416],width:_0x8057[806],top:0,left:0,visibility:_0x8057[1415]});this[_0x8057[60]][_0x8057[1417]]();this[_0x8057[494]][_0x8057[350]](_0x8057[740]);} ,setDropdownsFixed:function (){var top=this[_0x8057[494]][_0x8057[483]]()+this[_0x8057[63]][_0x8057[741]];var position=_0x8057[742];if(this[_0x8057[63]][_0x8057[743]]!==document){top=(this[_0x8057[494]][_0x8057[483]]()+this[_0x8057[494]][_0x8057[480]]()[_0x8057[482]])+this[_0x8057[63]][_0x8057[741]];position=_0x8057[744];} ;$(_0x8057[751])[_0x8057[20]](function (){$(this)[_0x8057[274]]({position:position,top:top+_0x8057[484]});} );} ,unsetDropdownsFixed:function (){var top=(this[_0x8057[494]][_0x8057[483]]()+this[_0x8057[494]][_0x8057[480]]()[_0x8057[482]]);$(_0x8057[751])[_0x8057[20]](function (){$(this)[_0x8057[274]]({position:_0x8057[744],top:top+_0x8057[484]});} );} };} ,upload:function (){return {init:function (id,url,callback){this[_0x8057[61]][_0x8057[1418]]=false;this[_0x8057[61]][_0x8057[1419]]=callback;this[_0x8057[61]][_0x8057[1036]]=url;this[_0x8057[61]][_0x8057[1420]]=$(id);this[_0x8057[61]][_0x8057[1421]]=$(_0x8057[1422]);this[_0x8057[61]][_0x8057[1423]]=$(_0x8057[1425])[_0x8057[770]](_0x8057[1424]);this[_0x8057[61]][_0x8057[1426]]=$(_0x8057[1427]);this[_0x8057[61]][_0x8057[1423]][_0x8057[327]](this[_0x8057[61]].$input);this[_0x8057[61]][_0x8057[1421]][_0x8057[327]](this[_0x8057[61]].$placeholdler);this[_0x8057[61]][_0x8057[1420]][_0x8057[327]](this[_0x8057[61]].$droparea);this[_0x8057[61]][_0x8057[1421]][_0x8057[708]](_0x8057[1428]);this[_0x8057[61]][_0x8057[1426]][_0x8057[708]](_0x8057[1428]);this[_0x8057[61]][_0x8057[1421]][_0x8057[425]](_0x8057[1429],$[_0x8057[281]](this[_0x8057[61]][_0x8057[818]],this));this[_0x8057[61]][_0x8057[1421]][_0x8057[425]](_0x8057[1430],$[_0x8057[281]](this[_0x8057[61]][_0x8057[1431]],this));this[_0x8057[61]][_0x8057[1426]][_0x8057[425]](_0x8057[1432],$[_0x8057[281]](function (e){e=e[_0x8057[421]]||e;this[_0x8057[61]][_0x8057[1433]](this[_0x8057[61]][_0x8057[1426]][0][_0x8057[416]][0],e);} ,this));this[_0x8057[61]][_0x8057[1421]][_0x8057[425]](_0x8057[1434],$[_0x8057[281]](function (e){e[_0x8057[413]]();this[_0x8057[61]][_0x8057[1421]][_0x8057[350]](_0x8057[1436])[_0x8057[347]](_0x8057[1435]);this[_0x8057[61]][_0x8057[845]](e);} ,this));} ,directUpload:function (file,e){this[_0x8057[61]][_0x8057[1418]]=true;this[_0x8057[61]][_0x8057[1433]](file,e);} ,onDrop:function (e){e=e[_0x8057[421]]||e;var files=e[_0x8057[417]][_0x8057[416]];this[_0x8057[61]][_0x8057[1433]](files[0],e);} ,traverseFile:function (file,e){if(this[_0x8057[63]][_0x8057[1401]]){this[_0x8057[61]][_0x8057[1437]](file);this[_0x8057[61]][_0x8057[1438]](file);return ;} ;var formData=!!window[_0x8057[422]]? new FormData():null;if(window[_0x8057[422]]){this[_0x8057[61]][_0x8057[1437]](file);var name=(this[_0x8057[61]][_0x8057[265]]==_0x8057[40])?this[_0x8057[63]][_0x8057[1439]]:this[_0x8057[63]][_0x8057[1440]];formData[_0x8057[327]](name,file);} ;this[_0x8057[55]][_0x8057[481]]();this[_0x8057[61]][_0x8057[1441]](formData,e);} ,setConfig:function (file){this[_0x8057[61]][_0x8057[1442]](file);if(this[_0x8057[61]][_0x8057[1418]]){this[_0x8057[61]][_0x8057[1036]]=(this[_0x8057[61]][_0x8057[265]]==_0x8057[40])?this[_0x8057[63]][_0x8057[777]]:this[_0x8057[63]][_0x8057[754]];this[_0x8057[61]][_0x8057[1419]]=(this[_0x8057[61]][_0x8057[265]]==_0x8057[40])?this[_0x8057[40]][_0x8057[43]]:this[_0x8057[38]][_0x8057[43]];} ;} ,getType:function (file){this[_0x8057[61]][_0x8057[265]]=_0x8057[40];if(this[_0x8057[63]][_0x8057[1443]][_0x8057[648]](file[_0x8057[265]])==-1){this[_0x8057[61]][_0x8057[265]]=_0x8057[38];} ;} ,getHiddenFields:function (obj,fd){if(obj===false|| typeof obj!==_0x8057[631]){return fd;} ;$[_0x8057[20]](obj,$[_0x8057[281]](function (k,v){if(v!==null&&v.toString()[_0x8057[648]](_0x8057[1039])===0){v=$(v)[_0x8057[388]]();} ;fd[_0x8057[327]](k,v);} ,this));return fd;} ,sendData:function (formData,e){if(this[_0x8057[61]][_0x8057[265]]==_0x8057[40]){formData=this[_0x8057[61]][_0x8057[1445]](this[_0x8057[63]][_0x8057[1444]],formData);formData=this[_0x8057[61]][_0x8057[1445]](this[_0x8057[61]][_0x8057[1446]],formData);} else {formData=this[_0x8057[61]][_0x8057[1445]](this[_0x8057[63]][_0x8057[1447]],formData);formData=this[_0x8057[61]][_0x8057[1445]](this[_0x8057[61]][_0x8057[1448]],formData);} ;var xhr= new XMLHttpRequest();xhr[_0x8057[1450]](_0x8057[1449],this[_0x8057[61]][_0x8057[1036]]);xhr[_0x8057[1451]]=$[_0x8057[281]](function (){if(xhr[_0x8057[1452]]==4){var data=xhr[_0x8057[1453]];data=data[_0x8057[541]](/^\[/,_0x8057[255]);data=data[_0x8057[541]](/\]$/,_0x8057[255]);var json;try{json=( typeof data===_0x8057[8]?$[_0x8057[298]](data):data);} catch(err){json={error:true};} ;this[_0x8057[55]][_0x8057[397]]();if(!this[_0x8057[61]][_0x8057[1418]]){this[_0x8057[61]][_0x8057[1421]][_0x8057[350]](_0x8057[1435]);} ;this[_0x8057[61]][_0x8057[1419]](json,this[_0x8057[61]][_0x8057[1418]],e);} ;} ,this);xhr[_0x8057[1454]](formData);} ,onDrag:function (e){e[_0x8057[413]]();this[_0x8057[61]][_0x8057[1421]][_0x8057[347]](_0x8057[1436]);} ,onDragLeave:function (e){e[_0x8057[413]]();this[_0x8057[61]][_0x8057[1421]][_0x8057[350]](_0x8057[1436]);} ,clearImageFields:function (){this[_0x8057[61]][_0x8057[1446]]={};} ,addImageFields:function (name,value){this[_0x8057[61]][_0x8057[1446]][name]=value;} ,removeImageFields:function (name){delete this[_0x8057[61]][_0x8057[1446]][name];} ,clearFileFields:function (){this[_0x8057[61]][_0x8057[1448]]={};} ,addFileFields:function (name,value){this[_0x8057[61]][_0x8057[1448]][name]=value;} ,removeFileFields:function (name){delete this[_0x8057[61]][_0x8057[1448]][name];} ,s3uploadFile:function (file){this[_0x8057[61]][_0x8057[1456]](file,$[_0x8057[281]](function (signedURL){this[_0x8057[61]][_0x8057[1455]](file,signedURL);} ,this));} ,s3executeOnSignedUrl:function (file,callback){var xhr= new XMLHttpRequest();var mark=_0x8057[1314];if(this[_0x8057[63]][_0x8057[1401]][_0x8057[10]](/\?/)!=_0x8057[11]){mark=_0x8057[661];} ;xhr[_0x8057[1450]](_0x8057[1457],this[_0x8057[63]][_0x8057[1401]]+mark+_0x8057[1458]+file[_0x8057[286]]+_0x8057[1459]+file[_0x8057[265]],true);if(xhr[_0x8057[1460]]){xhr[_0x8057[1460]](_0x8057[1461]);} ;var that=this;xhr[_0x8057[1451]]=function (e){if(this[_0x8057[1452]]==4&&this[_0x8057[1462]]==200){that[_0x8057[55]][_0x8057[481]]();callback(decodeURIComponent(this[_0x8057[1453]]));} else {if(this[_0x8057[1452]]==4&&this[_0x8057[1462]]!=200){} ;} ;} ;xhr[_0x8057[1454]]();} ,s3createCORSRequest:function (method,url){var xhr= new XMLHttpRequest();if(_0x8057[1463] in xhr){xhr[_0x8057[1450]](method,url,true);} else {if( typeof XDomainRequest!=_0x8057[14]){xhr= new XDomainRequest();xhr[_0x8057[1450]](method,url);} else {xhr=null;} ;} ;return xhr;} ,s3uploadToS3:function (file,url){var xhr=this[_0x8057[61]][_0x8057[1465]](_0x8057[1464],url);if(!xhr){} else {xhr[_0x8057[1466]]=$[_0x8057[281]](function (){if(xhr[_0x8057[1462]]==200){this[_0x8057[55]][_0x8057[397]]();var s3file=url[_0x8057[13]](_0x8057[1314]);if(!s3file[0]){return false;} ;if(!this[_0x8057[61]][_0x8057[1418]]){this[_0x8057[61]][_0x8057[1421]][_0x8057[350]](_0x8057[1435]);} ;var json={filelink:s3file[0]};if(this[_0x8057[61]][_0x8057[265]]==_0x8057[38]){var arr=s3file[0][_0x8057[13]](_0x8057[1289]);json[_0x8057[759]]=arr[arr[_0x8057[21]]-1];} ;this[_0x8057[61]][_0x8057[1419]](json,this[_0x8057[61]][_0x8057[1418]],false);} else {} ;} ,this);xhr[_0x8057[1467]]=function (){} ;xhr[_0x8057[61]][_0x8057[1468]]=function (e){} ;xhr[_0x8057[1470]](_0x8057[1469],file[_0x8057[265]]);xhr[_0x8057[1470]](_0x8057[1471],_0x8057[1472]);xhr[_0x8057[1454]](file);} ;} };} ,utils:function (){return {isMobile:function (){return /(iPhone|iPod|BlackBerry|Android)/[_0x8057[604]](navigator[_0x8057[1473]]);} ,isDesktop:function (){return !/(iPhone|iPod|iPad|BlackBerry|Android)/[_0x8057[604]](navigator[_0x8057[1473]]);} ,isString:function (obj){return Object[_0x8057[2]][_0x8057[1474]][_0x8057[6]](obj)==_0x8057[1475];} ,isEmpty:function (html,removeEmptyTags){html=html[_0x8057[541]](/[\u200B-\u200D\uFEFF]/g,_0x8057[255]);html=html[_0x8057[541]](/&nbsp;/gi,_0x8057[255]);html=html[_0x8057[541]](/<\/?br\s?\/?>/g,_0x8057[255]);html=html[_0x8057[541]](/\s/g,_0x8057[255]);html=html[_0x8057[541]](/^<p>[^\W\w\D\d]*?<\/p>$/i,_0x8057[255]);html=html[_0x8057[541]](/<iframe(.*?[^>])>$/i,_0x8057[628]);if(removeEmptyTags!==false){html=html[_0x8057[541]](/<[^\/>][^>]*><\/[^>]+>/gi,_0x8057[255]);html=html[_0x8057[541]](/<[^\/>][^>]*><\/[^>]+>/gi,_0x8057[255]);} ;html=$[_0x8057[390]](html);return html===_0x8057[255];} ,normalize:function (str){if( typeof (str)===_0x8057[14]){return 0;} ;return parseInt(str[_0x8057[541]](_0x8057[484],_0x8057[255]),10);} ,hexToRgb:function (hex){if( typeof hex==_0x8057[14]){return ;} ;if(hex[_0x8057[10]](/^#/)==-1){return hex;} ;var shorthandRegex=/^#?([a-f\d])([a-f\d])([a-f\d])$/i;hex=hex[_0x8057[541]](shorthandRegex,function (m,r,g,b){return r+r+g+g+b+b;} );var result=/^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i[_0x8057[1342]](hex);return _0x8057[1476]+parseInt(result[1],16)+_0x8057[668]+parseInt(result[2],16)+_0x8057[668]+parseInt(result[3],16)+_0x8057[1311];} ,getOuterHtml:function (el){return $(_0x8057[548])[_0x8057[327]]($(el)[_0x8057[1477]](0)[_0x8057[1011]]())[_0x8057[68]]();} ,getAlignmentElement:function (el){if($[_0x8057[306]](el[_0x8057[311]],this[_0x8057[63]][_0x8057[236]])!==-1){return $(el);} else {return $(el)[_0x8057[352]](this[_0x8057[63]][_0x8057[236]].toString()[_0x8057[325]](),this[_0x8057[261]][0]);} ;} ,removeEmptyAttr:function (el,attr){var $el=$(el);if( typeof $el[_0x8057[276]](attr)==_0x8057[14]){return true;} ;if($el[_0x8057[276]](attr)===_0x8057[255]){$el[_0x8057[346]](attr);return true;} ;return false;} ,removeEmpty:function (i,s){var $s=$(s);$s[_0x8057[332]](_0x8057[1478])[_0x8057[346]](_0x8057[90])[_0x8057[346]](_0x8057[301]);if($s[_0x8057[332]](_0x8057[1479])[_0x8057[21]]!==0){return ;} ;var text=$[_0x8057[390]]($s[_0x8057[770]]());if(this[_0x8057[62]][_0x8057[293]](text,false)){$s[_0x8057[331]]();} ;} ,saveScroll:function (){if(this[_0x8057[62]][_0x8057[588]]()){return ;} ;this[_0x8057[1480]]=this[_0x8057[261]][_0x8057[699]]();this[_0x8057[1232]]=$(window)[_0x8057[699]]();if(this[_0x8057[63]][_0x8057[1481]]){this[_0x8057[1482]]=$(this[_0x8057[63]][_0x8057[1481]])[_0x8057[699]]();} ;} ,restoreScroll:function (){if( typeof this[_0x8057[1019]]===_0x8057[14]&& typeof this[_0x8057[1232]]===_0x8057[14]){return ;} ;$(window)[_0x8057[699]](this[_0x8057[1232]]);this[_0x8057[261]][_0x8057[699]](this[_0x8057[1480]]);if(this[_0x8057[63]][_0x8057[1481]]){$(this[_0x8057[63]][_0x8057[1481]])[_0x8057[699]](this[_0x8057[1482]]);} ;} ,createSpaceElement:function (){var space=document[_0x8057[652]](_0x8057[101]);space[_0x8057[942]]=_0x8057[967];space[_0x8057[514]]=this[_0x8057[63]][_0x8057[515]];return space;} ,removeInlineTags:function (node){var tags=this[_0x8057[63]][_0x8057[678]];tags[_0x8057[16]](_0x8057[101]);if(node[_0x8057[311]]==_0x8057[155]){tags[_0x8057[16]](_0x8057[118]);} ;$(node)[_0x8057[332]](tags[_0x8057[241]](_0x8057[357]))[_0x8057[498]](_0x8057[1259])[_0x8057[336]]()[_0x8057[335]]();} ,replaceWithContents:function (node,removeInlineTags){var self=this;$(node)[_0x8057[284]](function (){if(removeInlineTags===true){self[_0x8057[62]][_0x8057[333]](this);} ;return $(this)[_0x8057[336]]();} );} ,replaceToTag:function (node,tag,removeInlineTags){var replacement;var self=this;$(node)[_0x8057[284]](function (){replacement=$(_0x8057[320]+tag+_0x8057[1357])[_0x8057[327]]($(this)[_0x8057[336]]());for(var i=0;i<this[_0x8057[910]][_0x8057[21]];i++){replacement[_0x8057[276]](this[_0x8057[910]][i][_0x8057[286]],this[_0x8057[910]][i][_0x8057[309]]);} ;if(removeInlineTags===true){self[_0x8057[62]][_0x8057[333]](replacement);} ;return replacement;} );return replacement;} ,isStartOfElement:function (){var block=this[_0x8057[56]][_0x8057[323]]();if(!block){return false;} ;var offset=this[_0x8057[33]][_0x8057[1483]](block);return (offset===0)?true:false;} ,isEndOfElement:function (){var block=this[_0x8057[56]][_0x8057[323]]();if(!block){return false;} ;var offset=this[_0x8057[33]][_0x8057[1483]](block);var text=$[_0x8057[390]]($(block)[_0x8057[770]]())[_0x8057[541]](/\n\r\n/g,_0x8057[255]);return (offset==text[_0x8057[21]])?true:false;} ,isEndOfEditor:function (){var block=this[_0x8057[261]][0];var offset=this[_0x8057[33]][_0x8057[1483]](block);var text=$[_0x8057[390]]($(block)[_0x8057[770]]())[_0x8057[541]](/\n\r\n/g,_0x8057[255]);return (offset==text[_0x8057[21]])?true:false;} ,isBlock:function (block){block=block[0]||block;return block&&this[_0x8057[62]][_0x8057[513]](block[_0x8057[311]]);} ,isBlockTag:function (tag){if( typeof tag==_0x8057[14]){return false;} ;return this[_0x8057[238]][_0x8057[604]](tag);} ,isTag:function (current,tag){var element=$(current)[_0x8057[352]](tag);if(element[_0x8057[21]]==1){return element[0];} ;return false;} ,isSelectAll:function (){return this[_0x8057[1484]];} ,enableSelectAll:function (){this[_0x8057[1484]]=true;} ,disableSelectAll:function (){this[_0x8057[1484]]=false;} ,isRedactorParent:function (el){if(!el){return false;} ;if($(el)[_0x8057[452]](_0x8057[1485])[_0x8057[21]]===0||$(el)[_0x8057[448]](_0x8057[396])){return false;} ;return el;} ,isCurrentOrParentHeader:function (){return this[_0x8057[62]][_0x8057[590]]([_0x8057[135],_0x8057[136],_0x8057[137],_0x8057[138],_0x8057[139],_0x8057[140]]);} ,isCurrentOrParent:function (tagName){var parent=this[_0x8057[56]][_0x8057[911]]();var current=this[_0x8057[56]][_0x8057[883]]();if($[_0x8057[1359]](tagName)){var matched=0;$[_0x8057[20]](tagName,$[_0x8057[281]](function (i,s){if(this[_0x8057[62]][_0x8057[1486]](current,parent,s)){matched++;} ;} ,this));return (matched===0)?false:true;} else {return this[_0x8057[62]][_0x8057[1486]](current,parent,tagName);} ;} ,isCurrentOrParentOne:function (current,parent,tagName){tagName=tagName[_0x8057[1487]]();return parent&&parent[_0x8057[311]]===tagName?parent:current&&current[_0x8057[311]]===tagName?current:false;} ,isOldIe:function (){return (this[_0x8057[62]][_0x8057[260]](_0x8057[259])&&parseInt(this[_0x8057[62]][_0x8057[260]](_0x8057[1488]),10)<9)?true:false;} ,isLessIe10:function (){return (this[_0x8057[62]][_0x8057[260]](_0x8057[259])&&parseInt(this[_0x8057[62]][_0x8057[260]](_0x8057[1488]),10)<10)?true:false;} ,isIe11:function (){return !!navigator[_0x8057[1473]][_0x8057[647]](/Trident\/7\./);} ,browser:function (browser){var ua=navigator[_0x8057[1473]][_0x8057[325]]();var match=/(opr)[\/]([\w.]+)/[_0x8057[1342]](ua)||/(chrome)[ \/]([\w.]+)/[_0x8057[1342]](ua)||/(webkit)[ \/]([\w.]+).*(safari)[ \/]([\w.]+)/[_0x8057[1342]](ua)||/(webkit)[ \/]([\w.]+)/[_0x8057[1342]](ua)||/(opera)(?:.*version|)[ \/]([\w.]+)/[_0x8057[1342]](ua)||/(msie) ([\w.]+)/[_0x8057[1342]](ua)||ua[_0x8057[648]](_0x8057[1489])>=0&&/(rv)(?::| )([\w.]+)/[_0x8057[1342]](ua)||ua[_0x8057[648]](_0x8057[1490])<0&&/(mozilla)(?:.*? rv:([\w.]+)|)/[_0x8057[1342]](ua)||[];if(browser==_0x8057[1491]){return ( typeof match[3]!=_0x8057[14])?match[3]==_0x8057[1491]:false;} ;if(browser==_0x8057[1488]){return match[2];} ;if(browser==_0x8057[1492]){return (match[1]==_0x8057[1493]||match[1]==_0x8057[1492]);} ;if(match[1]==_0x8057[1494]){return browser==_0x8057[259];} ;if(match[1]==_0x8057[1495]){return browser==_0x8057[1492];} ;return browser==match[1];} };} };$(window)[_0x8057[425]](_0x8057[1496],function (){$(_0x8057[1497])[_0x8057[4]]();} );Redactor[_0x8057[2]][_0x8057[22]][_0x8057[2]]=Redactor[_0x8057[2]];$[_0x8057[23]][_0x8057[5]][_0x8057[1008]]=function (protocol,convertLinks,convertUrlLinks,convertImageLinks,convertVideoLinks,linkSize){var urlCheck=_0x8057[1498];var regex= new RegExp(urlCheck,_0x8057[563]);var rProtocol=/(https?|ftp):\/\//i;var urlImage=/(https?:\/\/.*\.(?:png|jpg|jpeg|gif))/gi;var childNodes=(this[_0x8057[261]]?this[_0x8057[261]][0]:this)[_0x8057[1499]],i=childNodes[_0x8057[21]];while(i--){var n=childNodes[i];if(n[_0x8057[769]]===3&&n[_0x8057[773]]!==_0x8057[155]){var html=n[_0x8057[536]];if(convertVideoLinks&&html){var iframeStart=_0x8057[1500],iframeEnd=_0x8057[1501];if(html[_0x8057[647]](reUrlYoutube)){html=html[_0x8057[541]](reUrlYoutube,iframeStart+_0x8057[1502]+iframeEnd);$(n)[_0x8057[508]](html)[_0x8057[331]]();} else {if(html[_0x8057[647]](reUrlVimeo)){html=html[_0x8057[541]](reUrlVimeo,iframeStart+_0x8057[1503]+iframeEnd);$(n)[_0x8057[508]](html)[_0x8057[331]]();} ;} ;} ;if(convertImageLinks&&html&&html[_0x8057[647]](urlImage)){html=html[_0x8057[541]](urlImage,_0x8057[1504]);$(n)[_0x8057[508]](html)[_0x8057[331]]();return ;} ;if(html[_0x8057[10]](/\$/g)!=-1){html=html[_0x8057[541]](/\$/g,_0x8057[542]);} ;var matches=html[_0x8057[647]](regex);if(convertUrlLinks&&html&&matches){var len=matches[_0x8057[21]];for(var z=0;z<len;z++){if(matches[z][_0x8057[647]](/\.$/)!==null){matches[z]=matches[z][_0x8057[541]](/\.$/,_0x8057[255]);} ;var href=matches[z];var text=href;var space=_0x8057[255];if(href[_0x8057[647]](/\s$/)!==null){space=_0x8057[553];} ;var addProtocol=protocol+_0x8057[813];if(href[_0x8057[647]](rProtocol)!==null){addProtocol=_0x8057[255];} ;if(text[_0x8057[21]]>linkSize){text=text[_0x8057[713]](0,linkSize)+_0x8057[1169];} ;text=text[_0x8057[541]](/&#36;/g,_0x8057[1309])[_0x8057[541]](/&/g,_0x8057[665])[_0x8057[541]](/</g,_0x8057[664])[_0x8057[541]](/>/g,_0x8057[663]);html=html[_0x8057[541]](href,_0x8057[760]+addProtocol+$[_0x8057[390]](href)+_0x8057[730]+$[_0x8057[390]](text)+_0x8057[731]+space);} ;$(n)[_0x8057[508]](html)[_0x8057[331]]();} ;} else {if(n[_0x8057[769]]===1&&!/^(pre|a|button|textarea)$/i[_0x8057[604]](n[_0x8057[311]])){$[_0x8057[23]][_0x8057[5]][_0x8057[1008]][_0x8057[6]](n,protocol,convertLinks,convertUrlLinks,convertImageLinks,convertVideoLinks,linkSize);} ;} ;} ;} ;} )(jQuery);
/*!
 * Masonry PACKAGED v3.2.2
 * Cascading grid layout library
 * http://masonry.desandro.com
 * MIT License
 * by David DeSandro
 */


!function(a){function b(){}function c(a){function c(b){b.prototype.option||(b.prototype.option=function(b){a.isPlainObject(b)&&(this.options=a.extend(!0,this.options,b))})}function e(b,c){a.fn[b]=function(e){if("string"==typeof e){for(var g=d.call(arguments,1),h=0,i=this.length;i>h;h++){var j=this[h],k=a.data(j,b);if(k)if(a.isFunction(k[e])&&"_"!==e.charAt(0)){var l=k[e].apply(k,g);if(void 0!==l)return l}else f("no such method '"+e+"' for "+b+" instance");else f("cannot call methods on "+b+" prior to initialization; attempted to call '"+e+"'")}return this}return this.each(function(){var d=a.data(this,b);d?(d.option(e),d._init()):(d=new c(this,e),a.data(this,b,d))})}}if(a){var f="undefined"==typeof console?b:function(a){console.error(a)};return a.bridget=function(a,b){c(b),e(a,b)},a.bridget}}var d=Array.prototype.slice;"function"==typeof define&&define.amd?define("jquery-bridget/jquery.bridget",["jquery"],c):c("object"==typeof exports?require("jquery"):a.jQuery)}(window),function(a){function b(b){var c=a.event;return c.target=c.target||c.srcElement||b,c}var c=document.documentElement,d=function(){};c.addEventListener?d=function(a,b,c){a.addEventListener(b,c,!1)}:c.attachEvent&&(d=function(a,c,d){a[c+d]=d.handleEvent?function(){var c=b(a);d.handleEvent.call(d,c)}:function(){var c=b(a);d.call(a,c)},a.attachEvent("on"+c,a[c+d])});var e=function(){};c.removeEventListener?e=function(a,b,c){a.removeEventListener(b,c,!1)}:c.detachEvent&&(e=function(a,b,c){a.detachEvent("on"+b,a[b+c]);try{delete a[b+c]}catch(d){a[b+c]=void 0}});var f={bind:d,unbind:e};"function"==typeof define&&define.amd?define("eventie/eventie",f):"object"==typeof exports?module.exports=f:a.eventie=f}(this),function(a){function b(a){"function"==typeof a&&(b.isReady?a():g.push(a))}function c(a){var c="readystatechange"===a.type&&"complete"!==f.readyState;b.isReady||c||d()}function d(){b.isReady=!0;for(var a=0,c=g.length;c>a;a++){var d=g[a];d()}}function e(e){return"complete"===f.readyState?d():(e.bind(f,"DOMContentLoaded",c),e.bind(f,"readystatechange",c),e.bind(a,"load",c)),b}var f=a.document,g=[];b.isReady=!1,"function"==typeof define&&define.amd?define("doc-ready/doc-ready",["eventie/eventie"],e):"object"==typeof exports?module.exports=e(require("eventie")):a.docReady=e(a.eventie)}(window),function(){function a(){}function b(a,b){for(var c=a.length;c--;)if(a[c].listener===b)return c;return-1}function c(a){return function(){return this[a].apply(this,arguments)}}var d=a.prototype,e=this,f=e.EventEmitter;d.getListeners=function(a){var b,c,d=this._getEvents();if(a instanceof RegExp){b={};for(c in d)d.hasOwnProperty(c)&&a.test(c)&&(b[c]=d[c])}else b=d[a]||(d[a]=[]);return b},d.flattenListeners=function(a){var b,c=[];for(b=0;b<a.length;b+=1)c.push(a[b].listener);return c},d.getListenersAsObject=function(a){var b,c=this.getListeners(a);return c instanceof Array&&(b={},b[a]=c),b||c},d.addListener=function(a,c){var d,e=this.getListenersAsObject(a),f="object"==typeof c;for(d in e)e.hasOwnProperty(d)&&-1===b(e[d],c)&&e[d].push(f?c:{listener:c,once:!1});return this},d.on=c("addListener"),d.addOnceListener=function(a,b){return this.addListener(a,{listener:b,once:!0})},d.once=c("addOnceListener"),d.defineEvent=function(a){return this.getListeners(a),this},d.defineEvents=function(a){for(var b=0;b<a.length;b+=1)this.defineEvent(a[b]);return this},d.removeListener=function(a,c){var d,e,f=this.getListenersAsObject(a);for(e in f)f.hasOwnProperty(e)&&(d=b(f[e],c),-1!==d&&f[e].splice(d,1));return this},d.off=c("removeListener"),d.addListeners=function(a,b){return this.manipulateListeners(!1,a,b)},d.removeListeners=function(a,b){return this.manipulateListeners(!0,a,b)},d.manipulateListeners=function(a,b,c){var d,e,f=a?this.removeListener:this.addListener,g=a?this.removeListeners:this.addListeners;if("object"!=typeof b||b instanceof RegExp)for(d=c.length;d--;)f.call(this,b,c[d]);else for(d in b)b.hasOwnProperty(d)&&(e=b[d])&&("function"==typeof e?f.call(this,d,e):g.call(this,d,e));return this},d.removeEvent=function(a){var b,c=typeof a,d=this._getEvents();if("string"===c)delete d[a];else if(a instanceof RegExp)for(b in d)d.hasOwnProperty(b)&&a.test(b)&&delete d[b];else delete this._events;return this},d.removeAllListeners=c("removeEvent"),d.emitEvent=function(a,b){var c,d,e,f,g=this.getListenersAsObject(a);for(e in g)if(g.hasOwnProperty(e))for(d=g[e].length;d--;)c=g[e][d],c.once===!0&&this.removeListener(a,c.listener),f=c.listener.apply(this,b||[]),f===this._getOnceReturnValue()&&this.removeListener(a,c.listener);return this},d.trigger=c("emitEvent"),d.emit=function(a){var b=Array.prototype.slice.call(arguments,1);return this.emitEvent(a,b)},d.setOnceReturnValue=function(a){return this._onceReturnValue=a,this},d._getOnceReturnValue=function(){return this.hasOwnProperty("_onceReturnValue")?this._onceReturnValue:!0},d._getEvents=function(){return this._events||(this._events={})},a.noConflict=function(){return e.EventEmitter=f,a},"function"==typeof define&&define.amd?define("eventEmitter/EventEmitter",[],function(){return a}):"object"==typeof module&&module.exports?module.exports=a:e.EventEmitter=a}.call(this),function(a){function b(a){if(a){if("string"==typeof d[a])return a;a=a.charAt(0).toUpperCase()+a.slice(1);for(var b,e=0,f=c.length;f>e;e++)if(b=c[e]+a,"string"==typeof d[b])return b}}var c="Webkit Moz ms Ms O".split(" "),d=document.documentElement.style;"function"==typeof define&&define.amd?define("get-style-property/get-style-property",[],function(){return b}):"object"==typeof exports?module.exports=b:a.getStyleProperty=b}(window),function(a){function b(a){var b=parseFloat(a),c=-1===a.indexOf("%")&&!isNaN(b);return c&&b}function c(){}function d(){for(var a={width:0,height:0,innerWidth:0,innerHeight:0,outerWidth:0,outerHeight:0},b=0,c=g.length;c>b;b++){var d=g[b];a[d]=0}return a}function e(c){function e(){if(!m){m=!0;var d=a.getComputedStyle;if(j=function(){var a=d?function(a){return d(a,null)}:function(a){return a.currentStyle};return function(b){var c=a(b);return c||f("Style returned "+c+". Are you running this code in a hidden iframe on Firefox? See http://bit.ly/getsizebug1"),c}}(),k=c("boxSizing")){var e=document.createElement("div");e.style.width="200px",e.style.padding="1px 2px 3px 4px",e.style.borderStyle="solid",e.style.borderWidth="1px 2px 3px 4px",e.style[k]="border-box";var g=document.body||document.documentElement;g.appendChild(e);var h=j(e);l=200===b(h.width),g.removeChild(e)}}}function h(a){if(e(),"string"==typeof a&&(a=document.querySelector(a)),a&&"object"==typeof a&&a.nodeType){var c=j(a);if("none"===c.display)return d();var f={};f.width=a.offsetWidth,f.height=a.offsetHeight;for(var h=f.isBorderBox=!(!k||!c[k]||"border-box"!==c[k]),m=0,n=g.length;n>m;m++){var o=g[m],p=c[o];p=i(a,p);var q=parseFloat(p);f[o]=isNaN(q)?0:q}var r=f.paddingLeft+f.paddingRight,s=f.paddingTop+f.paddingBottom,t=f.marginLeft+f.marginRight,u=f.marginTop+f.marginBottom,v=f.borderLeftWidth+f.borderRightWidth,w=f.borderTopWidth+f.borderBottomWidth,x=h&&l,y=b(c.width);y!==!1&&(f.width=y+(x?0:r+v));var z=b(c.height);return z!==!1&&(f.height=z+(x?0:s+w)),f.innerWidth=f.width-(r+v),f.innerHeight=f.height-(s+w),f.outerWidth=f.width+t,f.outerHeight=f.height+u,f}}function i(b,c){if(a.getComputedStyle||-1===c.indexOf("%"))return c;var d=b.style,e=d.left,f=b.runtimeStyle,g=f&&f.left;return g&&(f.left=b.currentStyle.left),d.left=c,c=d.pixelLeft,d.left=e,g&&(f.left=g),c}var j,k,l,m=!1;return h}var f="undefined"==typeof console?c:function(a){console.error(a)},g=["paddingLeft","paddingRight","paddingTop","paddingBottom","marginLeft","marginRight","marginTop","marginBottom","borderLeftWidth","borderRightWidth","borderTopWidth","borderBottomWidth"];"function"==typeof define&&define.amd?define("get-size/get-size",["get-style-property/get-style-property"],e):"object"==typeof exports?module.exports=e(require("desandro-get-style-property")):a.getSize=e(a.getStyleProperty)}(window),function(a){function b(a,b){return a[g](b)}function c(a){if(!a.parentNode){var b=document.createDocumentFragment();b.appendChild(a)}}function d(a,b){c(a);for(var d=a.parentNode.querySelectorAll(b),e=0,f=d.length;f>e;e++)if(d[e]===a)return!0;return!1}function e(a,d){return c(a),b(a,d)}var f,g=function(){if(a.matchesSelector)return"matchesSelector";for(var b=["webkit","moz","ms","o"],c=0,d=b.length;d>c;c++){var e=b[c],f=e+"MatchesSelector";if(a[f])return f}}();if(g){var h=document.createElement("div"),i=b(h,"div");f=i?b:e}else f=d;"function"==typeof define&&define.amd?define("matches-selector/matches-selector",[],function(){return f}):"object"==typeof exports?module.exports=f:window.matchesSelector=f}(Element.prototype),function(a){function b(a,b){for(var c in b)a[c]=b[c];return a}function c(a){for(var b in a)return!1;return b=null,!0}function d(a){return a.replace(/([A-Z])/g,function(a){return"-"+a.toLowerCase()})}function e(a,e,f){function h(a,b){a&&(this.element=a,this.layout=b,this.position={x:0,y:0},this._create())}var i=f("transition"),j=f("transform"),k=i&&j,l=!!f("perspective"),m={WebkitTransition:"webkitTransitionEnd",MozTransition:"transitionend",OTransition:"otransitionend",transition:"transitionend"}[i],n=["transform","transition","transitionDuration","transitionProperty"],o=function(){for(var a={},b=0,c=n.length;c>b;b++){var d=n[b],e=f(d);e&&e!==d&&(a[d]=e)}return a}();b(h.prototype,a.prototype),h.prototype._create=function(){this._transn={ingProperties:{},clean:{},onEnd:{}},this.css({position:"absolute"})},h.prototype.handleEvent=function(a){var b="on"+a.type;this[b]&&this[b](a)},h.prototype.getSize=function(){this.size=e(this.element)},h.prototype.css=function(a){var b=this.element.style;for(var c in a){var d=o[c]||c;b[d]=a[c]}},h.prototype.getPosition=function(){var a=g(this.element),b=this.layout.options,c=b.isOriginLeft,d=b.isOriginTop,e=parseInt(a[c?"left":"right"],10),f=parseInt(a[d?"top":"bottom"],10);e=isNaN(e)?0:e,f=isNaN(f)?0:f;var h=this.layout.size;e-=c?h.paddingLeft:h.paddingRight,f-=d?h.paddingTop:h.paddingBottom,this.position.x=e,this.position.y=f},h.prototype.layoutPosition=function(){var a=this.layout.size,b=this.layout.options,c={};b.isOriginLeft?(c.left=this.position.x+a.paddingLeft+"px",c.right=""):(c.right=this.position.x+a.paddingRight+"px",c.left=""),b.isOriginTop?(c.top=this.position.y+a.paddingTop+"px",c.bottom=""):(c.bottom=this.position.y+a.paddingBottom+"px",c.top=""),this.css(c),this.emitEvent("layout",[this])};var p=l?function(a,b){return"translate3d("+a+"px, "+b+"px, 0)"}:function(a,b){return"translate("+a+"px, "+b+"px)"};h.prototype._transitionTo=function(a,b){this.getPosition();var c=this.position.x,d=this.position.y,e=parseInt(a,10),f=parseInt(b,10),g=e===this.position.x&&f===this.position.y;if(this.setPosition(a,b),g&&!this.isTransitioning)return void this.layoutPosition();var h=a-c,i=b-d,j={},k=this.layout.options;h=k.isOriginLeft?h:-h,i=k.isOriginTop?i:-i,j.transform=p(h,i),this.transition({to:j,onTransitionEnd:{transform:this.layoutPosition},isCleaning:!0})},h.prototype.goTo=function(a,b){this.setPosition(a,b),this.layoutPosition()},h.prototype.moveTo=k?h.prototype._transitionTo:h.prototype.goTo,h.prototype.setPosition=function(a,b){this.position.x=parseInt(a,10),this.position.y=parseInt(b,10)},h.prototype._nonTransition=function(a){this.css(a.to),a.isCleaning&&this._removeStyles(a.to);for(var b in a.onTransitionEnd)a.onTransitionEnd[b].call(this)},h.prototype._transition=function(a){if(!parseFloat(this.layout.options.transitionDuration))return void this._nonTransition(a);var b=this._transn;for(var c in a.onTransitionEnd)b.onEnd[c]=a.onTransitionEnd[c];for(c in a.to)b.ingProperties[c]=!0,a.isCleaning&&(b.clean[c]=!0);if(a.from){this.css(a.from);var d=this.element.offsetHeight;d=null}this.enableTransition(a.to),this.css(a.to),this.isTransitioning=!0};var q=j&&d(j)+",opacity";h.prototype.enableTransition=function(){this.isTransitioning||(this.css({transitionProperty:q,transitionDuration:this.layout.options.transitionDuration}),this.element.addEventListener(m,this,!1))},h.prototype.transition=h.prototype[i?"_transition":"_nonTransition"],h.prototype.onwebkitTransitionEnd=function(a){this.ontransitionend(a)},h.prototype.onotransitionend=function(a){this.ontransitionend(a)};var r={"-webkit-transform":"transform","-moz-transform":"transform","-o-transform":"transform"};h.prototype.ontransitionend=function(a){if(a.target===this.element){var b=this._transn,d=r[a.propertyName]||a.propertyName;if(delete b.ingProperties[d],c(b.ingProperties)&&this.disableTransition(),d in b.clean&&(this.element.style[a.propertyName]="",delete b.clean[d]),d in b.onEnd){var e=b.onEnd[d];e.call(this),delete b.onEnd[d]}this.emitEvent("transitionEnd",[this])}},h.prototype.disableTransition=function(){this.removeTransitionStyles(),this.element.removeEventListener(m,this,!1),this.isTransitioning=!1},h.prototype._removeStyles=function(a){var b={};for(var c in a)b[c]="";this.css(b)};var s={transitionProperty:"",transitionDuration:""};return h.prototype.removeTransitionStyles=function(){this.css(s)},h.prototype.removeElem=function(){this.element.parentNode.removeChild(this.element),this.emitEvent("remove",[this])},h.prototype.remove=function(){if(!i||!parseFloat(this.layout.options.transitionDuration))return void this.removeElem();var a=this;this.on("transitionEnd",function(){return a.removeElem(),!0}),this.hide()},h.prototype.reveal=function(){delete this.isHidden,this.css({display:""});var a=this.layout.options;this.transition({from:a.hiddenStyle,to:a.visibleStyle,isCleaning:!0})},h.prototype.hide=function(){this.isHidden=!0,this.css({display:""});var a=this.layout.options;this.transition({from:a.visibleStyle,to:a.hiddenStyle,isCleaning:!0,onTransitionEnd:{opacity:function(){this.isHidden&&this.css({display:"none"})}}})},h.prototype.destroy=function(){this.css({position:"",left:"",right:"",top:"",bottom:"",transition:"",transform:""})},h}var f=a.getComputedStyle,g=f?function(a){return f(a,null)}:function(a){return a.currentStyle};"function"==typeof define&&define.amd?define("outlayer/item",["eventEmitter/EventEmitter","get-size/get-size","get-style-property/get-style-property"],e):"object"==typeof exports?module.exports=e(require("wolfy87-eventemitter"),require("get-size"),require("desandro-get-style-property")):(a.Outlayer={},a.Outlayer.Item=e(a.EventEmitter,a.getSize,a.getStyleProperty))}(window),function(a){function b(a,b){for(var c in b)a[c]=b[c];return a}function c(a){return"[object Array]"===l.call(a)}function d(a){var b=[];if(c(a))b=a;else if(a&&"number"==typeof a.length)for(var d=0,e=a.length;e>d;d++)b.push(a[d]);else b.push(a);return b}function e(a,b){var c=n(b,a);-1!==c&&b.splice(c,1)}function f(a){return a.replace(/(.)([A-Z])/g,function(a,b,c){return b+"-"+c}).toLowerCase()}function g(c,g,l,n,o,p){function q(a,c){if("string"==typeof a&&(a=h.querySelector(a)),!a||!m(a))return void(i&&i.error("Bad "+this.constructor.namespace+" element: "+a));this.element=a,this.options=b({},this.constructor.defaults),this.option(c);var d=++r;this.element.outlayerGUID=d,s[d]=this,this._create(),this.options.isInitLayout&&this.layout()}var r=0,s={};return q.namespace="outlayer",q.Item=p,q.defaults={containerStyle:{position:"relative"},isInitLayout:!0,isOriginLeft:!0,isOriginTop:!0,isResizeBound:!0,isResizingContainer:!0,transitionDuration:"0.4s",hiddenStyle:{opacity:0,transform:"scale(0.001)"},visibleStyle:{opacity:1,transform:"scale(1)"}},b(q.prototype,l.prototype),q.prototype.option=function(a){b(this.options,a)},q.prototype._create=function(){this.reloadItems(),this.stamps=[],this.stamp(this.options.stamp),b(this.element.style,this.options.containerStyle),this.options.isResizeBound&&this.bindResize()},q.prototype.reloadItems=function(){this.items=this._itemize(this.element.children)},q.prototype._itemize=function(a){for(var b=this._filterFindItemElements(a),c=this.constructor.Item,d=[],e=0,f=b.length;f>e;e++){var g=b[e],h=new c(g,this);d.push(h)}return d},q.prototype._filterFindItemElements=function(a){a=d(a);for(var b=this.options.itemSelector,c=[],e=0,f=a.length;f>e;e++){var g=a[e];if(m(g))if(b){o(g,b)&&c.push(g);for(var h=g.querySelectorAll(b),i=0,j=h.length;j>i;i++)c.push(h[i])}else c.push(g)}return c},q.prototype.getItemElements=function(){for(var a=[],b=0,c=this.items.length;c>b;b++)a.push(this.items[b].element);return a},q.prototype.layout=function(){this._resetLayout(),this._manageStamps();var a=void 0!==this.options.isLayoutInstant?this.options.isLayoutInstant:!this._isLayoutInited;this.layoutItems(this.items,a),this._isLayoutInited=!0},q.prototype._init=q.prototype.layout,q.prototype._resetLayout=function(){this.getSize()},q.prototype.getSize=function(){this.size=n(this.element)},q.prototype._getMeasurement=function(a,b){var c,d=this.options[a];d?("string"==typeof d?c=this.element.querySelector(d):m(d)&&(c=d),this[a]=c?n(c)[b]:d):this[a]=0},q.prototype.layoutItems=function(a,b){a=this._getItemsForLayout(a),this._layoutItems(a,b),this._postLayout()},q.prototype._getItemsForLayout=function(a){for(var b=[],c=0,d=a.length;d>c;c++){var e=a[c];e.isIgnored||b.push(e)}return b},q.prototype._layoutItems=function(a,b){function c(){d.emitEvent("layoutComplete",[d,a])}var d=this;if(!a||!a.length)return void c();this._itemsOn(a,"layout",c);for(var e=[],f=0,g=a.length;g>f;f++){var h=a[f],i=this._getItemLayoutPosition(h);i.item=h,i.isInstant=b||h.isLayoutInstant,e.push(i)}this._processLayoutQueue(e)},q.prototype._getItemLayoutPosition=function(){return{x:0,y:0}},q.prototype._processLayoutQueue=function(a){for(var b=0,c=a.length;c>b;b++){var d=a[b];this._positionItem(d.item,d.x,d.y,d.isInstant)}},q.prototype._positionItem=function(a,b,c,d){d?a.goTo(b,c):a.moveTo(b,c)},q.prototype._postLayout=function(){this.resizeContainer()},q.prototype.resizeContainer=function(){if(this.options.isResizingContainer){var a=this._getContainerSize();a&&(this._setContainerMeasure(a.width,!0),this._setContainerMeasure(a.height,!1))}},q.prototype._getContainerSize=k,q.prototype._setContainerMeasure=function(a,b){if(void 0!==a){var c=this.size;c.isBorderBox&&(a+=b?c.paddingLeft+c.paddingRight+c.borderLeftWidth+c.borderRightWidth:c.paddingBottom+c.paddingTop+c.borderTopWidth+c.borderBottomWidth),a=Math.max(a,0),this.element.style[b?"width":"height"]=a+"px"}},q.prototype._itemsOn=function(a,b,c){function d(){return e++,e===f&&c.call(g),!0}for(var e=0,f=a.length,g=this,h=0,i=a.length;i>h;h++){var j=a[h];j.on(b,d)}},q.prototype.ignore=function(a){var b=this.getItem(a);b&&(b.isIgnored=!0)},q.prototype.unignore=function(a){var b=this.getItem(a);b&&delete b.isIgnored},q.prototype.stamp=function(a){if(a=this._find(a)){this.stamps=this.stamps.concat(a);for(var b=0,c=a.length;c>b;b++){var d=a[b];this.ignore(d)}}},q.prototype.unstamp=function(a){if(a=this._find(a))for(var b=0,c=a.length;c>b;b++){var d=a[b];e(d,this.stamps),this.unignore(d)}},q.prototype._find=function(a){return a?("string"==typeof a&&(a=this.element.querySelectorAll(a)),a=d(a)):void 0},q.prototype._manageStamps=function(){if(this.stamps&&this.stamps.length){this._getBoundingRect();for(var a=0,b=this.stamps.length;b>a;a++){var c=this.stamps[a];this._manageStamp(c)}}},q.prototype._getBoundingRect=function(){var a=this.element.getBoundingClientRect(),b=this.size;this._boundingRect={left:a.left+b.paddingLeft+b.borderLeftWidth,top:a.top+b.paddingTop+b.borderTopWidth,right:a.right-(b.paddingRight+b.borderRightWidth),bottom:a.bottom-(b.paddingBottom+b.borderBottomWidth)}},q.prototype._manageStamp=k,q.prototype._getElementOffset=function(a){var b=a.getBoundingClientRect(),c=this._boundingRect,d=n(a),e={left:b.left-c.left-d.marginLeft,top:b.top-c.top-d.marginTop,right:c.right-b.right-d.marginRight,bottom:c.bottom-b.bottom-d.marginBottom};return e},q.prototype.handleEvent=function(a){var b="on"+a.type;this[b]&&this[b](a)},q.prototype.bindResize=function(){this.isResizeBound||(c.bind(a,"resize",this),this.isResizeBound=!0)},q.prototype.unbindResize=function(){this.isResizeBound&&c.unbind(a,"resize",this),this.isResizeBound=!1},q.prototype.onresize=function(){function a(){b.resize(),delete b.resizeTimeout}this.resizeTimeout&&clearTimeout(this.resizeTimeout);var b=this;this.resizeTimeout=setTimeout(a,100)},q.prototype.resize=function(){this.isResizeBound&&this.needsResizeLayout()&&this.layout()},q.prototype.needsResizeLayout=function(){var a=n(this.element),b=this.size&&a;return b&&a.innerWidth!==this.size.innerWidth},q.prototype.addItems=function(a){var b=this._itemize(a);return b.length&&(this.items=this.items.concat(b)),b},q.prototype.appended=function(a){var b=this.addItems(a);b.length&&(this.layoutItems(b,!0),this.reveal(b))},q.prototype.prepended=function(a){var b=this._itemize(a);if(b.length){var c=this.items.slice(0);this.items=b.concat(c),this._resetLayout(),this._manageStamps(),this.layoutItems(b,!0),this.reveal(b),this.layoutItems(c)}},q.prototype.reveal=function(a){var b=a&&a.length;if(b)for(var c=0;b>c;c++){var d=a[c];d.reveal()}},q.prototype.hide=function(a){var b=a&&a.length;if(b)for(var c=0;b>c;c++){var d=a[c];d.hide()}},q.prototype.getItem=function(a){for(var b=0,c=this.items.length;c>b;b++){var d=this.items[b];if(d.element===a)return d}},q.prototype.getItems=function(a){if(a&&a.length){for(var b=[],c=0,d=a.length;d>c;c++){var e=a[c],f=this.getItem(e);f&&b.push(f)}return b}},q.prototype.remove=function(a){a=d(a);var b=this.getItems(a);if(b&&b.length){this._itemsOn(b,"remove",function(){this.emitEvent("removeComplete",[this,b])});for(var c=0,f=b.length;f>c;c++){var g=b[c];g.remove(),e(g,this.items)}}},q.prototype.destroy=function(){var a=this.element.style;a.height="",a.position="",a.width="";for(var b=0,c=this.items.length;c>b;b++){var d=this.items[b];d.destroy()}this.unbindResize();var e=this.element.outlayerGUID;delete s[e],delete this.element.outlayerGUID,j&&j.removeData(this.element,this.constructor.namespace)},q.data=function(a){var b=a&&a.outlayerGUID;return b&&s[b]},q.create=function(a,c){function d(){q.apply(this,arguments)}return Object.create?d.prototype=Object.create(q.prototype):b(d.prototype,q.prototype),d.prototype.constructor=d,d.defaults=b({},q.defaults),b(d.defaults,c),d.prototype.settings={},d.namespace=a,d.data=q.data,d.Item=function(){p.apply(this,arguments)},d.Item.prototype=new p,g(function(){for(var b=f(a),c=h.querySelectorAll(".js-"+b),e="data-"+b+"-options",g=0,k=c.length;k>g;g++){var l,m=c[g],n=m.getAttribute(e);try{l=n&&JSON.parse(n)}catch(o){i&&i.error("Error parsing "+e+" on "+m.nodeName.toLowerCase()+(m.id?"#"+m.id:"")+": "+o);continue}var p=new d(m,l);j&&j.data(m,a,p)}}),j&&j.bridget&&j.bridget(a,d),d},q.Item=p,q}var h=a.document,i=a.console,j=a.jQuery,k=function(){},l=Object.prototype.toString,m="function"==typeof HTMLElement||"object"==typeof HTMLElement?function(a){return a instanceof HTMLElement}:function(a){return a&&"object"==typeof a&&1===a.nodeType&&"string"==typeof a.nodeName},n=Array.prototype.indexOf?function(a,b){return a.indexOf(b)}:function(a,b){for(var c=0,d=a.length;d>c;c++)if(a[c]===b)return c;return-1};"function"==typeof define&&define.amd?define("outlayer/outlayer",["eventie/eventie","doc-ready/doc-ready","eventEmitter/EventEmitter","get-size/get-size","matches-selector/matches-selector","./item"],g):"object"==typeof exports?module.exports=g(require("eventie"),require("doc-ready"),require("wolfy87-eventemitter"),require("get-size"),require("desandro-matches-selector"),require("./item")):a.Outlayer=g(a.eventie,a.docReady,a.EventEmitter,a.getSize,a.matchesSelector,a.Outlayer.Item)}(window),function(a){function b(a,b){var d=a.create("masonry");return d.prototype._resetLayout=function(){this.getSize(),this._getMeasurement("columnWidth","outerWidth"),this._getMeasurement("gutter","outerWidth"),this.measureColumns();var a=this.cols;for(this.colYs=[];a--;)this.colYs.push(0);this.maxY=0},d.prototype.measureColumns=function(){if(this.getContainerWidth(),!this.columnWidth){var a=this.items[0],c=a&&a.element;this.columnWidth=c&&b(c).outerWidth||this.containerWidth}this.columnWidth+=this.gutter,this.cols=Math.floor((this.containerWidth+this.gutter)/this.columnWidth),this.cols=Math.max(this.cols,1)},d.prototype.getContainerWidth=function(){var a=this.options.isFitWidth?this.element.parentNode:this.element,c=b(a);this.containerWidth=c&&c.innerWidth},d.prototype._getItemLayoutPosition=function(a){a.getSize();var b=a.size.outerWidth%this.columnWidth,d=b&&1>b?"round":"ceil",e=Math[d](a.size.outerWidth/this.columnWidth);e=Math.min(e,this.cols);for(var f=this._getColGroup(e),g=Math.min.apply(Math,f),h=c(f,g),i={x:this.columnWidth*h,y:g},j=g+a.size.outerHeight,k=this.cols+1-f.length,l=0;k>l;l++)this.colYs[h+l]=j;return i},d.prototype._getColGroup=function(a){if(2>a)return this.colYs;for(var b=[],c=this.cols+1-a,d=0;c>d;d++){var e=this.colYs.slice(d,d+a);b[d]=Math.max.apply(Math,e)}return b},d.prototype._manageStamp=function(a){var c=b(a),d=this._getElementOffset(a),e=this.options.isOriginLeft?d.left:d.right,f=e+c.outerWidth,g=Math.floor(e/this.columnWidth);g=Math.max(0,g);var h=Math.floor(f/this.columnWidth);h-=f%this.columnWidth?0:1,h=Math.min(this.cols-1,h);for(var i=(this.options.isOriginTop?d.top:d.bottom)+c.outerHeight,j=g;h>=j;j++)this.colYs[j]=Math.max(i,this.colYs[j])},d.prototype._getContainerSize=function(){this.maxY=Math.max.apply(Math,this.colYs);var a={height:this.maxY};return this.options.isFitWidth&&(a.width=this._getContainerFitWidth()),a},d.prototype._getContainerFitWidth=function(){for(var a=0,b=this.cols;--b&&0===this.colYs[b];)a++;return(this.cols-a)*this.columnWidth-this.gutter},d.prototype.needsResizeLayout=function(){var a=this.containerWidth;return this.getContainerWidth(),a!==this.containerWidth},d}var c=Array.prototype.indexOf?function(a,b){return a.indexOf(b)}:function(a,b){for(var c=0,d=a.length;d>c;c++){var e=a[c];if(e===b)return c}return-1};"function"==typeof define&&define.amd?define(["outlayer/outlayer","get-size/get-size"],b):"object"==typeof exports?module.exports=b(require("outlayer"),require("get-size")):a.Masonry=b(a.Outlayer,a.getSize)}(window);
(function( window ) {
    var Tap = {};

    var utils = {};

    utils.attachEvent = function( element, eventName, callback ) {
        if ( 'addEventListener' in window ) {
            return element.addEventListener( eventName, callback, false );
        }
    };

    utils.fireFakeEvent = function( e, eventName ) {
        if ( document.createEvent ) {
            return e.target.dispatchEvent( utils.createEvent( eventName ) );
        }
    };

    utils.createEvent = function( name ) {
        if ( document.createEvent ) {
            var evnt = window.document.createEvent( 'HTMLEvents' );
            evnt.initEvent( name, true, true );
            evnt.eventName = name;

            return evnt;
        }
    };

    utils.getRealEvent = function( e ) {
        if ( e.originalEvent && e.originalEvent.touches && e.originalEvent.touches.length ) {
            return e.originalEvent.touches[ 0 ];
        } else if ( e.touches && e.touches.length ) {
            return e.touches[ 0 ];
        }

        return e;
    };

    var eventMatrix = [{
        // Touchable devices
        test: ( 'propertyIsEnumerable' in window || 'hasOwnProperty' in document ) && ( window.propertyIsEnumerable( 'ontouchstart' ) || document.hasOwnProperty( 'ontouchstart') ),
        events: {
            start: 'touchstart',
            move: 'touchmove',
            end: 'touchend'
        }
    }, {
        // IE10
        test: window.navigator.msPointerEnabled,
        events: {
            start: 'MSPointerDown',
            move: 'MSPointerMove',
            end: 'MSPointerUp'
        }
    }, {
        // Modern device agnostic web
        test: window.navigator.pointerEnabled,
        events: {
            start: 'pointerdown',
            move: 'pointermove',
            end: 'pointerup'
        }
    }];

    Tap.options = {
        eventName: 'tap',
        fingerMaxOffset: 11
    };

    var attachDeviceEvent, init, handlers, deviceEvents,
        coords = {};

    attachDeviceEvent = function( eventName ) {
        return utils.attachEvent( document.documentElement, deviceEvents[ eventName ], handlers[ eventName ] );
    };

    handlers = {
        start: function( e ) {
            e = utils.getRealEvent( e );

            coords.start = [ e.pageX, e.pageY ];
            coords.offset = [ 0, 0 ];
        },

        move: function( e ) {
            if ( !coords[ 'start' ] && !coords[ 'move' ] ) {
                return false;
            }

            e = utils.getRealEvent( e );

            coords.move = [ e.pageX, e.pageY ];
            coords.offset = [
                Math.abs( coords.move[ 0 ] - coords.start[ 0 ] ),
                Math.abs( coords.move[ 1 ] - coords.start[ 1 ] )
            ];
        },

        end: function( e ) {
            e = utils.getRealEvent( e );

            if ( coords.offset[ 0 ] < Tap.options.fingerMaxOffset && coords.offset[ 1 ] < Tap.options.fingerMaxOffset && !utils.fireFakeEvent( e, Tap.options.eventName ) ) {
                // Windows Phone 8.0 trigger `click` after `pointerup` firing
                // #16 https://github.com/pukhalski/tap/issues/16
                if ( window.navigator.msPointerEnabled || window.navigator.pointerEnabled ) {
                    var preventDefault = function( clickEvent ) {
                        clickEvent.preventDefault();
                        e.target.removeEventListener( 'click', preventDefault );
                    };

                    e.target.addEventListener( 'click', preventDefault, false );
                }

                e.preventDefault();
            }

            coords = {};
        },

        click: function( e ) {
            if ( !utils.fireFakeEvent( e, Tap.options.eventName ) ) {
                return e.preventDefault();
            }
        }
    };

    init = function() {
        var i = 0;

        for ( ; i < eventMatrix.length; i++ ) {
            if ( eventMatrix[ i ].test ) {
                deviceEvents = eventMatrix[ i ].events;

                attachDeviceEvent( 'start' );
                attachDeviceEvent( 'move' );
                attachDeviceEvent( 'end' );

                return false;
            }
        }

        return utils.attachEvent( document.documentElement, 'click', handlers[ 'click' ] );
    };

    utils.attachEvent( window, 'load', init );

    if (typeof define === 'function' && define.amd) {
        define(function () {
            init();
            return Tap;
        });
    } else {
        window.Tap = Tap;
    }

})( window );
/**
 * Owl Carousel v2.1.6
 * Copyright 2013-2016 David Deutsch
 * Licensed under MIT (https://github.com/OwlCarousel2/OwlCarousel2/blob/master/LICENSE)
 */
/**
 * Owl carousel
 * @version 2.1.6
 * @author Bartosz Wojciechowski
 * @author David Deutsch
 * @license The MIT License (MIT)
 * @todo Lazy Load Icon
 * @todo prevent animationend bubling
 * @todo itemsScaleUp
 * @todo Test Zepto
 * @todo stagePadding calculate wrong active classes
 */

;(function($, window, document, undefined) {

	/**
	 * Creates a carousel.
	 * @class The Owl Carousel.
	 * @public
	 * @param {HTMLElement|jQuery} element - The element to create the carousel for.
	 * @param {Object} [options] - The options
	 */
	function Owl(element, options) {

		/**
		 * Current settings for the carousel.
		 * @public
		 */
		this.settings = null;

		/**
		 * Current options set by the caller including defaults.
		 * @public
		 */
		this.options = $.extend({}, Owl.Defaults, options);

		/**
		 * Plugin element.
		 * @public
		 */
		this.$element = $(element);

		/**
		 * Proxied event handlers.
		 * @protected
		 */
		this._handlers = {};

		/**
		 * References to the running plugins of this carousel.
		 * @protected
		 */
		this._plugins = {};

		/**
		 * Currently suppressed events to prevent them from beeing retriggered.
		 * @protected
		 */
		this._supress = {};

		/**
		 * Absolute current position.
		 * @protected
		 */
		this._current = null;

		/**
		 * Animation speed in milliseconds.
		 * @protected
		 */
		this._speed = null;

		/**
		 * Coordinates of all items in pixel.
		 * @todo The name of this member is missleading.
		 * @protected
		 */
		this._coordinates = [];

		/**
		 * Current breakpoint.
		 * @todo Real media queries would be nice.
		 * @protected
		 */
		this._breakpoint = null;

		/**
		 * Current width of the plugin element.
		 */
		this._width = null;

		/**
		 * All real items.
		 * @protected
		 */
		this._items = [];

		/**
		 * All cloned items.
		 * @protected
		 */
		this._clones = [];

		/**
		 * Merge values of all items.
		 * @todo Maybe this could be part of a plugin.
		 * @protected
		 */
		this._mergers = [];

		/**
		 * Widths of all items.
		 */
		this._widths = [];

		/**
		 * Invalidated parts within the update process.
		 * @protected
		 */
		this._invalidated = {};

		/**
		 * Ordered list of workers for the update process.
		 * @protected
		 */
		this._pipe = [];

		/**
		 * Current state information for the drag operation.
		 * @todo #261
		 * @protected
		 */
		this._drag = {
			time: null,
			target: null,
			pointer: null,
			stage: {
				start: null,
				current: null
			},
			direction: null
		};

		/**
		 * Current state information and their tags.
		 * @type {Object}
		 * @protected
		 */
		this._states = {
			current: {},
			tags: {
				'initializing': [ 'busy' ],
				'animating': [ 'busy' ],
				'dragging': [ 'interacting' ]
			}
		};

		$.each([ 'onResize', 'onThrottledResize' ], $.proxy(function(i, handler) {
			this._handlers[handler] = $.proxy(this[handler], this);
		}, this));

		$.each(Owl.Plugins, $.proxy(function(key, plugin) {
			this._plugins[key.charAt(0).toLowerCase() + key.slice(1)]
				= new plugin(this);
		}, this));

		$.each(Owl.Workers, $.proxy(function(priority, worker) {
			this._pipe.push({
				'filter': worker.filter,
				'run': $.proxy(worker.run, this)
			});
		}, this));

		this.setup();
		this.initialize();
	}

	/**
	 * Default options for the carousel.
	 * @public
	 */
	Owl.Defaults = {
		items: 3,
		loop: false,
		center: false,
		rewind: false,

		mouseDrag: true,
		touchDrag: true,
		pullDrag: true,
		freeDrag: false,

		margin: 0,
		stagePadding: 0,

		merge: false,
		mergeFit: true,
		autoWidth: false,

		startPosition: 0,
		rtl: false,

		smartSpeed: 250,
		fluidSpeed: false,
		dragEndSpeed: false,

		responsive: {},
		responsiveRefreshRate: 200,
		responsiveBaseElement: window,

		fallbackEasing: 'swing',

		info: false,

		nestedItemSelector: false,
		itemElement: 'div',
		stageElement: 'div',

		refreshClass: 'owl-refresh',
		loadedClass: 'owl-loaded',
		loadingClass: 'owl-loading',
		rtlClass: 'owl-rtl',
		responsiveClass: 'owl-responsive',
		dragClass: 'owl-drag',
		itemClass: 'owl-item',
		stageClass: 'owl-stage',
		stageOuterClass: 'owl-stage-outer',
		grabClass: 'owl-grab'
	};

	/**
	 * Enumeration for width.
	 * @public
	 * @readonly
	 * @enum {String}
	 */
	Owl.Width = {
		Default: 'default',
		Inner: 'inner',
		Outer: 'outer'
	};

	/**
	 * Enumeration for types.
	 * @public
	 * @readonly
	 * @enum {String}
	 */
	Owl.Type = {
		Event: 'event',
		State: 'state'
	};

	/**
	 * Contains all registered plugins.
	 * @public
	 */
	Owl.Plugins = {};

	/**
	 * List of workers involved in the update process.
	 */
	Owl.Workers = [ {
		filter: [ 'width', 'settings' ],
		run: function() {
			this._width = this.$element.width();
		}
	}, {
		filter: [ 'width', 'items', 'settings' ],
		run: function(cache) {
			cache.current = this._items && this._items[this.relative(this._current)];
		}
	}, {
		filter: [ 'items', 'settings' ],
		run: function() {
			this.$stage.children('.cloned').remove();
		}
	}, {
		filter: [ 'width', 'items', 'settings' ],
		run: function(cache) {
			var margin = this.settings.margin || '',
				grid = !this.settings.autoWidth,
				rtl = this.settings.rtl,
				css = {
					'width': 'auto',
					'margin-left': rtl ? margin : '',
					'margin-right': rtl ? '' : margin
				};

			!grid && this.$stage.children().css(css);

			cache.css = css;
		}
	}, {
		filter: [ 'width', 'items', 'settings' ],
		run: function(cache) {
			var width = (this.width() / this.settings.items).toFixed(3) - this.settings.margin,
				merge = null,
				iterator = this._items.length,
				grid = !this.settings.autoWidth,
				widths = [];

			cache.items = {
				merge: false,
				width: width
			};

			while (iterator--) {
				merge = this._mergers[iterator];
				merge = this.settings.mergeFit && Math.min(merge, this.settings.items) || merge;

				cache.items.merge = merge > 1 || cache.items.merge;

				widths[iterator] = !grid ? this._items[iterator].width() : width * merge;
			}

			this._widths = widths;
		}
	}, {
		filter: [ 'items', 'settings' ],
		run: function() {
			var clones = [],
				items = this._items,
				settings = this.settings,
				view = Math.max(settings.items * 2, 4),
				size = Math.ceil(items.length / 2) * 2,
				repeat = settings.loop && items.length ? settings.rewind ? view : Math.max(view, size) : 0,
				append = '',
				prepend = '';

			repeat /= 2;

			while (repeat--) {
				clones.push(this.normalize(clones.length / 2, true));
				append = append + items[clones[clones.length - 1]][0].outerHTML;
				clones.push(this.normalize(items.length - 1 - (clones.length - 1) / 2, true));
				prepend = items[clones[clones.length - 1]][0].outerHTML + prepend;
			}

			this._clones = clones;

			$(append).addClass('cloned').appendTo(this.$stage);
			$(prepend).addClass('cloned').prependTo(this.$stage);
		}
	}, {
		filter: [ 'width', 'items', 'settings' ],
		run: function() {
			var rtl = this.settings.rtl ? 1 : -1,
				size = this._clones.length + this._items.length,
				iterator = -1,
				previous = 0,
				current = 0,
				coordinates = [];

			while (++iterator < size) {
				previous = coordinates[iterator - 1] || 0;
				current = this._widths[this.relative(iterator)] + this.settings.margin;
				coordinates.push(previous + current * rtl);
			}

			this._coordinates = coordinates;
		}
	}, {
		filter: [ 'width', 'items', 'settings' ],
		run: function() {
			var padding = this.settings.stagePadding,
				coordinates = this._coordinates,
				css = {
					'width': Math.ceil(Math.abs(coordinates[coordinates.length - 1])) + padding * 2,
					'padding-left': padding || '',
					'padding-right': padding || ''
				};

			this.$stage.css(css);
		}
	}, {
		filter: [ 'width', 'items', 'settings' ],
		run: function(cache) {
			var iterator = this._coordinates.length,
				grid = !this.settings.autoWidth,
				items = this.$stage.children();

			if (grid && cache.items.merge) {
				while (iterator--) {
					cache.css.width = this._widths[this.relative(iterator)];
					items.eq(iterator).css(cache.css);
				}
			} else if (grid) {
				cache.css.width = cache.items.width;
				items.css(cache.css);
			}
		}
	}, {
		filter: [ 'items' ],
		run: function() {
			this._coordinates.length < 1 && this.$stage.removeAttr('style');
		}
	}, {
		filter: [ 'width', 'items', 'settings' ],
		run: function(cache) {
			cache.current = cache.current ? this.$stage.children().index(cache.current) : 0;
			cache.current = Math.max(this.minimum(), Math.min(this.maximum(), cache.current));
			this.reset(cache.current);
		}
	}, {
		filter: [ 'position' ],
		run: function() {
			this.animate(this.coordinates(this._current));
		}
	}, {
		filter: [ 'width', 'position', 'items', 'settings' ],
		run: function() {
			var rtl = this.settings.rtl ? 1 : -1,
				padding = this.settings.stagePadding * 2,
				begin = this.coordinates(this.current()) + padding,
				end = begin + this.width() * rtl,
				inner, outer, matches = [], i, n;

			for (i = 0, n = this._coordinates.length; i < n; i++) {
				inner = this._coordinates[i - 1] || 0;
				outer = Math.abs(this._coordinates[i]) + padding * rtl;

				if ((this.op(inner, '<=', begin) && (this.op(inner, '>', end)))
					|| (this.op(outer, '<', begin) && this.op(outer, '>', end))) {
					matches.push(i);
				}
			}

			this.$stage.children('.active').removeClass('active');
			this.$stage.children(':eq(' + matches.join('), :eq(') + ')').addClass('active');

			if (this.settings.center) {
				this.$stage.children('.center').removeClass('center');
				this.$stage.children().eq(this.current()).addClass('center');
			}
		}
	} ];

	/**
	 * Initializes the carousel.
	 * @protected
	 */
	Owl.prototype.initialize = function() {
		this.enter('initializing');
		this.trigger('initialize');

		this.$element.toggleClass(this.settings.rtlClass, this.settings.rtl);

		if (this.settings.autoWidth && !this.is('pre-loading')) {
			var imgs, nestedSelector, width;
			imgs = this.$element.find('img');
			nestedSelector = this.settings.nestedItemSelector ? '.' + this.settings.nestedItemSelector : undefined;
			width = this.$element.children(nestedSelector).width();

			if (imgs.length && width <= 0) {
				this.preloadAutoWidthImages(imgs);
			}
		}

		this.$element.addClass(this.options.loadingClass);

		// create stage
		this.$stage = $('<' + this.settings.stageElement + ' class="' + this.settings.stageClass + '"/>')
			.wrap('<div class="' + this.settings.stageOuterClass + '"/>');

		// append stage
		this.$element.append(this.$stage.parent());

		// append content
		this.replace(this.$element.children().not(this.$stage.parent()));

		// check visibility
		if (this.$element.is(':visible')) {
			// update view
			this.refresh();
		} else {
			// invalidate width
			this.invalidate('width');
		}

		this.$element
			.removeClass(this.options.loadingClass)
			.addClass(this.options.loadedClass);

		// register event handlers
		this.registerEventHandlers();

		this.leave('initializing');
		this.trigger('initialized');
	};

	/**
	 * Setups the current settings.
	 * @todo Remove responsive classes. Why should adaptive designs be brought into IE8?
	 * @todo Support for media queries by using `matchMedia` would be nice.
	 * @public
	 */
	Owl.prototype.setup = function() {
		var viewport = this.viewport(),
			overwrites = this.options.responsive,
			match = -1,
			settings = null;

		if (!overwrites) {
			settings = $.extend({}, this.options);
		} else {
			$.each(overwrites, function(breakpoint) {
				if (breakpoint <= viewport && breakpoint > match) {
					match = Number(breakpoint);
				}
			});

			settings = $.extend({}, this.options, overwrites[match]);
			if (typeof settings.stagePadding === 'function') {
				settings.stagePadding = settings.stagePadding();
			}
			delete settings.responsive;

			// responsive class
			if (settings.responsiveClass) {
				this.$element.attr('class',
					this.$element.attr('class').replace(new RegExp('(' + this.options.responsiveClass + '-)\\S+\\s', 'g'), '$1' + match)
				);
			}
		}

		this.trigger('change', { property: { name: 'settings', value: settings } });
		this._breakpoint = match;
		this.settings = settings;
		this.invalidate('settings');
		this.trigger('changed', { property: { name: 'settings', value: this.settings } });
	};

	/**
	 * Updates option logic if necessery.
	 * @protected
	 */
	Owl.prototype.optionsLogic = function() {
		if (this.settings.autoWidth) {
			this.settings.stagePadding = false;
			this.settings.merge = false;
		}
	};

	/**
	 * Prepares an item before add.
	 * @todo Rename event parameter `content` to `item`.
	 * @protected
	 * @returns {jQuery|HTMLElement} - The item container.
	 */
	Owl.prototype.prepare = function(item) {
		var event = this.trigger('prepare', { content: item });

		if (!event.data) {
			event.data = $('<' + this.settings.itemElement + '/>')
				.addClass(this.options.itemClass).append(item)
		}

		this.trigger('prepared', { content: event.data });

		return event.data;
	};

	/**
	 * Updates the view.
	 * @public
	 */
	Owl.prototype.update = function() {
		var i = 0,
			n = this._pipe.length,
			filter = $.proxy(function(p) { return this[p] }, this._invalidated),
			cache = {};

		while (i < n) {
			if (this._invalidated.all || $.grep(this._pipe[i].filter, filter).length > 0) {
				this._pipe[i].run(cache);
			}
			i++;
		}

		this._invalidated = {};

		!this.is('valid') && this.enter('valid');
	};

	/**
	 * Gets the width of the view.
	 * @public
	 * @param {Owl.Width} [dimension=Owl.Width.Default] - The dimension to return.
	 * @returns {Number} - The width of the view in pixel.
	 */
	Owl.prototype.width = function(dimension) {
		dimension = dimension || Owl.Width.Default;
		switch (dimension) {
			case Owl.Width.Inner:
			case Owl.Width.Outer:
				return this._width;
			default:
				return this._width - this.settings.stagePadding * 2 + this.settings.margin;
		}
	};

	/**
	 * Refreshes the carousel primarily for adaptive purposes.
	 * @public
	 */
	Owl.prototype.refresh = function() {
		this.enter('refreshing');
		this.trigger('refresh');

		this.setup();

		this.optionsLogic();

		this.$element.addClass(this.options.refreshClass);

		this.update();

		this.$element.removeClass(this.options.refreshClass);

		this.leave('refreshing');
		this.trigger('refreshed');
	};

	/**
	 * Checks window `resize` event.
	 * @protected
	 */
	Owl.prototype.onThrottledResize = function() {
		window.clearTimeout(this.resizeTimer);
		this.resizeTimer = window.setTimeout(this._handlers.onResize, this.settings.responsiveRefreshRate);
	};

	/**
	 * Checks window `resize` event.
	 * @protected
	 */
	Owl.prototype.onResize = function() {
		if (!this._items.length) {
			return false;
		}

		if (this._width === this.$element.width()) {
			return false;
		}

		if (!this.$element.is(':visible')) {
			return false;
		}

		this.enter('resizing');

		if (this.trigger('resize').isDefaultPrevented()) {
			this.leave('resizing');
			return false;
		}

		this.invalidate('width');

		this.refresh();

		this.leave('resizing');
		this.trigger('resized');
	};

	/**
	 * Registers event handlers.
	 * @todo Check `msPointerEnabled`
	 * @todo #261
	 * @protected
	 */
	Owl.prototype.registerEventHandlers = function() {
		if ($.support.transition) {
			this.$stage.on($.support.transition.end + '.owl.core', $.proxy(this.onTransitionEnd, this));
		}

		if (this.settings.responsive !== false) {
			this.on(window, 'resize', this._handlers.onThrottledResize);
		}

		if (this.settings.mouseDrag) {
			this.$element.addClass(this.options.dragClass);
			this.$stage.on('mousedown.owl.core', $.proxy(this.onDragStart, this));
			this.$stage.on('dragstart.owl.core selectstart.owl.core', function() { return false });
		}

		if (this.settings.touchDrag){
			this.$stage.on('touchstart.owl.core', $.proxy(this.onDragStart, this));
			this.$stage.on('touchcancel.owl.core', $.proxy(this.onDragEnd, this));
		}
	};

	/**
	 * Handles `touchstart` and `mousedown` events.
	 * @todo Horizontal swipe threshold as option
	 * @todo #261
	 * @protected
	 * @param {Event} event - The event arguments.
	 */
	Owl.prototype.onDragStart = function(event) {
		var stage = null;

		if (event.which === 3) {
			return;
		}

		if ($.support.transform) {
			stage = this.$stage.css('transform').replace(/.*\(|\)| /g, '').split(',');
			stage = {
				x: stage[stage.length === 16 ? 12 : 4],
				y: stage[stage.length === 16 ? 13 : 5]
			};
		} else {
			stage = this.$stage.position();
			stage = {
				x: this.settings.rtl ?
					stage.left + this.$stage.width() - this.width() + this.settings.margin :
					stage.left,
				y: stage.top
			};
		}

		if (this.is('animating')) {
			$.support.transform ? this.animate(stage.x) : this.$stage.stop()
			this.invalidate('position');
		}

		this.$element.toggleClass(this.options.grabClass, event.type === 'mousedown');

		this.speed(0);

		this._drag.time = new Date().getTime();
		this._drag.target = $(event.target);
		this._drag.stage.start = stage;
		this._drag.stage.current = stage;
		this._drag.pointer = this.pointer(event);

		$(document).on('mouseup.owl.core touchend.owl.core', $.proxy(this.onDragEnd, this));

		$(document).one('mousemove.owl.core touchmove.owl.core', $.proxy(function(event) {
			var delta = this.difference(this._drag.pointer, this.pointer(event));

			$(document).on('mousemove.owl.core touchmove.owl.core', $.proxy(this.onDragMove, this));

			if (Math.abs(delta.x) < Math.abs(delta.y) && this.is('valid')) {
				return;
			}

			event.preventDefault();

			this.enter('dragging');
			this.trigger('drag');
		}, this));
	};

	/**
	 * Handles the `touchmove` and `mousemove` events.
	 * @todo #261
	 * @protected
	 * @param {Event} event - The event arguments.
	 */
	Owl.prototype.onDragMove = function(event) {
		var minimum = null,
			maximum = null,
			pull = null,
			delta = this.difference(this._drag.pointer, this.pointer(event)),
			stage = this.difference(this._drag.stage.start, delta);

		if (!this.is('dragging')) {
			return;
		}

		event.preventDefault();

		if (this.settings.loop) {
			minimum = this.coordinates(this.minimum());
			maximum = this.coordinates(this.maximum() + 1) - minimum;
			stage.x = (((stage.x - minimum) % maximum + maximum) % maximum) + minimum;
		} else {
			minimum = this.settings.rtl ? this.coordinates(this.maximum()) : this.coordinates(this.minimum());
			maximum = this.settings.rtl ? this.coordinates(this.minimum()) : this.coordinates(this.maximum());
			pull = this.settings.pullDrag ? -1 * delta.x / 5 : 0;
			stage.x = Math.max(Math.min(stage.x, minimum + pull), maximum + pull);
		}

		this._drag.stage.current = stage;

		this.animate(stage.x);
	};

	/**
	 * Handles the `touchend` and `mouseup` events.
	 * @todo #261
	 * @todo Threshold for click event
	 * @protected
	 * @param {Event} event - The event arguments.
	 */
	Owl.prototype.onDragEnd = function(event) {
		var delta = this.difference(this._drag.pointer, this.pointer(event)),
			stage = this._drag.stage.current,
			direction = delta.x > 0 ^ this.settings.rtl ? 'left' : 'right';

		$(document).off('.owl.core');

		this.$element.removeClass(this.options.grabClass);

		if (delta.x !== 0 && this.is('dragging') || !this.is('valid')) {
			this.speed(this.settings.dragEndSpeed || this.settings.smartSpeed);
			this.current(this.closest(stage.x, delta.x !== 0 ? direction : this._drag.direction));
			this.invalidate('position');
			this.update();

			this._drag.direction = direction;

			if (Math.abs(delta.x) > 3 || new Date().getTime() - this._drag.time > 300) {
				this._drag.target.one('click.owl.core', function() { return false; });
			}
		}

		if (!this.is('dragging')) {
			return;
		}

		this.leave('dragging');
		this.trigger('dragged');
	};

	/**
	 * Gets absolute position of the closest item for a coordinate.
	 * @todo Setting `freeDrag` makes `closest` not reusable. See #165.
	 * @protected
	 * @param {Number} coordinate - The coordinate in pixel.
	 * @param {String} direction - The direction to check for the closest item. Ether `left` or `right`.
	 * @return {Number} - The absolute position of the closest item.
	 */
	Owl.prototype.closest = function(coordinate, direction) {
		var position = -1,
			pull = 30,
			width = this.width(),
			coordinates = this.coordinates();

		if (!this.settings.freeDrag) {
			// check closest item
			$.each(coordinates, $.proxy(function(index, value) {
				// on a left pull, check on current index
				if (direction === 'left' && coordinate > value - pull && coordinate < value + pull) {
					position = index;
				// on a right pull, check on previous index
				// to do so, subtract width from value and set position = index + 1
				} else if (direction === 'right' && coordinate > value - width - pull && coordinate < value - width + pull) {
					position = index + 1;
				} else if (this.op(coordinate, '<', value)
					&& this.op(coordinate, '>', coordinates[index + 1] || value - width)) {
					position = direction === 'left' ? index + 1 : index;
				}
				return position === -1;
			}, this));
		}

		if (!this.settings.loop) {
			// non loop boundries
			if (this.op(coordinate, '>', coordinates[this.minimum()])) {
				position = coordinate = this.minimum();
			} else if (this.op(coordinate, '<', coordinates[this.maximum()])) {
				position = coordinate = this.maximum();
			}
		}

		return position;
	};

	/**
	 * Animates the stage.
	 * @todo #270
	 * @public
	 * @param {Number} coordinate - The coordinate in pixels.
	 */
	Owl.prototype.animate = function(coordinate) {
		var animate = this.speed() > 0;

		this.is('animating') && this.onTransitionEnd();

		if (animate) {
			this.enter('animating');
			this.trigger('translate');
		}

		if ($.support.transform3d && $.support.transition) {
			this.$stage.css({
				transform: 'translate3d(' + coordinate + 'px,0px,0px)',
				transition: (this.speed() / 1000) + 's'
			});
		} else if (animate) {
			this.$stage.animate({
				left: coordinate + 'px'
			}, this.speed(), this.settings.fallbackEasing, $.proxy(this.onTransitionEnd, this));
		} else {
			this.$stage.css({
				left: coordinate + 'px'
			});
		}
	};

	/**
	 * Checks whether the carousel is in a specific state or not.
	 * @param {String} state - The state to check.
	 * @returns {Boolean} - The flag which indicates if the carousel is busy.
	 */
	Owl.prototype.is = function(state) {
		return this._states.current[state] && this._states.current[state] > 0;
	};

	/**
	 * Sets the absolute position of the current item.
	 * @public
	 * @param {Number} [position] - The new absolute position or nothing to leave it unchanged.
	 * @returns {Number} - The absolute position of the current item.
	 */
	Owl.prototype.current = function(position) {
		if (position === undefined) {
			return this._current;
		}

		if (this._items.length === 0) {
			return undefined;
		}

		position = this.normalize(position);

		if (this._current !== position) {
			var event = this.trigger('change', { property: { name: 'position', value: position } });

			if (event.data !== undefined) {
				position = this.normalize(event.data);
			}

			this._current = position;

			this.invalidate('position');

			this.trigger('changed', { property: { name: 'position', value: this._current } });
		}

		return this._current;
	};

	/**
	 * Invalidates the given part of the update routine.
	 * @param {String} [part] - The part to invalidate.
	 * @returns {Array.<String>} - The invalidated parts.
	 */
	Owl.prototype.invalidate = function(part) {
		if ($.type(part) === 'string') {
			this._invalidated[part] = true;
			this.is('valid') && this.leave('valid');
		}
		return $.map(this._invalidated, function(v, i) { return i });
	};

	/**
	 * Resets the absolute position of the current item.
	 * @public
	 * @param {Number} position - The absolute position of the new item.
	 */
	Owl.prototype.reset = function(position) {
		position = this.normalize(position);

		if (position === undefined) {
			return;
		}

		this._speed = 0;
		this._current = position;

		this.suppress([ 'translate', 'translated' ]);

		this.animate(this.coordinates(position));

		this.release([ 'translate', 'translated' ]);
	};

	/**
	 * Normalizes an absolute or a relative position of an item.
	 * @public
	 * @param {Number} position - The absolute or relative position to normalize.
	 * @param {Boolean} [relative=false] - Whether the given position is relative or not.
	 * @returns {Number} - The normalized position.
	 */
	Owl.prototype.normalize = function(position, relative) {
		var n = this._items.length,
			m = relative ? 0 : this._clones.length;

		if (!this.isNumeric(position) || n < 1) {
			position = undefined;
		} else if (position < 0 || position >= n + m) {
			position = ((position - m / 2) % n + n) % n + m / 2;
		}

		return position;
	};

	/**
	 * Converts an absolute position of an item into a relative one.
	 * @public
	 * @param {Number} position - The absolute position to convert.
	 * @returns {Number} - The converted position.
	 */
	Owl.prototype.relative = function(position) {
		position -= this._clones.length / 2;
		return this.normalize(position, true);
	};

	/**
	 * Gets the maximum position for the current item.
	 * @public
	 * @param {Boolean} [relative=false] - Whether to return an absolute position or a relative position.
	 * @returns {Number}
	 */
	Owl.prototype.maximum = function(relative) {
		var settings = this.settings,
			maximum = this._coordinates.length,
			iterator,
			reciprocalItemsWidth,
			elementWidth;

		if (settings.loop) {
			maximum = this._clones.length / 2 + this._items.length - 1;
		} else if (settings.autoWidth || settings.merge) {
			iterator = this._items.length;
			reciprocalItemsWidth = this._items[--iterator].width();
			elementWidth = this.$element.width();
			while (iterator--) {
				reciprocalItemsWidth += this._items[iterator].width() + this.settings.margin;
				if (reciprocalItemsWidth > elementWidth) {
					break;
				}
			}
			maximum = iterator + 1;
		} else if (settings.center) {
			maximum = this._items.length - 1;
		} else {
			maximum = this._items.length - settings.items;
		}

		if (relative) {
			maximum -= this._clones.length / 2;
		}

		return Math.max(maximum, 0);
	};

	/**
	 * Gets the minimum position for the current item.
	 * @public
	 * @param {Boolean} [relative=false] - Whether to return an absolute position or a relative position.
	 * @returns {Number}
	 */
	Owl.prototype.minimum = function(relative) {
		return relative ? 0 : this._clones.length / 2;
	};

	/**
	 * Gets an item at the specified relative position.
	 * @public
	 * @param {Number} [position] - The relative position of the item.
	 * @return {jQuery|Array.<jQuery>} - The item at the given position or all items if no position was given.
	 */
	Owl.prototype.items = function(position) {
		if (position === undefined) {
			return this._items.slice();
		}

		position = this.normalize(position, true);
		return this._items[position];
	};

	/**
	 * Gets an item at the specified relative position.
	 * @public
	 * @param {Number} [position] - The relative position of the item.
	 * @return {jQuery|Array.<jQuery>} - The item at the given position or all items if no position was given.
	 */
	Owl.prototype.mergers = function(position) {
		if (position === undefined) {
			return this._mergers.slice();
		}

		position = this.normalize(position, true);
		return this._mergers[position];
	};

	/**
	 * Gets the absolute positions of clones for an item.
	 * @public
	 * @param {Number} [position] - The relative position of the item.
	 * @returns {Array.<Number>} - The absolute positions of clones for the item or all if no position was given.
	 */
	Owl.prototype.clones = function(position) {
		var odd = this._clones.length / 2,
			even = odd + this._items.length,
			map = function(index) { return index % 2 === 0 ? even + index / 2 : odd - (index + 1) / 2 };

		if (position === undefined) {
			return $.map(this._clones, function(v, i) { return map(i) });
		}

		return $.map(this._clones, function(v, i) { return v === position ? map(i) : null });
	};

	/**
	 * Sets the current animation speed.
	 * @public
	 * @param {Number} [speed] - The animation speed in milliseconds or nothing to leave it unchanged.
	 * @returns {Number} - The current animation speed in milliseconds.
	 */
	Owl.prototype.speed = function(speed) {
		if (speed !== undefined) {
			this._speed = speed;
		}

		return this._speed;
	};

	/**
	 * Gets the coordinate of an item.
	 * @todo The name of this method is missleanding.
	 * @public
	 * @param {Number} position - The absolute position of the item within `minimum()` and `maximum()`.
	 * @returns {Number|Array.<Number>} - The coordinate of the item in pixel or all coordinates.
	 */
	Owl.prototype.coordinates = function(position) {
		var multiplier = 1,
			newPosition = position - 1,
			coordinate;

		if (position === undefined) {
			return $.map(this._coordinates, $.proxy(function(coordinate, index) {
				return this.coordinates(index);
			}, this));
		}

		if (this.settings.center) {
			if (this.settings.rtl) {
				multiplier = -1;
				newPosition = position + 1;
			}

			coordinate = this._coordinates[position];
			coordinate += (this.width() - coordinate + (this._coordinates[newPosition] || 0)) / 2 * multiplier;
		} else {
			coordinate = this._coordinates[newPosition] || 0;
		}

		coordinate = Math.ceil(coordinate);

		return coordinate;
	};

	/**
	 * Calculates the speed for a translation.
	 * @protected
	 * @param {Number} from - The absolute position of the start item.
	 * @param {Number} to - The absolute position of the target item.
	 * @param {Number} [factor=undefined] - The time factor in milliseconds.
	 * @returns {Number} - The time in milliseconds for the translation.
	 */
	Owl.prototype.duration = function(from, to, factor) {
		if (factor === 0) {
			return 0;
		}

		return Math.min(Math.max(Math.abs(to - from), 1), 6) * Math.abs((factor || this.settings.smartSpeed));
	};

	/**
	 * Slides to the specified item.
	 * @public
	 * @param {Number} position - The position of the item.
	 * @param {Number} [speed] - The time in milliseconds for the transition.
	 */
	Owl.prototype.to = function(position, speed) {
		var current = this.current(),
			revert = null,
			distance = position - this.relative(current),
			direction = (distance > 0) - (distance < 0),
			items = this._items.length,
			minimum = this.minimum(),
			maximum = this.maximum();

		if (this.settings.loop) {
			if (!this.settings.rewind && Math.abs(distance) > items / 2) {
				distance += direction * -1 * items;
			}

			position = current + distance;
			revert = ((position - minimum) % items + items) % items + minimum;

			if (revert !== position && revert - distance <= maximum && revert - distance > 0) {
				current = revert - distance;
				position = revert;
				this.reset(current);
			}
		} else if (this.settings.rewind) {
			maximum += 1;
			position = (position % maximum + maximum) % maximum;
		} else {
			position = Math.max(minimum, Math.min(maximum, position));
		}

		this.speed(this.duration(current, position, speed));
		this.current(position);

		if (this.$element.is(':visible')) {
			this.update();
		}
	};

	/**
	 * Slides to the next item.
	 * @public
	 * @param {Number} [speed] - The time in milliseconds for the transition.
	 */
	Owl.prototype.next = function(speed) {
		speed = speed || false;
		this.to(this.relative(this.current()) + 1, speed);
	};

	/**
	 * Slides to the previous item.
	 * @public
	 * @param {Number} [speed] - The time in milliseconds for the transition.
	 */
	Owl.prototype.prev = function(speed) {
		speed = speed || false;
		this.to(this.relative(this.current()) - 1, speed);
	};

	/**
	 * Handles the end of an animation.
	 * @protected
	 * @param {Event} event - The event arguments.
	 */
	Owl.prototype.onTransitionEnd = function(event) {

		// if css2 animation then event object is undefined
		if (event !== undefined) {
			event.stopPropagation();

			// Catch only owl-stage transitionEnd event
			if ((event.target || event.srcElement || event.originalTarget) !== this.$stage.get(0)) {
				return false;
			}
		}

		this.leave('animating');
		this.trigger('translated');
	};

	/**
	 * Gets viewport width.
	 * @protected
	 * @return {Number} - The width in pixel.
	 */
	Owl.prototype.viewport = function() {
		var width;
		if (this.options.responsiveBaseElement !== window) {
			width = $(this.options.responsiveBaseElement).width();
		} else if (window.innerWidth) {
			width = window.innerWidth;
		} else if (document.documentElement && document.documentElement.clientWidth) {
			width = document.documentElement.clientWidth;
		} else {
			throw 'Can not detect viewport width.';
		}
		return width;
	};

	/**
	 * Replaces the current content.
	 * @public
	 * @param {HTMLElement|jQuery|String} content - The new content.
	 */
	Owl.prototype.replace = function(content) {
		this.$stage.empty();
		this._items = [];

		if (content) {
			content = (content instanceof jQuery) ? content : $(content);
		}

		if (this.settings.nestedItemSelector) {
			content = content.find('.' + this.settings.nestedItemSelector);
		}

		content.filter(function() {
			return this.nodeType === 1;
		}).each($.proxy(function(index, item) {
			item = this.prepare(item);
			this.$stage.append(item);
			this._items.push(item);
			this._mergers.push(item.find('[data-merge]').addBack('[data-merge]').attr('data-merge') * 1 || 1);
		}, this));

		this.reset(this.isNumeric(this.settings.startPosition) ? this.settings.startPosition : 0);

		this.invalidate('items');
	};

	/**
	 * Adds an item.
	 * @todo Use `item` instead of `content` for the event arguments.
	 * @public
	 * @param {HTMLElement|jQuery|String} content - The item content to add.
	 * @param {Number} [position] - The relative position at which to insert the item otherwise the item will be added to the end.
	 */
	Owl.prototype.add = function(content, position) {
		var current = this.relative(this._current);

		position = position === undefined ? this._items.length : this.normalize(position, true);
		content = content instanceof jQuery ? content : $(content);

		this.trigger('add', { content: content, position: position });

		content = this.prepare(content);

		if (this._items.length === 0 || position === this._items.length) {
			this._items.length === 0 && this.$stage.append(content);
			this._items.length !== 0 && this._items[position - 1].after(content);
			this._items.push(content);
			this._mergers.push(content.find('[data-merge]').addBack('[data-merge]').attr('data-merge') * 1 || 1);
		} else {
			this._items[position].before(content);
			this._items.splice(position, 0, content);
			this._mergers.splice(position, 0, content.find('[data-merge]').addBack('[data-merge]').attr('data-merge') * 1 || 1);
		}

		this._items[current] && this.reset(this._items[current].index());

		this.invalidate('items');

		this.trigger('added', { content: content, position: position });
	};

	/**
	 * Removes an item by its position.
	 * @todo Use `item` instead of `content` for the event arguments.
	 * @public
	 * @param {Number} position - The relative position of the item to remove.
	 */
	Owl.prototype.remove = function(position) {
		position = this.normalize(position, true);

		if (position === undefined) {
			return;
		}

		this.trigger('remove', { content: this._items[position], position: position });

		this._items[position].remove();
		this._items.splice(position, 1);
		this._mergers.splice(position, 1);

		this.invalidate('items');

		this.trigger('removed', { content: null, position: position });
	};

	/**
	 * Preloads images with auto width.
	 * @todo Replace by a more generic approach
	 * @protected
	 */
	Owl.prototype.preloadAutoWidthImages = function(images) {
		images.each($.proxy(function(i, element) {
			this.enter('pre-loading');
			element = $(element);
			$(new Image()).one('load', $.proxy(function(e) {
				element.attr('src', e.target.src);
				element.css('opacity', 1);
				this.leave('pre-loading');
				!this.is('pre-loading') && !this.is('initializing') && this.refresh();
			}, this)).attr('src', element.attr('src') || element.attr('data-src') || element.attr('data-src-retina'));
		}, this));
	};

	/**
	 * Destroys the carousel.
	 * @public
	 */
	Owl.prototype.destroy = function() {

		this.$element.off('.owl.core');
		this.$stage.off('.owl.core');
		$(document).off('.owl.core');

		if (this.settings.responsive !== false) {
			window.clearTimeout(this.resizeTimer);
			this.off(window, 'resize', this._handlers.onThrottledResize);
		}

		for (var i in this._plugins) {
			this._plugins[i].destroy();
		}

		this.$stage.children('.cloned').remove();

		this.$stage.unwrap();
		this.$stage.children().contents().unwrap();
		this.$stage.children().unwrap();

		this.$element
			.removeClass(this.options.refreshClass)
			.removeClass(this.options.loadingClass)
			.removeClass(this.options.loadedClass)
			.removeClass(this.options.rtlClass)
			.removeClass(this.options.dragClass)
			.removeClass(this.options.grabClass)
			.attr('class', this.$element.attr('class').replace(new RegExp(this.options.responsiveClass + '-\\S+\\s', 'g'), ''))
			.removeData('owl.carousel');
	};

	/**
	 * Operators to calculate right-to-left and left-to-right.
	 * @protected
	 * @param {Number} [a] - The left side operand.
	 * @param {String} [o] - The operator.
	 * @param {Number} [b] - The right side operand.
	 */
	Owl.prototype.op = function(a, o, b) {
		var rtl = this.settings.rtl;
		switch (o) {
			case '<':
				return rtl ? a > b : a < b;
			case '>':
				return rtl ? a < b : a > b;
			case '>=':
				return rtl ? a <= b : a >= b;
			case '<=':
				return rtl ? a >= b : a <= b;
			default:
				break;
		}
	};

	/**
	 * Attaches to an internal event.
	 * @protected
	 * @param {HTMLElement} element - The event source.
	 * @param {String} event - The event name.
	 * @param {Function} listener - The event handler to attach.
	 * @param {Boolean} capture - Wether the event should be handled at the capturing phase or not.
	 */
	Owl.prototype.on = function(element, event, listener, capture) {
		if (element.addEventListener) {
			element.addEventListener(event, listener, capture);
		} else if (element.attachEvent) {
			element.attachEvent('on' + event, listener);
		}
	};

	/**
	 * Detaches from an internal event.
	 * @protected
	 * @param {HTMLElement} element - The event source.
	 * @param {String} event - The event name.
	 * @param {Function} listener - The attached event handler to detach.
	 * @param {Boolean} capture - Wether the attached event handler was registered as a capturing listener or not.
	 */
	Owl.prototype.off = function(element, event, listener, capture) {
		if (element.removeEventListener) {
			element.removeEventListener(event, listener, capture);
		} else if (element.detachEvent) {
			element.detachEvent('on' + event, listener);
		}
	};

	/**
	 * Triggers a public event.
	 * @todo Remove `status`, `relatedTarget` should be used instead.
	 * @protected
	 * @param {String} name - The event name.
	 * @param {*} [data=null] - The event data.
	 * @param {String} [namespace=carousel] - The event namespace.
	 * @param {String} [state] - The state which is associated with the event.
	 * @param {Boolean} [enter=false] - Indicates if the call enters the specified state or not.
	 * @returns {Event} - The event arguments.
	 */
	Owl.prototype.trigger = function(name, data, namespace, state, enter) {
		var status = {
			item: { count: this._items.length, index: this.current() }
		}, handler = $.camelCase(
			$.grep([ 'on', name, namespace ], function(v) { return v })
				.join('-').toLowerCase()
		), event = $.Event(
			[ name, 'owl', namespace || 'carousel' ].join('.').toLowerCase(),
			$.extend({ relatedTarget: this }, status, data)
		);

		if (!this._supress[name]) {
			$.each(this._plugins, function(name, plugin) {
				if (plugin.onTrigger) {
					plugin.onTrigger(event);
				}
			});

			this.register({ type: Owl.Type.Event, name: name });
			this.$element.trigger(event);

			if (this.settings && typeof this.settings[handler] === 'function') {
				this.settings[handler].call(this, event);
			}
		}

		return event;
	};

	/**
	 * Enters a state.
	 * @param name - The state name.
	 */
	Owl.prototype.enter = function(name) {
		$.each([ name ].concat(this._states.tags[name] || []), $.proxy(function(i, name) {
			if (this._states.current[name] === undefined) {
				this._states.current[name] = 0;
			}

			this._states.current[name]++;
		}, this));
	};

	/**
	 * Leaves a state.
	 * @param name - The state name.
	 */
	Owl.prototype.leave = function(name) {
		$.each([ name ].concat(this._states.tags[name] || []), $.proxy(function(i, name) {
			this._states.current[name]--;
		}, this));
	};

	/**
	 * Registers an event or state.
	 * @public
	 * @param {Object} object - The event or state to register.
	 */
	Owl.prototype.register = function(object) {
		if (object.type === Owl.Type.Event) {
			if (!$.event.special[object.name]) {
				$.event.special[object.name] = {};
			}

			if (!$.event.special[object.name].owl) {
				var _default = $.event.special[object.name]._default;
				$.event.special[object.name]._default = function(e) {
					if (_default && _default.apply && (!e.namespace || e.namespace.indexOf('owl') === -1)) {
						return _default.apply(this, arguments);
					}
					return e.namespace && e.namespace.indexOf('owl') > -1;
				};
				$.event.special[object.name].owl = true;
			}
		} else if (object.type === Owl.Type.State) {
			if (!this._states.tags[object.name]) {
				this._states.tags[object.name] = object.tags;
			} else {
				this._states.tags[object.name] = this._states.tags[object.name].concat(object.tags);
			}

			this._states.tags[object.name] = $.grep(this._states.tags[object.name], $.proxy(function(tag, i) {
				return $.inArray(tag, this._states.tags[object.name]) === i;
			}, this));
		}
	};

	/**
	 * Suppresses events.
	 * @protected
	 * @param {Array.<String>} events - The events to suppress.
	 */
	Owl.prototype.suppress = function(events) {
		$.each(events, $.proxy(function(index, event) {
			this._supress[event] = true;
		}, this));
	};

	/**
	 * Releases suppressed events.
	 * @protected
	 * @param {Array.<String>} events - The events to release.
	 */
	Owl.prototype.release = function(events) {
		$.each(events, $.proxy(function(index, event) {
			delete this._supress[event];
		}, this));
	};

	/**
	 * Gets unified pointer coordinates from event.
	 * @todo #261
	 * @protected
	 * @param {Event} - The `mousedown` or `touchstart` event.
	 * @returns {Object} - Contains `x` and `y` coordinates of current pointer position.
	 */
	Owl.prototype.pointer = function(event) {
		var result = { x: null, y: null };

		event = event.originalEvent || event || window.event;

		event = event.touches && event.touches.length ?
			event.touches[0] : event.changedTouches && event.changedTouches.length ?
				event.changedTouches[0] : event;

		if (event.pageX) {
			result.x = event.pageX;
			result.y = event.pageY;
		} else {
			result.x = event.clientX;
			result.y = event.clientY;
		}

		return result;
	};

	/**
	 * Determines if the input is a Number or something that can be coerced to a Number
	 * @protected
	 * @param {Number|String|Object|Array|Boolean|RegExp|Function|Symbol} - The input to be tested
	 * @returns {Boolean} - An indication if the input is a Number or can be coerced to a Number
	 */
	Owl.prototype.isNumeric = function(number) {
		return !isNaN(parseFloat(number));
	};

	/**
	 * Gets the difference of two vectors.
	 * @todo #261
	 * @protected
	 * @param {Object} - The first vector.
	 * @param {Object} - The second vector.
	 * @returns {Object} - The difference.
	 */
	Owl.prototype.difference = function(first, second) {
		return {
			x: first.x - second.x,
			y: first.y - second.y
		};
	};

	/**
	 * The jQuery Plugin for the Owl Carousel
	 * @todo Navigation plugin `next` and `prev`
	 * @public
	 */
	$.fn.owlCarousel = function(option) {
		var args = Array.prototype.slice.call(arguments, 1);

		return this.each(function() {
			var $this = $(this),
				data = $this.data('owl.carousel');

			if (!data) {
				data = new Owl(this, typeof option == 'object' && option);
				$this.data('owl.carousel', data);

				$.each([
					'next', 'prev', 'to', 'destroy', 'refresh', 'replace', 'add', 'remove'
				], function(i, event) {
					data.register({ type: Owl.Type.Event, name: event });
					data.$element.on(event + '.owl.carousel.core', $.proxy(function(e) {
						if (e.namespace && e.relatedTarget !== this) {
							this.suppress([ event ]);
							data[event].apply(this, [].slice.call(arguments, 1));
							this.release([ event ]);
						}
					}, data));
				});
			}

			if (typeof option == 'string' && option.charAt(0) !== '_') {
				data[option].apply(data, args);
			}
		});
	};

	/**
	 * The constructor for the jQuery Plugin
	 * @public
	 */
	$.fn.owlCarousel.Constructor = Owl;

})(window.Zepto || window.jQuery, window, document);

/**
 * AutoRefresh Plugin
 * @version 2.1.0
 * @author Artus Kolanowski
 * @author David Deutsch
 * @license The MIT License (MIT)
 */
;(function($, window, document, undefined) {

	/**
	 * Creates the auto refresh plugin.
	 * @class The Auto Refresh Plugin
	 * @param {Owl} carousel - The Owl Carousel
	 */
	var AutoRefresh = function(carousel) {
		/**
		 * Reference to the core.
		 * @protected
		 * @type {Owl}
		 */
		this._core = carousel;

		/**
		 * Refresh interval.
		 * @protected
		 * @type {number}
		 */
		this._interval = null;

		/**
		 * Whether the element is currently visible or not.
		 * @protected
		 * @type {Boolean}
		 */
		this._visible = null;

		/**
		 * All event handlers.
		 * @protected
		 * @type {Object}
		 */
		this._handlers = {
			'initialized.owl.carousel': $.proxy(function(e) {
				if (e.namespace && this._core.settings.autoRefresh) {
					this.watch();
				}
			}, this)
		};

		// set default options
		this._core.options = $.extend({}, AutoRefresh.Defaults, this._core.options);

		// register event handlers
		this._core.$element.on(this._handlers);
	};

	/**
	 * Default options.
	 * @public
	 */
	AutoRefresh.Defaults = {
		autoRefresh: true,
		autoRefreshInterval: 500
	};

	/**
	 * Watches the element.
	 */
	AutoRefresh.prototype.watch = function() {
		if (this._interval) {
			return;
		}

		this._visible = this._core.$element.is(':visible');
		this._interval = window.setInterval($.proxy(this.refresh, this), this._core.settings.autoRefreshInterval);
	};

	/**
	 * Refreshes the element.
	 */
	AutoRefresh.prototype.refresh = function() {
		if (this._core.$element.is(':visible') === this._visible) {
			return;
		}

		this._visible = !this._visible;

		this._core.$element.toggleClass('owl-hidden', !this._visible);

		this._visible && (this._core.invalidate('width') && this._core.refresh());
	};

	/**
	 * Destroys the plugin.
	 */
	AutoRefresh.prototype.destroy = function() {
		var handler, property;

		window.clearInterval(this._interval);

		for (handler in this._handlers) {
			this._core.$element.off(handler, this._handlers[handler]);
		}
		for (property in Object.getOwnPropertyNames(this)) {
			typeof this[property] != 'function' && (this[property] = null);
		}
	};

	$.fn.owlCarousel.Constructor.Plugins.AutoRefresh = AutoRefresh;

})(window.Zepto || window.jQuery, window, document);

/**
 * Lazy Plugin
 * @version 2.1.0
 * @author Bartosz Wojciechowski
 * @author David Deutsch
 * @license The MIT License (MIT)
 */
;(function($, window, document, undefined) {

	/**
	 * Creates the lazy plugin.
	 * @class The Lazy Plugin
	 * @param {Owl} carousel - The Owl Carousel
	 */
	var Lazy = function(carousel) {

		/**
		 * Reference to the core.
		 * @protected
		 * @type {Owl}
		 */
		this._core = carousel;

		/**
		 * Already loaded items.
		 * @protected
		 * @type {Array.<jQuery>}
		 */
		this._loaded = [];

		/**
		 * Event handlers.
		 * @protected
		 * @type {Object}
		 */
		this._handlers = {
			'initialized.owl.carousel change.owl.carousel resized.owl.carousel': $.proxy(function(e) {
				if (!e.namespace) {
					return;
				}

				if (!this._core.settings || !this._core.settings.lazyLoad) {
					return;
				}

				if ((e.property && e.property.name == 'position') || e.type == 'initialized') {
					var settings = this._core.settings,
						n = (settings.center && Math.ceil(settings.items / 2) || settings.items),
						i = ((settings.center && n * -1) || 0),
						position = (e.property && e.property.value !== undefined ? e.property.value : this._core.current()) + i,
						clones = this._core.clones().length,
						load = $.proxy(function(i, v) { this.load(v) }, this);

					while (i++ < n) {
						this.load(clones / 2 + this._core.relative(position));
						clones && $.each(this._core.clones(this._core.relative(position)), load);
						position++;
					}
				}
			}, this)
		};

		// set the default options
		this._core.options = $.extend({}, Lazy.Defaults, this._core.options);

		// register event handler
		this._core.$element.on(this._handlers);
	};

	/**
	 * Default options.
	 * @public
	 */
	Lazy.Defaults = {
		lazyLoad: false
	};

	/**
	 * Loads all resources of an item at the specified position.
	 * @param {Number} position - The absolute position of the item.
	 * @protected
	 */
	Lazy.prototype.load = function(position) {
		var $item = this._core.$stage.children().eq(position),
			$elements = $item && $item.find('.owl-lazy');

		if (!$elements || $.inArray($item.get(0), this._loaded) > -1) {
			return;
		}

		$elements.each($.proxy(function(index, element) {
			var $element = $(element), image,
				url = (window.devicePixelRatio > 1 && $element.attr('data-src-retina')) || $element.attr('data-src');

			this._core.trigger('load', { element: $element, url: url }, 'lazy');

			if ($element.is('img')) {
				$element.one('load.owl.lazy', $.proxy(function() {
					$element.css('opacity', 1);
					this._core.trigger('loaded', { element: $element, url: url }, 'lazy');
				}, this)).attr('src', url);
			} else {
				image = new Image();
				image.onload = $.proxy(function() {
					$element.css({
						'background-image': 'url(' + url + ')',
						'opacity': '1'
					});
					this._core.trigger('loaded', { element: $element, url: url }, 'lazy');
				}, this);
				image.src = url;
			}
		}, this));

		this._loaded.push($item.get(0));
	};

	/**
	 * Destroys the plugin.
	 * @public
	 */
	Lazy.prototype.destroy = function() {
		var handler, property;

		for (handler in this.handlers) {
			this._core.$element.off(handler, this.handlers[handler]);
		}
		for (property in Object.getOwnPropertyNames(this)) {
			typeof this[property] != 'function' && (this[property] = null);
		}
	};

	$.fn.owlCarousel.Constructor.Plugins.Lazy = Lazy;

})(window.Zepto || window.jQuery, window, document);

/**
 * AutoHeight Plugin
 * @version 2.1.0
 * @author Bartosz Wojciechowski
 * @author David Deutsch
 * @license The MIT License (MIT)
 */
;(function($, window, document, undefined) {

	/**
	 * Creates the auto height plugin.
	 * @class The Auto Height Plugin
	 * @param {Owl} carousel - The Owl Carousel
	 */
	var AutoHeight = function(carousel) {
		/**
		 * Reference to the core.
		 * @protected
		 * @type {Owl}
		 */
		this._core = carousel;

		/**
		 * All event handlers.
		 * @protected
		 * @type {Object}
		 */
		this._handlers = {
			'initialized.owl.carousel refreshed.owl.carousel': $.proxy(function(e) {
				if (e.namespace && this._core.settings.autoHeight) {
					this.update();
				}
			}, this),
			'changed.owl.carousel': $.proxy(function(e) {
				if (e.namespace && this._core.settings.autoHeight && e.property.name == 'position'){
					this.update();
				}
			}, this),
			'loaded.owl.lazy': $.proxy(function(e) {
				if (e.namespace && this._core.settings.autoHeight
					&& e.element.closest('.' + this._core.settings.itemClass).index() === this._core.current()) {
					this.update();
				}
			}, this)
		};

		// set default options
		this._core.options = $.extend({}, AutoHeight.Defaults, this._core.options);

		// register event handlers
		this._core.$element.on(this._handlers);
	};

	/**
	 * Default options.
	 * @public
	 */
	AutoHeight.Defaults = {
		autoHeight: false,
		autoHeightClass: 'owl-height'
	};

	/**
	 * Updates the view.
	 */
	AutoHeight.prototype.update = function() {
		var start = this._core._current,
			end = start + this._core.settings.items,
			visible = this._core.$stage.children().toArray().slice(start, end),
			heights = [],
			maxheight = 0;

		$.each(visible, function(index, item) {
			heights.push($(item).height());
		});

		maxheight = Math.max.apply(null, heights);

		this._core.$stage.parent()
			.height(maxheight)
			.addClass(this._core.settings.autoHeightClass);
	};

	AutoHeight.prototype.destroy = function() {
		var handler, property;

		for (handler in this._handlers) {
			this._core.$element.off(handler, this._handlers[handler]);
		}
		for (property in Object.getOwnPropertyNames(this)) {
			typeof this[property] != 'function' && (this[property] = null);
		}
	};

	$.fn.owlCarousel.Constructor.Plugins.AutoHeight = AutoHeight;

})(window.Zepto || window.jQuery, window, document);

/**
 * Video Plugin
 * @version 2.1.0
 * @author Bartosz Wojciechowski
 * @author David Deutsch
 * @license The MIT License (MIT)
 */
;(function($, window, document, undefined) {

	/**
	 * Creates the video plugin.
	 * @class The Video Plugin
	 * @param {Owl} carousel - The Owl Carousel
	 */
	var Video = function(carousel) {
		/**
		 * Reference to the core.
		 * @protected
		 * @type {Owl}
		 */
		this._core = carousel;

		/**
		 * Cache all video URLs.
		 * @protected
		 * @type {Object}
		 */
		this._videos = {};

		/**
		 * Current playing item.
		 * @protected
		 * @type {jQuery}
		 */
		this._playing = null;

		/**
		 * All event handlers.
		 * @todo The cloned content removale is too late
		 * @protected
		 * @type {Object}
		 */
		this._handlers = {
			'initialized.owl.carousel': $.proxy(function(e) {
				if (e.namespace) {
					this._core.register({ type: 'state', name: 'playing', tags: [ 'interacting' ] });
				}
			}, this),
			'resize.owl.carousel': $.proxy(function(e) {
				if (e.namespace && this._core.settings.video && this.isInFullScreen()) {
					e.preventDefault();
				}
			}, this),
			'refreshed.owl.carousel': $.proxy(function(e) {
				if (e.namespace && this._core.is('resizing')) {
					this._core.$stage.find('.cloned .owl-video-frame').remove();
				}
			}, this),
			'changed.owl.carousel': $.proxy(function(e) {
				if (e.namespace && e.property.name === 'position' && this._playing) {
					this.stop();
				}
			}, this),
			'prepared.owl.carousel': $.proxy(function(e) {
				if (!e.namespace) {
					return;
				}

				var $element = $(e.content).find('.owl-video');

				if ($element.length) {
					$element.css('display', 'none');
					this.fetch($element, $(e.content));
				}
			}, this)
		};

		// set default options
		this._core.options = $.extend({}, Video.Defaults, this._core.options);

		// register event handlers
		this._core.$element.on(this._handlers);

		this._core.$element.on('click.owl.video', '.owl-video-play-icon', $.proxy(function(e) {
			this.play(e);
		}, this));
	};

	/**
	 * Default options.
	 * @public
	 */
	Video.Defaults = {
		video: false,
		videoHeight: false,
		videoWidth: false
	};

	/**
	 * Gets the video ID and the type (YouTube/Vimeo/vzaar only).
	 * @protected
	 * @param {jQuery} target - The target containing the video data.
	 * @param {jQuery} item - The item containing the video.
	 */
	Video.prototype.fetch = function(target, item) {
			var type = (function() {
					if (target.attr('data-vimeo-id')) {
						return 'vimeo';
					} else if (target.attr('data-vzaar-id')) {
						return 'vzaar'
					} else {
						return 'youtube';
					}
				})(),
				id = target.attr('data-vimeo-id') || target.attr('data-youtube-id') || target.attr('data-vzaar-id'),
				width = target.attr('data-width') || this._core.settings.videoWidth,
				height = target.attr('data-height') || this._core.settings.videoHeight,
				url = target.attr('href');

		if (url) {

			/*
					Parses the id's out of the following urls (and probably more):
					https://www.youtube.com/watch?v=:id
					https://youtu.be/:id
					https://vimeo.com/:id
					https://vimeo.com/channels/:channel/:id
					https://vimeo.com/groups/:group/videos/:id
					https://app.vzaar.com/videos/:id

					Visual example: https://regexper.com/#(http%3A%7Chttps%3A%7C)%5C%2F%5C%2F(player.%7Cwww.%7Capp.)%3F(vimeo%5C.com%7Cyoutu(be%5C.com%7C%5C.be%7Cbe%5C.googleapis%5C.com)%7Cvzaar%5C.com)%5C%2F(video%5C%2F%7Cvideos%5C%2F%7Cembed%5C%2F%7Cchannels%5C%2F.%2B%5C%2F%7Cgroups%5C%2F.%2B%5C%2F%7Cwatch%5C%3Fv%3D%7Cv%5C%2F)%3F(%5BA-Za-z0-9._%25-%5D*)(%5C%26%5CS%2B)%3F
			*/

			id = url.match(/(http:|https:|)\/\/(player.|www.|app.)?(vimeo\.com|youtu(be\.com|\.be|be\.googleapis\.com)|vzaar\.com)\/(video\/|videos\/|embed\/|channels\/.+\/|groups\/.+\/|watch\?v=|v\/)?([A-Za-z0-9._%-]*)(\&\S+)?/);

			if (id[3].indexOf('youtu') > -1) {
				type = 'youtube';
			} else if (id[3].indexOf('vimeo') > -1) {
				type = 'vimeo';
			} else if (id[3].indexOf('vzaar') > -1) {
				type = 'vzaar';
			} else {
				throw new Error('Video URL not supported.');
			}
			id = id[6];
		} else {
			throw new Error('Missing video URL.');
		}

		this._videos[url] = {
			type: type,
			id: id,
			width: width,
			height: height
		};

		item.attr('data-video', url);

		this.thumbnail(target, this._videos[url]);
	};

	/**
	 * Creates video thumbnail.
	 * @protected
	 * @param {jQuery} target - The target containing the video data.
	 * @param {Object} info - The video info object.
	 * @see `fetch`
	 */
	Video.prototype.thumbnail = function(target, video) {
		var tnLink,
			icon,
			path,
			dimensions = video.width && video.height ? 'style="width:' + video.width + 'px;height:' + video.height + 'px;"' : '',
			customTn = target.find('img'),
			srcType = 'src',
			lazyClass = '',
			settings = this._core.settings,
			create = function(path) {
				icon = '<div class="owl-video-play-icon"></div>';

				if (settings.lazyLoad) {
					tnLink = '<div class="owl-video-tn ' + lazyClass + '" ' + srcType + '="' + path + '"></div>';
				} else {
					tnLink = '<div class="owl-video-tn" style="opacity:1;background-image:url(' + path + ')"></div>';
				}
				target.after(tnLink);
				target.after(icon);
			};

		// wrap video content into owl-video-wrapper div
		target.wrap('<div class="owl-video-wrapper"' + dimensions + '></div>');

		if (this._core.settings.lazyLoad) {
			srcType = 'data-src';
			lazyClass = 'owl-lazy';
		}

		// custom thumbnail
		if (customTn.length) {
			create(customTn.attr(srcType));
			customTn.remove();
			return false;
		}

		if (video.type === 'youtube') {
			path = "//img.youtube.com/vi/" + video.id + "/hqdefault.jpg";
			create(path);
		} else if (video.type === 'vimeo') {
			$.ajax({
				type: 'GET',
				url: '//vimeo.com/api/v2/video/' + video.id + '.json',
				jsonp: 'callback',
				dataType: 'jsonp',
				success: function(data) {
					path = data[0].thumbnail_large;
					create(path);
				}
			});
		} else if (video.type === 'vzaar') {
			$.ajax({
				type: 'GET',
				url: '//vzaar.com/api/videos/' + video.id + '.json',
				jsonp: 'callback',
				dataType: 'jsonp',
				success: function(data) {
					path = data.framegrab_url;
					create(path);
				}
			});
		}
	};

	/**
	 * Stops the current video.
	 * @public
	 */
	Video.prototype.stop = function() {
		this._core.trigger('stop', null, 'video');
		this._playing.find('.owl-video-frame').remove();
		this._playing.removeClass('owl-video-playing');
		this._playing = null;
		this._core.leave('playing');
		this._core.trigger('stopped', null, 'video');
	};

	/**
	 * Starts the current video.
	 * @public
	 * @param {Event} event - The event arguments.
	 */
	Video.prototype.play = function(event) {
		var target = $(event.target),
			item = target.closest('.' + this._core.settings.itemClass),
			video = this._videos[item.attr('data-video')],
			width = video.width || '100%',
			height = video.height || this._core.$stage.height(),
			html;

		if (this._playing) {
			return;
		}

		this._core.enter('playing');
		this._core.trigger('play', null, 'video');

		item = this._core.items(this._core.relative(item.index()));

		this._core.reset(item.index());

		if (video.type === 'youtube') {
			html = '<iframe width="' + width + '" height="' + height + '" src="//www.youtube.com/embed/' +
				video.id + '?autoplay=1&v=' + video.id + '" frameborder="0" allowfullscreen></iframe>';
		} else if (video.type === 'vimeo') {
			html = '<iframe src="//player.vimeo.com/video/' + video.id +
				'?autoplay=1" width="' + width + '" height="' + height +
				'" frameborder="0" webkitallowfullscreen mozallowfullscreen allowfullscreen></iframe>';
		} else if (video.type === 'vzaar') {
			html = '<iframe frameborder="0"' + 'height="' + height + '"' + 'width="' + width +
				'" allowfullscreen mozallowfullscreen webkitAllowFullScreen ' +
				'src="//view.vzaar.com/' + video.id + '/player?autoplay=true"></iframe>';
		}

		$('<div class="owl-video-frame">' + html + '</div>').insertAfter(item.find('.owl-video'));

		this._playing = item.addClass('owl-video-playing');
	};

	/**
	 * Checks whether an video is currently in full screen mode or not.
	 * @todo Bad style because looks like a readonly method but changes members.
	 * @protected
	 * @returns {Boolean}
	 */
	Video.prototype.isInFullScreen = function() {
		var element = document.fullscreenElement || document.mozFullScreenElement ||
				document.webkitFullscreenElement;

		return element && $(element).parent().hasClass('owl-video-frame');
	};

	/**
	 * Destroys the plugin.
	 */
	Video.prototype.destroy = function() {
		var handler, property;

		this._core.$element.off('click.owl.video');

		for (handler in this._handlers) {
			this._core.$element.off(handler, this._handlers[handler]);
		}
		for (property in Object.getOwnPropertyNames(this)) {
			typeof this[property] != 'function' && (this[property] = null);
		}
	};

	$.fn.owlCarousel.Constructor.Plugins.Video = Video;

})(window.Zepto || window.jQuery, window, document);

/**
 * Animate Plugin
 * @version 2.1.0
 * @author Bartosz Wojciechowski
 * @author David Deutsch
 * @license The MIT License (MIT)
 */
;(function($, window, document, undefined) {

	/**
	 * Creates the animate plugin.
	 * @class The Navigation Plugin
	 * @param {Owl} scope - The Owl Carousel
	 */
	var Animate = function(scope) {
		this.core = scope;
		this.core.options = $.extend({}, Animate.Defaults, this.core.options);
		this.swapping = true;
		this.previous = undefined;
		this.next = undefined;

		this.handlers = {
			'change.owl.carousel': $.proxy(function(e) {
				if (e.namespace && e.property.name == 'position') {
					this.previous = this.core.current();
					this.next = e.property.value;
				}
			}, this),
			'drag.owl.carousel dragged.owl.carousel translated.owl.carousel': $.proxy(function(e) {
				if (e.namespace) {
					this.swapping = e.type == 'translated';
				}
			}, this),
			'translate.owl.carousel': $.proxy(function(e) {
				if (e.namespace && this.swapping && (this.core.options.animateOut || this.core.options.animateIn)) {
					this.swap();
				}
			}, this)
		};

		this.core.$element.on(this.handlers);
	};

	/**
	 * Default options.
	 * @public
	 */
	Animate.Defaults = {
		animateOut: false,
		animateIn: false
	};

	/**
	 * Toggles the animation classes whenever an translations starts.
	 * @protected
	 * @returns {Boolean|undefined}
	 */
	Animate.prototype.swap = function() {

		if (this.core.settings.items !== 1) {
			return;
		}

		if (!$.support.animation || !$.support.transition) {
			return;
		}

		this.core.speed(0);

		var left,
			clear = $.proxy(this.clear, this),
			previous = this.core.$stage.children().eq(this.previous),
			next = this.core.$stage.children().eq(this.next),
			incoming = this.core.settings.animateIn,
			outgoing = this.core.settings.animateOut;

		if (this.core.current() === this.previous) {
			return;
		}

		if (outgoing) {
			left = this.core.coordinates(this.previous) - this.core.coordinates(this.next);
			previous.one($.support.animation.end, clear)
				.css( { 'left': left + 'px' } )
				.addClass('animated owl-animated-out')
				.addClass(outgoing);
		}

		if (incoming) {
			next.one($.support.animation.end, clear)
				.addClass('animated owl-animated-in')
				.addClass(incoming);
		}
	};

	Animate.prototype.clear = function(e) {
		$(e.target).css( { 'left': '' } )
			.removeClass('animated owl-animated-out owl-animated-in')
			.removeClass(this.core.settings.animateIn)
			.removeClass(this.core.settings.animateOut);
		this.core.onTransitionEnd();
	};

	/**
	 * Destroys the plugin.
	 * @public
	 */
	Animate.prototype.destroy = function() {
		var handler, property;

		for (handler in this.handlers) {
			this.core.$element.off(handler, this.handlers[handler]);
		}
		for (property in Object.getOwnPropertyNames(this)) {
			typeof this[property] != 'function' && (this[property] = null);
		}
	};

	$.fn.owlCarousel.Constructor.Plugins.Animate = Animate;

})(window.Zepto || window.jQuery, window, document);

/**
 * Autoplay Plugin
 * @version 2.1.0
 * @author Bartosz Wojciechowski
 * @author Artus Kolanowski
 * @author David Deutsch
 * @license The MIT License (MIT)
 */
;(function($, window, document, undefined) {

	/**
	 * Creates the autoplay plugin.
	 * @class The Autoplay Plugin
	 * @param {Owl} scope - The Owl Carousel
	 */
	var Autoplay = function(carousel) {
		/**
		 * Reference to the core.
		 * @protected
		 * @type {Owl}
		 */
		this._core = carousel;

		/**
		 * The autoplay timeout.
		 * @type {Timeout}
		 */
		this._timeout = null;

		/**
		 * Indicates whenever the autoplay is paused.
		 * @type {Boolean}
		 */
		this._paused = false;

		/**
		 * All event handlers.
		 * @protected
		 * @type {Object}
		 */
		this._handlers = {
			'changed.owl.carousel': $.proxy(function(e) {
				if (e.namespace && e.property.name === 'settings') {
					if (this._core.settings.autoplay) {
						this.play();
					} else {
						this.stop();
					}
				} else if (e.namespace && e.property.name === 'position') {
					//console.log('play?', e);
					if (this._core.settings.autoplay) {
						this._setAutoPlayInterval();
					}
				}
			}, this),
			'initialized.owl.carousel': $.proxy(function(e) {
				if (e.namespace && this._core.settings.autoplay) {
					this.play();
				}
			}, this),
			'play.owl.autoplay': $.proxy(function(e, t, s) {
				if (e.namespace) {
					this.play(t, s);
				}
			}, this),
			'stop.owl.autoplay': $.proxy(function(e) {
				if (e.namespace) {
					this.stop();
				}
			}, this),
			'mouseover.owl.autoplay': $.proxy(function() {
				if (this._core.settings.autoplayHoverPause && this._core.is('rotating')) {
					this.pause();
				}
			}, this),
			'mouseleave.owl.autoplay': $.proxy(function() {
				if (this._core.settings.autoplayHoverPause && this._core.is('rotating')) {
					this.play();
				}
			}, this),
			'touchstart.owl.core': $.proxy(function() {
				if (this._core.settings.autoplayHoverPause && this._core.is('rotating')) {
					this.pause();
				}
			}, this),
			'touchend.owl.core': $.proxy(function() {
				if (this._core.settings.autoplayHoverPause) {
					this.play();
				}
			}, this)
		};

		// register event handlers
		this._core.$element.on(this._handlers);

		// set default options
		this._core.options = $.extend({}, Autoplay.Defaults, this._core.options);
	};

	/**
	 * Default options.
	 * @public
	 */
	Autoplay.Defaults = {
		autoplay: false,
		autoplayTimeout: 5000,
		autoplayHoverPause: false,
		autoplaySpeed: false
	};

	/**
	 * Starts the autoplay.
	 * @public
	 * @param {Number} [timeout] - The interval before the next animation starts.
	 * @param {Number} [speed] - The animation speed for the animations.
	 */
	Autoplay.prototype.play = function(timeout, speed) {
		this._paused = false;

		if (this._core.is('rotating')) {
			return;
		}

		this._core.enter('rotating');

		this._setAutoPlayInterval();
	};

	/**
	 * Gets a new timeout
	 * @private
	 * @param {Number} [timeout] - The interval before the next animation starts.
	 * @param {Number} [speed] - The animation speed for the animations.
	 * @return {Timeout}
	 */
	Autoplay.prototype._getNextTimeout = function(timeout, speed) {
		if ( this._timeout ) {
			window.clearTimeout(this._timeout);
		}
		return window.setTimeout($.proxy(function() {
			if (this._paused || this._core.is('busy') || this._core.is('interacting') || document.hidden) {
				return;
			}
			this._core.next(speed || this._core.settings.autoplaySpeed);
		}, this), timeout || this._core.settings.autoplayTimeout);
	};

	/**
	 * Sets autoplay in motion.
	 * @private
	 */
	Autoplay.prototype._setAutoPlayInterval = function() {
		this._timeout = this._getNextTimeout();
	};

	/**
	 * Stops the autoplay.
	 * @public
	 */
	Autoplay.prototype.stop = function() {
		if (!this._core.is('rotating')) {
			return;
		}

		window.clearTimeout(this._timeout);
		this._core.leave('rotating');
	};

	/**
	 * Stops the autoplay.
	 * @public
	 */
	Autoplay.prototype.pause = function() {
		if (!this._core.is('rotating')) {
			return;
		}

		this._paused = true;
	};

	/**
	 * Destroys the plugin.
	 */
	Autoplay.prototype.destroy = function() {
		var handler, property;

		this.stop();

		for (handler in this._handlers) {
			this._core.$element.off(handler, this._handlers[handler]);
		}
		for (property in Object.getOwnPropertyNames(this)) {
			typeof this[property] != 'function' && (this[property] = null);
		}
	};

	$.fn.owlCarousel.Constructor.Plugins.autoplay = Autoplay;

})(window.Zepto || window.jQuery, window, document);

/**
 * Navigation Plugin
 * @version 2.1.0
 * @author Artus Kolanowski
 * @author David Deutsch
 * @license The MIT License (MIT)
 */
;(function($, window, document, undefined) {
	'use strict';

	/**
	 * Creates the navigation plugin.
	 * @class The Navigation Plugin
	 * @param {Owl} carousel - The Owl Carousel.
	 */
	var Navigation = function(carousel) {
		/**
		 * Reference to the core.
		 * @protected
		 * @type {Owl}
		 */
		this._core = carousel;

		/**
		 * Indicates whether the plugin is initialized or not.
		 * @protected
		 * @type {Boolean}
		 */
		this._initialized = false;

		/**
		 * The current paging indexes.
		 * @protected
		 * @type {Array}
		 */
		this._pages = [];

		/**
		 * All DOM elements of the user interface.
		 * @protected
		 * @type {Object}
		 */
		this._controls = {};

		/**
		 * Markup for an indicator.
		 * @protected
		 * @type {Array.<String>}
		 */
		this._templates = [];

		/**
		 * The carousel element.
		 * @type {jQuery}
		 */
		this.$element = this._core.$element;

		/**
		 * Overridden methods of the carousel.
		 * @protected
		 * @type {Object}
		 */
		this._overrides = {
			next: this._core.next,
			prev: this._core.prev,
			to: this._core.to
		};

		/**
		 * All event handlers.
		 * @protected
		 * @type {Object}
		 */
		this._handlers = {
			'prepared.owl.carousel': $.proxy(function(e) {
				if (e.namespace && this._core.settings.dotsData) {
					this._templates.push('<div class="' + this._core.settings.dotClass + '">' +
						$(e.content).find('[data-dot]').addBack('[data-dot]').attr('data-dot') + '</div>');
				}
			}, this),
			'added.owl.carousel': $.proxy(function(e) {
				if (e.namespace && this._core.settings.dotsData) {
					this._templates.splice(e.position, 0, this._templates.pop());
				}
			}, this),
			'remove.owl.carousel': $.proxy(function(e) {
				if (e.namespace && this._core.settings.dotsData) {
					this._templates.splice(e.position, 1);
				}
			}, this),
			'changed.owl.carousel': $.proxy(function(e) {
				if (e.namespace && e.property.name == 'position') {
					this.draw();
				}
			}, this),
			'initialized.owl.carousel': $.proxy(function(e) {
				if (e.namespace && !this._initialized) {
					this._core.trigger('initialize', null, 'navigation');
					this.initialize();
					this.update();
					this.draw();
					this._initialized = true;
					this._core.trigger('initialized', null, 'navigation');
				}
			}, this),
			'refreshed.owl.carousel': $.proxy(function(e) {
				if (e.namespace && this._initialized) {
					this._core.trigger('refresh', null, 'navigation');
					this.update();
					this.draw();
					this._core.trigger('refreshed', null, 'navigation');
				}
			}, this)
		};

		// set default options
		this._core.options = $.extend({}, Navigation.Defaults, this._core.options);

		// register event handlers
		this.$element.on(this._handlers);
	};

	/**
	 * Default options.
	 * @public
	 * @todo Rename `slideBy` to `navBy`
	 */
	Navigation.Defaults = {
		nav: false,
		navText: [ 'prev', 'next' ],
		navSpeed: false,
		navElement: 'div',
		navContainer: false,
		navContainerClass: 'owl-nav',
		navClass: [ 'owl-prev', 'owl-next' ],
		slideBy: 1,
		dotClass: 'owl-dot',
		dotsClass: 'owl-dots',
		dots: true,
		dotsEach: false,
		dotsData: false,
		dotsSpeed: false,
		dotsContainer: false
	};

	/**
	 * Initializes the layout of the plugin and extends the carousel.
	 * @protected
	 */
	Navigation.prototype.initialize = function() {
		var override,
			settings = this._core.settings;

		// create DOM structure for relative navigation
		this._controls.$relative = (settings.navContainer ? $(settings.navContainer)
			: $('<div>').addClass(settings.navContainerClass).appendTo(this.$element)).addClass('disabled');

		this._controls.$previous = $('<' + settings.navElement + '>')
			.addClass(settings.navClass[0])
			.html(settings.navText[0])
			.prependTo(this._controls.$relative)
			.on('click', $.proxy(function(e) {
				this.prev(settings.navSpeed);
			}, this));
		this._controls.$next = $('<' + settings.navElement + '>')
			.addClass(settings.navClass[1])
			.html(settings.navText[1])
			.appendTo(this._controls.$relative)
			.on('click', $.proxy(function(e) {
				this.next(settings.navSpeed);
			}, this));

		// create DOM structure for absolute navigation
		if (!settings.dotsData) {
			this._templates = [ $('<div>')
				.addClass(settings.dotClass)
				.append($('<span>'))
				.prop('outerHTML') ];
		}

		this._controls.$absolute = (settings.dotsContainer ? $(settings.dotsContainer)
			: $('<div>').addClass(settings.dotsClass).appendTo(this.$element)).addClass('disabled');

		this._controls.$absolute.on('click', 'div', $.proxy(function(e) {
			var index = $(e.target).parent().is(this._controls.$absolute)
				? $(e.target).index() : $(e.target).parent().index();

			e.preventDefault();

			this.to(index, settings.dotsSpeed);
		}, this));

		// override public methods of the carousel
		for (override in this._overrides) {
			this._core[override] = $.proxy(this[override], this);
		}
	};

	/**
	 * Destroys the plugin.
	 * @protected
	 */
	Navigation.prototype.destroy = function() {
		var handler, control, property, override;

		for (handler in this._handlers) {
			this.$element.off(handler, this._handlers[handler]);
		}
		for (control in this._controls) {
			this._controls[control].remove();
		}
		for (override in this.overides) {
			this._core[override] = this._overrides[override];
		}
		for (property in Object.getOwnPropertyNames(this)) {
			typeof this[property] != 'function' && (this[property] = null);
		}
	};

	/**
	 * Updates the internal state.
	 * @protected
	 */
	Navigation.prototype.update = function() {
		var i, j, k,
			lower = this._core.clones().length / 2,
			upper = lower + this._core.items().length,
			maximum = this._core.maximum(true),
			settings = this._core.settings,
			size = settings.center || settings.autoWidth || settings.dotsData
				? 1 : settings.dotsEach || settings.items;

		if (settings.slideBy !== 'page') {
			settings.slideBy = Math.min(settings.slideBy, settings.items);
		}

		if (settings.dots || settings.slideBy == 'page') {
			this._pages = [];

			for (i = lower, j = 0, k = 0; i < upper; i++) {
				if (j >= size || j === 0) {
					this._pages.push({
						start: Math.min(maximum, i - lower),
						end: i - lower + size - 1
					});
					if (Math.min(maximum, i - lower) === maximum) {
						break;
					}
					j = 0, ++k;
				}
				j += this._core.mergers(this._core.relative(i));
			}
		}
	};

	/**
	 * Draws the user interface.
	 * @todo The option `dotsData` wont work.
	 * @protected
	 */
	Navigation.prototype.draw = function() {
		var difference,
			settings = this._core.settings,
			disabled = this._core.items().length <= settings.items,
			index = this._core.relative(this._core.current()),
			loop = settings.loop || settings.rewind;

		this._controls.$relative.toggleClass('disabled', !settings.nav || disabled);

		if (settings.nav) {
			this._controls.$previous.toggleClass('disabled', !loop && index <= this._core.minimum(true));
			this._controls.$next.toggleClass('disabled', !loop && index >= this._core.maximum(true));
		}

		this._controls.$absolute.toggleClass('disabled', !settings.dots || disabled);

		if (settings.dots) {
			difference = this._pages.length - this._controls.$absolute.children().length;

			if (settings.dotsData && difference !== 0) {
				this._controls.$absolute.html(this._templates.join(''));
			} else if (difference > 0) {
				this._controls.$absolute.append(new Array(difference + 1).join(this._templates[0]));
			} else if (difference < 0) {
				this._controls.$absolute.children().slice(difference).remove();
			}

			this._controls.$absolute.find('.active').removeClass('active');
			this._controls.$absolute.children().eq($.inArray(this.current(), this._pages)).addClass('active');
		}
	};

	/**
	 * Extends event data.
	 * @protected
	 * @param {Event} event - The event object which gets thrown.
	 */
	Navigation.prototype.onTrigger = function(event) {
		var settings = this._core.settings;

		event.page = {
			index: $.inArray(this.current(), this._pages),
			count: this._pages.length,
			size: settings && (settings.center || settings.autoWidth || settings.dotsData
				? 1 : settings.dotsEach || settings.items)
		};
	};

	/**
	 * Gets the current page position of the carousel.
	 * @protected
	 * @returns {Number}
	 */
	Navigation.prototype.current = function() {
		var current = this._core.relative(this._core.current());
		return $.grep(this._pages, $.proxy(function(page, index) {
			return page.start <= current && page.end >= current;
		}, this)).pop();
	};

	/**
	 * Gets the current succesor/predecessor position.
	 * @protected
	 * @returns {Number}
	 */
	Navigation.prototype.getPosition = function(successor) {
		var position, length,
			settings = this._core.settings;

		if (settings.slideBy == 'page') {
			position = $.inArray(this.current(), this._pages);
			length = this._pages.length;
			successor ? ++position : --position;
			position = this._pages[((position % length) + length) % length].start;
		} else {
			position = this._core.relative(this._core.current());
			length = this._core.items().length;
			successor ? position += settings.slideBy : position -= settings.slideBy;
		}

		return position;
	};

	/**
	 * Slides to the next item or page.
	 * @public
	 * @param {Number} [speed=false] - The time in milliseconds for the transition.
	 */
	Navigation.prototype.next = function(speed) {
		$.proxy(this._overrides.to, this._core)(this.getPosition(true), speed);
	};

	/**
	 * Slides to the previous item or page.
	 * @public
	 * @param {Number} [speed=false] - The time in milliseconds for the transition.
	 */
	Navigation.prototype.prev = function(speed) {
		$.proxy(this._overrides.to, this._core)(this.getPosition(false), speed);
	};

	/**
	 * Slides to the specified item or page.
	 * @public
	 * @param {Number} position - The position of the item or page.
	 * @param {Number} [speed] - The time in milliseconds for the transition.
	 * @param {Boolean} [standard=false] - Whether to use the standard behaviour or not.
	 */
	Navigation.prototype.to = function(position, speed, standard) {
		var length;

		if (!standard && this._pages.length) {
			length = this._pages.length;
			$.proxy(this._overrides.to, this._core)(this._pages[((position % length) + length) % length].start, speed);
		} else {
			$.proxy(this._overrides.to, this._core)(position, speed);
		}
	};

	$.fn.owlCarousel.Constructor.Plugins.Navigation = Navigation;

})(window.Zepto || window.jQuery, window, document);

/**
 * Hash Plugin
 * @version 2.1.0
 * @author Artus Kolanowski
 * @author David Deutsch
 * @license The MIT License (MIT)
 */
;(function($, window, document, undefined) {
	'use strict';

	/**
	 * Creates the hash plugin.
	 * @class The Hash Plugin
	 * @param {Owl} carousel - The Owl Carousel
	 */
	var Hash = function(carousel) {
		/**
		 * Reference to the core.
		 * @protected
		 * @type {Owl}
		 */
		this._core = carousel;

		/**
		 * Hash index for the items.
		 * @protected
		 * @type {Object}
		 */
		this._hashes = {};

		/**
		 * The carousel element.
		 * @type {jQuery}
		 */
		this.$element = this._core.$element;

		/**
		 * All event handlers.
		 * @protected
		 * @type {Object}
		 */
		this._handlers = {
			'initialized.owl.carousel': $.proxy(function(e) {
				if (e.namespace && this._core.settings.startPosition === 'URLHash') {
					$(window).trigger('hashchange.owl.navigation');
				}
			}, this),
			'prepared.owl.carousel': $.proxy(function(e) {
				if (e.namespace) {
					var hash = $(e.content).find('[data-hash]').addBack('[data-hash]').attr('data-hash');

					if (!hash) {
						return;
					}

					this._hashes[hash] = e.content;
				}
			}, this),
			'changed.owl.carousel': $.proxy(function(e) {
				if (e.namespace && e.property.name === 'position') {
					var current = this._core.items(this._core.relative(this._core.current())),
						hash = $.map(this._hashes, function(item, hash) {
							return item === current ? hash : null;
						}).join();

					if (!hash || window.location.hash.slice(1) === hash) {
						return;
					}

					window.location.hash = hash;
				}
			}, this)
		};

		// set default options
		this._core.options = $.extend({}, Hash.Defaults, this._core.options);

		// register the event handlers
		this.$element.on(this._handlers);

		// register event listener for hash navigation
		$(window).on('hashchange.owl.navigation', $.proxy(function(e) {
			var hash = window.location.hash.substring(1),
				items = this._core.$stage.children(),
				position = this._hashes[hash] && items.index(this._hashes[hash]);

			if (position === undefined || position === this._core.current()) {
				return;
			}

			this._core.to(this._core.relative(position), false, true);
		}, this));
	};

	/**
	 * Default options.
	 * @public
	 */
	Hash.Defaults = {
		URLhashListener: false
	};

	/**
	 * Destroys the plugin.
	 * @public
	 */
	Hash.prototype.destroy = function() {
		var handler, property;

		$(window).off('hashchange.owl.navigation');

		for (handler in this._handlers) {
			this._core.$element.off(handler, this._handlers[handler]);
		}
		for (property in Object.getOwnPropertyNames(this)) {
			typeof this[property] != 'function' && (this[property] = null);
		}
	};

	$.fn.owlCarousel.Constructor.Plugins.Hash = Hash;

})(window.Zepto || window.jQuery, window, document);

/**
 * Support Plugin
 *
 * @version 2.1.0
 * @author Vivid Planet Software GmbH
 * @author Artus Kolanowski
 * @author David Deutsch
 * @license The MIT License (MIT)
 */
;(function($, window, document, undefined) {

	var style = $('<support>').get(0).style,
		prefixes = 'Webkit Moz O ms'.split(' '),
		events = {
			transition: {
				end: {
					WebkitTransition: 'webkitTransitionEnd',
					MozTransition: 'transitionend',
					OTransition: 'oTransitionEnd',
					transition: 'transitionend'
				}
			},
			animation: {
				end: {
					WebkitAnimation: 'webkitAnimationEnd',
					MozAnimation: 'animationend',
					OAnimation: 'oAnimationEnd',
					animation: 'animationend'
				}
			}
		},
		tests = {
			csstransforms: function() {
				return !!test('transform');
			},
			csstransforms3d: function() {
				return !!test('perspective');
			},
			csstransitions: function() {
				return !!test('transition');
			},
			cssanimations: function() {
				return !!test('animation');
			}
		};

	function test(property, prefixed) {
		var result = false,
			upper = property.charAt(0).toUpperCase() + property.slice(1);

		$.each((property + ' ' + prefixes.join(upper + ' ') + upper).split(' '), function(i, property) {
			if (style[property] !== undefined) {
				result = prefixed ? property : true;
				return false;
			}
		});

		return result;
	}

	function prefixed(property) {
		return test(property, true);
	}

	if (tests.csstransitions()) {
		/* jshint -W053 */
		$.support.transition = new String(prefixed('transition'))
		$.support.transition.end = events.transition.end[ $.support.transition ];
	}

	if (tests.cssanimations()) {
		/* jshint -W053 */
		$.support.animation = new String(prefixed('animation'))
		$.support.animation.end = events.animation.end[ $.support.animation ];
	}

	if (tests.csstransforms()) {
		/* jshint -W053 */
		$.support.transform = new String(prefixed('transform'));
		$.support.transform3d = tests.csstransforms3d();
	}

})(window.Zepto || window.jQuery, window, document);
/*! PhotoSwipe - v4.1.2 - 2017-04-05
* http://photoswipe.com
* Copyright (c) 2017 Dmitry Semenov; */

(function (root, factory) { 
	if (typeof define === 'function' && define.amd) {
		define(factory);
	} else if (typeof exports === 'object') {
		module.exports = factory();
	} else {
		root.PhotoSwipe = factory();
	}
})(this, function () {

	'use strict';
	var PhotoSwipe = function(template, UiClass, items, options){

/*>>framework-bridge*/
/**
 *
 * Set of generic functions used by gallery.
 * 
 * You're free to modify anything here as long as functionality is kept.
 * 
 */
var framework = {
	features: null,
	bind: function(target, type, listener, unbind) {
		var methodName = (unbind ? 'remove' : 'add') + 'EventListener';
		type = type.split(' ');
		for(var i = 0; i < type.length; i++) {
			if(type[i]) {
				target[methodName]( type[i], listener, false);
			}
		}
	},
	isArray: function(obj) {
		return (obj instanceof Array);
	},
	createEl: function(classes, tag) {
		var el = document.createElement(tag || 'div');
		if(classes) {
			el.className = classes;
		}
		return el;
	},
	getScrollY: function() {
		var yOffset = window.pageYOffset;
		return yOffset !== undefined ? yOffset : document.documentElement.scrollTop;
	},
	unbind: function(target, type, listener) {
		framework.bind(target,type,listener,true);
	},
	removeClass: function(el, className) {
		var reg = new RegExp('(\\s|^)' + className + '(\\s|$)');
		el.className = el.className.replace(reg, ' ').replace(/^\s\s*/, '').replace(/\s\s*$/, ''); 
	},
	addClass: function(el, className) {
		if( !framework.hasClass(el,className) ) {
			el.className += (el.className ? ' ' : '') + className;
		}
	},
	hasClass: function(el, className) {
		return el.className && new RegExp('(^|\\s)' + className + '(\\s|$)').test(el.className);
	},
	getChildByClass: function(parentEl, childClassName) {
		var node = parentEl.firstChild;
		while(node) {
			if( framework.hasClass(node, childClassName) ) {
				return node;
			}
			node = node.nextSibling;
		}
	},
	arraySearch: function(array, value, key) {
		var i = array.length;
		while(i--) {
			if(array[i][key] === value) {
				return i;
			} 
		}
		return -1;
	},
	extend: function(o1, o2, preventOverwrite) {
		for (var prop in o2) {
			if (o2.hasOwnProperty(prop)) {
				if(preventOverwrite && o1.hasOwnProperty(prop)) {
					continue;
				}
				o1[prop] = o2[prop];
			}
		}
	},
	easing: {
		sine: {
			out: function(k) {
				return Math.sin(k * (Math.PI / 2));
			},
			inOut: function(k) {
				return - (Math.cos(Math.PI * k) - 1) / 2;
			}
		},
		cubic: {
			out: function(k) {
				return --k * k * k + 1;
			}
		}
		/*
			elastic: {
				out: function ( k ) {

					var s, a = 0.1, p = 0.4;
					if ( k === 0 ) return 0;
					if ( k === 1 ) return 1;
					if ( !a || a < 1 ) { a = 1; s = p / 4; }
					else s = p * Math.asin( 1 / a ) / ( 2 * Math.PI );
					return ( a * Math.pow( 2, - 10 * k) * Math.sin( ( k - s ) * ( 2 * Math.PI ) / p ) + 1 );

				},
			},
			back: {
				out: function ( k ) {
					var s = 1.70158;
					return --k * k * ( ( s + 1 ) * k + s ) + 1;
				}
			}
		*/
	},

	/**
	 * 
	 * @return {object}
	 * 
	 * {
	 *  raf : request animation frame function
	 *  caf : cancel animation frame function
	 *  transfrom : transform property key (with vendor), or null if not supported
	 *  oldIE : IE8 or below
	 * }
	 * 
	 */
	detectFeatures: function() {
		if(framework.features) {
			return framework.features;
		}
		var helperEl = framework.createEl(),
			helperStyle = helperEl.style,
			vendor = '',
			features = {};

		// IE8 and below
		features.oldIE = document.all && !document.addEventListener;

		features.touch = 'ontouchstart' in window;

		if(window.requestAnimationFrame) {
			features.raf = window.requestAnimationFrame;
			features.caf = window.cancelAnimationFrame;
		}

		features.pointerEvent = navigator.pointerEnabled || navigator.msPointerEnabled;

		// fix false-positive detection of old Android in new IE
		// (IE11 ua string contains "Android 4.0")
		
		if(!features.pointerEvent) { 

			var ua = navigator.userAgent;

			// Detect if device is iPhone or iPod and if it's older than iOS 8
			// http://stackoverflow.com/a/14223920
			// 
			// This detection is made because of buggy top/bottom toolbars
			// that don't trigger window.resize event.
			// For more info refer to _isFixedPosition variable in core.js

			if (/iP(hone|od)/.test(navigator.platform)) {
				var v = (navigator.appVersion).match(/OS (\d+)_(\d+)_?(\d+)?/);
				if(v && v.length > 0) {
					v = parseInt(v[1], 10);
					if(v >= 1 && v < 8 ) {
						features.isOldIOSPhone = true;
					}
				}
			}

			// Detect old Android (before KitKat)
			// due to bugs related to position:fixed
			// http://stackoverflow.com/questions/7184573/pick-up-the-android-version-in-the-browser-by-javascript
			
			var match = ua.match(/Android\s([0-9\.]*)/);
			var androidversion =  match ? match[1] : 0;
			androidversion = parseFloat(androidversion);
			if(androidversion >= 1 ) {
				if(androidversion < 4.4) {
					features.isOldAndroid = true; // for fixed position bug & performance
				}
				features.androidVersion = androidversion; // for touchend bug
			}	
			features.isMobileOpera = /opera mini|opera mobi/i.test(ua);

			// p.s. yes, yes, UA sniffing is bad, propose your solution for above bugs.
		}
		
		var styleChecks = ['transform', 'perspective', 'animationName'],
			vendors = ['', 'webkit','Moz','ms','O'],
			styleCheckItem,
			styleName;

		for(var i = 0; i < 4; i++) {
			vendor = vendors[i];

			for(var a = 0; a < 3; a++) {
				styleCheckItem = styleChecks[a];

				// uppercase first letter of property name, if vendor is present
				styleName = vendor + (vendor ? 
										styleCheckItem.charAt(0).toUpperCase() + styleCheckItem.slice(1) : 
										styleCheckItem);
			
				if(!features[styleCheckItem] && styleName in helperStyle ) {
					features[styleCheckItem] = styleName;
				}
			}

			if(vendor && !features.raf) {
				vendor = vendor.toLowerCase();
				features.raf = window[vendor+'RequestAnimationFrame'];
				if(features.raf) {
					features.caf = window[vendor+'CancelAnimationFrame'] || 
									window[vendor+'CancelRequestAnimationFrame'];
				}
			}
		}
			
		if(!features.raf) {
			var lastTime = 0;
			features.raf = function(fn) {
				var currTime = new Date().getTime();
				var timeToCall = Math.max(0, 16 - (currTime - lastTime));
				var id = window.setTimeout(function() { fn(currTime + timeToCall); }, timeToCall);
				lastTime = currTime + timeToCall;
				return id;
			};
			features.caf = function(id) { clearTimeout(id); };
		}

		// Detect SVG support
		features.svg = !!document.createElementNS && 
						!!document.createElementNS('http://www.w3.org/2000/svg', 'svg').createSVGRect;

		framework.features = features;

		return features;
	}
};

framework.detectFeatures();

// Override addEventListener for old versions of IE
if(framework.features.oldIE) {

	framework.bind = function(target, type, listener, unbind) {
		
		type = type.split(' ');

		var methodName = (unbind ? 'detach' : 'attach') + 'Event',
			evName,
			_handleEv = function() {
				listener.handleEvent.call(listener);
			};

		for(var i = 0; i < type.length; i++) {
			evName = type[i];
			if(evName) {

				if(typeof listener === 'object' && listener.handleEvent) {
					if(!unbind) {
						listener['oldIE' + evName] = _handleEv;
					} else {
						if(!listener['oldIE' + evName]) {
							return false;
						}
					}

					target[methodName]( 'on' + evName, listener['oldIE' + evName]);
				} else {
					target[methodName]( 'on' + evName, listener);
				}

			}
		}
	};
	
}

/*>>framework-bridge*/

/*>>core*/
//function(template, UiClass, items, options)

var self = this;

/**
 * Static vars, don't change unless you know what you're doing.
 */
var DOUBLE_TAP_RADIUS = 25, 
	NUM_HOLDERS = 3;

/**
 * Options
 */
var _options = {
	allowPanToNext:true,
	spacing: 0.12,
	bgOpacity: 1,
	mouseUsed: false,
	loop: true,
	pinchToClose: true,
	closeOnScroll: true,
	closeOnVerticalDrag: true,
	verticalDragRange: 0.75,
	hideAnimationDuration: 333,
	showAnimationDuration: 333,
	showHideOpacity: false,
	focus: true,
	escKey: true,
	arrowKeys: true,
	mainScrollEndFriction: 0.35,
	panEndFriction: 0.35,
	isClickableElement: function(el) {
        return el.tagName === 'A';
    },
    getDoubleTapZoom: function(isMouseClick, item) {
    	if(isMouseClick) {
    		return 1;
    	} else {
    		return item.initialZoomLevel < 0.7 ? 1 : 1.33;
    	}
    },
    maxSpreadZoom: 1.33,
	modal: true,

	// not fully implemented yet
	scaleMode: 'fit' // TODO
};
framework.extend(_options, options);


/**
 * Private helper variables & functions
 */

var _getEmptyPoint = function() { 
		return {x:0,y:0}; 
	};

var _isOpen,
	_isDestroying,
	_closedByScroll,
	_currentItemIndex,
	_containerStyle,
	_containerShiftIndex,
	_currPanDist = _getEmptyPoint(),
	_startPanOffset = _getEmptyPoint(),
	_panOffset = _getEmptyPoint(),
	_upMoveEvents, // drag move, drag end & drag cancel events array
	_downEvents, // drag start events array
	_globalEventHandlers,
	_viewportSize = {},
	_currZoomLevel,
	_startZoomLevel,
	_translatePrefix,
	_translateSufix,
	_updateSizeInterval,
	_itemsNeedUpdate,
	_currPositionIndex = 0,
	_offset = {},
	_slideSize = _getEmptyPoint(), // size of slide area, including spacing
	_itemHolders,
	_prevItemIndex,
	_indexDiff = 0, // difference of indexes since last content update
	_dragStartEvent,
	_dragMoveEvent,
	_dragEndEvent,
	_dragCancelEvent,
	_transformKey,
	_pointerEventEnabled,
	_isFixedPosition = true,
	_likelyTouchDevice,
	_modules = [],
	_requestAF,
	_cancelAF,
	_initalClassName,
	_initalWindowScrollY,
	_oldIE,
	_currentWindowScrollY,
	_features,
	_windowVisibleSize = {},
	_renderMaxResolution = false,
	_orientationChangeTimeout,


	// Registers PhotoSWipe module (History, Controller ...)
	_registerModule = function(name, module) {
		framework.extend(self, module.publicMethods);
		_modules.push(name);
	},

	_getLoopedId = function(index) {
		var numSlides = _getNumItems();
		if(index > numSlides - 1) {
			return index - numSlides;
		} else  if(index < 0) {
			return numSlides + index;
		}
		return index;
	},
	
	// Micro bind/trigger
	_listeners = {},
	_listen = function(name, fn) {
		if(!_listeners[name]) {
			_listeners[name] = [];
		}
		return _listeners[name].push(fn);
	},
	_shout = function(name) {
		var listeners = _listeners[name];

		if(listeners) {
			var args = Array.prototype.slice.call(arguments);
			args.shift();

			for(var i = 0; i < listeners.length; i++) {
				listeners[i].apply(self, args);
			}
		}
	},

	_getCurrentTime = function() {
		return new Date().getTime();
	},
	_applyBgOpacity = function(opacity) {
		_bgOpacity = opacity;
		self.bg.style.opacity = opacity * _options.bgOpacity;
	},

	_applyZoomTransform = function(styleObj,x,y,zoom,item) {
		if(!_renderMaxResolution || (item && item !== self.currItem) ) {
			zoom = zoom / (item ? item.fitRatio : self.currItem.fitRatio);	
		}
			
		styleObj[_transformKey] = _translatePrefix + x + 'px, ' + y + 'px' + _translateSufix + ' scale(' + zoom + ')';
	},
	_applyCurrentZoomPan = function( allowRenderResolution ) {
		if(_currZoomElementStyle) {

			if(allowRenderResolution) {
				if(_currZoomLevel > self.currItem.fitRatio) {
					if(!_renderMaxResolution) {
						_setImageSize(self.currItem, false, true);
						_renderMaxResolution = true;
					}
				} else {
					if(_renderMaxResolution) {
						_setImageSize(self.currItem);
						_renderMaxResolution = false;
					}
				}
			}
			

			_applyZoomTransform(_currZoomElementStyle, _panOffset.x, _panOffset.y, _currZoomLevel);
		}
	},
	_applyZoomPanToItem = function(item) {
		if(item.container) {

			_applyZoomTransform(item.container.style, 
								item.initialPosition.x, 
								item.initialPosition.y, 
								item.initialZoomLevel,
								item);
		}
	},
	_setTranslateX = function(x, elStyle) {
		elStyle[_transformKey] = _translatePrefix + x + 'px, 0px' + _translateSufix;
	},
	_moveMainScroll = function(x, dragging) {

		if(!_options.loop && dragging) {
			var newSlideIndexOffset = _currentItemIndex + (_slideSize.x * _currPositionIndex - x) / _slideSize.x,
				delta = Math.round(x - _mainScrollPos.x);

			if( (newSlideIndexOffset < 0 && delta > 0) || 
				(newSlideIndexOffset >= _getNumItems() - 1 && delta < 0) ) {
				x = _mainScrollPos.x + delta * _options.mainScrollEndFriction;
			} 
		}
		
		_mainScrollPos.x = x;
		_setTranslateX(x, _containerStyle);
	},
	_calculatePanOffset = function(axis, zoomLevel) {
		var m = _midZoomPoint[axis] - _offset[axis];
		return _startPanOffset[axis] + _currPanDist[axis] + m - m * ( zoomLevel / _startZoomLevel );
	},
	
	_equalizePoints = function(p1, p2) {
		p1.x = p2.x;
		p1.y = p2.y;
		if(p2.id) {
			p1.id = p2.id;
		}
	},
	_roundPoint = function(p) {
		p.x = Math.round(p.x);
		p.y = Math.round(p.y);
	},

	_mouseMoveTimeout = null,
	_onFirstMouseMove = function() {
		// Wait until mouse move event is fired at least twice during 100ms
		// We do this, because some mobile browsers trigger it on touchstart
		if(_mouseMoveTimeout ) { 
			framework.unbind(document, 'mousemove', _onFirstMouseMove);
			framework.addClass(template, 'pswp--has_mouse');
			_options.mouseUsed = true;
			_shout('mouseUsed');
		}
		_mouseMoveTimeout = setTimeout(function() {
			_mouseMoveTimeout = null;
		}, 100);
	},

	_bindEvents = function() {
		framework.bind(document, 'keydown', self);

		if(_features.transform) {
			// don't bind click event in browsers that don't support transform (mostly IE8)
			framework.bind(self.scrollWrap, 'click', self);
		}
		

		if(!_options.mouseUsed) {
			framework.bind(document, 'mousemove', _onFirstMouseMove);
		}

		framework.bind(window, 'resize scroll orientationchange', self);

		_shout('bindEvents');
	},

	_unbindEvents = function() {
		framework.unbind(window, 'resize scroll orientationchange', self);
		framework.unbind(window, 'scroll', _globalEventHandlers.scroll);
		framework.unbind(document, 'keydown', self);
		framework.unbind(document, 'mousemove', _onFirstMouseMove);

		if(_features.transform) {
			framework.unbind(self.scrollWrap, 'click', self);
		}

		if(_isDragging) {
			framework.unbind(window, _upMoveEvents, self);
		}

		clearTimeout(_orientationChangeTimeout);

		_shout('unbindEvents');
	},
	
	_calculatePanBounds = function(zoomLevel, update) {
		var bounds = _calculateItemSize( self.currItem, _viewportSize, zoomLevel );
		if(update) {
			_currPanBounds = bounds;
		}
		return bounds;
	},
	
	_getMinZoomLevel = function(item) {
		if(!item) {
			item = self.currItem;
		}
		return item.initialZoomLevel;
	},
	_getMaxZoomLevel = function(item) {
		if(!item) {
			item = self.currItem;
		}
		return item.w > 0 ? _options.maxSpreadZoom : 1;
	},

	// Return true if offset is out of the bounds
	_modifyDestPanOffset = function(axis, destPanBounds, destPanOffset, destZoomLevel) {
		if(destZoomLevel === self.currItem.initialZoomLevel) {
			destPanOffset[axis] = self.currItem.initialPosition[axis];
			return true;
		} else {
			destPanOffset[axis] = _calculatePanOffset(axis, destZoomLevel); 

			if(destPanOffset[axis] > destPanBounds.min[axis]) {
				destPanOffset[axis] = destPanBounds.min[axis];
				return true;
			} else if(destPanOffset[axis] < destPanBounds.max[axis] ) {
				destPanOffset[axis] = destPanBounds.max[axis];
				return true;
			}
		}
		return false;
	},

	_setupTransforms = function() {

		if(_transformKey) {
			// setup 3d transforms
			var allow3dTransform = _features.perspective && !_likelyTouchDevice;
			_translatePrefix = 'translate' + (allow3dTransform ? '3d(' : '(');
			_translateSufix = _features.perspective ? ', 0px)' : ')';	
			return;
		}

		// Override zoom/pan/move functions in case old browser is used (most likely IE)
		// (so they use left/top/width/height, instead of CSS transform)
	
		_transformKey = 'left';
		framework.addClass(template, 'pswp--ie');

		_setTranslateX = function(x, elStyle) {
			elStyle.left = x + 'px';
		};
		_applyZoomPanToItem = function(item) {

			var zoomRatio = item.fitRatio > 1 ? 1 : item.fitRatio,
				s = item.container.style,
				w = zoomRatio * item.w,
				h = zoomRatio * item.h;

			s.width = w + 'px';
			s.height = h + 'px';
			s.left = item.initialPosition.x + 'px';
			s.top = item.initialPosition.y + 'px';

		};
		_applyCurrentZoomPan = function() {
			if(_currZoomElementStyle) {

				var s = _currZoomElementStyle,
					item = self.currItem,
					zoomRatio = item.fitRatio > 1 ? 1 : item.fitRatio,
					w = zoomRatio * item.w,
					h = zoomRatio * item.h;

				s.width = w + 'px';
				s.height = h + 'px';


				s.left = _panOffset.x + 'px';
				s.top = _panOffset.y + 'px';
			}
			
		};
	},

	_onKeyDown = function(e) {
		var keydownAction = '';
		if(_options.escKey && e.keyCode === 27) { 
			keydownAction = 'close';
		} else if(_options.arrowKeys) {
			if(e.keyCode === 37) {
				keydownAction = 'prev';
			} else if(e.keyCode === 39) { 
				keydownAction = 'next';
			}
		}

		if(keydownAction) {
			// don't do anything if special key pressed to prevent from overriding default browser actions
			// e.g. in Chrome on Mac cmd+arrow-left returns to previous page
			if( !e.ctrlKey && !e.altKey && !e.shiftKey && !e.metaKey ) {
				if(e.preventDefault) {
					e.preventDefault();
				} else {
					e.returnValue = false;
				} 
				self[keydownAction]();
			}
		}
	},

	_onGlobalClick = function(e) {
		if(!e) {
			return;
		}

		// don't allow click event to pass through when triggering after drag or some other gesture
		if(_moved || _zoomStarted || _mainScrollAnimating || _verticalDragInitiated) {
			e.preventDefault();
			e.stopPropagation();
		}
	},

	_updatePageScrollOffset = function() {
		self.setScrollOffset(0, framework.getScrollY());		
	};
	


	



// Micro animation engine
var _animations = {},
	_numAnimations = 0,
	_stopAnimation = function(name) {
		if(_animations[name]) {
			if(_animations[name].raf) {
				_cancelAF( _animations[name].raf );
			}
			_numAnimations--;
			delete _animations[name];
		}
	},
	_registerStartAnimation = function(name) {
		if(_animations[name]) {
			_stopAnimation(name);
		}
		if(!_animations[name]) {
			_numAnimations++;
			_animations[name] = {};
		}
	},
	_stopAllAnimations = function() {
		for (var prop in _animations) {

			if( _animations.hasOwnProperty( prop ) ) {
				_stopAnimation(prop);
			} 
			
		}
	},
	_animateProp = function(name, b, endProp, d, easingFn, onUpdate, onComplete) {
		var startAnimTime = _getCurrentTime(), t;
		_registerStartAnimation(name);

		var animloop = function(){
			if ( _animations[name] ) {
				
				t = _getCurrentTime() - startAnimTime; // time diff
				//b - beginning (start prop)
				//d - anim duration

				if ( t >= d ) {
					_stopAnimation(name);
					onUpdate(endProp);
					if(onComplete) {
						onComplete();
					}
					return;
				}
				onUpdate( (endProp - b) * easingFn(t/d) + b );

				_animations[name].raf = _requestAF(animloop);
			}
		};
		animloop();
	};
	


var publicMethods = {

	// make a few local variables and functions public
	shout: _shout,
	listen: _listen,
	viewportSize: _viewportSize,
	options: _options,

	isMainScrollAnimating: function() {
		return _mainScrollAnimating;
	},
	getZoomLevel: function() {
		return _currZoomLevel;
	},
	getCurrentIndex: function() {
		return _currentItemIndex;
	},
	isDragging: function() {
		return _isDragging;
	},	
	isZooming: function() {
		return _isZooming;
	},
	setScrollOffset: function(x,y) {
		_offset.x = x;
		_currentWindowScrollY = _offset.y = y;
		_shout('updateScrollOffset', _offset);
	},
	applyZoomPan: function(zoomLevel,panX,panY,allowRenderResolution) {
		_panOffset.x = panX;
		_panOffset.y = panY;
		_currZoomLevel = zoomLevel;
		_applyCurrentZoomPan( allowRenderResolution );
	},

	init: function() {

		if(_isOpen || _isDestroying) {
			return;
		}

		var i;

		self.framework = framework; // basic functionality
		self.template = template; // root DOM element of PhotoSwipe
		self.bg = framework.getChildByClass(template, 'pswp__bg');

		_initalClassName = template.className;
		_isOpen = true;
				
		_features = framework.detectFeatures();
		_requestAF = _features.raf;
		_cancelAF = _features.caf;
		_transformKey = _features.transform;
		_oldIE = _features.oldIE;
		
		self.scrollWrap = framework.getChildByClass(template, 'pswp__scroll-wrap');
		self.container = framework.getChildByClass(self.scrollWrap, 'pswp__container');

		_containerStyle = self.container.style; // for fast access

		// Objects that hold slides (there are only 3 in DOM)
		self.itemHolders = _itemHolders = [
			{el:self.container.children[0] , wrap:0, index: -1},
			{el:self.container.children[1] , wrap:0, index: -1},
			{el:self.container.children[2] , wrap:0, index: -1}
		];

		// hide nearby item holders until initial zoom animation finishes (to avoid extra Paints)
		_itemHolders[0].el.style.display = _itemHolders[2].el.style.display = 'none';

		_setupTransforms();

		// Setup global events
		_globalEventHandlers = {
			resize: self.updateSize,

			// Fixes: iOS 10.3 resize event
			// does not update scrollWrap.clientWidth instantly after resize
			// https://github.com/dimsemenov/PhotoSwipe/issues/1315
			orientationchange: function() {
				clearTimeout(_orientationChangeTimeout);
				_orientationChangeTimeout = setTimeout(function() {
					if(_viewportSize.x !== self.scrollWrap.clientWidth) {
						self.updateSize();
					}
				}, 500);
			},
			scroll: _updatePageScrollOffset,
			keydown: _onKeyDown,
			click: _onGlobalClick
		};

		// disable show/hide effects on old browsers that don't support CSS animations or transforms, 
		// old IOS, Android and Opera mobile. Blackberry seems to work fine, even older models.
		var oldPhone = _features.isOldIOSPhone || _features.isOldAndroid || _features.isMobileOpera;
		if(!_features.animationName || !_features.transform || oldPhone) {
			_options.showAnimationDuration = _options.hideAnimationDuration = 0;
		}

		// init modules
		for(i = 0; i < _modules.length; i++) {
			self['init' + _modules[i]]();
		}
		
		// init
		if(UiClass) {
			var ui = self.ui = new UiClass(self, framework);
			ui.init();
		}

		_shout('firstUpdate');
		_currentItemIndex = _currentItemIndex || _options.index || 0;
		// validate index
		if( isNaN(_currentItemIndex) || _currentItemIndex < 0 || _currentItemIndex >= _getNumItems() ) {
			_currentItemIndex = 0;
		}
		self.currItem = _getItemAt( _currentItemIndex );

		
		if(_features.isOldIOSPhone || _features.isOldAndroid) {
			_isFixedPosition = false;
		}
		
		template.setAttribute('aria-hidden', 'false');
		if(_options.modal) {
			if(!_isFixedPosition) {
				template.style.position = 'absolute';
				template.style.top = framework.getScrollY() + 'px';
			} else {
				template.style.position = 'fixed';
			}
		}

		if(_currentWindowScrollY === undefined) {
			_shout('initialLayout');
			_currentWindowScrollY = _initalWindowScrollY = framework.getScrollY();
		}
		
		// add classes to root element of PhotoSwipe
		var rootClasses = 'pswp--open ';
		if(_options.mainClass) {
			rootClasses += _options.mainClass + ' ';
		}
		if(_options.showHideOpacity) {
			rootClasses += 'pswp--animate_opacity ';
		}
		rootClasses += _likelyTouchDevice ? 'pswp--touch' : 'pswp--notouch';
		rootClasses += _features.animationName ? ' pswp--css_animation' : '';
		rootClasses += _features.svg ? ' pswp--svg' : '';
		framework.addClass(template, rootClasses);

		self.updateSize();

		// initial update
		_containerShiftIndex = -1;
		_indexDiff = null;
		for(i = 0; i < NUM_HOLDERS; i++) {
			_setTranslateX( (i+_containerShiftIndex) * _slideSize.x, _itemHolders[i].el.style);
		}

		if(!_oldIE) {
			framework.bind(self.scrollWrap, _downEvents, self); // no dragging for old IE
		}	

		_listen('initialZoomInEnd', function() {
			self.setContent(_itemHolders[0], _currentItemIndex-1);
			self.setContent(_itemHolders[2], _currentItemIndex+1);

			_itemHolders[0].el.style.display = _itemHolders[2].el.style.display = 'block';

			if(_options.focus) {
				// focus causes layout, 
				// which causes lag during the animation, 
				// that's why we delay it untill the initial zoom transition ends
				template.focus();
			}
			 

			_bindEvents();
		});

		// set content for center slide (first time)
		self.setContent(_itemHolders[1], _currentItemIndex);
		
		self.updateCurrItem();

		_shout('afterInit');

		if(!_isFixedPosition) {

			// On all versions of iOS lower than 8.0, we check size of viewport every second.
			// 
			// This is done to detect when Safari top & bottom bars appear, 
			// as this action doesn't trigger any events (like resize). 
			// 
			// On iOS8 they fixed this.
			// 
			// 10 Nov 2014: iOS 7 usage ~40%. iOS 8 usage 56%.
			
			_updateSizeInterval = setInterval(function() {
				if(!_numAnimations && !_isDragging && !_isZooming && (_currZoomLevel === self.currItem.initialZoomLevel)  ) {
					self.updateSize();
				}
			}, 1000);
		}

		framework.addClass(template, 'pswp--visible');
	},

	// Close the gallery, then destroy it
	close: function() {
		if(!_isOpen) {
			return;
		}

		_isOpen = false;
		_isDestroying = true;
		_shout('close');
		_unbindEvents();

		_showOrHide(self.currItem, null, true, self.destroy);
	},

	// destroys the gallery (unbinds events, cleans up intervals and timeouts to avoid memory leaks)
	destroy: function() {
		_shout('destroy');

		if(_showOrHideTimeout) {
			clearTimeout(_showOrHideTimeout);
		}
		
		template.setAttribute('aria-hidden', 'true');
		template.className = _initalClassName;

		if(_updateSizeInterval) {
			clearInterval(_updateSizeInterval);
		}

		framework.unbind(self.scrollWrap, _downEvents, self);

		// we unbind scroll event at the end, as closing animation may depend on it
		framework.unbind(window, 'scroll', self);

		_stopDragUpdateLoop();

		_stopAllAnimations();

		_listeners = null;
	},

	/**
	 * Pan image to position
	 * @param {Number} x     
	 * @param {Number} y     
	 * @param {Boolean} force Will ignore bounds if set to true.
	 */
	panTo: function(x,y,force) {
		if(!force) {
			if(x > _currPanBounds.min.x) {
				x = _currPanBounds.min.x;
			} else if(x < _currPanBounds.max.x) {
				x = _currPanBounds.max.x;
			}

			if(y > _currPanBounds.min.y) {
				y = _currPanBounds.min.y;
			} else if(y < _currPanBounds.max.y) {
				y = _currPanBounds.max.y;
			}
		}
		
		_panOffset.x = x;
		_panOffset.y = y;
		_applyCurrentZoomPan();
	},
	
	handleEvent: function (e) {
		e = e || window.event;
		if(_globalEventHandlers[e.type]) {
			_globalEventHandlers[e.type](e);
		}
	},


	goTo: function(index) {

		index = _getLoopedId(index);

		var diff = index - _currentItemIndex;
		_indexDiff = diff;

		_currentItemIndex = index;
		self.currItem = _getItemAt( _currentItemIndex );
		_currPositionIndex -= diff;
		
		_moveMainScroll(_slideSize.x * _currPositionIndex);
		

		_stopAllAnimations();
		_mainScrollAnimating = false;

		self.updateCurrItem();
	},
	next: function() {
		self.goTo( _currentItemIndex + 1);
	},
	prev: function() {
		self.goTo( _currentItemIndex - 1);
	},

	// update current zoom/pan objects
	updateCurrZoomItem: function(emulateSetContent) {
		if(emulateSetContent) {
			_shout('beforeChange', 0);
		}

		// itemHolder[1] is middle (current) item
		if(_itemHolders[1].el.children.length) {
			var zoomElement = _itemHolders[1].el.children[0];
			if( framework.hasClass(zoomElement, 'pswp__zoom-wrap') ) {
				_currZoomElementStyle = zoomElement.style;
			} else {
				_currZoomElementStyle = null;
			}
		} else {
			_currZoomElementStyle = null;
		}
		
		_currPanBounds = self.currItem.bounds;	
		_startZoomLevel = _currZoomLevel = self.currItem.initialZoomLevel;

		_panOffset.x = _currPanBounds.center.x;
		_panOffset.y = _currPanBounds.center.y;

		if(emulateSetContent) {
			_shout('afterChange');
		}
	},


	invalidateCurrItems: function() {
		_itemsNeedUpdate = true;
		for(var i = 0; i < NUM_HOLDERS; i++) {
			if( _itemHolders[i].item ) {
				_itemHolders[i].item.needsUpdate = true;
			}
		}
	},

	updateCurrItem: function(beforeAnimation) {

		if(_indexDiff === 0) {
			return;
		}

		var diffAbs = Math.abs(_indexDiff),
			tempHolder;

		if(beforeAnimation && diffAbs < 2) {
			return;
		}


		self.currItem = _getItemAt( _currentItemIndex );
		_renderMaxResolution = false;
		
		_shout('beforeChange', _indexDiff);

		if(diffAbs >= NUM_HOLDERS) {
			_containerShiftIndex += _indexDiff + (_indexDiff > 0 ? -NUM_HOLDERS : NUM_HOLDERS);
			diffAbs = NUM_HOLDERS;
		}
		for(var i = 0; i < diffAbs; i++) {
			if(_indexDiff > 0) {
				tempHolder = _itemHolders.shift();
				_itemHolders[NUM_HOLDERS-1] = tempHolder; // move first to last

				_containerShiftIndex++;
				_setTranslateX( (_containerShiftIndex+2) * _slideSize.x, tempHolder.el.style);
				self.setContent(tempHolder, _currentItemIndex - diffAbs + i + 1 + 1);
			} else {
				tempHolder = _itemHolders.pop();
				_itemHolders.unshift( tempHolder ); // move last to first

				_containerShiftIndex--;
				_setTranslateX( _containerShiftIndex * _slideSize.x, tempHolder.el.style);
				self.setContent(tempHolder, _currentItemIndex + diffAbs - i - 1 - 1);
			}
			
		}

		// reset zoom/pan on previous item
		if(_currZoomElementStyle && Math.abs(_indexDiff) === 1) {

			var prevItem = _getItemAt(_prevItemIndex);
			if(prevItem.initialZoomLevel !== _currZoomLevel) {
				_calculateItemSize(prevItem , _viewportSize );
				_setImageSize(prevItem);
				_applyZoomPanToItem( prevItem ); 				
			}

		}

		// reset diff after update
		_indexDiff = 0;

		self.updateCurrZoomItem();

		_prevItemIndex = _currentItemIndex;

		_shout('afterChange');
		
	},



	updateSize: function(force) {
		
		if(!_isFixedPosition && _options.modal) {
			var windowScrollY = framework.getScrollY();
			if(_currentWindowScrollY !== windowScrollY) {
				template.style.top = windowScrollY + 'px';
				_currentWindowScrollY = windowScrollY;
			}
			if(!force && _windowVisibleSize.x === window.innerWidth && _windowVisibleSize.y === window.innerHeight) {
				return;
			}
			_windowVisibleSize.x = window.innerWidth;
			_windowVisibleSize.y = window.innerHeight;

			//template.style.width = _windowVisibleSize.x + 'px';
			template.style.height = _windowVisibleSize.y + 'px';
		}



		_viewportSize.x = self.scrollWrap.clientWidth;
		_viewportSize.y = self.scrollWrap.clientHeight;

		_updatePageScrollOffset();

		_slideSize.x = _viewportSize.x + Math.round(_viewportSize.x * _options.spacing);
		_slideSize.y = _viewportSize.y;

		_moveMainScroll(_slideSize.x * _currPositionIndex);

		_shout('beforeResize'); // even may be used for example to switch image sources


		// don't re-calculate size on inital size update
		if(_containerShiftIndex !== undefined) {

			var holder,
				item,
				hIndex;

			for(var i = 0; i < NUM_HOLDERS; i++) {
				holder = _itemHolders[i];
				_setTranslateX( (i+_containerShiftIndex) * _slideSize.x, holder.el.style);

				hIndex = _currentItemIndex+i-1;

				if(_options.loop && _getNumItems() > 2) {
					hIndex = _getLoopedId(hIndex);
				}

				// update zoom level on items and refresh source (if needsUpdate)
				item = _getItemAt( hIndex );

				// re-render gallery item if `needsUpdate`,
				// or doesn't have `bounds` (entirely new slide object)
				if( item && (_itemsNeedUpdate || item.needsUpdate || !item.bounds) ) {

					self.cleanSlide( item );
					
					self.setContent( holder, hIndex );

					// if "center" slide
					if(i === 1) {
						self.currItem = item;
						self.updateCurrZoomItem(true);
					}

					item.needsUpdate = false;

				} else if(holder.index === -1 && hIndex >= 0) {
					// add content first time
					self.setContent( holder, hIndex );
				}
				if(item && item.container) {
					_calculateItemSize(item, _viewportSize);
					_setImageSize(item);
					_applyZoomPanToItem( item );
				}
				
			}
			_itemsNeedUpdate = false;
		}	

		_startZoomLevel = _currZoomLevel = self.currItem.initialZoomLevel;
		_currPanBounds = self.currItem.bounds;

		if(_currPanBounds) {
			_panOffset.x = _currPanBounds.center.x;
			_panOffset.y = _currPanBounds.center.y;
			_applyCurrentZoomPan( true );
		}
		
		_shout('resize');
	},
	
	// Zoom current item to
	zoomTo: function(destZoomLevel, centerPoint, speed, easingFn, updateFn) {
		/*
			if(destZoomLevel === 'fit') {
				destZoomLevel = self.currItem.fitRatio;
			} else if(destZoomLevel === 'fill') {
				destZoomLevel = self.currItem.fillRatio;
			}
		*/

		if(centerPoint) {
			_startZoomLevel = _currZoomLevel;
			_midZoomPoint.x = Math.abs(centerPoint.x) - _panOffset.x ;
			_midZoomPoint.y = Math.abs(centerPoint.y) - _panOffset.y ;
			_equalizePoints(_startPanOffset, _panOffset);
		}

		var destPanBounds = _calculatePanBounds(destZoomLevel, false),
			destPanOffset = {};

		_modifyDestPanOffset('x', destPanBounds, destPanOffset, destZoomLevel);
		_modifyDestPanOffset('y', destPanBounds, destPanOffset, destZoomLevel);

		var initialZoomLevel = _currZoomLevel;
		var initialPanOffset = {
			x: _panOffset.x,
			y: _panOffset.y
		};

		_roundPoint(destPanOffset);

		var onUpdate = function(now) {
			if(now === 1) {
				_currZoomLevel = destZoomLevel;
				_panOffset.x = destPanOffset.x;
				_panOffset.y = destPanOffset.y;
			} else {
				_currZoomLevel = (destZoomLevel - initialZoomLevel) * now + initialZoomLevel;
				_panOffset.x = (destPanOffset.x - initialPanOffset.x) * now + initialPanOffset.x;
				_panOffset.y = (destPanOffset.y - initialPanOffset.y) * now + initialPanOffset.y;
			}

			if(updateFn) {
				updateFn(now);
			}

			_applyCurrentZoomPan( now === 1 );
		};

		if(speed) {
			_animateProp('customZoomTo', 0, 1, speed, easingFn || framework.easing.sine.inOut, onUpdate);
		} else {
			onUpdate(1);
		}
	}


};


/*>>core*/

/*>>gestures*/
/**
 * Mouse/touch/pointer event handlers.
 * 
 * separated from @core.js for readability
 */

var MIN_SWIPE_DISTANCE = 30,
	DIRECTION_CHECK_OFFSET = 10; // amount of pixels to drag to determine direction of swipe

var _gestureStartTime,
	_gestureCheckSpeedTime,

	// pool of objects that are used during dragging of zooming
	p = {}, // first point
	p2 = {}, // second point (for zoom gesture)
	delta = {},
	_currPoint = {},
	_startPoint = {},
	_currPointers = [],
	_startMainScrollPos = {},
	_releaseAnimData,
	_posPoints = [], // array of points during dragging, used to determine type of gesture
	_tempPoint = {},

	_isZoomingIn,
	_verticalDragInitiated,
	_oldAndroidTouchEndTimeout,
	_currZoomedItemIndex = 0,
	_centerPoint = _getEmptyPoint(),
	_lastReleaseTime = 0,
	_isDragging, // at least one pointer is down
	_isMultitouch, // at least two _pointers are down
	_zoomStarted, // zoom level changed during zoom gesture
	_moved,
	_dragAnimFrame,
	_mainScrollShifted,
	_currentPoints, // array of current touch points
	_isZooming,
	_currPointsDistance,
	_startPointsDistance,
	_currPanBounds,
	_mainScrollPos = _getEmptyPoint(),
	_currZoomElementStyle,
	_mainScrollAnimating, // true, if animation after swipe gesture is running
	_midZoomPoint = _getEmptyPoint(),
	_currCenterPoint = _getEmptyPoint(),
	_direction,
	_isFirstMove,
	_opacityChanged,
	_bgOpacity,
	_wasOverInitialZoom,

	_isEqualPoints = function(p1, p2) {
		return p1.x === p2.x && p1.y === p2.y;
	},
	_isNearbyPoints = function(touch0, touch1) {
		return Math.abs(touch0.x - touch1.x) < DOUBLE_TAP_RADIUS && Math.abs(touch0.y - touch1.y) < DOUBLE_TAP_RADIUS;
	},
	_calculatePointsDistance = function(p1, p2) {
		_tempPoint.x = Math.abs( p1.x - p2.x );
		_tempPoint.y = Math.abs( p1.y - p2.y );
		return Math.sqrt(_tempPoint.x * _tempPoint.x + _tempPoint.y * _tempPoint.y);
	},
	_stopDragUpdateLoop = function() {
		if(_dragAnimFrame) {
			_cancelAF(_dragAnimFrame);
			_dragAnimFrame = null;
		}
	},
	_dragUpdateLoop = function() {
		if(_isDragging) {
			_dragAnimFrame = _requestAF(_dragUpdateLoop);
			_renderMovement();
		}
	},
	_canPan = function() {
		return !(_options.scaleMode === 'fit' && _currZoomLevel ===  self.currItem.initialZoomLevel);
	},
	
	// find the closest parent DOM element
	_closestElement = function(el, fn) {
	  	if(!el || el === document) {
	  		return false;
	  	}

	  	// don't search elements above pswp__scroll-wrap
	  	if(el.getAttribute('class') && el.getAttribute('class').indexOf('pswp__scroll-wrap') > -1 ) {
	  		return false;
	  	}

	  	if( fn(el) ) {
	  		return el;
	  	}

	  	return _closestElement(el.parentNode, fn);
	},

	_preventObj = {},
	_preventDefaultEventBehaviour = function(e, isDown) {
	    _preventObj.prevent = !_closestElement(e.target, _options.isClickableElement);

		_shout('preventDragEvent', e, isDown, _preventObj);
		return _preventObj.prevent;

	},
	_convertTouchToPoint = function(touch, p) {
		p.x = touch.pageX;
		p.y = touch.pageY;
		p.id = touch.identifier;
		return p;
	},
	_findCenterOfPoints = function(p1, p2, pCenter) {
		pCenter.x = (p1.x + p2.x) * 0.5;
		pCenter.y = (p1.y + p2.y) * 0.5;
	},
	_pushPosPoint = function(time, x, y) {
		if(time - _gestureCheckSpeedTime > 50) {
			var o = _posPoints.length > 2 ? _posPoints.shift() : {};
			o.x = x;
			o.y = y; 
			_posPoints.push(o);
			_gestureCheckSpeedTime = time;
		}
	},

	_calculateVerticalDragOpacityRatio = function() {
		var yOffset = _panOffset.y - self.currItem.initialPosition.y; // difference between initial and current position
		return 1 -  Math.abs( yOffset / (_viewportSize.y / 2)  );
	},

	
	// points pool, reused during touch events
	_ePoint1 = {},
	_ePoint2 = {},
	_tempPointsArr = [],
	_tempCounter,
	_getTouchPoints = function(e) {
		// clean up previous points, without recreating array
		while(_tempPointsArr.length > 0) {
			_tempPointsArr.pop();
		}

		if(!_pointerEventEnabled) {
			if(e.type.indexOf('touch') > -1) {

				if(e.touches && e.touches.length > 0) {
					_tempPointsArr[0] = _convertTouchToPoint(e.touches[0], _ePoint1);
					if(e.touches.length > 1) {
						_tempPointsArr[1] = _convertTouchToPoint(e.touches[1], _ePoint2);
					}
				}
				
			} else {
				_ePoint1.x = e.pageX;
				_ePoint1.y = e.pageY;
				_ePoint1.id = '';
				_tempPointsArr[0] = _ePoint1;//_ePoint1;
			}
		} else {
			_tempCounter = 0;
			// we can use forEach, as pointer events are supported only in modern browsers
			_currPointers.forEach(function(p) {
				if(_tempCounter === 0) {
					_tempPointsArr[0] = p;
				} else if(_tempCounter === 1) {
					_tempPointsArr[1] = p;
				}
				_tempCounter++;

			});
		}
		return _tempPointsArr;
	},

	_panOrMoveMainScroll = function(axis, delta) {

		var panFriction,
			overDiff = 0,
			newOffset = _panOffset[axis] + delta[axis],
			startOverDiff,
			dir = delta[axis] > 0,
			newMainScrollPosition = _mainScrollPos.x + delta.x,
			mainScrollDiff = _mainScrollPos.x - _startMainScrollPos.x,
			newPanPos,
			newMainScrollPos;

		// calculate fdistance over the bounds and friction
		if(newOffset > _currPanBounds.min[axis] || newOffset < _currPanBounds.max[axis]) {
			panFriction = _options.panEndFriction;
			// Linear increasing of friction, so at 1/4 of viewport it's at max value. 
			// Looks not as nice as was expected. Left for history.
			// panFriction = (1 - (_panOffset[axis] + delta[axis] + panBounds.min[axis]) / (_viewportSize[axis] / 4) );
		} else {
			panFriction = 1;
		}
		
		newOffset = _panOffset[axis] + delta[axis] * panFriction;

		// move main scroll or start panning
		if(_options.allowPanToNext || _currZoomLevel === self.currItem.initialZoomLevel) {


			if(!_currZoomElementStyle) {
				
				newMainScrollPos = newMainScrollPosition;

			} else if(_direction === 'h' && axis === 'x' && !_zoomStarted ) {
				
				if(dir) {
					if(newOffset > _currPanBounds.min[axis]) {
						panFriction = _options.panEndFriction;
						overDiff = _currPanBounds.min[axis] - newOffset;
						startOverDiff = _currPanBounds.min[axis] - _startPanOffset[axis];
					}
					
					// drag right
					if( (startOverDiff <= 0 || mainScrollDiff < 0) && _getNumItems() > 1 ) {
						newMainScrollPos = newMainScrollPosition;
						if(mainScrollDiff < 0 && newMainScrollPosition > _startMainScrollPos.x) {
							newMainScrollPos = _startMainScrollPos.x;
						}
					} else {
						if(_currPanBounds.min.x !== _currPanBounds.max.x) {
							newPanPos = newOffset;
						}
						
					}

				} else {

					if(newOffset < _currPanBounds.max[axis] ) {
						panFriction =_options.panEndFriction;
						overDiff = newOffset - _currPanBounds.max[axis];
						startOverDiff = _startPanOffset[axis] - _currPanBounds.max[axis];
					}

					if( (startOverDiff <= 0 || mainScrollDiff > 0) && _getNumItems() > 1 ) {
						newMainScrollPos = newMainScrollPosition;

						if(mainScrollDiff > 0 && newMainScrollPosition < _startMainScrollPos.x) {
							newMainScrollPos = _startMainScrollPos.x;
						}

					} else {
						if(_currPanBounds.min.x !== _currPanBounds.max.x) {
							newPanPos = newOffset;
						}
					}

				}


				//
			}

			if(axis === 'x') {

				if(newMainScrollPos !== undefined) {
					_moveMainScroll(newMainScrollPos, true);
					if(newMainScrollPos === _startMainScrollPos.x) {
						_mainScrollShifted = false;
					} else {
						_mainScrollShifted = true;
					}
				}

				if(_currPanBounds.min.x !== _currPanBounds.max.x) {
					if(newPanPos !== undefined) {
						_panOffset.x = newPanPos;
					} else if(!_mainScrollShifted) {
						_panOffset.x += delta.x * panFriction;
					}
				}

				return newMainScrollPos !== undefined;
			}

		}

		if(!_mainScrollAnimating) {
			
			if(!_mainScrollShifted) {
				if(_currZoomLevel > self.currItem.fitRatio) {
					_panOffset[axis] += delta[axis] * panFriction;
				
				}
			}

			
		}
		
	},

	// Pointerdown/touchstart/mousedown handler
	_onDragStart = function(e) {

		// Allow dragging only via left mouse button.
		// As this handler is not added in IE8 - we ignore e.which
		// 
		// http://www.quirksmode.org/js/events_properties.html
		// https://developer.mozilla.org/en-US/docs/Web/API/event.button
		if(e.type === 'mousedown' && e.button > 0  ) {
			return;
		}

		if(_initialZoomRunning) {
			e.preventDefault();
			return;
		}

		if(_oldAndroidTouchEndTimeout && e.type === 'mousedown') {
			return;
		}

		if(_preventDefaultEventBehaviour(e, true)) {
			e.preventDefault();
		}



		_shout('pointerDown');

		if(_pointerEventEnabled) {
			var pointerIndex = framework.arraySearch(_currPointers, e.pointerId, 'id');
			if(pointerIndex < 0) {
				pointerIndex = _currPointers.length;
			}
			_currPointers[pointerIndex] = {x:e.pageX, y:e.pageY, id: e.pointerId};
		}
		


		var startPointsList = _getTouchPoints(e),
			numPoints = startPointsList.length;

		_currentPoints = null;

		_stopAllAnimations();

		// init drag
		if(!_isDragging || numPoints === 1) {

			

			_isDragging = _isFirstMove = true;
			framework.bind(window, _upMoveEvents, self);

			_isZoomingIn = 
				_wasOverInitialZoom = 
				_opacityChanged = 
				_verticalDragInitiated = 
				_mainScrollShifted = 
				_moved = 
				_isMultitouch = 
				_zoomStarted = false;

			_direction = null;

			_shout('firstTouchStart', startPointsList);

			_equalizePoints(_startPanOffset, _panOffset);

			_currPanDist.x = _currPanDist.y = 0;
			_equalizePoints(_currPoint, startPointsList[0]);
			_equalizePoints(_startPoint, _currPoint);

			//_equalizePoints(_startMainScrollPos, _mainScrollPos);
			_startMainScrollPos.x = _slideSize.x * _currPositionIndex;

			_posPoints = [{
				x: _currPoint.x,
				y: _currPoint.y
			}];

			_gestureCheckSpeedTime = _gestureStartTime = _getCurrentTime();

			//_mainScrollAnimationEnd(true);
			_calculatePanBounds( _currZoomLevel, true );
			
			// Start rendering
			_stopDragUpdateLoop();
			_dragUpdateLoop();
			
		}

		// init zoom
		if(!_isZooming && numPoints > 1 && !_mainScrollAnimating && !_mainScrollShifted) {
			_startZoomLevel = _currZoomLevel;
			_zoomStarted = false; // true if zoom changed at least once

			_isZooming = _isMultitouch = true;
			_currPanDist.y = _currPanDist.x = 0;

			_equalizePoints(_startPanOffset, _panOffset);

			_equalizePoints(p, startPointsList[0]);
			_equalizePoints(p2, startPointsList[1]);

			_findCenterOfPoints(p, p2, _currCenterPoint);

			_midZoomPoint.x = Math.abs(_currCenterPoint.x) - _panOffset.x;
			_midZoomPoint.y = Math.abs(_currCenterPoint.y) - _panOffset.y;
			_currPointsDistance = _startPointsDistance = _calculatePointsDistance(p, p2);
		}


	},

	// Pointermove/touchmove/mousemove handler
	_onDragMove = function(e) {

		e.preventDefault();

		if(_pointerEventEnabled) {
			var pointerIndex = framework.arraySearch(_currPointers, e.pointerId, 'id');
			if(pointerIndex > -1) {
				var p = _currPointers[pointerIndex];
				p.x = e.pageX;
				p.y = e.pageY; 
			}
		}

		if(_isDragging) {
			var touchesList = _getTouchPoints(e);
			if(!_direction && !_moved && !_isZooming) {

				if(_mainScrollPos.x !== _slideSize.x * _currPositionIndex) {
					// if main scroll position is shifted – direction is always horizontal
					_direction = 'h';
				} else {
					var diff = Math.abs(touchesList[0].x - _currPoint.x) - Math.abs(touchesList[0].y - _currPoint.y);
					// check the direction of movement
					if(Math.abs(diff) >= DIRECTION_CHECK_OFFSET) {
						_direction = diff > 0 ? 'h' : 'v';
						_currentPoints = touchesList;
					}
				}
				
			} else {
				_currentPoints = touchesList;
			}
		}	
	},
	// 
	_renderMovement =  function() {

		if(!_currentPoints) {
			return;
		}

		var numPoints = _currentPoints.length;

		if(numPoints === 0) {
			return;
		}

		_equalizePoints(p, _currentPoints[0]);

		delta.x = p.x - _currPoint.x;
		delta.y = p.y - _currPoint.y;

		if(_isZooming && numPoints > 1) {
			// Handle behaviour for more than 1 point

			_currPoint.x = p.x;
			_currPoint.y = p.y;
		
			// check if one of two points changed
			if( !delta.x && !delta.y && _isEqualPoints(_currentPoints[1], p2) ) {
				return;
			}

			_equalizePoints(p2, _currentPoints[1]);


			if(!_zoomStarted) {
				_zoomStarted = true;
				_shout('zoomGestureStarted');
			}
			
			// Distance between two points
			var pointsDistance = _calculatePointsDistance(p,p2);

			var zoomLevel = _calculateZoomLevel(pointsDistance);

			// slightly over the of initial zoom level
			if(zoomLevel > self.currItem.initialZoomLevel + self.currItem.initialZoomLevel / 15) {
				_wasOverInitialZoom = true;
			}

			// Apply the friction if zoom level is out of the bounds
			var zoomFriction = 1,
				minZoomLevel = _getMinZoomLevel(),
				maxZoomLevel = _getMaxZoomLevel();

			if ( zoomLevel < minZoomLevel ) {
				
				if(_options.pinchToClose && !_wasOverInitialZoom && _startZoomLevel <= self.currItem.initialZoomLevel) {
					// fade out background if zooming out
					var minusDiff = minZoomLevel - zoomLevel;
					var percent = 1 - minusDiff / (minZoomLevel / 1.2);

					_applyBgOpacity(percent);
					_shout('onPinchClose', percent);
					_opacityChanged = true;
				} else {
					zoomFriction = (minZoomLevel - zoomLevel) / minZoomLevel;
					if(zoomFriction > 1) {
						zoomFriction = 1;
					}
					zoomLevel = minZoomLevel - zoomFriction * (minZoomLevel / 3);
				}
				
			} else if ( zoomLevel > maxZoomLevel ) {
				// 1.5 - extra zoom level above the max. E.g. if max is x6, real max 6 + 1.5 = 7.5
				zoomFriction = (zoomLevel - maxZoomLevel) / ( minZoomLevel * 6 );
				if(zoomFriction > 1) {
					zoomFriction = 1;
				}
				zoomLevel = maxZoomLevel + zoomFriction * minZoomLevel;
			}

			if(zoomFriction < 0) {
				zoomFriction = 0;
			}

			// distance between touch points after friction is applied
			_currPointsDistance = pointsDistance;

			// _centerPoint - The point in the middle of two pointers
			_findCenterOfPoints(p, p2, _centerPoint);
		
			// paning with two pointers pressed
			_currPanDist.x += _centerPoint.x - _currCenterPoint.x;
			_currPanDist.y += _centerPoint.y - _currCenterPoint.y;
			_equalizePoints(_currCenterPoint, _centerPoint);

			_panOffset.x = _calculatePanOffset('x', zoomLevel);
			_panOffset.y = _calculatePanOffset('y', zoomLevel);

			_isZoomingIn = zoomLevel > _currZoomLevel;
			_currZoomLevel = zoomLevel;
			_applyCurrentZoomPan();

		} else {

			// handle behaviour for one point (dragging or panning)

			if(!_direction) {
				return;
			}

			if(_isFirstMove) {
				_isFirstMove = false;

				// subtract drag distance that was used during the detection direction  

				if( Math.abs(delta.x) >= DIRECTION_CHECK_OFFSET) {
					delta.x -= _currentPoints[0].x - _startPoint.x;
				}
				
				if( Math.abs(delta.y) >= DIRECTION_CHECK_OFFSET) {
					delta.y -= _currentPoints[0].y - _startPoint.y;
				}
			}

			_currPoint.x = p.x;
			_currPoint.y = p.y;

			// do nothing if pointers position hasn't changed
			if(delta.x === 0 && delta.y === 0) {
				return;
			}

			if(_direction === 'v' && _options.closeOnVerticalDrag) {
				if(!_canPan()) {
					_currPanDist.y += delta.y;
					_panOffset.y += delta.y;

					var opacityRatio = _calculateVerticalDragOpacityRatio();

					_verticalDragInitiated = true;
					_shout('onVerticalDrag', opacityRatio);

					_applyBgOpacity(opacityRatio);
					_applyCurrentZoomPan();
					return ;
				}
			}

			_pushPosPoint(_getCurrentTime(), p.x, p.y);

			_moved = true;
			_currPanBounds = self.currItem.bounds;
			
			var mainScrollChanged = _panOrMoveMainScroll('x', delta);
			if(!mainScrollChanged) {
				_panOrMoveMainScroll('y', delta);

				_roundPoint(_panOffset);
				_applyCurrentZoomPan();
			}

		}

	},
	
	// Pointerup/pointercancel/touchend/touchcancel/mouseup event handler
	_onDragRelease = function(e) {

		if(_features.isOldAndroid ) {

			if(_oldAndroidTouchEndTimeout && e.type === 'mouseup') {
				return;
			}

			// on Android (v4.1, 4.2, 4.3 & possibly older) 
			// ghost mousedown/up event isn't preventable via e.preventDefault,
			// which causes fake mousedown event
			// so we block mousedown/up for 600ms
			if( e.type.indexOf('touch') > -1 ) {
				clearTimeout(_oldAndroidTouchEndTimeout);
				_oldAndroidTouchEndTimeout = setTimeout(function() {
					_oldAndroidTouchEndTimeout = 0;
				}, 600);
			}
			
		}

		_shout('pointerUp');

		if(_preventDefaultEventBehaviour(e, false)) {
			e.preventDefault();
		}

		var releasePoint;

		if(_pointerEventEnabled) {
			var pointerIndex = framework.arraySearch(_currPointers, e.pointerId, 'id');
			
			if(pointerIndex > -1) {
				releasePoint = _currPointers.splice(pointerIndex, 1)[0];

				if(navigator.pointerEnabled) {
					releasePoint.type = e.pointerType || 'mouse';
				} else {
					var MSPOINTER_TYPES = {
						4: 'mouse', // event.MSPOINTER_TYPE_MOUSE
						2: 'touch', // event.MSPOINTER_TYPE_TOUCH 
						3: 'pen' // event.MSPOINTER_TYPE_PEN
					};
					releasePoint.type = MSPOINTER_TYPES[e.pointerType];

					if(!releasePoint.type) {
						releasePoint.type = e.pointerType || 'mouse';
					}
				}

			}
		}

		var touchList = _getTouchPoints(e),
			gestureType,
			numPoints = touchList.length;

		if(e.type === 'mouseup') {
			numPoints = 0;
		}

		// Do nothing if there were 3 touch points or more
		if(numPoints === 2) {
			_currentPoints = null;
			return true;
		}

		// if second pointer released
		if(numPoints === 1) {
			_equalizePoints(_startPoint, touchList[0]);
		}				


		// pointer hasn't moved, send "tap release" point
		if(numPoints === 0 && !_direction && !_mainScrollAnimating) {
			if(!releasePoint) {
				if(e.type === 'mouseup') {
					releasePoint = {x: e.pageX, y: e.pageY, type:'mouse'};
				} else if(e.changedTouches && e.changedTouches[0]) {
					releasePoint = {x: e.changedTouches[0].pageX, y: e.changedTouches[0].pageY, type:'touch'};
				}		
			}

			_shout('touchRelease', e, releasePoint);
		}

		// Difference in time between releasing of two last touch points (zoom gesture)
		var releaseTimeDiff = -1;

		// Gesture completed, no pointers left
		if(numPoints === 0) {
			_isDragging = false;
			framework.unbind(window, _upMoveEvents, self);

			_stopDragUpdateLoop();

			if(_isZooming) {
				// Two points released at the same time
				releaseTimeDiff = 0;
			} else if(_lastReleaseTime !== -1) {
				releaseTimeDiff = _getCurrentTime() - _lastReleaseTime;
			}
		}
		_lastReleaseTime = numPoints === 1 ? _getCurrentTime() : -1;
		
		if(releaseTimeDiff !== -1 && releaseTimeDiff < 150) {
			gestureType = 'zoom';
		} else {
			gestureType = 'swipe';
		}

		if(_isZooming && numPoints < 2) {
			_isZooming = false;

			// Only second point released
			if(numPoints === 1) {
				gestureType = 'zoomPointerUp';
			}
			_shout('zoomGestureEnded');
		}

		_currentPoints = null;
		if(!_moved && !_zoomStarted && !_mainScrollAnimating && !_verticalDragInitiated) {
			// nothing to animate
			return;
		}
	
		_stopAllAnimations();

		
		if(!_releaseAnimData) {
			_releaseAnimData = _initDragReleaseAnimationData();
		}
		
		_releaseAnimData.calculateSwipeSpeed('x');


		if(_verticalDragInitiated) {

			var opacityRatio = _calculateVerticalDragOpacityRatio();

			if(opacityRatio < _options.verticalDragRange) {
				self.close();
			} else {
				var initalPanY = _panOffset.y,
					initialBgOpacity = _bgOpacity;

				_animateProp('verticalDrag', 0, 1, 300, framework.easing.cubic.out, function(now) {
					
					_panOffset.y = (self.currItem.initialPosition.y - initalPanY) * now + initalPanY;

					_applyBgOpacity(  (1 - initialBgOpacity) * now + initialBgOpacity );
					_applyCurrentZoomPan();
				});

				_shout('onVerticalDrag', 1);
			}

			return;
		}


		// main scroll 
		if(  (_mainScrollShifted || _mainScrollAnimating) && numPoints === 0) {
			var itemChanged = _finishSwipeMainScrollGesture(gestureType, _releaseAnimData);
			if(itemChanged) {
				return;
			}
			gestureType = 'zoomPointerUp';
		}

		// prevent zoom/pan animation when main scroll animation runs
		if(_mainScrollAnimating) {
			return;
		}
		
		// Complete simple zoom gesture (reset zoom level if it's out of the bounds)  
		if(gestureType !== 'swipe') {
			_completeZoomGesture();
			return;
		}
	
		// Complete pan gesture if main scroll is not shifted, and it's possible to pan current image
		if(!_mainScrollShifted && _currZoomLevel > self.currItem.fitRatio) {
			_completePanGesture(_releaseAnimData);
		}
	},


	// Returns object with data about gesture
	// It's created only once and then reused
	_initDragReleaseAnimationData  = function() {
		// temp local vars
		var lastFlickDuration,
			tempReleasePos;

		// s = this
		var s = {
			lastFlickOffset: {},
			lastFlickDist: {},
			lastFlickSpeed: {},
			slowDownRatio:  {},
			slowDownRatioReverse:  {},
			speedDecelerationRatio:  {},
			speedDecelerationRatioAbs:  {},
			distanceOffset:  {},
			backAnimDestination: {},
			backAnimStarted: {},
			calculateSwipeSpeed: function(axis) {
				

				if( _posPoints.length > 1) {
					lastFlickDuration = _getCurrentTime() - _gestureCheckSpeedTime + 50;
					tempReleasePos = _posPoints[_posPoints.length-2][axis];
				} else {
					lastFlickDuration = _getCurrentTime() - _gestureStartTime; // total gesture duration
					tempReleasePos = _startPoint[axis];
				}
				s.lastFlickOffset[axis] = _currPoint[axis] - tempReleasePos;
				s.lastFlickDist[axis] = Math.abs(s.lastFlickOffset[axis]);
				if(s.lastFlickDist[axis] > 20) {
					s.lastFlickSpeed[axis] = s.lastFlickOffset[axis] / lastFlickDuration;
				} else {
					s.lastFlickSpeed[axis] = 0;
				}
				if( Math.abs(s.lastFlickSpeed[axis]) < 0.1 ) {
					s.lastFlickSpeed[axis] = 0;
				}
				
				s.slowDownRatio[axis] = 0.95;
				s.slowDownRatioReverse[axis] = 1 - s.slowDownRatio[axis];
				s.speedDecelerationRatio[axis] = 1;
			},

			calculateOverBoundsAnimOffset: function(axis, speed) {
				if(!s.backAnimStarted[axis]) {

					if(_panOffset[axis] > _currPanBounds.min[axis]) {
						s.backAnimDestination[axis] = _currPanBounds.min[axis];
						
					} else if(_panOffset[axis] < _currPanBounds.max[axis]) {
						s.backAnimDestination[axis] = _currPanBounds.max[axis];
					}

					if(s.backAnimDestination[axis] !== undefined) {
						s.slowDownRatio[axis] = 0.7;
						s.slowDownRatioReverse[axis] = 1 - s.slowDownRatio[axis];
						if(s.speedDecelerationRatioAbs[axis] < 0.05) {

							s.lastFlickSpeed[axis] = 0;
							s.backAnimStarted[axis] = true;

							_animateProp('bounceZoomPan'+axis,_panOffset[axis], 
								s.backAnimDestination[axis], 
								speed || 300, 
								framework.easing.sine.out, 
								function(pos) {
									_panOffset[axis] = pos;
									_applyCurrentZoomPan();
								}
							);

						}
					}
				}
			},

			// Reduces the speed by slowDownRatio (per 10ms)
			calculateAnimOffset: function(axis) {
				if(!s.backAnimStarted[axis]) {
					s.speedDecelerationRatio[axis] = s.speedDecelerationRatio[axis] * (s.slowDownRatio[axis] + 
												s.slowDownRatioReverse[axis] - 
												s.slowDownRatioReverse[axis] * s.timeDiff / 10);

					s.speedDecelerationRatioAbs[axis] = Math.abs(s.lastFlickSpeed[axis] * s.speedDecelerationRatio[axis]);
					s.distanceOffset[axis] = s.lastFlickSpeed[axis] * s.speedDecelerationRatio[axis] * s.timeDiff;
					_panOffset[axis] += s.distanceOffset[axis];

				}
			},

			panAnimLoop: function() {
				if ( _animations.zoomPan ) {
					_animations.zoomPan.raf = _requestAF(s.panAnimLoop);

					s.now = _getCurrentTime();
					s.timeDiff = s.now - s.lastNow;
					s.lastNow = s.now;
					
					s.calculateAnimOffset('x');
					s.calculateAnimOffset('y');

					_applyCurrentZoomPan();
					
					s.calculateOverBoundsAnimOffset('x');
					s.calculateOverBoundsAnimOffset('y');


					if (s.speedDecelerationRatioAbs.x < 0.05 && s.speedDecelerationRatioAbs.y < 0.05) {

						// round pan position
						_panOffset.x = Math.round(_panOffset.x);
						_panOffset.y = Math.round(_panOffset.y);
						_applyCurrentZoomPan();
						
						_stopAnimation('zoomPan');
						return;
					}
				}

			}
		};
		return s;
	},

	_completePanGesture = function(animData) {
		// calculate swipe speed for Y axis (paanning)
		animData.calculateSwipeSpeed('y');

		_currPanBounds = self.currItem.bounds;
		
		animData.backAnimDestination = {};
		animData.backAnimStarted = {};

		// Avoid acceleration animation if speed is too low
		if(Math.abs(animData.lastFlickSpeed.x) <= 0.05 && Math.abs(animData.lastFlickSpeed.y) <= 0.05 ) {
			animData.speedDecelerationRatioAbs.x = animData.speedDecelerationRatioAbs.y = 0;

			// Run pan drag release animation. E.g. if you drag image and release finger without momentum.
			animData.calculateOverBoundsAnimOffset('x');
			animData.calculateOverBoundsAnimOffset('y');
			return true;
		}

		// Animation loop that controls the acceleration after pan gesture ends
		_registerStartAnimation('zoomPan');
		animData.lastNow = _getCurrentTime();
		animData.panAnimLoop();
	},


	_finishSwipeMainScrollGesture = function(gestureType, _releaseAnimData) {
		var itemChanged;
		if(!_mainScrollAnimating) {
			_currZoomedItemIndex = _currentItemIndex;
		}


		
		var itemsDiff;

		if(gestureType === 'swipe') {
			var totalShiftDist = _currPoint.x - _startPoint.x,
				isFastLastFlick = _releaseAnimData.lastFlickDist.x < 10;

			// if container is shifted for more than MIN_SWIPE_DISTANCE, 
			// and last flick gesture was in right direction
			if(totalShiftDist > MIN_SWIPE_DISTANCE && 
				(isFastLastFlick || _releaseAnimData.lastFlickOffset.x > 20) ) {
				// go to prev item
				itemsDiff = -1;
			} else if(totalShiftDist < -MIN_SWIPE_DISTANCE && 
				(isFastLastFlick || _releaseAnimData.lastFlickOffset.x < -20) ) {
				// go to next item
				itemsDiff = 1;
			}
		}

		var nextCircle;

		if(itemsDiff) {
			
			_currentItemIndex += itemsDiff;

			if(_currentItemIndex < 0) {
				_currentItemIndex = _options.loop ? _getNumItems()-1 : 0;
				nextCircle = true;
			} else if(_currentItemIndex >= _getNumItems()) {
				_currentItemIndex = _options.loop ? 0 : _getNumItems()-1;
				nextCircle = true;
			}

			if(!nextCircle || _options.loop) {
				_indexDiff += itemsDiff;
				_currPositionIndex -= itemsDiff;
				itemChanged = true;
			}
			

			
		}

		var animateToX = _slideSize.x * _currPositionIndex;
		var animateToDist = Math.abs( animateToX - _mainScrollPos.x );
		var finishAnimDuration;


		if(!itemChanged && animateToX > _mainScrollPos.x !== _releaseAnimData.lastFlickSpeed.x > 0) {
			// "return to current" duration, e.g. when dragging from slide 0 to -1
			finishAnimDuration = 333; 
		} else {
			finishAnimDuration = Math.abs(_releaseAnimData.lastFlickSpeed.x) > 0 ? 
									animateToDist / Math.abs(_releaseAnimData.lastFlickSpeed.x) : 
									333;

			finishAnimDuration = Math.min(finishAnimDuration, 400);
			finishAnimDuration = Math.max(finishAnimDuration, 250);
		}

		if(_currZoomedItemIndex === _currentItemIndex) {
			itemChanged = false;
		}
		
		_mainScrollAnimating = true;
		
		_shout('mainScrollAnimStart');

		_animateProp('mainScroll', _mainScrollPos.x, animateToX, finishAnimDuration, framework.easing.cubic.out, 
			_moveMainScroll,
			function() {
				_stopAllAnimations();
				_mainScrollAnimating = false;
				_currZoomedItemIndex = -1;
				
				if(itemChanged || _currZoomedItemIndex !== _currentItemIndex) {
					self.updateCurrItem();
				}
				
				_shout('mainScrollAnimComplete');
			}
		);

		if(itemChanged) {
			self.updateCurrItem(true);
		}

		return itemChanged;
	},

	_calculateZoomLevel = function(touchesDistance) {
		return  1 / _startPointsDistance * touchesDistance * _startZoomLevel;
	},

	// Resets zoom if it's out of bounds
	_completeZoomGesture = function() {
		var destZoomLevel = _currZoomLevel,
			minZoomLevel = _getMinZoomLevel(),
			maxZoomLevel = _getMaxZoomLevel();

		if ( _currZoomLevel < minZoomLevel ) {
			destZoomLevel = minZoomLevel;
		} else if ( _currZoomLevel > maxZoomLevel ) {
			destZoomLevel = maxZoomLevel;
		}

		var destOpacity = 1,
			onUpdate,
			initialOpacity = _bgOpacity;

		if(_opacityChanged && !_isZoomingIn && !_wasOverInitialZoom && _currZoomLevel < minZoomLevel) {
			//_closedByScroll = true;
			self.close();
			return true;
		}

		if(_opacityChanged) {
			onUpdate = function(now) {
				_applyBgOpacity(  (destOpacity - initialOpacity) * now + initialOpacity );
			};
		}

		self.zoomTo(destZoomLevel, 0, 200,  framework.easing.cubic.out, onUpdate);
		return true;
	};


_registerModule('Gestures', {
	publicMethods: {

		initGestures: function() {

			// helper function that builds touch/pointer/mouse events
			var addEventNames = function(pref, down, move, up, cancel) {
				_dragStartEvent = pref + down;
				_dragMoveEvent = pref + move;
				_dragEndEvent = pref + up;
				if(cancel) {
					_dragCancelEvent = pref + cancel;
				} else {
					_dragCancelEvent = '';
				}
			};

			_pointerEventEnabled = _features.pointerEvent;
			if(_pointerEventEnabled && _features.touch) {
				// we don't need touch events, if browser supports pointer events
				_features.touch = false;
			}

			if(_pointerEventEnabled) {
				if(navigator.pointerEnabled) {
					addEventNames('pointer', 'down', 'move', 'up', 'cancel');
				} else {
					// IE10 pointer events are case-sensitive
					addEventNames('MSPointer', 'Down', 'Move', 'Up', 'Cancel');
				}
			} else if(_features.touch) {
				addEventNames('touch', 'start', 'move', 'end', 'cancel');
				_likelyTouchDevice = true;
			} else {
				addEventNames('mouse', 'down', 'move', 'up');	
			}

			_upMoveEvents = _dragMoveEvent + ' ' + _dragEndEvent  + ' ' +  _dragCancelEvent;
			_downEvents = _dragStartEvent;

			if(_pointerEventEnabled && !_likelyTouchDevice) {
				_likelyTouchDevice = (navigator.maxTouchPoints > 1) || (navigator.msMaxTouchPoints > 1);
			}
			// make variable public
			self.likelyTouchDevice = _likelyTouchDevice; 
			
			_globalEventHandlers[_dragStartEvent] = _onDragStart;
			_globalEventHandlers[_dragMoveEvent] = _onDragMove;
			_globalEventHandlers[_dragEndEvent] = _onDragRelease; // the Kraken

			if(_dragCancelEvent) {
				_globalEventHandlers[_dragCancelEvent] = _globalEventHandlers[_dragEndEvent];
			}

			// Bind mouse events on device with detected hardware touch support, in case it supports multiple types of input.
			if(_features.touch) {
				_downEvents += ' mousedown';
				_upMoveEvents += ' mousemove mouseup';
				_globalEventHandlers.mousedown = _globalEventHandlers[_dragStartEvent];
				_globalEventHandlers.mousemove = _globalEventHandlers[_dragMoveEvent];
				_globalEventHandlers.mouseup = _globalEventHandlers[_dragEndEvent];
			}

			if(!_likelyTouchDevice) {
				// don't allow pan to next slide from zoomed state on Desktop
				_options.allowPanToNext = false;
			}
		}

	}
});


/*>>gestures*/

/*>>show-hide-transition*/
/**
 * show-hide-transition.js:
 *
 * Manages initial opening or closing transition.
 *
 * If you're not planning to use transition for gallery at all,
 * you may set options hideAnimationDuration and showAnimationDuration to 0,
 * and just delete startAnimation function.
 * 
 */


var _showOrHideTimeout,
	_showOrHide = function(item, img, out, completeFn) {

		if(_showOrHideTimeout) {
			clearTimeout(_showOrHideTimeout);
		}

		_initialZoomRunning = true;
		_initialContentSet = true;
		
		// dimensions of small thumbnail {x:,y:,w:}.
		// Height is optional, as calculated based on large image.
		var thumbBounds; 
		if(item.initialLayout) {
			thumbBounds = item.initialLayout;
			item.initialLayout = null;
		} else {
			thumbBounds = _options.getThumbBoundsFn && _options.getThumbBoundsFn(_currentItemIndex);
		}

		var duration = out ? _options.hideAnimationDuration : _options.showAnimationDuration;

		var onComplete = function() {
			_stopAnimation('initialZoom');
			if(!out) {
				_applyBgOpacity(1);
				if(img) {
					img.style.display = 'block';
				}
				framework.addClass(template, 'pswp--animated-in');
				_shout('initialZoom' + (out ? 'OutEnd' : 'InEnd'));
			} else {
				self.template.removeAttribute('style');
				self.bg.removeAttribute('style');
			}

			if(completeFn) {
				completeFn();
			}
			_initialZoomRunning = false;
		};

		// if bounds aren't provided, just open gallery without animation
		if(!duration || !thumbBounds || thumbBounds.x === undefined) {

			_shout('initialZoom' + (out ? 'Out' : 'In') );

			_currZoomLevel = item.initialZoomLevel;
			_equalizePoints(_panOffset,  item.initialPosition );
			_applyCurrentZoomPan();

			template.style.opacity = out ? 0 : 1;
			_applyBgOpacity(1);

			if(duration) {
				setTimeout(function() {
					onComplete();
				}, duration);
			} else {
				onComplete();
			}

			return;
		}

		var startAnimation = function() {
			var closeWithRaf = _closedByScroll,
				fadeEverything = !self.currItem.src || self.currItem.loadError || _options.showHideOpacity;
			
			// apply hw-acceleration to image
			if(item.miniImg) {
				item.miniImg.style.webkitBackfaceVisibility = 'hidden';
			}

			if(!out) {
				_currZoomLevel = thumbBounds.w / item.w;
				_panOffset.x = thumbBounds.x;
				_panOffset.y = thumbBounds.y - _initalWindowScrollY;

				self[fadeEverything ? 'template' : 'bg'].style.opacity = 0.001;
				_applyCurrentZoomPan();
			}

			_registerStartAnimation('initialZoom');
			
			if(out && !closeWithRaf) {
				framework.removeClass(template, 'pswp--animated-in');
			}

			if(fadeEverything) {
				if(out) {
					framework[ (closeWithRaf ? 'remove' : 'add') + 'Class' ](template, 'pswp--animate_opacity');
				} else {
					setTimeout(function() {
						framework.addClass(template, 'pswp--animate_opacity');
					}, 30);
				}
			}

			_showOrHideTimeout = setTimeout(function() {

				_shout('initialZoom' + (out ? 'Out' : 'In') );
				

				if(!out) {

					// "in" animation always uses CSS transitions (instead of rAF).
					// CSS transition work faster here, 
					// as developer may also want to animate other things, 
					// like ui on top of sliding area, which can be animated just via CSS
					
					_currZoomLevel = item.initialZoomLevel;
					_equalizePoints(_panOffset,  item.initialPosition );
					_applyCurrentZoomPan();
					_applyBgOpacity(1);

					if(fadeEverything) {
						template.style.opacity = 1;
					} else {
						_applyBgOpacity(1);
					}

					_showOrHideTimeout = setTimeout(onComplete, duration + 20);
				} else {

					// "out" animation uses rAF only when PhotoSwipe is closed by browser scroll, to recalculate position
					var destZoomLevel = thumbBounds.w / item.w,
						initialPanOffset = {
							x: _panOffset.x,
							y: _panOffset.y
						},
						initialZoomLevel = _currZoomLevel,
						initalBgOpacity = _bgOpacity,
						onUpdate = function(now) {
							
							if(now === 1) {
								_currZoomLevel = destZoomLevel;
								_panOffset.x = thumbBounds.x;
								_panOffset.y = thumbBounds.y  - _currentWindowScrollY;
							} else {
								_currZoomLevel = (destZoomLevel - initialZoomLevel) * now + initialZoomLevel;
								_panOffset.x = (thumbBounds.x - initialPanOffset.x) * now + initialPanOffset.x;
								_panOffset.y = (thumbBounds.y - _currentWindowScrollY - initialPanOffset.y) * now + initialPanOffset.y;
							}
							
							_applyCurrentZoomPan();
							if(fadeEverything) {
								template.style.opacity = 1 - now;
							} else {
								_applyBgOpacity( initalBgOpacity - now * initalBgOpacity );
							}
						};

					if(closeWithRaf) {
						_animateProp('initialZoom', 0, 1, duration, framework.easing.cubic.out, onUpdate, onComplete);
					} else {
						onUpdate(1);
						_showOrHideTimeout = setTimeout(onComplete, duration + 20);
					}
				}
			
			}, out ? 25 : 90); // Main purpose of this delay is to give browser time to paint and
					// create composite layers of PhotoSwipe UI parts (background, controls, caption, arrows).
					// Which avoids lag at the beginning of scale transition.
		};
		startAnimation();

		
	};

/*>>show-hide-transition*/

/*>>items-controller*/
/**
*
* Controller manages gallery items, their dimensions, and their content.
* 
*/

var _items,
	_tempPanAreaSize = {},
	_imagesToAppendPool = [],
	_initialContentSet,
	_initialZoomRunning,
	_controllerDefaultOptions = {
		index: 0,
		errorMsg: '<div class="pswp__error-msg"><a href="%url%" target="_blank">The image</a> could not be loaded.</div>',
		forceProgressiveLoading: false, // TODO
		preload: [1,1],
		getNumItemsFn: function() {
			return _items.length;
		}
	};


var _getItemAt,
	_getNumItems,
	_initialIsLoop,
	_getZeroBounds = function() {
		return {
			center:{x:0,y:0}, 
			max:{x:0,y:0}, 
			min:{x:0,y:0}
		};
	},
	_calculateSingleItemPanBounds = function(item, realPanElementW, realPanElementH ) {
		var bounds = item.bounds;

		// position of element when it's centered
		bounds.center.x = Math.round((_tempPanAreaSize.x - realPanElementW) / 2);
		bounds.center.y = Math.round((_tempPanAreaSize.y - realPanElementH) / 2) + item.vGap.top;

		// maximum pan position
		bounds.max.x = (realPanElementW > _tempPanAreaSize.x) ? 
							Math.round(_tempPanAreaSize.x - realPanElementW) : 
							bounds.center.x;
		
		bounds.max.y = (realPanElementH > _tempPanAreaSize.y) ? 
							Math.round(_tempPanAreaSize.y - realPanElementH) + item.vGap.top : 
							bounds.center.y;
		
		// minimum pan position
		bounds.min.x = (realPanElementW > _tempPanAreaSize.x) ? 0 : bounds.center.x;
		bounds.min.y = (realPanElementH > _tempPanAreaSize.y) ? item.vGap.top : bounds.center.y;
	},
	_calculateItemSize = function(item, viewportSize, zoomLevel) {

		if (item.src && !item.loadError) {
			var isInitial = !zoomLevel;
			
			if(isInitial) {
				if(!item.vGap) {
					item.vGap = {top:0,bottom:0};
				}
				// allows overriding vertical margin for individual items
				_shout('parseVerticalMargin', item);
			}


			_tempPanAreaSize.x = viewportSize.x;
			_tempPanAreaSize.y = viewportSize.y - item.vGap.top - item.vGap.bottom;

			if (isInitial) {
				var hRatio = _tempPanAreaSize.x / item.w;
				var vRatio = _tempPanAreaSize.y / item.h;

				item.fitRatio = hRatio < vRatio ? hRatio : vRatio;
				//item.fillRatio = hRatio > vRatio ? hRatio : vRatio;

				var scaleMode = _options.scaleMode;

				if (scaleMode === 'orig') {
					zoomLevel = 1;
				} else if (scaleMode === 'fit') {
					zoomLevel = item.fitRatio;
				}

				if (zoomLevel > 1) {
					zoomLevel = 1;
				}

				item.initialZoomLevel = zoomLevel;
				
				if(!item.bounds) {
					// reuse bounds object
					item.bounds = _getZeroBounds(); 
				}
			}

			if(!zoomLevel) {
				return;
			}

			_calculateSingleItemPanBounds(item, item.w * zoomLevel, item.h * zoomLevel);

			if (isInitial && zoomLevel === item.initialZoomLevel) {
				item.initialPosition = item.bounds.center;
			}

			return item.bounds;
		} else {
			item.w = item.h = 0;
			item.initialZoomLevel = item.fitRatio = 1;
			item.bounds = _getZeroBounds();
			item.initialPosition = item.bounds.center;

			// if it's not image, we return zero bounds (content is not zoomable)
			return item.bounds;
		}
		
	},

	


	_appendImage = function(index, item, baseDiv, img, preventAnimation, keepPlaceholder) {
		

		if(item.loadError) {
			return;
		}

		if(img) {

			item.imageAppended = true;
			_setImageSize(item, img, (item === self.currItem && _renderMaxResolution) );
			
			baseDiv.appendChild(img);

			if(keepPlaceholder) {
				setTimeout(function() {
					if(item && item.loaded && item.placeholder) {
						item.placeholder.style.display = 'none';
						item.placeholder = null;
					}
				}, 500);
			}
		}
	},
	


	_preloadImage = function(item) {
		item.loading = true;
		item.loaded = false;
		var img = item.img = framework.createEl('pswp__img', 'img');
		var onComplete = function() {
			item.loading = false;
			item.loaded = true;

			if(item.loadComplete) {
				item.loadComplete(item);
			} else {
				item.img = null; // no need to store image object
			}
			img.onload = img.onerror = null;
			img = null;
		};
		img.onload = onComplete;
		img.onerror = function() {
			item.loadError = true;
			onComplete();
		};		

		img.src = item.src;// + '?a=' + Math.random();

		return img;
	},
	_checkForError = function(item, cleanUp) {
		if(item.src && item.loadError && item.container) {

			if(cleanUp) {
				item.container.innerHTML = '';
			}

			item.container.innerHTML = _options.errorMsg.replace('%url%',  item.src );
			return true;
			
		}
	},
	_setImageSize = function(item, img, maxRes) {
		if(!item.src) {
			return;
		}

		if(!img) {
			img = item.container.lastChild;
		}

		var w = maxRes ? item.w : Math.round(item.w * item.fitRatio),
			h = maxRes ? item.h : Math.round(item.h * item.fitRatio);
		
		if(item.placeholder && !item.loaded) {
			item.placeholder.style.width = w + 'px';
			item.placeholder.style.height = h + 'px';
		}

		img.style.width = w + 'px';
		img.style.height = h + 'px';
	},
	_appendImagesPool = function() {

		if(_imagesToAppendPool.length) {
			var poolItem;

			for(var i = 0; i < _imagesToAppendPool.length; i++) {
				poolItem = _imagesToAppendPool[i];
				if( poolItem.holder.index === poolItem.index ) {
					_appendImage(poolItem.index, poolItem.item, poolItem.baseDiv, poolItem.img, false, poolItem.clearPlaceholder);
				}
			}
			_imagesToAppendPool = [];
		}
	};
	


_registerModule('Controller', {

	publicMethods: {

		lazyLoadItem: function(index) {
			index = _getLoopedId(index);
			var item = _getItemAt(index);

			if(!item || ((item.loaded || item.loading) && !_itemsNeedUpdate)) {
				return;
			}

			_shout('gettingData', index, item);

			if (!item.src) {
				return;
			}

			_preloadImage(item);
		},
		initController: function() {
			framework.extend(_options, _controllerDefaultOptions, true);
			self.items = _items = items;
			_getItemAt = self.getItemAt;
			_getNumItems = _options.getNumItemsFn; //self.getNumItems;



			_initialIsLoop = _options.loop;
			if(_getNumItems() < 3) {
				_options.loop = false; // disable loop if less then 3 items
			}

			_listen('beforeChange', function(diff) {

				var p = _options.preload,
					isNext = diff === null ? true : (diff >= 0),
					preloadBefore = Math.min(p[0], _getNumItems() ),
					preloadAfter = Math.min(p[1], _getNumItems() ),
					i;


				for(i = 1; i <= (isNext ? preloadAfter : preloadBefore); i++) {
					self.lazyLoadItem(_currentItemIndex+i);
				}
				for(i = 1; i <= (isNext ? preloadBefore : preloadAfter); i++) {
					self.lazyLoadItem(_currentItemIndex-i);
				}
			});

			_listen('initialLayout', function() {
				self.currItem.initialLayout = _options.getThumbBoundsFn && _options.getThumbBoundsFn(_currentItemIndex);
			});

			_listen('mainScrollAnimComplete', _appendImagesPool);
			_listen('initialZoomInEnd', _appendImagesPool);



			_listen('destroy', function() {
				var item;
				for(var i = 0; i < _items.length; i++) {
					item = _items[i];
					// remove reference to DOM elements, for GC
					if(item.container) {
						item.container = null; 
					}
					if(item.placeholder) {
						item.placeholder = null;
					}
					if(item.img) {
						item.img = null;
					}
					if(item.preloader) {
						item.preloader = null;
					}
					if(item.loadError) {
						item.loaded = item.loadError = false;
					}
				}
				_imagesToAppendPool = null;
			});
		},


		getItemAt: function(index) {
			if (index >= 0) {
				return _items[index] !== undefined ? _items[index] : false;
			}
			return false;
		},

		allowProgressiveImg: function() {
			// 1. Progressive image loading isn't working on webkit/blink 
			//    when hw-acceleration (e.g. translateZ) is applied to IMG element.
			//    That's why in PhotoSwipe parent element gets zoom transform, not image itself.
			//    
			// 2. Progressive image loading sometimes blinks in webkit/blink when applying animation to parent element.
			//    That's why it's disabled on touch devices (mainly because of swipe transition)
			//    
			// 3. Progressive image loading sometimes doesn't work in IE (up to 11).

			// Don't allow progressive loading on non-large touch devices
			return _options.forceProgressiveLoading || !_likelyTouchDevice || _options.mouseUsed || screen.width > 1200; 
			// 1200 - to eliminate touch devices with large screen (like Chromebook Pixel)
		},

		setContent: function(holder, index) {

			if(_options.loop) {
				index = _getLoopedId(index);
			}

			var prevItem = self.getItemAt(holder.index);
			if(prevItem) {
				prevItem.container = null;
			}
	
			var item = self.getItemAt(index),
				img;
			
			if(!item) {
				holder.el.innerHTML = '';
				return;
			}

			// allow to override data
			_shout('gettingData', index, item);

			holder.index = index;
			holder.item = item;

			// base container DIV is created only once for each of 3 holders
			var baseDiv = item.container = framework.createEl('pswp__zoom-wrap'); 

			

			if(!item.src && item.html) {
				if(item.html.tagName) {
					baseDiv.appendChild(item.html);
				} else {
					baseDiv.innerHTML = item.html;
				}
			}

			_checkForError(item);

			_calculateItemSize(item, _viewportSize);
			
			if(item.src && !item.loadError && !item.loaded) {

				item.loadComplete = function(item) {

					// gallery closed before image finished loading
					if(!_isOpen) {
						return;
					}

					// check if holder hasn't changed while image was loading
					if(holder && holder.index === index ) {
						if( _checkForError(item, true) ) {
							item.loadComplete = item.img = null;
							_calculateItemSize(item, _viewportSize);
							_applyZoomPanToItem(item);

							if(holder.index === _currentItemIndex) {
								// recalculate dimensions
								self.updateCurrZoomItem();
							}
							return;
						}
						if( !item.imageAppended ) {
							if(_features.transform && (_mainScrollAnimating || _initialZoomRunning) ) {
								_imagesToAppendPool.push({
									item:item,
									baseDiv:baseDiv,
									img:item.img,
									index:index,
									holder:holder,
									clearPlaceholder:true
								});
							} else {
								_appendImage(index, item, baseDiv, item.img, _mainScrollAnimating || _initialZoomRunning, true);
							}
						} else {
							// remove preloader & mini-img
							if(!_initialZoomRunning && item.placeholder) {
								item.placeholder.style.display = 'none';
								item.placeholder = null;
							}
						}
					}

					item.loadComplete = null;
					item.img = null; // no need to store image element after it's added

					_shout('imageLoadComplete', index, item);
				};

				if(framework.features.transform) {
					
					var placeholderClassName = 'pswp__img pswp__img--placeholder'; 
					placeholderClassName += (item.msrc ? '' : ' pswp__img--placeholder--blank');

					var placeholder = framework.createEl(placeholderClassName, item.msrc ? 'img' : '');
					if(item.msrc) {
						placeholder.src = item.msrc;
					}
					
					_setImageSize(item, placeholder);

					baseDiv.appendChild(placeholder);
					item.placeholder = placeholder;

				}
				

				

				if(!item.loading) {
					_preloadImage(item);
				}


				if( self.allowProgressiveImg() ) {
					// just append image
					if(!_initialContentSet && _features.transform) {
						_imagesToAppendPool.push({
							item:item, 
							baseDiv:baseDiv, 
							img:item.img, 
							index:index, 
							holder:holder
						});
					} else {
						_appendImage(index, item, baseDiv, item.img, true, true);
					}
				}
				
			} else if(item.src && !item.loadError) {
				// image object is created every time, due to bugs of image loading & delay when switching images
				img = framework.createEl('pswp__img', 'img');
				img.style.opacity = 1;
				img.src = item.src;
				_setImageSize(item, img);
				_appendImage(index, item, baseDiv, img, true);
			}
			

			if(!_initialContentSet && index === _currentItemIndex) {
				_currZoomElementStyle = baseDiv.style;
				_showOrHide(item, (img ||item.img) );
			} else {
				_applyZoomPanToItem(item);
			}

			holder.el.innerHTML = '';
			holder.el.appendChild(baseDiv);
		},

		cleanSlide: function( item ) {
			if(item.img ) {
				item.img.onload = item.img.onerror = null;
			}
			item.loaded = item.loading = item.img = item.imageAppended = false;
		}

	}
});

/*>>items-controller*/

/*>>tap*/
/**
 * tap.js:
 *
 * Displatches tap and double-tap events.
 * 
 */

var tapTimer,
	tapReleasePoint = {},
	_dispatchTapEvent = function(origEvent, releasePoint, pointerType) {		
		var e = document.createEvent( 'CustomEvent' ),
			eDetail = {
				origEvent:origEvent, 
				target:origEvent.target, 
				releasePoint: releasePoint, 
				pointerType:pointerType || 'touch'
			};

		e.initCustomEvent( 'pswpTap', true, true, eDetail );
		origEvent.target.dispatchEvent(e);
	};

_registerModule('Tap', {
	publicMethods: {
		initTap: function() {
			_listen('firstTouchStart', self.onTapStart);
			_listen('touchRelease', self.onTapRelease);
			_listen('destroy', function() {
				tapReleasePoint = {};
				tapTimer = null;
			});
		},
		onTapStart: function(touchList) {
			if(touchList.length > 1) {
				clearTimeout(tapTimer);
				tapTimer = null;
			}
		},
		onTapRelease: function(e, releasePoint) {
			if(!releasePoint) {
				return;
			}

			if(!_moved && !_isMultitouch && !_numAnimations) {
				var p0 = releasePoint;
				if(tapTimer) {
					clearTimeout(tapTimer);
					tapTimer = null;

					// Check if taped on the same place
					if ( _isNearbyPoints(p0, tapReleasePoint) ) {
						_shout('doubleTap', p0);
						return;
					}
				}

				if(releasePoint.type === 'mouse') {
					_dispatchTapEvent(e, releasePoint, 'mouse');
					return;
				}

				var clickedTagName = e.target.tagName.toUpperCase();
				// avoid double tap delay on buttons and elements that have class pswp__single-tap
				if(clickedTagName === 'BUTTON' || framework.hasClass(e.target, 'pswp__single-tap') ) {
					_dispatchTapEvent(e, releasePoint);
					return;
				}

				_equalizePoints(tapReleasePoint, p0);

				tapTimer = setTimeout(function() {
					_dispatchTapEvent(e, releasePoint);
					tapTimer = null;
				}, 300);
			}
		}
	}
});

/*>>tap*/

/*>>desktop-zoom*/
/**
 *
 * desktop-zoom.js:
 *
 * - Binds mousewheel event for paning zoomed image.
 * - Manages "dragging", "zoomed-in", "zoom-out" classes.
 *   (which are used for cursors and zoom icon)
 * - Adds toggleDesktopZoom function.
 * 
 */

var _wheelDelta;
	
_registerModule('DesktopZoom', {

	publicMethods: {

		initDesktopZoom: function() {

			if(_oldIE) {
				// no zoom for old IE (<=8)
				return;
			}

			if(_likelyTouchDevice) {
				// if detected hardware touch support, we wait until mouse is used,
				// and only then apply desktop-zoom features
				_listen('mouseUsed', function() {
					self.setupDesktopZoom();
				});
			} else {
				self.setupDesktopZoom(true);
			}

		},

		setupDesktopZoom: function(onInit) {

			_wheelDelta = {};

			var events = 'wheel mousewheel DOMMouseScroll';
			
			_listen('bindEvents', function() {
				framework.bind(template, events,  self.handleMouseWheel);
			});

			_listen('unbindEvents', function() {
				if(_wheelDelta) {
					framework.unbind(template, events, self.handleMouseWheel);
				}
			});

			self.mouseZoomedIn = false;

			var hasDraggingClass,
				updateZoomable = function() {
					if(self.mouseZoomedIn) {
						framework.removeClass(template, 'pswp--zoomed-in');
						self.mouseZoomedIn = false;
					}
					if(_currZoomLevel < 1) {
						framework.addClass(template, 'pswp--zoom-allowed');
					} else {
						framework.removeClass(template, 'pswp--zoom-allowed');
					}
					removeDraggingClass();
				},
				removeDraggingClass = function() {
					if(hasDraggingClass) {
						framework.removeClass(template, 'pswp--dragging');
						hasDraggingClass = false;
					}
				};

			_listen('resize' , updateZoomable);
			_listen('afterChange' , updateZoomable);
			_listen('pointerDown', function() {
				if(self.mouseZoomedIn) {
					hasDraggingClass = true;
					framework.addClass(template, 'pswp--dragging');
				}
			});
			_listen('pointerUp', removeDraggingClass);

			if(!onInit) {
				updateZoomable();
			}
			
		},

		handleMouseWheel: function(e) {

			if(_currZoomLevel <= self.currItem.fitRatio) {
				if( _options.modal ) {

					if (!_options.closeOnScroll || _numAnimations || _isDragging) {
						e.preventDefault();
					} else if(_transformKey && Math.abs(e.deltaY) > 2) {
						// close PhotoSwipe
						// if browser supports transforms & scroll changed enough
						_closedByScroll = true;
						self.close();
					}

				}
				return true;
			}

			// allow just one event to fire
			e.stopPropagation();

			// https://developer.mozilla.org/en-US/docs/Web/Events/wheel
			_wheelDelta.x = 0;

			if('deltaX' in e) {
				if(e.deltaMode === 1 /* DOM_DELTA_LINE */) {
					// 18 - average line height
					_wheelDelta.x = e.deltaX * 18;
					_wheelDelta.y = e.deltaY * 18;
				} else {
					_wheelDelta.x = e.deltaX;
					_wheelDelta.y = e.deltaY;
				}
			} else if('wheelDelta' in e) {
				if(e.wheelDeltaX) {
					_wheelDelta.x = -0.16 * e.wheelDeltaX;
				}
				if(e.wheelDeltaY) {
					_wheelDelta.y = -0.16 * e.wheelDeltaY;
				} else {
					_wheelDelta.y = -0.16 * e.wheelDelta;
				}
			} else if('detail' in e) {
				_wheelDelta.y = e.detail;
			} else {
				return;
			}

			_calculatePanBounds(_currZoomLevel, true);

			var newPanX = _panOffset.x - _wheelDelta.x,
				newPanY = _panOffset.y - _wheelDelta.y;

			// only prevent scrolling in nonmodal mode when not at edges
			if (_options.modal ||
				(
				newPanX <= _currPanBounds.min.x && newPanX >= _currPanBounds.max.x &&
				newPanY <= _currPanBounds.min.y && newPanY >= _currPanBounds.max.y
				) ) {
				e.preventDefault();
			}

			// TODO: use rAF instead of mousewheel?
			self.panTo(newPanX, newPanY);
		},

		toggleDesktopZoom: function(centerPoint) {
			centerPoint = centerPoint || {x:_viewportSize.x/2 + _offset.x, y:_viewportSize.y/2 + _offset.y };

			var doubleTapZoomLevel = _options.getDoubleTapZoom(true, self.currItem);
			var zoomOut = _currZoomLevel === doubleTapZoomLevel;
			
			self.mouseZoomedIn = !zoomOut;

			self.zoomTo(zoomOut ? self.currItem.initialZoomLevel : doubleTapZoomLevel, centerPoint, 333);
			framework[ (!zoomOut ? 'add' : 'remove') + 'Class'](template, 'pswp--zoomed-in');
		}

	}
});


/*>>desktop-zoom*/

/*>>history*/
/**
 *
 * history.js:
 *
 * - Back button to close gallery.
 * 
 * - Unique URL for each slide: example.com/&pid=1&gid=3
 *   (where PID is picture index, and GID and gallery index)
 *   
 * - Switch URL when slides change.
 * 
 */


var _historyDefaultOptions = {
	history: true,
	galleryUID: 1
};

var _historyUpdateTimeout,
	_hashChangeTimeout,
	_hashAnimCheckTimeout,
	_hashChangedByScript,
	_hashChangedByHistory,
	_hashReseted,
	_initialHash,
	_historyChanged,
	_closedFromURL,
	_urlChangedOnce,
	_windowLoc,

	_supportsPushState,

	_getHash = function() {
		return _windowLoc.hash.substring(1);
	},
	_cleanHistoryTimeouts = function() {

		if(_historyUpdateTimeout) {
			clearTimeout(_historyUpdateTimeout);
		}

		if(_hashAnimCheckTimeout) {
			clearTimeout(_hashAnimCheckTimeout);
		}
	},

	// pid - Picture index
	// gid - Gallery index
	_parseItemIndexFromURL = function() {
		var hash = _getHash(),
			params = {};

		if(hash.length < 5) { // pid=1
			return params;
		}

		var i, vars = hash.split('&');
		for (i = 0; i < vars.length; i++) {
			if(!vars[i]) {
				continue;
			}
			var pair = vars[i].split('=');	
			if(pair.length < 2) {
				continue;
			}
			params[pair[0]] = pair[1];
		}
		if(_options.galleryPIDs) {
			// detect custom pid in hash and search for it among the items collection
			var searchfor = params.pid;
			params.pid = 0; // if custom pid cannot be found, fallback to the first item
			for(i = 0; i < _items.length; i++) {
				if(_items[i].pid === searchfor) {
					params.pid = i;
					break;
				}
			}
		} else {
			params.pid = parseInt(params.pid,10)-1;
		}
		if( params.pid < 0 ) {
			params.pid = 0;
		}
		return params;
	},
	_updateHash = function() {

		if(_hashAnimCheckTimeout) {
			clearTimeout(_hashAnimCheckTimeout);
		}


		if(_numAnimations || _isDragging) {
			// changing browser URL forces layout/paint in some browsers, which causes noticable lag during animation
			// that's why we update hash only when no animations running
			_hashAnimCheckTimeout = setTimeout(_updateHash, 500);
			return;
		}
		
		if(_hashChangedByScript) {
			clearTimeout(_hashChangeTimeout);
		} else {
			_hashChangedByScript = true;
		}


		var pid = (_currentItemIndex + 1);
		var item = _getItemAt( _currentItemIndex );
		if(item.hasOwnProperty('pid')) {
			// carry forward any custom pid assigned to the item
			pid = item.pid;
		}
		var newHash = _initialHash + '&'  +  'gid=' + _options.galleryUID + '&' + 'pid=' + pid;

		if(!_historyChanged) {
			if(_windowLoc.hash.indexOf(newHash) === -1) {
				_urlChangedOnce = true;
			}
			// first time - add new hisory record, then just replace
		}

		var newURL = _windowLoc.href.split('#')[0] + '#' +  newHash;

		if( _supportsPushState ) {

			if('#' + newHash !== window.location.hash) {
				history[_historyChanged ? 'replaceState' : 'pushState']('', document.title, newURL);
			}

		} else {
			if(_historyChanged) {
				_windowLoc.replace( newURL );
			} else {
				_windowLoc.hash = newHash;
			}
		}
		
		

		_historyChanged = true;
		_hashChangeTimeout = setTimeout(function() {
			_hashChangedByScript = false;
		}, 60);
	};



	

_registerModule('History', {

	

	publicMethods: {
		initHistory: function() {

			framework.extend(_options, _historyDefaultOptions, true);

			if( !_options.history ) {
				return;
			}


			_windowLoc = window.location;
			_urlChangedOnce = false;
			_closedFromURL = false;
			_historyChanged = false;
			_initialHash = _getHash();
			_supportsPushState = ('pushState' in history);


			if(_initialHash.indexOf('gid=') > -1) {
				_initialHash = _initialHash.split('&gid=')[0];
				_initialHash = _initialHash.split('?gid=')[0];
			}
			

			_listen('afterChange', self.updateURL);
			_listen('unbindEvents', function() {
				framework.unbind(window, 'hashchange', self.onHashChange);
			});


			var returnToOriginal = function() {
				_hashReseted = true;
				if(!_closedFromURL) {

					if(_urlChangedOnce) {
						history.back();
					} else {

						if(_initialHash) {
							_windowLoc.hash = _initialHash;
						} else {
							if (_supportsPushState) {

								// remove hash from url without refreshing it or scrolling to top
								history.pushState('', document.title,  _windowLoc.pathname + _windowLoc.search );
							} else {
								_windowLoc.hash = '';
							}
						}
					}
					
				}

				_cleanHistoryTimeouts();
			};


			_listen('unbindEvents', function() {
				if(_closedByScroll) {
					// if PhotoSwipe is closed by scroll, we go "back" before the closing animation starts
					// this is done to keep the scroll position
					returnToOriginal();
				}
			});
			_listen('destroy', function() {
				if(!_hashReseted) {
					returnToOriginal();
				}
			});
			_listen('firstUpdate', function() {
				_currentItemIndex = _parseItemIndexFromURL().pid;
			});

			

			
			var index = _initialHash.indexOf('pid=');
			if(index > -1) {
				_initialHash = _initialHash.substring(0, index);
				if(_initialHash.slice(-1) === '&') {
					_initialHash = _initialHash.slice(0, -1);
				}
			}
			

			setTimeout(function() {
				if(_isOpen) { // hasn't destroyed yet
					framework.bind(window, 'hashchange', self.onHashChange);
				}
			}, 40);
			
		},
		onHashChange: function() {

			if(_getHash() === _initialHash) {

				_closedFromURL = true;
				self.close();
				return;
			}
			if(!_hashChangedByScript) {

				_hashChangedByHistory = true;
				self.goTo( _parseItemIndexFromURL().pid );
				_hashChangedByHistory = false;
			}
			
		},
		updateURL: function() {

			// Delay the update of URL, to avoid lag during transition, 
			// and to not to trigger actions like "refresh page sound" or "blinking favicon" to often
			
			_cleanHistoryTimeouts();
			

			if(_hashChangedByHistory) {
				return;
			}

			if(!_historyChanged) {
				_updateHash(); // first time
			} else {
				_historyUpdateTimeout = setTimeout(_updateHash, 800);
			}
		}
	
	}
});


/*>>history*/
	framework.extend(self, publicMethods); };
	return PhotoSwipe;
});
/*! PhotoSwipe Default UI - 4.1.2 - 2017-04-05
* http://photoswipe.com
* Copyright (c) 2017 Dmitry Semenov; */
/**
*
* UI on top of main sliding area (caption, arrows, close button, etc.).
* Built just using public methods/properties of PhotoSwipe.
* 
*/

(function (root, factory) { 
	if (typeof define === 'function' && define.amd) {
		define(factory);
	} else if (typeof exports === 'object') {
		module.exports = factory();
	} else {
		root.PhotoSwipeUI_Default = factory();
	}
})(this, function () {

	'use strict';



var PhotoSwipeUI_Default =
 function(pswp, framework) {

	var ui = this;
	var _overlayUIUpdated = false,
		_controlsVisible = true,
		_fullscrenAPI,
		_controls,
		_captionContainer,
		_fakeCaptionContainer,
		_indexIndicator,
		_shareButton,
		_shareModal,
		_shareModalHidden = true,
		_initalCloseOnScrollValue,
		_isIdle,
		_listen,

		_loadingIndicator,
		_loadingIndicatorHidden,
		_loadingIndicatorTimeout,

		_galleryHasOneSlide,

		_options,
		_defaultUIOptions = {
			barsSize: {top:44, bottom:'auto'},
			closeElClasses: ['item', 'caption', 'zoom-wrap', 'ui', 'top-bar'], 
			timeToIdle: 4000, 
			timeToIdleOutside: 1000,
			loadingIndicatorDelay: 1000, // 2s
			
			addCaptionHTMLFn: function(item, captionEl /*, isFake */) {
				if(!item.title) {
					captionEl.children[0].innerHTML = '';
					return false;
				}
				captionEl.children[0].innerHTML = item.title;
				return true;
			},

			closeEl:true,
			captionEl: true,
			fullscreenEl: true,
			zoomEl: true,
			shareEl: true,
			counterEl: true,
			arrowEl: true,
			preloaderEl: true,

			tapToClose: false,
			tapToToggleControls: true,

			clickToCloseNonZoomable: true,

			shareButtons: [
				{id:'facebook', label:'Share on Facebook', url:'https://www.facebook.com/sharer/sharer.php?u={{url}}'},
				{id:'twitter', label:'Tweet', url:'https://twitter.com/intent/tweet?text={{text}}&url={{url}}'},
				{id:'pinterest', label:'Pin it', url:'http://www.pinterest.com/pin/create/button/'+
													'?url={{url}}&media={{image_url}}&description={{text}}'},
				{id:'download', label:'Download image', url:'{{raw_image_url}}', download:true}
			],
			getImageURLForShare: function( /* shareButtonData */ ) {
				return pswp.currItem.src || '';
			},
			getPageURLForShare: function( /* shareButtonData */ ) {
				return window.location.href;
			},
			getTextForShare: function( /* shareButtonData */ ) {
				return pswp.currItem.title || '';
			},
				
			indexIndicatorSep: ' / ',
			fitControlsWidth: 1200

		},
		_blockControlsTap,
		_blockControlsTapTimeout;



	var _onControlsTap = function(e) {
			if(_blockControlsTap) {
				return true;
			}


			e = e || window.event;

			if(_options.timeToIdle && _options.mouseUsed && !_isIdle) {
				// reset idle timer
				_onIdleMouseMove();
			}


			var target = e.target || e.srcElement,
				uiElement,
				clickedClass = target.getAttribute('class') || '',
				found;

			for(var i = 0; i < _uiElements.length; i++) {
				uiElement = _uiElements[i];
				if(uiElement.onTap && clickedClass.indexOf('pswp__' + uiElement.name ) > -1 ) {
					uiElement.onTap();
					found = true;

				}
			}

			if(found) {
				if(e.stopPropagation) {
					e.stopPropagation();
				}
				_blockControlsTap = true;

				// Some versions of Android don't prevent ghost click event 
				// when preventDefault() was called on touchstart and/or touchend.
				// 
				// This happens on v4.3, 4.2, 4.1, 
				// older versions strangely work correctly, 
				// but just in case we add delay on all of them)	
				var tapDelay = framework.features.isOldAndroid ? 600 : 30;
				_blockControlsTapTimeout = setTimeout(function() {
					_blockControlsTap = false;
				}, tapDelay);
			}

		},
		_fitControlsInViewport = function() {
			return !pswp.likelyTouchDevice || _options.mouseUsed || screen.width > _options.fitControlsWidth;
		},
		_togglePswpClass = function(el, cName, add) {
			framework[ (add ? 'add' : 'remove') + 'Class' ](el, 'pswp__' + cName);
		},

		// add class when there is just one item in the gallery
		// (by default it hides left/right arrows and 1ofX counter)
		_countNumItems = function() {
			var hasOneSlide = (_options.getNumItemsFn() === 1);

			if(hasOneSlide !== _galleryHasOneSlide) {
				_togglePswpClass(_controls, 'ui--one-slide', hasOneSlide);
				_galleryHasOneSlide = hasOneSlide;
			}
		},
		_toggleShareModalClass = function() {
			_togglePswpClass(_shareModal, 'share-modal--hidden', _shareModalHidden);
		},
		_toggleShareModal = function() {

			_shareModalHidden = !_shareModalHidden;
			
			
			if(!_shareModalHidden) {
				_toggleShareModalClass();
				setTimeout(function() {
					if(!_shareModalHidden) {
						framework.addClass(_shareModal, 'pswp__share-modal--fade-in');
					}
				}, 30);
			} else {
				framework.removeClass(_shareModal, 'pswp__share-modal--fade-in');
				setTimeout(function() {
					if(_shareModalHidden) {
						_toggleShareModalClass();
					}
				}, 300);
			}
			
			if(!_shareModalHidden) {
				_updateShareURLs();
			}
			return false;
		},

		_openWindowPopup = function(e) {
			e = e || window.event;
			var target = e.target || e.srcElement;

			pswp.shout('shareLinkClick', e, target);

			if(!target.href) {
				return false;
			}

			if( target.hasAttribute('download') ) {
				return true;
			}

			window.open(target.href, 'pswp_share', 'scrollbars=yes,resizable=yes,toolbar=no,'+
										'location=yes,width=550,height=420,top=100,left=' + 
										(window.screen ? Math.round(screen.width / 2 - 275) : 100)  );

			if(!_shareModalHidden) {
				_toggleShareModal();
			}
			
			return false;
		},
		_updateShareURLs = function() {
			var shareButtonOut = '',
				shareButtonData,
				shareURL,
				image_url,
				page_url,
				share_text;

			for(var i = 0; i < _options.shareButtons.length; i++) {
				shareButtonData = _options.shareButtons[i];

				image_url = _options.getImageURLForShare(shareButtonData);
				page_url = _options.getPageURLForShare(shareButtonData);
				share_text = _options.getTextForShare(shareButtonData);

				shareURL = shareButtonData.url.replace('{{url}}', encodeURIComponent(page_url) )
									.replace('{{image_url}}', encodeURIComponent(image_url) )
									.replace('{{raw_image_url}}', image_url )
									.replace('{{text}}', encodeURIComponent(share_text) );

				shareButtonOut += '<a href="' + shareURL + '" target="_blank" '+
									'class="pswp__share--' + shareButtonData.id + '"' +
									(shareButtonData.download ? 'download' : '') + '>' + 
									shareButtonData.label + '</a>';

				if(_options.parseShareButtonOut) {
					shareButtonOut = _options.parseShareButtonOut(shareButtonData, shareButtonOut);
				}
			}
			_shareModal.children[0].innerHTML = shareButtonOut;
			_shareModal.children[0].onclick = _openWindowPopup;

		},
		_hasCloseClass = function(target) {
			for(var  i = 0; i < _options.closeElClasses.length; i++) {
				if( framework.hasClass(target, 'pswp__' + _options.closeElClasses[i]) ) {
					return true;
				}
			}
		},
		_idleInterval,
		_idleTimer,
		_idleIncrement = 0,
		_onIdleMouseMove = function() {
			clearTimeout(_idleTimer);
			_idleIncrement = 0;
			if(_isIdle) {
				ui.setIdle(false);
			}
		},
		_onMouseLeaveWindow = function(e) {
			e = e ? e : window.event;
			var from = e.relatedTarget || e.toElement;
			if (!from || from.nodeName === 'HTML') {
				clearTimeout(_idleTimer);
				_idleTimer = setTimeout(function() {
					ui.setIdle(true);
				}, _options.timeToIdleOutside);
			}
		},
		_setupFullscreenAPI = function() {
			if(_options.fullscreenEl && !framework.features.isOldAndroid) {
				if(!_fullscrenAPI) {
					_fullscrenAPI = ui.getFullscreenAPI();
				}
				if(_fullscrenAPI) {
					framework.bind(document, _fullscrenAPI.eventK, ui.updateFullscreen);
					ui.updateFullscreen();
					framework.addClass(pswp.template, 'pswp--supports-fs');
				} else {
					framework.removeClass(pswp.template, 'pswp--supports-fs');
				}
			}
		},
		_setupLoadingIndicator = function() {
			// Setup loading indicator
			if(_options.preloaderEl) {
			
				_toggleLoadingIndicator(true);

				_listen('beforeChange', function() {

					clearTimeout(_loadingIndicatorTimeout);

					// display loading indicator with delay
					_loadingIndicatorTimeout = setTimeout(function() {

						if(pswp.currItem && pswp.currItem.loading) {

							if( !pswp.allowProgressiveImg() || (pswp.currItem.img && !pswp.currItem.img.naturalWidth)  ) {
								// show preloader if progressive loading is not enabled, 
								// or image width is not defined yet (because of slow connection)
								_toggleLoadingIndicator(false); 
								// items-controller.js function allowProgressiveImg
							}
							
						} else {
							_toggleLoadingIndicator(true); // hide preloader
						}

					}, _options.loadingIndicatorDelay);
					
				});
				_listen('imageLoadComplete', function(index, item) {
					if(pswp.currItem === item) {
						_toggleLoadingIndicator(true);
					}
				});

			}
		},
		_toggleLoadingIndicator = function(hide) {
			if( _loadingIndicatorHidden !== hide ) {
				_togglePswpClass(_loadingIndicator, 'preloader--active', !hide);
				_loadingIndicatorHidden = hide;
			}
		},
		_applyNavBarGaps = function(item) {
			var gap = item.vGap;

			if( _fitControlsInViewport() ) {
				
				var bars = _options.barsSize; 
				if(_options.captionEl && bars.bottom === 'auto') {
					if(!_fakeCaptionContainer) {
						_fakeCaptionContainer = framework.createEl('pswp__caption pswp__caption--fake');
						_fakeCaptionContainer.appendChild( framework.createEl('pswp__caption__center') );
						_controls.insertBefore(_fakeCaptionContainer, _captionContainer);
						framework.addClass(_controls, 'pswp__ui--fit');
					}
					if( _options.addCaptionHTMLFn(item, _fakeCaptionContainer, true) ) {

						var captionSize = _fakeCaptionContainer.clientHeight;
						gap.bottom = parseInt(captionSize,10) || 44;
					} else {
						gap.bottom = bars.top; // if no caption, set size of bottom gap to size of top
					}
				} else {
					gap.bottom = bars.bottom === 'auto' ? 0 : bars.bottom;
				}
				
				// height of top bar is static, no need to calculate it
				gap.top = bars.top;
			} else {
				gap.top = gap.bottom = 0;
			}
		},
		_setupIdle = function() {
			// Hide controls when mouse is used
			if(_options.timeToIdle) {
				_listen('mouseUsed', function() {
					
					framework.bind(document, 'mousemove', _onIdleMouseMove);
					framework.bind(document, 'mouseout', _onMouseLeaveWindow);

					_idleInterval = setInterval(function() {
						_idleIncrement++;
						if(_idleIncrement === 2) {
							ui.setIdle(true);
						}
					}, _options.timeToIdle / 2);
				});
			}
		},
		_setupHidingControlsDuringGestures = function() {

			// Hide controls on vertical drag
			_listen('onVerticalDrag', function(now) {
				if(_controlsVisible && now < 0.95) {
					ui.hideControls();
				} else if(!_controlsVisible && now >= 0.95) {
					ui.showControls();
				}
			});

			// Hide controls when pinching to close
			var pinchControlsHidden;
			_listen('onPinchClose' , function(now) {
				if(_controlsVisible && now < 0.9) {
					ui.hideControls();
					pinchControlsHidden = true;
				} else if(pinchControlsHidden && !_controlsVisible && now > 0.9) {
					ui.showControls();
				}
			});

			_listen('zoomGestureEnded', function() {
				pinchControlsHidden = false;
				if(pinchControlsHidden && !_controlsVisible) {
					ui.showControls();
				}
			});

		};



	var _uiElements = [
		{ 
			name: 'caption', 
			option: 'captionEl',
			onInit: function(el) {  
				_captionContainer = el; 
			} 
		},
		{ 
			name: 'share-modal', 
			option: 'shareEl',
			onInit: function(el) {  
				_shareModal = el;
			},
			onTap: function() {
				_toggleShareModal();
			} 
		},
		{ 
			name: 'button--share', 
			option: 'shareEl',
			onInit: function(el) { 
				_shareButton = el;
			},
			onTap: function() {
				_toggleShareModal();
			} 
		},
		{ 
			name: 'button--zoom', 
			option: 'zoomEl',
			onTap: pswp.toggleDesktopZoom
		},
		{ 
			name: 'counter', 
			option: 'counterEl',
			onInit: function(el) {  
				_indexIndicator = el;
			} 
		},
		{ 
			name: 'button--close', 
			option: 'closeEl',
			onTap: pswp.close
		},
		{ 
			name: 'button--arrow--left', 
			option: 'arrowEl',
			onTap: pswp.prev
		},
		{ 
			name: 'button--arrow--right', 
			option: 'arrowEl',
			onTap: pswp.next
		},
		{ 
			name: 'button--fs', 
			option: 'fullscreenEl',
			onTap: function() {  
				if(_fullscrenAPI.isFullscreen()) {
					_fullscrenAPI.exit();
				} else {
					_fullscrenAPI.enter();
				}
			} 
		},
		{ 
			name: 'preloader', 
			option: 'preloaderEl',
			onInit: function(el) {  
				_loadingIndicator = el;
			} 
		}

	];

	var _setupUIElements = function() {
		var item,
			classAttr,
			uiElement;

		var loopThroughChildElements = function(sChildren) {
			if(!sChildren) {
				return;
			}

			var l = sChildren.length;
			for(var i = 0; i < l; i++) {
				item = sChildren[i];
				classAttr = item.className;

				for(var a = 0; a < _uiElements.length; a++) {
					uiElement = _uiElements[a];

					if(classAttr.indexOf('pswp__' + uiElement.name) > -1  ) {

						if( _options[uiElement.option] ) { // if element is not disabled from options
							
							framework.removeClass(item, 'pswp__element--disabled');
							if(uiElement.onInit) {
								uiElement.onInit(item);
							}
							
							//item.style.display = 'block';
						} else {
							framework.addClass(item, 'pswp__element--disabled');
							//item.style.display = 'none';
						}
					}
				}
			}
		};
		loopThroughChildElements(_controls.children);

		var topBar =  framework.getChildByClass(_controls, 'pswp__top-bar');
		if(topBar) {
			loopThroughChildElements( topBar.children );
		}
	};


	

	ui.init = function() {

		// extend options
		framework.extend(pswp.options, _defaultUIOptions, true);

		// create local link for fast access
		_options = pswp.options;

		// find pswp__ui element
		_controls = framework.getChildByClass(pswp.scrollWrap, 'pswp__ui');

		// create local link
		_listen = pswp.listen;


		_setupHidingControlsDuringGestures();

		// update controls when slides change
		_listen('beforeChange', ui.update);

		// toggle zoom on double-tap
		_listen('doubleTap', function(point) {
			var initialZoomLevel = pswp.currItem.initialZoomLevel;
			if(pswp.getZoomLevel() !== initialZoomLevel) {
				pswp.zoomTo(initialZoomLevel, point, 333);
			} else {
				pswp.zoomTo(_options.getDoubleTapZoom(false, pswp.currItem), point, 333);
			}
		});

		// Allow text selection in caption
		_listen('preventDragEvent', function(e, isDown, preventObj) {
			var t = e.target || e.srcElement;
			if(
				t && 
				t.getAttribute('class') && e.type.indexOf('mouse') > -1 && 
				( t.getAttribute('class').indexOf('__caption') > 0 || (/(SMALL|STRONG|EM)/i).test(t.tagName) ) 
			) {
				preventObj.prevent = false;
			}
		});

		// bind events for UI
		_listen('bindEvents', function() {
			framework.bind(_controls, 'pswpTap click', _onControlsTap);
			framework.bind(pswp.scrollWrap, 'pswpTap', ui.onGlobalTap);

			if(!pswp.likelyTouchDevice) {
				framework.bind(pswp.scrollWrap, 'mouseover', ui.onMouseOver);
			}
		});

		// unbind events for UI
		_listen('unbindEvents', function() {
			if(!_shareModalHidden) {
				_toggleShareModal();
			}

			if(_idleInterval) {
				clearInterval(_idleInterval);
			}
			framework.unbind(document, 'mouseout', _onMouseLeaveWindow);
			framework.unbind(document, 'mousemove', _onIdleMouseMove);
			framework.unbind(_controls, 'pswpTap click', _onControlsTap);
			framework.unbind(pswp.scrollWrap, 'pswpTap', ui.onGlobalTap);
			framework.unbind(pswp.scrollWrap, 'mouseover', ui.onMouseOver);

			if(_fullscrenAPI) {
				framework.unbind(document, _fullscrenAPI.eventK, ui.updateFullscreen);
				if(_fullscrenAPI.isFullscreen()) {
					_options.hideAnimationDuration = 0;
					_fullscrenAPI.exit();
				}
				_fullscrenAPI = null;
			}
		});


		// clean up things when gallery is destroyed
		_listen('destroy', function() {
			if(_options.captionEl) {
				if(_fakeCaptionContainer) {
					_controls.removeChild(_fakeCaptionContainer);
				}
				framework.removeClass(_captionContainer, 'pswp__caption--empty');
			}

			if(_shareModal) {
				_shareModal.children[0].onclick = null;
			}
			framework.removeClass(_controls, 'pswp__ui--over-close');
			framework.addClass( _controls, 'pswp__ui--hidden');
			ui.setIdle(false);
		});
		

		if(!_options.showAnimationDuration) {
			framework.removeClass( _controls, 'pswp__ui--hidden');
		}
		_listen('initialZoomIn', function() {
			if(_options.showAnimationDuration) {
				framework.removeClass( _controls, 'pswp__ui--hidden');
			}
		});
		_listen('initialZoomOut', function() {
			framework.addClass( _controls, 'pswp__ui--hidden');
		});

		_listen('parseVerticalMargin', _applyNavBarGaps);
		
		_setupUIElements();

		if(_options.shareEl && _shareButton && _shareModal) {
			_shareModalHidden = true;
		}

		_countNumItems();

		_setupIdle();

		_setupFullscreenAPI();

		_setupLoadingIndicator();
	};

	ui.setIdle = function(isIdle) {
		_isIdle = isIdle;
		_togglePswpClass(_controls, 'ui--idle', isIdle);
	};

	ui.update = function() {
		// Don't update UI if it's hidden
		if(_controlsVisible && pswp.currItem) {
			
			ui.updateIndexIndicator();

			if(_options.captionEl) {
				_options.addCaptionHTMLFn(pswp.currItem, _captionContainer);

				_togglePswpClass(_captionContainer, 'caption--empty', !pswp.currItem.title);
			}

			_overlayUIUpdated = true;

		} else {
			_overlayUIUpdated = false;
		}

		if(!_shareModalHidden) {
			_toggleShareModal();
		}

		_countNumItems();
	};

	ui.updateFullscreen = function(e) {

		if(e) {
			// some browsers change window scroll position during the fullscreen
			// so PhotoSwipe updates it just in case
			setTimeout(function() {
				pswp.setScrollOffset( 0, framework.getScrollY() );
			}, 50);
		}
		
		// toogle pswp--fs class on root element
		framework[ (_fullscrenAPI.isFullscreen() ? 'add' : 'remove') + 'Class' ](pswp.template, 'pswp--fs');
	};

	ui.updateIndexIndicator = function() {
		if(_options.counterEl) {
			_indexIndicator.innerHTML = (pswp.getCurrentIndex()+1) + 
										_options.indexIndicatorSep + 
										_options.getNumItemsFn();
		}
	};
	
	ui.onGlobalTap = function(e) {
		e = e || window.event;
		var target = e.target || e.srcElement;

		if(_blockControlsTap) {
			return;
		}

		if(e.detail && e.detail.pointerType === 'mouse') {

			// close gallery if clicked outside of the image
			if(_hasCloseClass(target)) {
				pswp.close();
				return;
			}

			if(framework.hasClass(target, 'pswp__img')) {
				if(pswp.getZoomLevel() === 1 && pswp.getZoomLevel() <= pswp.currItem.fitRatio) {
					if(_options.clickToCloseNonZoomable) {
						pswp.close();
					}
				} else {
					pswp.toggleDesktopZoom(e.detail.releasePoint);
				}
			}
			
		} else {

			// tap anywhere (except buttons) to toggle visibility of controls
			if(_options.tapToToggleControls) {
				if(_controlsVisible) {
					ui.hideControls();
				} else {
					ui.showControls();
				}
			}

			// tap to close gallery
			if(_options.tapToClose && (framework.hasClass(target, 'pswp__img') || _hasCloseClass(target)) ) {
				pswp.close();
				return;
			}
			
		}
	};
	ui.onMouseOver = function(e) {
		e = e || window.event;
		var target = e.target || e.srcElement;

		// add class when mouse is over an element that should close the gallery
		_togglePswpClass(_controls, 'ui--over-close', _hasCloseClass(target));
	};

	ui.hideControls = function() {
		framework.addClass(_controls,'pswp__ui--hidden');
		_controlsVisible = false;
	};

	ui.showControls = function() {
		_controlsVisible = true;
		if(!_overlayUIUpdated) {
			ui.update();
		}
		framework.removeClass(_controls,'pswp__ui--hidden');
	};

	ui.supportsFullscreen = function() {
		var d = document;
		return !!(d.exitFullscreen || d.mozCancelFullScreen || d.webkitExitFullscreen || d.msExitFullscreen);
	};

	ui.getFullscreenAPI = function() {
		var dE = document.documentElement,
			api,
			tF = 'fullscreenchange';

		if (dE.requestFullscreen) {
			api = {
				enterK: 'requestFullscreen',
				exitK: 'exitFullscreen',
				elementK: 'fullscreenElement',
				eventK: tF
			};

		} else if(dE.mozRequestFullScreen ) {
			api = {
				enterK: 'mozRequestFullScreen',
				exitK: 'mozCancelFullScreen',
				elementK: 'mozFullScreenElement',
				eventK: 'moz' + tF
			};

			

		} else if(dE.webkitRequestFullscreen) {
			api = {
				enterK: 'webkitRequestFullscreen',
				exitK: 'webkitExitFullscreen',
				elementK: 'webkitFullscreenElement',
				eventK: 'webkit' + tF
			};

		} else if(dE.msRequestFullscreen) {
			api = {
				enterK: 'msRequestFullscreen',
				exitK: 'msExitFullscreen',
				elementK: 'msFullscreenElement',
				eventK: 'MSFullscreenChange'
			};
		}

		if(api) {
			api.enter = function() { 
				// disable close-on-scroll in fullscreen
				_initalCloseOnScrollValue = _options.closeOnScroll; 
				_options.closeOnScroll = false; 

				if(this.enterK === 'webkitRequestFullscreen') {
					pswp.template[this.enterK]( Element.ALLOW_KEYBOARD_INPUT );
				} else {
					return pswp.template[this.enterK](); 
				}
			};
			api.exit = function() { 
				_options.closeOnScroll = _initalCloseOnScrollValue;

				return document[this.exitK](); 

			};
			api.isFullscreen = function() { return document[this.elementK]; };
		}

		return api;
	};



};
return PhotoSwipeUI_Default;


});


/*!
 * parallax.js v1.3.1 (http://pixelcog.github.io/parallax.js/)
 * @copyright 2015 PixelCog, Inc.
 * @license MIT (https://github.com/pixelcog/parallax.js/blob/master/LICENSE)
 */


;(function ( $, window, document, undefined ) {

  // Polyfill for requestAnimationFrame
  // via: https://gist.github.com/paulirish/1579671

  (function() {
    var lastTime = 0;
    var vendors = ['ms', 'moz', 'webkit', 'o'];
    for(var x = 0; x < vendors.length && !window.requestAnimationFrame; ++x) {
      window.requestAnimationFrame = window[vendors[x]+'RequestAnimationFrame'];
      window.cancelAnimationFrame = window[vendors[x]+'CancelAnimationFrame']
                                 || window[vendors[x]+'CancelRequestAnimationFrame'];
    }

    if (!window.requestAnimationFrame)
      window.requestAnimationFrame = function(callback) {
        var currTime = new Date().getTime();
        var timeToCall = Math.max(0, 16 - (currTime - lastTime));
        var id = window.setTimeout(function() { callback(currTime + timeToCall); },
          timeToCall);
        lastTime = currTime + timeToCall;
        return id;
      };

    if (!window.cancelAnimationFrame)
      window.cancelAnimationFrame = function(id) {
        clearTimeout(id);
      };
  }());


  // Parallax Constructor

  function Parallax(element, options) {
    var self = this;

    if (typeof options == 'object') {
      delete options.refresh;
      delete options.render;
      $.extend(this, options);
    }

    this.$element = $(element);

    if (!this.imageSrc && this.$element.is('img')) {
      this.imageSrc = this.$element.attr('src');
    }

    var positions = (this.position + '').toLowerCase().match(/\S+/g) || [];

    if (positions.length < 1) {
      positions.push('center');
    }
    if (positions.length == 1) {
      positions.push(positions[0]);
    }

    if (positions[0] == 'top' || positions[0] == 'bottom' || positions[1] == 'left' || positions[1] == 'right') {
      positions = [positions[1], positions[0]];
    }

    if (this.positionX != undefined) positions[0] = this.positionX.toLowerCase();
    if (this.positionY != undefined) positions[1] = this.positionY.toLowerCase();

    self.positionX = positions[0];
    self.positionY = positions[1];

    if (this.positionX != 'left' && this.positionX != 'right') {
      if (isNaN(parseInt(this.positionX))) {
        this.positionX = 'center';
      } else {
        this.positionX = parseInt(this.positionX);
      }
    }

    if (this.positionY != 'top' && this.positionY != 'bottom') {
      if (isNaN(parseInt(this.positionY))) {
        this.positionY = 'center';
      } else {
        this.positionY = parseInt(this.positionY);
      }
    }

    this.position =
      this.positionX + (isNaN(this.positionX)? '' : 'px') + ' ' +
      this.positionY + (isNaN(this.positionY)? '' : 'px');

    if (navigator.userAgent.match(/(iPod|iPhone|iPad)/)) {
      if (this.iosFix && !this.$element.is('img')) {
        this.$element.css({
          backgroundImage: 'url(' + this.imageSrc + ')',
          backgroundSize: 'cover',
          backgroundPosition: this.position
        });
      }
      return this;
    }

    if (navigator.userAgent.match(/(Android)/)) {
      if (this.androidFix && !this.$element.is('img')) {
        this.$element.css({
          backgroundImage: 'url(' + this.imageSrc + ')',
          backgroundSize: 'cover',
          backgroundPosition: this.position
        });
      }
      return this;
    }

    if (document.location.pathname == "/") {
      this.$mirror = $('<div />').prependTo('body');
    } else {
      this.$mirror = $('<div />').prependTo('article');
    }

    this.$slider = $('<img />').prependTo(this.$mirror);

    this.$mirror.addClass('parallax-mirror').css({
      visibility: 'hidden',
      zIndex: this.zIndex,
      position: 'fixed',
      top: 0,
      left: 0,
      overflow: 'hidden'
    });

    this.$slider.addClass('parallax-slider').one('load', function() {
      if (!self.naturalHeight || !self.naturalWidth) {
        self.naturalHeight = this.naturalHeight || this.height || 1;
        self.naturalWidth  = this.naturalWidth  || this.width  || 1;
      }
      self.aspectRatio = self.naturalWidth / self.naturalHeight;

      Parallax.isSetup || Parallax.setup();
      Parallax.sliders.push(self);
      Parallax.isFresh = false;
      Parallax.requestRender();
    });

    this.$slider[0].src = this.imageSrc;

    if (this.naturalHeight && this.naturalWidth || this.$slider[0].complete) {
      this.$slider.trigger('load');
    }

  };


  // Parallax Instance Methods

  $.extend(Parallax.prototype, {
    speed:    0.2,
    bleed:    0,
    zIndex:   100,
    iosFix:   true,
    androidFix: true,
    position: 'center',
    overScrollFix: false,

    refresh: function() {
      this.boxWidth        = this.$element.outerWidth();
      this.boxHeight       = this.$element.outerHeight() + this.bleed * 2;
      this.boxOffsetTop    = this.$element.offset().top - this.bleed;
      this.boxOffsetLeft   = this.$element.offset().left;
      this.boxOffsetBottom = this.boxOffsetTop + this.boxHeight;

      var winHeight = Parallax.winHeight;
      var docHeight = Parallax.docHeight;
      var maxOffset = Math.min(this.boxOffsetTop, docHeight - winHeight);
      var minOffset = Math.max(this.boxOffsetTop + this.boxHeight - winHeight, 0);
      var imageHeightMin = this.boxHeight + (maxOffset - minOffset) * (1 - this.speed) | 0;
      var imageOffsetMin = (this.boxOffsetTop - maxOffset) * (1 - this.speed) | 0;

      if (imageHeightMin * this.aspectRatio >= this.boxWidth) {
        this.imageWidth    = imageHeightMin * this.aspectRatio | 0;
        this.imageHeight   = imageHeightMin;
        this.offsetBaseTop = imageOffsetMin;

        var margin = this.imageWidth - this.boxWidth;

        if (this.positionX == 'left') {
          this.offsetLeft = 0;
        } else if (this.positionX == 'right') {
          this.offsetLeft = - margin;
        } else if (!isNaN(this.positionX)) {
          this.offsetLeft = Math.max(this.positionX, - margin);
        } else {
          this.offsetLeft = - margin / 2 | 0;
        }
      } else {
        this.imageWidth    = this.boxWidth;
        this.imageHeight   = this.boxWidth / this.aspectRatio | 0;
        this.offsetLeft    = 0;

        var margin = this.imageHeight - imageHeightMin;

        if (this.positionY == 'top') {
          this.offsetBaseTop = imageOffsetMin;
        } else if (this.positionY == 'bottom') {
          this.offsetBaseTop = imageOffsetMin - margin;
        } else if (!isNaN(this.positionY)) {
          this.offsetBaseTop = imageOffsetMin + Math.max(this.positionY, - margin);
        } else {
          this.offsetBaseTop = imageOffsetMin - margin / 2 | 0;
        }
      }
    },

    render: function() {
      var scrollTop    = Parallax.scrollTop;
      var scrollLeft   = Parallax.scrollLeft;
      var overScroll   = this.overScrollFix ? Parallax.overScroll : 0;
      var scrollBottom = scrollTop + Parallax.winHeight;

      if (this.boxOffsetBottom > scrollTop && this.boxOffsetTop < scrollBottom) {
        this.visibility = 'visible';
      } else {
        this.visibility = 'hidden';
      }
      this.mirrorTop = this.boxOffsetTop  - scrollTop;
      this.mirrorLeft = this.boxOffsetLeft - scrollLeft;
      this.offsetTop = this.offsetBaseTop - this.mirrorTop * (1 - this.speed);

      this.$mirror.css({
        transform: 'translate3d(0px, 0px, 0px)',
        visibility: this.visibility,
        top: this.mirrorTop - overScroll,
        left: this.mirrorLeft,
        height: this.boxHeight,
        width: this.boxWidth
      });

      this.$slider.css({
        transform: 'translate3d(0px, 0px, 0px)',
        position: 'absolute',
        top: this.offsetTop,
        left: this.offsetLeft,
        height: this.imageHeight,
        width: this.imageWidth,
        maxWidth: 'none'
      });
    }
  });


  // Parallax Static Methods

  $.extend(Parallax, {
    scrollTop:    0,
    scrollLeft:   0,
    winHeight:    0,
    winWidth:     0,
    docHeight:    1 << 30,
    docWidth:     1 << 30,
    sliders:      [],
    isReady:      false,
    isFresh:      false,
    isBusy:       false,

    setup: function() {
      if (this.isReady) return;

      var $doc = $(document), $win = $(window);
      var loadDimensions = function() {
        Parallax.winHeight = $win.height();
        Parallax.winWidth  = $win.width();
        Parallax.docHeight = $doc.height();
        Parallax.docWidth  = $doc.width();
      };
      var loadScrollPosition = function() {
        var scrollTopMax  = Parallax.docHeight - Parallax.winHeight;
        var scrollLeftMax = Parallax.docWidth  - Parallax.winWidth;
        Parallax.scrollTop  = Math.max(0, Math.min(scrollTopMax,  $win.scrollTop()));
        Parallax.scrollLeft = Math.max(0, Math.min(scrollLeftMax, $win.scrollLeft()));
        Parallax.overScroll = Math.max($win.scrollTop() - scrollTopMax, Math.min($win.scrollTop(), 0));
      };

      $win.on('resize.px.parallax load.px.parallax', function() {
          loadDimensions();
          Parallax.isFresh = false;
          Parallax.requestRender();
        })
        .on('scroll.px.parallax load.px.parallax', function() {
          loadScrollPosition();
          Parallax.requestRender();
        });

      loadDimensions();
      loadScrollPosition();

      this.isReady = true;
    },

    configure: function(options) {
      if (typeof options == 'object') {
        delete options.refresh;
        delete options.render;
        $.extend(this.prototype, options);
      }
    },

    refresh: function() {
      $.each(this.sliders, function(){ this.refresh() });
      this.isFresh = true;
    },

    render: function() {
      this.isFresh || this.refresh();
      $.each(this.sliders, function(){ this.render() });
    },

    requestRender: function() {
      var self = this;

      if (!this.isBusy) {
        this.isBusy = true;
        window.requestAnimationFrame(function() {
          self.render();
          self.isBusy = false;
        });
      }
    }
  });


  // Parallax Plugin Definition

  function Plugin(option) {
    return this.each(function () {
      var $this = $(this);
      var options = typeof option == 'object' && option;

      if (this == window || this == document || $this.is('body')) {
        Parallax.configure(options);
      }
      else if (!$this.data('px.parallax')) {
        options = $.extend({}, $this.data(), options);
        $this.data('px.parallax', new Parallax(this, options));
      }
      if (typeof option == 'string') {
        Parallax[option]();
      }
    })
  };

  var old = $.fn.parallax;

  $.fn.parallax             = Plugin;
  $.fn.parallax.Constructor = Parallax;


  // Parallax No Conflict

  $.fn.parallax.noConflict = function () {
    $.fn.parallax = old;
    return this;
  };


  // Parallax Data-API

  $(document).on('ready.px.parallax.data-api', function () {
    $('[data-parallax="scroll"]').parallax();
  });

}(jQuery, window, document));
/*
Copyright 2012 Igor Vaynberg

Version: 3.5.4 Timestamp: Sun Aug 30 13:30:32 EDT 2015

This software is licensed under the Apache License, Version 2.0 (the "Apache License") or the GNU
General Public License version 2 (the "GPL License"). You may choose either license to govern your
use of this software only upon the condition that you accept all of the terms of either the Apache
License or the GPL License.

You may obtain a copy of the Apache License and the GPL License at:

    http://www.apache.org/licenses/LICENSE-2.0
    http://www.gnu.org/licenses/gpl-2.0.html

Unless required by applicable law or agreed to in writing, software distributed under the
Apache License or the GPL License is distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR
CONDITIONS OF ANY KIND, either express or implied. See the Apache License and the GPL License for
the specific language governing permissions and limitations under the Apache License and the GPL License.
*/

(function ($) {
    if(typeof $.fn.each2 == "undefined") {
        $.extend($.fn, {
            /*
            * 4-10 times faster .each replacement
            * use it carefully, as it overrides jQuery context of element on each iteration
            */
            each2 : function (c) {
                var j = $([0]), i = -1, l = this.length;
                while (
                    ++i < l
                    && (j.context = j[0] = this[i])
                    && c.call(j[0], i, j) !== false //"this"=DOM, i=index, j=jQuery object
                );
                return this;
            }
        });
    }
})(jQuery);

(function ($, undefined) {
    "use strict";
    /*global document, window, jQuery, console */

    if (window.Select2 !== undefined) {
        return;
    }

    var AbstractSelect2, SingleSelect2, MultiSelect2, nextUid, sizer,
        lastMousePosition={x:0,y:0}, $document, scrollBarDimensions,

    KEY = {
        TAB: 9,
        ENTER: 13,
        ESC: 27,
        SPACE: 32,
        LEFT: 37,
        UP: 38,
        RIGHT: 39,
        DOWN: 40,
        SHIFT: 16,
        CTRL: 17,
        ALT: 18,
        PAGE_UP: 33,
        PAGE_DOWN: 34,
        HOME: 36,
        END: 35,
        BACKSPACE: 8,
        DELETE: 46,
        isArrow: function (k) {
            k = k.which ? k.which : k;
            switch (k) {
            case KEY.LEFT:
            case KEY.RIGHT:
            case KEY.UP:
            case KEY.DOWN:
                return true;
            }
            return false;
        },
        isControl: function (e) {
            var k = e.which;
            switch (k) {
            case KEY.SHIFT:
            case KEY.CTRL:
            case KEY.ALT:
                return true;
            }

            if (e.metaKey) return true;

            return false;
        },
        isFunctionKey: function (k) {
            k = k.which ? k.which : k;
            return k >= 112 && k <= 123;
        }
    },
    MEASURE_SCROLLBAR_TEMPLATE = "<div class='select2-measure-scrollbar'></div>",

    DIACRITICS = {"\u24B6":"A","\uFF21":"A","\u00C0":"A","\u00C1":"A","\u00C2":"A","\u1EA6":"A","\u1EA4":"A","\u1EAA":"A","\u1EA8":"A","\u00C3":"A","\u0100":"A","\u0102":"A","\u1EB0":"A","\u1EAE":"A","\u1EB4":"A","\u1EB2":"A","\u0226":"A","\u01E0":"A","\u00C4":"A","\u01DE":"A","\u1EA2":"A","\u00C5":"A","\u01FA":"A","\u01CD":"A","\u0200":"A","\u0202":"A","\u1EA0":"A","\u1EAC":"A","\u1EB6":"A","\u1E00":"A","\u0104":"A","\u023A":"A","\u2C6F":"A","\uA732":"AA","\u00C6":"AE","\u01FC":"AE","\u01E2":"AE","\uA734":"AO","\uA736":"AU","\uA738":"AV","\uA73A":"AV","\uA73C":"AY","\u24B7":"B","\uFF22":"B","\u1E02":"B","\u1E04":"B","\u1E06":"B","\u0243":"B","\u0182":"B","\u0181":"B","\u24B8":"C","\uFF23":"C","\u0106":"C","\u0108":"C","\u010A":"C","\u010C":"C","\u00C7":"C","\u1E08":"C","\u0187":"C","\u023B":"C","\uA73E":"C","\u24B9":"D","\uFF24":"D","\u1E0A":"D","\u010E":"D","\u1E0C":"D","\u1E10":"D","\u1E12":"D","\u1E0E":"D","\u0110":"D","\u018B":"D","\u018A":"D","\u0189":"D","\uA779":"D","\u01F1":"DZ","\u01C4":"DZ","\u01F2":"Dz","\u01C5":"Dz","\u24BA":"E","\uFF25":"E","\u00C8":"E","\u00C9":"E","\u00CA":"E","\u1EC0":"E","\u1EBE":"E","\u1EC4":"E","\u1EC2":"E","\u1EBC":"E","\u0112":"E","\u1E14":"E","\u1E16":"E","\u0114":"E","\u0116":"E","\u00CB":"E","\u1EBA":"E","\u011A":"E","\u0204":"E","\u0206":"E","\u1EB8":"E","\u1EC6":"E","\u0228":"E","\u1E1C":"E","\u0118":"E","\u1E18":"E","\u1E1A":"E","\u0190":"E","\u018E":"E","\u24BB":"F","\uFF26":"F","\u1E1E":"F","\u0191":"F","\uA77B":"F","\u24BC":"G","\uFF27":"G","\u01F4":"G","\u011C":"G","\u1E20":"G","\u011E":"G","\u0120":"G","\u01E6":"G","\u0122":"G","\u01E4":"G","\u0193":"G","\uA7A0":"G","\uA77D":"G","\uA77E":"G","\u24BD":"H","\uFF28":"H","\u0124":"H","\u1E22":"H","\u1E26":"H","\u021E":"H","\u1E24":"H","\u1E28":"H","\u1E2A":"H","\u0126":"H","\u2C67":"H","\u2C75":"H","\uA78D":"H","\u24BE":"I","\uFF29":"I","\u00CC":"I","\u00CD":"I","\u00CE":"I","\u0128":"I","\u012A":"I","\u012C":"I","\u0130":"I","\u00CF":"I","\u1E2E":"I","\u1EC8":"I","\u01CF":"I","\u0208":"I","\u020A":"I","\u1ECA":"I","\u012E":"I","\u1E2C":"I","\u0197":"I","\u24BF":"J","\uFF2A":"J","\u0134":"J","\u0248":"J","\u24C0":"K","\uFF2B":"K","\u1E30":"K","\u01E8":"K","\u1E32":"K","\u0136":"K","\u1E34":"K","\u0198":"K","\u2C69":"K","\uA740":"K","\uA742":"K","\uA744":"K","\uA7A2":"K","\u24C1":"L","\uFF2C":"L","\u013F":"L","\u0139":"L","\u013D":"L","\u1E36":"L","\u1E38":"L","\u013B":"L","\u1E3C":"L","\u1E3A":"L","\u0141":"L","\u023D":"L","\u2C62":"L","\u2C60":"L","\uA748":"L","\uA746":"L","\uA780":"L","\u01C7":"LJ","\u01C8":"Lj","\u24C2":"M","\uFF2D":"M","\u1E3E":"M","\u1E40":"M","\u1E42":"M","\u2C6E":"M","\u019C":"M","\u24C3":"N","\uFF2E":"N","\u01F8":"N","\u0143":"N","\u00D1":"N","\u1E44":"N","\u0147":"N","\u1E46":"N","\u0145":"N","\u1E4A":"N","\u1E48":"N","\u0220":"N","\u019D":"N","\uA790":"N","\uA7A4":"N","\u01CA":"NJ","\u01CB":"Nj","\u24C4":"O","\uFF2F":"O","\u00D2":"O","\u00D3":"O","\u00D4":"O","\u1ED2":"O","\u1ED0":"O","\u1ED6":"O","\u1ED4":"O","\u00D5":"O","\u1E4C":"O","\u022C":"O","\u1E4E":"O","\u014C":"O","\u1E50":"O","\u1E52":"O","\u014E":"O","\u022E":"O","\u0230":"O","\u00D6":"O","\u022A":"O","\u1ECE":"O","\u0150":"O","\u01D1":"O","\u020C":"O","\u020E":"O","\u01A0":"O","\u1EDC":"O","\u1EDA":"O","\u1EE0":"O","\u1EDE":"O","\u1EE2":"O","\u1ECC":"O","\u1ED8":"O","\u01EA":"O","\u01EC":"O","\u00D8":"O","\u01FE":"O","\u0186":"O","\u019F":"O","\uA74A":"O","\uA74C":"O","\u01A2":"OI","\uA74E":"OO","\u0222":"OU","\u24C5":"P","\uFF30":"P","\u1E54":"P","\u1E56":"P","\u01A4":"P","\u2C63":"P","\uA750":"P","\uA752":"P","\uA754":"P","\u24C6":"Q","\uFF31":"Q","\uA756":"Q","\uA758":"Q","\u024A":"Q","\u24C7":"R","\uFF32":"R","\u0154":"R","\u1E58":"R","\u0158":"R","\u0210":"R","\u0212":"R","\u1E5A":"R","\u1E5C":"R","\u0156":"R","\u1E5E":"R","\u024C":"R","\u2C64":"R","\uA75A":"R","\uA7A6":"R","\uA782":"R","\u24C8":"S","\uFF33":"S","\u1E9E":"S","\u015A":"S","\u1E64":"S","\u015C":"S","\u1E60":"S","\u0160":"S","\u1E66":"S","\u1E62":"S","\u1E68":"S","\u0218":"S","\u015E":"S","\u2C7E":"S","\uA7A8":"S","\uA784":"S","\u24C9":"T","\uFF34":"T","\u1E6A":"T","\u0164":"T","\u1E6C":"T","\u021A":"T","\u0162":"T","\u1E70":"T","\u1E6E":"T","\u0166":"T","\u01AC":"T","\u01AE":"T","\u023E":"T","\uA786":"T","\uA728":"TZ","\u24CA":"U","\uFF35":"U","\u00D9":"U","\u00DA":"U","\u00DB":"U","\u0168":"U","\u1E78":"U","\u016A":"U","\u1E7A":"U","\u016C":"U","\u00DC":"U","\u01DB":"U","\u01D7":"U","\u01D5":"U","\u01D9":"U","\u1EE6":"U","\u016E":"U","\u0170":"U","\u01D3":"U","\u0214":"U","\u0216":"U","\u01AF":"U","\u1EEA":"U","\u1EE8":"U","\u1EEE":"U","\u1EEC":"U","\u1EF0":"U","\u1EE4":"U","\u1E72":"U","\u0172":"U","\u1E76":"U","\u1E74":"U","\u0244":"U","\u24CB":"V","\uFF36":"V","\u1E7C":"V","\u1E7E":"V","\u01B2":"V","\uA75E":"V","\u0245":"V","\uA760":"VY","\u24CC":"W","\uFF37":"W","\u1E80":"W","\u1E82":"W","\u0174":"W","\u1E86":"W","\u1E84":"W","\u1E88":"W","\u2C72":"W","\u24CD":"X","\uFF38":"X","\u1E8A":"X","\u1E8C":"X","\u24CE":"Y","\uFF39":"Y","\u1EF2":"Y","\u00DD":"Y","\u0176":"Y","\u1EF8":"Y","\u0232":"Y","\u1E8E":"Y","\u0178":"Y","\u1EF6":"Y","\u1EF4":"Y","\u01B3":"Y","\u024E":"Y","\u1EFE":"Y","\u24CF":"Z","\uFF3A":"Z","\u0179":"Z","\u1E90":"Z","\u017B":"Z","\u017D":"Z","\u1E92":"Z","\u1E94":"Z","\u01B5":"Z","\u0224":"Z","\u2C7F":"Z","\u2C6B":"Z","\uA762":"Z","\u24D0":"a","\uFF41":"a","\u1E9A":"a","\u00E0":"a","\u00E1":"a","\u00E2":"a","\u1EA7":"a","\u1EA5":"a","\u1EAB":"a","\u1EA9":"a","\u00E3":"a","\u0101":"a","\u0103":"a","\u1EB1":"a","\u1EAF":"a","\u1EB5":"a","\u1EB3":"a","\u0227":"a","\u01E1":"a","\u00E4":"a","\u01DF":"a","\u1EA3":"a","\u00E5":"a","\u01FB":"a","\u01CE":"a","\u0201":"a","\u0203":"a","\u1EA1":"a","\u1EAD":"a","\u1EB7":"a","\u1E01":"a","\u0105":"a","\u2C65":"a","\u0250":"a","\uA733":"aa","\u00E6":"ae","\u01FD":"ae","\u01E3":"ae","\uA735":"ao","\uA737":"au","\uA739":"av","\uA73B":"av","\uA73D":"ay","\u24D1":"b","\uFF42":"b","\u1E03":"b","\u1E05":"b","\u1E07":"b","\u0180":"b","\u0183":"b","\u0253":"b","\u24D2":"c","\uFF43":"c","\u0107":"c","\u0109":"c","\u010B":"c","\u010D":"c","\u00E7":"c","\u1E09":"c","\u0188":"c","\u023C":"c","\uA73F":"c","\u2184":"c","\u24D3":"d","\uFF44":"d","\u1E0B":"d","\u010F":"d","\u1E0D":"d","\u1E11":"d","\u1E13":"d","\u1E0F":"d","\u0111":"d","\u018C":"d","\u0256":"d","\u0257":"d","\uA77A":"d","\u01F3":"dz","\u01C6":"dz","\u24D4":"e","\uFF45":"e","\u00E8":"e","\u00E9":"e","\u00EA":"e","\u1EC1":"e","\u1EBF":"e","\u1EC5":"e","\u1EC3":"e","\u1EBD":"e","\u0113":"e","\u1E15":"e","\u1E17":"e","\u0115":"e","\u0117":"e","\u00EB":"e","\u1EBB":"e","\u011B":"e","\u0205":"e","\u0207":"e","\u1EB9":"e","\u1EC7":"e","\u0229":"e","\u1E1D":"e","\u0119":"e","\u1E19":"e","\u1E1B":"e","\u0247":"e","\u025B":"e","\u01DD":"e","\u24D5":"f","\uFF46":"f","\u1E1F":"f","\u0192":"f","\uA77C":"f","\u24D6":"g","\uFF47":"g","\u01F5":"g","\u011D":"g","\u1E21":"g","\u011F":"g","\u0121":"g","\u01E7":"g","\u0123":"g","\u01E5":"g","\u0260":"g","\uA7A1":"g","\u1D79":"g","\uA77F":"g","\u24D7":"h","\uFF48":"h","\u0125":"h","\u1E23":"h","\u1E27":"h","\u021F":"h","\u1E25":"h","\u1E29":"h","\u1E2B":"h","\u1E96":"h","\u0127":"h","\u2C68":"h","\u2C76":"h","\u0265":"h","\u0195":"hv","\u24D8":"i","\uFF49":"i","\u00EC":"i","\u00ED":"i","\u00EE":"i","\u0129":"i","\u012B":"i","\u012D":"i","\u00EF":"i","\u1E2F":"i","\u1EC9":"i","\u01D0":"i","\u0209":"i","\u020B":"i","\u1ECB":"i","\u012F":"i","\u1E2D":"i","\u0268":"i","\u0131":"i","\u24D9":"j","\uFF4A":"j","\u0135":"j","\u01F0":"j","\u0249":"j","\u24DA":"k","\uFF4B":"k","\u1E31":"k","\u01E9":"k","\u1E33":"k","\u0137":"k","\u1E35":"k","\u0199":"k","\u2C6A":"k","\uA741":"k","\uA743":"k","\uA745":"k","\uA7A3":"k","\u24DB":"l","\uFF4C":"l","\u0140":"l","\u013A":"l","\u013E":"l","\u1E37":"l","\u1E39":"l","\u013C":"l","\u1E3D":"l","\u1E3B":"l","\u017F":"l","\u0142":"l","\u019A":"l","\u026B":"l","\u2C61":"l","\uA749":"l","\uA781":"l","\uA747":"l","\u01C9":"lj","\u24DC":"m","\uFF4D":"m","\u1E3F":"m","\u1E41":"m","\u1E43":"m","\u0271":"m","\u026F":"m","\u24DD":"n","\uFF4E":"n","\u01F9":"n","\u0144":"n","\u00F1":"n","\u1E45":"n","\u0148":"n","\u1E47":"n","\u0146":"n","\u1E4B":"n","\u1E49":"n","\u019E":"n","\u0272":"n","\u0149":"n","\uA791":"n","\uA7A5":"n","\u01CC":"nj","\u24DE":"o","\uFF4F":"o","\u00F2":"o","\u00F3":"o","\u00F4":"o","\u1ED3":"o","\u1ED1":"o","\u1ED7":"o","\u1ED5":"o","\u00F5":"o","\u1E4D":"o","\u022D":"o","\u1E4F":"o","\u014D":"o","\u1E51":"o","\u1E53":"o","\u014F":"o","\u022F":"o","\u0231":"o","\u00F6":"o","\u022B":"o","\u1ECF":"o","\u0151":"o","\u01D2":"o","\u020D":"o","\u020F":"o","\u01A1":"o","\u1EDD":"o","\u1EDB":"o","\u1EE1":"o","\u1EDF":"o","\u1EE3":"o","\u1ECD":"o","\u1ED9":"o","\u01EB":"o","\u01ED":"o","\u00F8":"o","\u01FF":"o","\u0254":"o","\uA74B":"o","\uA74D":"o","\u0275":"o","\u01A3":"oi","\u0223":"ou","\uA74F":"oo","\u24DF":"p","\uFF50":"p","\u1E55":"p","\u1E57":"p","\u01A5":"p","\u1D7D":"p","\uA751":"p","\uA753":"p","\uA755":"p","\u24E0":"q","\uFF51":"q","\u024B":"q","\uA757":"q","\uA759":"q","\u24E1":"r","\uFF52":"r","\u0155":"r","\u1E59":"r","\u0159":"r","\u0211":"r","\u0213":"r","\u1E5B":"r","\u1E5D":"r","\u0157":"r","\u1E5F":"r","\u024D":"r","\u027D":"r","\uA75B":"r","\uA7A7":"r","\uA783":"r","\u24E2":"s","\uFF53":"s","\u00DF":"s","\u015B":"s","\u1E65":"s","\u015D":"s","\u1E61":"s","\u0161":"s","\u1E67":"s","\u1E63":"s","\u1E69":"s","\u0219":"s","\u015F":"s","\u023F":"s","\uA7A9":"s","\uA785":"s","\u1E9B":"s","\u24E3":"t","\uFF54":"t","\u1E6B":"t","\u1E97":"t","\u0165":"t","\u1E6D":"t","\u021B":"t","\u0163":"t","\u1E71":"t","\u1E6F":"t","\u0167":"t","\u01AD":"t","\u0288":"t","\u2C66":"t","\uA787":"t","\uA729":"tz","\u24E4":"u","\uFF55":"u","\u00F9":"u","\u00FA":"u","\u00FB":"u","\u0169":"u","\u1E79":"u","\u016B":"u","\u1E7B":"u","\u016D":"u","\u00FC":"u","\u01DC":"u","\u01D8":"u","\u01D6":"u","\u01DA":"u","\u1EE7":"u","\u016F":"u","\u0171":"u","\u01D4":"u","\u0215":"u","\u0217":"u","\u01B0":"u","\u1EEB":"u","\u1EE9":"u","\u1EEF":"u","\u1EED":"u","\u1EF1":"u","\u1EE5":"u","\u1E73":"u","\u0173":"u","\u1E77":"u","\u1E75":"u","\u0289":"u","\u24E5":"v","\uFF56":"v","\u1E7D":"v","\u1E7F":"v","\u028B":"v","\uA75F":"v","\u028C":"v","\uA761":"vy","\u24E6":"w","\uFF57":"w","\u1E81":"w","\u1E83":"w","\u0175":"w","\u1E87":"w","\u1E85":"w","\u1E98":"w","\u1E89":"w","\u2C73":"w","\u24E7":"x","\uFF58":"x","\u1E8B":"x","\u1E8D":"x","\u24E8":"y","\uFF59":"y","\u1EF3":"y","\u00FD":"y","\u0177":"y","\u1EF9":"y","\u0233":"y","\u1E8F":"y","\u00FF":"y","\u1EF7":"y","\u1E99":"y","\u1EF5":"y","\u01B4":"y","\u024F":"y","\u1EFF":"y","\u24E9":"z","\uFF5A":"z","\u017A":"z","\u1E91":"z","\u017C":"z","\u017E":"z","\u1E93":"z","\u1E95":"z","\u01B6":"z","\u0225":"z","\u0240":"z","\u2C6C":"z","\uA763":"z","\u0386":"\u0391","\u0388":"\u0395","\u0389":"\u0397","\u038A":"\u0399","\u03AA":"\u0399","\u038C":"\u039F","\u038E":"\u03A5","\u03AB":"\u03A5","\u038F":"\u03A9","\u03AC":"\u03B1","\u03AD":"\u03B5","\u03AE":"\u03B7","\u03AF":"\u03B9","\u03CA":"\u03B9","\u0390":"\u03B9","\u03CC":"\u03BF","\u03CD":"\u03C5","\u03CB":"\u03C5","\u03B0":"\u03C5","\u03C9":"\u03C9","\u03C2":"\u03C3"};

    $document = $(document);

    nextUid=(function() { var counter=1; return function() { return counter++; }; }());


    function reinsertElement(element) {
        var placeholder = $(document.createTextNode(''));

        element.before(placeholder);
        placeholder.before(element);
        placeholder.remove();
    }

    function stripDiacritics(str) {
        // Used 'uni range + named function' from http://jsperf.com/diacritics/18
        function match(a) {
            return DIACRITICS[a] || a;
        }

        return str.replace(/[^\u0000-\u007E]/g, match);
    }

    function indexOf(value, array) {
        var i = 0, l = array.length;
        for (; i < l; i = i + 1) {
            if (equal(value, array[i])) return i;
        }
        return -1;
    }

    function measureScrollbar () {
        var $template = $( MEASURE_SCROLLBAR_TEMPLATE );
        $template.appendTo(document.body);

        var dim = {
            width: $template.width() - $template[0].clientWidth,
            height: $template.height() - $template[0].clientHeight
        };
        $template.remove();

        return dim;
    }

    /**
     * Compares equality of a and b
     * @param a
     * @param b
     */
    function equal(a, b) {
        if (a === b) return true;
        if (a === undefined || b === undefined) return false;
        if (a === null || b === null) return false;
        // Check whether 'a' or 'b' is a string (primitive or object).
        // The concatenation of an empty string (+'') converts its argument to a string's primitive.
        if (a.constructor === String) return a+'' === b+''; // a+'' - in case 'a' is a String object
        if (b.constructor === String) return b+'' === a+''; // b+'' - in case 'b' is a String object
        return false;
    }

    /**
     * Splits the string into an array of values, transforming each value. An empty array is returned for nulls or empty
     * strings
     * @param string
     * @param separator
     */
    function splitVal(string, separator, transform) {
        var val, i, l;
        if (string === null || string.length < 1) return [];
        val = string.split(separator);
        for (i = 0, l = val.length; i < l; i = i + 1) val[i] = transform(val[i]);
        return val;
    }

    function getSideBorderPadding(element) {
        return element.outerWidth(false) - element.width();
    }

    function installKeyUpChangeEvent(element) {
        var key="keyup-change-value";
        element.on("keydown", function () {
            if ($.data(element, key) === undefined) {
                $.data(element, key, element.val());
            }
        });
        element.on("keyup", function () {
            var val= $.data(element, key);
            if (val !== undefined && element.val() !== val) {
                $.removeData(element, key);
                element.trigger("keyup-change");
            }
        });
    }


    /**
     * filters mouse events so an event is fired only if the mouse moved.
     *
     * filters out mouse events that occur when mouse is stationary but
     * the elements under the pointer are scrolled.
     */
    function installFilteredMouseMove(element) {
        element.on("mousemove", function (e) {
            var lastpos = lastMousePosition;
            if (lastpos === undefined || lastpos.x !== e.pageX || lastpos.y !== e.pageY) {
                $(e.target).trigger("mousemove-filtered", e);
            }
        });
    }

    /**
     * Debounces a function. Returns a function that calls the original fn function only if no invocations have been made
     * within the last quietMillis milliseconds.
     *
     * @param quietMillis number of milliseconds to wait before invoking fn
     * @param fn function to be debounced
     * @param ctx object to be used as this reference within fn
     * @return debounced version of fn
     */
    function debounce(quietMillis, fn, ctx) {
        ctx = ctx || undefined;
        var timeout;
        return function () {
            var args = arguments;
            window.clearTimeout(timeout);
            timeout = window.setTimeout(function() {
                fn.apply(ctx, args);
            }, quietMillis);
        };
    }

    function installDebouncedScroll(threshold, element) {
        var notify = debounce(threshold, function (e) { element.trigger("scroll-debounced", e);});
        element.on("scroll", function (e) {
            if (indexOf(e.target, element.get()) >= 0) notify(e);
        });
    }

    function focus($el) {
        if ($el[0] === document.activeElement) return;

        /* set the focus in a 0 timeout - that way the focus is set after the processing
            of the current event has finished - which seems like the only reliable way
            to set focus */
        window.setTimeout(function() {
            var el=$el[0], pos=$el.val().length, range;

            $el.focus();

            /* make sure el received focus so we do not error out when trying to manipulate the caret.
                sometimes modals or others listeners may steal it after its set */
            var isVisible = (el.offsetWidth > 0 || el.offsetHeight > 0);
            if (isVisible && el === document.activeElement) {

                /* after the focus is set move the caret to the end, necessary when we val()
                    just before setting focus */
                if(el.setSelectionRange)
                {
                    el.setSelectionRange(pos, pos);
                }
                else if (el.createTextRange) {
                    range = el.createTextRange();
                    range.collapse(false);
                    range.select();
                }
            }
        }, 0);
    }

    function getCursorInfo(el) {
        el = $(el)[0];
        var offset = 0;
        var length = 0;
        if ('selectionStart' in el) {
            offset = el.selectionStart;
            length = el.selectionEnd - offset;
        } else if ('selection' in document) {
            el.focus();
            var sel = document.selection.createRange();
            length = document.selection.createRange().text.length;
            sel.moveStart('character', -el.value.length);
            offset = sel.text.length - length;
        }
        return { offset: offset, length: length };
    }

    function killEvent(event) {
        event.preventDefault();
        event.stopPropagation();
    }
    function killEventImmediately(event) {
        event.preventDefault();
        event.stopImmediatePropagation();
    }

    function measureTextWidth(e) {
        if (!sizer){
            var style = e[0].currentStyle || window.getComputedStyle(e[0], null);
            sizer = $(document.createElement("div")).css({
                position: "absolute",
                left: "-10000px",
                top: "-10000px",
                display: "none",
                fontSize: style.fontSize,
                fontFamily: style.fontFamily,
                fontStyle: style.fontStyle,
                fontWeight: style.fontWeight,
                letterSpacing: style.letterSpacing,
                textTransform: style.textTransform,
                whiteSpace: "nowrap"
            });
            sizer.attr("class","select2-sizer");
            $(document.body).append(sizer);
        }
        sizer.text(e.val());
        return sizer.width();
    }

    function syncCssClasses(dest, src, adapter) {
        var classes, replacements = [], adapted;

        classes = $.trim(dest.attr("class"));

        if (classes) {
            classes = '' + classes; // for IE which returns object

            $(classes.split(/\s+/)).each2(function() {
                if (this.indexOf("select2-") === 0) {
                    replacements.push(this);
                }
            });
        }

        classes = $.trim(src.attr("class"));

        if (classes) {
            classes = '' + classes; // for IE which returns object

            $(classes.split(/\s+/)).each2(function() {
                if (this.indexOf("select2-") !== 0) {
                    adapted = adapter(this);

                    if (adapted) {
                        replacements.push(adapted);
                    }
                }
            });
        }

        dest.attr("class", replacements.join(" "));
    }


    function markMatch(text, term, markup, escapeMarkup) {
        var match=stripDiacritics(text.toUpperCase()).indexOf(stripDiacritics(term.toUpperCase())),
            tl=term.length;

        if (match<0) {
            markup.push(escapeMarkup(text));
            return;
        }

        markup.push(escapeMarkup(text.substring(0, match)));
        markup.push("<span class='select2-match'>");
        markup.push(escapeMarkup(text.substring(match, match + tl)));
        markup.push("</span>");
        markup.push(escapeMarkup(text.substring(match + tl, text.length)));
    }

    function defaultEscapeMarkup(markup) {
        var replace_map = {
            '\\': '&#92;',
            '&': '&amp;',
            '<': '&lt;',
            '>': '&gt;',
            '"': '&quot;',
            "'": '&#39;',
            "/": '&#47;'
        };

        return String(markup).replace(/[&<>"'\/\\]/g, function (match) {
            return replace_map[match];
        });
    }

    /**
     * Produces an ajax-based query function
     *
     * @param options object containing configuration parameters
     * @param options.params parameter map for the transport ajax call, can contain such options as cache, jsonpCallback, etc. see $.ajax
     * @param options.transport function that will be used to execute the ajax request. must be compatible with parameters supported by $.ajax
     * @param options.url url for the data
     * @param options.data a function(searchTerm, pageNumber, context) that should return an object containing query string parameters for the above url.
     * @param options.dataType request data type: ajax, jsonp, other datatypes supported by jQuery's $.ajax function or the transport function if specified
     * @param options.quietMillis (optional) milliseconds to wait before making the ajaxRequest, helps debounce the ajax function if invoked too often
     * @param options.results a function(remoteData, pageNumber, query) that converts data returned form the remote request to the format expected by Select2.
     *      The expected format is an object containing the following keys:
     *      results array of objects that will be used as choices
     *      more (optional) boolean indicating whether there are more results available
     *      Example: {results:[{id:1, text:'Red'},{id:2, text:'Blue'}], more:true}
     */
    function ajax(options) {
        var timeout, // current scheduled but not yet executed request
            handler = null,
            quietMillis = options.quietMillis || 100,
            ajaxUrl = options.url,
            self = this;

        return function (query) {
            window.clearTimeout(timeout);
            timeout = window.setTimeout(function () {
                var data = options.data, // ajax data function
                    url = ajaxUrl, // ajax url string or function
                    transport = options.transport || $.fn.select2.ajaxDefaults.transport,
                    // deprecated - to be removed in 4.0  - use params instead
                    deprecated = {
                        type: options.type || 'GET', // set type of request (GET or POST)
                        cache: options.cache || false,
                        jsonpCallback: options.jsonpCallback||undefined,
                        dataType: options.dataType||"json"
                    },
                    params = $.extend({}, $.fn.select2.ajaxDefaults.params, deprecated);

                data = data ? data.call(self, query.term, query.page, query.context) : null;
                url = (typeof url === 'function') ? url.call(self, query.term, query.page, query.context) : url;

                if (handler && typeof handler.abort === "function") { handler.abort(); }

                if (options.params) {
                    if ($.isFunction(options.params)) {
                        $.extend(params, options.params.call(self));
                    } else {
                        $.extend(params, options.params);
                    }
                }

                $.extend(params, {
                    url: url,
                    dataType: options.dataType,
                    data: data,
                    success: function (data) {
                        // TODO - replace query.page with query so users have access to term, page, etc.
                        // added query as third paramter to keep backwards compatibility
                        var results = options.results(data, query.page, query);
                        query.callback(results);
                    },
                    error: function(jqXHR, textStatus, errorThrown){
                        var results = {
                            hasError: true,
                            jqXHR: jqXHR,
                            textStatus: textStatus,
                            errorThrown: errorThrown
                        };

                        query.callback(results);
                    }
                });
                handler = transport.call(self, params);
            }, quietMillis);
        };
    }

    /**
     * Produces a query function that works with a local array
     *
     * @param options object containing configuration parameters. The options parameter can either be an array or an
     * object.
     *
     * If the array form is used it is assumed that it contains objects with 'id' and 'text' keys.
     *
     * If the object form is used it is assumed that it contains 'data' and 'text' keys. The 'data' key should contain
     * an array of objects that will be used as choices. These objects must contain at least an 'id' key. The 'text'
     * key can either be a String in which case it is expected that each element in the 'data' array has a key with the
     * value of 'text' which will be used to match choices. Alternatively, text can be a function(item) that can extract
     * the text.
     */
    function local(options) {
        var data = options, // data elements
            dataText,
            tmp,
            text = function (item) { return ""+item.text; }; // function used to retrieve the text portion of a data item that is matched against the search

         if ($.isArray(data)) {
            tmp = data;
            data = { results: tmp };
        }

         if ($.isFunction(data) === false) {
            tmp = data;
            data = function() { return tmp; };
        }

        var dataItem = data();
        if (dataItem.text) {
            text = dataItem.text;
            // if text is not a function we assume it to be a key name
            if (!$.isFunction(text)) {
                dataText = dataItem.text; // we need to store this in a separate variable because in the next step data gets reset and data.text is no longer available
                text = function (item) { return item[dataText]; };
            }
        }

        return function (query) {
            var t = query.term, filtered = { results: [] }, process;
            if (t === "") {
                query.callback(data());
                return;
            }

            process = function(datum, collection) {
                var group, attr;
                datum = datum[0];
                if (datum.children) {
                    group = {};
                    for (attr in datum) {
                        if (datum.hasOwnProperty(attr)) group[attr]=datum[attr];
                    }
                    group.children=[];
                    $(datum.children).each2(function(i, childDatum) { process(childDatum, group.children); });
                    if (group.children.length || query.matcher(t, text(group), datum)) {
                        collection.push(group);
                    }
                } else {
                    if (query.matcher(t, text(datum), datum)) {
                        collection.push(datum);
                    }
                }
            };

            $(data().results).each2(function(i, datum) { process(datum, filtered.results); });
            query.callback(filtered);
        };
    }

    // TODO javadoc
    function tags(data) {
        var isFunc = $.isFunction(data);
        return function (query) {
            var t = query.term, filtered = {results: []};
            var result = isFunc ? data(query) : data;
            if ($.isArray(result)) {
                $(result).each(function () {
                    var isObject = this.text !== undefined,
                        text = isObject ? this.text : this;
                    if (t === "" || query.matcher(t, text)) {
                        filtered.results.push(isObject ? this : {id: this, text: this});
                    }
                });
                query.callback(filtered);
            }
        };
    }

    /**
     * Checks if the formatter function should be used.
     *
     * Throws an error if it is not a function. Returns true if it should be used,
     * false if no formatting should be performed.
     *
     * @param formatter
     */
    function checkFormatter(formatter, formatterName) {
        if ($.isFunction(formatter)) return true;
        if (!formatter) return false;
        if (typeof(formatter) === 'string') return true;
        throw new Error(formatterName +" must be a string, function, or falsy value");
    }

  /**
   * Returns a given value
   * If given a function, returns its output
   *
   * @param val string|function
   * @param context value of "this" to be passed to function
   * @returns {*}
   */
    function evaluate(val, context) {
        if ($.isFunction(val)) {
            var args = Array.prototype.slice.call(arguments, 2);
            return val.apply(context, args);
        }
        return val;
    }

    function countResults(results) {
        var count = 0;
        $.each(results, function(i, item) {
            if (item.children) {
                count += countResults(item.children);
            } else {
                count++;
            }
        });
        return count;
    }

    /**
     * Default tokenizer. This function uses breaks the input on substring match of any string from the
     * opts.tokenSeparators array and uses opts.createSearchChoice to create the choice object. Both of those
     * two options have to be defined in order for the tokenizer to work.
     *
     * @param input text user has typed so far or pasted into the search field
     * @param selection currently selected choices
     * @param selectCallback function(choice) callback tho add the choice to selection
     * @param opts select2's opts
     * @return undefined/null to leave the current input unchanged, or a string to change the input to the returned value
     */
    function defaultTokenizer(input, selection, selectCallback, opts) {
        var original = input, // store the original so we can compare and know if we need to tell the search to update its text
            dupe = false, // check for whether a token we extracted represents a duplicate selected choice
            token, // token
            index, // position at which the separator was found
            i, l, // looping variables
            separator; // the matched separator

        if (!opts.createSearchChoice || !opts.tokenSeparators || opts.tokenSeparators.length < 1) return undefined;

        while (true) {
            index = -1;

            for (i = 0, l = opts.tokenSeparators.length; i < l; i++) {
                separator = opts.tokenSeparators[i];
                index = input.indexOf(separator);
                if (index >= 0) break;
            }

            if (index < 0) break; // did not find any token separator in the input string, bail

            token = input.substring(0, index);
            input = input.substring(index + separator.length);

            if (token.length > 0) {
                token = opts.createSearchChoice.call(this, token, selection);
                if (token !== undefined && token !== null && opts.id(token) !== undefined && opts.id(token) !== null) {
                    dupe = false;
                    for (i = 0, l = selection.length; i < l; i++) {
                        if (equal(opts.id(token), opts.id(selection[i]))) {
                            dupe = true; break;
                        }
                    }

                    if (!dupe) selectCallback(token);
                }
            }
        }

        if (original!==input) return input;
    }

    function cleanupJQueryElements() {
        var self = this;

        $.each(arguments, function (i, element) {
            self[element].remove();
            self[element] = null;
        });
    }

    /**
     * Creates a new class
     *
     * @param superClass
     * @param methods
     */
    function clazz(SuperClass, methods) {
        var constructor = function () {};
        constructor.prototype = new SuperClass;
        constructor.prototype.constructor = constructor;
        constructor.prototype.parent = SuperClass.prototype;
        constructor.prototype = $.extend(constructor.prototype, methods);
        return constructor;
    }

    AbstractSelect2 = clazz(Object, {

        // abstract
        bind: function (func) {
            var self = this;
            return function () {
                func.apply(self, arguments);
            };
        },

        // abstract
        init: function (opts) {
            var results, search, resultsSelector = ".select2-results";

            // prepare options
            this.opts = opts = this.prepareOpts(opts);

            this.id=opts.id;

            // destroy if called on an existing component
            if (opts.element.data("select2") !== undefined &&
                opts.element.data("select2") !== null) {
                opts.element.data("select2").destroy();
            }

            this.container = this.createContainer();

            this.liveRegion = $('.select2-hidden-accessible');
            if (this.liveRegion.length == 0) {
                this.liveRegion = $("<span>", {
                        role: "status",
                        "aria-live": "polite"
                    })
                    .addClass("select2-hidden-accessible")
                    .appendTo(document.body);
            }

            this.containerId="s2id_"+(opts.element.attr("id") || "autogen"+nextUid());
            this.containerEventName= this.containerId
                .replace(/([.])/g, '_')
                .replace(/([;&,\-\.\+\*\~':"\!\^#$%@\[\]\(\)=>\|])/g, '\\$1');
            this.container.attr("id", this.containerId);

            this.container.attr("title", opts.element.attr("title"));

            this.body = $(document.body);

            syncCssClasses(this.container, this.opts.element, this.opts.adaptContainerCssClass);

            this.container.attr("style", opts.element.attr("style"));
            this.container.css(evaluate(opts.containerCss, this.opts.element));
            this.container.addClass(evaluate(opts.containerCssClass, this.opts.element));

            this.elementTabIndex = this.opts.element.attr("tabindex");

            // swap container for the element
            this.opts.element
                .data("select2", this)
                .attr("tabindex", "-1")
                .before(this.container)
                .on("click.select2", killEvent); // do not leak click events

            this.container.data("select2", this);

            this.dropdown = this.container.find(".select2-drop");

            syncCssClasses(this.dropdown, this.opts.element, this.opts.adaptDropdownCssClass);

            this.dropdown.addClass(evaluate(opts.dropdownCssClass, this.opts.element));
            this.dropdown.data("select2", this);
            this.dropdown.on("click", killEvent);

            this.results = results = this.container.find(resultsSelector);
            this.search = search = this.container.find("input.select2-input");

            this.queryCount = 0;
            this.resultsPage = 0;
            this.context = null;

            // initialize the container
            this.initContainer();

            this.container.on("click", killEvent);

            installFilteredMouseMove(this.results);

            this.dropdown.on("mousemove-filtered", resultsSelector, this.bind(this.highlightUnderEvent));
            this.dropdown.on("touchstart touchmove touchend", resultsSelector, this.bind(function (event) {
                this._touchEvent = true;
                this.highlightUnderEvent(event);
            }));
            this.dropdown.on("touchmove", resultsSelector, this.bind(this.touchMoved));
            this.dropdown.on("touchstart touchend", resultsSelector, this.bind(this.clearTouchMoved));

            // Waiting for a click event on touch devices to select option and hide dropdown
            // otherwise click will be triggered on an underlying element
            this.dropdown.on('click', this.bind(function (event) {
                if (this._touchEvent) {
                    this._touchEvent = false;
                    this.selectHighlighted();
                }
            }));

            installDebouncedScroll(80, this.results);
            this.dropdown.on("scroll-debounced", resultsSelector, this.bind(this.loadMoreIfNeeded));

            // do not propagate change event from the search field out of the component
            $(this.container).on("change", ".select2-input", function(e) {e.stopPropagation();});
            $(this.dropdown).on("change", ".select2-input", function(e) {e.stopPropagation();});

            // if jquery.mousewheel plugin is installed we can prevent out-of-bounds scrolling of results via mousewheel
            if ($.fn.mousewheel) {
                results.mousewheel(function (e, delta, deltaX, deltaY) {
                    var top = results.scrollTop();
                    if (deltaY > 0 && top - deltaY <= 0) {
                        results.scrollTop(0);
                        killEvent(e);
                    } else if (deltaY < 0 && results.get(0).scrollHeight - results.scrollTop() + deltaY <= results.height()) {
                        results.scrollTop(results.get(0).scrollHeight - results.height());
                        killEvent(e);
                    }
                });
            }

            installKeyUpChangeEvent(search);
            search.on("keyup-change input paste", this.bind(this.updateResults));
            search.on("focus", function () { search.addClass("select2-focused"); });
            search.on("blur", function () { search.removeClass("select2-focused");});

            this.dropdown.on("mouseup", resultsSelector, this.bind(function (e) {
                if ($(e.target).closest(".select2-result-selectable").length > 0) {
                    this.highlightUnderEvent(e);
                    this.selectHighlighted(e);
                }
            }));

            // trap all mouse events from leaving the dropdown. sometimes there may be a modal that is listening
            // for mouse events outside of itself so it can close itself. since the dropdown is now outside the select2's
            // dom it will trigger the popup close, which is not what we want
            // focusin can cause focus wars between modals and select2 since the dropdown is outside the modal.
            this.dropdown.on("click mouseup mousedown touchstart touchend focusin", function (e) { e.stopPropagation(); });

            this.lastSearchTerm = undefined;

            if ($.isFunction(this.opts.initSelection)) {
                // initialize selection based on the current value of the source element
                this.initSelection();

                // if the user has provided a function that can set selection based on the value of the source element
                // we monitor the change event on the element and trigger it, allowing for two way synchronization
                this.monitorSource();
            }

            if (opts.maximumInputLength !== null) {
                this.search.attr("maxlength", opts.maximumInputLength);
            }

            var disabled = opts.element.prop("disabled");
            if (disabled === undefined) disabled = false;
            this.enable(!disabled);

            var readonly = opts.element.prop("readonly");
            if (readonly === undefined) readonly = false;
            this.readonly(readonly);

            // Calculate size of scrollbar
            scrollBarDimensions = scrollBarDimensions || measureScrollbar();

            this.autofocus = opts.element.prop("autofocus");
            opts.element.prop("autofocus", false);
            if (this.autofocus) this.focus();

            this.search.attr("placeholder", opts.searchInputPlaceholder);
        },

        // abstract
        destroy: function () {
            var element=this.opts.element, select2 = element.data("select2"), self = this;

            this.close();

            if (element.length && element[0].detachEvent && self._sync) {
                element.each(function () {
                    if (self._sync) {
                        this.detachEvent("onpropertychange", self._sync);
                    }
                });
            }
            if (this.propertyObserver) {
                this.propertyObserver.disconnect();
                this.propertyObserver = null;
            }
            this._sync = null;

            if (select2 !== undefined) {
                select2.container.remove();
                select2.liveRegion.remove();
                select2.dropdown.remove();
                element.removeData("select2")
                    .off(".select2");
                if (!element.is("input[type='hidden']")) {
                    element
                        .show()
                        .prop("autofocus", this.autofocus || false);
                    if (this.elementTabIndex) {
                        element.attr({tabindex: this.elementTabIndex});
                    } else {
                        element.removeAttr("tabindex");
                    }
                    element.show();
                } else {
                    element.css("display", "");
                }
            }

            cleanupJQueryElements.call(this,
                "container",
                "liveRegion",
                "dropdown",
                "results",
                "search"
            );
        },

        // abstract
        optionToData: function(element) {
            if (element.is("option")) {
                return {
                    id:element.prop("value"),
                    text:element.text(),
                    element: element.get(),
                    css: element.attr("class"),
                    disabled: element.prop("disabled"),
                    locked: equal(element.attr("locked"), "locked") || equal(element.data("locked"), true)
                };
            } else if (element.is("optgroup")) {
                return {
                    text:element.attr("label"),
                    children:[],
                    element: element.get(),
                    css: element.attr("class")
                };
            }
        },

        // abstract
        prepareOpts: function (opts) {
            var element, select, idKey, ajaxUrl, self = this;

            element = opts.element;

            if (element.get(0).tagName.toLowerCase() === "select") {
                this.select = select = opts.element;
            }

            if (select) {
                // these options are not allowed when attached to a select because they are picked up off the element itself
                $.each(["id", "multiple", "ajax", "query", "createSearchChoice", "initSelection", "data", "tags"], function () {
                    if (this in opts) {
                        throw new Error("Option '" + this + "' is not allowed for Select2 when attached to a <select> element.");
                    }
                });
            }

            opts.debug = opts.debug || $.fn.select2.defaults.debug;

            // Warnings for options renamed/removed in Select2 4.0.0
            // Only when it's enabled through debug mode
            if (opts.debug && console && console.warn) {
                // id was removed
                if (opts.id != null) {
                    console.warn(
                        'Select2: The `id` option has been removed in Select2 4.0.0, ' +
                        'consider renaming your `id` property or mapping the property before your data makes it to Select2. ' +
                        'You can read more at https://select2.github.io/announcements-4.0.html#changed-id'
                    );
                }

                // text was removed
                if (opts.text != null) {
                    console.warn(
                        'Select2: The `text` option has been removed in Select2 4.0.0, ' +
                        'consider renaming your `text` property or mapping the property before your data makes it to Select2. ' +
                        'You can read more at https://select2.github.io/announcements-4.0.html#changed-id'
                    );
                }

                // sortResults was renamed to results
                if (opts.sortResults != null) {
                    console.warn(
                        'Select2: the `sortResults` option has been renamed to `sorter` in Select2 4.0.0. '
                    );
                }

                // selectOnBlur was renamed to selectOnClose
                if (opts.selectOnBlur != null) {
                    console.warn(
                        'Select2: The `selectOnBlur` option has been renamed to `selectOnClose` in Select2 4.0.0.'
                    );
                }

                // ajax.results was renamed to ajax.processResults
                if (opts.ajax != null && opts.ajax.results != null) {
                    console.warn(
                        'Select2: The `ajax.results` option has been renamed to `ajax.processResults` in Select2 4.0.0.'
                    );
                }

                // format* options were renamed to language.*
                if (opts.formatNoResults != null) {
                    console.warn(
                        'Select2: The `formatNoResults` option has been renamed to `language.noResults` in Select2 4.0.0.'
                    );
                }
                if (opts.formatSearching != null) {
                    console.warn(
                        'Select2: The `formatSearching` option has been renamed to `language.searching` in Select2 4.0.0.'
                    );
                }
                if (opts.formatInputTooShort != null) {
                    console.warn(
                        'Select2: The `formatInputTooShort` option has been renamed to `language.inputTooShort` in Select2 4.0.0.'
                    );
                }
                if (opts.formatInputTooLong != null) {
                    console.warn(
                        'Select2: The `formatInputTooLong` option has been renamed to `language.inputTooLong` in Select2 4.0.0.'
                    );
                }
                if (opts.formatLoading != null) {
                    console.warn(
                        'Select2: The `formatLoading` option has been renamed to `language.loadingMore` in Select2 4.0.0.'
                    );
                }
                if (opts.formatSelectionTooBig != null) {
                    console.warn(
                        'Select2: The `formatSelectionTooBig` option has been renamed to `language.maximumSelected` in Select2 4.0.0.'
                    );
                }

                if (opts.element.data('select2Tags')) {
                    console.warn(
                        'Select2: The `data-select2-tags` attribute has been renamed to `data-tags` in Select2 4.0.0.'
                    );
                }
            }

            // Aliasing options renamed in Select2 4.0.0

            // data-select2-tags -> data-tags
            if (opts.element.data('tags') != null) {
                var elemTags = opts.element.data('tags');

                // data-tags should actually be a boolean
                if (!$.isArray(elemTags)) {
                    elemTags = [];
                }

                opts.element.data('select2Tags', elemTags);
            }

            // sortResults -> sorter
            if (opts.sorter != null) {
                opts.sortResults = opts.sorter;
            }

            // selectOnBlur -> selectOnClose
            if (opts.selectOnClose != null) {
                opts.selectOnBlur = opts.selectOnClose;
            }

            // ajax.results -> ajax.processResults
            if (opts.ajax != null) {
                if ($.isFunction(opts.ajax.processResults)) {
                    opts.ajax.results = opts.ajax.processResults;
                }
            }

            // Formatters/language options
            if (opts.language != null) {
                var lang = opts.language;

                // formatNoMatches -> language.noMatches
                if ($.isFunction(lang.noMatches)) {
                    opts.formatNoMatches = lang.noMatches;
                }

                // formatSearching -> language.searching
                if ($.isFunction(lang.searching)) {
                    opts.formatSearching = lang.searching;
                }

                // formatInputTooShort -> language.inputTooShort
                if ($.isFunction(lang.inputTooShort)) {
                    opts.formatInputTooShort = lang.inputTooShort;
                }

                // formatInputTooLong -> language.inputTooLong
                if ($.isFunction(lang.inputTooLong)) {
                    opts.formatInputTooLong = lang.inputTooLong;
                }

                // formatLoading -> language.loadingMore
                if ($.isFunction(lang.loadingMore)) {
                    opts.formatLoading = lang.loadingMore;
                }

                // formatSelectionTooBig -> language.maximumSelected
                if ($.isFunction(lang.maximumSelected)) {
                    opts.formatSelectionTooBig = lang.maximumSelected;
                }
            }

            opts = $.extend({}, {
                populateResults: function(container, results, query) {
                    var populate, id=this.opts.id, liveRegion=this.liveRegion;

                    populate=function(results, container, depth) {

                        var i, l, result, selectable, disabled, compound, node, label, innerContainer, formatted;

                        results = opts.sortResults(results, container, query);

                        // collect the created nodes for bulk append
                        var nodes = [];
                        for (i = 0, l = results.length; i < l; i = i + 1) {

                            result=results[i];

                            disabled = (result.disabled === true);
                            selectable = (!disabled) && (id(result) !== undefined);

                            compound=result.children && result.children.length > 0;

                            node=$("<li></li>");
                            node.addClass("select2-results-dept-"+depth);
                            node.addClass("select2-result");
                            node.addClass(selectable ? "select2-result-selectable" : "select2-result-unselectable");
                            if (disabled) { node.addClass("select2-disabled"); }
                            if (compound) { node.addClass("select2-result-with-children"); }
                            node.addClass(self.opts.formatResultCssClass(result));
                            node.attr("role", "presentation");

                            label=$(document.createElement("div"));
                            label.addClass("select2-result-label");
                            label.attr("id", "select2-result-label-" + nextUid());
                            label.attr("role", "option");

                            formatted=opts.formatResult(result, label, query, self.opts.escapeMarkup);
                            if (formatted!==undefined) {
                                label.html(formatted);
                                node.append(label);
                            }


                            if (compound) {
                                innerContainer=$("<ul></ul>");
                                innerContainer.addClass("select2-result-sub");
                                populate(result.children, innerContainer, depth+1);
                                node.append(innerContainer);
                            }

                            node.data("select2-data", result);
                            nodes.push(node[0]);
                        }

                        // bulk append the created nodes
                        container.append(nodes);
                        liveRegion.text(opts.formatMatches(results.length));
                    };

                    populate(results, container, 0);
                }
            }, $.fn.select2.defaults, opts);

            if (typeof(opts.id) !== "function") {
                idKey = opts.id;
                opts.id = function (e) { return e[idKey]; };
            }

            if ($.isArray(opts.element.data("select2Tags"))) {
                if ("tags" in opts) {
                    throw "tags specified as both an attribute 'data-select2-tags' and in options of Select2 " + opts.element.attr("id");
                }
                opts.tags=opts.element.data("select2Tags");
            }

            if (select) {
                opts.query = this.bind(function (query) {
                    var data = { results: [], more: false },
                        term = query.term,
                        children, placeholderOption, process;

                    process=function(element, collection) {
                        var group;
                        if (element.is("option")) {
                            if (query.matcher(term, element.text(), element)) {
                                collection.push(self.optionToData(element));
                            }
                        } else if (element.is("optgroup")) {
                            group=self.optionToData(element);
                            element.children().each2(function(i, elm) { process(elm, group.children); });
                            if (group.children.length>0) {
                                collection.push(group);
                            }
                        }
                    };

                    children=element.children();

                    // ignore the placeholder option if there is one
                    if (this.getPlaceholder() !== undefined && children.length > 0) {
                        placeholderOption = this.getPlaceholderOption();
                        if (placeholderOption) {
                            children=children.not(placeholderOption);
                        }
                    }

                    children.each2(function(i, elm) { process(elm, data.results); });

                    query.callback(data);
                });
                // this is needed because inside val() we construct choices from options and their id is hardcoded
                opts.id=function(e) { return e.id; };
            } else {
                if (!("query" in opts)) {
                    if ("ajax" in opts) {
                        ajaxUrl = opts.element.data("ajax-url");
                        if (ajaxUrl && ajaxUrl.length > 0) {
                            opts.ajax.url = ajaxUrl;
                        }
                        opts.query = ajax.call(opts.element, opts.ajax);
                    } else if ("data" in opts) {
                        opts.query = local(opts.data);
                    } else if ("tags" in opts) {
                        opts.query = tags(opts.tags);
                        if (opts.createSearchChoice === undefined) {
                            opts.createSearchChoice = function (term) { return {id: $.trim(term), text: $.trim(term)}; };
                        }
                        if (opts.initSelection === undefined) {
                            opts.initSelection = function (element, callback) {
                                var data = [];
                                $(splitVal(element.val(), opts.separator, opts.transformVal)).each(function () {
                                    var obj = { id: this, text: this },
                                        tags = opts.tags;
                                    if ($.isFunction(tags)) tags=tags();
                                    $(tags).each(function() { if (equal(this.id, obj.id)) { obj = this; return false; } });
                                    data.push(obj);
                                });

                                callback(data);
                            };
                        }
                    }
                }
            }
            if (typeof(opts.query) !== "function") {
                throw "query function not defined for Select2 " + opts.element.attr("id");
            }

            if (opts.createSearchChoicePosition === 'top') {
                opts.createSearchChoicePosition = function(list, item) { list.unshift(item); };
            }
            else if (opts.createSearchChoicePosition === 'bottom') {
                opts.createSearchChoicePosition = function(list, item) { list.push(item); };
            }
            else if (typeof(opts.createSearchChoicePosition) !== "function")  {
                throw "invalid createSearchChoicePosition option must be 'top', 'bottom' or a custom function";
            }

            return opts;
        },

        /**
         * Monitor the original element for changes and update select2 accordingly
         */
        // abstract
        monitorSource: function () {
            var el = this.opts.element, observer, self = this;

            el.on("change.select2", this.bind(function (e) {
                if (this.opts.element.data("select2-change-triggered") !== true) {
                    this.initSelection();
                }
            }));

            this._sync = this.bind(function () {

                // sync enabled state
                var disabled = el.prop("disabled");
                if (disabled === undefined) disabled = false;
                this.enable(!disabled);

                var readonly = el.prop("readonly");
                if (readonly === undefined) readonly = false;
                this.readonly(readonly);

                if (this.container) {
                    syncCssClasses(this.container, this.opts.element, this.opts.adaptContainerCssClass);
                    this.container.addClass(evaluate(this.opts.containerCssClass, this.opts.element));
                }

                if (this.dropdown) {
                    syncCssClasses(this.dropdown, this.opts.element, this.opts.adaptDropdownCssClass);
                    this.dropdown.addClass(evaluate(this.opts.dropdownCssClass, this.opts.element));
                }

            });

            // IE8-10 (IE9/10 won't fire propertyChange via attachEventListener)
            if (el.length && el[0].attachEvent) {
                el.each(function() {
                    this.attachEvent("onpropertychange", self._sync);
                });
            }

            // safari, chrome, firefox, IE11
            observer = window.MutationObserver || window.WebKitMutationObserver|| window.MozMutationObserver;
            if (observer !== undefined) {
                if (this.propertyObserver) { delete this.propertyObserver; this.propertyObserver = null; }
                this.propertyObserver = new observer(function (mutations) {
                    $.each(mutations, self._sync);
                });
                this.propertyObserver.observe(el.get(0), { attributes:true, subtree:false });
            }
        },

        // abstract
        triggerSelect: function(data) {
            var evt = $.Event("select2-selecting", { val: this.id(data), object: data, choice: data });
            this.opts.element.trigger(evt);
            return !evt.isDefaultPrevented();
        },

        /**
         * Triggers the change event on the source element
         */
        // abstract
        triggerChange: function (details) {

            details = details || {};
            details= $.extend({}, details, { type: "change", val: this.val() });
            // prevents recursive triggering
            this.opts.element.data("select2-change-triggered", true);
            this.opts.element.trigger(details);
            this.opts.element.data("select2-change-triggered", false);

            // some validation frameworks ignore the change event and listen instead to keyup, click for selects
            // so here we trigger the click event manually
            this.opts.element.click();

            // ValidationEngine ignores the change event and listens instead to blur
            // so here we trigger the blur event manually if so desired
            if (this.opts.blurOnChange)
                this.opts.element.blur();
        },

        //abstract
        isInterfaceEnabled: function()
        {
            return this.enabledInterface === true;
        },

        // abstract
        enableInterface: function() {
            var enabled = this._enabled && !this._readonly,
                disabled = !enabled;

            if (enabled === this.enabledInterface) return false;

            this.container.toggleClass("select2-container-disabled", disabled);
            this.close();
            this.enabledInterface = enabled;

            return true;
        },

        // abstract
        enable: function(enabled) {
            if (enabled === undefined) enabled = true;
            if (this._enabled === enabled) return;
            this._enabled = enabled;

            this.opts.element.prop("disabled", !enabled);
            this.enableInterface();
        },

        // abstract
        disable: function() {
            this.enable(false);
        },

        // abstract
        readonly: function(enabled) {
            if (enabled === undefined) enabled = false;
            if (this._readonly === enabled) return;
            this._readonly = enabled;

            this.opts.element.prop("readonly", enabled);
            this.enableInterface();
        },

        // abstract
        opened: function () {
            return (this.container) ? this.container.hasClass("select2-dropdown-open") : false;
        },

        // abstract
        positionDropdown: function() {
            var $dropdown = this.dropdown,
                container = this.container,
                offset = container.offset(),
                height = container.outerHeight(false),
                width = container.outerWidth(false),
                dropHeight = $dropdown.outerHeight(false),
                $window = $(window),
                windowWidth = $window.width(),
                windowHeight = $window.height(),
                viewPortRight = $window.scrollLeft() + windowWidth,
                viewportBottom = $window.scrollTop() + windowHeight,
                dropTop = offset.top + height,
                dropLeft = offset.left,
                enoughRoomBelow = dropTop + dropHeight <= viewportBottom,
                enoughRoomAbove = (offset.top - dropHeight) >= $window.scrollTop(),
                dropWidth = $dropdown.outerWidth(false),
                enoughRoomOnRight = function() {
                    return dropLeft + dropWidth <= viewPortRight;
                },
                enoughRoomOnLeft = function() {
                    return offset.left + viewPortRight + container.outerWidth(false)  > dropWidth;
                },
                aboveNow = $dropdown.hasClass("select2-drop-above"),
                bodyOffset,
                above,
                changeDirection,
                css,
                resultsListNode;

            // always prefer the current above/below alignment, unless there is not enough room
            if (aboveNow) {
                above = true;
                if (!enoughRoomAbove && enoughRoomBelow) {
                    changeDirection = true;
                    above = false;
                }
            } else {
                above = false;
                if (!enoughRoomBelow && enoughRoomAbove) {
                    changeDirection = true;
                    above = true;
                }
            }

            //if we are changing direction we need to get positions when dropdown is hidden;
            if (changeDirection) {
                $dropdown.hide();
                offset = this.container.offset();
                height = this.container.outerHeight(false);
                width = this.container.outerWidth(false);
                dropHeight = $dropdown.outerHeight(false);
                viewPortRight = $window.scrollLeft() + windowWidth;
                viewportBottom = $window.scrollTop() + windowHeight;
                dropTop = offset.top + height;
                dropLeft = offset.left;
                dropWidth = $dropdown.outerWidth(false);
                $dropdown.show();

                // fix so the cursor does not move to the left within the search-textbox in IE
                this.focusSearch();
            }

            if (this.opts.dropdownAutoWidth) {
                resultsListNode = $('.select2-results', $dropdown)[0];
                $dropdown.addClass('select2-drop-auto-width');
                $dropdown.css('width', '');
                // Add scrollbar width to dropdown if vertical scrollbar is present
                dropWidth = $dropdown.outerWidth(false) + (resultsListNode.scrollHeight === resultsListNode.clientHeight ? 0 : scrollBarDimensions.width);
                dropWidth > width ? width = dropWidth : dropWidth = width;
                dropHeight = $dropdown.outerHeight(false);
            }
            else {
                this.container.removeClass('select2-drop-auto-width');
            }

            //console.log("below/ droptop:", dropTop, "dropHeight", dropHeight, "sum", (dropTop+dropHeight)+" viewport bottom", viewportBottom, "enough?", enoughRoomBelow);
            //console.log("above/ offset.top", offset.top, "dropHeight", dropHeight, "top", (offset.top-dropHeight), "scrollTop", this.body.scrollTop(), "enough?", enoughRoomAbove);

            // fix positioning when body has an offset and is not position: static
            if (this.body.css('position') !== 'static') {
                bodyOffset = this.body.offset();
                dropTop -= bodyOffset.top;
                dropLeft -= bodyOffset.left;
            }

            if (!enoughRoomOnRight() && enoughRoomOnLeft()) {
                dropLeft = offset.left + this.container.outerWidth(false) - dropWidth;
            }

            css =  {
                left: dropLeft,
                width: width
            };

            if (above) {
                this.container.addClass("select2-drop-above");
                $dropdown.addClass("select2-drop-above");
                dropHeight = $dropdown.outerHeight(false);
                css.top = offset.top - dropHeight;
                css.bottom = 'auto';
            }
            else {
                css.top = dropTop;
                css.bottom = 'auto';
                this.container.removeClass("select2-drop-above");
                $dropdown.removeClass("select2-drop-above");
            }
            css = $.extend(css, evaluate(this.opts.dropdownCss, this.opts.element));

            $dropdown.css(css);
        },

        // abstract
        shouldOpen: function() {
            var event;

            if (this.opened()) return false;

            if (this._enabled === false || this._readonly === true) return false;

            event = $.Event("select2-opening");
            this.opts.element.trigger(event);
            return !event.isDefaultPrevented();
        },

        // abstract
        clearDropdownAlignmentPreference: function() {
            // clear the classes used to figure out the preference of where the dropdown should be opened
            this.container.removeClass("select2-drop-above");
            this.dropdown.removeClass("select2-drop-above");
        },

        /**
         * Opens the dropdown
         *
         * @return {Boolean} whether or not dropdown was opened. This method will return false if, for example,
         * the dropdown is already open, or if the 'open' event listener on the element called preventDefault().
         */
        // abstract
        open: function () {

            if (!this.shouldOpen()) return false;

            this.opening();

            // Only bind the document mousemove when the dropdown is visible
            $document.on("mousemove.select2Event", function (e) {
                lastMousePosition.x = e.pageX;
                lastMousePosition.y = e.pageY;
            });

            return true;
        },

        /**
         * Performs the opening of the dropdown
         */
        // abstract
        opening: function() {
            var cid = this.containerEventName,
                scroll = "scroll." + cid,
                resize = "resize."+cid,
                orient = "orientationchange."+cid,
                mask;

            this.container.addClass("select2-dropdown-open").addClass("select2-container-active");

            this.clearDropdownAlignmentPreference();

            if(this.dropdown[0] !== this.body.children().last()[0]) {
                this.dropdown.detach().appendTo(this.body);
            }

            // create the dropdown mask if doesn't already exist
            mask = $("#select2-drop-mask");
            if (mask.length === 0) {
                mask = $(document.createElement("div"));
                mask.attr("id","select2-drop-mask").attr("class","select2-drop-mask");
                mask.hide();
                mask.appendTo(this.body);
                mask.on("mousedown touchstart click", function (e) {
                    // Prevent IE from generating a click event on the body
                    reinsertElement(mask);

                    var dropdown = $("#select2-drop"), self;
                    if (dropdown.length > 0) {
                        self=dropdown.data("select2");
                        if (self.opts.selectOnBlur) {
                            self.selectHighlighted({noFocus: true});
                        }
                        self.close();
                        e.preventDefault();
                        e.stopPropagation();
                    }
                });
            }

            // ensure the mask is always right before the dropdown
            if (this.dropdown.prev()[0] !== mask[0]) {
                this.dropdown.before(mask);
            }

            // move the global id to the correct dropdown
            $("#select2-drop").removeAttr("id");
            this.dropdown.attr("id", "select2-drop");

            // show the elements
            mask.show();

            this.positionDropdown();
            this.dropdown.show();
            this.positionDropdown();

            this.dropdown.addClass("select2-drop-active");

            // attach listeners to events that can change the position of the container and thus require
            // the position of the dropdown to be updated as well so it does not come unglued from the container
            var that = this;
            this.container.parents().add(window).each(function () {
                $(this).on(resize+" "+scroll+" "+orient, function (e) {
                    if (that.opened()) that.positionDropdown();
                });
            });


        },

        // abstract
        close: function () {
            if (!this.opened()) return;

            var cid = this.containerEventName,
                scroll = "scroll." + cid,
                resize = "resize."+cid,
                orient = "orientationchange."+cid;

            // unbind event listeners
            this.container.parents().add(window).each(function () { $(this).off(scroll).off(resize).off(orient); });

            this.clearDropdownAlignmentPreference();

            $("#select2-drop-mask").hide();
            this.dropdown.removeAttr("id"); // only the active dropdown has the select2-drop id
            this.dropdown.hide();
            this.container.removeClass("select2-dropdown-open").removeClass("select2-container-active");
            this.results.empty();

            // Now that the dropdown is closed, unbind the global document mousemove event
            $document.off("mousemove.select2Event");

            this.clearSearch();
            this.search.removeClass("select2-active");

            // Remove the aria active descendant for highlighted element
            this.search.removeAttr("aria-activedescendant");
            this.opts.element.trigger($.Event("select2-close"));
        },

        /**
         * Opens control, sets input value, and updates results.
         */
        // abstract
        externalSearch: function (term) {
            this.open();
            this.search.val(term);
            this.updateResults(false);
        },

        // abstract
        clearSearch: function () {

        },

        /**
         * @return {Boolean} Whether or not search value was changed.
         * @private
         */
        prefillNextSearchTerm: function () {
            // initializes search's value with nextSearchTerm (if defined by user)
            // ignore nextSearchTerm if the dropdown is opened by the user pressing a letter
            if(this.search.val() !== "") {
                return false;
            }

            var nextSearchTerm = this.opts.nextSearchTerm(this.data(), this.lastSearchTerm);
            if(nextSearchTerm !== undefined){
                this.search.val(nextSearchTerm);
                this.search.select();
                return true;
            }

            return false;
        },

        //abstract
        getMaximumSelectionSize: function() {
            return evaluate(this.opts.maximumSelectionSize, this.opts.element);
        },

        // abstract
        ensureHighlightVisible: function () {
            var results = this.results, children, index, child, hb, rb, y, more, topOffset;

            index = this.highlight();

            if (index < 0) return;

            if (index == 0) {

                // if the first element is highlighted scroll all the way to the top,
                // that way any unselectable headers above it will also be scrolled
                // into view

                results.scrollTop(0);
                return;
            }

            children = this.findHighlightableChoices().find('.select2-result-label');

            child = $(children[index]);

            topOffset = (child.offset() || {}).top || 0;

            hb = topOffset + child.outerHeight(true);

            // if this is the last child lets also make sure select2-more-results is visible
            if (index === children.length - 1) {
                more = results.find("li.select2-more-results");
                if (more.length > 0) {
                    hb = more.offset().top + more.outerHeight(true);
                }
            }

            rb = results.offset().top + results.outerHeight(false);
            if (hb > rb) {
                results.scrollTop(results.scrollTop() + (hb - rb));
            }
            y = topOffset - results.offset().top;

            // make sure the top of the element is visible
            if (y < 0 && child.css('display') != 'none' ) {
                results.scrollTop(results.scrollTop() + y); // y is negative
            }
        },

        // abstract
        findHighlightableChoices: function() {
            return this.results.find(".select2-result-selectable:not(.select2-disabled):not(.select2-selected)");
        },

        // abstract
        moveHighlight: function (delta) {
            var choices = this.findHighlightableChoices(),
                index = this.highlight();

            while (index > -1 && index < choices.length) {
                index += delta;
                var choice = $(choices[index]);
                if (choice.hasClass("select2-result-selectable") && !choice.hasClass("select2-disabled") && !choice.hasClass("select2-selected")) {
                    this.highlight(index);
                    break;
                }
            }
        },

        // abstract
        highlight: function (index) {
            var choices = this.findHighlightableChoices(),
                choice,
                data;

            if (arguments.length === 0) {
                return indexOf(choices.filter(".select2-highlighted")[0], choices.get());
            }

            if (index >= choices.length) index = choices.length - 1;
            if (index < 0) index = 0;

            this.removeHighlight();

            choice = $(choices[index]);
            choice.addClass("select2-highlighted");

            // ensure assistive technology can determine the active choice
            this.search.attr("aria-activedescendant", choice.find(".select2-result-label").attr("id"));

            this.ensureHighlightVisible();

            this.liveRegion.text(choice.text());

            data = choice.data("select2-data");
            if (data) {
                this.opts.element.trigger({ type: "select2-highlight", val: this.id(data), choice: data });
            }
        },

        removeHighlight: function() {
            this.results.find(".select2-highlighted").removeClass("select2-highlighted");
        },

        touchMoved: function() {
            this._touchMoved = true;
        },

        clearTouchMoved: function() {
          this._touchMoved = false;
        },

        // abstract
        countSelectableResults: function() {
            return this.findHighlightableChoices().length;
        },

        // abstract
        highlightUnderEvent: function (event) {
            var el = $(event.target).closest(".select2-result-selectable");
            if (el.length > 0 && !el.is(".select2-highlighted")) {
                var choices = this.findHighlightableChoices();
                this.highlight(choices.index(el));
            } else if (el.length == 0) {
                // if we are over an unselectable item remove all highlights
                this.removeHighlight();
            }
        },

        // abstract
        loadMoreIfNeeded: function () {
            var results = this.results,
                more = results.find("li.select2-more-results"),
                below, // pixels the element is below the scroll fold, below==0 is when the element is starting to be visible
                page = this.resultsPage + 1,
                self=this,
                term=this.search.val(),
                context=this.context;

            if (more.length === 0) return;
            below = more.offset().top - results.offset().top - results.height();

            if (below <= this.opts.loadMorePadding) {
                more.addClass("select2-active");
                this.opts.query({
                        element: this.opts.element,
                        term: term,
                        page: page,
                        context: context,
                        matcher: this.opts.matcher,
                        callback: this.bind(function (data) {

                    // ignore a response if the select2 has been closed before it was received
                    if (!self.opened()) return;


                    self.opts.populateResults.call(this, results, data.results, {term: term, page: page, context:context});
                    self.postprocessResults(data, false, false);

                    if (data.more===true) {
                        more.detach().appendTo(results).html(self.opts.escapeMarkup(evaluate(self.opts.formatLoadMore, self.opts.element, page+1)));
                        window.setTimeout(function() { self.loadMoreIfNeeded(); }, 10);
                    } else {
                        more.remove();
                    }
                    self.positionDropdown();
                    self.resultsPage = page;
                    self.context = data.context;
                    this.opts.element.trigger({ type: "select2-loaded", items: data });
                })});
            }
        },

        /**
         * Default tokenizer function which does nothing
         */
        tokenize: function() {

        },

        /**
         * @param initial whether or not this is the call to this method right after the dropdown has been opened
         */
        // abstract
        updateResults: function (initial) {
            var search = this.search,
                results = this.results,
                opts = this.opts,
                data,
                self = this,
                input,
                term = search.val(),
                lastTerm = $.data(this.container, "select2-last-term"),
                // sequence number used to drop out-of-order responses
                queryNumber;

            // prevent duplicate queries against the same term
            if (initial !== true && lastTerm && equal(term, lastTerm)) return;

            $.data(this.container, "select2-last-term", term);

            // if the search is currently hidden we do not alter the results
            if (initial !== true && (this.showSearchInput === false || !this.opened())) {
                return;
            }

            function postRender() {
                search.removeClass("select2-active");
                self.positionDropdown();
                if (results.find('.select2-no-results,.select2-selection-limit,.select2-searching').length) {
                    self.liveRegion.text(results.text());
                }
                else {
                    self.liveRegion.text(self.opts.formatMatches(results.find('.select2-result-selectable:not(".select2-selected")').length));
                }
            }

            function render(html) {
                results.html(html);
                postRender();
            }

            queryNumber = ++this.queryCount;

            var maxSelSize = this.getMaximumSelectionSize();
            if (maxSelSize >=1) {
                data = this.data();
                if ($.isArray(data) && data.length >= maxSelSize && checkFormatter(opts.formatSelectionTooBig, "formatSelectionTooBig")) {
                    render("<li class='select2-selection-limit'>" + evaluate(opts.formatSelectionTooBig, opts.element, maxSelSize) + "</li>");
                    return;
                }
            }

            if (search.val().length < opts.minimumInputLength) {
                if (checkFormatter(opts.formatInputTooShort, "formatInputTooShort")) {
                    render("<li class='select2-no-results'>" + evaluate(opts.formatInputTooShort, opts.element, search.val(), opts.minimumInputLength) + "</li>");
                } else {
                    render("");
                }
                if (initial && this.showSearch) this.showSearch(true);
                return;
            }

            if (opts.maximumInputLength && search.val().length > opts.maximumInputLength) {
                if (checkFormatter(opts.formatInputTooLong, "formatInputTooLong")) {
                    render("<li class='select2-no-results'>" + evaluate(opts.formatInputTooLong, opts.element, search.val(), opts.maximumInputLength) + "</li>");
                } else {
                    render("");
                }
                return;
            }

            if (opts.formatSearching && this.findHighlightableChoices().length === 0) {
                render("<li class='select2-searching'>" + evaluate(opts.formatSearching, opts.element) + "</li>");
            }

            search.addClass("select2-active");

            this.removeHighlight();

            // give the tokenizer a chance to pre-process the input
            input = this.tokenize();
            if (input != undefined && input != null) {
                search.val(input);
            }

            this.resultsPage = 1;

            opts.query({
                element: opts.element,
                    term: search.val(),
                    page: this.resultsPage,
                    context: null,
                    matcher: opts.matcher,
                    callback: this.bind(function (data) {
                var def; // default choice

                // ignore old responses
                if (queryNumber != this.queryCount) {
                  return;
                }

                // ignore a response if the select2 has been closed before it was received
                if (!this.opened()) {
                    this.search.removeClass("select2-active");
                    return;
                }

                // handle ajax error
                if(data.hasError !== undefined && checkFormatter(opts.formatAjaxError, "formatAjaxError")) {
                    render("<li class='select2-ajax-error'>" + evaluate(opts.formatAjaxError, opts.element, data.jqXHR, data.textStatus, data.errorThrown) + "</li>");
                    return;
                }

                // save context, if any
                this.context = (data.context===undefined) ? null : data.context;
                // create a default choice and prepend it to the list
                if (this.opts.createSearchChoice && search.val() !== "") {
                    def = this.opts.createSearchChoice.call(self, search.val(), data.results);
                    if (def !== undefined && def !== null && self.id(def) !== undefined && self.id(def) !== null) {
                        if ($(data.results).filter(
                            function () {
                                return equal(self.id(this), self.id(def));
                            }).length === 0) {
                            this.opts.createSearchChoicePosition(data.results, def);
                        }
                    }
                }

                if (data.results.length === 0 && checkFormatter(opts.formatNoMatches, "formatNoMatches")) {
                    render("<li class='select2-no-results'>" + evaluate(opts.formatNoMatches, opts.element, search.val()) + "</li>");
                    if(this.showSearch){
                        this.showSearch(search.val());
                    }
                    return;
                }

                results.empty();
                self.opts.populateResults.call(this, results, data.results, {term: search.val(), page: this.resultsPage, context:null});

                if (data.more === true && checkFormatter(opts.formatLoadMore, "formatLoadMore")) {
                    results.append("<li class='select2-more-results'>" + opts.escapeMarkup(evaluate(opts.formatLoadMore, opts.element, this.resultsPage)) + "</li>");
                    window.setTimeout(function() { self.loadMoreIfNeeded(); }, 10);
                }

                this.postprocessResults(data, initial);

                postRender();

                this.opts.element.trigger({ type: "select2-loaded", items: data });
            })});
        },

        // abstract
        cancel: function () {
            this.close();
        },

        // abstract
        blur: function () {
            // if selectOnBlur == true, select the currently highlighted option
            if (this.opts.selectOnBlur)
                this.selectHighlighted({noFocus: true});

            this.close();
            this.container.removeClass("select2-container-active");
            // synonymous to .is(':focus'), which is available in jquery >= 1.6
            if (this.search[0] === document.activeElement) { this.search.blur(); }
            this.clearSearch();
            this.selection.find(".select2-search-choice-focus").removeClass("select2-search-choice-focus");
        },

        // abstract
        focusSearch: function () {
            focus(this.search);
        },

        // abstract
        selectHighlighted: function (options) {
            if (this._touchMoved) {
              this.clearTouchMoved();
              return;
            }
            var index=this.highlight(),
                highlighted=this.results.find(".select2-highlighted"),
                data = highlighted.closest('.select2-result').data("select2-data");

            if (data) {
                this.highlight(index);
                this.onSelect(data, options);
            } else if (options && options.noFocus) {
                this.close();
            }
        },

        // abstract
        getPlaceholder: function () {
            var placeholderOption;
            return this.opts.element.attr("placeholder") ||
                this.opts.element.attr("data-placeholder") || // jquery 1.4 compat
                this.opts.element.data("placeholder") ||
                this.opts.placeholder ||
                ((placeholderOption = this.getPlaceholderOption()) !== undefined ? placeholderOption.text() : undefined);
        },

        // abstract
        getPlaceholderOption: function() {
            if (this.select) {
                var firstOption = this.select.children('option').first();
                if (this.opts.placeholderOption !== undefined ) {
                    //Determine the placeholder option based on the specified placeholderOption setting
                    return (this.opts.placeholderOption === "first" && firstOption) ||
                           (typeof this.opts.placeholderOption === "function" && this.opts.placeholderOption(this.select));
                } else if ($.trim(firstOption.text()) === "" && firstOption.val() === "") {
                    //No explicit placeholder option specified, use the first if it's blank
                    return firstOption;
                }
            }
        },

        /**
         * Get the desired width for the container element.  This is
         * derived first from option `width` passed to select2, then
         * the inline 'style' on the original element, and finally
         * falls back to the jQuery calculated element width.
         */
        // abstract
        initContainerWidth: function () {
            function resolveContainerWidth() {
                var style, attrs, matches, i, l, attr;

                if (this.opts.width === "off") {
                    return null;
                } else if (this.opts.width === "element"){
                    return this.opts.element.outerWidth(false) === 0 ? 'auto' : this.opts.element.outerWidth(false) + 'px';
                } else if (this.opts.width === "copy" || this.opts.width === "resolve") {
                    // check if there is inline style on the element that contains width
                    style = this.opts.element.attr('style');
                    if (typeof(style) === "string") {
                        attrs = style.split(';');
                        for (i = 0, l = attrs.length; i < l; i = i + 1) {
                            attr = attrs[i].replace(/\s/g, '');
                            matches = attr.match(/^width:(([-+]?([0-9]*\.)?[0-9]+)(px|em|ex|%|in|cm|mm|pt|pc))/i);
                            if (matches !== null && matches.length >= 1)
                                return matches[1];
                        }
                    }

                    if (this.opts.width === "resolve") {
                        // next check if css('width') can resolve a width that is percent based, this is sometimes possible
                        // when attached to input type=hidden or elements hidden via css
                        style = this.opts.element.css('width');
                        if (style.indexOf("%") > 0) return style;

                        // finally, fallback on the calculated width of the element
                        return (this.opts.element.outerWidth(false) === 0 ? 'auto' : this.opts.element.outerWidth(false) + 'px');
                    }

                    return null;
                } else if ($.isFunction(this.opts.width)) {
                    return this.opts.width();
                } else {
                    return this.opts.width;
               }
            };

            var width = resolveContainerWidth.call(this);
            if (width !== null) {
                this.container.css("width", width);
            }
        }
    });

    SingleSelect2 = clazz(AbstractSelect2, {

        // single

        createContainer: function () {
            var container = $(document.createElement("div")).attr({
                "class": "select2-container"
            }).html([
                "<a href='javascript:void(0)' class='select2-choice' tabindex='-1'>",
                "   <span class='select2-chosen'>&#160;</span><abbr class='select2-search-choice-close'></abbr>",
                "   <span class='select2-arrow' role='presentation'><b role='presentation'></b></span>",
                "</a>",
                "<label for='' class='select2-offscreen'></label>",
                "<input class='select2-focusser select2-offscreen' type='text' aria-haspopup='true' role='button' />",
                "<div class='select2-drop select2-display-none'>",
                "   <div class='select2-search'>",
                "       <label for='' class='select2-offscreen'></label>",
                "       <input type='text' autocomplete='off' autocorrect='off' autocapitalize='off' spellcheck='false' class='select2-input' role='combobox' aria-expanded='true'",
                "       aria-autocomplete='list' />",
                "   </div>",
                "   <ul class='select2-results' role='listbox'>",
                "   </ul>",
                "</div>"].join(""));
            return container;
        },

        // single
        enableInterface: function() {
            if (this.parent.enableInterface.apply(this, arguments)) {
                this.focusser.prop("disabled", !this.isInterfaceEnabled());
            }
        },

        // single
        opening: function () {
            var el, range, len;

            if (this.opts.minimumResultsForSearch >= 0) {
                this.showSearch(true);
            }

            this.parent.opening.apply(this, arguments);

            if (this.showSearchInput !== false) {
                // IE appends focusser.val() at the end of field :/ so we manually insert it at the beginning using a range
                // all other browsers handle this just fine

                this.search.val(this.focusser.val());
            }
            if (this.opts.shouldFocusInput(this)) {
                this.search.focus();
                // move the cursor to the end after focussing, otherwise it will be at the beginning and
                // new text will appear *before* focusser.val()
                el = this.search.get(0);
                if (el.createTextRange) {
                    range = el.createTextRange();
                    range.collapse(false);
                    range.select();
                } else if (el.setSelectionRange) {
                    len = this.search.val().length;
                    el.setSelectionRange(len, len);
                }
            }

            this.prefillNextSearchTerm();

            this.focusser.prop("disabled", true).val("");
            this.updateResults(true);
            this.opts.element.trigger($.Event("select2-open"));
        },

        // single
        close: function () {
            if (!this.opened()) return;
            this.parent.close.apply(this, arguments);

            this.focusser.prop("disabled", false);

            if (this.opts.shouldFocusInput(this)) {
                this.focusser.focus();
            }
        },

        // single
        focus: function () {
            if (this.opened()) {
                this.close();
            } else {
                this.focusser.prop("disabled", false);
                if (this.opts.shouldFocusInput(this)) {
                    this.focusser.focus();
                }
            }
        },

        // single
        isFocused: function () {
            return this.container.hasClass("select2-container-active");
        },

        // single
        cancel: function () {
            this.parent.cancel.apply(this, arguments);
            this.focusser.prop("disabled", false);

            if (this.opts.shouldFocusInput(this)) {
                this.focusser.focus();
            }
        },

        // single
        destroy: function() {
            $("label[for='" + this.focusser.attr('id') + "']")
                .attr('for', this.opts.element.attr("id"));
            this.parent.destroy.apply(this, arguments);

            cleanupJQueryElements.call(this,
                "selection",
                "focusser"
            );
        },

        // single
        initContainer: function () {

            var selection,
                container = this.container,
                dropdown = this.dropdown,
                idSuffix = nextUid(),
                elementLabel;

            if (this.opts.minimumResultsForSearch < 0) {
                this.showSearch(false);
            } else {
                this.showSearch(true);
            }

            this.selection = selection = container.find(".select2-choice");

            this.focusser = container.find(".select2-focusser");

            // add aria associations
            selection.find(".select2-chosen").attr("id", "select2-chosen-"+idSuffix);
            this.focusser.attr("aria-labelledby", "select2-chosen-"+idSuffix);
            this.results.attr("id", "select2-results-"+idSuffix);
            this.search.attr("aria-owns", "select2-results-"+idSuffix);

            // rewrite labels from original element to focusser
            this.focusser.attr("id", "s2id_autogen"+idSuffix);

            elementLabel = $("label[for='" + this.opts.element.attr("id") + "']");
            this.opts.element.on('focus.select2', this.bind(function () { this.focus(); }));

            this.focusser.prev()
                .text(elementLabel.text())
                .attr('for', this.focusser.attr('id'));

            // Ensure the original element retains an accessible name
            var originalTitle = this.opts.element.attr("title");
            this.opts.element.attr("title", (originalTitle || elementLabel.text()));

            this.focusser.attr("tabindex", this.elementTabIndex);

            // write label for search field using the label from the focusser element
            this.search.attr("id", this.focusser.attr('id') + '_search');

            this.search.prev()
                .text($("label[for='" + this.focusser.attr('id') + "']").text())
                .attr('for', this.search.attr('id'));

            this.search.on("keydown", this.bind(function (e) {
                if (!this.isInterfaceEnabled()) return;

                // filter 229 keyCodes (input method editor is processing key input)
                if (229 == e.keyCode) return;

                if (e.which === KEY.PAGE_UP || e.which === KEY.PAGE_DOWN) {
                    // prevent the page from scrolling
                    killEvent(e);
                    return;
                }

                switch (e.which) {
                    case KEY.UP:
                    case KEY.DOWN:
                        this.moveHighlight((e.which === KEY.UP) ? -1 : 1);
                        killEvent(e);
                        return;
                    case KEY.ENTER:
                        this.selectHighlighted();
                        killEvent(e);
                        return;
                    case KEY.TAB:
                        this.selectHighlighted({noFocus: true});
                        return;
                    case KEY.ESC:
                        this.cancel(e);
                        killEvent(e);
                        return;
                }
            }));

            this.search.on("blur", this.bind(function(e) {
                // a workaround for chrome to keep the search field focussed when the scroll bar is used to scroll the dropdown.
                // without this the search field loses focus which is annoying
                if (document.activeElement === this.body.get(0)) {
                    window.setTimeout(this.bind(function() {
                        if (this.opened() && this.results && this.results.length > 1) {
                            this.search.focus();
                        }
                    }), 0);
                }
            }));

            this.focusser.on("keydown", this.bind(function (e) {
                if (!this.isInterfaceEnabled()) return;

                if (e.which === KEY.TAB || KEY.isControl(e) || KEY.isFunctionKey(e) || e.which === KEY.ESC) {
                    return;
                }

                if (this.opts.openOnEnter === false && e.which === KEY.ENTER) {
                    killEvent(e);
                    return;
                }

                if (e.which == KEY.DOWN || e.which == KEY.UP
                    || (e.which == KEY.ENTER && this.opts.openOnEnter)) {

                    if (e.altKey || e.ctrlKey || e.shiftKey || e.metaKey) return;

                    this.open();
                    killEvent(e);
                    return;
                }

                if (e.which == KEY.DELETE || e.which == KEY.BACKSPACE) {
                    if (this.opts.allowClear) {
                        this.clear();
                    }
                    killEvent(e);
                    return;
                }
            }));


            installKeyUpChangeEvent(this.focusser);
            this.focusser.on("keyup-change input", this.bind(function(e) {
                if (this.opts.minimumResultsForSearch >= 0) {
                    e.stopPropagation();
                    if (this.opened()) return;
                    this.open();
                }
            }));

            selection.on("mousedown touchstart", "abbr", this.bind(function (e) {
                if (!this.isInterfaceEnabled()) {
                    return;
                }

                this.clear();
                killEventImmediately(e);
                this.close();

                if (this.selection) {
                    this.selection.focus();
                }
            }));

            selection.on("mousedown touchstart", this.bind(function (e) {
                // Prevent IE from generating a click event on the body
                reinsertElement(selection);

                if (!this.container.hasClass("select2-container-active")) {
                    this.opts.element.trigger($.Event("select2-focus"));
                }

                if (this.opened()) {
                    this.close();
                } else if (this.isInterfaceEnabled()) {
                    this.open();
                }

                killEvent(e);
            }));

            dropdown.on("mousedown touchstart", this.bind(function() {
                if (this.opts.shouldFocusInput(this)) {
                    this.search.focus();
                }
            }));

            selection.on("focus", this.bind(function(e) {
                killEvent(e);
            }));

            this.focusser.on("focus", this.bind(function(){
                if (!this.container.hasClass("select2-container-active")) {
                    this.opts.element.trigger($.Event("select2-focus"));
                }
                this.container.addClass("select2-container-active");
            })).on("blur", this.bind(function() {
                if (!this.opened()) {
                    this.container.removeClass("select2-container-active");
                    this.opts.element.trigger($.Event("select2-blur"));
                }
            }));
            this.search.on("focus", this.bind(function(){
                if (!this.container.hasClass("select2-container-active")) {
                    this.opts.element.trigger($.Event("select2-focus"));
                }
                this.container.addClass("select2-container-active");
            }));

            this.initContainerWidth();
            this.opts.element.hide();
            this.setPlaceholder();

        },

        // single
        clear: function(triggerChange) {
            var data=this.selection.data("select2-data");
            if (data) { // guard against queued quick consecutive clicks
                var evt = $.Event("select2-clearing");
                this.opts.element.trigger(evt);
                if (evt.isDefaultPrevented()) {
                    return;
                }
                var placeholderOption = this.getPlaceholderOption();
                this.opts.element.val(placeholderOption ? placeholderOption.val() : "");
                this.selection.find(".select2-chosen").empty();
                this.selection.removeData("select2-data");
                this.setPlaceholder();

                if (triggerChange !== false){
                    this.opts.element.trigger({ type: "select2-removed", val: this.id(data), choice: data });
                    this.triggerChange({removed:data});
                }
            }
        },

        /**
         * Sets selection based on source element's value
         */
        // single
        initSelection: function () {
            var selected;
            if (this.isPlaceholderOptionSelected()) {
                this.updateSelection(null);
                this.close();
                this.setPlaceholder();
            } else {
                var self = this;
                this.opts.initSelection.call(null, this.opts.element, function(selected){
                    if (selected !== undefined && selected !== null) {
                        self.updateSelection(selected);
                        self.close();
                        self.setPlaceholder();
                        self.lastSearchTerm = self.search.val();
                    }
                });
            }
        },

        isPlaceholderOptionSelected: function() {
            var placeholderOption;
            if (this.getPlaceholder() === undefined) return false; // no placeholder specified so no option should be considered
            return ((placeholderOption = this.getPlaceholderOption()) !== undefined && placeholderOption.prop("selected"))
                || (this.opts.element.val() === "")
                || (this.opts.element.val() === undefined)
                || (this.opts.element.val() === null);
        },

        // single
        prepareOpts: function () {
            var opts = this.parent.prepareOpts.apply(this, arguments),
                self=this;

            if (opts.element.get(0).tagName.toLowerCase() === "select") {
                // install the selection initializer
                opts.initSelection = function (element, callback) {
                    var selected = element.find("option").filter(function() { return this.selected && !this.disabled });
                    // a single select box always has a value, no need to null check 'selected'
                    callback(self.optionToData(selected));
                };
            } else if ("data" in opts) {
                // install default initSelection when applied to hidden input and data is local
                opts.initSelection = opts.initSelection || function (element, callback) {
                    var id = element.val();
                    //search in data by id, storing the actual matching item
                    var match = null;
                    opts.query({
                        matcher: function(term, text, el){
                            var is_match = equal(id, opts.id(el));
                            if (is_match) {
                                match = el;
                            }
                            return is_match;
                        },
                        callback: !$.isFunction(callback) ? $.noop : function() {
                            callback(match);
                        }
                    });
                };
            }

            return opts;
        },

        // single
        getPlaceholder: function() {
            // if a placeholder is specified on a single select without a valid placeholder option ignore it
            if (this.select) {
                if (this.getPlaceholderOption() === undefined) {
                    return undefined;
                }
            }

            return this.parent.getPlaceholder.apply(this, arguments);
        },

        // single
        setPlaceholder: function () {
            var placeholder = this.getPlaceholder();

            if (this.isPlaceholderOptionSelected() && placeholder !== undefined) {

                // check for a placeholder option if attached to a select
                if (this.select && this.getPlaceholderOption() === undefined) return;

                this.selection.find(".select2-chosen").html(this.opts.escapeMarkup(placeholder));

                this.selection.addClass("select2-default");

                this.container.removeClass("select2-allowclear");
            }
        },

        // single
        postprocessResults: function (data, initial, noHighlightUpdate) {
            var selected = 0, self = this, showSearchInput = true;

            // find the selected element in the result list

            this.findHighlightableChoices().each2(function (i, elm) {
                if (equal(self.id(elm.data("select2-data")), self.opts.element.val())) {
                    selected = i;
                    return false;
                }
            });

            // and highlight it
            if (noHighlightUpdate !== false) {
                if (initial === true && selected >= 0) {
                    this.highlight(selected);
                } else {
                    this.highlight(0);
                }
            }

            // hide the search box if this is the first we got the results and there are enough of them for search

            if (initial === true) {
                var min = this.opts.minimumResultsForSearch;
                if (min >= 0) {
                    this.showSearch(countResults(data.results) >= min);
                }
            }
        },

        // single
        showSearch: function(showSearchInput) {
            if (this.showSearchInput === showSearchInput) return;

            this.showSearchInput = showSearchInput;

            this.dropdown.find(".select2-search").toggleClass("select2-search-hidden", !showSearchInput);
            this.dropdown.find(".select2-search").toggleClass("select2-offscreen", !showSearchInput);
            //add "select2-with-searchbox" to the container if search box is shown
            $(this.dropdown, this.container).toggleClass("select2-with-searchbox", showSearchInput);
        },

        // single
        onSelect: function (data, options) {

            if (!this.triggerSelect(data)) { return; }

            var old = this.opts.element.val(),
                oldData = this.data();

            this.opts.element.val(this.id(data));
            this.updateSelection(data);

            this.opts.element.trigger({ type: "select2-selected", val: this.id(data), choice: data });

            this.lastSearchTerm = this.search.val();
            this.close();

            if ((!options || !options.noFocus) && this.opts.shouldFocusInput(this)) {
                this.focusser.focus();
            }

            if (!equal(old, this.id(data))) {
                this.triggerChange({ added: data, removed: oldData });
            }
        },

        // single
        updateSelection: function (data) {

            var container=this.selection.find(".select2-chosen"), formatted, cssClass;

            this.selection.data("select2-data", data);

            container.empty();
            if (data !== null) {
                formatted=this.opts.formatSelection(data, container, this.opts.escapeMarkup);
            }
            if (formatted !== undefined) {
                container.append(formatted);
            }
            cssClass=this.opts.formatSelectionCssClass(data, container);
            if (cssClass !== undefined) {
                container.addClass(cssClass);
            }

            this.selection.removeClass("select2-default");

            if (this.opts.allowClear && this.getPlaceholder() !== undefined) {
                this.container.addClass("select2-allowclear");
            }
        },

        // single
        val: function () {
            var val,
                triggerChange = false,
                data = null,
                self = this,
                oldData = this.data();

            if (arguments.length === 0) {
                return this.opts.element.val();
            }

            val = arguments[0];

            if (arguments.length > 1) {
                triggerChange = arguments[1];

                if (this.opts.debug && console && console.warn) {
                    console.warn(
                        'Select2: The second option to `select2("val")` is not supported in Select2 4.0.0. ' +
                        'The `change` event will always be triggered in 4.0.0.'
                    );
                }
            }

            if (this.select) {
                if (this.opts.debug && console && console.warn) {
                    console.warn(
                        'Select2: Setting the value on a <select> using `select2("val")` is no longer supported in 4.0.0. ' +
                        'You can use the `.val(newValue).trigger("change")` method provided by jQuery instead.'
                    );
                }

                this.select
                    .val(val)
                    .find("option").filter(function() { return this.selected }).each2(function (i, elm) {
                        data = self.optionToData(elm);
                        return false;
                    });
                this.updateSelection(data);
                this.setPlaceholder();
                if (triggerChange) {
                    this.triggerChange({added: data, removed:oldData});
                }
            } else {
                // val is an id. !val is true for [undefined,null,'',0] - 0 is legal
                if (!val && val !== 0) {
                    this.clear(triggerChange);
                    return;
                }
                if (this.opts.initSelection === undefined) {
                    throw new Error("cannot call val() if initSelection() is not defined");
                }
                this.opts.element.val(val);
                this.opts.initSelection(this.opts.element, function(data){
                    self.opts.element.val(!data ? "" : self.id(data));
                    self.updateSelection(data);
                    self.setPlaceholder();
                    if (triggerChange) {
                        self.triggerChange({added: data, removed:oldData});
                    }
                });
            }
        },

        // single
        clearSearch: function () {
            this.search.val("");
            this.focusser.val("");
        },

        // single
        data: function(value) {
            var data,
                triggerChange = false;

            if (arguments.length === 0) {
                data = this.selection.data("select2-data");
                if (data == undefined) data = null;
                return data;
            } else {
                if (this.opts.debug && console && console.warn) {
                    console.warn(
                        'Select2: The `select2("data")` method can no longer set selected values in 4.0.0, ' +
                        'consider using the `.val()` method instead.'
                    );
                }

                if (arguments.length > 1) {
                    triggerChange = arguments[1];
                }
                if (!value) {
                    this.clear(triggerChange);
                } else {
                    data = this.data();
                    this.opts.element.val(!value ? "" : this.id(value));
                    this.updateSelection(value);
                    if (triggerChange) {
                        this.triggerChange({added: value, removed:data});
                    }
                }
            }
        }
    });

    MultiSelect2 = clazz(AbstractSelect2, {

        // multi
        createContainer: function () {
            var container = $(document.createElement("div")).attr({
                "class": "select2-container select2-container-multi"
            }).html([
                "<ul class='select2-choices'>",
                "  <li class='select2-search-field'>",
                "    <label for='' class='select2-offscreen'></label>",
                "    <input type='text' autocomplete='off' autocorrect='off' autocapitalize='off' spellcheck='false' class='select2-input'>",
                "  </li>",
                "</ul>",
                "<div class='select2-drop select2-drop-multi select2-display-none'>",
                "   <ul class='select2-results'>",
                "   </ul>",
                "</div>"].join(""));
            return container;
        },

        // multi
        prepareOpts: function () {
            var opts = this.parent.prepareOpts.apply(this, arguments),
                self=this;

            // TODO validate placeholder is a string if specified
            if (opts.element.get(0).tagName.toLowerCase() === "select") {
                // install the selection initializer
                opts.initSelection = function (element, callback) {

                    var data = [];

                    element.find("option").filter(function() { return this.selected && !this.disabled }).each2(function (i, elm) {
                        data.push(self.optionToData(elm));
                    });
                    callback(data);
                };
            } else if ("data" in opts) {
                // install default initSelection when applied to hidden input and data is local
                opts.initSelection = opts.initSelection || function (element, callback) {
                    var ids = splitVal(element.val(), opts.separator, opts.transformVal);
                    //search in data by array of ids, storing matching items in a list
                    var matches = [];
                    opts.query({
                        matcher: function(term, text, el){
                            var is_match = $.grep(ids, function(id) {
                                return equal(id, opts.id(el));
                            }).length;
                            if (is_match) {
                                matches.push(el);
                            }
                            return is_match;
                        },
                        callback: !$.isFunction(callback) ? $.noop : function() {
                            // reorder matches based on the order they appear in the ids array because right now
                            // they are in the order in which they appear in data array
                            var ordered = [];
                            for (var i = 0; i < ids.length; i++) {
                                var id = ids[i];
                                for (var j = 0; j < matches.length; j++) {
                                    var match = matches[j];
                                    if (equal(id, opts.id(match))) {
                                        ordered.push(match);
                                        matches.splice(j, 1);
                                        break;
                                    }
                                }
                            }
                            callback(ordered);
                        }
                    });
                };
            }

            return opts;
        },

        // multi
        selectChoice: function (choice) {

            var selected = this.container.find(".select2-search-choice-focus");
            if (selected.length && choice && choice[0] == selected[0]) {

            } else {
                if (selected.length) {
                    this.opts.element.trigger("choice-deselected", selected);
                }
                selected.removeClass("select2-search-choice-focus");
                if (choice && choice.length) {
                    this.close();
                    choice.addClass("select2-search-choice-focus");
                    this.opts.element.trigger("choice-selected", choice);
                }
            }
        },

        // multi
        destroy: function() {
            $("label[for='" + this.search.attr('id') + "']")
                .attr('for', this.opts.element.attr("id"));
            this.parent.destroy.apply(this, arguments);

            cleanupJQueryElements.call(this,
                "searchContainer",
                "selection"
            );
        },

        // multi
        initContainer: function () {

            var selector = ".select2-choices", selection;

            this.searchContainer = this.container.find(".select2-search-field");
            this.selection = selection = this.container.find(selector);

            var _this = this;
            this.selection.on("click", ".select2-container:not(.select2-container-disabled) .select2-search-choice:not(.select2-locked)", function (e) {
                _this.search[0].focus();
                _this.selectChoice($(this));
            });

            // rewrite labels from original element to focusser
            this.search.attr("id", "s2id_autogen"+nextUid());

            this.search.prev()
                .text($("label[for='" + this.opts.element.attr("id") + "']").text())
                .attr('for', this.search.attr('id'));
            this.opts.element.on('focus.select2', this.bind(function () { this.focus(); }));

            this.search.on("input paste", this.bind(function() {
                if (this.search.attr('placeholder') && this.search.val().length == 0) return;
                if (!this.isInterfaceEnabled()) return;
                if (!this.opened()) {
                    this.open();
                }
            }));

            this.search.attr("tabindex", this.elementTabIndex);

            this.keydowns = 0;
            this.search.on("keydown", this.bind(function (e) {
                if (!this.isInterfaceEnabled()) return;

                ++this.keydowns;
                var selected = selection.find(".select2-search-choice-focus");
                var prev = selected.prev(".select2-search-choice:not(.select2-locked)");
                var next = selected.next(".select2-search-choice:not(.select2-locked)");
                var pos = getCursorInfo(this.search);

                if (selected.length &&
                    (e.which == KEY.LEFT || e.which == KEY.RIGHT || e.which == KEY.BACKSPACE || e.which == KEY.DELETE || e.which == KEY.ENTER)) {
                    var selectedChoice = selected;
                    if (e.which == KEY.LEFT && prev.length) {
                        selectedChoice = prev;
                    }
                    else if (e.which == KEY.RIGHT) {
                        selectedChoice = next.length ? next : null;
                    }
                    else if (e.which === KEY.BACKSPACE) {
                        if (this.unselect(selected.first())) {
                            this.search.width(10);
                            selectedChoice = prev.length ? prev : next;
                        }
                    } else if (e.which == KEY.DELETE) {
                        if (this.unselect(selected.first())) {
                            this.search.width(10);
                            selectedChoice = next.length ? next : null;
                        }
                    } else if (e.which == KEY.ENTER) {
                        selectedChoice = null;
                    }

                    this.selectChoice(selectedChoice);
                    killEvent(e);
                    if (!selectedChoice || !selectedChoice.length) {
                        this.open();
                    }
                    return;
                } else if (((e.which === KEY.BACKSPACE && this.keydowns == 1)
                    || e.which == KEY.LEFT) && (pos.offset == 0 && !pos.length)) {

                    this.selectChoice(selection.find(".select2-search-choice:not(.select2-locked)").last());
                    killEvent(e);
                    return;
                } else {
                    this.selectChoice(null);
                }

                if (this.opened()) {
                    switch (e.which) {
                    case KEY.UP:
                    case KEY.DOWN:
                        this.moveHighlight((e.which === KEY.UP) ? -1 : 1);
                        killEvent(e);
                        return;
                    case KEY.ENTER:
                        this.selectHighlighted();
                        killEvent(e);
                        return;
                    case KEY.TAB:
                        this.selectHighlighted({noFocus:true});
                        this.close();
                        return;
                    case KEY.ESC:
                        this.cancel(e);
                        killEvent(e);
                        return;
                    }
                }

                if (e.which === KEY.TAB || KEY.isControl(e) || KEY.isFunctionKey(e)
                 || e.which === KEY.BACKSPACE || e.which === KEY.ESC) {
                    return;
                }

                if (e.which === KEY.ENTER) {
                    if (this.opts.openOnEnter === false) {
                        return;
                    } else if (e.altKey || e.ctrlKey || e.shiftKey || e.metaKey) {
                        return;
                    }
                }

                this.open();

                if (e.which === KEY.PAGE_UP || e.which === KEY.PAGE_DOWN) {
                    // prevent the page from scrolling
                    killEvent(e);
                }

                if (e.which === KEY.ENTER) {
                    // prevent form from being submitted
                    killEvent(e);
                }

            }));

            this.search.on("keyup", this.bind(function (e) {
                this.keydowns = 0;
                this.resizeSearch();
            })
            );

            this.search.on("blur", this.bind(function(e) {
                this.container.removeClass("select2-container-active");
                this.search.removeClass("select2-focused");
                this.selectChoice(null);
                if (!this.opened()) this.clearSearch();
                e.stopImmediatePropagation();
                this.opts.element.trigger($.Event("select2-blur"));
            }));

            this.container.on("click", selector, this.bind(function (e) {
                if (!this.isInterfaceEnabled()) return;
                if ($(e.target).closest(".select2-search-choice").length > 0) {
                    // clicked inside a select2 search choice, do not open
                    return;
                }
                this.selectChoice(null);
                this.clearPlaceholder();
                if (!this.container.hasClass("select2-container-active")) {
                    this.opts.element.trigger($.Event("select2-focus"));
                }
                this.open();
                this.focusSearch();
                e.preventDefault();
            }));

            this.container.on("focus", selector, this.bind(function () {
                if (!this.isInterfaceEnabled()) return;
                if (!this.container.hasClass("select2-container-active")) {
                    this.opts.element.trigger($.Event("select2-focus"));
                }
                this.container.addClass("select2-container-active");
                this.dropdown.addClass("select2-drop-active");
                this.clearPlaceholder();
            }));

            this.initContainerWidth();
            this.opts.element.hide();

            // set the placeholder if necessary
            this.clearSearch();
        },

        // multi
        enableInterface: function() {
            if (this.parent.enableInterface.apply(this, arguments)) {
                this.search.prop("disabled", !this.isInterfaceEnabled());
            }
        },

        // multi
        initSelection: function () {
            var data;
            if (this.opts.element.val() === "" && this.opts.element.text() === "") {
                this.updateSelection([]);
                this.close();
                // set the placeholder if necessary
                this.clearSearch();
            }
            if (this.select || this.opts.element.val() !== "") {
                var self = this;
                this.opts.initSelection.call(null, this.opts.element, function(data){
                    if (data !== undefined && data !== null) {
                        self.updateSelection(data);
                        self.close();
                        // set the placeholder if necessary
                        self.clearSearch();
                    }
                });
            }
        },

        // multi
        clearSearch: function () {
            var placeholder = this.getPlaceholder(),
                maxWidth = this.getMaxSearchWidth();

            if (placeholder !== undefined  && this.getVal().length === 0 && this.search.hasClass("select2-focused") === false) {
                this.search.val(placeholder).addClass("select2-default");
                // stretch the search box to full width of the container so as much of the placeholder is visible as possible
                // we could call this.resizeSearch(), but we do not because that requires a sizer and we do not want to create one so early because of a firefox bug, see #944
                this.search.width(maxWidth > 0 ? maxWidth : this.container.css("width"));
            } else {
                this.search.val("").width(10);
            }
        },

        // multi
        clearPlaceholder: function () {
            if (this.search.hasClass("select2-default")) {
                this.search.val("").removeClass("select2-default");
            }
        },

        // multi
        opening: function () {
            this.clearPlaceholder(); // should be done before super so placeholder is not used to search
            this.resizeSearch();

            this.parent.opening.apply(this, arguments);

            this.focusSearch();

            this.prefillNextSearchTerm();
            this.updateResults(true);

            if (this.opts.shouldFocusInput(this)) {
                this.search.focus();
            }
            this.opts.element.trigger($.Event("select2-open"));
        },

        // multi
        close: function () {
            if (!this.opened()) return;
            this.parent.close.apply(this, arguments);
        },

        // multi
        focus: function () {
            this.close();
            this.search.focus();
        },

        // multi
        isFocused: function () {
            return this.search.hasClass("select2-focused");
        },

        // multi
        updateSelection: function (data) {
            var ids = {}, filtered = [], self = this;

            // filter out duplicates
            $(data).each(function () {
                if (!(self.id(this) in ids)) {
                    ids[self.id(this)] = 0;
                    filtered.push(this);
                }
            });

            this.selection.find(".select2-search-choice").remove();
            this.addSelectedChoice(filtered);
            self.postprocessResults();
        },

        // multi
        tokenize: function() {
            var input = this.search.val();
            input = this.opts.tokenizer.call(this, input, this.data(), this.bind(this.onSelect), this.opts);
            if (input != null && input != undefined) {
                this.search.val(input);
                if (input.length > 0) {
                    this.open();
                }
            }

        },

        // multi
        onSelect: function (data, options) {

            if (!this.triggerSelect(data) || data.text === "") { return; }

            this.addSelectedChoice(data);

            this.opts.element.trigger({ type: "selected", val: this.id(data), choice: data });

            // keep track of the search's value before it gets cleared
            this.lastSearchTerm = this.search.val();

            this.clearSearch();
            this.updateResults();

            if (this.select || !this.opts.closeOnSelect) this.postprocessResults(data, false, this.opts.closeOnSelect===true);

            if (this.opts.closeOnSelect) {
                this.close();
                this.search.width(10);
            } else {
                if (this.countSelectableResults()>0) {
                    this.search.width(10);
                    this.resizeSearch();
                    if (this.getMaximumSelectionSize() > 0 && this.val().length >= this.getMaximumSelectionSize()) {
                        // if we reached max selection size repaint the results so choices
                        // are replaced with the max selection reached message
                        this.updateResults(true);
                    } else {
                        // initializes search's value with nextSearchTerm and update search result
                        if (this.prefillNextSearchTerm()) {
                            this.updateResults();
                        }
                    }
                    this.positionDropdown();
                } else {
                    // if nothing left to select close
                    this.close();
                    this.search.width(10);
                }
            }

            // since its not possible to select an element that has already been
            // added we do not need to check if this is a new element before firing change
            this.triggerChange({ added: data });

            if (!options || !options.noFocus)
                this.focusSearch();
        },

        // multi
        cancel: function () {
            this.close();
            this.focusSearch();
        },

        addSelectedChoice: function (data) {
            var val = this.getVal(), self = this;
            $(data).each(function () {
                val.push(self.createChoice(this));
            });
            this.setVal(val);
        },

        createChoice: function (data) {
            var enableChoice = !data.locked,
                enabledItem = $(
                    "<li class='select2-search-choice'>" +
                    "    <div></div>" +
                    "    <a href='#' class='select2-search-choice-close' tabindex='-1'></a>" +
                    "</li>"),
                disabledItem = $(
                    "<li class='select2-search-choice select2-locked'>" +
                    "<div></div>" +
                    "</li>");
            var choice = enableChoice ? enabledItem : disabledItem,
                id = this.id(data),
                formatted,
                cssClass;

            formatted=this.opts.formatSelection(data, choice.find("div"), this.opts.escapeMarkup);
            if (formatted != undefined) {
                choice.find("div").replaceWith($("<div></div>").html(formatted));
            }
            cssClass=this.opts.formatSelectionCssClass(data, choice.find("div"));
            if (cssClass != undefined) {
                choice.addClass(cssClass);
            }

            if(enableChoice){
              choice.find(".select2-search-choice-close")
                  .on("mousedown", killEvent)
                  .on("click dblclick", this.bind(function (e) {
                  if (!this.isInterfaceEnabled()) return;

                  this.unselect($(e.target));
                  this.selection.find(".select2-search-choice-focus").removeClass("select2-search-choice-focus");
                  killEvent(e);
                  this.close();
                  this.focusSearch();
              })).on("focus", this.bind(function () {
                  if (!this.isInterfaceEnabled()) return;
                  this.container.addClass("select2-container-active");
                  this.dropdown.addClass("select2-drop-active");
              }));
            }

            choice.data("select2-data", data);
            choice.insertBefore(this.searchContainer);

            return id;
        },

        // multi
        unselect: function (selected) {
            var val = this.getVal(),
                data,
                index;
            selected = selected.closest(".select2-search-choice");

            if (selected.length === 0) {
                throw "Invalid argument: " + selected + ". Must be .select2-search-choice";
            }

            data = selected.data("select2-data");

            if (!data) {
                // prevent a race condition when the 'x' is clicked really fast repeatedly the event can be queued
                // and invoked on an element already removed
                return;
            }

            var evt = $.Event("select2-removing");
            evt.val = this.id(data);
            evt.choice = data;
            this.opts.element.trigger(evt);

            if (evt.isDefaultPrevented()) {
                return false;
            }

            while((index = indexOf(this.id(data), val)) >= 0) {
                val.splice(index, 1);
                this.setVal(val);
                if (this.select) this.postprocessResults();
            }

            selected.remove();

            this.opts.element.trigger({ type: "select2-removed", val: this.id(data), choice: data });
            this.triggerChange({ removed: data });

            return true;
        },

        // multi
        postprocessResults: function (data, initial, noHighlightUpdate) {
            var val = this.getVal(),
                choices = this.results.find(".select2-result"),
                compound = this.results.find(".select2-result-with-children"),
                self = this;

            choices.each2(function (i, choice) {
                var id = self.id(choice.data("select2-data"));
                if (indexOf(id, val) >= 0) {
                    choice.addClass("select2-selected");
                    // mark all children of the selected parent as selected
                    choice.find(".select2-result-selectable").addClass("select2-selected");
                }
            });

            compound.each2(function(i, choice) {
                // hide an optgroup if it doesn't have any selectable children
                if (!choice.is('.select2-result-selectable')
                    && choice.find(".select2-result-selectable:not(.select2-selected)").length === 0) {
                    choice.addClass("select2-selected");
                }
            });

            if (this.highlight() == -1 && noHighlightUpdate !== false && this.opts.closeOnSelect === true){
                self.highlight(0);
            }

            //If all results are chosen render formatNoMatches
            if(!this.opts.createSearchChoice && !choices.filter('.select2-result:not(.select2-selected)').length > 0){
                if(!data || data && !data.more && this.results.find(".select2-no-results").length === 0) {
                    if (checkFormatter(self.opts.formatNoMatches, "formatNoMatches")) {
                        this.results.append("<li class='select2-no-results'>" + evaluate(self.opts.formatNoMatches, self.opts.element, self.search.val()) + "</li>");
                    }
                }
            }

        },

        // multi
        getMaxSearchWidth: function() {
            return this.selection.width() - getSideBorderPadding(this.search);
        },

        // multi
        resizeSearch: function () {
            var minimumWidth, left, maxWidth, containerLeft, searchWidth,
                sideBorderPadding = getSideBorderPadding(this.search);

            minimumWidth = measureTextWidth(this.search) + 10;

            left = this.search.offset().left;

            maxWidth = this.selection.width();
            containerLeft = this.selection.offset().left;

            searchWidth = maxWidth - (left - containerLeft) - sideBorderPadding;

            if (searchWidth < minimumWidth) {
                searchWidth = maxWidth - sideBorderPadding;
            }

            if (searchWidth < 40) {
                searchWidth = maxWidth - sideBorderPadding;
            }

            if (searchWidth <= 0) {
              searchWidth = minimumWidth;
            }

            this.search.width(Math.floor(searchWidth));
        },

        // multi
        getVal: function () {
            var val;
            if (this.select) {
                val = this.select.val();
                return val === null ? [] : val;
            } else {
                val = this.opts.element.val();
                return splitVal(val, this.opts.separator, this.opts.transformVal);
            }
        },

        // multi
        setVal: function (val) {
            if (this.select) {
                this.select.val(val);
            } else {
                var unique = [], valMap = {};
                // filter out duplicates
                $(val).each(function () {
                    if (!(this in valMap)) {
                        unique.push(this);
                        valMap[this] = 0;
                    }
                });
                this.opts.element.val(unique.length === 0 ? "" : unique.join(this.opts.separator));
            }
        },

        // multi
        buildChangeDetails: function (old, current) {
            var current = current.slice(0),
                old = old.slice(0);

            // remove intersection from each array
            for (var i = 0; i < current.length; i++) {
                for (var j = 0; j < old.length; j++) {
                    if (equal(this.opts.id(current[i]), this.opts.id(old[j]))) {
                        current.splice(i, 1);
                        i--;
                        old.splice(j, 1);
                        break;
                    }
                }
            }

            return {added: current, removed: old};
        },


        // multi
        val: function (val, triggerChange) {
            var oldData, self=this;

            if (arguments.length === 0) {
                return this.getVal();
            }

            oldData=this.data();
            if (!oldData.length) oldData=[];

            // val is an id. !val is true for [undefined,null,'',0] - 0 is legal
            if (!val && val !== 0) {
                this.opts.element.val("");
                this.updateSelection([]);
                this.clearSearch();
                if (triggerChange) {
                    this.triggerChange({added: this.data(), removed: oldData});
                }
                return;
            }

            // val is a list of ids
            this.setVal(val);

            if (this.select) {
                this.opts.initSelection(this.select, this.bind(this.updateSelection));
                if (triggerChange) {
                    this.triggerChange(this.buildChangeDetails(oldData, this.data()));
                }
            } else {
                if (this.opts.initSelection === undefined) {
                    throw new Error("val() cannot be called if initSelection() is not defined");
                }

                this.opts.initSelection(this.opts.element, function(data){
                    var ids=$.map(data, self.id);
                    self.setVal(ids);
                    self.updateSelection(data);
                    self.clearSearch();
                    if (triggerChange) {
                        self.triggerChange(self.buildChangeDetails(oldData, self.data()));
                    }
                });
            }
            this.clearSearch();
        },

        // multi
        onSortStart: function() {
            if (this.select) {
                throw new Error("Sorting of elements is not supported when attached to <select>. Attach to <input type='hidden'/> instead.");
            }

            // collapse search field into 0 width so its container can be collapsed as well
            this.search.width(0);
            // hide the container
            this.searchContainer.hide();
        },

        // multi
        onSortEnd:function() {

            var val=[], self=this;

            // show search and move it to the end of the list
            this.searchContainer.show();
            // make sure the search container is the last item in the list
            this.searchContainer.appendTo(this.searchContainer.parent());
            // since we collapsed the width in dragStarted, we resize it here
            this.resizeSearch();

            // update selection
            this.selection.find(".select2-search-choice").each(function() {
                val.push(self.opts.id($(this).data("select2-data")));
            });
            this.setVal(val);
            this.triggerChange();
        },

        // multi
        data: function(values, triggerChange) {
            var self=this, ids, old;
            if (arguments.length === 0) {
                 return this.selection
                     .children(".select2-search-choice")
                     .map(function() { return $(this).data("select2-data"); })
                     .get();
            } else {
                old = this.data();
                if (!values) { values = []; }
                ids = $.map(values, function(e) { return self.opts.id(e); });
                this.setVal(ids);
                this.updateSelection(values);
                this.clearSearch();
                if (triggerChange) {
                    this.triggerChange(this.buildChangeDetails(old, this.data()));
                }
            }
        }
    });

    $.fn.select2 = function () {

        var args = Array.prototype.slice.call(arguments, 0),
            opts,
            select2,
            method, value, multiple,
            allowedMethods = ["val", "destroy", "opened", "open", "close", "focus", "isFocused", "container", "dropdown", "onSortStart", "onSortEnd", "enable", "disable", "readonly", "positionDropdown", "data", "search"],
            valueMethods = ["opened", "isFocused", "container", "dropdown"],
            propertyMethods = ["val", "data"],
            methodsMap = { search: "externalSearch" };

        this.each(function () {
            if (args.length === 0 || typeof(args[0]) === "object") {
                opts = args.length === 0 ? {} : $.extend({}, args[0]);
                opts.element = $(this);

                if (opts.element.get(0).tagName.toLowerCase() === "select") {
                    multiple = opts.element.prop("multiple");
                } else {
                    multiple = opts.multiple || false;
                    if ("tags" in opts) {opts.multiple = multiple = true;}
                }

                select2 = multiple ? new window.Select2["class"].multi() : new window.Select2["class"].single();
                select2.init(opts);
            } else if (typeof(args[0]) === "string") {

                if (indexOf(args[0], allowedMethods) < 0) {
                    throw "Unknown method: " + args[0];
                }

                value = undefined;
                select2 = $(this).data("select2");
                if (select2 === undefined) return;

                method=args[0];

                if (method === "container") {
                    value = select2.container;
                } else if (method === "dropdown") {
                    value = select2.dropdown;
                } else {
                    if (methodsMap[method]) method = methodsMap[method];

                    value = select2[method].apply(select2, args.slice(1));
                }
                if (indexOf(args[0], valueMethods) >= 0
                    || (indexOf(args[0], propertyMethods) >= 0 && args.length == 1)) {
                    return false; // abort the iteration, ready to return first matched value
                }
            } else {
                throw "Invalid arguments to select2 plugin: " + args;
            }
        });
        return (value === undefined) ? this : value;
    };

    // plugin defaults, accessible to users
    $.fn.select2.defaults = {
        debug: false,
        width: "copy",
        loadMorePadding: 0,
        closeOnSelect: true,
        openOnEnter: true,
        containerCss: {},
        dropdownCss: {},
        containerCssClass: "",
        dropdownCssClass: "",
        formatResult: function(result, container, query, escapeMarkup) {
            var markup=[];
            markMatch(this.text(result), query.term, markup, escapeMarkup);
            return markup.join("");
        },
        transformVal: function(val) {
            return $.trim(val);
        },
        formatSelection: function (data, container, escapeMarkup) {
            return data ? escapeMarkup(this.text(data)) : undefined;
        },
        sortResults: function (results, container, query) {
            return results;
        },
        formatResultCssClass: function(data) {return data.css;},
        formatSelectionCssClass: function(data, container) {return undefined;},
        minimumResultsForSearch: 0,
        minimumInputLength: 0,
        maximumInputLength: null,
        maximumSelectionSize: 0,
        id: function (e) { return e == undefined ? null : e.id; },
        text: function (e) {
          if (e && this.data && this.data.text) {
            if ($.isFunction(this.data.text)) {
              return this.data.text(e);
            } else {
              return e[this.data.text];
            }
          } else {
            return e.text;
          }
        },
        matcher: function(term, text) {
            return stripDiacritics(''+text).toUpperCase().indexOf(stripDiacritics(''+term).toUpperCase()) >= 0;
        },
        separator: ",",
        tokenSeparators: [],
        tokenizer: defaultTokenizer,
        escapeMarkup: defaultEscapeMarkup,
        blurOnChange: false,
        selectOnBlur: false,
        adaptContainerCssClass: function(c) { return c; },
        adaptDropdownCssClass: function(c) { return null; },
        nextSearchTerm: function(selectedObject, currentSearchTerm) { return undefined; },
        searchInputPlaceholder: '',
        createSearchChoicePosition: 'top',
        shouldFocusInput: function (instance) {
            // Attempt to detect touch devices
            var supportsTouchEvents = (('ontouchstart' in window) ||
                                       (navigator.msMaxTouchPoints > 0));

            // Only devices which support touch events should be special cased
            if (!supportsTouchEvents) {
                return true;
            }

            // Never focus the input if search is disabled
            if (instance.opts.minimumResultsForSearch < 0) {
                return false;
            }

            return true;
        }
    };

    $.fn.select2.locales = [];

    $.fn.select2.locales['en'] = {
         formatMatches: function (matches) { if (matches === 1) { return "One result is available, press enter to select it."; } return matches + " results are available, use up and down arrow keys to navigate."; },
         formatNoMatches: function () { return "No matches found"; },
         formatAjaxError: function (jqXHR, textStatus, errorThrown) { return "Loading failed"; },
         formatInputTooShort: function (input, min) { var n = min - input.length; return "Please enter " + n + " or more character" + (n == 1 ? "" : "s"); },
         formatInputTooLong: function (input, max) { var n = input.length - max; return "Please delete " + n + " character" + (n == 1 ? "" : "s"); },
         formatSelectionTooBig: function (limit) { return "You can only select " + limit + " item" + (limit == 1 ? "" : "s"); },
         formatLoadMore: function (pageNumber) { return "Loading more results…"; },
         formatSearching: function () { return "Searching…"; }
    };

    $.extend($.fn.select2.defaults, $.fn.select2.locales['en']);

    $.fn.select2.ajaxDefaults = {
        transport: $.ajax,
        params: {
            type: "GET",
            cache: false,
            dataType: "json"
        }
    };

    // exports
    window.Select2 = {
        query: {
            ajax: ajax,
            local: local,
            tags: tags
        }, util: {
            debounce: debounce,
            markMatch: markMatch,
            escapeMarkup: defaultEscapeMarkup,
            stripDiacritics: stripDiacritics
        }, "class": {
            "abstract": AbstractSelect2,
            "single": SingleSelect2,
            "multi": MultiSelect2
        }
    };

}(jQuery));
//! Likely v0.91 by Ilya Birman - http://ilyabirman.net/projects/likely/
//! Based on Social Likes v3.0.10 by Artem Sapegin - http://sapegin.github.com/social-likes - Licensed MIT

! function(factory) {
    if (typeof define === 'funciton' && define.amd) {
        define(['jquery'], factory);
    } else {
        factory(jQuery);
    }
}(function($, undefined) {
    'use strict';

    var prefix = "likely";
    var classPrefix = prefix + "__";
    var protocol = "https:" === location.protocol ? "https:" : "http:";
    var a = '<svg xmlns="http://www.w3.org/2000/svg" width="30" height="30" viewBox="0 0 50 50"><path d="M';
    var p = 'z"/></svg>';

    /**
     * Buttons
     */
    var services = {
        facebook: {
            svgi: "26 20v-3c0-1.3.3-2 2.4-2H31v-5h-4c-5 0-7 3.3-7 7v3h-4v5h4v15h6V25h4.4l.6-5h-5",
            counterUrl: "https://graph.facebook.com/?id={url}&callback=?",
            convertNumber: function(data) {
                return data.share.share_count;
            },
            popupUrl: 'https://www.facebook.com/sharer/sharer.php?u={url}',
            popupWidth: 600,
            popupHeight: 500
        },
        twitter: {
            svgi: "39.2 16.8c-1.1.5-2.2.8-3.5 1 1.2-.8 2.2-1.9 2.7-3.3-1.2.7-2.5 1.2-3.8 1.5-1.1-1.2-2.7-1.9-4.4-1.9-3.3 0-6.1 2.7-6.1 6.1 0 .5.1.9.2 1.4-5-.2-9.5-2.7-12.5-6.3-.5.7-.8 1.7-.8 2.8 0 2.1 1.1 4 2.7 5-1 0-1.9-.3-2.7-.8v.1c0 2.9 2.1 5.4 4.9 5.9-.5.1-1 .2-1.6.2-.4 0-.8 0-1.1-.1.8 2.4 3 4.2 5.7 4.2-2.1 1.6-4.7 2.6-7.5 2.6-.5 0-1 0-1.4-.1 2.4 1.9 5.6 2.9 9 2.9 11.1 0 17.2-9.2 17.2-17.2V20c1.2-.9 2.2-1.9 3-3.2",
            counterUrl: "https://opensharecount.com/count.json?url={url}",
            convertNumber: function(data) {
                return data.count;
            },
            popupUrl: "https://twitter.com/intent/tweet?url={url}&text={title}",
            popupWidth: 600,
            popupHeight: 450,
            click: function() {
                // Add colon to improve readability
                if (!/[\.\?:\-–—]\s*$/.test(this.options.title)) this.options.title += ':';
                return true;
            }
        },
        vkontakte: {
            svgi: "25.1 35.9h2s.6-.1.9-.4c.3-.3.3-.9.3-.9s0-2.6 1.2-3c1.2-.4 2.8 2.6 4.4 3.7 1.2.9 2.1.7 2.1.7l4.4-.1s2.3-.1 1.2-2c-.1-.1-.6-1.3-3.3-3.8-2.8-2.6-2.4-2.1.9-6.6 2-2.7 2.8-4.3 2.6-5.1-.2-.7-1.7-.5-1.7-.5h-5s-.4-.1-.6.1c-.3.2-.4.5-.4.5s-.8 2.1-1.8 3.9c-2.2 3.7-3.1 3.9-3.4 3.7-.8-.5-.6-2.2-.6-3.3 0-3.6.6-5.1-1.1-5.5-.5-.1-.9-.2-2.3-.2-1.8 0-3.3 0-4.1.4-.6.3-1 .9-.7.9.3 0 1.1.2 1.5.7.4.9.4 2.4.4 2.4s.3 4.3-.7 4.8c-.7.4-1.6-.4-3.6-3.8-1-1.7-1.8-3.7-1.8-3.7s-.1-.4-.4-.6c-.3-.2-.8-.3-.8-.3H10s-.7 0-1 .3c-.2.3 0 .8 0 .8s3.7 8.6 7.9 13c3.9 4.2 8.2 3.9 8.2 3.9",
            counterUrl: protocol + "//vk.com/share.php?act=count&url={url}&index={index}",
            counter: function(urls, deferred) {
                var options = services.vkontakte;
                if (!options._) {
                    options._ = [];
                    var counts = urls.length;
                    var sum = 0;
                    if (!window.VK) window.VK = {};
                    window.VK.Share = {
                        count: function(idx, number) {
                            sum = sum + number;
                            if (!--counts) {
                                options._[idx].resolve(sum);
                            }
                        }
                    };
                }

                var index = options._.length;
                options._.push(deferred);
                $.each(urls, function(i, url) {
                    $.getScript(makeUrl(options.counterUrl, {url: url, index: index})).fail(deferred.reject);
                });
            },
            popupUrl: protocol + "//vk.com/share.php?url={url}&title={title}",
            popupWidth: 550,
            popupHeight: 330
        }
    };

    /**
     * Counters manager
     */
    var counters = {
        promises: {},
        fetch: function(service, urls, extraOptions) {
            if (!counters.promises[service]) counters.promises[service] = {};
            var servicePromises = counters.promises[service];
            var url = urls[0];

            if (!extraOptions.forceUpdate && servicePromises[url]) {
                return servicePromises[url];
            } else {
                var options = $.extend({}, services[service], extraOptions);
                var deferred = $.Deferred();

                var sum = 0;
                var currentStepIndex = urls.length;
                var loadSum = function(urls) {
                    var url = urls.splice(0, 1);
                    var jsonUrl = options.counterUrl && makeUrl(options.counterUrl, {url: url});

                    $.getJSON(jsonUrl)
                        .done(function(data) {
                            try {
                                var number = data;
                                if ($.isFunction(options.convertNumber)) {
                                    number = options.convertNumber(data);
                                }

                                sum = sum + number;

                                // Not `facebook` because it work with redirected urls correctly
                                if (--currentStepIndex && service !== 'facebook') {
                                    loadSum(urls);
                                } else {
                                    deferred.resolve(sum);
                                }
                            } catch (e) {
                                deferred.reject(e);
                            }
                        })
                        .fail(deferred.reject);
                };

                if (urls.length && $.isFunction(options.counter)) {
                    options.counter(urls, deferred);
                } else {
                    loadSum(urls);
                }

                servicePromises[url] = deferred.promise();
                return servicePromises[url];
            }
        }
    };

    /**
     * jQuery plugin
     */
    $.fn.socialLikes = function(options) {
        return this.each(function() {
            var elem = $(this);
            var instance = elem.data(prefix);
            if (instance) {
                if ($.isPlainObject(options)) {
                    instance.update(options);
                }
            }
            else {
                instance = new SocialLikes(elem, $.extend({}, $.fn.socialLikes.defaults, options, dataToOptions(elem)));
                elem.data(prefix, instance);
            }
        });
    };

    $.fn.socialLikes.defaults = {
        url: window.location.href.replace(window.location.hash, ''),
        title: document.title,
        counters: true,
        zeroes: false,
        wait: 500,
        popupCheckInterval: 500
    };

    function SocialLikes(container, options) {
        this.container = container;
        this.options = options;
        this.init();
    }

    SocialLikes.prototype = {
        init: function() {
            // Add class in case of manual initialization
            this.container.addClass(prefix);

            this.initUserButtons();

            this.countersLeft = 0;
            this.number = 0;
            this.container.on('counter.' + prefix, $.proxy(this.updateCounter, this));

            var buttons = this.container.children();

            this.buttons = [];
            buttons.each($.proxy(function(idx, elem) {
                var button = new Button($(elem), this.options);
                this.buttons.push(button);
                if (button.options.counterUrl) this.countersLeft++;
            }, this));

            if (this.options.counters) {
                this.timer = setTimeout($.proxy(this.appear, this), this.options.wait);
            }
            else {
                this.appear();
            }
        },
        initUserButtons: function() {
            if (!this.userButtonInited && window.socialLikesButtons) {
                $.extend(true, services, socialLikesButtons);
            }
            this.userButtonInited = true;
        },
        update: function(options) {
            if (!options.forceUpdate && options.url === this.options.url) return;

            // Reset counters
            this.number = 0;
            this.countersLeft = this.buttons.length;
            if (this.widget) this.widget.find('.' + prefix + '__counter').remove();

            // Update options
            $.extend(this.options, options);
            for (var buttonIdx = 0; buttonIdx < this.buttons.length; buttonIdx++) {
                this.buttons[buttonIdx].update(options);
            }
        },
        updateCounter: function(e, service, number) {
            if (number) {
                this.number += number;
            }

            this.countersLeft--;
            if (this.countersLeft === 0) {
                this.appear();

                // @TODO
                // this.ready();

                this.container.addClass(prefix + "_ready");
                this.container.trigger("ready." + prefix, this.number);
            }
        },
        appear: function() {
            this.container.addClass(prefix + '_visible');
        },
        getCounterElem: function() {
            var counterElem = this.widget.find('.' + classPrefix + 'counter_single');
            if (!counterElem.length) {
                counterElem = $('<span>', {
                    'class': getElementClassNames('counter', 'single')
                });
                this.widget.append(counterElem);
            }
            return counterElem;
        }
    };

    function Button(widget, options) {
        this.widget = widget;
        this.options = $.extend({}, options);
        this.detectService();

        if (this.service) {
            this.init();
        }
    }

    Button.prototype = {
        init: function() {
            this.detectParams();
            this.initHtml();
            setTimeout($.proxy(this.initCounter, this), 0);
        },
        update: function(e) {
            $.extend(this.options, {forceUpdate: false}, options);
            this.widget.find('.' + prefix + '__counter').remove();  // Remove old counter
            this.initCounter();
        },
        detectService: function() {
            var service = this.widget.data('service');
            if (!service) {
                var node = this.widget[0];
                var classes = node.classList || node.className.split(' ');
                for (var classIdx = 0; classIdx < classes.length; classIdx++) {
                    var cls = classes[classIdx];
                    if (services[cls]) {
                        service = cls;
                        break;
                    }
                }
                if (!service) return;
            }
            this.service = service;
            $.extend(this.options, services[service]);
        },
        detectParams: function() {
            var data = this.widget.data();

            // Custom page counter URL or number
            if (data.counter) {
                var number = parseInt(data.counter, 10);
                if (isNaN(number)) {
                    this.options.counterUrl = data.counter;
                }
                else {
                    this.options.counterNumber = number;
                }
            }

            // Custom page title
            if (data.title) {
                this.options.title = data.title;
            }

            // Custom page URL
            if (data.url) {
                this.options.url = data.url;
            }
        },
        initHtml: function() {
            var options = this.options;
            var widget = this.widget;

            // Button
            var button = $('<span>', {
                'class': this.getElementClassNames('button'),
                'text': widget.text()
            });
            if (options.clickUrl) {
                var url = makeUrl(options.clickUrl, {
                    url: options.url,
                    title: options.title
                });
                var link = $('<a>', {
                    href: url
                });
                this.cloneDataAttrs(widget, link);
                widget.replaceWith(link);
                this.widget = widget = link;
            }
            else {
                widget.on('click', $.proxy(this.click, this));
            }

            widget.removeClass(this.service);
            widget.addClass(this.getElementClassNames('widget'));

            // Icon
            var icon = $('<span class="likely__icon">' + a + options.svgi + p + "</span>", {
                'class': this.getElementClassNames('icon')
            });

            widget.empty().append(icon).append(button);
            this.button = button;
        },
        initCounter: function() {
            if (this.options.counters) {
                if (this.options.counterNumber) {
                    this.updateCounter(this.options.counterNumber);
                }
                else {
                    var extraOptions = {
                        counterUrl: this.options.counterUrl,
                        forceUpdate: this.options.forceUpdate
                    };
                    counters.fetch(this.service, [this.options.url, this.options.oldUrl], extraOptions)
                        .always($.proxy(this.updateCounter, this));
                }
            }
        },
        cloneDataAttrs: function(source, destination) {
            var data = source.data();
            for (var key in data) {
                if (data.hasOwnProperty(key)) {
                    destination.data(key, data[key]);
                }
            }
        },
        getElementClassNames: function(elem) {
            return getElementClassNames(elem, this.service);
        },
        updateCounter: function(number) {
            number = parseInt(number, 10) || 0;

            var params = {
                'class': this.getElementClassNames('counter'),
                'text': number
            };
            if (!number && !this.options.zeroes) {
                params['class'] += ' ' + prefix + '__counter_empty';
                params.text = '0';
            }
            var counterElem = $('<span>', params);
            this.widget.append(counterElem);

            this.widget.trigger('counter.' + prefix, [this.service, number]);
        },
        click: function(e) {
            var options = this.options;
            var process = true;
            if ($.isFunction(options.click)) {
                process = options.click.call(this, e);
            }
            if (process) {
                var url = makeUrl(options.popupUrl, {
                    url: options.url,
                    title: options.title
                });
                url = this.addAdditionalParamsToUrl(url);
                this.openPopup(url, {
                    width: options.popupWidth,
                    height: options.popupHeight
                });
            }
            return false;
        },
        addAdditionalParamsToUrl: function(url) {
            var params = $.param($.extend(this.widget.data(), this.options.data));
            if ($.isEmptyObject(params)) return url;
            var glue = url.indexOf('?') === -1 ? '?' : '&';
            return url + glue + params;
        },
        openPopup: function(url, params) {
            var left = Math.round(screen.width/2 - params.width/2);
            var top = 0;
            if (screen.height > params.height) {
                top = Math.round(screen.height/3 - params.height/2);
            }

            var win = window.open(url, 'sl_' + this.service, 'left=' + left + ',top=' + top + ',' +
               'width=' + params.width + ',height=' + params.height + ',personalbar=0,toolbar=0,scrollbars=1,resizable=1');
            if (win) {
                win.focus();
                this.widget.trigger('popup_opened.' + prefix, [this.service, win]);
                var timer = setInterval($.proxy(function() {
                    if (!win.closed) return;
                    clearInterval(timer);
                    this.widget.trigger('popup_closed.' + prefix, this.service);
                }, this), this.options.popupCheckInterval);
            }
            else {
                location.href = url;
            }
        }
    };

    function dataToOptions(elem) {
        function upper(m, l) {
            return e.toUpper()
        }

        var options = {};
        var data = elem.data();

        for (var key in data) {
            var value = data[key];
            if (value === 'yes') value = true;
            else if (value === 'no') value = false;
            options[key.replace(/-(\w)/g, upper)] = value;
        }

        return options;
    }

    function makeUrl(url, context) {
        return template(url, context, encodeURIComponent);
    }

    function template(tmpl, context, filter) {
        return tmpl.replace(/\{([^\}]+)\}/g, function(m, key) {
            // If key doesn't exists in the context we should keep template tag as is
            return key in context ? (filter ? filter(context[key]) : context[key]) : m;
        });
    }

    function getElementClassNames(elem, mod) {
        var cls = classPrefix + elem;
        return cls + ' ' + cls + '_' + mod;
    }

    /**
     * Auto initialization
     */
    $(function() {
        $('.' + prefix).socialLikes();
    });
});
function AdFox_SetLayerVis(spritename,state){
   document.getElementById(spritename).style.visibility=state;
}

function AdFox_Open(AF_id){
   AdFox_SetLayerVis('AdFox_DivBaseFlash_'+AF_id, "hidden");
   AdFox_SetLayerVis('AdFox_DivOverFlash_'+AF_id, "visible");
}

function AdFox_Close(AF_id){
   AdFox_SetLayerVis('AdFox_DivOverFlash_'+AF_id, "hidden");
   AdFox_SetLayerVis('AdFox_DivBaseFlash_'+AF_id, "visible");
}

function AdFox_getCodeScript(AF_n,AF_id,AF_src){
   var AF_doc;
   if(AF_n<10){
      try{
	     if(document.all && !window.opera){
		    AF_doc = window.frames['AdFox_iframe_'+AF_id].document;
			}else if(document.getElementById){
			         AF_doc = document.getElementById('AdFox_iframe_'+AF_id).contentDocument;
					 }
		 }catch(e){}
    if(AF_doc){
	   AF_doc.write('<scr'+'ipt onerror="document.close();" type="text/javascript" src="'+AF_src+'"><\/scr'+'ipt>');
	   }else{
	      setTimeout('AdFox_getCodeScript('+(++AF_n)+','+AF_id+',"'+AF_src+'");', 100);
		  }
		  }
}

function adfoxSdvigContent(banID, flashWidth, flashHeight){
	var obj = document.getElementById('adfoxBanner'+banID).style;
	if (flashWidth == '100%') obj.width = flashWidth;
	    else obj.width = flashWidth + "px";
	if (flashHeight == '100%') obj.height = flashHeight;
	    else obj.height = flashHeight + "px";
}

function adfoxVisibilityFlash(banName, flashWidth, flashHeight){
    	var obj = document.getElementById(banName).style;
	if (flashWidth == '100%') obj.width = flashWidth;
	    else obj.width = flashWidth + "px";
	if (flashHeight == '100%') obj.height = flashHeight;
	    else obj.height = flashHeight + "px";
}

function adfoxStart(banID, FirShowFlNum, constVisFlashFir, sdvigContent, flash1Width, flash1Height, flash2Width, flash2Height){
	if (FirShowFlNum == 1) adfoxVisibilityFlash('adfoxFlash1'+banID, flash1Width, flash1Height);
	    else if (FirShowFlNum == 2) {
		    adfoxVisibilityFlash('adfoxFlash2'+banID, flash2Width, flash2Height);
	        if (constVisFlashFir == 'yes') adfoxVisibilityFlash('adfoxFlash1'+banID, flash1Width, flash1Height);
		    if (sdvigContent == 'yes') adfoxSdvigContent(banID, flash2Width, flash2Height);
		        else adfoxSdvigContent(banID, flash1Width, flash1Height);
	}
}

function adfoxOpen(banID, constVisFlashFir, sdvigContent, flash2Width, flash2Height){
	var aEventOpenClose = new Image();
	var obj = document.getElementById("aEventOpen"+banID);
	if (obj) aEventOpenClose.src =  obj.title+'&rand='+Math.random()*1000000+'&prb='+Math.random()*1000000;
	adfoxVisibilityFlash('adfoxFlash2'+banID, flash2Width, flash2Height);
	if (constVisFlashFir != 'yes') adfoxVisibilityFlash('adfoxFlash1'+banID, 1, 1);
	if (sdvigContent == 'yes') adfoxSdvigContent(banID, flash2Width, flash2Height);
}


function adfoxClose(banID, constVisFlashFir, sdvigContent, flash1Width, flash1Height){
	var aEventOpenClose = new Image();
	var obj = document.getElementById("aEventClose"+banID);
	if (obj) aEventOpenClose.src =  obj.title+'&rand='+Math.random()*1000000+'&prb='+Math.random()*1000000;
	adfoxVisibilityFlash('adfoxFlash2'+banID, 1, 1);
	if (constVisFlashFir != 'yes') adfoxVisibilityFlash('adfoxFlash1'+banID, flash1Width, flash1Height);
	if (sdvigContent == 'yes') adfoxSdvigContent(banID, flash1Width, flash1Height);
}
;
// By Paul Irish
// http://www.paulirish.com/2009/throttled-smartresize-jquery-event-handler/

(function($,sr){

  // debouncing function from John Hann
  // http://unscriptable.com/index.php/2009/03/20/debouncing-javascript-methods/
  var debounce = function (func, threshold, execAsap) {
      var timeout;

      return function debounced () {
          var obj = this, args = arguments;
          function delayed () {
              if (!execAsap)
                  func.apply(obj, args);
              timeout = null;
          };

          if (timeout)
              clearTimeout(timeout);
          else if (execAsap)
              func.apply(obj, args);

          timeout = setTimeout(delayed, threshold || 100);
      };
  }
  // smartresize
  jQuery.fn[sr] = function(fn){  return fn ? this.bind('resize', debounce(fn)) : this.trigger(sr); };

})(jQuery,'smartresize');
(function(window, $) {

    var jqXHR = $.ajaxSettings.xhr;

    // If onprogress not supported by browser, do nothing
    if (!("onprogress" in jqXHR())) {
        return;
    }

    $.ajaxSettings.xhr = function() {
        var xhr = jqXHR();

        if (xhr instanceof window.XMLHttpRequest) {
            xhr.addEventListener('progress', this.progress, false);
        }

        if (xhr.upload) {
            xhr.upload.addEventListener('progress', this.progress, false);
        }

        return xhr;
    };

})(window, jQuery);

(function(window, $) {

    // Cache window and document jquery wrapers
    $.window = $(window);
    $.document = $(document);

})(window, jQuery);

(function(window) {

    window.app = {};

    // Utilities
    app.utils = {};

    // Store for some data
    app.storage = {};

    app.storage.Utils = {};

})(window);

(function(window) {

    var app = window.app;

    // Body

    var Module = function(root, middlewareConnections) {
        var module = root.constructor;

        module.prototype = {};

        for (var key in root) {
            if (key != 'constructor') {
                module.prototype[key] = root[key];
            }
        }

        var instance = new module();

        instance.ID = new Date().getTime();

        instance.MIDDLEWARE = new app.Middleware(instance, middlewareConnections);
    };

    Module.prototype = {};

    // Export

    app.Module = Module;

})(window);

(function(window) {

    var app = window.app;

    // Body

    var Modules = function() {
        this.store = [];
    };

    Modules.prototype = {};

    Modules.prototype.create = function(moduleObject) {
        var that = this;
        var moduleBody = moduleObject.body(app, $);

        if (moduleObject.onDocumentReady) {
            $(function() {
                that._initModule(moduleBody, moduleObject.export);
            });
        }
        else {
            that._initModule(moduleBody, moduleObject.export);
        }
    };

    Modules.prototype._initModule = function(moduleBody, moduleObjectExport) {
        var module = new app.Module(moduleBody, moduleObjectExport);

        this.store.push(module);
    };

    // Export

    app.Modules = new Modules();

})(window);

(function(window) {

    var app = window.app;

    // Body

    var Middleware = function(module, connections) {
        var that = this;

        that.addListeners = function() {
            for (var key in connections) {
                var listener = that._getMethod(module, connections[key]);

                if (listener) {
                    app.world.listen(key + '.' + module.ID, listener.bind(module));
                }
            }
        };

        that.init();
    };

    Middleware.prototype = {};

    Middleware.prototype.init = function() {
        this.addListeners();
    };

    Middleware.prototype._getMethod = function(module, methodPath) {
        var method;

        if (~methodPath.indexOf('.')) {
            var methodPathArray = methodPath.split('.');

            var potentialMethod = module[methodPathArray[0]];

            for (var i = 1, l = methodPathArray.length; i < l; i++) {
                potentialMethod = potentialMethod[methodPathArray[i]];
            }

            method = potentialMethod;
        }
        else {
            method = module[methodPath];
        }

        return method;
    };

    // Export

    app.Middleware = Middleware;

})(window);

(function(window, $) {

    var app = window.app;

    app.world = {};

    app.world.say = function(name, obj) {
        $.window.trigger(name, obj);
    };

    app.world.listen = function(nameWithNamespace, listener) {
        $.window.on(nameWithNamespace, function(e, obj) {
            listener(obj);
        });
    };

})(window, jQuery);

(function(window, $) {

    var app = window.app;

    var csrf_param, csrf_token;

    $(function() {
        csrf_param = $('meta[name=csrf-param]').attr('content');
        csrf_token = $('meta[name=csrf-token]').attr('content');
    });

    app.utils.ajax_plain = function(url, data) {
        data.url = url;

        data.dataType = data.dataType || 'html';

        // Don't send ajax if ajax method is GET or if jquery-ujs has been
        // included
        if (data.type !== 'get' && !$.rails.CSRFProtection) {
            data['data'][csrf_param] = csrf_token;
        }

        return $.ajax(data);
    };

    app.utils.ajax = function(route, route_obj, data) {
        var customMethod,
            routeParsed = app.utils.router.parse(route, route_obj),
            method = routeParsed[1],
            url = routeParsed[0];

        if (!data['data']) {
            data['data'] = {};
        }

        data['type'] = method;

        return app.utils.ajax_plain(url, data);
    };

})(window, jQuery);

(function( window, $ ) {

    var app = window.app;

    // Sandbox

    var location = window.location;

    // Body

    var Hash = function(name, options) {
        this.name = name;
        this.options = options;
        this.regExp = new RegExp('\\&' + name + '\\[([\\d,]+)\\]');
        this.regExp_full = new RegExp('^(.*\\&' + name + '\\[)([\\d,]*)(\\].*)$');
    };

    Hash.prototype.remove = function(value) {
        var arr = this.getArray();

        var i = arr.indexOf(value);
        if (i != -1) {
            arr.splice(i, 1);
        }

        this.setArray(arr);
    };

    Hash.prototype.add = function(value) {
        var arr = this.getArray();

        var i = arr.indexOf(value);
        if (i == -1) {
            arr.push(value);
        }

        this.setArray(arr);
    };

    Hash.prototype.setArray = function(array) {
        var parsed;
        var ids = array.join(this.options.array_separator);

        if (location.hash.indexOf('&' + this.name) == -1) {
            parsed = location.hash + '&' + this.name + '[' + ids + ']';
        }
        else {
            parsed = location.hash.replace(this.regExp_full, '$1' + ids + '$3');
        }

        location.hash = parsed;
    };

    Hash.prototype.getArray = function() {
        var hash = location.hash;
        var matchedHash = hash.match(this.regExp);
        var out;

        if (matchedHash) {
            out = matchedHash[1].split(this.options.array_separator);
            out = out.map(function(item) {
                return parseInt(item);
            });
        }
        else {
            out = [];
        }

        return out;
    };

    Hash.defaultOptions = {
        array_separator: ','
    };

    // Export

    app.utils.hash = function(name, userOptions) {
        var options = $.extend({}, Hash.defaultOptions, userOptions || {});

        return new Hash(name, options);
    };

})( window, jQuery );

(function( window, $ ) {

    var app = window.app;

    app.utils.key = {
        Enter: 13,
        Esc: 27,

        ArrowLeft: 37,
        ArrowTop: 38,
        ArrowRight: 39,
        ArrowDown: 40,

        Space: 32
    };

})( window, jQuery );

(function(window, $) {

    var app = window.app;

    var router = app.utils.router = {};

    router.parse = function(name, obj) {
        var route = app.routes[name];
        var isDefaultMethod = (typeof route === 'string');
        var out = (isDefaultMethod) ? route : route[0];

        for (var key in obj) {
            var value = obj[key];
            var regexp = new RegExp(':' + key + '(\\/?)', 'g');

            out = out.replace(regexp, value + '$1');
        }

        return [out, (isDefaultMethod) ? app.routes['method'] : route[1]];
    };

    router.url = function(name, obj) {
        var route = app.routes[name];

        if (typeof route !== 'string') {
            route = route[0];
        }

        for (var key in obj) {
            var value = obj[key];
            var regexp = new RegExp(':' + key + '(\\/?)', 'g');

            route = route.replace(regexp, value + '$1');
        }

        return route;
    };

})(window, jQuery);

(function( window, $ ) {

    var app = window.app;

    // Body

    var Url = function(route, routeObj) {
        this.href = app.utils.router.url(route, routeObj);
    };

    Url.prototype.open = function(target) {
        if (target == '_blank') {
            window.open(this.href, target);
        }
        else {
            window.location.href = this.href;
        }
    };

    // Export

    app.utils.url = function(route, routeObj) {
        return new Url(route, routeObj);
    };

})( window, jQuery );
(function() {
  this.app.routes = {
    mailchimp_subscriptions: ['/mailchimp_subscriptions', 'post'],
    method: 'post',
    order_print: ['/post_receipts/:tpl?:orders', 'get'],
    search: ['/search', 'get'],
    home: ['/', 'get']
  };

}).call(this);


  app.routes['admin_menu_item'] = ['/admin/menu_items/:id', ''];
  app.routes['admin_menu_items'] = ['/admin/menu_items', ''];
  app.routes['admin_root'] = ['/admin', ''];
  app.routes['admin_static_page'] = ['/admin/static_pages/:id', ''];
  app.routes['admin_static_pages'] = ['/admin/static_pages', ''];
  app.routes['cancel_user_registration'] = ['/users/cancel', ''];
  app.routes['ckeditor'] = ['', ''];
  app.routes['destroy_user_session'] = ['/users/sign_out', ''];
  app.routes['edit_user_password'] = ['/users/password/edit', ''];
  app.routes['edit_user_registration'] = ['/users/edit', ''];
  app.routes['logout'] = ['/logout', ''];
  app.routes['menu_items'] = ['/api/menu_items', ''];
  app.routes['new_user_password'] = ['/users/password/new', ''];
  app.routes['new_user_registration'] = ['/users/sign_up', ''];
  app.routes['new_user_session'] = ['/users/sign_in', ''];
  app.routes['rails_info'] = ['/rails/info', ''];
  app.routes['rails_info_properties'] = ['/rails/info/properties', ''];
  app.routes['rails_info_routes'] = ['/rails/info/routes', ''];
  app.routes['rails_mailers'] = ['/rails/mailers', ''];
  app.routes['static_page'] = ['/api/static_pages/:id', ''];
  app.routes['static_pages'] = ['/api/static_pages', ''];
  app.routes['user'] = ['/user', ''];
  app.routes['user_password'] = ['/users/password', ''];
  app.routes['user_registration'] = ['/users', ''];
  app.routes['user_session'] = ['/users/sign_in', ''];
jQuery(function() { jQuery.scrollDepth(); });
(function() {
  app.Modules.create({
    body: function(app, $) {
      return {
        constructor: function() {
          return this.init();
        },
        init: function() {
          if ($.window.width() < 768) {
            return this._colorize_article_preview_background();
          }
        },
        _colorize_article_preview_background: function() {
          var $bgs, self;
          $bgs = $([]);
          $bgs = $bgs.filter(function(i, el) {
            return i % 2;
          });
          self = this;
          return $bgs.each(function(i, el) {
            el.style.display = 'block';
            el.style.backgroundImage = '';
            return el.style.backgroundColor = self._colors[Math.floor(Math.random() * self._colors.length)];
          });
        },
        _colors: ['rgb(30, 40, 200)', 'rgb(250, 255, 0)', 'rgb(92, 255, 234)', 'rgb(255, 110, 110)', 'rgb(198, 198, 198)', 'rgb(254, 224, 120)', 'rgb(255, 0, 120)', 'rgb(255, 80, 80)', 'rgb(200, 100, 240)', 'rgb(55, 255, 160)', 'rgb(15, 190, 230)', 'rgb(255, 0, 0)', 'rgb(0, 100, 255)']
      };
    },
    onDocumentReady: true
  });

}).call(this);
(function() {
  app.Modules.create({
    body: function(app, $) {
      return {
        constructor: function() {
          return this.init();
        },
        init: function() {
          var handler_mouseenter, handler_mouseleave, self;
          self = this;
          handler_mouseenter = function() {
            return self._set_reverse_color(this);
          };
          handler_mouseleave = function() {
            return self._set_usual_color(this);
          };
          return $.document.on('mouseenter', this._selector, handler_mouseenter).on('mouseleave', this._selector, handler_mouseleave);
        },
        _update_style: function($block, style) {
          return $block.find('.article-preview__title span').attr('style', style);
        },
        _set_reverse_color: function(element) {
          return this._set_color(element, true);
        },
        _set_usual_color: function(element) {
          return this._set_color(element);
        },
        _set_color: function(element, reverse) {
          var $block, colors, style;
          $block = $(element).closest('.article-preview');
          colors = this._colors($block, reverse);
          style = this._style(colors);
          return this._update_style($block, style);
        },
        _style: function(colors) {
          var style;
          style = '';
          style = style + ("background-color: " + colors[1] + "; box-shadow: -10px 0 0 " + colors[1] + ", 10px 0 0 " + colors[1] + ";");
          return style = style + ("color: " + colors[0] + ";");
        },
        _colors: function($block, reverse) {
          var bg_color, colors, default_title_color, title_color;
          default_title_color = $block.is('.article-preview--fluid') ? '#fff' : '#000';
          bg_color = $block.attr('data-format_title_background_color');
          title_color = $block.attr('data-format_title_color') || default_title_color;
          if (bg_color === title_color) {
            title_color = '#000';
          }
          colors = [title_color, bg_color];
          if (reverse) {
            colors = colors.reverse();
          }
          return colors;
        },
        _selector: '.article-preview[data-format_title_color] a.article-preview__body, .article-preview[data-format_title_color] a.js-biglinks'
      };
    }
  });

}).call(this);
(function() {
  app.Modules.create({
    body: function(app, $) {
      return {
        constructor: function() {
          return this.init();
        },
        init: function() {
          if ($('.articles-list__footer').length === 2) {
            return $('.articles-list__footer').first().hide();
          }
        }
      };
    },
    onDocumentReady: true
  });

}).call(this);
(function() {
  app.Modules.create({
    body: function(app, $) {
      return {
        constructor: function() {
          return this.init();
        },
        init: function() {
          var $asides, $banner, $text, delta, iterator, minimum_top, startShifting, updateMinimumTop;
          $banner = $('.banner-wrapper--aside');
          if (!$banner.size()) {
            return false;
          }
          $text = $banner.parent();
          $asides = this._remove_blank_asides($text.find('aside:not(.banner-wrapper--aside)'));
          delta = 40;
          minimum_top = 0;
          updateMinimumTop = function() {
            return minimum_top = $banner.get(0).offsetTop + 420 + delta;
          };
          iterator = function(i, elem) {
            var $aside, shift, style, style_translate;
            $aside = $(elem);
            if (elem.offsetTop < minimum_top) {
              shift = minimum_top - elem.offsetTop;
              style_translate = "translateY(" + shift + "px)";
              style = {
                transform: style_translate,
                MozTransform: style_translate,
                WebkitTransform: style_translate
              };
              $aside.css(style);
              return minimum_top = minimum_top + $aside.outerHeight(true);
            }
          };
          startShifting = function() {
            updateMinimumTop();
            return $asides.each(iterator);
          };
          return $.window.on('resize', startShifting);
        },
        _remove_blank_asides: function($elements) {
          var iterator;
          iterator = function(elem) {
            if ($.trim(elem.innerText).length > 0) {
              return true;
            }
          };
          return $($elements.toArray().filter(iterator));
        }
      };
    },
    onDocumentReady: true
  });

}).call(this);
(function() {
  app.Modules.create({
    body: function(app, $) {
      return {
        constructor: function() {
          return this.init();
        },
        init: function() {
          return $.ajax({
            url: 'http://kyky.org/supers.json',
            jsonp: "callback",
            dataType: "jsonp",
            data: {
              format: 'json'
            },
            success: function(data) {
              var i, j, results, selector;
              results = [];
              for (i = j = 0; j < 4; i = ++j) {
                selector = '.inner__' + (i + 1) + ' ';
                if (i !== 0) {
                  $(selector + '.friends__kyky__topic').text(data.objects[i].category);
                }
                $(selector + '.friends__kyky__image img').attr('src', data.objects[i].photo);
                $(selector + '.friends__kyky__title').text(data.objects[i].title);
                results.push($(selector + 'a').attr('href', data.objects[i].url));
              }
              return results;
            }
          });
        }
      };
    },
    onDocumentReady: true
  });

}).call(this);
(function() {
  app.Modules.create({
    body: function(app, $) {
      return {
        constructor: function() {
          return this.init();
        },
        init: function() {
          var $elems;
          $elems = $('.disqus-comment-count');
          if ($elems.size()) {
            return this.load();
          }
        },
        load: function() {
          var dsq, element;
          window.disqus_shortname = 'rfrm-io';
          dsq = document.createElement('script');
          dsq.type = 'text/javascript';
          dsq.async = true;
          dsq.src = '//rfrm-io.disqus.com/count.js';
          element = document.getElementsByTagName('head')[0] || document.getElementsByTagName('body')[0];
          return element.appendChild(dsq);
        }
      };
    },
    onDocumentReady: true
  });

}).call(this);
(function() {
  app.Modules.create({
    body: function(app, $) {
      return {
        constructor: function() {
          return this.init();
        },
        init: function() {
          this.$root = $('.comments');
          this.opened_name = '-opened';
          this.loaded_name = '-loaded';
          this.handlers();
          return this._fix_parallax();
        },
        handlers: function() {
          var self, trigger;
          self = this;
          trigger = function(e) {
            e.preventDefault();
            return self.toggle(this);
          };
          return $.document.on('click', '.js-comments__trigger', trigger);
        },
        toggle: function(trigger) {
          if (this._is_opened()) {
            return this.close();
          } else {
            return this.open();
          }
        },
        close: function() {
          this.$root.removeClass(this.opened_name);
          return $.window.trigger('resize');
        },
        open: function() {
          var check_loading, self, timer;
          timer = null;
          self = this;
          check_loading = function() {
            var minimal_height;
            minimal_height = 100;
            if (self.$root.find('iframe').height() > minimal_height) {
              clearTimeout(timer);
              return $.window.trigger('resize');
            } else {
              return timer = setTimeout(check_loading, 100);
            }
          };
          if (!this._is_loaded()) {
            this.load_disqus();
            check_loading();
          }
          this.$root.addClass(this.opened_name);
          return $.window.trigger('resize');
        },
        load_disqus: function() {
          var dsq, element;
          dsq = document.createElement('script');
          dsq.type = 'text/javascript';
          dsq.async = true;
          dsq.src = '//rfrm-io.disqus.com/embed.js';
          element = document.getElementsByTagName('head')[0] || document.getElementsByTagName('body')[0];
          element.appendChild(dsq);
          return this.$root.addClass(this.loaded_name);
        },
        _fix_parallax: function() {
          var $element, callback, checkChanges, prevHeight, tiemer;
          $element = $.document;
          prevHeight = $element.height();
          callback = function() {
            return $.window.trigger('resize');
          };
          checkChanges = function() {
            var currentHeight;
            currentHeight = $element.height();
            if (currentHeight !== prevHeight) {
              callback();
              return prevHeight = currentHeight;
            }
          };
          return tiemer = setInterval(checkChanges, 300);
        },
        _is_opened: function() {
          return this.$root.hasClass(this.opened_name);
        },
        _is_loaded: function() {
          return this.$root.hasClass(this.loaded_name);
        }
      };
    },
    onDocumentReady: true
  });

}).call(this);
(function() {
  app.Modules.create({
    body: function(app, $) {
      return {
        constructor: function() {
          return this.init();
        },
        init: function() {
          var that;
          that = this;
          $('.js-likely').each(this.eachLikely.bind(this));
          return $(window).load(function() {
            return $.each(['facebook', 'vkontakte', 'twitter'], function(index, service) {
              var selector;
              selector = '.likely__widget_' + service;
              return $(selector).each(function() {
                var number;
                number = $(this).children('.likely__counter_' + service).text();
                if (!number) {
                  number = 0;
                }
                return $(this).attr('data-count', number);
              });
            });
          });
        },
        eachLikely: function(index, element) {
          var $likely, $widgets, counter, sum, that, widget_loaded;
          that = this;
          $likely = $(element);
          $widgets = $likely.children();
          counter = 0;
          widget_loaded = 0;
          sum = function(number) {
            counter = counter + number;
            if (++widget_loaded) {
              return that.loaded($likely, counter);
            }
          };
          return $widgets.on('counter.likely', function(event, service, number) {
            sum(number);
            return $('.' + service).attr('data-count', number);
          });
        },
        loaded: function($likely, counter) {
          var $article, ajaxData, content_id, content_type;
          $article = $('article').first();
          content_id = $article.attr('data-id') || -1;
          content_type = $article.attr('data-route-name');
          ajaxData = {
            data: {
              value: counter
            }
          };
          return app.utils.ajax(content_type, {
            id: content_id
          }, ajaxData);
        }
      };
    },
    onDocumentReady: true
  });

}).call(this);
(function() {
  app.Modules.create({
    body: function(app, $) {
      return {
        constructor: function() {
          return this.init();
        },
        init: function() {
          if ($.window.width() < 768) {
            return this._update_list();
          }
        },
        _update_list: function() {
          var $list, $listItemWidth, child_iterator;
          $list = $('.list-small__list');
          $listItemWidth = $('.list-small__list__item');
          $listItemWidth.width($.window.width() - 20);
          child_iterator = function(n, list) {
            return $(list).each(function(n, item) {
              if (n >= 3) {
                return $(item).remove();
              }
            });
          };
          $list.each(child_iterator);
          $list.css('width', $listItemWidth.width());
          return $list.slick(this._slick_params);
        },
        _slick_params: {
          arrows: false,
          draggable: true,
          focusOnSelect: true,
          infinite: false,
          slidesToScroll: 1
        }
      };
    },
    onDocumentReady: true
  });

}).call(this);
(function() {
  app.Modules.create({
    body: function(app, $) {
      return {
        constructor: function() {
          return this.init();
        },
        init: function() {
          var facebook, i, item, len, ref, twitter, vk;
          facebook = {
            name: 'facebook',
            width: 600,
            height: 600
          };
          twitter = {
            name: 'twitter',
            width: 626,
            height: 438
          };
          vk = {
            name: 'vkontakte',
            width: 600,
            height: 600
          };
          ref = [facebook, twitter, vk];
          for (i = 0, len = ref.length; i < len; i++) {
            item = ref[i];
            this.handle(item);
          }
          [facebook, twitter, vk].forEach(this.set_reposts);
          this.check_position;
          $(window).scroll(this.check_position);
          return $(window).resize(this.check_position);
        },
        handle: function(social) {
          return $('.socicons.repost .' + social.name).on('click', function(e) {
            e.preventDefault();
            window.open(this.href, 'social-popup', 'menubar=no,toolbar=no,resizable=yes,scrollbars=yes,left=370,top=115,width=' + social.width + ',height=' + social.height);
            return false;
          });
        },
        set_reposts: function() {
          return $('.js-likely').children().on('counter.likely', function(event, service, number) {
            return $('.postpage .socicons.repost.-black .' + service).attr('data-count', number);
          });
        },
        check_position: function() {
          var $el, el_height, el_top_offset, post_height, post_top_offset, window_top_offset;
          if (!$('.article').length) {
            return;
          }
          $el = $('.article__reposts .socicons');
          el_height = $el.height() + 30;
          el_top_offset = 200;
          post_top_offset = $('.article__content').offset().top;
          post_height = $('.article__section').height() + $('.article__section').position().top;
          window_top_offset = $(window).scrollTop();
          $el.removeClass('fixed absolute');
          if (post_height - window_top_offset <= el_top_offset + el_height) {
            return $el.addClass('absolute');
          } else if (post_top_offset - window_top_offset <= el_top_offset) {
            return $el.addClass('fixed');
          }
        }
      };
    },
    onDocumentReady: true
  });

}).call(this);
/*

Libs











Libs Plugins





Application









*/

;
